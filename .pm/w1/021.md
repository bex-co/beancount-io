# Tolerated ledger warnings corrupt subsequent JSON error output

**Minor · 25–40 minutes.** CLI-QA-009.

Use this synthetic main.bean:
```beancount
option "operating_currency" "USD"
2026-01-01 open Assets:Checking USD
2026-01-01 open Assets:Savings USD
2026-01-01 open Equity:Opening USD
2026-02-01 * "Opening"
  Assets:Checking 100 USD
  Assets:Savings 200 USD
  Equity:Opening -300 USD
```

Append `2026-03-01 balance Assets:Checking 999 USD`, then run:
```sh
uv run bea --json --file /absolute/qa/main.bean query 'SELECT does_not_exist' --allow-errors
```
Actual: exit 2 with a raw balance-warning line followed by an error JSON object
on stderr. Parsing the complete stderr as JSON fails. The same query on a valid
ledger produces one JSON error object. Successful partial reads have valid
stdout JSON; existing stderr warnings on success are not the reported defect.

Cause: output.render_ledger_errors prints tolerated warnings immediately;
main's later error boundary emits its JSON object after them. Delay/consolidate
diagnostics for JSON failures so the error remains one parseable object and
retains the loader warnings as structured details. Do not discard warnings or
weaken strict reads. Earlier report invalid-period failures share this path.
Acceptance: failing query/report after allowed loader errors produces one
structured error with both diagnostics and correct exit code; valid-ledger
errors and successful partial results retain their contracts.
Dedupe: full-board JSON-warning search found no matching CLI item; output/error
boundary history retains immediate warning emission. Repeated earlier repros
and current fresh-process capture establish the failure.

Revalidated 2026-09-10, HEAD 4574a7d8, checkout uv run bea on macOS arm64.
Synthetic files, isolated child config/cache, closed stdin, no credentials or
production calls. Run commands from cli/. A1/A2: predictable commands and
machine-readable accounting results. No backend change needed.
