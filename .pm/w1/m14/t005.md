---
id: w1/m14/t005
title: Test coverage
worker: worker1
status: todo
estimate: 45m
depends_on: [w1/m14/t003]
---

## Objective

Pin the `import-id` shapes, cross-tool dedup, legacy-key matching, and append-only diffs.

## Context

- `cli/tests/test_import.py` covers preview, duplicates, and apply; `cli/tests/test_ledger_safety.py` covers write safety.
- The skill's dedup.md worked example (`2026-05-07|-54.20|TRADER JOES #123 SEATTLE WA|Assets:Bank:Checking`) can serve as a golden hash input.

## Steps

1. Assert the exact `import-id` values written for a native-ID row and a hash row, including the occurrence suffix for identical same-day rows.
2. Assert a hand-written skill-style entry is skipped as an exact duplicate, and a `bea_import_id` entry still matches.
3. Assert appending to two- and four-space files changes only added lines (compare unified diff line counts).

## Files

- `cli/tests/test_import.py`, `cli/tests/test_ledger_safety.py`, `cli/tests/fixtures/`

## Acceptance criteria

- [ ] Reverting t001 or t002 fails at least one new test.
- [ ] `make test` passes.

## Out of scope

- Fuzzy-duplicate heuristics.
