---
id: w1/m14/t003
title: Adoption surface
worker: worker1
status: todo
estimate: 20m
depends_on: [w1/m14/t001, w1/m14/t002]
---

## Objective

Confirm the convention and the append-only rule are documented wherever a user or agent would look.

## Context

- Docs: `cli/docs/IMPORTING.md` ("Duplicate decisions"), `cli/README.md` ("Import bank exports", "Write safely"), `cli/docs/USAGE.md`.
- Agent-facing: `skills/CLAUDE.md` names the `import-id` convention as a shared trust rail; it should now say the CLI follows it.

## Steps

1. Run the IMPORTING.md walkthrough as written and read the resulting ledger.
2. Update `skills/CLAUDE.md` wording; run `python3 scripts/check-agent-guidance.py`.
3. Check the root `README.md` CLI paragraph needs no change.

## Files

- `cli/docs/IMPORTING.md`, `cli/README.md`, `cli/docs/USAGE.md`, `skills/CLAUDE.md`

## Acceptance criteria

- [ ] Every documented example runs as written and the written metadata matches the docs.
- [ ] Guidance check passes.

## Out of scope

- Skill behavior changes (m18).
