---
id: w1/m12/t002
title: Add missing REST endpoints (CLI auth-session flow) to backend-v2
worker: worker1
status: todo
estimate: 60m
depends_on: [w1/m12/t001]
---

## Objective

Close every gap from t001's list by adding the missing REST endpoints to backend-v2 — expected: the CLI auth-session create/get/consume flow — so the v1 OpenAPI spec covers 100% of the CLI's hosted surface, with matching annotations (summary, description, examples) from day one.

## Context

- Backend package: `backend-cluster/backend-v2/` (GraphQL/REST/MCP with a surface-parity zero-debt gate).
- The parity suite and `yarn generate-v1-openapi` snapshot check run in `.github/workflows/ci-backend-parity.yml`.
- New endpoints must carry the annotation discipline t007 will enforce: `summary`, `description`, examples, and `x-cli-destructive` where applicable.

## Steps

1. Implement each gap-list endpoint as a REST adapter over the existing operations (no new business logic — same op class the GraphQL mutations use).
2. Annotate fully: summary/description/examples per operation, request/response schemas with constraints.
3. Regenerate the v1 OpenAPI snapshot (`yarn generate-v1-openapi`) and run `yarn typecheck` + `yarn test` in backend-v2 — parity gate must stay green.

## Files

- `backend-cluster/backend-v2/` (REST adapters, op wiring)
- `backend-cluster/idl/` (regenerated spec snapshot)

## Acceptance criteria

- [ ] Every t001 gap-list entry exists in the regenerated v1 spec with full annotations.
- [ ] backend-v2 `yarn typecheck` and `yarn test` (parity + op-class coverage) pass.

## Out of scope

- Any CLI-side change (t003+).
- Retiring the GraphQL auth-session mutations from the backend — other clients may use them.
