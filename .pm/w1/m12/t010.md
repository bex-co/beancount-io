---
id: w1/m12/t010
title: Test coverage
worker: worker1
status: todo
estimate: 60m
depends_on: [w1/m12/t008]
---

## Objective

Add meaningful tests for the behavior this milestone shipped: contract pinning, error semantics, the annotation gate, and destructive-op confirmation. Tests must assert real behavior and failure modes — no tautologies, no snapshot-everything.

## Context

- Standing closing task.
- Existing contract-test precedent: `cli/tests/test_contract.py`; fixtures in `cli/tests/conftest.py`. Mock at the HTTP layer (e.g. respx/httpx transport), not by stubbing the generated client's methods.

## Steps

1. Test the exit-code mapping: mocked 401/404/422/500 responses produce the documented exit codes and messages (401 mentions `bea cloud login`).
2. Test the auth-session flow logic (create → poll → consume) against mocked responses, including the expired-session path.
3. Test the stub generator's gate: a fixture spec missing a `description` fails generation naming the operation; a valid fixture generates a stub whose `--help` carries the spec's summary.
4. Test `bea cloud ledger delete` prompts and that `--yes` bypasses; test drift detection: a mutated pin makes `make spec-check` fail.

## Files

- `cli/tests/` (new/extended test modules, fixture specs)

## Acceptance criteria

- [ ] Each listed behavior has a test that fails if the behavior regresses.
- [ ] `make check-all` green.

## Out of scope

- Live-server integration tests in CI (manual verification happened in t005).
