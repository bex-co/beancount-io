---
id: w1/m25/t006
title: Adoption surface
worker: worker1
status: done
estimate: 25m
depends_on: [w1/m25/t001, w1/m25/t002, w1/m25/t003, w1/m25/t004, w1/m25/t005]
---

## Objective

Verify the automation contract is documented as shipped: the JSON envelope's failure shape, the partial-effects field, the `--json` composition rule, and the empty-value rule.

## Context

- Root `CLAUDE.md` compatibility rules: every `AGENTS.md` is a relative symlink to its `CLAUDE.md`, and root `.claude/skills` links to `../.agents/skills`.
- Customer skill catalog: `skills/CLAUDE.md`; customer skills live in `skills/.claude/skills/beancount-*`.
- `cli/docs/REFERENCE.md` is generated — run `make docs`, never hand-edit.

## Steps

1. Update the JSON-envelope and exit-code sections of `cli/docs/USAGE.md` with the failure shape, the files-written field, and the `--json` plus `--format` rule.
2. Run `make docs` and confirm `make docs-check` is green.
3. Check the `beancount-ask` skill, which parses `bea --json query` output, against the shipped envelope.
4. Run `python3 skills/scripts/ci-check.py` and `python3 scripts/check-agent-guidance.py`.

## Files

- cli/docs/USAGE.md
- cli/docs/REFERENCE.md
- skills/.claude/skills/beancount-ask/
- skills/CLAUDE.md

## Acceptance criteria

- [ ] The usage guide documents the failure envelope, the partial-effects field, and the composition and empty-value rules.
- [ ] `make docs-check` passes and the documented examples run in CI.
- [ ] `beancount-ask` parses the shipped envelope for both Claude Code and Codex; the skills and agent-guidance checks pass.

## Out of scope

- Rewriting unrelated documentation sections.
- Shipping a release; this milestone lands on `main` only.
