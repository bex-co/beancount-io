---
id: w1/m25/t009
title: Closeout
worker: worker1
status: todo
estimate: 15m
depends_on: [w1/m25/t008]
---

## Objective

Close w1/m25 once every other task is complete **and** its definition of done actually holds. Completing this task is the move into `done/`.

## Context

- Canonical closeout procedure: `.agents/skills/pm/SKILL.md`, the `done` subcommand.
- A milestone lands in `done/` when its observable end state is real, not when the code is merely written.

## Steps

1. Re-read the `## Definition of done` in `w1/m25/README.md` and verify each bullet against `main`.
2. Set every remaining task's `status: done` and move each `tNNN.md` into `w1/m25/done/`.
3. Mark every row `— **DONE**`, set `**Status:** done`, move the milestone to `.pm/w1/done/m25/`, and check the box in `.pm/w1/README.md`.
4. Close each absorbed inbox note with `/pm done`, citing the shipping commit.

## Files

- .pm/w1/m25/
- .pm/w1/README.md

## Acceptance criteria

- [ ] Every definition-of-done bullet is verified against `main`, not assumed.
- [ ] `.pm/w1/m25/` no longer exists; the milestone is only at `.pm/w1/done/m25/` with `**Status:** done`.
- [ ] The workstream checkbox is `- [x]` and every absorbed note is closed or explicitly carried forward.

## Out of scope

- Closing while any definition-of-done bullet is unmet — block the milestone instead.
