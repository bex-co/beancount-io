---
id: w1/m13/t005
title: Adoption surface
worker: worker1
status: todo
estimate: 20m
depends_on: [w1/m13/t004]
---

## Objective

Confirm the new read behavior is discoverable and correct on every surface it exposes.

## Context

- Surfaces: `bea --help` (the `--strict` flag), `bea report … --help`, `cli/README.md`, `cli/docs/USAGE.md`, and the root `README.md` CLI section.
- Agent-facing: the `beancount-ask` skill documents `bea --file <ledger> query`; its guidance must not assume refusal in a terminal.

## Steps

1. Run the README quick start and the USAGE report examples as written on a fresh ledger and on `bean-example`.
2. Check `skills/.claude/skills/beancount-ask/SKILL.md` for wording that contradicts the policy; file drift as an inbox note rather than fixing here.
3. Run `python3 scripts/check-agent-guidance.py` if any instruction file changed.

## Files

- `cli/README.md`, `cli/docs/USAGE.md`, `README.md`

## Acceptance criteria

- [ ] Every command in the edited docs runs as written.
- [ ] Any skill drift is recorded on the board.

## Out of scope

- Editing skills (m18).
