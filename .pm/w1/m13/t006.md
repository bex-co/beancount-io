---
id: w1/m13/t006
title: Simplify
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m13/t005]
---

## Objective

Run `/simplify` over the code this milestone changed; behavior-preserving cleanup only.

## Context

- Touched: `cli/src/cli/output.py`, `cli/src/cli/context.py`, `cli/src/cli/commands/report.py`, `cli/src/cli/main.py`.

## Steps

1. `/simplify` on the diff since the milestone started.
2. Re-run `make check-all` from `cli/`.

## Files

- As listed in Context.

## Acceptance criteria

- [ ] No behavior change; tests from t007 still pass.

## Out of scope

- New features.
