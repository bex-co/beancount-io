---
id: w1/m21/t001
title: "Adapt ledger initialization to the single bea installation"
worker: worker1
status: todo
estimate: 45m
depends_on: [w1/m20/t008]
---

## Objective

Update the completed m18 onboarding design for ADR014's independent engine without requiring another accounting CLI installation.

## Context

- m18 remains completed history; this milestone is its explicit ADR014 follow-up.
- beancount-init currently always runs uv add fava beancount even when bea is available. A requested Fava browser workflow has its own legitimate runtime needs.

## Steps

1. Make the bea-ledger path create and validate the existing starter through bea without separately installing Beancount or configuring the engine's private paths.
2. Keep explicitly requested Fava web setup as a separate supported workflow with its own dependencies; do not make it a prerequisite for bea ledger operations.
3. Preserve the starter template, opening balance, and no-bea fallback. Document initial engine provisioning and supported bea recovery.

## Files

- skills/.claude/skills/beancount-init/SKILL.md; its evals and fixtures
- skills/CLAUDE.md

## Acceptance criteria

- [ ] A user with only bea can create/check the starter ledger through both agents without a second Beancount install step.
- [ ] Requested Fava setup and the no-bea fallback remain usable and clearly scoped; template behavior is preserved.

## Out of scope

- Adding a bea web server, deleting the Fava workflow, or changing account/dedup conventions.
