# CSV import rejects documented whitespace-trimmed headers

**Minor · 30–45 minutes.** CLI-QA-001.

Create main.bean:
```beancount
option "operating_currency" "USD"
2026-01-01 open Assets:Cash USD
2026-01-01 open Expenses:Uncategorized USD
```
Save rows.csv:
```csv
 Date , Description , Amount 
2026-02-15,Shop,-12.34
```
```sh
uv run bea --json --file /absolute/qa/main.bean import /absolute/qa/rows.csv --csv auto --account Assets:Cash --apply
```
Twice on fresh files: exit 2, "Row 2: the mapping names column 'Date' for date,
which the CSV lacks." Ledger unchanged. Unique unpadded headers work; earlier
explicit-mapping and BOM controls establish the scope. IMPORTING.md promises
header stripping, so this is supported input.

Cause: csv_mapper.read_header strips names but extraction's DictReader retains
raw keys; _cell cannot find the normalized name. Date inference also uses raw
keys. Normalize consistently across discovery/inference/extraction and reject
ambiguous normalized names. Regression: padded/BOM headers import exact amounts;
unique headers still work and normalized collisions fail before writes.
Dedupe: full-board searches and current open milestone review found no matching
CLI issue; completed w1/m16 introduced the mapper. Recent history 24204071 and
aa5bd335 retains the mismatch. Coordinate normalization with w1/015 without
combining the distinct failure evidence.

Verified 2026-09-10 at `4574a7d8b5a91641e7bc4aee436c706c0ec7fef1`, checkout
`uv run bea`, macOS arm64, isolated child config/cache, closed stdin and no token.
All files are synthetic. Run from cli/ with absolute fixture paths. A1/A2:
automated bookkeeping must preserve imported values and typed results.
No production access or backend change is required.
