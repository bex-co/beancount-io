The `beancount-ask` skill lists CLI reads for answering ledger questions but
predates the m15 ergonomics: `bea balance --json` (pruned trial-balance
subtrees, same tree shape as `report trial-balance`) and `list transaction
--search/--tag/--link` belong in its tool list. Pick up in m18 (skills
converge on `bea`), which already owns skill edits — do not fix here.
