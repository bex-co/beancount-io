---
id: w1/m29/t009
title: Test coverage
worker: worker1
status: todo
estimate: 45m
depends_on: [w1/m29/t007, w1/m29/t008]
---

## Objective

Pin the failure properties, because the ones that matter here are all failure properties.

## Context

- Tests must assert real behavior and failure modes; never tautological or snapshot-everything.
- CLI tests live in `cli/tests/` and run under pytest-xdist, so every test owns its own state.

## Steps

1. Identify the behavior this milestone shipped that is not yet pinned by a test.
2. Add the missing cases, including the failure modes and the exact exit codes or payloads.
3. Confirm each new test fails against the pre-milestone behavior.

## Files

- `cli/tests/` (or the owning package's test directory)

## Acceptance criteria

- [ ] Tests assert that a validation failure, timeout, non-2xx status, or empty body never replaces or deletes the last good revision.
- [ ] Tests assert redirect refusal, the origin and alias policy, the per-load URL cap, and the body size cap.
- [ ] Tests assert ledger-authored precedence, read-only refusal of writes to a virtual entry, and that the customer's files are never rewritten.
- [ ] `cd cli && make check-all` passes.

## Out of scope

- Coverage-percentage targets; the bar is meaningful assertions, not a number.
