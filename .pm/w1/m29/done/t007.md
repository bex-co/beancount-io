---
id: w1/m29/t007
title: Adoption surface
worker: worker1
status: done
estimate: 25m
depends_on: [w1/m29/t004, w1/m29/t005, w1/m29/t006]
---

## Objective

Verify the one-line setup is documented where a customer and an agent will find it, and that price guidance in the skills matches the shipped behavior.

## Context

- Root `CLAUDE.md` compatibility rules: every `AGENTS.md` is a relative symlink to its `CLAUDE.md`, and root `.claude/skills` links to `../.agents/skills`.
- Customer skill catalog: `skills/CLAUDE.md`; customer skills live in `skills/.claude/skills/beancount-*`.
- `cli/docs/REFERENCE.md` is generated — run `make docs`, never hand-edit.

## Steps

1. Document the managed include in `cli/README.md` and `cli/docs/USAGE.md`: the one-line setup, the offline and strict modes, the status vocabulary, and the portable export.
2. Make the distinction PRFAQ002 section 16 asks for explicit: a managed URL include versus an upstream-compatible local include.
3. Run `make docs` and confirm `make docs-check` is green for the new subcommands.
4. Update price-related guidance in the `beancount-*` skills, then run `python3 skills/scripts/ci-check.py` and `python3 scripts/check-agent-guidance.py`.

## Files

- cli/README.md
- cli/docs/USAGE.md
- cli/docs/REFERENCE.md
- skills/CLAUDE.md
- docs/prfaqs/PRFAQ002-include-live-price.md

## Acceptance criteria

- [ ] The README and usage guide describe the one-line setup, offline and strict modes, status, and export, and every fenced example runs in CI.
- [ ] The managed-versus-local include distinction is explicit in the customer documentation and the skill guidance.
- [ ] `make docs-check`, `python3 skills/scripts/ci-check.py`, and `python3 scripts/check-agent-guidance.py` pass for both Claude Code and Codex.

## Out of scope

- Rewriting unrelated documentation sections.
- Shipping a release; this milestone lands on `main` only.
