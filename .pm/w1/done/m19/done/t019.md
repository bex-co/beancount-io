---
id: w1/m19/t019
title: "Move validated directive writes to the helper"
worker: worker1
status: done
estimate: 60m
depends_on: [w1/m19/t018]
---

## Objective

Keep existing add and ledger-write guarantees while executing engine-dependent work in the helper.

## Context

- directives/writer.py, commands/add.py, and ledger_write.py build and validate Beancount objects.
- Previous milestones established full-ledger validation, append-only alignment, and import-id deduplication.

## Steps

1. Move directive formatting, validation, and actual writes into complete helper operations used by every add variant and bulk transaction input.
2. Preserve --pad-from behavior, include routing, metadata, append-only alignment, and failure atomicity.
3. Keep preview/confirmation in bea and make helper failures leave ledger bytes unchanged.

## Files

- cli/src/cli/directives/writer.py; cli/src/cli/commands/add.py; cli/src/cli/ledger_write.py
- engine helper; cli/tests/

## Acceptance criteria

- [ ] All add variants and bulk writes preserve existing results and filesystem effects.
- [ ] Invalid writes do not change files, exact amounts survive the process boundary, and the frontend imports no engine types.

## Out of scope

- Changing accounting semantics, bypassing confirmations, or passing engine objects across processes.
