---
id: w1/m19/t021
title: "Move init and import accounting operations to the helper"
worker: worker1
status: done
estimate: 60m
depends_on: [w1/m19/t019]
---

## Objective

Preserve starter-ledger creation and import review/application while removing their frontend engine imports.

## Context

- commands/init.py, commands/import_.py, csv_mapper.py, and shared ledger writes depend on Beancount.
- Beangulp remains optional under ADR012; default CSV mapping must work without it.

## Steps

1. Move engine-dependent normalization, CSV-to-entry conversion, deduplication, validation, and writes to complete helper operations.
2. Run configured importer execution in the selected engine environment; keep frontend column/rule input, preview, and apply decisions.
3. Verify the existing starter output, metadata/import-id convention, duplicate choices, and no-write previews.

## Files

- cli/src/cli/commands/init.py; cli/src/cli/commands/import_.py; cli/src/cli/csv_mapper.py
- engine helper; cli/tests/

## Acceptance criteria

- [ ] Default init and CSV import work with one bea installation and no Beangulp extra.
- [ ] Preview, duplicate review, and validated application retain existing results/file effects without frontend engine loading.

## Out of scope

- New bank importers, changing the starter template, or enabling optional dependencies by default.
