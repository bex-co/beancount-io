---
id: w1/m16/t007
title: Test coverage
worker: worker1
status: done
estimate: 60m
depends_on: [w1/m16/t005]
---

## Objective

Pin mapping parsing, sign conventions, date formats, rules matching, the flagged queue, remembered mappings, and error exits.

## Context

- `cli/tests/test_import.py` has fixtures for the example CSV and the preview/apply flow.

## Steps

1. Mapping: signed amount, debit/credit pair, `sign=bank`, ISO and explicit date formats, missing required field exits 2, bad row exits 2 with line and column.
2. Rules: first-match order, case-insensitivity, narration fallback, unmatched default with `!`, invalid regex exits 2.
3. Memory: header-signature reuse, different header does not reuse, explicit flags win.
4. Dedup: a `--csv` import writes the m14 `import-id` shape and re-import skips every row.

## Files

- `cli/tests/test_import.py`, `cli/tests/fixtures/`

## Acceptance criteria

- [ ] Reverting t001, t002, or t003 individually fails at least one new test.
- [ ] `make test` passes.

## Out of scope

- Property-based testing of CSV dialects.
