---
id: w1/m16/t005
title: Adoption surface
worker: worker1
status: done
estimate: 20m
depends_on: [w1/m16/t004]
---

## Objective

Confirm the no-code import is discoverable and works exactly as documented on every surface.

## Context

- Surfaces: `bea import --help`, `cli/docs/IMPORTING.md`, `cli/README.md`, root `README.md` CLI paragraph, `docs/adrs/`.
- Agent-facing: `skills/.claude/skills/beancount-import/SKILL.md` and `beancount-importer-author/SKILL.md` describe when to write an importer; record the drift for m18 rather than editing here.

## Steps

1. Run the IMPORTING.md walkthrough as written from a clean directory with only `bea` installed from source.
2. Check `bea import --help` shows `--csv`, `--account`, `--rules`, `--default-account`, `--date-format` with one-line help each.
3. File skill drift as an inbox note.

## Files

- `cli/docs/IMPORTING.md`, `cli/README.md`, `README.md`

## Acceptance criteria

- [ ] Walkthrough passes as written.
- [ ] Drift recorded on the board.

## Out of scope

- Skill edits (m18).
