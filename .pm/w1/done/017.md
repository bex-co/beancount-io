# BQL JSON drops acquisition dates and labels from cost lots

**Minor · 25–40 minutes.** CLI-QA-011.

Create main.bean:
```beancount
2026-01-01 open Assets:Stock AAPL
2026-01-01 open Assets:Cash USD
2026-02-01 * "First"
  Assets:Stock 1 AAPL {100 USD, "first"}
  Assets:Cash -100 USD
2026-02-02 * "Second"
  Assets:Stock 1 AAPL {100 USD, "second"}
  Assets:Cash -100 USD
```
```sh
uv run bea --json --file /absolute/qa/main.bean query "SELECT sum(position) WHERE account = 'Assets:Stock'"
uv run bea --json --file /absolute/qa/main.bean list transaction
```
Query exits 0 and returns two identical objects: units 1 AAPL, cost only number
100/currency USD. Both acquisition dates and labels disappear. Listing retains
2026-02-01/first and 2026-02-02/second. Earlier repeated Position and Inventory
queries plus explicit cost_date queries establish the same loss. Files unchanged.
Expected: typed Position/Inventory results retain Cost fields needed to identify
lots; plain Amount results keep their current shape.

Cause: output.jsonable at :151–154 treats every number/currency object as Amount
before reaching _asdict. Cost also has date/label. Distinguish Cost and retain
its fields recursively; preserve exact decimals and ISO dates. Regression:
Position and Inventory retain both distinct lots, ordinary Amount and null cost
remain compatible. Raw query results are runtime-confirmed; other shared output
callers need targeted coverage, not assumed impact. Human table abbreviation
is an upstream presentation choice and is outside this finding.
Dedupe: current full-board serialization search found no matching task. w1/011
is cost-label string corruption on writes, a different root. Original amount
conversion traces to 00a0ce22; current implementation still drops Cost fields.

Verified 2026-09-10 at `4574a7d8b5a91641e7bc4aee436c706c0ec7fef1`, checkout
`uv run bea`, macOS arm64, isolated child config/cache, closed stdin and no token.
All files are synthetic. Run from cli/ with absolute fixture paths. A1/A2:
automated bookkeeping must preserve imported values and typed results.
No production access or backend change is required.
