---
id: w1/m2/t004
title: Test coverage — regression guarding ledgerId is passed
worker: worker1
status: done
estimate: 45m
depends_on: [w1/m2/t002]
---

## Objective

Add a meaningful regression test so a screen can never again resolve currency from an unscoped `useLedgerMeta(userId)` call.

## Context

- Runner is the custom jest-lite (`mobile/scripts/`, `yarn test:unit`) with limited matchers (`toBe`/`toEqual`/`toBeCloseTo`/`toBeTruthy`/`toBeFalsy`/`toThrow` + `.not`) and no `@/` alias for value imports — use relative imports.
- Prefer a test that asserts real behavior over a snapshot. Options, in order of preference:
  1. A pure-logic test around currency resolution (given `currencies: ["USD"]` → symbol `$`; given empty → fallback `"USD"`), if the selection logic can be factored into a testable unit (coordinate with t003's helper if one is extracted).
  2. A source-level guard test that scans the four screen files (or all of `src/screens`) and asserts none contains the `useLedgerMeta(userId)` call shape lacking a second argument — cheap, and it directly encodes the invariant this milestone establishes.
- Whatever the form, the test must FAIL on the pre-fix code and PASS after.

## Steps

1. Write the test (unit-logic and/or the source-guard scan).
2. Confirm it fails against the old call shape and passes now; `yarn test:unit` green.

## Files

- `mobile/src/__tests__/` (new test file)

## Acceptance criteria

- [ ] New test asserts the selected-ledger currency invariant and fails on the pre-fix pattern
- [ ] `yarn test:unit` passes

## Out of scope

- E2E/simulator automation (manual verification lives in t001/t002)
