---
id: w1/m2/t003
title: Simplify
worker: worker1
status: done
estimate: 20m
depends_on: [w1/m2/t002]
---

## Objective

Run `/simplify` over the code changed by this milestone (reuse / simplification / efficiency; behavior-preserving only).

## Context

- Four screens now call `useLedgerMeta(userId, ledgerId)` identically. Consider whether the shared `currency = currencies[0] ?? "USD"` derivation (repeated in Home and account-detail) is worth extracting into a small helper/hook — only if it genuinely removes duplication without over-abstracting.
- Do not change behavior; this is cleanup, not the fix.

## Steps

1. Run `/simplify` scoped to the files touched in t001/t002.
2. Apply only behavior-preserving improvements; re-run `yarn lint` + `yarn typecheck`.

## Files

- Files changed in t001 and t002.

## Acceptance criteria

- [ ] `/simplify` run; behavior-preserving cleanups applied (or explicitly none warranted)
- [ ] Lint + typecheck still green

## Out of scope

- Bug-hunting (that was the fix itself); new tests (t004)
