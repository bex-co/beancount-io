---
id: w1/m2/t002
title: Fix Reports, account-detail, quick-add — same missing ledgerId
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m2/t001]
---

## Objective

Apply the same fix as t001 to the other three `useLedgerMeta` callers that omit the selected `ledgerId`, so their currency also tracks the selected ledger.

## Context

Three more callers share the Home-screen bug (`grep -rn "useLedgerMeta(" mobile/src`):

- `mobile/src/screens/reports-screen/reports-screen.tsx:57` — `useLedgerMeta(userId)`; `ledgerId` already in scope at line 49 (`useLedgerGuard()`). One-line fix.
- `mobile/src/screens/account-detail-screen/account-detail-screen.tsx:103` — `useLedgerMeta(userId)`; `ledgerId` already in scope at line 95 (`useLedgerGuard()`). One-line fix.
- `mobile/src/screens/add-transaction-screen/quick-add-accounts-selector.tsx:80` — `useLedgerMeta(userId)`; here `ledgerId` is derived from `useReactiveVar(ledgerVar)` at line 104, **below** the call. Hoist that derivation above line 80 (or read `ledgerVar` inline), then pass it. Watch the existing `?? ""` fallback used at line 110.

## Steps

1. Reports & account-detail: change `useLedgerMeta(userId)` → `useLedgerMeta(userId, ledgerId)`.
2. quick-add: move the `ledgerId` derivation above the `useLedgerMeta` call, then pass it; keep the existing empty-string fallback semantics.
3. Verify each screen shows the selected ledger's currency against `open_ledger/airbnb` (USD).

## Files

- `mobile/src/screens/reports-screen/reports-screen.tsx`
- `mobile/src/screens/account-detail-screen/account-detail-screen.tsx`
- `mobile/src/screens/add-transaction-screen/quick-add-accounts-selector.tsx`

## Acceptance criteria

- [ ] All three screens pass the selected `ledgerId` to `useLedgerMeta`
- [ ] `grep -rn "useLedgerMeta(userId)" mobile/src` returns no matches (every caller now scoped)
- [ ] `yarn lint` and `yarn typecheck` pass (quick-add hoist introduces no hook-order/TDZ issues)

## Out of scope

- Regression test (t004); simplification (t003)
