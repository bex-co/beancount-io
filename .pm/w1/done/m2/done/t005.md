---
id: w1/m2/t005
title: Closeout
worker: worker1
status: done
estimate: 15m
depends_on: [w1/m2/t004]
---

## Objective

Close the milestone once its definition of done actually holds — all four screens show the selected ledger's currency and the regression test guards it.

## Context

- Only run this when t001–t004 are done AND the DoD is real: with `open_ledger/airbnb` selected, Home Net Worth renders `$`, and no `useLedgerMeta(userId)` (unscoped) calls remain.
- Closing = the move: run `/pm done w1/m2/t005` last to trigger the milestone move (set remaining tasks `status: done`, move `tNNN.md` into `done/`, mark rows `— **DONE**`, set `**Status:** done`, move `w1/m2/` → `w1/done/m2/`, check `- [x]` in `w1/README.md`).

## Steps

1. Verify the DoD end state in the running app (airbnb → `$`).
2. Run `/pm done w1/m2/t005`.

## Files

- `.pm/w1/m2/` (board mechanics only)

## Acceptance criteria

- [ ] DoD verified in-app before closing
- [ ] Milestone moved to `w1/done/m2/`; `w1/README.md` box checked

## Out of scope

- Any code change (must already be complete)
