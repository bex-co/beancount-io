---
id: w1/m11/t008
title: Closeout
worker: worker1
status: done
estimate: 15m
depends_on: [w1/m11/t007]
---

## Objective

Close the milestone once every other task is complete **and the definition of done actually holds**: the new command tree ships, old paths are gone, the boundary is enforced, docs match.

## Context

- Standing closing task — completing it *is* the move (`/pm done w1/m11/t008` triggers the milestone move).

## Steps

1. Re-verify the milestone's definition of done end to end.
2. Set every remaining task's `status: done`; move each `tNNN.md` to `w1/m11/done/`.
3. Mark every row `— **DONE**` and set `**Status:** done` in `w1/m11/README.md`.
4. Move `w1/m11/` to `w1/done/m11/` and check `- [x]` for m11 in `w1/README.md`.

## Files

- `.pm/w1/m11/`, `.pm/w1/README.md`

## Acceptance criteria

- [ ] Milestone exists only at `w1/done/m11/`; workstream checkbox checked; all statuses consistent.

## Out of scope

- Starting m12 work (separate milestone, sequenced after this one).
