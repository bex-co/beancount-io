---
id: w1/m11/t007
title: Test coverage
worker: worker1
status: done
estimate: 45m
depends_on: [w1/m11/t005]
---

## Objective

Add meaningful tests for the behavior this milestone shipped: the command tree resolves as designed, old paths are gone, and help output carries the Local/Cloud grouping. Tests must assert real behavior and failure modes — no snapshot-everything or tautologies.

## Context

- Standing closing task.
- Typer's `CliRunner` supports invoking the app in-process; fixtures live in `cli/tests/conftest.py`.
- The boundary test (t003) already covers imports; this task covers UX-visible behavior.

## Steps

1. Test `bea cloud login|logout|status` and `bea cloud ledger …` resolve to commands (exit code / help text, no server needed).
2. Test `bea auth` and top-level `bea ledger` fail with unknown-command errors.
3. Test `bea --help` places every command in its intended panel group.
4. Test a cloud command without credentials fails fast with guidance pointing at `bea cloud login`.

## Files

- `cli/tests/` (new/extended test modules)

## Acceptance criteria

- [ ] Each listed behavior has a test that fails if the behavior regresses.
- [ ] `make check-all` green.

## Out of scope

- Transport-level tests (m12 owns the REST client).
