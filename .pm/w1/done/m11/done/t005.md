---
id: w1/m11/t005
title: Adoption surface
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m11/t002, w1/m11/t003, w1/m11/t004]
---

## Objective

Verify the restructured command tree is discoverable and usable on every surface it touches, for both Claude Code and Codex.

## Context

- Standing closing task — the milestone reshapes the user- and agent-facing CLI surface.
- Root `CLAUDE.md` compatibility rules: `AGENTS.md` symlinks, `.claude/skills` / `.agents/skills` shared-skill links.

## Steps

1. Follow `cli/README.md` install + quickstart exactly as written against the new tree; fix drift.
2. Check the root `README.md` and `CLAUDE.md` package-table descriptions of the CLI still hold.
3. Confirm any `bea`-invoking skills behave identically under Claude Code and Codex (same instructions, same triggers).
4. Run `python3 scripts/check-agent-guidance.py` if instruction files changed.
5. File follow-up inbox notes for any drift too large to fix inline.

## Files

- `cli/README.md`, root `README.md`, root `CLAUDE.md`, `skills/`

## Acceptance criteria

- [ ] Quickstart works exactly as written; no surface still shows the old command paths.
- [ ] Agent-guidance check passes if instruction files were touched.

## Out of scope

- New documentation beyond fixing drift.
