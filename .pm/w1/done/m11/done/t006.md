---
id: w1/m11/t006
title: Simplify
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m11/t005]
---

## Objective

Run `/simplify` over the code this milestone changed (command registration, cloud package, boundary test) — reuse, simplification, efficiency; behavior-preserving only.

## Context

- Standing closing task.
- Scope: `cli/src/cli/main.py`, `cli/src/cli/commands/cloud/`, `cli/tests/test_command_boundaries.py`.

## Steps

1. Run `/simplify` scoped to the milestone's diff.
2. Apply accepted cleanups; re-run `make check-all`.

## Files

- Files changed by m11 tasks.

## Acceptance criteria

- [ ] `/simplify` findings addressed or explicitly declined.
- [ ] `make check-all` green after cleanups.

## Out of scope

- Bug hunting (that is `/code-review`, not this task).
