---
id: w1/m11/t004
title: Sweep docs and cross-repo references to the old command paths
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m11/t001]
---

## Objective

Update every tracked reference to `bea auth …` and top-level `bea ledger …` so documentation, skills, and the board describe only the new tree. The CLI is unreleased, so there is no deprecation story to write — old paths simply disappear.

## Context

- Known reference surfaces: `cli/README.md`, `cli/docs/USAGE.md`, `cli/docs/PRFAQ.md`, `cli/CLAUDE.md`, `skills/` (any skill that shells out to `bea`), root `README.md`/`CLAUDE.md` package tables.
- Find the rest mechanically: `rg -n 'bea (auth|ledger)\b'` over the tracked tree.

## Steps

1. Run the sweep and fix every hit to the `bea cloud …` form; while in `docs/USAGE.md`, restructure the command reference to mirror the Local/Cloud grouping from t002.
2. Re-run the sweep to confirm zero remaining hits outside historical records (`.pm/*/done/`, git history).
3. If any skill's behavior (not just wording) depended on the old paths, fix the skill and note it for the Adoption surface task.

## Files

- `cli/README.md`, `cli/docs/USAGE.md`, `cli/docs/PRFAQ.md`, `cli/CLAUDE.md`
- `skills/` (hits from the sweep)

## Acceptance criteria

- [ ] `rg -n 'bea (auth|ledger)\b'` over tracked files returns no hits outside `done/` history.
- [ ] `docs/USAGE.md` examples run as written against the restructured CLI.

## Out of scope

- REST/transport wording changes (m12 owns those).
