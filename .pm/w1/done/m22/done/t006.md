---
id: w1/m22/t006
title: "Add regression coverage and verify CI plus installed artifacts"
worker: worker1
status: done
estimate: 50m
depends_on: [w1/m22/t004, w1/m22/t005]
---

## Objective

Prove the fixes in real installed CLI journeys and complete the package CI gate.

## Context

The released 0.2.0 smoke passed 75 commands but missed these cases. New tests must fail on the reproduced behavior rather than merely asserting registration or mocked outputs.

## Steps

1. Add meaningful tests for missing/existing export destinations, numberification, native help content/forwarding, and PTY redirect/reset/requery with separate native controls.
2. Run make check-all from cli/; build wheel/sdist using make release-artifacts and test installed artifacts in isolated environments, without relying on checkout imports.
3. Confirm the affected commit’s CI is green before any separately authorized release. Recheck all eleven doctor operations and the 365-day/300-USD report control; retain single-distribution and frontend-isolation assertions.

## Files

- cli/tests/
- cli/scripts/smoke-installed.py
- cli/Makefile (commands)
- cli/tmp/ (isolated evidence only)

## Acceptance criteria

- [x] Regression tests demonstrably cover each original failure; full package checks and installed wheel/sdist journeys pass.
- [x] Original ledger remains untouched; all eleven doctor controls and full-year daily report still pass; published artifacts would contain only beancount-io.
- [x] CI status and exact tested artifact/version are recorded; no release is declared successful on local tests alone.

## Out of scope

- Independent engine PyPI packaging, unrelated cloud/API changes, and modifying customer ledgers.

## Final verification

Implementation: b75b1b671bd225adef3f53cb2f77f61ac23422ab (`fix(cli): honor JSON exports and restore native CLI usability`). [CI (cli)](https://github.com/bex-co/beancount-io/actions/runs/34730363219) completed successfully: all 15 jobs, including wheel/sdist installs on Linux/macOS/Windows with Python 3.12/3.14 and Homebrew on Linux/macOS. [Secret scan](https://github.com/bex-co/beancount-io/actions/runs/34730363090) passed.

Local full gate: 648 tests passed, plus lint, Vulture, formatting, mypy, spec and generated-documentation checks. Sixteen focused regression cases cover exports, ignored options, help, and a real PTY redirect/reset/requery/failure-recovery sequence. Wheel/Python 3.12 and sdist/pip/Python 3.14 each passed the expanded 80-command installed smoke. Separate installed-wheel checks matched all eleven doctor controls, preserved 365 daily report periods and 300 USD, and passed the full shell session plus .exit/EOF with no traceback. Original ledger hashes matched. Guidance compatibility validation passed; root package descriptions required no changes.

QA artifacts remain local under cli/tmp/m22/: check-all.log, wheel.log, sdist.log, audit-results.json and results/pty-*.json. Upstream's current shell still contains the broken reset handler; a narrow engine-side override carries its removal condition. No new dependency or separate distribution was introduced. The milestone ships on main; these changes are not a new PyPI version.
