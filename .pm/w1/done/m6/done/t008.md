---
id: w1/m6/t008
title: Test coverage
worker: worker1
status: done
estimate: 45m
depends_on: [w1/m6/t006]
---

## Objective

Add meaningful tests for the shipped behavior and its failure modes, beyond the unit cases written inline in t001–t005.

## Context

- Nothing tests the dashboard consent pages as components; `mobile-consent-state.test.ts` is a pure reducer test that never touches `uid`.
- Nothing on the mobile side tests the welcome screen.

## Steps

1. Dashboard: a component test that `uid`, `scope`, and `screen_hint` survive login → register → OTP → approve into the form action.
2. Backend: hint rejection, hint forwarding, and non-mobile isolation cases if t001 left any gap.
3. Mobile: welcome screen renders the right failure line per flow; `buildAuthorizationUrl` never emits a hint for legacy or sign-in flows.

## Files

- `dashboard/src/features/oauth/**/__tests__/`
- `backend-cluster/backend-v2/src/features/oauth/api/__tests__/oidc-route.test.ts`
- `mobile/src/common/oauth/__tests__/`, `mobile/src/screens/welcome/__tests__/`

## Acceptance criteria

- [x] Each test asserts real behavior or a failure mode; no snapshot-everything or tautological tests.
- [x] All three packages' test gates pass.

## Out of scope

- End-to-end device automation.
