---
id: w1/m18/t005
title: Adoption surface
worker: worker1
status: done
estimate: 20m
depends_on: [w1/m18/t004]
---

## Objective

Confirm the converged skills read the same under Claude Code and Codex and that every install pointer works.

## Context

- Root `CLAUDE.md` compatibility rules: `.claude/skills` and `.agents/skills` both point at `skills/.claude/skills`; `AGENTS.md` symlinks; `python3 scripts/check-agent-guidance.py`.
- Install pointers inside skills must match `cli/README.md` (`brew install bex-co/tap/bea`, `uv tool install beancount-io`).

## Steps

1. Invoke `/beancount-init` and `/beancount-ask` once each from Claude Code and once from Codex on a scratch ledger; confirm the same commands run.
2. Check the `skills/CLAUDE.md` table rows and the root `README.md` skills sentence reflect the change.
3. Run the guidance check.

## Files

- `skills/CLAUDE.md`, `README.md`

## Acceptance criteria

- [ ] Same behavior in both agents; guidance check passes.

## Out of scope

- Dashboard or mobile surfaces.
