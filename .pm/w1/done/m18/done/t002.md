---
id: w1/m18/t002
title: "beancount-import writes through bea import --csv / bea add transactions when available"
worker: worker1
status: done
estimate: 60m
depends_on: [w1/m18/t001]
---

## Objective

Let the import skill keep its judgment (format detection, categorization from history, confirm gate) and delegate staging, duplicate review, validation, and the atomic write to the CLI.

## Context

- `skills/.claude/skills/beancount-import/SKILL.md` runs a seven-stage pipeline and appends text itself, then runs `bean-check`; `references/dedup.md` defines `import-id`.
- After m16, `bea import file.csv --csv … --account … --rules …` produces the preview, duplicate table, and diff; `bea add transactions --from -` accepts a validated JSON batch (`cli/docs/USAGE.md` "Bulk transactions from JSON") with `meta` for `import-id`.
- `bea import` and the skill share the `import-id` convention after m14.

## Steps

1. Add a "with `bea`" branch: for CSV, map columns into `--csv`, write the skill's suggested categories into a temporary rules file, and run the preview; present `bea`'s duplicate table as the review table; write with `--apply --duplicates …` after confirmation.
2. For OFX and QIF, or when the mapping cannot express the file, keep the skill's own parsing but write the confirmed batch with `bea add transactions --from -` including `import-id` metadata, instead of appending text.
3. Keep the no-`bea` path unchanged; state the rule once at the top of the skill.
4. Update `references/dedup.md` to note `bea import` follows it and add the `bea` exact-duplicate behavior to the dedup layer description.

## Files

- `skills/.claude/skills/beancount-import/SKILL.md`, `references/dedup.md`, `references/formats.md`, `evals/`

## Acceptance criteria

- [ ] With `bea` on PATH the skill never appends ledger text directly.
- [ ] A re-run on the same export writes zero entries via either path.
- [ ] Skills CI passes.

## Out of scope

- `beancount-importer-author` and `beancount-migrate` (follow-up inbox note if drift is found).
