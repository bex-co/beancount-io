---
id: w1/m13/t004
title: Document the policy in USAGE, README, and a PRFAQ amendment
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m13/t003]
---

## Objective

State the strictness rule once in each document that describes reads, so the docs and the executable agree.

## Context

- `cli/docs/USAGE.md` sections "Checking, formatting, querying" and "Reports" describe refusal as unconditional.
- `cli/README.md` sections "Check" and "Valuation and signs" repeat it.
- `cli/docs/PRFAQ.md` FAQ 9 ("Analytical output must disclose invalid-ledger state") is the principle; add a dated amendment describing the terminal exception, in the style of the existing 2026-09-07 amendments.

## Steps

1. Replace the two refusal paragraphs with one rule: lenient with a banner in a terminal; strict under `--json`, when stdout is not a terminal, under `CI`, or with `--strict`; `--allow-errors` opts strict mode into partial answers.
2. Add `--strict` to both global-option tables.
3. Describe missing-price behavior with the per-commodity summary and the strict-mode error shape.
4. Add the PRFAQ amendment.

## Files

- `cli/docs/USAGE.md`
- `cli/README.md`
- `cli/docs/PRFAQ.md`

## Acceptance criteria

- [ ] Every documented example in the edited sections runs as written.
- [ ] `grep -n "Pass --allow-errors" cli/README.md cli/docs/USAGE.md` shows the flag only where strict mode is described.

## Out of scope

- The README restructuring in m17.
