---
id: w1/m13/t007
title: Test coverage
worker: worker1
status: done
estimate: 45m
depends_on: [w1/m13/t005]
---

## Objective

Pin the policy and the valuation behavior with tests that fail when either regresses.

## Context

- `cli/tests/test_contract.py` already asserts exit codes for reads on an unloadable ledger; extend it rather than adding a parallel file.
- `cli/tests/conftest.py` has fixtures for ledgers and JSON invocations; a pseudo-terminal can be simulated by patching `isatty`.

## Steps

1. Add cases: terminal read with errors exits 0 with a banner; `--json`, piped, and `--strict` exit 1; `check` unchanged.
2. Add a `bean-example`-style fixture with an unpriced commodity and assert units rendering, the per-commodity summary count, and the strict-mode JSON shape.
3. Add the EUR precision fixture asserting `4.91 USD` in text and `"4.9050"` in JSON.

## Files

- `cli/tests/test_contract.py`
- `cli/tests/fixtures/`

## Acceptance criteria

- [ ] Reverting t001, t002, or t003 individually makes at least one new test fail.
- [ ] `make test` passes.

## Out of scope

- Snapshot tests of whole report outputs.
