---
id: w1/m24/t009
title: Adoption surface
worker: worker1
status: done
estimate: 25m
depends_on: [w1/m24/t001, w1/m24/t002, w1/m24/t003, w1/m24/t004, w1/m24/t005, w1/m24/t006, w1/m24/t007, w1/m24/t008]
---

## Objective

Verify the no-code CSV path is documented as it now behaves, and that the importing guide, the tutorial and the `beancount-import` skill all still work exactly as written.

## Context

- Root `CLAUDE.md` compatibility rules: every `AGENTS.md` is a relative symlink to its `CLAUDE.md`, and root `.claude/skills` links to `../.agents/skills`.
- Customer skill catalog: `skills/CLAUDE.md`; customer skills live in `skills/.claude/skills/beancount-*`.
- `cli/docs/REFERENCE.md` is generated — run `make docs`, never hand-edit.

## Steps

1. Update `cli/docs/IMPORTING.md` for delimiter, encoding, amount spellings, matchable rule fields, and sticky-recall visibility.
2. Re-run every fenced example in `cli/docs/IMPORTING.md` and `cli/docs/TUTORIAL.md`; the executable-examples test must stay green.
3. Run `make docs` and confirm `make docs-check` is green for the new `--csv` keys.
4. Check the `beancount-import` and `beancount-migrate` skill text against the new behavior, then run `python3 skills/scripts/ci-check.py` and `python3 scripts/check-agent-guidance.py`.

## Files

- cli/docs/IMPORTING.md
- cli/docs/TUTORIAL.md
- cli/docs/REFERENCE.md
- skills/.claude/skills/beancount-import/
- skills/CLAUDE.md

## Acceptance criteria

- [ ] The importing guide and tutorial describe the shipped behavior and every example in them runs in CI.
- [ ] `make docs-check`, `python3 skills/scripts/ci-check.py`, and `python3 scripts/check-agent-guidance.py` all pass.
- [ ] The `beancount-import` skill's `bea import --csv` route works on the new fixture set for both Claude Code and Codex.

## Out of scope

- Rewriting unrelated documentation sections.
- Shipping a release; this milestone lands on `main` only.
