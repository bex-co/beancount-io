---
id: w1/m17/t002
title: "Slim cli/README.md to install, quick start, command map, and links; USAGE holds the contract"
worker: worker1
status: done
estimate: 60m
depends_on: [w1/m17/t001]
---

## Objective

Make the README a landing page and USAGE the single contract, removing every paragraph that exists in both.

## Context

- `cli/README.md` sections after "Quick start" ("Write safely and use split ledgers", "Add directives", "List directives", "Check, format, and query", "Financial reports", "JSON and automation", "Configuration and stored state") repeat USAGE with different wording.
- The README is rendered on the package registry, so links must be absolute (already fixed once in w2/m25).
- `cli/README.md` "Development", "Releases", and "License" stay; "Releases" is maintainer-facing and can move to `cli/docs/RELEASING.md` with a link.

## Steps

1. Rewrite the README to: one-paragraph pitch, Install, Quick start (ending in `bea report balance-sheet`), Command map, Exit codes, "Learn more" links (TUTORIAL, USAGE, IMPORTING, REFERENCE), Development, License.
2. Move any README-only fact that USAGE lacks into USAGE; delete the rest of the duplicated prose.
3. Move the release procedure to `cli/docs/RELEASING.md`; update `cli/CLAUDE.md` and `.github/workflows/release-cli.yml` comments that point at README sections.
4. Check `pyproject.toml` `readme` still points at the README and that the registry rendering has no relative links.

## Files

- `cli/README.md`, `cli/docs/USAGE.md`, `cli/docs/RELEASING.md` (new), `cli/CLAUDE.md`

## Acceptance criteria

- [ ] `wc -l cli/README.md` under 200.
- [ ] No sentence appears verbatim in both README and USAGE (spot-check with a shared-lines script in the task).
- [ ] Every link in the README resolves on the registry page and on GitHub.

## Out of scope

- Restructuring USAGE beyond absorbing README-only facts.
