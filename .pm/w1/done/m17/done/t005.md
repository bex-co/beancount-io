---
id: w1/m17/t005
title: Adoption surface
worker: worker1
status: done
estimate: 20m
depends_on: [w1/m17/t004]
---

## Objective

Confirm the new document set is what a visitor, the registry, and a coding agent actually see.

## Context

- Surfaces: GitHub `cli/README.md`, the registry landing page (rendered from the same file), root `README.md` package table and CLI paragraph, `cli/CLAUDE.md` "See README.md … docs/USAGE.md" pointer, and `skills/.claude/skills/beancount-*` links into `cli/docs/`.

## Steps

1. Render the README with a Markdown renderer that follows registry rules (no relative links) and click every link.
2. Update the root README package table description and `cli/CLAUDE.md` pointers to name TUTORIAL and REFERENCE.
3. Grep skills for links into `cli/docs/` and fix any that moved; run `python3 scripts/check-agent-guidance.py`.

## Files

- `README.md`, `cli/CLAUDE.md`, `skills/.claude/skills/*/SKILL.md` (links only)

## Acceptance criteria

- [ ] No broken links; guidance check passes.

## Out of scope

- Downstream sites.
