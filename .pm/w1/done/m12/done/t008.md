---
id: w1/m12/t008
title: Adoption surface
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m12/t006, w1/m12/t007]
---

## Objective

Verify the REST-era CLI is discoverable and usable on every surface it touches, for both Claude Code and Codex.

## Context

- Standing closing task — the milestone changes shipped CLI behavior (error/exit-code semantics, delete confirmation) and the contributor codegen workflow.

## Steps

1. Follow `cli/README.md` install + quickstart exactly as written; fix drift.
2. Verify `docs/USAGE.md` documents the exit-code mapping and the confirmation flags.
3. Check root `README.md`/`CLAUDE.md` package tables still describe the CLI accurately (no GraphQL wording).
4. Confirm `bea`-invoking skills behave identically under Claude Code and Codex; run `python3 scripts/check-agent-guidance.py` if instruction files changed.
5. File follow-up inbox notes for drift too large to fix inline.

## Files

- `cli/README.md`, `cli/docs/USAGE.md`, root `README.md`, root `CLAUDE.md`, `skills/`

## Acceptance criteria

- [ ] Quickstart works exactly as written; no surface still describes the GraphQL transport.
- [ ] Agent-guidance check passes if instruction files were touched.

## Out of scope

- New documentation beyond fixing drift.
