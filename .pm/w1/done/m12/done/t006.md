---
id: w1/m12/t006
title: Delete `cli/graphql/`, `gql_client/`, and GraphQL codegen dependencies
worker: worker1
status: done
estimate: 20m
depends_on: [w1/m12/t005]
---

## Objective

Remove every GraphQL artifact from the CLI so exactly one contract and one generation chain remain: the pinned OpenAPI snapshot.

## Context

- To delete: `cli/graphql/` (schema + operations), `cli/src/cli/api/gql_client/`, the ariadne-codegen (or equivalent) dependency and its config in `pyproject.toml`.
- `cli/CLAUDE.md` documents the GraphQL codegen convention ("change `graphql/schema.graphql` … then run `make codegen`") and the layout table — both must describe the OpenAPI chain instead.

## Steps

1. Delete the directories; remove the codegen dependency via `uv` (never hand-edit `uv.lock`).
2. Update `cli/CLAUDE.md` (layout + conventions) and any remaining references (`rg -in 'graphql' cli/`).
3. Run `make deadcode` and `make check-all` to confirm nothing dangles.

## Files

- `cli/graphql/` (removed), `cli/src/cli/api/gql_client/` (removed)
- `cli/pyproject.toml`, `cli/uv.lock` (via `uv`), `cli/CLAUDE.md`

## Acceptance criteria

- [ ] `rg -in 'graphql' cli/ --glob '!uv.lock'` returns no tracked hits.
- [ ] `make check-all` green.

## Out of scope

- Backend GraphQL surface — untouched; dashboard and mobile still use it.
