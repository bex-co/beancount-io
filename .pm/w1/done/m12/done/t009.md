---
id: w1/m12/t009
title: Simplify
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m12/t008]
---

## Objective

Run `/simplify` over the code this milestone changed — client seam, migrated commands, error mapping, stub generator; reuse, simplification, efficiency; behavior-preserving only.

## Context

- Standing closing task.
- Scope: `cli/src/cli/api/`, `cli/src/cli/commands/cloud/`, `cli/src/cli/errors.py`, `cli/scripts/gen_cloud_commands.py`. Generated output is exempt — simplify the generator, not its output.

## Steps

1. Run `/simplify` scoped to the milestone's diff.
2. Apply accepted cleanups; re-run `make codegen` + `make check-all`.

## Files

- Files changed by m12 tasks (excluding generated directories).

## Acceptance criteria

- [ ] `/simplify` findings addressed or explicitly declined.
- [ ] `make check-all` green after cleanups.

## Out of scope

- Bug hunting (that is `/code-review`, not this task).
