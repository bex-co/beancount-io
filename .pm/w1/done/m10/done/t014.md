---
id: w1/m10/t014
title: "Expose latest commit, history, and commit details"
worker: worker1
status: done
estimate: 60m
depends_on: [w1/m10/t003]
---

## Objective

Let integrations inspect ledger history and changes through REST and MCP.

## Context

- Follow this milestone's README.md parity contract and COVERAGE.md baseline map, including after the milestone is archived.
- Baseline operations: `Query.getLatestLedgerCommit`, `Query.listCommits`, `Query.getCommitDetails`.
- Read the owning package's CLAUDE.md before implementation. Keep current credential authority and named structural exceptions unless the milestone contract explicitly resolves a contradiction.

## Steps

1. Reuse repository/history services with the same revision, branch, page, and detail parameters used by GraphQL.
2. Preserve commit IDs, authors, timestamps, diffs, missing-revision behavior, and visibility checks.
3. Test branch/revision selection, page boundaries, unauthorized private ledgers, and source outages.

## Files

- `backend-cluster/backend-v2/src/features/ledger/api/`
- `backend-cluster/backend-v2/src/features/ledger/service/`
- `backend-cluster/backend-v2/src/features/gitea/`
- `backend-cluster/backend-v2/src/features/ai-agent/api/`

## Acceptance criteria

- [ ] All three history operations have matching results on every eligible surface.
- [ ] History never bypasses current ledger access because a commit ID is known.
- [ ] Legacy/default branch behavior stays compatible.

## Out of scope

- Other task families in this milestone; broad rewrites of established domain services.
- Removing eligible operations, widening credential authority, or changing exclusions merely to make parity counts pass.
