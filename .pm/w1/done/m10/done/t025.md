---
id: w1/m10/t025
title: "Expose temporary asset upload and download URLs"
worker: worker1
status: done
estimate: 45m
depends_on: [w1/m10/t003, w1/m10/t002]
---

## Objective

Let external clients participate in upload-based workflows while retaining current temporary-object ownership and expiry rules.

## Context

- Follow this milestone's README.md parity contract and COVERAGE.md baseline map, including after the milestone is archived.
- Baseline operations: `Query.generateTempAssetDownloadUrl`, `Mutation.generateTempAssetUploadUrl`.
- Read the owning package's CLAUDE.md before implementation. Keep current credential authority and named structural exceptions unless the milestone contract explicitly resolves a contradiction.

## Steps

1. Add URL-creation adapters through the established asset service/workflow.
2. Preserve allowed content metadata, size/expiry fields, tmp ownership, URL lifetime, and storage key validation.
3. Test the usable upload/download flow with isolated storage fixtures and denied foreign/malformed/expired object references.

## Files

- `backend-cluster/backend-v2/src/features/s3/`
- `backend-cluster/backend-v2/src/features/ledger/workflow/`
- `backend-cluster/backend-v2/src/features/ai-agent/api/`

## Acceptance criteria

- [ ] Both baseline operations have working equivalent bindings.
- [ ] A caller cannot mint access to another user's temporary asset.
- [ ] Signed URLs and credentials are absent from recorded fixtures, logs, and board evidence.

## Out of scope

- Other task families in this milestone; broad rewrites of established domain services.
- Removing eligible operations, widening credential authority, or changing exclusions merely to make parity counts pass.
