---
id: w1/m10/t006
title: "Reach structured BQL and full report parameters through MCP"
worker: worker1
status: done
estimate: 60m
depends_on: [w1/m10/t003]
---

## Objective

Close the structured-query gap and provide a working transport for all existing report filters and pagination parameters.

## Context

- Follow this milestone's README.md parity contract and COVERAGE.md baseline map, including after the milestone is archived.
- Baseline operations: `ledger.queryShell`.
- Read the owning package's CLAUDE.md before implementation. Keep current credential authority and named structural exceptions unless the milestone contract explicitly resolves a contradiction.

## Steps

1. Expose LedgerShellService's typed table/text result through MCP while preserving existing runBqlQuery text calls.
2. Implement and test actual URI matching or an explicit structured query tool for optional account/filter/time/interval/conversion and paging inputs; reuse existing validators rather than publishing nonmatching templates.
3. Compare defaults, empty values, reserved characters, colons, Unicode, sorting, limits, invalid combinations, and currency/decimal representations through all three adapters.

## Files

- `backend-cluster/backend-v2/src/features/ai-agent/api/mcp-resources.ts`
- `backend-cluster/backend-v2/src/features/ai-agent/tools/bql-query-tool.ts`
- `backend-cluster/backend-v2/src/features/ledger/api/rest/v1/analysis-handler.ts`
- `backend-cluster/backend-v2/src/features/ledger/service/ledger-shell-service.ts`

## Acceptance criteria

- [ ] MCP can obtain typed columns and rows as well as formatted BQL text.
- [ ] A time-filtered report and a paginated read produce the same normalized result across actual adapters.
- [ ] Optional inputs are exercised, not dropped from parity fixtures; invalid inputs fail without fetching unrelated data.

## Out of scope

- Other task families in this milestone; broad rewrites of established domain services.
- Removing eligible operations, widening credential authority, or changing exclusions merely to make parity counts pass.
