---
id: w1/m10/t023
title: "Expose file and receipt parsing contracts"
worker: worker1
status: done
estimate: 60m
depends_on: [w1/m10/t003, w1/m10/t002]
---

## Objective

Make assisted parsing reachable through REST and MCP with the existing validation, asset ownership, and AI-use requirements.

## Context

- Follow this milestone's README.md parity contract and COVERAGE.md baseline map, including after the milestone is archived.
- Baseline operations: `Mutation.parseFile`, `Mutation.parseReceipt`.
- Read the owning package's CLAUDE.md before implementation. Keep current credential authority and named structural exceptions unless the milestone contract explicitly resolves a contradiction.

## Steps

1. Reuse existing parsing services and available receipt-tool implementations; register actual MCP/REST adapters rather than recreating parsing logic.
2. Preserve accepted input forms, model/output schemas, source ownership, quota charging, and actionable provider errors.
3. Test using controlled provider/storage boundaries so unit and contract checks do not send paid model calls or private documents.

## Files

- `backend-cluster/backend-v2/src/features/llm/`
- `backend-cluster/backend-v2/src/features/ledger/workflow/`
- `backend-cluster/backend-v2/src/features/ai-agent/tools/parse-receipt-tool.ts`
- `backend-cluster/backend-v2/src/features/ai-agent/api/`

## Acceptance criteria

- [ ] Both parsing operations have equivalent input and normalized output contracts.
- [ ] Denied ownership, scope, or AI-use access is rejected before storage/provider calls.
- [ ] Provider failures and budget behavior remain meaningful across protocol envelopes.

## Out of scope

- Other task families in this milestone; broad rewrites of established domain services.
- Removing eligible operations, widening credential authority, or changing exclusions merely to make parity counts pass.
