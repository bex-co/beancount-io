---
id: w1/m10/t007
title: "Complete ledger discovery and metadata adapters"
worker: worker1
status: done
estimate: 60m
depends_on: [w1/m10/t003, w1/m10/t004]
---

## Objective

Make ledger discovery, ownership/search selection, and current/legacy metadata contracts available from REST and MCP.

## Context

- Follow this milestone's README.md parity contract and COVERAGE.md baseline map, including after the milestone is archived.
- Baseline operations: `Query.listLedgers`, `Query.listUserOwnedLedgers`, `Query.searchLedgers`, `Query.getLedger`, `Query.ledgerMeta`.
- Read the owning package's CLAUDE.md before implementation. Keep current credential authority and named structural exceptions unless the milestone contract explicitly resolves a contradiction.

## Steps

1. Implement missing adapters over the existing catalog and metadata services; use explicit argument mappings for owned/search variants.
2. Support discovery before selecting a ledger and retain the restriction of pinned credentials.
3. Test exact metadata fields, visibility, search/ownership behavior, empty results, pagination where supported, and access revocation. Bind legacy aliases only after proving their contracts equivalent.

## Files

- `backend-cluster/backend-v2/src/features/ledger/api/`
- `backend-cluster/backend-v2/src/features/ledger/service/`
- `backend-cluster/backend-v2/src/features/ai-agent/api/`

## Acceptance criteria

- [ ] All five COVERAGE.md operations have executable bindings on every eligible surface.
- [ ] Discovery never reveals inaccessible ledgers, and explicit selection does not expand a pin.
- [ ] Legacy metadata fields and search/ownership semantics match their existing GraphQL contract.

## Out of scope

- Other task families in this milestone; broad rewrites of established domain services.
- Removing eligible operations, widening credential authority, or changing exclusions merely to make parity counts pass.
