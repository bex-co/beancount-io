---
id: w1/m10/t018
title: "Complete file rename and align existing file mutation behavior"
worker: worker1
status: done
estimate: 60m
depends_on: [w1/m10/t003, w1/m10/t002]
---

## Objective

Close rename coverage and audit already-mapped file operations for validation, concurrency, batch, and preview differences.

## Context

- Follow this milestone's README.md parity contract and COVERAGE.md baseline map, including after the milestone is archived.
- Baseline operations: `Mutation.renameLedgerFile`.
- Read the owning package's CLAUDE.md before implementation. Keep current credential authority and named structural exceptions unless the milestone contract explicitly resolves a contradiction.

## Steps

1. Expose rename using the existing repository/workflow boundary and retain safe-path and SHA checks.
2. Compare create/update/replace/delete, line-range reads, atomic batches, commit messages, and dry-run behavior across current REST, GraphQL, and MCP implementations; reuse domain logic where behavior must agree.
3. Resolve actual contract differences without relying on stale comments about shared implementations. Advertise precisely which checks a preview executes. Where one adapter supports preview, implement equivalent checks and no-write behavior on its eligible twins; an explicit rejection is not equivalent support.

## Files

- `backend-cluster/backend-v2/src/features/ledger/api/rest/v1/files-handler.ts`
- `backend-cluster/backend-v2/src/features/ledger/service/ledger-repo-service.ts`
- `backend-cluster/backend-v2/src/features/ledger/workflow/`
- `backend-cluster/backend-v2/src/features/ai-agent/tools/edit-ledger-files-tool.ts`

## Acceptance criteria

- [ ] Rename has working adapters and equivalent repository effects.
- [ ] Existing mapped file operations have tested argument, SHA, atomicity, and preview contracts.
- [ ] No adapter claims Beancount validation or write authorization from a preview that never executes those checks.
- [ ] Supported previews work equivalently across eligible surfaces; preview rejection is reserved for an operation contract offering no preview on any surface.

## Out of scope

- Other task families in this milestone; broad rewrites of established domain services.
- Removing eligible operations, widening credential authority, or changing exclusions merely to make parity counts pass.
