---
id: w1/m10/t017
title: "Expose optimistic entry-slice updates and deletions"
worker: worker1
status: done
estimate: 60m
depends_on: [w1/m10/t016]
---

## Objective

Provide precise source-slice mutations with the same concurrency and validation guarantees on REST and MCP.

## Context

- Follow this milestone's README.md parity contract and COVERAGE.md baseline map, including after the milestone is archived.
- Baseline operations: `Mutation.deleteLedgerEntrySourceSlice`, `Mutation.deleteMultipleLedgerEntrySourceSlices`, `Mutation.updateLedgerEntrySourceSlice`.
- Read the owning package's CLAUDE.md before implementation. Keep current credential authority and named structural exceptions unless the milestone contract explicitly resolves a contradiction.

## Steps

1. Add single-delete, batch-delete, and update adapters to the protected entry workflow.
2. Retain source range/hash requirements, inclusive/exclusive range semantics, stale-content rejection, and batch atomicity.
3. Exercise real committed output and stale-hash, missing-range, overlap, missing-file, and authorization failures.

## Files

- `backend-cluster/backend-v2/src/features/ledger/api/`
- `backend-cluster/backend-v2/src/features/ledger/workflow/`
- `backend-cluster/backend-v2/src/features/ai-agent/api/mcp-tools.ts`

## Acceptance criteria

- [ ] All three operations preserve the GraphQL concurrency contract.
- [ ] Stale or missing required hashes fail visibly without writes.
- [ ] Equivalent valid inputs produce identical normalized ledger contents across adapters.

## Out of scope

- Other task families in this milestone; broad rewrites of established domain services.
- Removing eligible operations, widening credential authority, or changing exclusions merely to make parity counts pass.
