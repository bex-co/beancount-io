---
id: w1/m15/t006
title: Adoption surface
worker: worker1
status: done
estimate: 20m
depends_on: [w1/m15/t001, w1/m15/t002, w1/m15/t003, w1/m15/t004, w1/m15/t005]
---

## Objective

Confirm every new option and command is discoverable in help and docs and works as written.

## Context

- Surfaces: `bea --help`, `bea list transaction --help`, `bea balance --help`, `bea add transaction --help`, `cli/README.md`, `cli/docs/USAGE.md`, root `README.md` CLI section.
- Agent-facing: `skills/.claude/skills/beancount-ask/SKILL.md` lists CLI reads; `bea balance --json` and `--search` belong in its tool list.

## Steps

1. Run every example added by t001–t005 as written on a fresh ledger and on `bean-example`.
2. Add `bea balance` to the README and USAGE command maps and the root README paragraph if it lists commands.
3. Record skill drift as an inbox note for m18 rather than editing skills here.

## Files

- `cli/README.md`, `cli/docs/USAGE.md`, `README.md`

## Acceptance criteria

- [ ] All documented examples run as written.
- [ ] Command maps list `bea balance`.

## Out of scope

- Skill edits (m18).
