# Report account regex errors are misclassified and help says substring

**Minor · 25–40 minutes.** CLI-QA-003.

On a valid ledger with at least one transaction, run:
```sh
uv run bea --json --file /absolute/qa/main.bean report overview --account '['
```
Actual: exit 1, validation error "unterminated character set at position 5".
Expected: actionable account-filter usage error, exit 2. Existing help calls
this a substring filter despite regex-capable behavior (`.` matches everything).
Earlier fresh-process runs confirmed all four reports; ordinary account names
and nonmatching literal list filters are controls.

Cause: commands/report.py passes account to Fava AccountFilter, which invokes
regex matching, but catches only ValueError/OverflowError/FilterError as time
errors. Correct account-expression diagnostics and help, regenerate reference.
Preserve existing transaction-level regex semantics: completed w1/m15/t002
explicitly excludes changing them. Do not replace regex with literal matching.
Acceptance: malformed expression names the account filter and exits 2 in human
and JSON modes; valid patterns and literal list semantics remain unchanged.
Dedupe: reviewed full-board filter/regex results and completed m15 task; no
matching CLI note. This is independent of the balance alias in w1/020.

Revalidated 2026-09-10, HEAD 4574a7d8, checkout uv run bea on macOS arm64.
Synthetic files, isolated child config/cache, closed stdin, no credentials or
production calls. Run commands from cli/. A1/A2: predictable commands and
machine-readable accounting results. No backend change needed.
