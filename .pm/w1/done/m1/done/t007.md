---
id: w1/m1/t007
title: Simplify
worker: worker1
status: done
estimate: 20m
depends_on: [w1/m1/t006]
---

## Objective

Run `/simplify` over the code this milestone changed — reuse, simplification, efficiency — while preserving behavior.

## Context

- Changed surface: `dashboard/src/features/ai-agent/pages/ask-ai/` (`index.tsx`, `chat-input.tsx`, new chips component) and `dashboard/src/features/ai-agent/locales/`.

## Steps

1. Run `/simplify` scoped to the changed files.
2. Apply only behavior-preserving improvements; re-run lint/typecheck after.

## Acceptance criteria

- [ ] `/simplify` findings addressed or explicitly declined with reason.
- [ ] `yarn lint` and `yarn typecheck` pass in `dashboard/`.

## Out of scope

- Refactoring the SSE parsing pipeline beyond what the milestone touched.
