---
id: w1/m1/t006
title: Adoption surface
worker: worker1
status: done
estimate: 20m
depends_on: [w1/m1/t001, w1/m1/t002, w1/m1/t003, w1/m1/t004, w1/m1/t005]
---

## Objective

Verify the shipped Ask-page changes are discoverable and correctly described on every user- and agent-facing surface they touch.

## Context

- Dashboard docs: `dashboard/README.md`, `dashboard/docs/` (e.g. `i18n.md`, `TRANSLATIONS.md` if key conventions changed), `dashboard/AGENTS.md` (feature structure / i18n sections).
- The ai-agent feature README/docs, if any, plus the locale aggregation in `dashboard/src/locales/index.ts`.

## Steps

1. Check `dashboard/README.md` and `dashboard/docs/` for any description of the Ask page that now differs from behavior (suggestions, stop, retry); update or flag drift as follow-up.
2. Confirm new translation keys are merged by the aggregation layer for all languages.
3. Confirm `dashboard/AGENTS.md` needs no update (feature structure unchanged) — update it only if conventions actually changed.

## Acceptance criteria

- [ ] No doc describes pre-milestone Ask behavior as current.
- [ ] All new keys resolve in every locale at runtime.
- [ ] Any discovered drift is either fixed or captured as a new inbox note.

## Out of scope

- Marketing/changelog announcements.
