---
id: w1/m1/t009
title: Closeout
worker: worker1
status: done
estimate: 10m
depends_on: [w1/m1/t007, w1/m1/t008]
---

## Objective

Close the milestone once all other tasks are complete and the definition of done actually holds on the running dashboard.

## Steps

1. Verify the DoD end state by hand on `/ledger/:owner/:name/ask` (focus, Esc, chips, stop, retry).
2. Run `/pm done w1/m1/t009` last: set all remaining tasks `status: done`, move `tNNN.md` files to `w1/m1/done/`, mark rows `— **DONE**`, set `**Status:** done`, move `w1/m1/` to `w1/done/m1/`, and check `- [x]` in `w1/README.md`.

## Acceptance criteria

- [ ] DoD verified on a running build, not just from code reading.
- [ ] Milestone fully moved under `w1/done/` with statuses in sync across all three places.

## Out of scope

- Starting P1 work (persistence, follow-ups) — that is a separate milestone.
