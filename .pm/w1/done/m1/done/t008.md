---
id: w1/m1/t008
title: Test coverage
worker: worker1
status: done
estimate: 45m
depends_on: [w1/m1/t006]
---

## Objective

Add meaningful tests for the behavior this milestone shipped — real behavior and failure modes, no coverage gaming.

## Context

- Existing test patterns: `dashboard/src/features/ai-agent/pages/ask-ai/__tests__/` (`chat-input.test.tsx`) and `dashboard/src/features/ai-agent/components/__tests__/`; Vitest via `yarn test` in `dashboard/`.

## Steps

1. Chat-input tests: autofocus on mount (and skipped under coarse pointer), `Esc` clears, refocus after loading completes.
2. Chips tests: 3–4 rendered from the pool in empty state, click submits, hidden once messages exist.
3. Stream tests: stop aborts in-flight fetch and marks the message stopped; error renders retry; retry resubmits the same question; limit-reached shows no retry.

## Acceptance criteria

- [ ] Tests exist for each acceptance criterion of t001–t005 that can be asserted in Vitest.
- [ ] Failure modes (abort, retry) are asserted, not just happy paths.
- [ ] `yarn test` passes in `dashboard/`.

## Out of scope

- E2E/browser tests.
