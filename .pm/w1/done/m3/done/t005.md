---
id: w1/m3/t005
title: Simplify the changed sidebar code
worker: worker1
status: done
estimate: 20m
depends_on: [w1/m3/t004]
---

## Objective

Run `/simplify` over the code this milestone changed — reuse, simplification, and efficiency cleanups only; behavior-preserving.

## Context

- Changed surface: `dashboard/src/common/components/ui/sidebar.tsx`, the two sidebar render sites (t004), and any new `sidebar-state.ts` / cookie wiring.
- Look for: dead constants (e.g. the now-unused fixed `SIDEBAR_WIDTH`), duplicated clamp/parse logic, redundant state, unnecessary re-renders.

## Steps

1. Run `/simplify` scoped to the milestone's changed files.
2. Apply behavior-preserving cleanups; keep the drag-path re-render-free.

## Files

- files touched by t001–t004.

## Acceptance criteria

- [ ] No behavior change; sidebar resize + persistence still pass their checks.
- [ ] No leftover dead width constants or duplicated clamp logic.

## Out of scope

- New features or bug hunting (use /code-review separately if needed).
