---
id: w1/m3/t007
title: Closeout
worker: worker1
status: done
estimate: 10m
depends_on: [w1/m3/t006]
---

## Objective

Close the milestone once its definition of done actually holds — not merely when the code is written.

## Context

- Standing closing task. Run last, via `/pm done w1/m3/t007`, which triggers the milestone move.

## Steps

1. Verify the DoD in `w1/m3/README.md`: drag-resize works and is clamped, width + open-state persist across reload with no SSR flash, both layouts have the handle, toggle preserved.
2. Set every remaining task `status: done`, move each `tNNN.md` into `w1/m3/done/`, mark rows `— **DONE**`, set `**Status:** done`.
3. Move `w1/m3/` to `w1/done/m3/` and check `- [x] **m3**` in `w1/README.md`.

## Files

- `.pm/w1/m3/**`, `.pm/w1/README.md`.

## Acceptance criteria

- [ ] DoD observably met before the move.
- [ ] Milestone lives under `w1/done/m3/` and its box is checked.

## Out of scope

- Any deferred follow-ups become new inbox notes.
