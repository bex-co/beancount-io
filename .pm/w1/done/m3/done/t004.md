---
id: w1/m3/t004
title: Render SidebarRail in LedgerSidebar and DashboardSidebar
worker: worker1
status: done
estimate: 20m
depends_on: [w1/m3/t003]
---

## Objective

Actually surface the drag handle: `<SidebarRail />` is not rendered by either sidebar today, so the resize behavior from t003 is invisible until it is mounted.

## Context

- Ledger app nav: `dashboard/src/common/components/ledger-layout/ledger-sidebar.tsx` renders `<Sidebar>` with no `<SidebarRail>` child (~393).
- Landing nav: `dashboard/src/features/ledger-list/pages/dashboard-page/components/dashboard-sidebar.tsx` — same omission.
- Both mount the single `SidebarProvider`, so both pick up the width state automatically.

## Steps

1. Render `<SidebarRail />` inside `<Sidebar>` in `ledger-sidebar.tsx`.
2. Do the same in `dashboard-sidebar.tsx`.
3. Verify the handle appears on the sidebar's trailing edge in both layouts and that offcanvas/mobile behavior is unaffected.

## Files

- `dashboard/src/common/components/ledger-layout/ledger-sidebar.tsx`
- `dashboard/src/features/ledger-list/pages/dashboard-page/components/dashboard-sidebar.tsx`

## Acceptance criteria

- [ ] The resize handle is present and functional in the ledger app layout.
- [ ] The resize handle is present and functional in the dashboard-list landing layout.
- [ ] Mobile Sheet / offcanvas collapse behavior is unchanged.

## Out of scope

- Per-layout differing widths (not needed; shared width is fine).
