---
id: w1/m3/t006
title: Tests for width persistence + drag-resize behavior
worker: worker1
status: done
estimate: 40m
depends_on: [w1/m3/t004]
---

## Objective

Add meaningful tests asserting the real behavior this milestone ships: clamping, cookie persistence round-trip, and the drag interaction committing width once.

## Context

- Test stack mirrors the rest of `dashboard/` (Vitest + Testing Library). Reference the state/codec and the provider directly.
- Assert real behavior and failure modes — no tautological or snapshot-everything tests.

## Steps

1. Unit-test `clampSidebarWidth()` (below-min, above-max, rounding).
2. Test the persistence round-trip: set width → cookie holds it → a fresh provider reads it back; likewise the `sidebar_state` open-state read fix.
3. Test the rail drag: simulate `pointerdown` → `pointermove` → `pointerup` updates `--sidebar-width` and commits a single clamped `setWidth`; double-click still toggles.

## Files

- new test files alongside `dashboard/src/common/components/ui/` (e.g. `__tests__/sidebar-resize.test.tsx`, `__tests__/sidebar-state.test.ts`).

## Acceptance criteria

- [ ] Tests fail if clamping, persistence, or the single-commit-on-release behavior regress.
- [ ] `yarn test:unit` passes in `dashboard/`.

## Out of scope

- E2E/browser tests.
