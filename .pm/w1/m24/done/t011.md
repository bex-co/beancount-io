---
id: w1/m24/t011
title: Test coverage
worker: worker1
status: done
estimate: 45m
depends_on: [w1/m24/t009, w1/m24/t010]
---

## Objective

Pin the real-world export shapes as fixtures so a regression is caught by file shape, not by prose.

## Context

- Tests must assert real behavior and failure modes; never tautological or snapshot-everything.
- CLI tests live in `cli/tests/` and run under pytest-xdist, so every test owns its own state.

## Steps

1. Identify the behavior this milestone shipped that is not yet pinned by a test.
2. Add the missing cases, including the failure modes and the exact exit codes or payloads.
3. Confirm each new test fails against the pre-milestone behavior.

## Files

- `cli/tests/` (or the owning package's test directory)

## Acceptance criteria

- [ ] A fixture set covers a US bank export with `$1,000.00` and `(4.50)`, an EU `;`-delimited export with `1.000,00`, a tab-delimited broker export, a cp1252 file, a BOM file, blank trailer rows, a bare `ID` column, and a two-claimant header.
- [ ] Each fixture is asserted end to end: preview states, applied entries, and `bea check` green afterwards.
- [ ] Dedup is asserted across two runs of the same fixture.
- [ ] `cd cli && make check-all` passes.

## Out of scope

- Coverage-percentage targets; the bar is meaningful assertions, not a number.
