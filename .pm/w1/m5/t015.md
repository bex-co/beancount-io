---
id: w1/m5/t015
title: Closeout
worker: worker1
status: todo
estimate: 15m
depends_on: [w1/m5/t014]
---

## Objective

Close the milestone only after the observable hosted and self-hosted native OAuth outcome, security prerequisites, compatibility policy, public guidance, and tests are all real.

## Context

- Standing closing task. Run last through `/pm done w1/m5/t015`; completing it performs the mandatory board moves.
- Key rotation, audience/scope enforcement, and removal of tracked private signing material are release gates rather than deferrable cleanup.

## Steps

1. Verify every item in `w1/m5/README.md`'s definition of done against the shipped service, mobile builds, self-host stack, public documentation, and test results.
2. Confirm all implementation and standing tasks are actually complete, including the Simplify task, before changing board status.
3. Set every remaining task to `status: done`, move each task into `w1/m5/done/`, mark every row `— **DONE**`, and set the milestone status to done.
4. Move `w1/m5/` to `w1/done/m5/` and check the m5 box in `w1/README.md`.

## Files

- `.pm/w1/m5/**`
- `.pm/w1/README.md`

## Acceptance criteria

- [ ] The full definition of done is observably met before the move.
- [ ] No old OAuth signing key remains accepted and no private key material is tracked.
- [ ] All completed tasks exist only under `done/`.
- [ ] The milestone exists only at `w1/done/m5/`, says `**Status:** done`, and is checked in the workstream README.

## Out of scope

- Future native-app attestation, multi-account support, or permanent support for the legacy WebView bridge.
