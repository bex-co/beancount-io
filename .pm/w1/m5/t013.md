---
id: w1/m5/t013
title: Simplify
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m5/t012]
---

## Objective

Run `/simplify` over the milestone's changed code and consolidate authorization, discovery, token lifecycle, and compatibility logic without weakening any security boundary.

## Context

- The work spans provider configuration, shared API authorization, dashboard consent, mobile discovery, secure persistence, Apollo retry, and deployment configuration.
- Duplicate token refresh, URL construction, or scope checks create security and lifecycle races even when each copy appears correct alone.

## Steps

1. Run `/simplify` over every code path changed by m5.
2. Consolidate expected-resource construction, scope enforcement, discovery validation, token refresh, and session teardown behind one authoritative implementation per package.
3. Remove redundant component effects/state and ensure concurrent callers share token work rather than create request waterfalls.
4. Re-run focused negative and lifecycle tests after simplification.

## Files

- Backend, dashboard, mobile, and deploy files changed by `w1/m5/t001` through `t012`.

## Acceptance criteria

- [ ] Each package has one authoritative implementation for its OAuth resource, scope, discovery, and token-lifecycle responsibilities.
- [ ] Simplification preserves all milestone behavior and security failure modes.
- [ ] No package gains a cross-package runtime import.

## Out of scope

- Refactoring unrelated authentication, Apollo, or deployment code.
