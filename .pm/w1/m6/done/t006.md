---
id: w1/m6/t006
title: Adoption surface
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m6/t004, w1/m6/t005]
---

## Objective

Make the changed sign-up contract discoverable wherever a user, self-hoster, or coding agent would look for it.

## Context

- `mobile/README.md` "Mobile OAuth contract" describes the authorization request; it must mention `screen_hint=signup`.
- `backend-cluster/backend-v2/README.md` documents the provider; self-hosters need to know the parameter is accepted and forwarded.
- Root `README.md` / `CLAUDE.md` package tables and `skills/CLAUDE.md` are unaffected unless something new is user-visible there.

## Steps

1. Update the mobile README contract and the backend-v2 README with the new parameter and the signed-in-browser choice.
2. Walk the README instructions as written against the shipped flow.
3. Confirm `AGENTS.md` symlinks and `python3 scripts/check-agent-guidance.py` still pass if any `CLAUDE.md` changed.

## Files

- `mobile/README.md`
- `backend-cluster/backend-v2/README.md`

## Acceptance criteria

- [x] Both READMEs describe `screen_hint=signup` and the account-choice behavior accurately.
- [x] No other surface (package tables, skills table, symlinks) has drifted; any drift is filed as follow-up.

## Out of scope

- Store listing copy under `mobile/metadata/`.
