---
id: w1/m6/t009
title: Closeout
worker: worker1
status: todo
estimate: 15m
depends_on: [w1/m6/t008]
---

## Objective

Close the milestone once its definition of done is actually met — verified in the iOS simulator against the hosted service, not merely code-complete.

## Context

- Verification is read-only until registration; creating a test account writes to the hosted service, so ask before doing it and remove what you created afterwards.
- Follow the `done` subcommand: mark every task done, move `tNNN.md` files to `w1/m6/done/`, set `**Status:** done`, move `w1/m6/` to `w1/done/m6/`, and check the box in `w1/README.md`.

## Steps

1. Walk every bullet of the milestone's definition of done in the simulator (fresh sign-up, sign-in, signed-in-browser choice, welcome copy in light and dark).
2. Run `/pm done w1/m6/t009` last.

## Files

- `.pm/w1/m6/`, `.pm/w1/README.md`

## Acceptance criteria

- [ ] Every DoD bullet observed to hold.
- [ ] Milestone exists only at `.pm/w1/done/m6/` with `**Status:** done` and the workstream checkbox checked.

## Out of scope

- Anything not in this milestone's DoD.
