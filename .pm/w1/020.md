# Filtered balance keeps unrelated totals and valuation requirements

**Major · 40–60 minutes.** CLI-QA-006.

Use this synthetic main.bean:
```beancount
option "operating_currency" "USD"
2026-01-01 open Assets:Checking USD
2026-01-01 open Assets:Savings USD
2026-01-01 open Equity:Opening USD
2026-02-01 * "Opening"
  Assets:Checking 100 USD
  Assets:Savings 200 USD
  Equity:Opening -300 USD
```

```sh
uv run bea --json --file /absolute/qa/main.bean balance Checking
```
Exit 0, only child Assets:Checking 100 USD remains visible, but its Assets
parent balance_children is 300 USD and account_filter is null. Expected: totals
and scope match the selected account subtree. Querying Checking independently
returns 100. Earlier repeated controls also show an unrelated unpriced BTC
holding can make the USD Checking balance fail before pruning.

Cause: commands/report.py balance alias performs valuation before _prune_tree;
pruning removes children but retains their parent's precomputed totals and
unfiltered metadata. Scope inventory before valuation and recompute ancestor
aggregates for the retained subtree; report the actual filter. Do not change
report --account transaction-level regex semantics.

Acceptance: fixture parent and child both show 100 USD; unrelated commodities
do not require prices for this selection; whole balance remains 300. Include
parent/child and closed-account regressions: prior controls show closing a
parent can hide a still-open funded child. Verify historical time filters and
explicit include-closed behavior where supported. Avoid adding child balances
twice when recomputing ancestors.
Dedupe: current full-board filter results and completed m15 balance-alias work
reviewed; no matching open CLI note. This is a separate path from w1/018.

Revalidated 2026-09-10, HEAD 4574a7d8, checkout uv run bea on macOS arm64.
Synthetic files, isolated child config/cache, closed stdin, no credentials or
production calls. Run commands from cli/. A1/A2: predictable commands and
machine-readable accounting results. No backend change needed.
