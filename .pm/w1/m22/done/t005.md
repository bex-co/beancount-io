---
id: w1/m22/t005
title: "Simplify the CLI parity fixes"
worker: worker1
status: done
estimate: 20m
depends_on: [w1/m22/t004]
---

## Objective

Review the changed code with /simplify while preserving the corrected behavior.

## Context

Reuse the existing launcher, envelope and upstream rendering; avoid duplicating the shell or option inventories unnecessarily.

## Steps

1. Run /simplify on the milestone changes and remove needless duplication/indirection.
2. Re-run focused checks after any resulting edits.

## Files

- CLI files changed by t001–t003

## Acceptance criteria

- [x] The fixes reuse existing boundaries and have no unnecessary parallel implementations; focused behavioral checks still pass.

## Out of scope

- Independent engine PyPI packaging, unrelated cloud/API changes, and modifying customer ledgers.

## Completion evidence

Implemented and reviewed in this milestone. `cli/tests/test_cli_parity_qa.py` passes all 16 regression cases, including the real PTY sequence. `make check-all`: 648 passed; installed wheel (Python 3.12) and sdist (Python 3.14/pip): 80 smoke commands each. Upstream doctor help is generated from its command registry. Reuse, quality and efficiency reviews completed; documentation and generated reference updated. CI and final board closeout remain t006/t007.
