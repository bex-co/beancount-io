---
id: w1/m15/t008
title: Test coverage
worker: worker1
status: todo
estimate: 45m
depends_on: [w1/m15/t006]
---

## Objective

Pin each ergonomic behavior with a test that fails when it regresses.

## Context

- `cli/tests/test_command_tree.py` pins the command tree and help; `cli/tests/test_output.py` covers table rendering; `cli/tests/test_customer_workflows.py` runs end-to-end flows.

## Steps

1. Filters: search/tag/link matching, composition with `--account`, JSON envelope unchanged.
2. `bea balance`: pruned tree content, no-argument equivalence with trial balance, JSON shape.
3. Positional narration: value written, conflict exits 2.
4. Tables: width cap under a fake terminal size, piped output unchanged.
5. Small fixes: `format` with `--file`, stdin batch, help first lines under 60 characters, absolute `Next:` path, plural and wording strings.

## Files

- `cli/tests/test_command_tree.py`, `test_output.py`, `test_customer_workflows.py`, `test_import.py`

## Acceptance criteria

- [ ] Reverting any of t001–t005 fails at least one new test.
- [ ] `make test` passes.

## Out of scope

- Snapshotting whole tables.
