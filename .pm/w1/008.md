# USAGE understates `--json` for `cloud ledger show` / create / delete

**Severity:** minor  
**Package:** `cli` · docs

## Repro / evidence

`docs/USAGE.md` § JSON output: read-side list names only `cloud status` and `cloud ledger list`. Live `bea --json cloud ledger show|create|delete` already emit the standard envelope (tick-1 captures under `cli/tmp/qa-20260910/cloud-run-1/evidence/`).

The same section lists write envelopes for `init` / `import` / `format` / `add` but not the hosted create/delete successes.

## Expected

Document every cloud command that emits JSON success (at least `show`; create/delete as write envelopes) beside the existing logout/clone exceptions.

## Fix

Update `docs/USAGE.md` (and regenerate reference only if help text changes). No behavior change required unless intentionally disabling JSON on those commands.

## Estimate

~15m
