---
id: w1/m18/t007
title: Test coverage
worker: worker1
status: todo
estimate: 45m
depends_on: [w1/m18/t005]
---

## Objective

Pin the converged behavior with evals and CI checks that fail if a skill drifts back to direct appends or a different template.

## Context

- Skills carry `evals.json` and fixture ledgers checked by `skills/scripts/ci-check.py`; `.github/workflows/ci-skills.yml` runs it.

## Steps

1. Add evals: init produces the thirteen-account template (both paths); import re-run is a no-op via `bea`; reconcile uses `--pad-from` for an adjustment.
2. Add a CI assertion that no `beancount-*` skill contains a raw "append to the ledger file" instruction outside the documented fallback block.
3. Add a `ci-check.py` unit test for the `bea` resolution path.

## Files

- `skills/.claude/skills/*/evals.json`, `skills/scripts/ci-check.py`, `skills/scripts/` tests

## Acceptance criteria

- [ ] Reverting t001, t002, or t003 fails at least one eval or CI check.
- [ ] Skills CI passes.

## Out of scope

- Live-model evals.
