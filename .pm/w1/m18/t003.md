---
id: w1/m18/t003
title: "beancount-reconcile, beancount-close, and beancount-ask prefer bea for checks, pads, and reads"
worker: worker1
status: todo
estimate: 45m
depends_on: [w1/m18/t002]
---

## Objective

Point the remaining stateful and read-only skills at the CLI's validated primitives.

## Context

- `beancount-reconcile/SKILL.md` appends missing entries and a `balance` assertion, then runs `bean-check`; its opening-adjustment guidance predates `bea add balance --pad-from`.
- `beancount-close/SKILL.md` delegates to reconcile and verifies with `bean-check`.
- `beancount-ask/SKILL.md` already lists `bea --file <ledger> query` as its second tool; after m15 `bea --json balance` and `list transaction --search` exist.

## Steps

1. Reconcile: write missing entries with `bea add transactions --from -`, the period-end assertion with `bea add balance`, and an explicit opening adjustment with `bea add balance --pad-from`; verify with `bea check`; keep the bean-* fallback.
2. Close: use `bea check` and `bea report income-statement --time <month>` for the summary it commits.
3. Ask: make `bea --json query` and `bea --json balance` the first tools, `bean-query` the fallback; mention `--search` for lookups.
4. State the prefer/fallback rule once per skill.

## Files

- `skills/.claude/skills/beancount-reconcile/SKILL.md`, `beancount-close/SKILL.md`, `beancount-ask/SKILL.md`, their `references/` and `evals/`

## Acceptance criteria

- [ ] Each edited skill names `bea` first and the bean-* tool as fallback exactly once.
- [ ] Reconcile's opening-adjustment path uses `--pad-from`.
- [ ] Skills CI passes.

## Out of scope

- `beancount-options` and `beancount-migrate`.
