---
id: w1/m18/t004
title: "Skills CI runs bea check beside bean-check; skills/CLAUDE.md names the shared rails"
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m18/t003]
---

## Objective

Make the CI gate for skills exercise the same validator the skills now call, and document the rule in one place.

## Context

- `skills/scripts/ci-check.py::find_bean_check` resolves `bean-check` directly or through `uv run --project cli bean-check`; `check_ledgers` runs it on every `*ledger.beancount` and expects listed failure-mode fixtures to fail.
- `skills/CLAUDE.md` describes the trust rails (propose-then-confirm, existing accounts only, `bean-check` gate, `import-id` convention).

## Steps

1. In `ci-check.py`, resolve `bea` the same way (`uv run --project cli bea`) and run `bea --file <fixture> check` on each fixture, requiring the same pass/fail set as `bean-check`.
2. In `skills/CLAUDE.md`, state the rule: skills prefer `bea` for writes, checks, and JSON reads when installed and fall back to bean-*; list the primitives.
3. Run `python3 scripts/check-agent-guidance.py`.

## Files

- `skills/scripts/ci-check.py`, `skills/CLAUDE.md`, `.github/workflows/ci-skills.yml` (only if the CLI sync step changes)

## Acceptance criteria

- [ ] CI output shows `OK bea check` lines for every fixture and the failure-mode fixtures fail under both tools.
- [ ] Guidance check passes.

## Out of scope

- New fixtures.
