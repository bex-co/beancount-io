---
id: w1/m16/t006
title: Simplify
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m16/t005]
---

## Objective

Run `/simplify` over the importer and mapping code; behavior-preserving only.

## Context

- Touched: `cli/src/cli/commands/import_.py` and any helper module it gained.

## Steps

1. `/simplify` on the milestone diff; look for duplication with `cli/docs/examples/csv_importers.py` parsing.
2. `make check-all` from `cli/`.

## Files

- As listed.

## Acceptance criteria

- [ ] No behavior change; t007 tests pass.

## Out of scope

- New features.
