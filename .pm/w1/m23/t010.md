---
id: w1/m23/t010
title: Test coverage
worker: worker1
status: todo
estimate: 45m
depends_on: [w1/m23/t008, w1/m23/t009]
---

## Objective

Pin the exit contract with one matrix test that replays every absorbed reproducer, so a future change cannot quietly restore a false green.

## Context

- Tests must assert real behavior and failure modes; never tautological or snapshot-everything.
- CLI tests live in `cli/tests/` and run under pytest-xdist, so every test owns its own state.

## Steps

1. Identify the behavior this milestone shipped that is not yet pinned by a test.
2. Add the missing cases, including the failure modes and the exact exit codes or payloads.
3. Confirm each new test fails against the pre-milestone behavior.

## Files

- `cli/tests/` (or the owning package's test directory)

## Acceptance criteria

- [ ] A single parameterized test covers every reproducer from the absorbed notes and asserts exit code plus on-disk effect for each.
- [ ] The destination-alias cases assert the ledger's hash is unchanged after the refusal.
- [ ] Each new case fails against the pre-milestone behavior.
- [ ] `cd cli && make check-all` passes.

## Out of scope

- Coverage-percentage targets; the bar is meaningful assertions, not a number.
