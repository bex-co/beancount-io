---
id: w1/m23/t008
title: Adoption surface
worker: worker1
status: todo
estimate: 25m
depends_on: [w1/m23/t001, w1/m23/t002, w1/m23/t003, w1/m23/t004, w1/m23/t005, w1/m23/t006, w1/m23/t007]
---

## Objective

Verify the exit contract is discoverable everywhere it is promised: the usage guide's exit-code table, the generated reference, and the customer skill text that tells agents to branch on exit status.

## Context

- Root `CLAUDE.md` compatibility rules: every `AGENTS.md` is a relative symlink to its `CLAUDE.md`, and root `.claude/skills` links to `../.agents/skills`.
- Customer skill catalog: `skills/CLAUDE.md`; customer skills live in `skills/.claude/skills/beancount-*`.
- `cli/docs/REFERENCE.md` is generated — run `make docs`, never hand-edit.

## Steps

1. State the rule beside the exit-code table in `cli/docs/USAGE.md`: exit 0 means the documented effect happened and the ledger still checks.
2. Run `make docs` so `cli/docs/REFERENCE.md` picks up any changed help or new flags, and confirm `make docs-check` is green.
3. Re-read the `--allow-errors` guidance in `skills/CLAUDE.md` and the `beancount-*` skills; correct anything that now over-promises.
4. Run `python3 scripts/check-agent-guidance.py` and confirm the behavior reads the same for Claude Code and Codex.

## Files

- cli/docs/USAGE.md
- cli/docs/REFERENCE.md
- skills/CLAUDE.md
- skills/.claude/skills/

## Acceptance criteria

- [ ] `cli/docs/USAGE.md` states the exit-0 rule and every changed operation's contract.
- [ ] `make docs-check` passes and the generated reference matches the command tree.
- [ ] No skill instructs an agent to rely on behavior this milestone changed.
- [ ] `python3 scripts/check-agent-guidance.py` passes.

## Out of scope

- Rewriting unrelated documentation sections.
- Shipping a release; this milestone lands on `main` only.
