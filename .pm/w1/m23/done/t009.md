---
id: w1/m23/t009
title: Simplify
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m23/t008]
---

## Objective

Run `/simplify` over the code this milestone changed: reuse, simplification, efficiency, altitude. Behavior-preserving only — no fix, no new capability.

## Context

- `/simplify` is quality-only; correctness findings belong to `/code-review`.
- Scope is the diff produced by this milestone's implementation tasks.

## Steps

1. Run `/simplify` over the changed files.
2. Apply the findings that are genuinely behavior-preserving.
3. Re-run the owning package's checks and confirm the suite is still green.

## Files

- cli/src/cli/
- cli/src/bea_engine/

## Acceptance criteria

- [ ] Every applied change is behavior-preserving and the package's full check gate passes.
- [ ] Findings deliberately not applied are named with a one-line reason.

## Out of scope

- Behavior changes, new features, and bug fixes — those belong to the implementation tasks.

## Simplify notes

Applied (all behavior-preserving, full gate green):

- `doctor.py`: hoisted the 7 identical `refuse_json` calls into `_refuse_json()`.
- `doctor.py`: extracted `_replayed()` for the capture/replay/exit-nonzero pattern shared by directories, scoped, and missing-open.
- `write.py`: merged the double `LedgerError` raise into a conditional message plus one raise.
- `format.py`: extracted `_problem_parts()` for the tallies shared by `_check_message` and `_problems_message`.
- `bea_engine/query.py`: extracted `_find_alias()` shared by `_refuse_alias` and the interactive `do_output` guard.
- `example.py`: dropped the redundant `not startswith("--")` clause (implied by `startswith("-o")`).

Deliberately not applied:

- treeify/doctor replay duplication: a forced union crosses altitude (process plumbing vs rendering), and each copy's flush behavior fits its own path.
- `_same_file` duplicated in frontend and engine: separate processes; sharing would violate the frontend/engine boundary.
- Closure double walks in format `_targets`: consolidating would reorder missing-include lines when named order differs from sorted order.
- `_register`'s `assert forwarder is not None`: mypy narrowing for the closure, not dead code.
- `_replay_without_congratulations`: single caller; a filter parameter would complicate the shared path.
- `_check_message` early return: load-bearing (different suffix from the general path).
- Test `_bea`/`_env` per-file helpers: repo convention; a shared helper exceeds milestone scope.
- `models.py` field repetition: explicit per-model beats an `Annotated` alias for greppability.
- `reader.py` bool guard: explicit impossible-input guard; removal changes the `True`-lineno path.
- `list.py` conditional column unpacking: keeps headers and cells aligned in one expression each.
