---
id: w1/m17/t004
title: "First-month tutorial: init, accounts, import, reconcile, month report"
worker: worker1
status: todo
estimate: 60m
depends_on: [w1/m17/t003]
---

## Objective

Give a newcomer one narrative that ends with a closed month, using only commands that exist.

## Context

- Steps the review found scattered across four documents: `bea init` with a real history date; `bea add open` for the accounts the template lacks; a CSV import with `--csv` and `--rules` (m16) or the example importer; `bea add balance --pad-from` against a statement balance; `bea list transaction --flag '!'` as the review queue; `bea report income-statement --time month` and `bea report balance-sheet`.
- The tutorial ships as a `bash`-fenced document so t003 runs it.

## Steps

1. Write `cli/docs/TUTORIAL.md` in second person with one section per week: set up, record daily purchases, import the month's export, reconcile against the statement, read the month.
2. Use a small CSV checked in under `cli/docs/examples/` so the import step runs offline.
3. Link it from the README "Learn more" list and from `cli/docs/USAGE.md`'s top.

## Files

- `cli/docs/TUTORIAL.md` (new), `cli/docs/examples/first-month.csv` (new)
- `cli/README.md`, `cli/docs/USAGE.md`

## Acceptance criteria

- [ ] The tutorial passes under t003's runner from a clean temporary directory.
- [ ] It never asks the reader to write Python or BQL.

## Out of scope

- Hosted or AI steps.
