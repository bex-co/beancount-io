---
id: w1/m17/t007
title: Test coverage
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m17/t005]
---

## Objective

Pin the generator's completeness and the example runner's failure modes.

## Context

- The generator and runner are themselves test infrastructure; they need tests that prove they catch drift.

## Steps

1. Generator: every leaf in the command tree has a section; a renamed option produces a diff; the drift check exits nonzero on a stale file.
2. Runner: a fence with a failing command reports document and line; a `norun` fence without a reason fails; expectations table entries must reference an existing fence.

## Files

- `cli/tests/test_docs_examples.py`, `cli/tests/test_release.py` (if the Makefile target list is pinned there)

## Acceptance criteria

- [ ] Reverting t001 or t003 fails at least one test.
- [ ] `make test` passes.

## Out of scope

- Testing prose.
