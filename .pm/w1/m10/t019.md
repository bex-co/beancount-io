---
id: w1/m10/t019
title: "Expose self profile and public configuration reads"
worker: worker1
status: todo
estimate: 60m
depends_on: [w1/m10/t003]
---

## Objective

Complete the remaining self-profile and public configuration contracts without introducing an artificial ledger requirement.

## Context

- Follow this milestone's README.md parity contract and COVERAGE.md baseline map, including after the milestone is archived.
- Baseline operations: `Query.userProfile`, `Query.allTierQuotas`, `Query.health`, `Query.featureFlags`.
- Read the owning package's CLAUDE.md before implementation. Keep current credential authority and named structural exceptions unless the milestone contract explicitly resolves a contradiction.

## Steps

1. Add adapters for current-user profile, pricing quota catalog, health, and feature flags using their established service/public boundaries.
2. Preserve exact-self profile access and each public operation's visibility; distinguish MCP transport authentication from the operation's public classification.
3. Test field completeness, anonymous REST behavior where established, authenticated MCP use, defaults, and source errors.

## Files

- `backend-cluster/backend-v2/src/features/auth/`
- `backend-cluster/backend-v2/src/features/healthz/`
- `backend-cluster/backend-v2/src/features/feature-usage/`
- `backend-cluster/backend-v2/src/features/ai-agent/api/`
- `backend-cluster/backend-v2/src/server/api/op-class.ts`

## Acceptance criteria

- [ ] All four baseline operations have executable contract mappings.
- [ ] Public endpoint aliases count only when their payload matches the recorded operation contract.
- [ ] Profile access remains exact-self and account-scoped.

## Out of scope

- Other task families in this milestone; broad rewrites of established domain services.
- Removing eligible operations, widening credential authority, or changing exclusions merely to make parity counts pass.
