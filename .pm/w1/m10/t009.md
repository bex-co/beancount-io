---
id: w1/m10/t009
title: "Expose user SSH public-key reads and mutations"
worker: worker1
status: todo
estimate: 60m
depends_on: [w1/m10/t003]
---

## Objective

Add the four missing public-key management contracts without confusing user-owned keys with ledger files or API secrets.

## Context

- Follow this milestone's README.md parity contract and COVERAGE.md baseline map, including after the milestone is archived.
- Baseline operations: `Query.listPublicKeys`, `Query.getPublicKey`, `Mutation.createPublicKey`, `Mutation.deletePublicKey`.
- Read the owning package's CLAUDE.md before implementation. Keep current credential authority and named structural exceptions unless the milestone contract explicitly resolves a contradiction.

## Steps

1. Reuse the protected user public-key service for list/get/create/delete through REST and MCP.
2. Match input validation, uniqueness, key identifiers, response fields, exact-self ownership, and credential ceilings.
3. Test authorized outcomes and cross-user denial for every branch, including a credential that is ledger-pinned.

## Files

- `backend-cluster/backend-v2/src/features/gitea/`
- `backend-cluster/backend-v2/src/features/ai-agent/api/`
- `backend-cluster/backend-v2/src/server/api/op-class.ts`

## Acceptance criteria

- [ ] All four key operations have working adapters and matching domain results.
- [ ] Only the caller's current authority can manage keys; arbitrary user IDs are not an authorization bypass.
- [ ] No private keys or bearer credentials enter API examples, snapshots, or board evidence.

## Out of scope

- Other task families in this milestone; broad rewrites of established domain services.
- Removing eligible operations, widening credential authority, or changing exclusions merely to make parity counts pass.
