---
id: w1/m10/t013
title: "Complete journal variants and source-file reads"
worker: worker1
status: todo
estimate: 60m
depends_on: [w1/m10/t006]
---

## Objective

Make journal and source-file contracts available without losing ordering, entry context, text representation, or pagination.

## Context

- Follow this milestone's README.md parity contract and COVERAGE.md baseline map, including after the milestone is archived.
- Baseline operations: `Query.getLedgerJournal`, `Query.getLedgerPlaintextJournal`, `Query.getLedgerAccountJournal`, `Query.journalEntries`, `Query.getLedgerSourceFiles`.
- Read the owning package's CLAUDE.md before implementation. Keep current credential authority and named structural exceptions unless the milestone contract explicitly resolves a contradiction.

## Steps

1. Implement or explicitly bind current and legacy journal variants through existing services, preserving their input/output differences.
2. Carry account/time/filter arguments, ordering and page boundaries through REST and MCP; expose source-file metadata separately from a directory listing when their shapes differ.
3. Test adjacent pages for missing/duplicated entries, text and structured representations, empty results, and revoked access.

## Files

- `backend-cluster/backend-v2/src/features/ledger/api/`
- `backend-cluster/backend-v2/src/features/ledger/service/`
- `backend-cluster/backend-v2/src/features/ai-agent/api/`

## Acceptance criteria

- [ ] All five baseline operations have tested equivalent bindings.
- [ ] A generic paginated journal route counts for an alias only when required filtering and output mapping are proven.
- [ ] Responses have a documented bounding/pagination strategy without silently truncating an existing contract.

## Out of scope

- Other task families in this milestone; broad rewrites of established domain services.
- Removing eligible operations, widening credential authority, or changing exclusions merely to make parity counts pass.
