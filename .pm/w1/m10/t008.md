---
id: w1/m10/t008
title: "Expose ledger creation, updates, and deletion"
worker: worker1
status: todo
estimate: 60m
depends_on: [w1/m10/t003, w1/m10/t004]
---

## Objective

Let authorized REST and MCP clients manage ledger lifecycle through the established application boundary.

## Context

- Follow this milestone's README.md parity contract and COVERAGE.md baseline map, including after the milestone is archived.
- Baseline operations: `Mutation.createLedger`, `Mutation.updateLedger`, `Mutation.deleteLedger`.
- Read the owning package's CLAUDE.md before implementation. Keep current credential authority and named structural exceptions unless the milestone contract explicitly resolves a contradiction.

## Steps

1. Add create/update/delete adapters with shared validation and operation-specific service/workflow dispatch.
2. Keep creation's user-scoped target distinct from an existing-ledger mutation; preserve credential-pin, owner, collaborator, quota, and destructive cleanup rules.
3. Test resulting repository state and cleanup, wrong-target denial, partial failure handling, and compatibility with the GraphQL caller.

## Files

- `backend-cluster/backend-v2/src/features/ledger/api/`
- `backend-cluster/backend-v2/src/features/ledger/service/`
- `backend-cluster/backend-v2/src/features/ledger/workflow/`
- `backend-cluster/backend-v2/src/features/ai-agent/api/mcp-tools.ts`

## Acceptance criteria

- [ ] All three lifecycle operations run through each eligible adapter with equivalent outcomes.
- [ ] Read/write credentials cannot acquire administrative authority from a grouped tool.
- [ ] Denied creation/deletion performs no repository or bank side effects.

## Out of scope

- Other task families in this milestone; broad rewrites of established domain services.
- Removing eligible operations, widening credential authority, or changing exclusions merely to make parity counts pass.
