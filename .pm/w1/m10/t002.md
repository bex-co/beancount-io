---
id: w1/m10/t002
title: "Make grouped operation mappings and parity contracts executable"
worker: worker1
status: todo
estimate: 60m
depends_on: [w1/m10/t001]
---

## Objective

Extend the existing registry and test infrastructure so a mapped operation proves an executable adapter branch and an equivalent contract.

## Context

- Follow this milestone's README.md parity contract and COVERAGE.md baseline map, including after the milestone is archived.
- Read the owning package's CLAUDE.md before implementation. Keep current credential authority and named structural exceptions unless the milestone contract explicitly resolves a contradiction.

## Steps

1. Reuse current feature descriptors, composition root, and operation table; model grouped MCP discriminator branches explicitly instead of treating one registered tool name as proof for every verb.
2. Keep reads discoverable as resources and group tools only when their operations have coherent inputs and compatible operational classes; each selected operation calls its exact protected service.
3. Build contract-test helpers that execute HTTP REST handlers, GraphQL operations, and an MCP client against one controlled fixture. Compare normalized domain data and effects while allowing protocol envelopes to differ.

## Files

- `backend-cluster/backend-v2/src/server/api/op-class.ts`
- `backend-cluster/backend-v2/src/server/api/composition-root.ts`
- `backend-cluster/backend-v2/src/server/api/__tests__/`
- `backend-cluster/backend-v2/src/features/ai-agent/api/`

## Acceptance criteria

- [ ] A test fails when a grouped verb's branch is absent, unreachable, or mapped to the wrong canonical action.
- [ ] Runtime registrations, input/output schemas, and the contract inventory can be checked together.
- [ ] Tests do not replace REST routing with direct service calls or truncate inputs to MCP's current subset.

## Out of scope

- Other task families in this milestone; broad rewrites of established domain services.
- Removing eligible operations, widening credential authority, or changing exclusions merely to make parity counts pass.
