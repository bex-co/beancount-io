---
id: w1/m10/t021
title: "Expose public social discovery and ledger starring"
worker: worker1
status: todo
estimate: 60m
depends_on: [w1/m10/t003, w1/m10/t002]
---

## Objective

Reach the six eligible social operations while preserving public response shapes and the caller's own starring authority.

## Context

- Follow this milestone's README.md parity contract and COVERAGE.md baseline map, including after the milestone is archived.
- Baseline operations: `Query.getUserProfile`, `Query.getUserFollowers`, `Query.getUserFollowing`, `Query.getUserStarredRepos`, `Mutation.starLedger`, `Mutation.unstarLedger`.
- Read the owning package's CLAUDE.md before implementation. Keep current credential authority and named structural exceptions unless the milestone contract explicitly resolves a contradiction.

## Steps

1. Add public profile/follower/following/starred-repository read adapters with exact target, pagination, and visibility semantics.
2. Expose star/unstar through the existing protected service and explicit selected ledger.
3. Test public/private response boundaries, own-star effects, malformed targets, and absent users. Keep private follow/account-edit operations outside the frozen target.

## Files

- `backend-cluster/backend-v2/src/features/gitea/`
- `backend-cluster/backend-v2/src/features/ledger/api/`
- `backend-cluster/backend-v2/src/features/ai-agent/api/`

## Acceptance criteria

- [ ] All six baseline operations have faithful bindings and tests.
- [ ] Public discovery never returns private enrichment or inaccessible repositories.
- [ ] Starring changes only the authenticated caller's relationship.

## Out of scope

- Other task families in this milestone; broad rewrites of established domain services.
- Removing eligible operations, widening credential authority, or changing exclusions merely to make parity counts pass.
