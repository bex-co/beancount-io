---
id: w1/m10/t015
title: "Complete document and archive download contracts"
worker: worker1
status: todo
estimate: 60m
depends_on: [w1/m10/t003, w1/m10/t002]
---

## Objective

Provide usable MCP and REST document/archive access with equivalent target selection, authorization, and delivery behavior.

## Context

- Follow this milestone's README.md parity contract and COVERAGE.md baseline map, including after the milestone is archived.
- Baseline operations: `Query.getLedgerAssetDownloadUrl`, `Query.getLedgerArchiveDownloadUrl`, `ledger.downloadArchive`, `ledger.downloadArchive.legacy`.
- Read the owning package's CLAUDE.md before implementation. Keep current credential authority and named structural exceptions unless the milestone contract explicitly resolves a contradiction.

## Steps

1. Map URL-discovery and byte-download operations separately. Choose an MCP blob or resource-link contract that a real client can consume while preserving authorization and archive selection.
2. Preserve legacy archive aliases, expiry and filename/content-type behavior, path validation, and the current shared archive rate bucket.
3. Exercise the complete download path, comparing archive bytes/content and failures. Keep GraphQL's existing raw-byte exclusion explicit rather than claiming a JSON envelope streams an archive.

## Files

- `backend-cluster/backend-v2/src/features/ledger/api/rest/v1/`
- `backend-cluster/backend-v2/src/features/ledger/api/`
- `backend-cluster/backend-v2/src/features/ai-agent/api/`
- `backend-cluster/backend-v2/src/server/api/rate-limit.ts`

## Acceptance criteria

- [ ] All four operations have real client-usable delivery on eligible surfaces.
- [ ] A resource link counts only when its authorized fetch and equivalent content are tested end to end.
- [ ] No public/private asset leak, unsafe path, stale grant, or expired URL succeeds.

## Out of scope

- Other task families in this milestone; broad rewrites of established domain services.
- Removing eligible operations, widening credential authority, or changing exclusions merely to make parity counts pass.
