---
id: w1/m7/t004
title: Verify every path end to end in the iOS simulator against the hosted service
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m7/t002, w1/m7/t003]
---

## Objective

See the no-consent flow actually run on a device before anything is documented as shipped.

## Context

- `mobile/CLAUDE.md` "Verifying in the iOS simulator" — launch, deep link, screenshot, `expo-mcp` taps; typing via the Simulator keyboard.
- Fresh sign-up creates a real account on the hosted service: ask before doing it and remove the account afterwards if a self-serve path exists; otherwise use an existing test account for sign-in and the signed-in-browser case.

## Steps

1. Fresh sign-in: no approve screen, lands in the app.
2. Signed-in browser (second launch after logout with the cookie kept): one tap, lands in the app.
3. Dismiss the browser mid-flow: back on welcome, no session.
4. Fresh sign-up (with permission): register → OTP → app, no approve screen.
5. Capture light and dark screenshots of the "Returning to Beancount…" state.

## Files

- None (verification); attach screenshots to the PR.

## Acceptance criteria

- [ ] Each of the four paths observed to behave as the milestone DoD states.
- [ ] No Approve/Cancel pair or scope list appeared anywhere.

## Out of scope

- Android device verification (no emulator workflow documented yet).
