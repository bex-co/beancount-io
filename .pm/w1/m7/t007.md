---
id: w1/m7/t007
title: Test coverage
worker: worker1
status: todo
estimate: 45m
depends_on: [w1/m7/t005]
---

## Objective

Add meaningful tests for the shipped behavior and its failure modes.

## Context

- The dashboard has no component tests for the interaction pages; the auto-submit and retry paths are new behavior with a real failure mode (POST rejected, network down).

## Steps

1. Dashboard component test: `authenticated` triggers exactly one POST with `decision=approve` and the interaction `scope`; a failed POST shows retry and preserves `uid`/`scope`; the signed-in `continue` step posts once on tap.
2. Backend: any remaining gap around login-result-only completion or scope mismatch rejection for the mobile client.

## Files

- `dashboard/src/features/oauth/**/__tests__/`
- `backend-cluster/backend-v2/src/features/oauth/api/__tests__/oidc-route.test.ts`

## Acceptance criteria

- [ ] Tests assert real behavior and failure modes; no tautological or snapshot-everything tests.
- [ ] Dashboard and backend test gates pass.

## Out of scope

- Device automation.
