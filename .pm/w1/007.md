The `beancount-import` skill still tells agents a bank CSV needs an importer
before `bea import` works, and `beancount-importer-author` triggers on any
bank CSV without one — but m16 shipped `bea import --csv` (column mapping),
`--rules` categorization with the `!` flagged queue, and remembered mappings,
so the no-code path is now the entry ticket and a Python importer is the
advanced path. Update both skills' triggers and walkthroughs to try `--csv`
first. Pick up in m18 (skills converge on `bea`), which already owns skill
edits — do not fix here.
