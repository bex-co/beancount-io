# Complete localized Errors table labels

**Severity:** minor. **Estimate:** 30 minutes. **Owner:** dashboard. **A2/A3:** localized error navigation should identify the diagnostic and source line in the reader's selected language.

Production `https://beancount.io`, signed out, isolated headless Chromium149.0.7827.55, light theme. Local HEAD26d54a9d, fetched main bd5d1d31; production main-BIr0jJni.js, exact deployed SHA unknown.

Open `/ledger/open_ledger/crypto-example/errors?lang=zh`. Fresh390×844 and1440×1000 loads have html.lang=zh, correctly localized navigation, and a populated nine-error table. Its headers are `错误 Message / 文件名 / Line`; source cells say `Line 35` through `Line 43`. At390px the Spanish route `?lang=es` similarly shows `Mensaje de Error / Nombre de archivo / Line` and English Line prefixes. Japanese1440 is a working control: `エラーメッセージ / ファイル名 / 行`, with `行 35`.

The actual GetLedgerErrors HTTP200 returns nine valid source locations in prices/live.bean, lines35–43, with managed-price diagnostic messages. These raw ledger diagnostics are separate from the dashboard's authored column headings and line prefixes; their English text is not the subject of this finding. File paths, commodity identifiers and source line numbers must remain literal and unchanged.

Root: `dashboard/src/features/ledger-data/errors/locales/zh.ts` stores `page.errors.errorMessage` as `错误 Message` and `page.errors.line` as `Line`; the Spanish catalog also stores Line. The renderer correctly uses those keys in the loading headings (`errors/index.tsx:68,70`), settled headings (`:116,122`), and source line cells (`:169`). There is no locale-selection or API failure.

Catalog audit: the line key is still English in12 non-English catalogs: bg,ca,de,es,fa,fr,nl,pt,ru,sk,uk,zh. ja/ko are translated and en is correct. The error-message heading is also unfinished in ca/fr (`Error Message`) and ru/uk/zh (`Ошибка Message`, `Помилка Message`, `错误 Message`). Those catalog values are source-verified; only Chinese, Spanish and the Japanese control were exercised live.

Fix these existing human-facing labels in the affected catalogs, using natural terminology for an error message and a source-code line. Preserve the existing keys and their use in both loading and settled tables; keep the Japanese/Korean/English controls, raw diagnostics, line numbers and keyboard source navigation. No new language, dependency, backend translation or API change. Do not mechanically ban shared words or translate source syntax.

Acceptance: fresh Chinese desktop/narrow and Spanish narrow reproduce fully localized headings and line prefixes, while the same nine errors still point to the same source lines. Verify the other affected catalog entries and preserve the Japanese control. Use a focused real-catalog rendered assertion for the reported table labels, rather than merely asserting key existence or snapshotting every translation; run normal dashboard checks when implementing.

Dedupe/history: all board states searched for Errors localization, these keys and literal mixed strings. Existing m14 repairs Catalan shared navigation/Journal;100 repairs Russian/Ukrainian Journal headers;111 repairs profile social labels. None includes the Errors catalogs. The affected values date to af5339de and survive the later64b477da completeness pass; current main retains them. Completed w3/166 made the source button keyboard reachable and remains a working control.

Evidence: `dashboard/tmp/qa-20260917/errors-localization.json` contains all four live cases and the real diagnostic response; `dashboard/tmp/qa-20260917/errors-locale-catalogs.json` records both keys across15 catalogs. `dashboard/tmp/qa-20260917/errors-zh-390.png` was viewed. English/390 restored. No product edits, production writes, commits or shipping; other engines, native screen-reader speech and patched behavior remain unverified.
