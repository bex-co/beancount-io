---
id: w4/m9/t008
title: Test coverage
worker: worker1
status: todo
estimate: 45m
depends_on: [w4/m9/t006]
---

## Objective

Add tests that assert the real behavior and failure modes of the price verbs and the skill changes.

## Context

- `cli/tests/` uses pytest with fixtures under `cli/tests/fixtures`; `test_customer_workflows.py` and `test_command_tree.py` show the command-level style.
- Network must be stubbed: fake the `beanprice` source call, never hit a real API in CI.

## Steps

1. Fetch: writes one directive, second run reports duplicate, `--dry-run` leaves the file byte-identical, partial source failure writes successes and exits `1`, missing extra exits `2` with the hint.
2. Sources: unsourced commodity gets a pasteable suggestion; base currency is not flagged; JSON row shape.
3. Skills: evals updated in t004 pass the skills CI check.

## Files

- `cli/tests/test_price.py` (new)

## Acceptance criteria

- [ ] Each definition-of-done bullet has at least one failing-then-passing test.
- [ ] `make check-all` and the skills CI check pass.

## Out of scope

- Tests that only assert a mock returned what it was told.
