---
id: w4/m9/t006
title: Adoption surface
worker: worker1
status: todo
estimate: 25m
depends_on: [w4/m9/t005]
---

## Objective

Confirm the price verbs and skill changes are discoverable and usable on every surface they touch, for both Claude Code and Codex.

## Context

- Surfaces: `cli/README.md` install and quick start, root `README.md` package table wording for `cli/` and `skills/`, root `CLAUDE.md` package table, `skills/CLAUDE.md` skills table rows for `beancount-close` and `beancount-ask`, and the `AGENTS.md` / `.claude/skills` / `.agents/skills` symlinks.
- Run `python3 scripts/check-agent-guidance.py` after touching instruction files.

## Steps

1. Walk each surface and fix wording that no longer matches the shipped behavior.
2. Verify the close and ask skills trigger and behave the same under Claude Code and Codex.
3. Record drift you did not fix as a follow-up inbox note through `/pm add`.

## Files

- `cli/README.md`, `README.md`, `CLAUDE.md`, `skills/CLAUDE.md`

## Acceptance criteria

- [ ] Root and package tables mention price fetching where the CLI capabilities are listed.
- [ ] `python3 scripts/check-agent-guidance.py` passes.

## Out of scope

- New features.
