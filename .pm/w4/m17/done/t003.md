---
id: w4/m17/t003
title: Verify the public discovery adoption journey
worker: worker1
status: done
estimate: 10m
depends_on: [w4/m17/t002]
---

## Objective

Exercise actual public search, matching result activation and Back at desktop/narrow. Check that labels and instructions still describe the working flow; avoid adding documentation for a timing workaround.

## Context

See [FINDINGS.md](./FINDINGS.md) and the milestone definition of done. Preserve completed w3/164's profile history contract.

## Files

Public profile discovery UI and existing guidance if inaccurate

## Acceptance criteria

- [x] Readers can type and select immediately without a dead end or needing to wait for the URL.

## Out of scope

Backend/API changes, collection server-cap changes, global router behavior, new dependencies and unrelated ledger-list fixes.

## Adoption verification

Completed 2026-09-19. The public discovery journey was exercised live against a local production build reading the public `open_ledger` profile anonymously, in fresh isolated headless Chromium contexts at 1440x1000 and 390x844. 22 checks, all passing.

The reported dead end is gone. Typing budget and opening the only matching budgeting-envelopes card with no pause lands on `/ledger/open_ledger/budgeting-envelopes` and stays there after every outstanding timer and read settles. The URL never contains `undefined`, and neither "User not found" nor "The requested resource could not be found" appears. No page errors are raised in any run.

Both typing cadences were covered, because the original reproduction was cadence-sensitive: keys typed as fast as the automation can send them, and the 120ms-per-key rate FINDINGS recorded. Both widths behave identically, so the narrow journey is not a separate risk.

The return journey works as the definition of done requires. Back lands on `/ledger/open_ledger?lang=en&q=budget` with the input still reading budget and the collection showing "Showing 1 of 1 ledgers" — the search the reader could see when they left, restored from the history entry t002 writes on departure. Forward reopens budgeting-envelopes.

FINDINGS' own control still holds: letting the URL settle to `q=budget` before activating the card opens the ledger at both widths, so the repair did not fix the race by breaking the ordinary path.

Documentation reviewed with no change required. The repair introduces no feature, package, skill or setup variable, and the only profile-related line in `dashboard/README.md` concerns crawler indexability of public profile surfaces, which this work does not touch. `python3 scripts/check-agent-guidance.py` reports all 17 scopes canonical.

Not claimed: this records browser behaviour against a local build with anonymous public reads, not the hosted deployment. The cached-destination variant FINDINGS mentions without claiming reproduced was not separately constructed; the settled control above is the closest observed equivalent.
