---
id: w4/m12/t001
title: Delete unconsumed GraphQL operation documents, allowlisting the billing operations behind ADR001
worker: worker1
status: todo
estimate: 30m
depends_on: []
---

## Objective

Remove the operation documents nothing in the app uses, and record the ones deliberately kept, so t003's check starts from a clean, explained inventory.

## Context

- Verified 2026-09-15: zero references to the generated hook or `Document` outside `src/generated-graphql/` for `subscriptionStatus.graphql`, `createSubscriptionSession.graphql`, `cancelSubscription.graphql`, `getAiCfoUsage.graphql`, `insertReceiptTransaction.graphql`, `journalEntries.graphql`, and `userProfile.graphql` in `mobile/src/common/graphql/queries/`.
- Decision (2026-09-15): keep the three subscription operations on an allowlist citing `docs/adrs/ADR001-mobile-billing.md` (Proposed). Confirm the other four individually, defaulting to delete.
- Before deleting `getAiCfoUsage.graphql`, read `docs/adrs/ADR002-mobile-ai-assistant.md`. If it stages that operation, allowlist it with that reason instead.
- Mobile's documents are not shared with the dashboard or backend.

## Steps

1. Delete the confirmed-unused documents.
2. Run `yarn codegen`; never hand-edit `src/generated-graphql/`.
3. Record each kept operation with its reason in the allowlist format t003 will read.

## Files

- `mobile/src/common/graphql/queries/*.graphql`
- `mobile/src/generated-graphql/` (regenerated only)

## Acceptance criteria

- [ ] Every remaining operation document is either consumed or allowlisted with a cited reason.
- [ ] `yarn typecheck` and `yarn test:unit` pass.

## Out of scope

- Implementing mobile billing, and changes to dashboard or backend documents.
