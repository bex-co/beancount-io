---
id: w4/m7/t005
title: Guardrail test for unlabeled icon-only pressables
worker: worker1
status: todo
estimate: 30m
depends_on: [w4/m7/t002, w4/m7/t003, w4/m7/t004]
---

## Objective

Add a static-source unit test in the jest-lite runner that fails when an icon-only pressable under `src/screens` or `src/components` lacks an `accessibilityLabel`, so the pass cannot regress silently.

## Context

- `src/__tests__/settings-navigation.test.ts` already scans source text; follow that style.
- Heuristic: a `TouchableOpacity`/`Pressable` whose children are only an `Ionicons`/`Image` element and no `Text`.

## Steps

1. Write the scanner with an allowlist file for deliberate exceptions, each with a reason.
2. Confirm it fails by removing one label, then restore it.

## Files

- mobile/src/__tests__/accessibility-labels.test.ts

## Acceptance criteria

- [ ] The test passes on the fixed tree and fails when a label is removed.
- [ ] Every allowlist entry carries a reason.

## Out of scope

- Runtime accessibility testing frameworks (new dependency).
