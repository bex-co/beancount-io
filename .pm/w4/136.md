# Match translated signup username limits to the 20-character validator

Severity: minor. Estimate: 40m, dashboard. A2: a visitor should receive the same accurate username requirements regardless of interface language. Repeated dashboard QA, 2026-09-18.

Production beancount.io, signed out, Chinese/light, isolated headless Chromium149.0.7827.55 at390×844 and a fresh1440×1000 load. HEAD26d54a9d, fetched origin/mainbd5d1d31, bundle main-BIr0jJni.js; exact deployed SHA unknown. Only local field validation was exercised: no signup submission, email, password entry or account creation. The mutation observer recorded zero operations.

1. Open `/auth/sign-up?lang=zh`. The Username placeholder says `输入你的用户名（最多16个字符）` (at most16 characters).
2. Enter `qaabcdefghijklmnopqrs` (21 ASCII characters) in Username and Tab away. The invalid field's associated message says `用户名最多16个字符` (at most16 characters). Fresh desktop repeats.
3. Shorten to `qaabcdefghijklmnopqr` (20 characters) and blur. The error disappears and the field is no longer invalid. Thus the claimed16-character boundary does not match this form's actual20-character boundary.
4. Working control: `/auth/sign-up?lang=en` advertises max20; the same21-character value produces **Username must be at most 20 characters**. Wait for hydration before filling and verify the actual input value; a premature automation fill encountered a generated-default username and was replaced by this settled control.

Root: `dashboard/src/features/auth/components/register-form/index.tsx:55–59` correctly enforces max20 using `auth.usernameMaxLength`; line167 displays `auth.enterUsername`. Both keys still say16 in twelve auth catalogs: bg, ca, de, es, fa, fr, nl, pt, ru, sk, uk and zh. Persian spells it۱۶. English/Japanese/Korean already say20. The Chinese keys are at `features/auth/locales/zh.ts:116–118,367–369`. Several other affected maximum-error messages also contain untranslated English fragments; translate the complete corrected messages rather than preserving those fragments. Chinese and English were exercised live; the other catalog values are source-confirmed.

Fix those two messages in all twelve affected locales to describe the existing20-character rule accurately. Preserve the lowercase/alphanumeric/underscore rule, username validation and accessible hint/error associations. Do not reduce the validator to16 to match stale copy. No new key, dependency or backend/API behavior is required. The server signup input/service was inspected but never invoked; this finding concerns the observable dashboard form contract and does not claim a completed20-character account registration.

Blast radius: shared RegisterForm is used by the registration page and OAuth, mobile-consent and identity-consent wrappers. The latter can hide the generated username and were not exercised. Keep that behavior intact. m15 separately owns missing error associations on other registration fields; Username already has the correct association, demonstrated here. This issue only corrects the contents of its guidance.

Dedupe/history: all board states searched for username length/max16/max20 and translated validation. No existing item covers this mismatch. Both stale Chinese keys and the correct English20 copy date toaf5339de; later register-form fixes preserve max20, and no repair appears on fetched main. No deployment-lag issue or backend parity gap is alleged.

Acceptance: Chinese fresh desktop/narrow placeholder and21-character error say20; a20-character value clears the local error, while21 remains invalid. The twelve affected catalogs agree with this boundary and use complete localized wording; English/Japanese/Korean controls remain correct. Cover the real translated form's boundary/error behavior, preserving Username's described-by/invalid state and generated/hidden mode. Run dashboard gates when implementing. Other browsers, actual signup/OTP, consent flows and patched behavior remain unverified.

Evidence: verified `dashboard/tmp/qa-20260917/signup-username-limit.json` records both Chinese loads, the20-character recovery, exact English control and zero mutations. `signup-username-limit-zh-390.png` was visually inspected. Draft username cleared and the temporary request observer removed. No product fixes, commits or pushes.
