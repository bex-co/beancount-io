# Statement section descriptions claim USD for a custom-unit ledger

Severity: minor copy defect. Estimate: 30m, dashboard. A2: a newcomer reading an example statement should see descriptions consistent with its actual units. Source: repeated dashboard QA, 2026-09-17.

Environment: production https://beancount.io, anonymous read-only access to public `open_ledger/minimax`; QA-owned isolated headless Chromium 149.0.7827.55, English/light. Cash Flow at 390×844 including fresh reload, Income Statement at 1440×1000. Local main `26d54a9db7d33c31a549bd12a0454a1c01f3f038`; deployed main-BIr0jJni.js, exact deployed commit unverified.

Repro: open `/ledger/open_ledger/minimax/cash-flow?time=2026`, wait for the report, and read the activity sections. Operating Activities says “Detailed breakdown of minimax Operating Activities with USD and other commodity values”, while its table header and amounts are MUSD. Investing and Financing use the same USD description. Reload: identical. Open `/ledger/open_ledger/minimax/income-statement?time=2026`: Income and Expenses repeat the USD description above MUSD tables.

Expected: the description accurately describes the returned account/unit values without hard-coding USD. Actual: it advertises a unit absent from this ledger. Do not reinterpret MUSD as USD or change arithmetic to fit the copy.

Controls: `GetLedgerAttributes` returns HTTP 200 with `currencies:["MUSD"]`. Cash Flow's actual values reconcile: -357.997 operating -202.980 investing +984.261 financing = +423.284 MUSD net change; opening 507.621 +423.284 = closing 930.905 MUSD. Its downloaded Markdown correctly says “Ledger units shown: MUSD”, uses MUSD on every summary row, and retains the custom-unit management-schedule notice. The ordinary USD example tested earlier has matching descriptions. No API or export-unit defect is claimed.

Root: translation key `page.reports.hierarchyListDescription` hard-codes USD in `dashboard/src/features/reports/locales/en.ts:24` and the common catalog `src/i18n/locales/common/en.ts:757`; both catalog families contain the key in 15 locales (German also explicitly says USD). Cash Flow `cash-flow-content.tsx:168` supplies only ledgerName and sectionName to the description while independently supplying the real primaryCurrency to the table. `balance-sheet/hierarchy-list-card.tsx` faithfully renders the supplied description. The UI consumer, not the API, introduces this unit claim.

Fix: make this shared translated description unit-neutral, for example describing account balances by commodity, and update both catalog families through the existing localization convention. A unit-neutral sentence avoids falsely claiming that a primary currency exists in every filtered result. Preserve ledger/section interpolation, table primary-unit headings, all amount/sign behavior and export notices. Verify non-USD and ordinary USD examples plus a non-English translation; run the normal dashboard checks. No dependency or API change is needed.

Blast radius: all six production call sites of this translation key are in Balance Sheet (Assets, Liabilities, Equity), Income Statement (Income, Expenses), and Cash Flow's reusable activity section (Operating, Investing, Financing). Balance Sheet and non-English runtime behavior are source-traced, not independently reproduced in this finding. Trial Balance does not call this key. Loading, denied, offline and expired-session behavior are not affected by the static-copy cause and were not exercised for this candidate.

Dedupe: searched all `.pm/**/*.md`, including done/blocked, for the key, exact description, hard-coded USD and unit/currency descriptions; no overlapping item. Completed export work owns correct custom-unit disclosures and is a passing control. `git log -S 'with USD and other'` traces the description to `af5339de`; current main retains it, with no later source repair. No deployment-lag disposition applies.

Verified ignored evidence: `dashboard/tmp/qa-20260917/minimax-unit-description.json` records the fresh Cash Flow and Income Statement text plus the API unit control; `minimax-usd-description.png` is the visually inspected narrow screenshot; `minimax-cash-flow.md` is the actual downloaded report. No product fix, production mutation, commit or push.

Additional live Balance Sheet evidence: public-company-template at390px, English/light, time=2026. Assets, Liabilities and Equity descriptions each claim USD above MUSD-only tables. The three totals are23,000/−10,000/−13,000 MUSD, and the actual CSV uses unit=MUSD with matching raw values. This independently confirms the three previously source-traced Balance Sheet call sites; the same unit-neutral copy repair already covers them. Evidence: dashboard/tmp/qa-20260917/template-no-activity-balance.csv and template-no-activity-controls.json. No additional issue or arithmetic defect.
