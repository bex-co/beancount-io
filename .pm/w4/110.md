# Explain generated opening and conversion rows in Account Entry Context

Severity: minor, with working inline postings. Estimate: 60m, dashboard. A2: inspecting a valid opening balance or conversion row should explain the report-generated transaction rather than claim the record is missing.

Production https://beancount.io, signed out, isolated headless Chromium149.0.7827.55, English/light, fresh390×844 and1440×1000. Open `/ledger/open_ledger/stock-example/account/Assets%3ABrokerage%3AACME?time=2026-02&lang=en`, then activate the January31 row “Opening balance for 'Assets:Brokerage:ACME' (Summarization)”, flagS. Both runs settle into Entry Context / “The requested resource could not be found.” Narrow screenshot inspected. LocalHEAD26d54a9d, fetchedmain358ba4dc; production main-BIr0jJni.js, exact deployed SHA unverified.

The actual GetLedgerAccountJournal read returns HTTP200 and two rows. The generated row has hashd6dfe184d235b6010ab97a91726f9375, date2026-01-31, flagS, meta=null and four postings:150ACME at68.20USD paired with−10230USD Opening-Balances, plus100ACME at84.60USD paired with−8460USD. Its displayed250ACME /18690USD opening balance is correct. GetLedgerEntryContext for that hash returns HTTP200 with GraphQL NOT_FOUND. Inline expansion correctly exposes all four postings. Control: the ordinary February20 sale opens actual source `transactions/sales.bean:45` and its six source lines. No missing ordinary source or arithmetic defect is established.

Producer/contract: ledger `features/ledger/service/ledger-journal-service.ts:690–740` deliberately time-clamps account journals and hashes synthetic entries absent from the full source ID map. `foundation/rustledger/time-clamp.ts:583–601` constructs these opening transactions with FLAG_SUMMARIZE=S; createEntriesFromBalances at391–441 supplies the real postings. They have no source directive. getContext at601–638 resolves actual parsed source and correctly cannot find this generated hash. The global Journal deliberately excludes S/C at579–584, so this observed case is Account Journal. Preserve the Fava-compatible balances, journal inclusion and API contracts; do not fabricate source or remove the opening row.

Root: `dashboard/src/features/journal/components/entry-context-dialog.tsx:36–43` recognizes only generated padding flagP. An S row falls through to EntryContextPanel at144–155, which queries its impossible source. The existing GeneratedEntryPanel already renders read-only date/narration/postings, but its explanation atjournal/locales/en.ts:422 specifically claims a pad directive. Extend the existing generated-entry handling for the known summarization flag with accurate localized opening-balance wording; do not describe S as padding. Reuse the panel and preserve its source/edit/delete exclusion regardless of write permission. Do not classify ordinary rows from missing metadata alone, blanket-convert NOT_FOUND into generated content, or infer generation from translated narration. Preserve normal source-backed fetching and error classification.

Acceptance: this S row opens a read-only generated opening-balance explanation and all four supplied postings without requesting an unavailable source; desktop/narrow, Escape and opener focus work. Existing P explanation and ordinary source controls remain correct. Rendered coverage should exercise S on a writer-capable test context (still no source/edit/delete), P, an ordinary thin row with null metadata, and ordinary missing/read-failure paths. Update the15 journal locale catalogs through their existing convention and run dashboard gates. No backend change, dependency or new data fetch. Other generated flags T/U and cold direct links without a supplied journal row remain unverified and outside this finding.

Dedupe/history: completed w3/146 explicitly fixed only P and left other flags unverified. Current history traces the P guard to254cfea0; later shared-panel extraction50e34ffa and focus repair87d3c63b retain that guard. The older short SHA recorded in146's closeout is not available locally; it was not independently inspected. Completedm43 covers browser entry routes and genuine missing contexts; it does not supply a generated S fallback in the row dialog. w4/m22 concerns editor readonly accessibility and requires no change to this generated panel. All queues, including done/blocked, searched for summarization/opening-balance context; no matching pending repair found.

Evidence: `dashboard/tmp/qa-20260917/summary-entry-context.json` contains both widths, actual account/context reads, inline expansion and ordinary source control; `summary-entry-context-390.png`. No product edits, authenticated actions, source writes, commits or pushes.

Live P control: minimax Journal time2026-06-29, padding hash508f22c1ceccbfb12344da6959fec867, opens the correct pad explanation and two opposite3994.853MUSD postings with only Close and no context read. Pointer row activation and keyboard Enter on its date button both pass. Evidence: `dashboard/tmp/qa-20260917/generated-padding-context-control.json`. This confirms the existing padding fix is deployed;110 addresses the distinct S case.


Additional same-cause reproduction: `/ledger/open_ledger/example/account/Equity%3AConversions%3ACurrent?time=2016&lang=en`, fresh390×844 and1440×1000, Monthly/At Cost. The2016-12-31 row has flagC and narration `Conversion for -0.01663 USD`; activating its date shows the same resource-not-found dialog. Real GetLedgerAccountJournal HTTP200 returns total1, hash`23c4e9456fd243392ff41eedfb53a382`, meta=null, and one posting: `Equity:Conversions:Current 0.01663 USD @ 0 NOTHING`. Change and balance are both0.01663USD. GetLedgerEntryContext for that hash returns200/NOT_FOUND/data null. Desktop inline expansion correctly shows the supplied posting and zero price.

Producer `backend-cluster/ledger/src/foundation/rustledger/time-clamp.ts:635–695` intentionally creates this flagC transaction to clear the residual cost balance; `:736–742` inserts it during clamping. It is not a missing source file or an arithmetic error. The same P-only dialog guard causes both S and C failures. Extend this note's narrow repair to known C rows, with an accurate generated-conversion explanation distinct from padding and opening balances. Preserve its supplied unit/price information and no-source/no-edit/no-delete behavior, including writer-capable rendered fixtures. Do not infer other flags or special-case ordinary missing entries. This adds one explanation/fixture to the existing presenter repair; the estimate is60m, still one bounded inbox item.

Evidence: `dashboard/tmp/qa-20260917/conversion-entry-context.json` (both fresh-width dialogs and real request/response), `dashboard/tmp/qa-20260917/conversion-entry-context-390.png` (viewed). Current main bd5d1d31 retains both producer and consumer. S and P controls above remain applicable; T/U and cold direct links remain unverified. No product edits or production writes.
