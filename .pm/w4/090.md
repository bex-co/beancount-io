# Resize charts when their containers become visible or change width

Severity: minor chart-layout defect. Estimate: 60m, dashboard. A2: a reader who restores a saved-hidden report chart should see its full plot and currency legend without having to resize the browser.

Environment: production https://beancount.io, anonymous synthetic open_ledger/example, isolated headless Chromium149.0.7827.55,320×800 dark and light,1440×1000 light. Local main26d54a9db7d33c31a549bd12a0454a1c01f3f038; deployed main-BIr0jJni.js, exact deployed commit unverified. No data interception or fabricated chart values.

Repro: open `/ledger/open_ledger/example/cash-flow?lang=en&time=2016-01`, hide charts, reload with that saved preference, then press Show charts. After the reveal animation settles, the canvas and its painter root remain100px wide inside the304px chart host at320px. Most of the chart area is blank; the currency legend shows an icon and1/3 pagination but no readable currency name. Repeat from a fresh hidden load in light mode: same100/304. Repeat at1440: canvas100px inside1136px host. The report and its numeric tables load successfully; this is rendering geometry, not missing data.

Controls: changing the viewport to390 resizes canvas and host to374px. Switching dark→light while the chart is visible recreates the instance at304px, also restoring proper dimensions. Starting with a correctly sized desktop canvas1136px, collapse the sidebar without changing the viewport: the host grows to1344px but canvas stays1136px. Restoring the sidebar restores the old host width. The chart-visibility and sidebar preferences were restored after testing.

Root: `dashboard/src/common/components/react-echarts/client.tsx:29–45` initializes even when the host is hidden;65–73 responds only to window resize. `features/reports/components/collapsible-charts-section.tsx` intentionally leaves children mounted, applies hidden after collapse, and initially sets hidden from the saved preference. Revealing it does not emit a window resize. Installed zrender `lib/canvas/helper.js:67–78` falls back from zero client/computed dimensions to parseInt(root.style.width), so the chart's100% inline width becomes100 pixels while hidden. The app never asks the existing instance to remeasure after reveal. Sidebar width changes demonstrate the same missing container-size observation.

Repair the shared client wrapper's container-size lifecycle, for example with a properly cleaned-up ResizeObserver that resizes the current instance when measurable host dimensions change. Handle zero-size/hidden states, theme recreation and disposal without resize loops or unnecessary instance recreation. Do not patch zrender, remove hidden/inert, dispatch synthetic global resize events from individual report pages, or weaken the pending-data safeguards. Preserve instance refs, options, loading state, lazy loading, SSR placeholders and existing window-resize behavior.

Dedupe/history: searched all .pm including done/blocked for ResizeObserver, hidden/revealed charts, canvas width, chart/sidebar sizing and window resize. Completed w3/127 fixed invisible keyboard controls and introduced the shared hidden/inert wrapper in254cfea0; preserve that repair. Completed w2/m22 owns lazy runtime loading;7b22c891 correctly applies the current theme. Neither observes container changes. Current wrapper history af5339de/76b61b35/7b22c891 retains the window-only listener. Open w4/m16 concerns chart-view state during data reads, not canvas measurement; w4/087 concerns a commodity card's intrinsic CSS width, not this host/painter mismatch. No matching active repair or newer fix found.

Blast radius: all production consumers of shared ReactECharts inherit this lifecycle. Cash Flow hidden-load and desktop-sidebar journeys are directly reproduced; sibling report and other chart surfaces are source-traced risks, not additional live findings.

Acceptance: hidden-at-mount→shown and container-width-only changes bring the canvas into agreement with the host after settling, with readable legend labels, on narrow/desktop and light/dark. A normal visible-mount control remains correct. Add a meaningful lifecycle test driving container-size notifications without a window resize, and verify cleanup and theme recreation; existing instance/update/ref tests must still pass. Preserve hidden controls' exclusion from keyboard/accessibility navigation. Run dashboard gates and verify at least one sibling report in the browser; other engines and patched behavior remain unverified.

Evidence: `dashboard/tmp/qa-20260917/chart-container-sizing.json` records both appearances, desktop hidden-load, theme-recreation control and sidebar-only mismatch. Visually inspected `chart-hidden-reveal-light320.png` and `cashflow-dark-320.png` show the100px plot and clipped legend. The latter also records the separate header bug in084. No product changes, production writes or shipping.

Sibling reproduction: Balance Sheet `/ledger/open_ledger/example/balance-sheet?time=2016-01&lang=en` at390×844 light, Hide charts→reload→Show charts, also leaves a100px canvas inside374px host. Recorded in the same JSON artifact. Balance Sheet is now live-confirmed; other consumers remain source-traced. Chart visibility was restored afterward.

Additional live consumer: stock-example Commodities at1440×1000, with all three real price charts mounted. Keyboard Enter on the sidebar rail collapses it; each canvas remains510px while its host grows to614px. A fresh collapsed reload correctly produces614/614. Reopen the sidebar and ArrowRight once to272px expanded width: each canvas remains614px while its host shrinks to502px. `sidebar-commodity-chart-size-control.json` records all three charts and the passing fresh-load control. This extends the existing shared resize defect, not a new finding or page-specific fix. Sidebar width/open preferences were restored to256px/open and a real viewport resize performed afterward.
