# Give Budget, Commodities, Statistics and Settings tables meaningful accessible names

Severity: minor accessibility defect. Promoted from w4/091 to m20; expanded estimate: 150m across implementation, integration and verification, dashboard. A2: readers using table navigation should be able to distinguish which account or currency pair a history belongs to.

Environment: production https://beancount.io, anonymous synthetic open_ledger/budgeting-envelopes, isolated headless Chromium149.0.7827.55, English/light390×844 and fresh1440×1000. Local main26d54a9db7d33c31a549bd12a0454a1c01f3f038; deployed main-BIr0jJni.js, exact deployed commit unverified.

Repro: open `/ledger/open_ledger/budgeting-envelopes/budget`, with All intervals and All time selected. Seven cards show Dining, Groceries, Insurance:Car, Rent, Transport, Travel and Utilities budget identities. Each contains a Date / Interval / Amount history table. Chromium's native accessibility tree reports seven table nodes with empty names. Every table lacks a caption, aria-label and aria-labelledby. Fresh desktop reload repeats all seven empty names. The visible account/interval/currency identity is outside the table and has no programmatic association with it.

Expected: each history table has a stable, meaningful accessible name tied to its visible budget identity, so the seven otherwise similarly structured tables can be distinguished. Include currency to disambiguate multiple groups for one account. Keep all existing rows, native table semantics, visible labels and formatting. The existing groupBudgetEntries rule groups by account plus currency; interval changes remain entries within the same history, and this repair must preserve that rule.

Root: `dashboard/src/features/ledger-data/budget/budget-history-table.tsx:51` renders bare Table without a caption or accessible name. Its sole production caller is budget-chart-card.tsx:409; the visible CardTitle at275–277 and interval/currency badges at278 onward are unassociated siblings. Shared Table already forwards native props. Associate existing visible identity with the table using stable, collision-safe IDs, or supply an equivalent localized caption; no global Table default, new dependency or financial-data change is needed.

Controls: native rows and Date/Interval/Amount column headers are present, the Insurance card shows600 USD actual against100 USD current target, and no Delete buttons are exposed to this public reader. The defect is missing table identity, not lost amounts, missing row semantics or a permission failure. Actual screen-reader speech/shortcut behavior is unverified; the report is based on native accessibility metadata and DOM associations.

Dedupe/history: all .pm including done/blocked searched for BudgetHistoryTable, budget table names/captions and unnamed tables. Completed w3/m32 names shared hierarchy tables but explicitly excludes other tables; this component does not use HierarchyList. Completed w3/123 concerns native mobile budget-row values. Current budget history originated ataf5339de; later focus repairf6b7f952 and card actual-value repair74fe6cbb do not add an association. No matching active item or later naming repair found.

Acceptance: native browser inspection at narrow/desktop exposes meaningful distinct table names matching the visible budget groups. A rendered interaction/filter test verifies names stay paired with the right history after filtering, and a fixture with one account in different currency groups avoids ambiguous naming or duplicate IDs while preserving interval changes within each history. Preserve read-only action gating and existing rows; run dashboard gates. Other locales, authenticated actions, other engines and patched results remain unverified.

Evidence: `dashboard/tmp/qa-20260917/budget-table-names.json` records narrow/fresh-desktop native table names, visible account identities, all missing DOM name associations and zero public Delete controls. No product changes, production writes or shipping.


Additional reproduced scope — commodity history: fresh desktop1440×1000 and390×844 at `/ledger/open_ledger/stock-example/commodities`, open ACME/USD and BMKT/USD histories (narrow activation by keyboard Enter). Both native AX table names are empty, with no caption/aria-label/aria-labelledby. Both tables have identical Date / Price (USD) headers and13rows, so the base commodity is absent from the table identity. Real GetLedgerCommodities returns200 with ACME13/BMKT13/NWRB15 prices; disclosures remain correctly named and keyboard-operable. This is a second local authoring site for the same missing history identity, grouped into this table-naming repair rather than requiring a global Table default.

Commodity root: `dashboard/src/features/ledger-data/commodities/commodity-price-history.tsx:45` renders bare Table. Its existing tableId belongs to the surrounding div targeted by the disclosure's aria-controls; that relationship does not name the table. pairLabel already exists at22 and the visible card identity is outside. Supply a stable base/quote pair association or localized caption without altering precision, row order, collapse behavior or the named disclosure. Commit a4194ff3 introduced the deliberate keyboard alternative under former w3/051; current file has no later repair. Existing tests verify keyboard disclosure and low-price decimals, but do not query table names. Dedupe included former issue history, w4/087 (independent grid/button overflow) and all current queues. Add two same-quote/different-base histories to the rendered naming test, with close/reopen and original keyboard/precision controls. Evidence `dashboard/tmp/qa-20260917/commodity-table-names.json`; actual assistive-technology speech remains unverified. The former60m estimate covered these two authoring sites; Statistics expands the milestone scope below.


## Statistics extension and promotion

Fresh production `open_ledger/synopsys/statistics?lang=en` at390×844 and1440×1000 has three native AX table nodes, all with empty names. DOM confirms no caption/aria-label/aria-labelledby for Postings per Account (26 rows, Account/Count), Account Last Entries (15 rows, Account/Last Entry Date/Balance), and Entries Count by Type (12 rows, Entry Type/Count/Percentage). Visible headings exist as preceding siblings, but they do not name their tables. Linear reading can still encounter the headings; the defect concerns identification during table navigation, not unreadable rows. Actual screen-reader commands remain unverified. Trial Balance is a positive control: its table is named Trial Balance in the native AX tree.

Roots: `dashboard/src/features/ledger-data/statistics/postings-per-account.tsx:149`, `account-last-entries.tsx:153`, and `entries-count-by-type.tsx:127` render bare Table elements below unassociated h3 headings. All three bare openings trace to af5339de. Skeleton branches repeat the omission; runtime evidence here is settled data only. Use existing localized headings with stable IDs or equivalent consumer-owned labels, preserving separate loading/error/empty states and avoiding duplicate IDs. No new translation phrase is required for these existing headings.

Dedupe: completed w3/m32 only repaired HierarchyList naming; completed w3/m14 changes filtered Statistics counts; commit49562f6e makes Statistics account destinations keyboard-reachable links (referenced historically as w3/056). Completed w3/138 fixes zero-denominator percentages and w3/193 fixes postings terminology. None names these tables. Preserve those repairs, the count queries, account links, row values, sorting, permissions and localized headings. Existing w4/079 names unrelated report comboboxes. No API/parity change is needed.

Evidence: `dashboard/tmp/qa-20260917/statistics-table-names.json` contains both native names and corresponding DOM associations, row counts and headers. Original Budget and Commodities evidence above remains part of this finding group. Expanding to five table-authoring components plus coordinated tests and public validation exceeds the inbox sizing threshold, so091 is retired in favor of m20. No product changes, writes or shipping have occurred.


## Read-only Settings extension (2026-09-18)

Anonymous `/ledger/open_ledger/example/settings` exposes Beancount Options and Fava Options. At desktop1440×1000 and fresh390×844/fresh1440 reloads, both native Chromium AX table names are empty. Both tables have identical Option / Value column headings and no caption, aria-label or aria-labelledby. Their visible CardTitles are unassociated siblings. This prevents identifying the option family by the table name; rows remain readable. No actual screen-reader speech is claimed.

Roots: `dashboard/src/features/ledger-data/settings/beancount-options-section/index.tsx:76` and `fava-options-section/index.tsx:101` render bare Table elements below existing localized CardTitles at60 and85 respectively. The omission originates in af5339de; localization changes in aeec496b do not associate these titles. Shared Table forwards native props. Reuse the existing localized visible identities with stable consumer-owned associations; no global label or new translation is needed.

Controls: these two sections intentionally render to public readers. General/Visibility sections remain under Authenticated; Collaborators/Danger remain under LedgerAdminPermission. No inputs, textareas or selects were exposed in main, and no writes were attempted. Preserve those permission gates, all option values and table semantics. Dedupe across current, done and blocked board items found no Settings table-naming repair; this extends the existing missing-table-identity group rather than creating a separate issue.

Evidence: `dashboard/tmp/qa-20260917/settings-table-names.json` records desktop, narrow and fresh desktop DOM associations, native AX names, snapshots and absence of writable forms. The coordinated scope is now seven table-authoring components and four public journeys, estimated150m including regression coverage and checks. Tests should identify both Settings tables by their distinct names while retaining public read-only behavior. Other locales, patched results and actual assistive-technology speech remain unverified.
