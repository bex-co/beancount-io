# Keep Upcoming Events boundaries in the viewer's calendar timezone

**Severity:** minor. **Estimate:** 50 minutes. **Owner:** dashboard. **A2/A3:** the event shortcut and count should include the configured future days without counting yesterday or hiding the last eligible day.

Reproduced against production beancount.io with the real public open_ledger/example data, signed out, English/light, isolated headless Chromium149.0.7827.55. **Browser clock/timezone controls were simulated**; no event response or production ledger was changed. HEAD26d54a9d/main bd5d1d31, deployed main-BIr0jJni.js, exact deploy SHA unknown.

Use separate owned contexts with timezoneId UTC and Pacific/Kiritimati. Set Playwright page.clock.setFixedTime before loading Journal. The real GetLedgerEvents response contains Chicago dated2016-11-16, with neighboring events2016-04-19 and2016-11-29; the real GetLedger favaOptions.upcomingEvents is7.

- Fixed instant2016-11-17T00:00:00Z: both contexts have local calendar date November17. At1440×1000, UTC correctly has no Upcoming Events status link; UTC+14 incorrectly shows Events with badge1 for yesterday's November16 event.
- Fixed instant2016-11-09T00:00:00Z: both have local calendar date November9. After fresh390×844 reloads and opening the sidebar, UTC correctly shows Events1 for November16 (inclusive today+7); UTC+14 hides it. Fresh1440×1000 reloads repeat that omission.

Root: `dashboard/src/common/components/ledger-layout/ledger-sidebar.tsx:79–94` computes local midnight and local endDate with setHours/setDate, then converts both to UTC using toISOString().slice(0,10). In UTC+14, the November9–16 local interval becomes November8–15 strings. Plain event dates are compared against those shifted strings. The count drives the Events badge/link and collapsed Advanced badge. The source dates to af5339de and is unchanged on fetched main.

Producer is correct: backend-v2 `ledger-data-resolver.query.ts:256` forwards event.date unchanged; ledger `ledger-data-service.ts:297` lists real Event directives. Fix only the dashboard comparison bounds, using existing date-fns/local calendar formatting or equivalent local year/month/day parts for both endpoints. Keep the inclusive configured horizon, genuine zero count, current Events-route visibility and date-only event contract. Do not change API date strings, ledger events, the user's timezone, or introduce an alternate date library. Avoid feeding toISOString into the existing string formatter, since that already loses the local day.

Acceptance: reproduce both boundaries above and verify corrected counts at390/1440; same-day events, the last included day, next-day exclusion, zero/custom upcomingEvents and DST/month/year boundaries remain correct in UTC, an eastern zone and a western zone. Add a meaningful sidebar/helper regression driven by timezone-aware dates, preserving existing badge and link behavior. Run dashboard gates when implementing. No global date-policy rewrite is needed.

Dedupe: searched all .pm states for upcoming events, countUpcomingEvents, timezone and the bound identifiers; no matching item. w4/094 is Events empty-state wording; w4/114 concerns relative-period export labels; neither owns this local/UTC conversion. Current real-time examples have no near-term events, so this report does not claim today's public sidebar is visibly wrong. Other engines and physical timezone devices remain unverified.

Evidence: `dashboard/tmp/qa-20260917/upcoming-event-timezone-candidate.json` contains the real responses/options and six context/date/viewport snapshots. `upcoming-event-boundary-0-390.png` is the UTC control; `upcoming-event-boundary-1-390.png` was visually inspected for the missing eastern-zone link. No production mutation or product changes.

Corroboration: already-installed WebKit18.2, owned headless390×844/Pacific-Kiritimati, repeats both simulated-clock boundaries: November9 omits Events and a fresh November17 reload shows Events1. `upcoming-event-webkit-control.json` records both local clock values and sidebar snapshots. That owned browser was closed. This older engine control does not establish current Safari or physical-device behavior; the original Chromium UTC comparisons remain the passing controls.
