# Name the Git Clone dialog and protocol URL fields

Severity: minor. Estimate: 35m, dashboard. A2/A3: newcomers using assistive technology can identify the public example's clone popup and URL field. Repeated dashboard QA, 2026-09-18.

Environment: production beancount.io, anonymous public open_ledger/freelancer-invoicing, English/light, isolated headless Chromium149.0.7827.55;1440×1000 and fresh768×1024. HEAD26d54a9d, fetched origin/mainbd5d1d31, deployed main-BIr0jJni.js; exact deploy SHA unverified.

Open `/ledger/open_ledger/freelancer-invoicing/files/tree/main/transactions`, activate Git Clone and wait for its animation to finish. Chromium's native accessibility tree reports dialog name="" and textbox name="", although Clone Repository is visibly rendered and the textbox contains the SSH URL. Switch to HTTP: the corresponding HTTPS URL field also has name="". Fresh reload at768 reproduces both unnamed dialog and initial SSH field. Copy SSH clone URL and Copy HTTP clone URL buttons have correct names; Escape closes the popup and returns focus to Git Clone. No copy action or clipboard access was performed. Below640 the trigger is deliberately hidden, so no390 popup failure is claimed.

Root: `dashboard/src/features/ledger-editor/directory-browse/components/ledger-clone-url-menu.tsx:145–153` renders PopoverContent and a heading without an ID/name association. `CloneUrlInput` at74–75 renders a read-only Input with value but no label; the neighboring copy button's aria-label does not name this input. The shared Popover forwards naming props. The URL is a value, not its accessible name. The [WAI-ARIA dialog role](https://www.w3.org/TR/wai-aria-1.2/#dialog) requires a name; associate the existing localized Clone Repository heading with this specific dialog. Give each read-only URL input a localized protocol-specific identity using an explicit association with its HTTP/SSH label plus the existing localized clone context, or an appropriate translated field label. Do not label a textbox with the copy action or put a generic name on every Popover.

Preserve URL values, read-only behavior, text selection, existing copy labels/status/error handling, tab switching, archive error/retry behavior and Escape focus return. No backend change, dependency, clipboard automation or global popup redesign is required. One LedgerCloneUrlMenu caller is LedgerDirectoryView; canonical tree and legacy directory-content wrappers inherit it. Both CloneUrlInput instances are in this menu. Only the nested canonical tree route was exercised for these names.

Dedupe: all board states and targeted source/deleted-note history checked. Former w3/013, removed by070795fd, specifically repaired copy-button names, which pass here; w3/done/117 repaired archive feedback. Neither specified dialog or URL-field naming. w4/123 owns account-navigation and shared filter-combobox popups; this separately reproduced clone form has its own heading/field associations and fits a small independent repair. The omissions remain in the current source after254cfea0.

Acceptance: rendered Git Clone has its localized dialog name; SSH and HTTP read-only textboxes have distinct meaningful names and correct unchanged values. Check both tabs, Escape focus restoration and existing copy/archive tests, then repeat native-tree inspection at1440 and768. Run normal dashboard gates when implementing. Actual screen-reader speech, other engines/locales, authenticated key management and patched behavior remain unverified.

Evidence: `dashboard/tmp/qa-20260917/clone-dialog-names.json` holds native AX records for desktop SSH/HTTP and fresh768. `clone-dialog-names-settled-768.png` was visually inspected after opacity1 and completed animation; the earlier similarly named capture was taken during animation and is not the visual evidence. No product edits, production mutations, commits or pushes.
