# Preserve invalid-filter guidance on Account and Events

Severity: minor. Estimate: 45m, dashboard. A2: readers should know to correct an invalid date instead of seeing an unexplained data failure. Repeated dashboard QA, 2026-09-18.

Production beancount.io, anonymous public open_ledger/stock-example, English/light, isolated headless Chromium149.0.7827.55 at390×844 and fresh1440×1000 reload. HEAD26d54a9d, fetched origin/mainbd5d1d31, deployed main-BIr0jJni.js; exact deployment SHA unknown.

Open `/ledger/open_ledger/stock-example/account/Assets%3ABrokerage`. At narrow width, open Filters, enter2026-02-30 in Time, press Enter, wait and close the sheet. The Account Report shows only **Error loading account data**, repeated as subtitle and card text. Fresh desktop reload of the resulting URL behaves identically. The real GetLedgerAccountReport response isHTTP200 with `extensions.code:"BAD_USER_INPUT"` and message `Failed to parse date: 2026-02-30`. Expected: the existing safe localized invalid-input guidance that tells the reader to check/correct the input.

The global Time control remains available; correcting it to2026-04 restores the Account chart and journal, including the April17 ACME sale. Do not claim a permanent navigation trap. Working comparison: main Journal with the invalid date displays **Some of the information provided is invalid. Please check and try again.** That control was independently captured in this run. No ledger data changed.

Root: `dashboard/src/features/reports/account/index.tsx:367–374` discards its caught error and passes the fixed `page.accountReport.errorLoading` subtitle to PageErrorState. `features/journal/components/journal-states.tsx:164–197` renders that subtitle without an error classification. AccountPage is its only production caller. `common/lib/errors/error-message.ts:19,39–48` already maps classified Apollo BAD_USER_INPUT to safe localized correction guidance.

Use that existing error mapper at the Account caller, or reuse the existing classified ReportErrorState with the caught error and appropriate title. Do not pass a fixed message that overrides the classified guidance; never expose raw server messages. Preserve safe fallbacks for unknown/transport failures, existing loading/empty branches, global filter availability and successful recovery. No new parser, translation inventory, dependency or backend/API change is needed. The separate AccountJournal read-error branch is not reproduced here and is not part of this small repair.

Dedupe/history: searched all board states and targeted source history for Account error guidance. w3/done/133, shipped86ca4531, repaired Trial Balance, Balance Sheet and Income Statement callers; its scope did not include AccountPage. Those main-statement and Journal controls pass in this run. The Account branch remains unchanged fromaf5339de. w4/099 covers stale pending conversion data, not a settled classified failure.

Acceptance: a real classified Apollo error at the Account query boundary produces the existing localized input-correction message; an unknown or transport error keeps an appropriate safe fallback. Correcting or clearing Time restores the requested account data at desktop/narrow widths. Cover the owning caller with meaningful rejected-query/recovery tests rather than retesting only the shared mapper, and run dashboard gates when implementing. Other engines/locales, expired credentials, REST/MCP controls and patched behavior remain unverified; no backend defect is alleged.

Evidence: `dashboard/tmp/qa-20260917/account-invalid-period.json` contains both settled snapshots, sanitized real response and successful April recovery. `account-invalid-period-390.png` was visually inspected. Passing main-Journal evidence is `journal-invalid-date-control.json`. No product fixes, production mutations, commits or pushes.

Additional reproduced caller: Events `/ledger/open_ledger/example/events?time=2026-02-30` at1440 and fresh390 reload shows **Error loading data / Failed to Load Events**. Real GetLedgerEvents returnsHTTP200/BAD_USER_INPUT with the same invalid-date classification. Correcting Time to2016 restores four location events (April11/19 and November16/29). `features/ledger-data/events/index.tsx:142` supplies a fixed errorMessage to QueryView; `common/components/query-view.tsx:37–38` forwards both message and error, but `state-components.tsx:96–100` deliberately prioritizes the explicit message. Remove or appropriately condition this caller override so classified correction guidance can reach the user; retain the shared component's documented override contract. This is a second small caller repair in the same error-guidance item, not a change to the backend parser or global error precedence. Cover Events' classified failure and recovery alongside Account. Commodities/Errors also have explicit messages in source, but no corresponding classified failure was reproduced and they are out of scope.

Events evidence: `events-invalid-period.json` contains both UI states, sanitized response and four-event recovery; settled `events-invalid-period-390.png` was visually inspected (the earlier animation capture was replaced). Full-board searches found no Events invalid-date guidance item; w4/094 concerns misleading normal/empty copy. Events' override remains fromaf5339de on fetched main. The additional caller and rendered checks bring this bounded item to45m; authenticated/error-class/other-engine limitations above still apply.
