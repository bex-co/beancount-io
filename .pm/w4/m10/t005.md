---
id: w4/m10/t005
title: "Contributing guide: first-PR walkthrough per package, complete layout table, and where to ask"
worker: worker1
status: todo
estimate: 30m
depends_on: [w4/m10/t003]
---

## Objective

Bring `CONTRIBUTING.md` up to date with the monorepo as it is today and give a newcomer a concrete first-PR path per package.

## Context

- The layout table lists only `mobile/`, `dashboard/`, `cli/`, and `skills/`; `backend-cluster/`, `deploy/`, and `docs/` are missing. Root `README.md` and `CLAUDE.md` package tables are the source of truth.
- The "Development setup" section shows only the mobile setup; each package README has its own quick start to link.
- Root `README.md` "Quality bar" holds the per-package checks; link, do not copy.
- Add how issues are triaged (labels, `good first issue`, the public `.pm` board) and where to ask (Telegram), plus the new security policy and code of conduct links from t003.

## Steps

1. Rewrite the layout table from the root package tables.
2. Add a "Your first pull request" section: pick a `good first issue`, set up the one package, run its checks, open the PR with the template.
3. Add the triage and board paragraph and the community links.

## Files

- `CONTRIBUTING.md`

## Acceptance criteria

- [ ] Every active package in the root table appears in the layout table with a link to its README.
- [ ] Every command in the guide runs as written.
- [ ] All internal links resolve.

## Out of scope

- Package-level CONTRIBUTING files.
