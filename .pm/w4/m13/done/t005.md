---
id: w4/m13/t005
title: Cover the real layout/router regression and run dashboard gates
worker: worker1
status: done
estimate: 60m
depends_on: [w4/m13/t004]
---

## Objective and acceptance

Add meaningful integration coverage with the actual ledger layout/router: character-by-character Accounts input keeps focus, Journal year/account/expression changes reset offset, explicit offsets and Back survive, and genuine pending report/ledger changes do not expose stale exports. Preserve the m30 stale-data tests while replacing any assertion that needlessly requires unmounting for every navigation. Run yarn format:check, yarn lint, yarn test and yarn build from dashboard. Accepted outcome: tests fail on the original interaction and pass after repair, with native gates green.

## Context

See [FINDINGS.md](./FINDINGS.md) for exact repros, evidence and source locations. Scope: dashboard only; production ledger mutation is not needed.

Include real SSR/hydration coverage for equivalent percent-space and plus-space date ranges: initial overview nodes and a populated Income Statement table remain connected, amounts match, and no hydration exception occurs. Preserve genuine pending filter and ledger-change controls, including the passing delayed-response cross-ledger journey in FINDINGS.md.

Exercise BQL with the real router/layout: hold query A, submit B through the editor shortcut, settle B then A, and retain both correct results and pending identities without a history reopen. The existing query-retention test mocks navigation and cannot cover this unmount. Preserve history-only overlap and cross-ledger rejection controls.

## Implementation and verification

Completed 2026-09-18. The remaining gap was the hydration half of the acceptance: the only SSR check for the encoded date ranges mocked `Outlet` as a bare element and mocked every router hook, which cannot show a populated report surviving hydration. `dashboard/src/common/components/ledger-layout/__tests__/ledger-layout-hydration.test.tsx` now renders the real layout, route tree, loaders and providers to markup on a server router — a populated overview carrying Financial position, Money movement and Recent activity with a Net Worth amount, and a populated Income Statement table — then hydrates that markup with a client router still ingesting equivalent matches. For both the percent-space and plus-space date-range spellings the original heading, amount and table nodes stay connected, the amounts and the parsed time are unchanged, `onRecoverableError` is never called, no loading status card appears, and `main` is not `aria-busy`. Reverting the layout's scope comparison to plain `state.isLoading` fails all four cases; restoring it passes.

The rest of the acceptance was already met by t001 and t002 and was re-verified against `main` before writing any code. `accounts-filter-url.test.tsx:209` types Checking character by character at desktop and mobile widths and asserts the same input keeps focus and caret, including a mid-string edit. `journal-pagination-url.test.tsx:267` holds the navigation for shared time, account, expression and Clear all, asserts no stale rows or Export while pending, and asserts every new-scope read requests offset 0, alongside explicit-offset reload, drill-down Back and out-of-range coercion. `ledger-layout-scope-navigation.test.tsx:160` uses real route loaders to withhold the previous amount and Export across time, account, filter, ledger and report-path changes, and retains the mounted report for local and encoded-path navigation. `query-navigation.test.tsx:349,406` holds query A, submits B through the editor shortcut, settles B then A, and keeps both result cards and their CSV actions with the same editor node, plus the cross-ledger rejection control. The m30 stale-data assertions in `ledger-layout-pending.test.tsx` are preserved unchanged.

Gates from `dashboard/`: `yarn format:check`, `yarn lint` (tsc, ESLint and Knip), `yarn test` and `yarn build` all pass. The suite went from 373 files / 4,186 passed to 374 files / 4,190 passed, with the one pre-existing skip unchanged. `gitleaks dir . --redact --verbose` reports 26 findings, all in untracked and ignored local runtime artifacts; a scan of the changed directory reports no leaks. No runtime code changed for this task.
