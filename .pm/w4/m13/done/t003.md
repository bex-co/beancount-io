---
id: w4/m13/t003
title: Verify adoption journeys and guidance
worker: worker1
status: done
estimate: 10m
depends_on: [w4/m13/t002]
---

## Objective and acceptance

Repeat the actual Accounts, Journal and overlapping BQL submission journeys at desktop and narrow widths. Check dashboard README and scoped CLAUDE/AGENTS guidance against changed behavior; review root package-table accuracy if scope changes. No new package or skill is expected. Keep user and agent instructions consistent and record any drift as follow-up rather than unrelated edits. Accepted outcome: these journeys are usable and existing guidance accurately describes them.

## Context

See [FINDINGS.md](./FINDINGS.md) for exact repros, evidence and source locations. Scope: dashboard only; production ledger mutation is not needed.

## Adoption verification

Repeated the shipped production-preview journeys at 1440px and 390px with anonymous public-ledger reads. Accounts accepts Checking character by character, retains the original input and caret, and preserves search/type through account drill-down, Back, and reload. Journal's shared filters and Clear all start at offset 0 while explicit offset reload, account Back, and subtype toggles retain the intended behavior. Real Monaco keyboard submissions retain held query A while B returns, then show A=303 and B=404 in their own result cards with CSV actions; both widths issue exactly two successful reads with no page errors. The encoded-range overview and Income Statement hydration controls also retain their initial nodes without errors.

Reviewed dashboard/README.md, dashboard/AGENTS.md, the ledger-data and reports scoped guides, and the root dashboard package entries. Updated reports guidance for scope-sensitive loading, Journal's atomic shared-filter pagination reset, and the actual BQL `query` URL key. Existing setup, optional-query, package, and skill descriptions remain applicable; no new package, skill, or setup variable is required. The repository's current AGENTS-only structure was introduced by 04f274d2; its checker passes all 17 scopes, the shared development-skills link, and the separate customer tree.

A fresh development server starts and serves Accounts with HTTP 200; after the account list is ready, typing Checking produces the matching account and URL without page errors. A production-oriented network-idle wait timed out against the development server, so readiness was verified through the populated UI instead. Production-preview browser checks all passed. Formatting and agent-guidance checks pass; the unchanged runtime code retains bd39bd4b's full native-gate evidence. No hosted-deployment or fresh dependency-install claim is made.
