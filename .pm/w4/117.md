# Preserve small nonzero values in chart axis labels

**Severity:** minor. **Estimate:** 45 minutes. **Owner:** dashboard. **A2/A3:** readers should be able to distinguish the scale of a fractional holding without every tick claiming zero.

Production `https://beancount.io`, signed out, English/light, isolated headless Chromium149.0.7827.55. Local HEAD26d54a9d; fetched main bd5d1d31 retains this code. Asset `main-BIr0jJni.js`; exact deployed SHA unknown.

Open `/ledger/open_ledger/crypto-example/account/Assets%3ACrypto%3AMining%3ABTC?time=2025&lang=en`. Select Account Balance, Monthly, Units. Fresh1440×1000 and390×844 loads show a rising BTC line, but every Y-axis label reads `0.0`. ECharts' actual tick values are0,0.01,0.02,0.03,0.04 and approximately0.044; its rendered text spans contain six identical `0.0` labels. Real GetLedgerAccountReport HTTP200 with conversion=units supplies March0.004BTC, then monthly increments through December0.040BTC; the plotted series matches. Actual pointer hover on December correctly says `2025-12 / BTC: 0.04`, and desktop journal Balance correctly shows0.040BTC. This is axis formatting, not lost holdings or incorrect calculations.

Independent fractional-USD case: `/ledger/open_ledger/example/account/Equity%3AConversions%3ACurrent?time=2016&lang=en`, Monthly/At Cost, fresh390 and1440. The valid generated conversion carries0.01663USD; nonzero ticks0.003,0.006,0.009,0.012,0.015 and0.018 all render `0.0`. Its separate Entry Context failure is already covered by110, not this issue. Working scale control: switch the BTC account to At Cost; the USD axis reads0.0,1.0k,2.0k,3.0k,4.0k,4.5k.

Root: `dashboard/src/common/lib/chart/chart.ts:12–14` always calls `numbro(value).format("0.0a")`. Installed numbro2.5.0 parses that as one mantissa digit (`dist/numbro.js:953–967`); directly exercising the installed dependency confirms0.003,0.006,0.012,0.018 and−0.003 all become `0.0`, while1000 becomes1.0k. `features/reports/balance-sheet/line-chart.tsx:143–145` installs this formatter on the Account/Balance Sheet Y axis. The API and chart model retain the real values. The formatter dates to af5339de and has no subsequent repair.

Bounded fix: make the shared formatter preserve meaningful precision for fractional magnitudes, so nonzero ticks are not rendered as zero and the reproduced adjacent ticks remain distinguishable. Preserve signs, actual zero, string-number inputs, existing large-value abbreviations, plotted data and full tooltip values. Use the existing formatting dependency or standard number formatting; do not round ledger data, force all charts to cents, or change axis domains to conceal the problem. Scientific notation for extremely small values is acceptable if it remains readable and unambiguous. This is not a request to remove `.0` from ordinary zero ticks or redesign all number presentation.

Blast radius: four production formatter sites: Balance Sheet LineChart (also Account Balance); Income Statement date-balance common axes (single/stacked charts and Account Changes Over Time); Overview Net Worth; Budget charts. Only the Account cases above were independently reproduced. BQL charts use their own axis configuration and are outside this repair. Keep the shared fix within dashboard; no API, credential-policy, engine or dependency change.

Acceptance: both BTC widths display distinct nonzero labels for0.01–0.04; fractional USD ticks0.003–0.018 remain distinguishable. Verify positive/negative small fractions, zero, string inputs and existing k/m/b boundaries with meaningful formatter coverage, plus a rendered chart-option/browser control proving the consumer uses it. The current “small numbers” tests only cover100 and999, so they do not catch this failure. Run dashboard gates when implementing. Other chart consumers, locales, engines, authenticated roles and a patched implementation remain unverified.

Dedupe: searched all board states for formatYAxisNumber, numbro, small/zero/rounded axis ticks and precision. Completed w4/024 and060 concern mobile's separate shortNumber and currency annotations; w3/189 repaired account-journal quantity formatting, whose correct0.040BTC output is a positive control here. Open112 covers keyboard/text chart alternatives;116 covers legend contrast. None owns this numeric tick formatting.

Evidence: `dashboard/tmp/qa-20260917/small-axis-labels.json` contains both widths, live series/ticks/rendered text, API data, tooltip and working control. `dashboard/tmp/qa-20260917/small-btc-axis-1440.png` was visually inspected; narrow images are `small-btc-axis-390.png` and `small-axis-390.png` in the same directory. The reproduction and values above stand independently of ignored files. At Cost/390 restored; no product changes or production mutations.

Additional working precision control: Commodities CUSDC/USD at390px renders distinct axis labels0.0192,0.0200,0.0220,0.0240,0.0260 and0.0278; its keyboard price-history table retains source strings such as0.0240/0.0241/0.0253. Evidence: `dashboard/tmp/qa-20260917/commodity-small-axis-control.json`. That separate quote formatter (`features/ledger-data/commodities/format-commodity-price.ts`) was repaired by4009047c, former w3/012. Preserve it; this is a remaining report-axis surface, not a regression of that repair. Avoid a cross-feature import from common chart utilities into the Commodities feature.

Additional positive control: crypto-example BQL `SELECT date, sum(position) WHERE account = 'Assets:Crypto:Mining:BTC' GROUP BY date ORDER BY date` renders18 actual0.004 BTC points at390px, with distinct canvas labels0/0.001/0.002/0.003/0.004 and a full-value result table. An actual pointer move inside the plot shows2025-08-31 / BTC /0.004 BTC. BQL chart-utils supplies no shared abbreviated y-axis formatter, so this path is unaffected. `dashboard/tmp/qa-20260917/bql-small-axis-control.json` records its live chart model/rendered labels and API; preserve this behavior instead of applying117 to every chart.
