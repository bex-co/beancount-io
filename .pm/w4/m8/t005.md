---
id: w4/m8/t005
title: Adoption surface
worker: worker1
status: done
estimate: 25m
depends_on: [w4/m8/t004]
---

## Objective

Check that what this milestone ships is discoverable and usable on every surface it exposes, and that the documented steps still work exactly as written.

## Context

- `mobile/README.md`'s Languages section and `docs/app-store-localization.md` should describe the Play listing coverage; the `skills/CLAUDE.md` row for `mobile-release` should mention Play listing steps if the skill's description changes.
- Root `README.md` and `CLAUDE.md` package tables; `mobile/README.md` and `mobile/CLAUDE.md`.
- `AGENTS.md` symlinks and the shared-skills links are validated by `python3 scripts/check-agent-guidance.py`.

## Steps

1. Walk each documented surface as a newcomer would and fix stale or missing steps.
2. Update the package tables only where the shipped capability changes a row.
3. Run `python3 scripts/check-agent-guidance.py` from the repo root.
4. File any drift that is out of scope as an inbox note rather than diverging silently.

## Files

- mobile/README.md
- mobile/docs/app-store-localization.md
- skills/CLAUDE.md

## Acceptance criteria

- [ ] Every surface that exposes the change documents it and the documented steps work as written.
- [ ] `python3 scripts/check-agent-guidance.py` passes.
- [ ] Drift discovered but not fixed is filed on the board.

## Out of scope

- Marketing copy or launch posts.

## Validation evidence

- 2026-09-10: walked every surface — `mobile/README.md` (command table, Languages/Play paragraph), `mobile/CLAUDE.md` (Play section, CI row), root `CLAUDE.md` (CI row), `skills/CLAUDE.md` (`mobile-release` row), `mobile-release` skill (baseline/generate/apply-verify sections), `docs/app-store-localization.md` (Play choreography). Each documented command re-verified as written: `play:baseline` (t001), `play:generate` (idempotent, 16 files, plan SHA stable at `b4921f16…`), `metadata:validate`, `screenshots:validate`, `plan-play`/`apply-play`/`verify-play`.
- `python3 scripts/check-agent-guidance.py` passes (16 scopes, shared-skills links).
- No out-of-scope drift found; no inbox note filed.
