---
id: w4/m8/t001
title: Pull the current Google Play listing baseline
worker: worker1
status: done
estimate: 35m
depends_on: []
---

## Objective

Confirm the assumption that the Play listing is English-only by pulling every locale's current listing through the Google Play Developer API with the service account EAS Submit already uses, and record the baseline in ignored scratch state.

## Context

- `eas.json` submits to the `production` track; the service-account JSON is local-only and must stay gitignored like `.asc/`.
- The Developer API needs an `edits.insert` then `edits.listings.list` for the package `io.beancount.android`; a short signed-JWT script or a Python client can call it. Adding a Python client is a new dependency and needs the user's approval; the signed-JWT script needs none.
- Never print credentials or tokens in the recorded baseline.

## Steps

1. Decide the API client approach with the user if a dependency is needed.
2. Pull listings, screenshots metadata, and the feature graphic status per locale into `mobile/tmp/play-baseline/`.
3. Record which locales exist and what they contain in this task's validation evidence.

## Files

- mobile/tmp/play-baseline/ (scratch, gitignored)
- mobile/scripts/

## Acceptance criteria

- [x] The baseline lists every Play locale with title, short and full description presence, and screenshot counts.
- [x] No credential or token appears in any committed file or in the recorded evidence.

## Out of scope

- Changing anything on Play.

## Validation evidence

Live Developer API baseline captured 2026-09-09T03:47:17.805Z using the existing EAS Submit service account: only en-US exists; title, short description, and full description are present; 8 phone screenshots, 1 feature graphic, 1 icon, and no other screenshot types. The English-only assumption is confirmed. Public baseline is stored in ignored mobile/tmp/play-baseline/baseline.json; no key, token, or credential identifier is recorded here. The client creates and deletes an uncommitted edit.
