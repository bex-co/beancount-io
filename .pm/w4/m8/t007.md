---
id: w4/m8/t007
title: Test coverage
worker: worker1
status: done
estimate: 45m
depends_on: [w4/m8/t005]
---

## Objective

Add meaningful tests for the behavior this milestone shipped, asserting real outcomes and failure modes.

## Context

- Mobile unit tests run through `yarn test:unit` (the jest-lite runner in `mobile/scripts/`); dashboard tests through `yarn test`; backend-v2 through `yarn test`.
- Tests must fail when the covered behavior is broken; no snapshot-everything or tautological tests.

## Steps

1. List the behaviors and failure modes shipped by the implementation tasks.
2. Write one focused test per behavior; sabotage the implementation once to confirm each test fails.
3. Run the owning package's full check gate.

## Files

- mobile/src/__tests__/
- mobile/scripts/

## Acceptance criteria

- [ ] Locale mapping, limit enforcement, word-boundary trimming, and the dry-run credential redaction are each covered by a test that asserts the outcome.
- [ ] Each new test was seen to fail against a deliberately broken implementation before landing.

## Out of scope

- Coverage-number chasing.

## Validation evidence

- 2026-09-10: all four required behaviors are covered by outcome-asserting tests (`play-metadata.test.ts`, `play-api.test.ts`), and each was seen to fail against a deliberately broken implementation (sources backed up to `/tmp`, restored byte-identical afterward):
  1. Locale mapping — truncated derivation to 15 locales → "derives all 16 locales" fails (28 passed, 1 failed).
  2. Limit enforcement — relaxed the code-point limit by one → limit test fails (28 passed, 1 failed).
  3. Word-boundary trimming — returned the untrimmed string → trimming test plus the derived-copy validation fail (27 passed, 2 failed).
  4. Dry-run credential redaction — altered the redacted network-error message → redaction test fails (28 passed, 1 failed).
- After restore: 29 passed, `plan-play` SHA-256 still `b4921f1697ffee42c3218ce4ac7fd7ba454e3e62b3aab73d7fedfcca5cf5636f`.
