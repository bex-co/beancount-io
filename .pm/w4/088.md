# Keep current-page link semantics aligned with the actual destination

Severity: minor accessibility defect. Estimate: 50m, dashboard. A2: public-ledger readers should receive an accurate current-page indication when navigating with assistive technology.

Environment: production https://beancount.io, anonymous open_ledger/example, isolated headless Chromium149.0.7827.55, English/light320×800 and fresh1440×1000. Local main26d54a9db7d33c31a549bd12a0454a1c01f3f038; deployed main-BIr0jJni.js, exact deployed commit unverified.

Repro: open `/ledger/open_ledger/example/documents`, open the narrow sidebar, and inspect its navigation links. Both Overview and Documents declare aria-current="page". Navigate to Journal and reopen: Overview and Journal now both declare current page. Fresh desktop Journal reproduces this: Overview has data-active="false" but aria-current="page", while Journal has both data-active="true" and aria-current="page". The owner link to `/ledger/open_ledger` and the Related Pages Overview link also declare current page although neither is the current destination. Fresh Journal with `?time=2016` repeats all four declarations. On the actual Overview page its sidebar link correctly has both active=true and current=page; Journal is no longer marked current. Navigation and mobile-sheet dismissal work.

Expected: current-page semantics identify the actual page, consistent with the existing visual selection. Parent destinations may represent navigation hierarchy but must not claim to be the current page. Preserve filter/search propagation, keyboard navigation, grouped/collapsed sidebar behavior and visual active styling.

Root: `dashboard/src/common/components/ledger-layout/ledger-sidebar.tsx:330–335` computes exact currentPath equality for SidebarMenuButton's data-active, but its TanStack Link omits matching activeOptions. Installed router `src/link.tsx:209–235` defaults exact=false and matches pathname prefixes, then STATIC_ACTIVE_PROPS at724 adds aria-current=page. The owner Link in `ledger-switcher.tsx:224` and Link in `common/components/related-links.tsx:34` use the same default. These three application consumers account for the reproduced false declarations. Use destination-appropriate exact matching at these consumers, with a regression for filtered URLs; do not patch the dependency, change router-wide defaults or rewrite the navigation system. Inspect grouped/flyout consumers for consistency without changing deliberate section-selection behavior.

Dedupe/history: searched all .pm, including done/blocked, for aria-current/current-page, active links, prefix matching and sidebar selection. Completed w1/027 fixes ledger/language picker rows, a different control and state. Completed w3/m39 adds landmarks and skip navigation; it does not repair Link matching. Sidebar history af5339de/4652596f/042a6fbb/0bab7e0d retains the omitted options. Recent switcher009efb1e fixes picker-row current state, leaving these owner-link semantics unchanged. No newer main repair identified.

Acceptance: a real-router navigation test fails on the current code and passes after the repair: Overview itself is current, Journal/Documents each mark their own sidebar destination, and owner/Overview links cease claiming current page on descendants. Include a filtered Journal URL and related-page links. Verify narrow navigation closes the sheet and desktop/collapsed navigation still works; run dashboard gates. Avoid a mocked Link that cannot reproduce the router's automatic attributes.

Evidence: `dashboard/tmp/qa-20260917/navigation-current-page.json` records fresh Journal, filtered Journal and Overview control href/current/data-active values. Native Chromium AX inspection exposes the corresponding link names/URLs but does not expose a current property in this API; actual screen-reader speech is unverified. DOM aria-current is the reproduced defect, not a claimed speech transcript. No product changes, real-account writes or shipping.
