# Use grammatical singular counts in BQL, Statistics and Events

Minor copy defect; estimate 50m, dashboard. A2: query, Statistics and Events output should use grammatical count labels. Repeated dashboard QA, 2026-09-17.

Repro: on production `/ledger/open_ledger/example/query`, execute `SELECT date LIMIT 1` with Cmd+Enter, or open `/ledger/open_ledger/example/query?query=SELECT%20date%20LIMIT%201`. Both runs show one data row (`2015-01-01`) and the label **1 rows**. Expected: **1 row**, or a count-neutral **Rows: 1** label. The five-row control correctly returns five data rows; no result-count, query or export defect is claimed.

Environment: anonymous public synthetic example, QA-owned isolated headless Chromium 149.0.7827.55, 1440×1000, English/light; production asset main-BIr0jJni.js, exact deployed commit unverified. Local main 26d54a9db7d33c31a549bd12a0454a1c01f3f038.

Cause/fix: `features/bql/components/query-result-card.tsx:120` passes the correct rows.length to `bql.rowCount`; `features/bql/locales/en.ts:36` hardcodes `{count} rows`. Use supported localization pluralization, or the simpler count-neutral label, and keep all 15 locale shapes complete if keys change. This translation has one production consumer. Empty results use the separate no-data message; preserve that, table row counts, virtualization and CSV behavior. Verify one/many rendered labels with the real locale; existing mocked tests only assert the key. This reversible wording fix does not need a new test suite.

Dedupe: searched all queues including done/blocked for rowCount, 1 rows, singular/plural rows. Historical w1/m15's “1 rows were rejected” is CLI import output, a different package. BQL m28/m37 concern table structure/cell values. Path history af5339de/64b477da leaves the string unchanged; no newer repair or overlapping open item. Public API succeeds; the grammar error is in the client translation. BQL narrow width, other locales and actual screen-reader speech were not exercised for this finding.

Verified evidence: `dashboard/tmp/qa-20260917/query-single-row.json` and `query-single-row.png`, from the independent fresh-load repeat. No product fixes, production mutations or shipping.

Statistics extension, same copy-repair scope: fresh390×844 and1440×1000 on `/ledger/open_ledger/stock-example/statistics?filter=narration%3A%22NWRB%204-for-1%20share%20split%22&lang=en` show **Postings count per account (1 Accounts)** and **Total 1 entries across 12 types**. Actual GetLedgerPostingsPerAccount succeeds200 with one row, Assets:Brokerage:NWRB count2. GetLedgerEntriesCountPerType correctly reports one Transaction, all other types zero. The share split has two postings to the same account, making this a real one-account fixture. The earlier attempted generic account filter did not establish this case. Five Account Last Entries and its full-ledger dates remain intentional Fava semantics; a one-account last-entry roster is still unverified.

Additional roots: `features/ledger-data/statistics/postings-per-account.tsx:143–145` concatenates totalRows with the always-plural journal.accounts; `entries-count-by-type.tsx:120–123` assembles the total with the static “entries across” fragment in `statistics/locales/en.ts:20–22`. Both authoring paths remain from af5339de. Fix these two displayed counts with supported count-aware localization or natural count-neutral labels, preserving the meanings of postings, accounts, directive entries and types. Do not change shared journal.accounts globally into a singular label. Keep the existing zero-denominator percentage behavior, per-account links and filtered counts. Verify zero/one/many wording with the actual locale, and keep all15 catalogs complete if introducing keys.

Additional dedupe: completedw3/m14 repaired Statistics filter scope, and193 repaired “postings” terminology; retain both. w2/010, w2/020 and w3/194 concern backend/CLI write or commit messages, not these client summaries. Saved `dashboard/tmp/qa-20260917/statistics-single-count.json` includes both viewport copies and actual responses; `statistics-single-count-390.png` was visually inspected. No new finding group or milestone; the estimate now includes these two summaries.


Events extension (2026-09-18): production `/ledger/open_ledger/example/events?lang=en&time=2015-01` at1440×1000 and fresh390×844 shows **1 of 1 events** above the single January1 employer event. The captured desktop GetLedgerEvents request has time2015-01 and returns200 with exactly one event. Narrow reload independently repeats the UI; its SSR result was not independently captured. Earlier unfiltered9-of9 and2016-period4-of4 controls have correct plural wording and counts. Local Chicago plus employer filtering correctly reports no matches, and a period change clears those local filters.

Root: `dashboard/src/features/ledger-data/events/index.tsx:193` passes correct filtered/total values to `page.events.eventsCount`; `events/locales/en.ts:16` fixes the noun as events. The string remains unchanged from af5339de; the later Related Files route change does not affect it. Use a count-neutral construction such as Events:1 of1, or supported localization keyed to the total, preserving the filtered/total meaning. Include this fourth local summary in the same bounded wording repair; estimated50m total. Do not change filtering, time semantics, event types or w4/094's separate misleading description/empty-state scope.

Dedupe across current/done/blocked board items found no Events singular-count repair. Evidence `dashboard/tmp/qa-20260917/events-single-count.json` and visually inspected `events-single-count-390.png`. Verify zero/one/many wording with real locale output; no new test framework, product changes or writes occurred. Other locales and actual screen-reader speech remain unverified.
