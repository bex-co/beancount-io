# Name account-navigation and report-filter popups

**Severity:** minor. **Estimate:** 60 minutes. **Owner:** dashboard. **A2:** assistive technology should identify navigation/filter popups as well as the inputs that open them.

Production beancount.io, signed out, English/light, owned headless Chromium149.0.7827.55; HEAD26d54a9d, main bd5d1d31, deployed main-BIr0jJni.js (exact deploy SHA unknown).

Open a public example ledger, then Search accounts... beside Accounts. At1440×1000, including a fresh reload, the popup has role=dialog with no aria-label or aria-labelledby. Chromium Accessibility.getFullAXTree reports an unignored dialog with name="". The inner combobox correctly has name Search accounts.... At390×844 after a fresh reload, open Toggle Sidebar then Search accounts...: the native tree contains named dialog Sidebar and a second unnamed dialog. Rechecking after the opening animation settles repeats it; this is not an opacity/loading observation. The [WAI-ARIA dialog role](https://www.w3.org/TR/wai-aria-1.2/#dialog) requires an accessible name for both this nonmodal dialog and ordinary modal dialogs.

Root: `dashboard/src/common/components/ledger-layout/go-to-account.tsx` passes the translated placeholder to Command (naming its input) but gives PopoverContent no name. `common/components/ui/popover.tsx` forwards supported attributes unchanged to the Radix content, so the caller can supply the existing localized account-search string without a new primitive or translation. No API result determines the missing dialog name.

Give this account-navigation PopoverContent a localized accessible name using the existing account-search string or a stable heading association. Preserve Command's separate input label, loading/error/retry content, narrow placement, keyboard filtering and selection, and focus return. Do not assign one hardcoded name to all shared popovers or expand this small repair into a popup redesign.

Working controls: desktop searching Slate then Enter opens Liabilities:US:Chase:Slate with the existing Chicago tag filter retained; Account Journal loads. Desktop Escape after the popup detaches restores the Search accounts... trigger. Narrow Sidebar already has a valid dialog name. Include an actual role-and-name assertion for the rendered popup, and repeat native-tree checks at both widths; merely checking the input placeholder would miss this defect. Run dashboard gates when implementing.

Dedupe/history: all board states searched for unnamed dialogs, account palettes and popover labels. Historical w3/074, removed by4cad3dda, explicitly fixed the two Command input names; reading that original note confirms its scope was inputs. That fix passes here. Earlier dd6f2f71 and d7c0c449 repaired account loading/retry and placement. This missing outer label remains since af5339de. Other popover consumers are not included without their own reproduction; no authenticated ledger-switcher behavior is claimed.

Evidence: `dashboard/tmp/qa-20260917/account-search-dialog-names.json` holds native AX nodes and both viewport snapshots; `account-search-dialog-390.png` was inspected after animation settled. Actual screen-reader speech, other browsers, other locales and patched behavior remain unverified. No production mutation or product edit.

Additional same-pattern reproduction: Statistics `/ledger/open_ledger/example/statistics?lang=en&time=2016-02`,390×844, Filters → Time. The native tree has named dialog Report filters and an unnamed suggestion dialog; its named Time input is outside the popup. A fresh1440×1000 reload and opening Time repeats the unnamed dialog. `report-filter-popup-names.json` records both. This is `common/components/ui/combobox.tsx:253`, whose PopoverContent also has no label despite already resolving the localized field placeholder. Include that shared Combobox popup in this repair using its field identity; do not rename or remove the existing input/listbox semantics. Preserve blank/custom values and popover focus/blur behavior. The source-derived six ledger form wrappers reuse this component and should inherit a meaningful localized popup name; they were not exercised with authenticated forms. Coordinate w4/095 and113 in this same file (Escape/active-option defects), preserving their separate behavior scope. Existing w3/m20 provides the correctly named Report filters sheet, not this inner popup. Two small caller/component associations plus meaningful rendered checks remain a sub-hour item; no global Popover default name or unrelated popup sweep is intended.
