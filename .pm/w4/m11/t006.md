---
id: w4/m11/t006
title: Adoption surface
worker: worker1
status: todo
estimate: 25m
depends_on: [w4/m11/t005]
---

## Objective

Check that the changed balance display is described and shown correctly everywhere users and contributors meet it, and flag drift as follow-up work rather than letting it diverge silently.

## Context

- `mobile/README.md` (product tour), `mobile/docs/marketing-showcase/` (the root `README.md` embeds `01-home.webp`, which shows Net Worth), and the store screenshot pipeline (`yarn screenshots:build`, `yarn screenshots:validate`, `yarn metadata:validate`).
- The root `README.md` and `CLAUDE.md` mobile rows.

## Steps

1. Decide whether the Home and Accounts showcase images now differ materially from the app.
2. If they do, regenerate them through the existing pipeline, or record an inbox note when regeneration belongs to the next store release.
3. Confirm no README text describes balances in a way the new rule contradicts.

## Files

- `mobile/README.md`, `mobile/docs/marketing-showcase/` (only if regenerated)

## Acceptance criteria

- [ ] Showcase and store images are either current or tracked by a follow-up note.
- [ ] No documentation contradicts the display rule.

## Out of scope

- Cutting a store release (the `mobile-release` skill).
