# Expose the selected Net Worth chart/table view to assistive technology

Severity: minor accessibility defect. Estimate: 25m, dashboard. A2: public-ledger readers using assistive technology should receive the same active-view information as sighted readers.

Environment: production https://beancount.io, anonymous public demo open_ledger/circle, QA-owned isolated headless Chromium149.0.7827.55, English/light320×740 and fresh1440×1000. Local main26d54a9db7d33c31a549bd12a0454a1c01f3f038; main-BIr0jJni.js deployed asset, exact deployed commit unverified.

Repro: open `/ledger/open_ledger/circle`; focus Show table in Net Worth and press Enter. The view changes to12 monthly date/amount pairs and the Table button gets the selected background color. Both buttons keep only their action names, Show chart and Show table, with no pressed/selected state. Repeat after fresh desktop reload, selecting Table then Chart: visual selection changes each way but the native Chromium accessibility tree exposes only ordinary buttons with invalid=false/focusable=true (plus focused on the active element), no selection or pressed property. DOM aria-pressed and aria-selected are null in both states.

Expected: exactly the active view is programmatically identified, synchronized with the visible highlight. Fix the existing two buttons with aria-pressed derived from `view`, preserving their localized names and keyboard behavior. A new custom widget or dependency is unnecessary. Do not add aria-selected to an ordinary button without appropriate semantics.

Root: `dashboard/src/features/reports/overview/components/net-worth-card.tsx:164–184` derives only Button variant from chart/table state and sets names/click handlers; no state attribute is forwarded. Shared Button already forwards native props. Sole production NetWorthCard consumer is reports/overview/index.tsx:169. All Overview ledgers share it; only Circle was live-reproduced here. No API change, chart math or table redesign is needed.

Working controls: Enter activates Table, clicking Chart restores the chart, and the table's June2026 value3,509,975,000USD matches the headline. Large amounts fit at320px. This is a missing state announcement, not an execution, arithmetic, chart-data or naming failure.

Dedupe/history: all .pm including done/blocked searched for Net Worth view selection, Show chart/Show table and aria-pressed. Completed w3/139 repairs single-point chart visibility; w3/144 repairs date locale. Neither owns active-view semantics. Current source history af5339de/86ca4531 retains the omission; no later repair or deployment lag identified. Report combobox naming in w4/079 concerns different controls.

Acceptance: initial chart exposes pressed=true and table=false; selecting Table reverses them, selecting Chart restores them, and keyboard activation/focus and numeric output remain correct. Add a meaningful component interaction assertion to existing NetWorthCard coverage and run dashboard gates. Empty/loading data, other languages/engines, authenticated role and actual screen-reader speech are unverified; keep existing behavior.

Evidence: verified `dashboard/tmp/qa-20260917/networth-view-state.json` records fresh before/after/return DOM props/classes and native accessibility-tree properties. The320px Overview screenshot `circle-overview-320.png` is a large-number layout control, not evidence of the semantic omission. No product changes or shipping.

Live positive control: Budget on open_ledger/budgeting-envelopes at390px exposes All/All time pressed=true and every sibling false; keyboard selection of This year reverses its time-span state. The existing `features/ledger-data/budget/budget-filter-pills.tsx` demonstrates the same visual-button pattern with aria-pressed and a named group. Use that established behavior as guidance, without adding a cross-feature import.
