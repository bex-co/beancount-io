# Keep query-history text and actions inside narrow cards

**Severity:** minor. **Estimate:** 35 minutes. **Owner:** dashboard. **A2/A3:** a useful account query should not hide its own export and history-removal controls on a phone.

Production `https://beancount.io`, signed out, English/light, isolated headless Chromium149.0.7827.55. Local HEAD26d54a9d, fetched main bd5d1d31; deployed main-BIr0jJni.js, exact SHA unknown.

At390×844, open `/ledger/open_ledger/example/query?lang=en` and execute:

```sql
SELECT date, account, number WHERE account = 'Expenses:Taxes:Y2016:US:Federal:PreTax401k' LIMIT 3
```

The query succeeds with three real rows. Its history card visibly cuts off the SQL and shows neither Download CSV nor Delete query. Reloading the bookmarked query repeats it. The card occupies x8–382, but the query text occupies x49–419.86; Download CSV is at x427.86–463.86 and Delete query at x467.86–503.86. The card clips these controls with overflow:hidden. Document and main scrollWidth remain390, so page-overflow checks alone miss the defect. Fresh320px likewise clips them, with the card ending at312. Collapsing the card does not make its header actions fit.

Working controls: at1440px the same query and both actions fit. A short query card such as `select * from balances` fits at390. Keyboard Tab can reach the clipped Download button, causing the hidden-overflow card to scroll horizontally by123px and displace the left side of its summary; this is not a claim that the button is absent from the DOM or unreachable by keyboard. The default visible pointer target is missing until that side effect.

Real QueryShell HTTP200 receives the complete SQL above and returns three columns/date-account-number and three rows, including2016-01-14,2016-01-28 and2016-02-11 with1200.00. Neither the query editor nor API truncates the account name. The defect is the result-card header, independent of result-cell clipping in m18.

Root: `dashboard/src/features/bql/components/query-result-card.tsx:204–241` puts an overflow-hidden details card around a flex summary. Its query wrapper at220 is `flex-1 text-left` without a shrinking minimum; the pre at221 uses whitespace-pre-wrap/break-words, whose wrapping does not remove the flex item's intrinsic minimum width. The long account token determines the370.86px text width and pushes the shrink-0 action group beyond the card. The structure dates to af5339de and survives the later table/Inventory repairs.

Fix the summary's local flex sizing so the SQL text can shrink and wrap within the available width while both actions remain visible and operable. Preserve full query text, native details keyboard behavior, title/accessible names, action click propagation, loading/error states, table scrolling, virtualization and data. Do not truncate away the account name, hide actions, or allow horizontal scrolling of the whole card as the repair. No global Button or query/API change and no dependency.

Acceptance: the recorded long query, a short query, and collapsed/expanded history cards fit at320/390 and desktop; both header actions remain inside the visible card before focus. Keyboard traversal/activation retains usable text and action positions without scrolling the entire card sideways. Use browser geometry or equivalent layout verification; a class-name-only assertion would not establish this behavior. Preserve CSV output and query history semantics and run dashboard gates when implementing.

Dedupe: all board states searched for BQL/query history header/summary overflow, long SQL and clipped actions. Completed w3/m28 repaired result-table scrolling/semantics; w3/m37 repaired cell values. Open m18 owns virtual-row cell completeness, not this summary;077 owns row-count grammar. Coordinate edits to the shared file without expanding those issues. QueryResultCard has one production owner, the BQL page; all history cards share this layout. Other engines, locales, pointer download from the displaced state, and a patched implementation remain unverified.

Evidence: `dashboard/tmp/qa-20260917/bql-long-summary.json` contains initial/reloaded390,320,desktop, keyboard/collapsed geometry and the real API result. `dashboard/tmp/qa-20260917/bql-long-summary-390.png` was visually inspected. No product changes, production writes, commits or shipping.
