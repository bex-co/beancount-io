# Preserve Account Journal pagination when returning from transaction source

Severity: minor. Estimate: 60m, dashboard. A2: a reader inspecting transaction source should return to the same account-journal page.

Environment: production https://beancount.io, anonymous synthetic open_ledger/example, isolated headless Chromium149.0.7827.55, English/light1440×1000 and fresh390×844. Local/fetched main26d54a9db7d33c31a549bd12a0454a1c01f3f038; deployed asset main-BIr0jJni.js, exact deployed SHA unverified.

Repro: open `/ledger/open_ledger/example/account/Assets%3AUS%3ABofA%3AChecking`, then Next. Page2 is current; first entry is2017-06-21 Wine-Tarner Cable. Activate its date, then “Open source at main.bean:773” in Entry Context. Source opens at `/files/blob/main/main.bean?editMode=false&lineNumber=773`. Browser Back returns to the account URL, but page1 is current and the first entry is2017-09-07 Hoogle Payroll. Both desktop and fresh narrow repeats lose the position. Page changes never alter the account URL.

API control: actual GetLedgerAccountJournal reads return HTTP200,total275,limit20. Next correctly requests offset20; Back requests offset0. The server honors the selected offset; the client forgets it. No query interception, ledger edits or authentication mutations.

Root: `dashboard/src/features/reports/account/index.tsx:96` owns offset in component useState(0), passes it to GetLedgerAccountJournal and JournalPagination. Source navigation unmounts AccountJournalTable, and Back initializes offset0. The account route has no page-specific search validation or persistence. Persist this account-journal position in validated navigation state scoped to ledger/account and its filter identity; preserve real filter-change resets, sensible count bounds, pending-data safeguards and normal browser history. Do not turn account pagination into a shared report filter.

Dedupe/history: all .pm queues including done/blocked searched for account-journal pagination, source return and Back restoration. Completed w3/131 fixed the separate main Journal page in4d701a91; this account implementation still has its original component-only offset from af5339de. w4/m13 owns the shared router-loading boundary's Accounts typing and main Journal shared-filter reset, and explicitly leaves other account pagination unverified. Completed w3/m13 and m30 account work owns filter input/reset correctness; m31 owns Entry Context keyboard/focus. No existing item owns this account-page history state. Latest be1251ab account-filter wiring does not repair the lifetime.

Acceptance: desktop/narrow account page2→source→Back restores page2 and offset20, including cached reads. Forward still opens the selected source. Verify another account and genuine shared/type/subtype filter changes reset or restore according to the scoped identity; malformed/out-of-range persisted values remain bounded. Add a real router/unmount/back regression asserting actual account query offsets, not a mocked pagination button. Preserve main Journal's completed history behavior and run dashboard format/lint/test/build. Other browsers, authenticated editing, patched behavior and account-filter return combinations remain unverified.

Evidence: `dashboard/tmp/qa-20260917/account-source-back.json` records narrow before/source/after and real public account queries; desktop before/after also recorded in the live QA transcript. A wrong-role locator timed out before the narrow repeat; correcting it to the actual button reproduced the issue and is not a product finding. No product fixes or shipping.
