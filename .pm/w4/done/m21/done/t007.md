---
id: w4/m21/t007
title: Closeout
worker: worker1
status: done
estimate: 10m
depends_on: [w4/m21/t006]
---

## Objective

Verify the definition of done, record evidence and close the milestone through /pm.

See [FINDINGS.md](./FINDINGS.md) for evidence, source and limits.

## Acceptance criteria

- [x] All prior tasks and dashboard checks pass with the reproduced monetary errors resolved.
- [x] Move completed tasks and milestone into done, synchronize README status and the workstream checkbox.

## Outcome

Re-ran every recorded figure against a fresh production build of the shipped
code, by real pointer hover on the public ledgers.

| Definition-of-done clause | Verified |
| --- | --- |
| Tax balances keep 52,047.35 USD and 18,000 IRAUSD apart | `Cash Flow → Expenses:Taxes 52,047.35 USD`, IRAUSD disclosed, never added |
| Synopsys revenue displays 7,054 MUSD | `Income:Revenue → Cash Flow 7,054 MUSD` (at 390) |
| Adyen NetRevenue in EUR | `Income:NetRevenue → Cash Flow 26,328,539,000 EUR` |
| Parent balances and roles preserved | `Assets:US 73,386.297 USD` — cash leaves out, VACHR out |
| 386.28 USD and 25 VACHR never become 411.28 | `Other: 386.28 (0.48%)` |
| Single-unit control intact | `Slate: 1,768.88 (100%)` |
| Income no longer sums unlike units | `Income:US 129,974.58 USD`, against the API's `{ USD: -129974.58, IRAUSD: -18000, VACHR: -130 }` |

Mixed-unit scope is clear to readers: the example overview renders five charts
with no page errors and three disclosures — the Sankey and the bar chart name
IRAUSD and VACHR, the assets pie names VACHR — each naming only what it omits.
The single-currency crypto ledger renders four charts and no note. All fifteen
locales carry the note as a real translation. The Balance Sheet grid is two
cells side by side at the same top, after the fragment regression was found and
fixed in `af3f009a`.

Nested cash accounts are excluded before grouping, declared-role and filter
contracts hold (asserted in both directions in t006), and no API, dependency or
backend change was made — the response always carried the right units.

Gates from `dashboard/`: `yarn format:check`, `yarn lint`, `yarn test` (383
files, 4319 passed, 1 pre-existing skip) and `yarn build` all pass.

Limits, stated rather than glossed:

- **No screen reader or other engine.** Chromium only, emulated viewports at
  1440x1000 and 390x844.
- **Narrow tooltip geometry is unchanged and unverified.** FINDINGS recorded
  that a long Adyen tooltip extends past the narrow viewport; this milestone
  corrected what the tooltip *says*, not where it sits, and makes no claim about
  a fully visible narrow amount.
- **Public read-only ledgers only.** No authenticated or private ledger was
  exercised, and no production write occurred.
- **Consolidated browser suite proved unreliable** at sweeping canvas tooltips —
  small Sankey edges and a 0.48% slice are easy for a hover grid to miss, and
  one run was invalidated when a gate rebuild replaced the bundle mid-flight.
  Every figure above comes from targeted per-chart probes that reproduce; the
  suite now fails loudly when a page renders no charts rather than reporting
  empty tooltips as passes.

Follow-up left open: [w4/146](../../146.md) — the display unit could prefer the
ledger's declared operating currency, which the net-worth cards already read,
instead of inferring it from account counts.
