# Expose read-only Monaco state in source and entry readers

Updated engine control, 2026-09-18: independently installed Chrome153.0.8010.52, fresh headless temporary profile390×844, anonymous `/ledger/open_ledger/crypto-example/files/blob/main/main.offline.bean`, repeats native AX readonly=false and an EditContext DIV without aria-readonly. Actual Monaco options readOnly/domReadOnly are true. Ordinary x is rejected; the complete model remains unchanged and equals the real public file content (1123 UTF-8 bytes). An initial visible-row comparison differed because focus changed the virtualized viewport; full-model comparison rules out an edit. Evidence `dashboard/tmp/qa-20260917/readonly-editor-chrome153.json`; no Save or mutation. CDP detached and the owned browser closed. Scope and estimate remain unchanged.

Severity: minor accessibility-state defect. Original file-only estimate: 55m, dashboard; expanded m22 scope totals125m. A2: visitors reading public ledger source should be told that the editor is read-only before attempting an edit.

Production https://beancount.io, signed out, English/light, isolated headless Chromium149.0.7827.55 at390×844 and fresh1440×1000. Independent installed Chromium151.0.7922.34 at390×844 repeats the result. Local/fetched main26d54a9db7d33c31a549bd12a0454a1c01f3f038; deployed main-BIr0jJni.js, exact deployed SHA unverified. These are identified installed builds, not a claim about the latest browser release.

Repro: load `/ledger/open_ledger/example/files/blob/main/main.bean?lineNumber=1&lang=en` at390, or README.md with the same query in a fresh desktop context. Wait for the source editor. The native accessibility tree exposes its Editor content textbox with `readonly=false`; its input surface is a div with an attached EditContext and no aria-readonly. The Chromium151 README repeat agrees. The expected state is readonly=true for this public viewer.

Functional control: focus the fresh desktop README at the first line and press a single ordinary x key. The first line stays unchanged and Monaco reports “Cannot edit in read-only editor.” No save action or shortcut was invoked. Source write guards remain in place; this finding does not claim editable production data, an authorization bypass or a saved change. Native AX also calls its content editable/settable, but those properties alone are not the proof: the differing readonly property is the relevant evidence. Actual screen-reader speech and commands were not exercised.

Root: `dashboard/src/features/ledger-editor/file-editor/components/ledger-file-view/text-file-view.tsx` passes `readOnly={!effectiveEditMode}`. `text-editor.tsx:209–210` correctly sets both readOnly and domReadOnly (unchanged since af5339de), but leaves Monaco's EditContext mode at its default. Installed Monaco0.55.1 enables that mode when supported. Its `esm/vs/editor/browser/controller/editContext/native/screenReaderSupport.js:91–95` sets textbox semantics, autocomplete and label without setting aria-readonly. The native context div has no native readonly attribute. In contrast, `editContext/textArea/textAreaEditContext.js:520` applies readonly to the textarea when domReadOnly/readOnly are true. Recent file-editor shortcut/CI fixes do not change this input-mode behavior.

Isolated diagnostic control: in a separate Chromium149 context, removing window.EditContext before app initialization forces Monaco's textarea fallback. That same public README then has a native readonly textarea and AX readonly=true. This is an explicitly simulated browser-capability control, not a deployed fix or a tested physical screen reader. It narrows the problem to the current native input integration. Monaco already exposes the supported editContext option; assess a local read-only-mode fallback or another supported integration repair without changing dependencies or globally removing a browser API in the product.

Repair the owning file-editor integration so the active input reports its actual read-only/editable mode and stays correct when view mode changes. Preserve selection/copy, source line targeting, Find/Escape behavior, keyboard navigation, content, permission gates and the existing persistent editor/draft lifecycle. BQL and authorized editing must remain editable. Do not replace this with a static label that leaves the contradictory readonly state false, or introduce a broad editor rewrite.

Acceptance: native Chromium inspection of anonymous main.bean and README at390/1440 reports readonly=true; typing remains rejected. Verify the editable state and view/edit transitions using existing local component/state fixtures, plus existing draft/selection/shortcut regressions. A mocked Editor that merely echoes props is insufficient evidence for the native accessibility repair; repeat with real Monaco in a browser and run dashboard native gates. No new dependency is required. Authenticated production editing, actual assistive-technology speech, Firefox/WebKit and the patched behavior remain unverified.

Dedupe: all .pm queues including done/blocked searched for Monaco/source viewer readonly accessibility and aria-readonly. Earlier file-editor draft/Find work protects different behavior; m20 names financial tables,098 names page landmarks, and079 names report selectors. No active repair or newer source fix for this state found.

Evidence: `dashboard/tmp/qa-20260917/readonly-editor-state.json` preserves three real production DOM/native-AX reproductions, the separately labelled simulated fallback, exact browser versions and the rejected-keypress control. Extra contexts and the Chromium151 process were closed. No product edits, ledger writes, commits or shipping.

Additional passing controls in the original390px public main.bean viewer: Cmd+F finds2017-09-07 (1 of1), scrolls the target into view, and its257px-wide reduced Find widget fits. Escape closes Find and focuses Editor content; Shift+Tab reaches More actions. Tab can also reach Monaco's focusable sticky source heading. Preserve these behaviors. Evidence `dashboard/tmp/qa-20260917/readonly-find-control.json`.


## Expanded Entry Context reproduction and shared integration scope

Public example Journal2016 page2, first entry RiverBank Properties2016-11-03/main.bean:595: open its date button. At390×844 and a fresh1440×1000 reload, the Entry Context Editor content is an EditContext div with no aria-readonly and native AX readonly=false. A single ordinary x key at Home is rejected with “Cannot edit in read-only editor”; visible source text stays exactly unchanged. No save/delete action was available or invoked, and this is not an authorization bypass.

In a separately labelled diagnostic context, delete window.EditContext before loading the same real page. Entry Context now uses a textarea but still has native readOnly=false and AX readonly=false, unlike the passing source-viewer fallback above. Root: `features/journal/components/entry-context-panel.tsx:309–314` passes readOnly=!canWrite without domReadOnly. The extracted panel's current lines trace to50e34ffa. Installed Monaco textAreaEditContext.js:523 only applies native readonly when domReadOnly and readOnly are both true (apart from disabled IME). Both consumers also share the native EditContext limitation already proven above.

Use the existing `common/components/monaco-editor/index.tsx` integration or appropriate owning options to expose the actual read-only mode through supported Monaco behavior. Merely disabling EditContext at the file-viewer call site would leave this Entry Context case broken. Static caller inspection also finds two always-read-only deletion previews at `features/ledger-data/accounts/delete-account-dialog.tsx:208,245`, with the same missing domReadOnly option; those writer-only previews are source-derived scope, not production reproductions. Keep BQL, create-file and authorized entry/source editing editable, with view-mode changes, persistent models and draft guards intact. No dependency or global browser-capability removal is required.

Acceptance includes real native-AX checks of source and Entry Context in supported EditContext and textarea fallback modes, plus existing local permission/view-edit fixtures and source-derived preview coverage. Source line navigation, Find/copy, read-only key rejection and dialog focus must remain correct. Source navigation from Journalpage2 to main.bean595 then Back correctly retains offset60, firstdate2016-11-03 and61–120/393; preserve this passing control. Account's separate local-page reset is w4/093, not reproduced here.

Evidence: `dashboard/tmp/qa-20260917/entry-readonly-editor-state.json` stores both actual viewports, the rejected keypress and labelled fallback DOM/native-AX results. `journal-page2-source-back-control.json` records the passing round trip. Extra diagnostic context closed. Origin358ba4dc changes board records only; implicated source is unchanged. This expands102's same read-only-state finding group; multi-consumer integration and real-mode coverage now exceed one hour, so102 is promoted to m22 rather than left as a55m note.

### Older WebKit DOM corroboration

A separate owned headless WebKit18.2 session at390×844 loads the real public main.bean595 source without overriding browser capabilities. window.EditContext is absent; the source viewer uses TEXTAREA with native readOnly=true. The same engine opens Journal2016/offset60 RiverBank Entry Context at main.bean595 and uses TEXTAREA with readOnly=false. This independently corroborates the missing domReadOnly integration in the fallback consumer. The Playwright accessibility snapshot does not expose a readonly property here, so no WebKit native-AX readonly value or screen-reader speech is claimed. No edit keypress was attempted in this repeat; existing Chromium rejection evidence remains the read-only behavior control. This older engine is not evidence about current Safari. Saved `dashboard/tmp/qa-20260917/webkit-readonly-source-control.json`; owned WebKit closed. No additional finding, task or scope.

Additional keyboard control: the Chromium EditContext source viewer for stock-example transactions/sales.bean permits Tab to leave the editor and Shift+Tab to return. It is the last tabbable page element, so the intervening BODY active element reflects leaving page focus, not a trap. `dashboard/tmp/qa-20260917/source-tab-exit-control.json` records the actual elements. Preserve this behavior while repairing read-only accessibility state; no keyboard-trap claim is added.
