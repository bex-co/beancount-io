---
id: w4/m15/t006
title: Close and archive the verified repair
worker: worker1
status: done
estimate: 10m
depends_on: [w4/m15/t005]
---

## Objective

Archive only after the observable definition of done is met.

## Context

See [FINDINGS.md](./FINDINGS.md) and the milestone DoD. This schedules dashboard work; it does not authorize real credential submissions, recovery emails or account creation.

## Steps

Record implementation and validation evidence. Through /pm done, mark completed task rows/frontmatter, move task files into done, set milestone status done, move the entire milestone to w4/done/m15 and check its workstream checkbox. Do not close on code presence alone.

## Files

.pm/w4/m15; .pm/w4/README.md

## Acceptance criteria

- [x] Definition of done holds, evidence is recorded and no completed task or milestone remains in the open tree.

## Out of scope

Authentication policy/schema changes, reset-token workflows, new dependencies, production mutations and unrelated form redesign.

## Closeout

Completed 2026-09-19. Every clause of the definition of done was checked against the shipped code, a live signed-out walk and the rendered suite — not inferred from the diff.

- **Invalid state and descriptions.** Login's email and password, recovery's email, and registration's email, password and confirmation all expose `aria-invalid="true"` with an `aria-describedby` resolving to a `role="alert"` paragraph carrying the visible message. Verified live at 1440x1000 and 390x844 (38 checks, all passing) and pinned by 13 rendered cases.
- **Stable, non-colliding ids; descriptions clear; hints persist.** Ids follow the inputs' existing scheme (`email-error`, `password-error`, `confirmPassword-error`, `firstName-error`, `lastName-error`, and the prefixed `forgot-password-email-error`). They cannot collide: `login-page`, `forgot-password-page`, the register flow's early return, and the OAuth, mobile and identity consent pages are step machines whose login, forgot-password and register branches are mutually exclusive. Correction clears both attributes with no stale association, and Username's `username-hint` survives a failed submit.
- **Announced without duplication; existing behaviour intact.** Each error is announced once by its own `role="alert"`, matching the pattern Username already used. First-invalid focus still lands on email, onBlur/onTouched timing, password visibility toggles, the recovery form's disabled-until-valid button, submit blocking and the server-error alert are all unchanged and covered.
- **No mutation on empty local validation.** The captured GraphQL operation list was empty across all six live page visits, and the rendered suite asserts the submit handler and `SendForgotPasswordLink` are never invoked. No credential was entered, no reset email sent and no account created.
- **Meaningful tests plus browser metadata.** `features/auth/components/__tests__/field-error-association.test.tsx` covers clean, invalid and corrected states against the real components and resolvers, requiring every referenced id to resolve rather than accepting a visible string; reverting the three components fails 12 of its 13 cases. Desktop and narrow accessibility metadata came from the t003 walk.
- **Gates and honest limits.** `yarn format:check`, `yarn lint`, `yarn build` and `yarn test` pass from `dashboard/` at 376 files and 4,236 tests. Live screen-reader speech is not claimed — this records browser and accessibility-tree metadata — and the OAuth, mobile and identity consent wrappers remain source-traced only, as FINDINGS states.

Standing closing work is complete: t003 verified the adoption surface and found no documentation drift, t004 ran the simplify review and fixed the one real finding it produced, and t005 added the regression suite.

Beyond the stated scope, t004 also repaired `firstName` and `lastName`, which sat unassociated in a file where four other fields had been fixed. Recorded rather than folded in: the reset-password and OTP forms inside auth, the importer `editable-cell` cluster, and the structural observation that `common/components/ui/form.tsx` already provides this association for roughly a dozen other forms — making auth the sole hand-rolled outlier — are captured with their evidence and the two primitive fixes that must precede any migration in [140](../../140.md), shipped in `ef926f87`.
