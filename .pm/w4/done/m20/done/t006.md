---
id: w4/m20/t006
title: Closeout
worker: worker1
status: done
estimate: 10m
depends_on: [w4/m20/t005]
---

## Objective

Close the milestone with /pm done only after the repairs and required verification are complete.

## Context

See [FINDINGS.md](./FINDINGS.md) for reproductions, source, controls and evidence limits.

## Acceptance criteria

- [x] Record implementation and validation evidence, move completed tasks and milestone to done, and synchronize the workstream checkbox.
- [x] Retired091 stays retired; do not claim authenticated actions or assistive-technology speech that was not exercised.

## Out of scope

API/data changes, global Table defaults, new dependencies, unrelated control labels and a broad accessibility redesign.

## Outcome

Re-ran every recorded check against a fresh production build of the shipped
code: **46/46** at 1440x1000 and 390x844, reading Chromium's own accessibility
tree over CDP.

All seven authoring sites now expose a meaningful name matching their visible
identity:

| Journey | Names |
| --- | --- |
| Budget | `Expenses:Dining USD`, `Expenses:Groceries USD`, `Expenses:Insurance:Car USD`, `Expenses:Rent USD`, `Expenses:Transport USD`, `Expenses:Travel USD`, `Expenses:Utilities USD` |
| Commodities | `ACME/USD`, `BMKT/USD`, `NWRB/USD` |
| Statistics | `Postings per Account`, `Account Last Entries`, `Entries Count by Type` |
| Settings | `Beancount Options`, `Fava Options` |

Definition of done, clause by clause:

- all fifteen tables across the four public journeys are named, and every name
  is distinct within its page;
- names stay correct through supported interactions — narrowing the budget
  interval filter to Monthly leaves all seven present, named and distinct, and
  a commodity history keeps its name across close and reopen, including when
  opened by keyboard;
- associations are stable and collision-safe: every `aria-labelledby` idref
  resolves to exactly one element, and no page in the set has a duplicate id at
  all;
- existing data and semantics are intact — 26 / 15 / 12 Statistics rows,
  10 / 19 Settings rows, 13 / 13 / 15 commodity prices at unchanged precision
  (`61.40`, `0.0225`), 41 account links still reachable inside the Statistics
  tables, no Delete control exposed to the public budget reader, and zero
  inputs, textareas or selects on the public Settings page;
- localized headings are reused rather than replaced — no translation key was
  added or changed anywhere in this milestone;
- meaningful rendered regressions exist and are load-bearing: stripping the
  associations back to bare `<Table>` fails 12 tests across 4 files;
- `yarn format:check`, `yarn lint`, `yarn test` (379 files, 4278 passed, 1
  pre-existing skip) and `yarn build` pass.

**Not claimed.** No assistive-technology speech or shortcut behaviour was
exercised — every result here is native accessibility-tree metadata and DOM
association, the same boundary FINDINGS drew. No authenticated action was taken:
the budget Delete control and the gated Settings sections were confirmed absent
for an anonymous reader, not driven. Other locales and other browser engines are
unverified.

Retired `w4/091` stays retired: its tombstone in the workstream README still
points at this milestone, and no file was recreated for it.

Follow-up left open deliberately: [w4/145](../../145.md) records the copy-pasted
Statistics and Settings section shells, whose extraction would make this naming
contract structural rather than seven correct copies.
