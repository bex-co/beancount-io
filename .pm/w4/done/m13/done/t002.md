---
id: w4/m13/t002
title: Keep Journal pagination resets correct through scope changes
worker: worker1
status: done
estimate: 30m
depends_on: [w4/m13/t001]
---

## Objective and acceptance

Coordinate with the shared layout fix so genuine time/account/expression changes and Clear all reset Journal to offset 0. Relevant files: features/journal/pages/journal-page.tsx and common/providers/ledger-search-params-provider/. Preserve explicit offset links, reload, subtype filters and journal→account→Back restoration. Reproduce the 2016 page-2 scenario in FINDINGS.md. Do not reset on every mount or weaken pending exports.

## Context

See [FINDINGS.md](./FINDINGS.md) for exact repros, evidence and source locations. Scope: dashboard only; production ledger mutation is not needed.

## Implementation and verification

Shared time/account/expression edits and Clear all now clear Journal's offset in the same router navigation. The provider compares normalized filter values and limits this reset to the Journal route. Journal's local reset identity now owns directive/subtype edits only; history and explicit links can restore a shared scope together with its requested offset. The existing out-of-range correction is unchanged.

The original failure reproduced in a production preview at 1440px and 390px using anonymous public example-ledger reads: selecting 2016 from offset 60 sent offset 60 and settled at results 61–120 / 393, starting 2016-11-03. After the repair, both widths request offset 0 and show 1–60 / 393, beginning 2016-12-31 Cafe Modagor. Account Expenses, expression `payee:"."`, and Clear all likewise request offset 0 and show the first 60 of 760, 758, and 1044 results respectively. Explicit offset reload, Journal→account→Back, and selecting/clearing Pending transactions pass at both widths. These checks exercised the local production build with recorded public reads, not an assertion that the hosted deployment was already updated.

The Journal integration suite now mounts the actual router, layout, provider, subtype controls, and Export button. Four held-navigation cases check that stale rows and Export disappear while pending and that every new-scope query requests offset 0. History, explicit scope/offset navigation, out-of-range offsets, and unrelated action parameters are covered. Reverting both source fixes makes all four new shared-filter cases fail (4 failed, 7 passed); restoring them passes. Provider tests preserve normalized no-op offsets and unrelated search on other routes.

Dashboard format, lint (TypeScript/ESLint/Knip), production build, and the full suite passed: 373 files, 4186 tests passed, one existing skipped test. No new dependencies or production ledger writes.
