---
id: w4/m13/t006
title: Close out only after all live journeys pass
worker: worker1
status: done
estimate: 10m
depends_on: [w4/m13/t005]
---

## Objective and acceptance

Verify the milestone definition of done against the implementation and repeated browser journeys. Do not infer live success from mocked page tests. Use /pm done to mark/move remaining tasks to m13/done, mark all README rows DONE and Status done, move m13 to w4/done/m13, and check the workstream box. Accepted outcome: the DoD holds and no completed milestone/tasks remain in the open tree.

## Context

See [FINDINGS.md](./FINDINGS.md) for exact repros, evidence and source locations. Scope: dashboard only; production ledger mutation is not needed.

## Verification

Completed 2026-09-18. The definition of done was checked against live browser journeys, not inferred from the page tests. A local production build served the real dashboard while anonymous read-only reads of the public `open_ledger` example, apple, and stock-example ledgers came from the public Beancount.io API through a single origin, so the browser exercised the shipped boundary rather than the hosted deployment. 62 checks ran in fresh isolated headless Chromium contexts at 1440x1000 and 390x844; all 62 passed.

Accounts: typing Checking character by character at a real cadence keeps the same input focused with the caret at the end for all eight characters, settles at `search=Checking` with the single Assets:US:BofA:Checking row, and keyboard selection of a type chip keeps that chip focused with `aria-pressed=true`. Back returns to the unfiltered list and a reloaded `search=Checking&type=Assets` URL restores both filters.

Journal: from `offset=60` (Results 61-120 / 1044) a Time change to 2016 drops the offset and settles at Results 1-60 / 393 starting 2016-12-31 Cafe Modagor. A shared Account change settles at Results 1-60 / 760, a shared expression `payee:"."` at Results 1-60 / 758, and Clear all at Results 1-60 / 1044, each without an offset. An explicit `offset=60&time=2016` reload still shows Results 61-120 / 393, and a Postings account drill-down followed by Back returns to that same page. A filter matching nothing reports Results 0-0 / 0 with no rows.

Pending and cross-ledger reads: with the new read held, an in-app Time change on the Income Statement removes all three tables and the Export control and marks main `aria-busy`, with no stale amount left standing for the new scope; releasing the read restores three tables and Export at `time=2024`. A held previous-ledger read released after moving to another ledger never places the old ledger's amount in the new report.

Hydration: `%20` and `+` spellings of the same date range on the stock-example overview and the apple Income Statement both keep the server-rendered nodes connected with unchanged text (Income -637,334, OtherNet -572, Revenue -636,762 for 2025-09 - 2026-06), emit no React 418 or other hydration error, show no loading card and leave main not busy. Unfiltered and plain-year loads pass as controls.

BQL: holding query A before transport, submitting query B through the editor shortcut, settling B and then releasing A leaves the same editor node mounted throughout, keeps A's card executing while B returns 404, and lands 303 in A's own card with both Download CSV actions enabled and no history reopen.

The milestone's last clause also holds: the integration checks added in t001, t002 and t005 mount the real pending layout and router rather than isolated page mocks, and `yarn format:check`, `yarn lint`, `yarn test` (374 files, 4,190 passed, 1 pre-existing skip) and `yarn build` all pass from `dashboard/`. No production ledger was mutated and no credentials were used; every journey was an anonymous read.
