---
id: w4/m19/t003
title: Adoption surface
worker: worker1
status: done
estimate: 15m
depends_on: [w4/m19/t002]
---

## Objective

Verify public Star and Follow entry points at desktop and narrow widths; check owning README quickstarts and root package tables for drift. No new skill or package is introduced.

## Context

See [FINDINGS.md](./FINDINGS.md) for reproduction, source and evidence limits.

## Acceptance criteria

- [x] Return targets remain usable; any documentation drift is recorded and no unnecessary documentation rewrite is introduced.

## Out of scope

Backend/API changes, authentication policy changes, new dependencies and unrelated login defects.

## Outcome

Re-ran the public entry points against a build carrying both t001 and t002,
signed out, 12/12 at 1440 and 390:

- Star from a ledger with a query and a fragment, and Follow from a profile with
  a fragment only, both emit the destination exactly as entered with one `#`;
- replaying each emitted continuation loads the intended page — the check that
  matters, since the malformed target used to render "Error loading data /
  invalid information";
- the plain-path control and a query-only destination are unchanged.

Documentation surfaces checked for drift, none found:

- root `README.md` — its package table lists `dashboard/` and is unaffected; its
  "Star on GitHub" links are about the repository, not the product's Star
  action;
- `dashboard/README.md` — names `/login`, `/sign-up` and `/auth` only as SEO
  routes, and its script table is untouched;
- `dashboard/docs/event-taxonomy.md` — tracks a `login` event from
  `use-login-form.ts`; the `next` parameter is not part of any event contract;
- `AGENTS.md`, `skills/AGENTS.md` and `.agents/AGENTS.md` — no skill, command,
  package or setting was added or changed, so no catalog entry applies.

Nothing documents the continuation's URL shape, so there was nothing to correct
and no rewrite was introduced. No code changed for this task.
