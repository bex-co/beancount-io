---
id: w4/m1/t006
title: Adoption surface
worker: worker1
status: done
estimate: 20m
depends_on: [w4/m1/t005]
---

## Objective

Ensure the newly shipped statement-download capability is discoverable and accurately described wherever dashboard reporting is introduced to users or contributors.

## Context

- This is a user-facing capability, so the standing adoption-surface review is required.
- Applicable surfaces may include the dashboard README, public documentation, report empty/help copy, and the root package description; update only surfaces where report capabilities are actually enumerated.
- No new package, skill, instruction scope, or symlink is expected from this milestone, but verify that assumption.

## Steps

1. Audit the owning package README, root README/package table, public docs, and report help copy for places that enumerate financial-report capabilities.
2. Add a terse discoverability note and exact CSV / Print-Save-as-PDF usage where applicable; record why unchanged surfaces are not applicable.
3. Verify all documented steps and labels match the shipped interface.
4. Confirm no `CLAUDE.md`, `AGENTS.md`, or skills-symlink structural change was introduced.

## Files

- `dashboard/README.md` (if applicable)
- `README.md` (if applicable)
- `docs/**` (if applicable)
- `dashboard/src/features/reports/export/**`
- `.pm/w4/m1/t006.md` (audit record)

## Acceptance criteria

- [x] Users can discover from an applicable public surface that Balance Sheet and P&L support CSV and Print / Save as PDF.
- [x] Documented labels and steps match the product exactly.
- [x] Instruction and skills symlinks remain valid, or any unrelated drift is recorded as follow-up work.

## Out of scope

- Launch campaigns, unsolicited promotion, new skills, or documentation for deferred XLSX/scheduling features.

## Completion

- Updated `dashboard/README.md`, the applicable public package surface, with the exact **Spreadsheet CSV** and **Print / Save as PDF** flow. The root README package table stays intentionally high-level and no public doc/help page separately enumerates dashboard statement actions, so duplicating the note there was declined.
- Updated the existing report-scoped `CLAUDE.md` to document the feature/common ownership boundary and the intentionally limited statement scope; no instruction file or symlink was added, moved, or removed.
- Verified `dashboard/src/features/reports/AGENTS.md` still resolves to its sibling `CLAUDE.md` and root `.agents/skills` still resolves to `skills/.claude/skills/`.
