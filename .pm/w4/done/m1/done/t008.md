---
id: w4/m1/t008
title: Test coverage
worker: worker1
status: done
estimate: 45m
depends_on: [w4/m1/t007]
---

## Objective

Prove the exported data, security behavior, report wiring, and print semantics with meaningful automated tests and final package checks.

## Context

- Tests belong beside their report/export code under `__tests__/`.
- Security and financial-format failure modes matter more than broad render snapshots.
- Dashboard CI runs `yarn format:check`, `yarn lint`, `yarn test`, and `yarn build` from `dashboard/`.

## Steps

1. Add focused tests for hierarchy flattening, display signs, rollups, multi-currency rows, zero/closed filtering, and deterministic ordering.
2. Add CSV tests for BOM, formula injection, Unicode, delimiters/newlines, raw amount preservation, filename sanitization, empty data, and cleanup.
3. Add component tests showing both reports pass their current context to CSV and print actions and handle failures without duplicate analytics.
4. Test semantic print headings/table associations and the absence of dashboard-only content.
5. Run the complete dashboard CI command set.

## Files

- `dashboard/src/features/reports/export/**/__tests__/**`
- `dashboard/src/common/lib/export/__tests__/**`
- `dashboard/src/features/reports/balance-sheet/__tests__/**`
- `dashboard/src/features/reports/income-statement/__tests__/**`

## Acceptance criteria

- [x] Tests assert real output values and failure behavior rather than relying on broad snapshots.
- [x] A malicious formula-like account name cannot produce an executable CSV cell.
- [x] Both statement pages are covered for CSV and print wiring using active report context.
- [x] `yarn format:check`, `yarn lint`, `yarn test`, and `yarn build` pass from `dashboard/`.

## Out of scope

- Browser-matrix automation, pixel-perfect PDF snapshots, service integration tests, or performance load testing.

## Completion

- Added value-level tests for both report adapters/wiring, exact totals, hierarchy/filter/sign mapping, formula safety, BOM/Unicode, stable CSV schema and names, object-URL cleanup, semantic print markup, keyboard operation, empty/busy states, failures, and privacy-safe analytics.
- Final dashboard checks on 2026-08-16: `yarn format:check` passed; `yarn lint` passed; `yarn test --run` passed 227 files and 3,172 tests with one existing skip; `yarn build` passed the client and server production builds.
- The complete suite still prints pre-existing test warnings, but exits successfully with no new failure.
