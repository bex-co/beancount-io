---
id: w4/m1/t007
title: Simplify
worker: worker1
status: done
estimate: 20m
depends_on: [w4/m1/t006]
---

## Objective

Run the repository's simplification pass over the statement-export implementation and remove avoidable duplication or complexity without changing behavior.

## Context

- Pay special attention to duplicated CSV escaping/download code, Balance Sheet versus P&L adapters, hierarchy recursion, filename sanitization, and print-only rendering.
- Preserve feature ownership: statement behavior stays under `features/reports/export/`; only genuinely generic format helpers belong under `common/lib/export/`.

## Steps

1. Run `/simplify` over the files changed by t001–t006.
2. Consolidate repeated logic and remove unused abstractions while keeping public component boundaries clear.
3. Re-run focused tests after each behavior-preserving change.
4. Record any larger refactor as follow-up rather than expanding this milestone.

## Files

- `dashboard/src/features/reports/export/**`
- `dashboard/src/features/reports/balance-sheet/**`
- `dashboard/src/features/reports/income-statement/**`
- `dashboard/src/common/lib/export/**`
- `dashboard/src/features/bql/lib/export-utils.ts`

## Acceptance criteria

- [x] `/simplify` findings are applied or explicitly declined with rationale.
- [x] Secure CSV behavior has one authoritative generic implementation.
- [x] Balance Sheet and P&L share export infrastructure without obscuring their distinct statement semantics.
- [x] Focused tests remain green with no intended behavior change.

## Out of scope

- Unrelated report refactors, redesigning the interactive report pages, or changing GraphQL schemas.

## Completion

- Simplification review retained one authoritative secure CSV implementation under `common/lib/export/`; BQL and legacy public functions are compatibility wrappers, not serializers.
- Balance Sheet and P&L share hierarchy, amount, context, CSV, print, menu, and analytics infrastructure while their adapters keep distinct section and sign semantics.
- Kept the compact model/adapter module rather than adding speculative type/filename layers, centralized locale entry structure, and used a body-level print portal to remove brittle app-shell selectors.
- No larger refactor was needed; focused export, BQL, and legacy CSV tests remained green after the pass.
