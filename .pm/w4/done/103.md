# Describe the actual Money movement comparison window

Severity: minor misleading report label. Estimate: 40m, dashboard. A2: public readers should understand the period behind an overview comparison.

Signed-out production Chromium 149.0.7827.55, English/light: open `/ledger/open_ledger/stock-example?time=2026-02-01%20-%202026-03-31&lang=en`. March Money in is +115 USD and its footer says “Previous 3 months average” +1,686 USD, although this selection contains only one prior month. Fresh 1440×1000 and 390×844 controls reproduce it. Extend the selection to January–March: the same March total remains +115 but the identically labelled average becomes +843. December–March produces +562. Money out similarly shows −4.95, −2.48 and −1.65. The averages demonstrably use one, two and three selected prior points, respectively.

Root: `dashboard/src/features/reports/overview/lib/overview-utils.ts:295` selects earlier points from the already filtered series and caps them at three; `averageBalances` at260 divides by the actual points.length. `components/money-movement-section.tsx:139` always renders `previousThreeMonthsAverage`. Its English value and translator description in `dashboard-translations.ts:94` and27 promise three months; all15 locales express the same fixed window. Both helper and label trace to af5339de, unchanged at local/fetched main26d54a9db7d33c31a549bd12a0454a1c01f3f038. Production entry main-BIr0jJni.js; exact deployed SHA unverified.

Use wording that accurately explains an average over up to three prior selected periods, or expose the actual comparison count to the label. Account for partially selected first months without describing them as complete months. Update the15 locale values and translator description. Preserve the existing available-point calculation, signs, rounding, three-point cap, filters and empty-average dash; this finding does not establish an arithmetic defect or authorize fetching outside the selected range.

Acceptance: one-, two- and three-point windows describe their actual scope while retaining the observed values above. Check a partial-first-month and a zero-prior-point selection, both cards and narrow/desktop layouts. Existing full-window average tests must remain valid; add a meaningful window-count case if the chosen repair introduces count-dependent behavior. No API/dependency change is needed. Other locales' runtime and a patched implementation remain unverified.

Dedupe: all .pm queues including done/blocked searched for previous/three-month averages; no matching report-label issue. w4/done/038 concerns unrelated budget proration. Completed w3/145's partial-period drill-down remains correct: February15–March15 overview links and actual Income Statement Markdown preserve March1–15. Do not reopen that issue.

Evidence: `dashboard/tmp/qa-20260917/movement-average-window.json` contains all four viewport/window texts; `movement-one-month-average-390.png` records the narrow view. Partial drill-down controls are `stock-partial-drill-control.json` and `stock-partial-income.md`. No account, ledger or server writes, product edits, commits or shipping.

---

**Resolved 2026-09-19** — Reproduced against a local build of the public
stock-example, exactly as recorded here: March's Money in stayed +115 USD while
the single label "Previous 3 months average" sat above +1,686, +843 and +562 as
the selection grew from one to three prior months, and Money out showed -4.95,
-2.48 and -1.65. A selection containing no prior month showed "—" under the
same label.

`overview-utils.ts` takes the earlier points from the already filtered series,
caps them at three, and divides by however many it found — correct arithmetic
under a label that promised a fixed window. The label now reads "Average of up
to 3 preceding periods" in all fifteen locales, with a translator description
that says what it is. It no longer claims three, and it no longer claims
months, which also keeps it honest when a selection starts mid-month and its
first point is a partial period rather than a complete one. The key was renamed
from `previousThreeMonthsAverage` to `precedingPeriodsAverage` so the name
stops promising the same thing.

Copy only: the available-point calculation, the three-point cap, the signs,
rounding, filters and the empty-average dash are untouched, nothing is fetched
outside the selected range, and no count-dependent behaviour was introduced, so
no window-count test was needed. The existing full-window average tests still
pass unchanged.

Verified in a real browser, 13/13 at 1440px and 390px: the one-, two- and
three-prior-period selections keep exactly the values above under the new
label; a selection with no prior period still shows "—"; a mid-month start
(2026-01-15 - 2026-03-31) behaves like its point count; and French, Russian and
Japanese render their own wording. Gates from `dashboard/`:
`yarn format:check`, `yarn lint`, `yarn test` (395 files, 4436 passed, 1
pre-existing skip) and `yarn build` all pass. Other locales' runtime remains
unverified. Shipped in `9e463804` (`dashboard`).
