---
id: w4/m16/t006
title: Close out after all four report journeys pass
worker: worker1
status: done
estimate: 15m
depends_on: [w4/m16/t005]
---

## Objective

Replay all FINDINGS journeys on the changed application, record checks and limits, and close via /pm only when acceptance holds.

## Context

See [FINDINGS.md](./FINDINGS.md) and the milestone definition of done. Preserve the settled-data/export requirements in reports/CLAUDE.md.

## Files

FINDINGS.md; milestone README; task statuses through /pm

## Acceptance criteria

- [x] All four reports (including Trial Balance conversion changes) plus fresh narrow Cash Flow preserve selection; cached control and pending safeguards pass. No false completion based on mocked content.

## Out of scope

API/schema changes, financial calculations, new dependencies and unrelated navigation preferences.

## Closeout

Completed 2026-09-19. Every clause of the definition of done was checked against the shipped code, live browser journeys and the regression suite.

- **Cash Flow By Activity survives an uncached interval, desktop and narrow agreeing.** Verified live at 1440x1000 and 390x844 against the public `open_ledger/public-company-template` ledger: By Activity is still selected after Monthly becomes Quarterly, through the desktop tab strip and the narrow View selector alike.
- **Income Statement Income and Balance Sheet Assets likewise.** Income survives an uncached Yearly and Assets survives an uncached Weekly, both widths. Other valid selections follow the same contract because the page now owns the selection for every option, not a special case per chart — and `chartMode` on Income Statement was lifted the same way in t004 after the review found it had the identical defect.
- **Trial Balance Equity survives an uncached conversion, cached conversions still retain it.** Equity survives changing At Cost to Units at both widths, and returning to the now-cached Monthly on Cash Flow also retains By Activity, so the repair did not simply mask the cached path.
- **Pending reads stay explicit and expose no stale values or exports.** Unchanged by design: `selectSettledReportData` still classifies every loading response as pending and each page still returns `ReportLoadingState`, so the statement, its amounts and its export are withheld under new interval or conversion metadata. Cash Flow's suite pins this directly, asserting the loading state is what renders while pending. Collapsing and re-expanding the charts section leaves the selection intact, and error, retry and responsive switching were not touched.
- **State is scoped to the report and ledger.** Per-page React state, no global store, no new dependency, no router rewrite and no stale-data fallback. The ledger layout keys its subtree by ledger id, so a ledger switch discards it and no old ledger's selection reaches a new ledger.
- **Meaningful tests plus green gates.** All four reports have a lifetime regression driven through the real page across pending and success, plus a cached control and the pending-export assertion; reverting the four pages to a constant with a no-op setter fails exactly four cases, one per report. `yarn format:check`, `yarn lint`, `yarn build` and `yarn test` pass from `dashboard/` at 376 files and 4,241 tests.

Standing closing work is complete: t003 verified the adoption journey and made the one documentation change this milestone needed — `reports/AGENTS.md` now states that reader-chosen view state belongs to the page, which is the guidance whose absence caused the defect — and t004 ran the simplify review, which found a fifth instance of the defect and four test renders that had silently fallen back to Radix's uncontrolled mode.

Recorded rather than folded in, in [142](../../142.md): the outer ledger-layout boundary also unmounts the page on a shared account, filter or time change, so the selection still resets there. That boundary belongs to m13 by this milestone's own coordination note, and closing it is a decision about whether a chart choice should outlive a scope change, not a reflex fix. The overview `net-worth-card` chart/table choice has the same defect outside these four reports, and two smaller test-hygiene items are noted alongside. The flaky `amount-step-precision` focus assertion observed while running these gates is in [141](../../141.md), confirmed unrelated to this milestone.
