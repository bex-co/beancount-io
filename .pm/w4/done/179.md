# Finish the two Statistics singular-count summaries missed by w4/077

Severity: **minor** copy defect. Package: **dashboard**. Estimate: **35–45m**.
Source: continuous dashboard QA for w4; **A2** — reader-facing summaries should
use grammatical count labels. This is the remaining scope of a closed finding,
not a new count-calculation failure.

Production `https://beancount.io`, anonymous synthetic `open_ledger/stock-example`,
isolated headless Chromium149.0.7827.55, English. Local main `390568c2`, served
`main-d5j4FX-V.js`; exact deployed revision unverified.

1. Open `/ledger/open_ledger/stock-example/statistics?filter=narration%3A%22NWRB%204-for-1%20share%20split%22&lang=en`
   at1440×1000/light.
2. Postings per Account says **Postings count per account (1 Accounts)**;
   Entries Count by Type says **Total 1 entries across 12 types**.
3. Fresh-load the same URL at390×844/dark. Both exact strings repeat.
4. Actual GraphQL reads succeed HTTP200 in both runs: GetLedgerPostingsPerAccount
   returns one account, `Assets:Brokerage:NWRB`, count2; GetLedgerEntriesCountPerType
   returns Transaction1 and eleven zero types. The share split has two postings
   to one account. The data and displayed counts are correct.

Working controls: clearing the expression gives12 accounts and87 entries, where
plural wording works. A nonexistent-tag filter gives the existing no-postings
state, total0 and em dashes for undefined percentages. Account Last Entries now
uses the corrected neutral phrase, “Last entry date and balance per account (5)”.
Its lifetime dates plus filtered balances are intentional and outside this issue.

Cause: `dashboard/src/features/ledger-data/statistics/postings-per-account.tsx:135–136`
concatenates totalRows with the always-plural shared `journal.accounts` label.
`entries-count-by-type.tsx:112–114` concatenates a count with the fixed
`page.statistics.entriesAcrossTypes` fragment (“entries across”). These two
summary paths each have one production consumer on Statistics. The numbers
come correctly from their dedicated filtered GraphQL reads.

Use natural count-neutral localized summaries (for example “Accounts: 1” and
“Entries: 1; types: 12”), or supported pluralization. Keep the whole phrase in
its owning Statistics catalog instead of assembling grammar from unrelated
fragments. Preserve postings/account/entry distinctions, all15 locale shapes,
filtered results, empty/error states, table names and destinations. Do not
change shared journal.accounts globally or alter the API. Verify actual-locale
zero/one/many output and run dashboard gates; this wording repair needs no new
test framework or mirror-of-implementation suite.

Dedupe/history: searched all board states for the exact strings and both summary
keys. Completed **w4/077** explicitly included these two Statistics summaries,
but its closing commit `c7514dbe` changed BQL, Events and Statistics'
accountLastEntriesDescription only. The two reproduced paths remain unchanged
on current main; this is unfinished acceptance, not deployment lag. Later
`5f909d39` moved the section shells without changing the phrases. Targeted
history traces entriesAcrossTypes to `af5339de`; no overlapping open repair was
found. Keep w4/077's successful labels and completed w3/m14's filtered-count
semantics intact.

Evidence: `dashboard/tmp/qa-20260921-loop/statistics-singular-repeat.json`
contains both fresh views and actual API responses;
`statistics-singular-390.png` was visually inspected;
`statistics-singular-controls.json` records zero/many controls. The captured
trailing analytics aborts are navigation noise, not a cause or product finding.

Unverified: other locales/engines, actual screen-reader speech and patched
behavior. No product changes, production writes, commit or shipping.

---

**Resolved 2026-09-23** — Both Statistics summaries are now single, count-neutral phrases in the Statistics catalog across all 15 locales: `page.statistics.postingsPerAccountSummary` ("Postings count per account ({count})", matching w4/077's sibling) and `page.statistics.entriesByTypeSummary` ("Entries: {entries} · Types: {types}"). The retired fragments (`entryCountPerAccount`, `total`, `entriesAcrossTypes`, `types`) and the now-unused shared `journal.accounts` label were removed, as the unused-translation guard requires. The existing component tests had pinned the ungrammatical English (`(1 Accounts)`, `across 1 types`); they now pin zero, one, and many against the real English catalog. Non-English wording is by the implementer and has not had native review. Not re-verified on production. Shipped in `4099c11f` (`dashboard`).
