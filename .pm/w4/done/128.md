# Gate file creation and upload forms on ledger write permission

Severity: minor permission-UI defect. Estimate: 60m, dashboard. A2: public readers should not be invited to prepare repository changes they cannot save. Repeated dashboard QA, 2026-09-18.

Environment: production https://beancount.io, anonymous public `open_ledger/freelancer-invoicing`, English/light, QA-owned isolated headless Chromium149.0.7827.55,1440×1000 and fresh390×844. Local main26d54a9db7d33c31a549bd12a0454a1c01f3f038; fetched origin/mainbd5d1d31503f39f73a3201ca9cd36513cac37169; deployed main-BIr0jJni.js, exact deployed commit unverified.

Reproduction:

1. Open `/ledger/open_ledger/freelancer-invoicing/files/new/main/transactions`. The anonymous header offers Log In / Sign Up, but the page renders an editable filename field and Monaco editor. Enter `qa-local-draft-only.bean` and a harmless comment in the local buffer: **Save becomes enabled**. A fresh390 reload repeats this behavior and accepts the comment. Clear both fields afterward; Save becomes disabled again.
2. Open `/ledger/open_ledger/freelancer-invoicing/files/upload/main/transactions`. Stage a small synthetic text file through the file-input API. The page lists the local file, accepts an upload comment and enables **Upload Files**. A fresh390 load through the supported `/files/upload?dirPath=transactions` alias resolves to the canonical route and repeats it. Cancel discards the staged selection and returns to the directory.
3. Working control: the ordinary public directory hides Create File and Upload Files. Its writer-only buttons are already wrapped by LedgerWritePermission. Existing public blob readers also gate effective edit mode on the same permission contract.

Only browser-local drafts were prepared. Neither Save nor Upload was activated, no create/update request was sent, and no repository resource was created. The request observer recorded zero GraphQL mutations during these controls. This is misleading editing UI, not a server authorization bypass. Authenticated reader/writer sessions and actual submissions remain unverified.

Permission evidence: real anonymous GetLedger returns200 with permissions:null; it does not return a false push flag. `common/hooks/use-ledger-permission.ts:5–14` explicitly derives canWrite=false for an unauthenticated visitor. Backend `ledger-resolver.mutation.ts:68–80` requires authentication for createLedgerFile, and `features/ledger/workflow/ledger-workflow.ts:570–574` independently checks LEDGER_FILES_WRITE. Those checks were traced, not exercised by attempting a mutation, and must remain unchanged.

Root: `dashboard/src/features/ledger-editor/create-file/index.tsx:89` disables Save only for filename validation or saving; handleSave invokes CreateLedgerFile without a canWrite guard. `upload-files/index.tsx:60–61` disables Upload only when no files are staged or an upload is in progress; handleUpload likewise invokes CreateLedgerFile without a permission check. Neither page reads useLedgerPermission or gates its editor/file picker. The canonical route wrappers mount these pages directly; compatibility aliases redirect into them. The directory's guarded buttons do not protect these entrypoints.

Use the existing ledger permission contract for both pages and their action handlers, failing closed for anonymous, non-writer and unresolved access. Show a useful sign-in/read-only state with a route back to readable files before inviting a draft or file selection. Cover canonical and existing alias entrypoints without changing alias paths or creating redirect loops. Preserve authenticated writer behavior, valid empty-file creation, filename checks, dirty-state protection, file re-selection and source viewing. If access changes during a legitimate draft, stop further writes without silently discarding that draft. Keep the repair in dashboard; no API/parity or dependency change is needed.

Dedupe/history: all board states and relevant deleted history checked. Historical w3/040, shipped in5d70e9f3, repaired TextFileView's existing-file editMode/shortcut path only; these separate create/upload pages were outside that scope. Current w4/m22 concerns read-only accessibility metadata, not allowing new drafts. Completed w3/196 (89a311a0) fixes invalid filenames,197/b9bdc71b fixes clean beforeunload, and61854497 fixes upload re-selection. None adds page/action permission checks. The omission originates in the initial af5339de pages and remains after those repairs.

Acceptance: rendered route tests show no editable/stageable write form or callable write handler for guest, reader and loading permissions, including both aliases. An authorized-writer fixture preserves the actual form, validation and draft/re-selection behavior. Include a permission-loss case and confirm public directory/source reads remain usable. Replay fresh narrow/desktop before considering complete; run dashboard format, lint, tests and build. Do not test denial by writing to production.

Evidence: `dashboard/tmp/qa-20260917/anonymous-file-write-controls.json` includes all four prepared states, permission response, mutation count and restored directory. Screenshots `anonymous-create-form-1440.png`, `anonymous-upload-form-1440.png`, `anonymous-create-form-390.png`, and `anonymous-upload-form-390.png`; both narrow images were visually inspected. Drafts were cleared/cancelled and the temporary request observer removed. No product changes, commits or pushes.

---

**Resolved 2026-09-19** — Both pages now read the existing `useLedgerPermission` contract, which already derives `canWrite=false` for anonymous and unresolved access, so the gate fails closed without touching the hook, the API or the traced server checks. A shared `features/ledger-editor/shared/components/write-access-required.tsx` replaces the form with the reason — `common.errors.unauthenticated` for a signed-out reader, `common.errors.forbidden` for a signed-in one without write access — and a `ledgerEditor.browseParentDirectory` link back to the directory the reader came from. All three strings already exist in every locale, so no new translation keys were added. The write handlers also fail closed independently of their disabled buttons, which is what makes the permission-loss case safe: the page is only replaced when there is nothing to lose (no draft, no staged files), so a writer who loses access mid-draft keeps the form and everything typed while the write itself is refused. Both defects were reproduced first at HEAD against the public read-only API on `open_ledger/freelancer-invoicing`: signed out at `/files/new/main/transactions` the filename field and Monaco editor rendered and Save became enabled as soon as `qa-local-draft-only.bean` was typed, and `/files/upload/main/transactions` offered a file input and a "Choose your files" button. No mutation was ever sent — neither Save nor Upload was activated. After the change, both routes render the sign-in panel with no filename field, no editor, no Save, no file input and no upload button, at 1440×1000 and at 390×844 with no horizontal page scroll; the "Browse parent directory" link resolves to `/files/tree/main/transactions`. The compatibility alias `/files/upload?dirPath=transactions` still resolves to the canonical route and is gated there too, with no redirect loop. Public reads are unaffected: the same directory still lists its six files with its writer-only buttons hidden as before. `features/ledger-editor/shared/__tests__/file-write-permission.test.tsx` adds ten cases driving both pages as guest, as a signed-in reader without write access, and as a writer — asserting no form, editor, Save, picker or upload button for the first two, the correct reason for each, the directory the link points back at, and that the writer keeps the real form, filename validation and picker; plus the permission-loss case, where the draft and editor contents survive, Save is disabled and clicking it calls no mutation. Seven of the ten fail when the gate is removed; the three that still pass are the writer-path controls. The existing `create-file-draft-guard` suite gained a writer fixture, since drafting is writer-only behaviour. Not verified: authenticated reader and writer sessions against the live API, and actual submissions — denial was deliberately not tested by writing to production. Dashboard gates all pass (`yarn format:check`, `yarn lint`, `yarn test` — 411 files, 4622 passed, 1 pre-existing skip — and `yarn build`). Shipped in `8b128b00` (`dashboard`).
