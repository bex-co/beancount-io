# Remove obsolete skills-onboarding claims from the CLI README

**Estimate:** 20m. **Owner:** cli. **Depends on:** none. **Pillars:** A2/A3.

`cli/README.md` still says ledger-skill onboarding updates await m21 in two places, although [w1/m21](../w1/done/m21/README.md) is complete. Replace both pending-work claims with the shipped one-install behavior and links to the customer [installation guide](../../skills/docs/installation.md) and [first-query walkthrough](../../skills/docs/first-query.md).

**Acceptance:** neither obsolete pending claim remains; links resolve from the CLI README and agree with `skills/README.md` and `skills/CLAUDE.md`. A newcomer can follow CLI installation into their first agent query without interpreting internal milestone references. This is documentation cleanup, not another implementation of m21.

**Source:** user-approved item 2 from `/pm-brainstorm w4`, 2026-09-16. Kept as an inbox note because the work is under one hour.

---

**Resolved 2026-09-16** — fixed in `cli` by `docs(cli): point the README at the shipped skills onboarding (w4/072)`. Triage confirmed the note at HEAD before the fix: [w1/m21](../w1/done/m21/README.md) reads `**Status:** done` with all eight tasks marked `— **DONE**`, while `cli/README.md` still deferred to it twice — "Ledger-skill updates that still mention a separate Beancount install are tracked in milestone m21" in Install, and "ledger-skill onboarding updates remain milestone m21" in Development. The note was not stale. Both passages now state the shipped behavior — the eight `beancount-*` skills run on the single `bea` install and never ask for a separate Beancount — and the Install one links to the [installation guide](../../skills/docs/installation.md) and the [first-query walkthrough](../../skills/docs/first-query.md), so the path from installing `bea` to a first agent query no longer passes through an internal milestone reference. The links use the absolute `github.com/bex-co/beancount-io/blob/main/...` form the rest of that README uses, because it ships to PyPI where relative paths do not resolve. The "eight" count agrees with `skills/README.md` and the root `README.md`; `skills/CLAUDE.md` is unchanged and still describes the same one-install model.
