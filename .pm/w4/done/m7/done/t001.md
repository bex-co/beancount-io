---
id: w4/m7/t001
title: Audit the ten uncovered screens with the Accessibility Inspector
worker: worker1
status: done
estimate: 45m
depends_on: []
---

## Objective

Produce a per-screen findings list for the screens that have no accessibility props today, so the fix tasks work from evidence rather than guesses.

## Context

- Screens with zero `accessibilityLabel`/`accessibilityRole` today: reports, settings, notifications, commit detail, edit transaction, payee input, narration input, referral, ledger tab, multi-postings transaction form.
- Xcode's Accessibility Inspector audits the booted simulator; deep links (`beancount:///(app)/<route>`) reach screens below the fold since expo-mcp cannot scroll.
- Check labels, roles, states (selected, disabled, expanded), 44pt touch targets, and dynamic-type clipping at the largest accessibility size.

## Steps

1. Boot the simulator, open each screen by deep link, run the audit in light and dark.
2. Record findings under `mobile/tmp/a11y-2026-09/` and summarize them in this task's validation evidence.
3. Group findings into the three fix tasks.

## Files

- mobile/tmp/a11y-2026-09/ (scratch, gitignored)

## Acceptance criteria

- [ ] Every listed screen has an audit record with counts by category.
- [ ] The three fix tasks each have a concrete findings list attached.

## Out of scope

- Fixing anything in this task.
