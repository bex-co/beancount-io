# Translate public-profile social tabs and their empty states

Severity: minor incomplete localization. Estimate: 50m, dashboard. A2: readers using a supported language should understand the public profile's social navigation and empty results.

Production https://beancount.io, signed out, isolated headless Chromium149.0.7827.55, light theme. Fresh `/ledger/open_ledger?lang=ru` at390×844 and1440×1000 renders `[Книги, Starred (0), Following (1), Followers (142)]`. Selecting Starred shows “No starred repositories.” The surrounding Follow action, profile introduction, account date and footer are Russian, and document.lang is ru. Fresh Chinese desktop reproduces the same English social labels/message beside 账簿, with document.lang zh. Role-based tab locators identify the same English labels. The header's follower count also uses the untranslated Followers label. Narrow screenshot inspected.

Controls: GetUserStarredRepos returns HTTP200 with an empty list and no GraphQL error, so this is a legitimate empty result, distinct from106's false-empty failure handling. Japanese desktop renders `[帳簿, スター (0), フォロー中 (1), フォロワー (142)]` and スターリポジトリがありません. Locale selection works. German320px keyboard End/Home scrolls the focused first/last tab into view without document overflow; preserve that behavior when labels grow. Counts are live data and are not fixed acceptance values.

Root: `dashboard/src/features/user-profile/locales/{bg,ca,de,es,fa,fr,nl,pt,ru,sk,uk,zh}.ts` retain the exact English messages for six keys: `userProfile.tabs.followers`, `.tabs.following`, `.tabs.starred`, `.noFollowers`, `.noFollowing`, `.noStarredRepos`. Source comparison covers all15 catalogs; en is the source language, ja/ko already have translated values. Russian lines88–110 show the six defaults, originating in af5339de and unchanged on fetchedmain358ba4dc. LocalHEAD26d54a9d; deployed main-BIr0jJni.js, exact deployed SHA unverified.

Consumers pass the correct keys: `components/user-profile-tabs.tsx:168–182,219,256,293`; `pages/user-profile-page.tsx:206` uses the follower label in the profile header; `components/user-profile-skeleton.tsx:94–97` shares the tabs. Translate the six defaults in the twelve affected catalogs through the existing convention, preserving the distinction between people following this user and people this user follows. Keep established native wording consistent with the existing translated Follow/Unfollow actions. Do not translate usernames, ledger names, counts or API data. Preserve English and existing Japanese/Korean messages. No routing, query, component-state or dependency changes are required.

Acceptance: Russian narrow/desktop and Chinese render localized social tabs, header follower label and successful starred-empty message; localized empty follower/following messages are source-traced scope and should also be checked. Verify longer tab labels remain reachable by keyboard/horizontal scrolling, correct tab selection and count binding, and Japanese/English controls. Run normal dashboard gates. Avoid tests that merely repeat each chosen sentence. Non-Russian/Chinese live failures, empty follower/following runtime and patched behavior remain unverified.

Dedupe: all .pm queues including done/blocked searched for these keys, social-tab translations and empty-profile strings.106 owns request-failure handling, m17 search navigation, m14 Catalan journal/common terms,100 Russian/Ukrainian Journal headers; none owns these profile catalog defaults. Coordinate106 so its error branch remains distinct from the translated successful-empty states. No whole-profile translation sweep or SEO-copy scope is implied.

Evidence: `dashboard/tmp/qa-20260917/profile-social-localization.json`, `profile-social-locale-catalogs.json`, `profile-russian-social-390.png`, and the passing `profile-tabs-keyboard-320-control.json`. No profile writes, follows/stars, credentials, product changes, commits or pushes. Extra context closed; original browser restored to English390px.

---

**Resolved 2026-09-19** — Confirmed in source and against a local build: twelve
catalogs (`bg`, `ca`, `de`, `es`, `fa`, `fr`, `nl`, `pt`, `ru`, `sk`, `uk`,
`zh`) held the exact English defaults for all six keys, while `en` is the
source and `ja`/`ko` were already translated.

All twelve now carry native wording for the three tab labels and the three
empty messages, keeping the distinction between people following this profile
and people it follows, and matching each catalog's existing Follow/Unfollow
terms. German keeps `Follower`, which is the German word rather than an
untranslated default. English, Japanese and Korean are untouched, and no
usernames, ledger names, counts or API data are translated. Catalog content
only — no routing, query, component-state or dependency change, so `w4/106`'s
failure branch stays distinct from these successful-empty states.

Verified in a real browser, 13/13 across two scripts: Russian, Chinese, German,
French, Japanese and English each render their own tabs with counts bound
correctly — `["Избранное (0)", "Подписки (1)", "Подписчики (162)"]`,
`["已加星标 (0)", "关注中 (1)", "粉丝 (162)"]`, `["Markiert (0)", "Folgt (1)",
"Follower (162)"]` — at 1440px and 390px; the profile header's follower label
and the successful starred-empty message are localized, with the Starred tab
selected from `?tab=starred`; and at 320px in Russian the longer labels still
scroll within the tab strip, with End and Home focusing the last and first tab
in view. No test repeats the chosen sentences, per this note. Gates from
`dashboard/`: `yarn format:check`, `yarn lint`, `yarn test` (398 files, 4467
passed, 1 pre-existing skip) and `yarn build` all pass.

Reported honestly: the first full-suite run failed on
`amount-step-precision.test.tsx`, the known flake tracked in `w4/141`; it
passes on its own and the suite is green on re-run, and a locale catalog cannot
affect journal focus. A fourth sighting is recorded in that note.

Noted while measuring: a Russian profile at 320px does scroll the document
horizontally, but the culprit is the "Копировать ссылку" button, not a social
tab — measured identically before this change, so it is pre-existing and filed
separately. Shipped in `e1092bff` (`dashboard`).
