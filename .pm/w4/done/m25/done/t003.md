---
id: w4/m25/t003
title: Repair what the tuned predicate finds beyond those
worker: worker1
status: done
estimate: 30m
depends_on: [w4/m25/t002]
---

## Objective

Work the remainder of t001's hit list — the Latin-script damage that does not
share a key with a repair already shipped elsewhere.

## Context

- t002 clears the keys with known-good precedents. What is left is whatever
  t001's predicate finds on its own, and its list belongs in that task.
- Candidates seen during triage, to be confirmed against the tuned predicate
  rather than taken on trust: `page.commodities.failedToLoadCommodities` (pt,
  `Falha ao Carregar Commodities`), `page.dashboard.feedError`, and the
  `auth.backToSignIn` shape in nl (`Terug to Sign in`).
- Some hits will be false positives that t001's tuning did not remove. Record
  them as such rather than translating a string that was already correct — a
  loanword rewritten into a calque is a regression, not a fix.

## Steps

1. Take t001's recorded hit list, minus everything t002 repaired.
2. For each, compare against `en[key]` and the same key in a locale where it reads correctly.
3. Translate the genuine ones; for a false positive, extend the predicate's allowlist and say which term and why.
4. Run the gates from `dashboard/`.

## Files

- Whichever `locales/{ca,de,es,fr,nl,pt,sk}.ts` files the list names.
- `dashboard/src/test/locale-scan.ts`, if a term needs allowlisting.

## Acceptance criteria

- [ ] Every entry on t001's list is either repaired, allowlisted with a stated reason, or recorded in `KNOWN_DEVIATIONS` against a filed note.
- [ ] No correct translation was rewritten; anything left alone is named.
- [ ] The dashboard gates pass.

## Out of scope

- Wording improvements to strings that are not half-translated.

## Closed by triage

Nothing was left for this task. It was written to work "the remainder of t001's
hit list" — the Latin-script damage that does not share a key with a repair
already shipped elsewhere. After t002 (`ce0c0bfc`), running t001's predicate
over all seven Latin-script locales reports **zero** offenders, so the
remainder is empty.

That is not a coincidence: t002 worked by key, and every key on its list was
one already repaired in another locale. The overlap the milestone's `## Why
now` bet on turned out to be total — all 30 of t001's hits fell inside it.

The candidates this task listed for confirmation were checked and none needs
work:

- `page.commodities.failedToLoadCommodities` (pt, `Falha ao Carregar
  Commodities`) — `Commodities` is the term Portuguese finance uses, and it is
  on the predicate's allowlist for that reason. The capitalized `Carregar` is
  English title case carried into a sentence, which is a style nit rather than
  a half-translation.
- `page.dashboard.feedError` (pt, `Falha ao carregar o feed`) — `feed` is a
  borrowed term and correct.
- `auth.backToSignIn` (nl, `Terug to Sign in`) — real, and repaired in t002
  as part of the `auth` group.

Evidence: commit `ce0c0bfc`; predicate as recorded in `w4/m25/t001`'s Result;
`0` hits across ca, de, es, fr, nl, pt and sk at that commit.
