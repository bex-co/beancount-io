---
id: w4/m18/t006
title: Close out after desktop and narrow inspection passes
worker: worker1
status: done
estimate: 10m
depends_on: [w4/m18/t005]
---

## Objective

Re-run the actual recorded queries against the built change, inspect values and layout, record evidence and limits, and close only after the complete-value contract holds.

## Context

See [FINDINGS.md](./FINDINGS.md) and the milestone definition of done; preserve w3/m28/m37 behavior.

## Files

Milestone evidence and final review

## Acceptance criteria

- [x] Every inventory unit and PRINT posting is readable at both widths, with no outstanding required work.

## Out of scope

Backend/API changes, Holdings layout changes, financial rounding policy, new dependencies and unrelated BQL copy fixes.

## Outcome

Re-ran every recorded query against a fresh production build of the shipped
code. 75/75 checks passed.

- **70/70** across the three browser suites at 1440x1000 and 390x844: 30
  geometry checks (the reproduced single-row inventory, eleven multi-unit
  inventory rows before and after scrolling, and the 1001-row control), 16
  line-structure checks (PRINT's four lines and three postings, exact inventory
  units, a compact one-unit cell), and 24 journey checks driving the real
  editor by pointer and by keyboard.
- **5/5** closing controls: the inventory CSV still carries all eight
  `<amount> <currency>` pairs, the PRINT CSV still carries all three posting
  amounts, the large-result CSV still parses as exactly 1001 data rows, and the
  Holdings page renders at both widths with no clipped cell.

Definition of done, clause by clause:

- the eight-unit asset Inventory is fully inspectable, cell height equal to
  content height (157/157 desktop, 153/153 narrow) where it was 35 over 157
  before;
- PRINT's Opening transaction exposes all four lines, their indentation and all
  three posting amounts at both widths;
- no separate full-cell view was needed, so there is no keyboard action to
  discover and no focus to return; ordinary one-line cells stay compact at 36px
  desktop / 32px narrow;
- multiple tall cells neither overlap nor break row indexes
  (`aria-rowcount` 1002 and 401), columns stay aligned to the header within
  1px, later rows stay reachable, the 1001-row control keeps only 15–25 rows in
  the DOM, and Holdings is untouched;
- API data, decimal precision, inventory formatting and CSV bytes are
  unchanged — the shared formatter and the export component were never
  modified;
- geometry regressions are real rather than text-presence assertions, and both
  halves of the repair fail them when reverted; `yarn format:check`,
  `yarn lint`, `yarn test` (377 files, 4253 passed, 1 pre-existing skip) and
  `yarn build` all pass.

Limits worth recording:

- Two measured trade-offs, both in t002: at 390px an account name too long for
  a 169px column now wraps to a second line instead of being ellipsized, so the
  1001-row control's rows are 53px there rather than 33px (desktop unchanged at
  37px); and because heights are measured, a programmatic jump straight to
  `scrollHeight` on a 1001-row result can land short once before reaching the
  last row. Nothing is hidden in either case.
- JSDOM has no layout engine, so the unit suite cannot measure the wrapping of
  a single long line; that half rests on the browser checks.
- The journeys cover the public read-only query path over an anonymous session
  on two synthetic open ledgers. Authenticated roles, other browsers and
  screen-reader speech are not covered. The narrow width is an emulated
  viewport.
- The Holdings control confirms the page renders unclipped at both widths; the
  specific by-cost-currency figures FINDINGS quoted were not located on the
  default Holdings view, so that narrower control was not reproduced.
