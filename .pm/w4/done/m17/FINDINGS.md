# Profile search debounce replaces a selected ledger with /ledger/undefined

Severity: major navigation defect with a timing-dependent trigger. Dashboard only. Production https://beancount.io, anonymous public open_ledger/budgeting-envelopes fixture, isolated headless Chromium149.0.7827.55, English/light1440×1000 and fresh390×844. Local/fetched main26d54a9db7d33c31a549bd12a0454a1c01f3f038; deployed asset main-BIr0jJni.js, exact deployed SHA unverified.

Repro: fresh-load `/ledger/open_ledger`. Type budget into Search ledgers at120ms per character. Immediately activate the only matching budgeting-envelopes card. The browser initially changes to `/ledger/open_ledger/budgeting-envelopes`, then ends at `/ledger/undefined?q=budget` showing “User not found” and “The requested resource could not be found.” Desktop activation completed13ms after typing returned; fresh narrow completed27ms after typing returned. A preceding desktop fill/click27ms independently reproduced it. These timings are recorded automation observations, not a claim about typical human speed. No request interception, artificial network delay or server mutation was used.

Control: fresh narrow profile, fill budget and allow the URL to become `/ledger/open_ledger?q=budget` before activating the same card. The ledger remains open after settlement. Back returns to that profile URL, full budget input and Showing1of1ledgers. Thus the public ledger exists, selection works after the debounce, and the wrong username is introduced by client navigation. Sorting A–Z and ordinary120ms/key typing also work before departure. A separate fast-cache lost-draft result is not claimed reproduced; test it as part of the same persistence contract.

Root: `dashboard/src/features/user-profile/components/ledger-collection.tsx:24` sets a250ms URL debounce. The effect at70–80 schedules updateSearch and cancels only on dependency change/unmount. updateSearch at53–59 calls the profile-scoped navigate with `to: "."`, replace:true and no explicit username params. RepositoryListItem43–46 starts the ledger route while the prior component can remain mounted during the real pending read. Its timer then starts another navigation. The installed router-core router.ts:1814 onward builds against pendingBuiltLocation/latestLocation; params at1857–1876 come from that current destination, which has ledgerOwner/ledgerName rather than username. Relative profile navigation therefore interpolates undefined. Merely supplying username would still let a stale update hijack navigation back to the old profile; merely cancelling the timer would leave the unpersisted visible search behind. Repair the lifecycle and departure snapshot together.

Producer/history: ledger route loader `routes/ledger.$ledgerOwner.$ledgerName.tsx:33–66` legitimately awaits GetLedger; preserve its access/error handling and pending lifecycle. Completed w3/164 introduced this debounce/persistence in4d701a91. Its history test starts with q already in the URL; its debounce test waits for settlement before leaving, so neither combines an outstanding timer with route navigation. Existing search/sort/show validation is intentional and must remain.

Dedupe: all .pm including done/blocked searched for profile search, debounce navigation, delayed timers, undefined username and Back restoration. Completed w3/164 describes the original all-local-state loss and provides the behavior to preserve; no active follow-up owns this new race. w4/m13 is a different pending layout and w4/093 is account pagination. No newer change to LedgerCollection after4d701a91 found.

Fix scope: make delayed profile writes safe against navigation start and profile identity changes, and retain the latest visible draft in the correct history entry before leaving. Keep the no-history-churn/typing responsiveness contract; don't add a global preference store, arbitrary sleeps, dependencies, or a router-core patch. Preserve native card links and modified-click behavior, route authorization, same-profile tabs, sorting/show-more reset rules and bounded/hostile-input validation.

Acceptance: reproduce both widths with departure before250ms and a real pending destination, then confirm the selected ledger survives settlement and Back restores the typed query. Add a cached/fast destination control, delayed/error destination, another profile identity, tab departure and immediate clear+open. Test actual route lifetime and history, including no extra entries per keystroke, and keep the existing settled-search control. Actual patched behavior, other engines, authenticated/private ledgers and those expanded race combinations remain unverified.

Evidence: `dashboard/tmp/qa-20260917/profile-search-navigation-race.json` holds desktop/narrow immediate and settled URLs, visible not-found messages and the successful delayed-selection/Back control. No product fixes, created resources, commits or pushes.
