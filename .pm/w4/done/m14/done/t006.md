---
id: w4/m14/t006
title: Close out the verified Catalan repair
worker: worker1
status: done
estimate: 10m
depends_on: [w4/m14/t005]
---

## Objective

Close and archive only when the definition of done is met.

## Context

Read [FINDINGS.md](./FINDINGS.md) and the milestone definition of done. This is report-scheduled dashboard work, not authorization for production ledger writes.

## Steps

Record implementation and live/controlled validation evidence. Through /pm done, mark tasks done and move them into the milestone's done directory; set milestone status done, move the complete milestone to w4/done/m14 and check its workstream checkbox. Do not close on code presence alone.

## Files

.pm/w4/m14; .pm/w4/README.md

## Acceptance criteria

- [x] The DoD holds, evidence is recorded, and no completed task or milestone remains in the open tree.

## Out of scope

New API behavior, ledger writes, new dependencies, unrelated locales and redesigning translation infrastructure.

## Closeout

Completed 2026-09-19. Every clause of the definition of done was checked against the shipped code and a repeated live walk, not inferred from the catalogs. The walk ran against a local production build of the current HEAD with anonymous read-only reads of the public `open_ledger/example` ledger; 30 checks in fresh isolated headless Chromium contexts at 1440x1000 and 390x844, all passing, repeated after the t004 and t005 commits so the evidence matches what shipped.

- The Catalan Overview link reads "Resum general" and no longer claims missing Trial Balance data, at both widths; at 390px it is reached through the sidebar toggle.
- The shared payee/tag placeholder reads "Filtrar per etiqueta, beneficiari, ..." and Clear all reads "Esborrar tot", in the desktop controls and inside the narrow Filtres sheet.
- Journal Open, Note, Pending, Other, Metadata and Payee/Narration read Obrir, Nota, the literal "!" with "Transaccions pendents" as its name, the literal "x" with "Altres transaccions", Metadades and Beneficiari/Descripció. None of the eight reported validation or loading sentences appears on any label, title or placeholder.
- Selecting Obrir and deselecting Transacció sends `directiveTypes: ["Open"]` with `time: "2016"` and returns exactly 53 entries; clicking Esborrar tot drops `time` from the URL and restores 1,044 transactions with Transaction selected.
- English is unchanged at both widths (Open, Note, Metadata, Postings, Overview). API query variables, persisted directive selections, table semantics and filter counts all behave as before — the persisted selection surviving a reload is why the Clear all journey runs in its own fresh context. No route, permission or API surface changed; only catalog values did.
- A real-catalog regression covers the reported labels and their actions in `features/journal/components/__tests__/catalan-control-labels.test.tsx` (33 cases, failing 27 of 33 against the pre-repair catalogs), and `yarn format:check`, `yarn lint`, `yarn build` and `yarn test` pass from `dashboard/` at 375 files and 4,223 tests.

One extra check beyond the stated clauses, because t004 changed a key outside the original corruption: the Catalan balance sheet now renders "Fons propis" and "Patrimoni net" as distinct labels, so the Equity and Net Worth tabs can be told apart.

Recorded rather than folded in: the same key-to-meaning shift is still live on five other Catalan feature catalogs — trial-balance, account, overview, reports, income-statement and the file editor — which this milestone's journeys never visited. That is captured with full evidence and a measured one-line guard in [139](../../139.md), shipped in `4024e2f6`. Two smaller follow-ups are noted there too: `common.directivesLimitApproaching` and `common.directivesLimitReached` are untranslated English in Catalan, and zh renders `journal.pending` as a word rather than the "!" flag. No authenticated form journeys are claimed.
