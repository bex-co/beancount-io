---
id: w4/m2/t003
title: Charts — net cash flow over time + per-activity breakdown
worker: worker1
status: done
estimate: 40m
depends_on: [w4/m2/t002]
---

## Objective

Add the chart section to the cash-flow page: net cash flow over time and a per-activity (operating/investing/financing) breakdown, with the standard interval and conversion selectors.

## Context

- Chart idioms: ECharts 6+, options constructed in component files (`reports/CLAUDE.md`); reuse `DateBalanceChart` / `LineChart` patterns from income-statement/balance-sheet.
- Collapsible chart section + cookie-persisted visibility (`useCookieStorageState`) as on the sibling pages.
- Open question in ADR002: stacked bars vs waterfall per period — pick stacked bars for v1 (simpler, consistent with income-statement chart modes); note the choice in the PR.

## Steps

1. Net-cash-flow-over-time chart fed by the t001 model's interval series.
2. Per-activity stacked bar chart per interval.
3. Wire IntervalSelect / ConversionSelect / chart visibility toggle.

## Files

- `dashboard/src/features/reports/cash-flow/` (chart components)

## Acceptance criteria

- [ ] Charts render for the example ledger at all intervals and conversions.
- [ ] Chart visibility toggle persists via cookie like sibling pages.

## Out of scope

- Duplicating the overview Sankey onto this page (ADR002: link, don't duplicate).
