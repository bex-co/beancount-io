---
id: w4/m2/t004
title: Account status panel — cash & equivalents in this report
worker: worker1
status: done
estimate: 30m
depends_on: [w4/m2/t002]
---

## Objective

Add the "Cash & cash equivalents in this report" panel: every cash account with open/closed status, current balance, and last-activity date, so users can see and audit what counted as cash.

## Context

- Data: `GetLedgerAccountDirectives` (`openedAt`, `closedAt`, balance, entry counts) — `dashboard/src/features/ledger-data/accounts/graphql/accounts.graphql`.
- Behavior precedent: balance-sheet closed-account filtering (`showClosedAccounts`, `closedAccountNames` in `balance-sheet/utils.ts`) — closed, zero-balance accounts hidden by default.
- This panel is the trust surface for the heuristic classification (ADR002).

## Steps

1. Query account directives alongside the page loader data.
2. Render the CCE set from t001 with status/balance/last-activity per account.
3. Default-hide closed zero-balance accounts with a show/hide toggle consistent with sibling pages.

## Files

- `dashboard/src/features/reports/cash-flow/` (panel component)

## Acceptance criteria

- [ ] Panel lists exactly the accounts the CCE heuristic included, with status and balance.
- [ ] Closed zero-balance accounts are hidden by default and revealable.

## Out of scope

- User correction of the CCE set (metadata override — deferred follow-up).
