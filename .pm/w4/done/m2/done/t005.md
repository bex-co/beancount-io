---
id: w4/m2/t005
title: Export integration — `cash_flow` StatementKind + CSV/Markdown/print
worker: worker1
status: done
estimate: 45m
depends_on: [w4/m2/t001]
---

## Objective

Extend the statement export model with a third kind: `buildCashFlowDocument`, section keys for the three activities, and handling in the CSV, Markdown, and print renderers — with the heuristic-classification and CCE-set disclosure notices.

## Context

- `dashboard/src/features/reports/export/model.ts` — `StatementKind`, builders (`buildBalanceSheetDocument`, `buildProfitAndLossDocument`); add `buildCashFlowDocument`.
- Renderers: `csv.ts`, `markdown.ts`, `printable-statement.tsx` (+ `statement-print.css`, which must stay in the inline bundle — w4/001).
- w4/m1 conventions: printed signs follow financial-statement presentation; CSV keeps raw ledger amount + display amount; unaudited-management-statement status; multi-unit schedule rules.
- Disclosure notices (ADR002): heuristic activity classification, inferred CCE set — ride the existing print-notices framework.
- Touchpoints: `StatementExportMenu` analytics types (`report_export_*` events), 15 export locale files, renderer tests.

## Steps

1. Add `"cash_flow"` to `StatementKind` + section keys (operating/investing/financing).
2. Implement `buildCashFlowDocument` from the t001 model.
3. Extend CSV/Markdown/print renderers + notices; add locale strings to all export locale files.
4. Wire `StatementExportMenu` into the cash-flow page header (done in t002's layout; this task supplies the document).

## Files

- `dashboard/src/features/reports/export/` (model, csv, markdown, printable-statement, presentation, locales)

## Acceptance criteria

- [ ] Export menu on the cash-flow page produces CSV, Markdown, and print/PDF output.
- [ ] Printed statement carries the heuristic-classification + CCE disclosure notices.
- [ ] `report_export_*` analytics accept the new report type.

## Out of scope

- Trial-balance or other statement kinds.
