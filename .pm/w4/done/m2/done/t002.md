---
id: w4/m2/t002
title: Cash-flow page — route, loader, three-section statement layout
worker: worker1
status: done
estimate: 45m
depends_on: [w4/m2/t001]
---

## Objective

Create the cash-flow report page: TanStack route, SSR-safe loader, and the statement body — operating / investing / financing sections with account detail rows, per-activity subtotals, and the net increase/decrease in CCE bottom line.

## Context

- Pattern to follow: `dashboard/src/routes/ledger.$ledgerOwner.$ledgerName.income-statement.tsx` + `dashboard/src/features/reports/income-statement/{index.tsx,loader.ts,income-statement-content.tsx}`.
- Layout idioms: `HierarchyListCard`, `PageHeader`, `LedgerPageSEO`, interval/conversion selectors, ledger search params via `getLedgerSearchParams()`.
- Data: existing queries only (`GetLedgerOverview`-style hierarchies/intervals); printed signs follow financial-statement presentation (w4/m1 convention).

## Steps

1. Add route `ledger.$ledgerOwner.$ledgerName.cash-flow.tsx` + `features/reports/cash-flow/` feature dir (index/loader/content).
2. Compose the three activity sections from the t001 model; subtotal per activity; CCE bottom line reconciling opening → closing cash.
3. Honor the standard filters/search params (account, filter, time, conversion, closed accounts).

## Files

- `dashboard/src/routes/ledger.$ledgerOwner.$ledgerName.cash-flow.tsx`
- `dashboard/src/features/reports/cash-flow/` (new)

## Acceptance criteria

- [ ] Page renders the three sections + CCE bottom line for the public example ledger.
- [ ] Sign conventions match ADR002 (investing outflows negative).
- [ ] Filters and conversion selectors behave like the income-statement page.

## Out of scope

- Charts (t003), status panel (t004), exports (t005), nav registration (t006).
