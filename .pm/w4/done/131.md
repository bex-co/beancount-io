# Preserve visible tab selection in forced-colors mode

Severity: minor. Estimate: 60m, dashboard. A2: users browsing example reports with a high-contrast palette can identify their selected view. Repeated dashboard QA, 2026-09-18.

Environment: production https://beancount.io, anonymous public open_ledger/example, English/light, isolated headless Chromium149.0.7827.55 at1440×1000. Browser forced-colors emulation, not a physical Windows high-contrast session. HEAD26d54a9d, fetched origin/mainbd5d1d31, deployed main-BIr0jJni.js; exact deployment SHA unverified.

Reproduce: enable forced-colors:active, open `/ledger/open_ledger/example/income-statement?time=2016-02`, and wait for the report to settle. Net Profit is selected, but all five tabs have the same visible black text, white background and1px black border. Select Income, then move focus outside the tablist and wait for transitions: the panel correctly changes to Income, but the tab strip still has no visible selected marker. Fresh reload reproduces; repeating with reduced-motion:no-preference excludes the initial reduced-motion setting as the cause. `aria-selected` remains correct; this is not an inaccessible-state or failed-navigation claim.

Measured selected and unselected tabs: font-weight500, color black, border1px solid black, box-shadow none, no outline. Selected background white versus unselected transparent over the white tablist produces the same visible fill. In normal colors, the selected tab has a white fill and shadow against the muted list. At390×844, the responsive View combobox explicitly displays Net Profit, then Expenses after selection, and remains usable: that is a passing control.

Root: `dashboard/src/common/components/ui/tabs.tsx:63–71` uses background and shadow as the default variant's only persistent active-state cues; its transparent borders become visible for every tab and the shadow disappears under forced colors. `common/components/responsive-tab-trigger-list/index.tsx:53–59` uses that default variant. No forced-colors override exists. [W3C's tabs example](https://www.w3.org/WAI/ARIA/apg/patterns/tabs/examples/tabs-manual/#accessibility-features) explains preserving separately perceivable selection and keyboard focus under high contrast; use that principle rather than copying its example implementation.

The profile underline variant reproduces the same lost selection at1440 and fresh390: `/ledger/open_ledger?tab=starred` has correct Starred selection and empty-state content, but every tab receives the same2px black bottom border. The selected underline merges into one continuous line. `tabs.tsx:74–80` varies only border/text color; forced colors removes that distinction too.

Fix both shared tab variants with a persistent selected indicator that survives forced colors, using system colors and/or a distinct border/underline geometry. Preserve a separately visible keyboard-focus indicator, layout dimensions, normal light/dark appearance, Radix selection/keyboard behavior and narrow Select fallback. Do not disable forced color adjustment globally or add a dependency.

Blast radius: the responsive helper serves Income Statement, Balance Sheet, Cash Flow, Trial Balance, Account and Holdings. Other default-variant consumers are New Entry directive tabs and the file Clone menu. Profile uses the separate underline variant and was also reproduced live at both widths. Income Statement, its narrow fallback and profile tabs were exercised; the other listed consumers are source-derived regression scope. No API or data change is needed; loading, denied and network-error behavior are outside this presentation defect.

Dedupe: searched all open/done/blocked board records for forced colors, high contrast and selected tabs, plus deleted-history searches. Existing086 concerns missing semantic state on Net Worth's plain Chart/Table buttons;116 concerns low-contrast canvas legends; neither owns this tab styling defect. Shared tabs source originates inaf5339de with no later repair in targeted history.

Acceptance: selected Net Profit and subsequently selected Income remain visually identifiable after focus moves away in forced-colors mode; keyboard focus is independently visible. Repeat a fresh desktop load and narrow View selection, repeat profile Ledgers/Starred switching at1440 and390, then check ordinary light/dark styling and other shared consumers. Use browser-rendered CSS verification for the forced-colors behavior rather than a class-name-only test; run normal dashboard gates when implementing. Actual Windows palettes, other engines, screen-reader speech, authenticated forms and patched behavior remain unverified.

Evidence: `dashboard/tmp/qa-20260917/report-forced-colors-tabs.json`, `report-forced-colors-1440.png` and `report-forced-colors-settled-1440.png`; both cited screenshots visually inspected. The same JSON includes profile styles; `profile-forced-colors-390.png` was visually inspected and `profile-forced-colors-1440.png` is an additional capture. The separate earlier repeat screenshot caught a transient border color before settling and is not the acceptance evidence. No product edits, ledger writes, commits or pushes.

---

**Resolved 2026-09-19** — Both shared variants in `common/components/ui/tabs.tsx` now carry a forced-colors selected indicator built from system colour keywords, which that mode honours. The pill variant's selected tab takes `Highlight` with `HighlightText` text and a matching border, while every tab's otherwise-transparent border becomes `Canvas` so it stops showing; the underline variant gives only the selected tab a `Highlight` bottom border and sinks the rest into `Canvas`, leaving one visible underline instead of a continuous line. Because the focus ring is a box-shadow and forced colors drops it, focus also gets its own solid `CanvasText` outline at a 2px offset, so selection and focus stay separately perceivable. Nothing disables `forced-color-adjust`, no dependency was added, and layout dimensions are unchanged. Both defects were reproduced first at HEAD at 1440×1000 under forced-colors emulation: on Income Statement all five tabs measured black text, `1px solid black` border and no shadow, with the selected white fill indistinguishable from the transparent ones over the white list; on the profile at `?tab=starred` all four tabs measured `2px solid black` bottom borders. After the change the selected report tab measures a Highlight fill with white text while the others keep Canvas borders and transparent fills, the marker follows selection after clicking Income with focus blurred, and only the selected profile tab carries a Highlight underline. Keyboard focus was checked by arrowing along the profile tablist: the focused tab reports `outline-style: solid`, `2px`, `CanvasText`, offset 2px, distinct from the Highlight underline beneath it. With forced colors switched back off, ordinary styling is byte-for-byte what it was: the selected pill keeps its white fill, shadow and transparent border, and the selected profile tab keeps its primary-coloured underline with the others transparent. `__tests__/tabs-forced-colors.test.tsx` adds eight cases that render the real components, take the class lists they produce, compile them with the project's own Tailwind and assert on the emitted CSS rather than on class names: that the Highlight declarations exist and are scoped to `[data-state="active"]`, that the Canvas ones are not, that the underline behaves the same way, that a solid CanvasText focus outline is emitted, and that the ordinary shadow cue stays outside the media query. All eight fail when the forced-colors rules are removed. As the note requires, the rendered geometry itself was established by the browser measurements above rather than by those tests. Not verified: real Windows high-contrast palettes, other engines, screen-reader speech, and the other default-variant consumers (New Entry directive tabs, the file Clone menu, and the remaining report pages), which share this component and inherit the change by construction but were not exercised individually. Dashboard gates all pass (`yarn format:check`, `yarn lint`, `yarn test` — 414 files, 4647 passed, 1 pre-existing skip — and `yarn build`). Shipped in `cae07deb` (`dashboard`).
