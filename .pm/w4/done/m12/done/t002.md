---
id: w4/m12/t002
title: Delete the test-only `select-transaction-amount` module and leftover pins for removed features
worker: worker1
status: done
estimate: 20m
depends_on: []
---

## Objective

Remove the dead, divergent duplicate of the app's transaction-amount decision, and any remaining test that pins strings for a removed feature, so t004's check starts clean.

## Context

- `mobile/src/screens/home-screen/selectors/select-transaction-amount.ts` is imported only by `selectors/__tests__/select-transaction-amount.test.ts` (verified 2026-09-15). The live implementation is `selectTransactionAmount` in `src/screens/transactions-screen/utils/transaction-display-utils.ts`, which handles multi-currency postings the dead module gets wrong (w4/025 section 2b).
- `src/translations/__tests__/index.test.ts` still has a "has cadence keys" case pinning `off`, `weekly`, and `monthly`. The Email Report feature they served is gone; check whether any other code uses those keys.

## Steps

1. Delete the module and its test.
2. If the cadence keys have no remaining consumer, delete them from every locale together with the test case; otherwise leave them.

## Files

- `mobile/src/screens/home-screen/selectors/select-transaction-amount.ts` and its test
- `mobile/src/translations/*.ts`, `mobile/src/translations/__tests__/index.test.ts` (only if the keys are unused)

## Acceptance criteria

- [ ] No file references `select-transaction-amount`, `getSignedTransactionAmount`, or `formatSignedAmount`.
- [ ] Cadence keys are either used by live code or removed with their pinning case.
- [ ] `yarn lint` and `yarn test:unit` pass.

## Out of scope

- Changing `selectTransactionAmount` (the transfer netting in `w4/done/004` is separate).
