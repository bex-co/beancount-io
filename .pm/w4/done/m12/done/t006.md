---
id: w4/m12/t006
title: Test coverage
worker: worker1
status: done
estimate: 40m
depends_on: [w4/m12/t003, w4/m12/t004]
---

## Objective

Add fixture-based tests that fail if either check stops catching its shape or starts flagging legitimate code.

## Context

- Tests must run through the package's existing test runner and import the real check modules.

## Steps

1. Operation check: an unconsumed document fails; a document consumed through a hook passes; one consumed only through its `Document` export passes; an allowlisted one passes; references inside `src/generated-graphql/` do not count.
2. Test-only module check: a module imported only by a test fails; one with a production importer passes; one reached only through a barrel with no production importer fails; an allowlisted script passes; Expo Router routes pass.

## Files

- Tests beside the new checks.

## Acceptance criteria

- [ ] Each case above has a test, and each test fails when the behavior it covers is broken on purpose.
- [ ] `yarn test:unit` passes.

## Out of scope

- Tests of knip itself.
