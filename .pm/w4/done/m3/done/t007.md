---
id: w4/m3/t007
title: Adoption surface
worker: worker1
status: done
estimate: 20m
depends_on: [w4/m3/t004, w4/m3/t005, w4/m3/t006]
---

## Objective

Verify the shipped change is discoverable and usable on every surface it touches: the `cash-flow-role` key is documented where users and agents look, and existing docs that describe cash-flow classification still tell the truth.

## Context

- This milestone changes user- and agent-facing behavior (ledger metadata alters rendered reports).
- Surfaces: `dashboard/README.md`, `dashboard/src/features/reports/CLAUDE.md`, root `README.md` / `CLAUDE.md` package tables (backend-cluster path now carries the GraphQL change), user docs under `docs/` if a reports page exists.

## Steps

1. Check every doc that mentions cash-flow classification heuristics; update to mention the override.
2. Ensure the `cash-flow-role` key appears in at least one canonical, greppable doc (ADR003 + feature CLAUDE.md).
3. Verify no stale claims remain (e.g. "classification is always inferred").

## Files

- docs listed above, as needed

## Acceptance criteria

- [ ] A user or agent can discover the `cash-flow-role` key from repo docs alone
- [ ] No doc contradicts the shipped behavior

## Out of scope

- Marketing/launch posts
