---
id: w4/m3/t003
title: Cash-flow loader — merge declared roles into model + status panel
worker: worker1
status: done
estimate: 40m
depends_on: [w4/m3/t001, w4/m3/t002]
---

## Objective

Fetch account metadata in the cash-flow page loader and merge declared roles through the resolver into the cash-flow model: CCE set membership, activity sections, and bottom line all honor declarations; accounts with invalid values surface in the account status panel with an "unknown value, using default" note.

## Context

- Loader: `dashboard/src/features/reports/cash-flow/loader.ts`; model: `lib/model.ts`; status panel: `lib/cash-account-status.ts` + `cash-account-status-panel.tsx`.
- Unannotated ledgers must render byte-identical to today (heuristic path unchanged when no metadata present).
- Resolver from t002; account-details query from t001.

## Steps

1. Extend the loader to fetch account details (meta) alongside interval totals.
2. Pass meta through `buildCashFlowModel` so role resolution replaces raw heuristic calls.
3. Thread invalid-value findings into the account status panel data.

## Files

- `dashboard/src/features/reports/cash-flow/loader.ts`
- `dashboard/src/features/reports/cash-flow/lib/model.ts`
- `dashboard/src/features/reports/cash-flow/lib/cash-account-status.ts`
- `dashboard/src/features/reports/cash-flow/graphql/cash-flow.graphql`

## Acceptance criteria

- [ ] Annotated ledger: declared roles drive CCE set, sections, and bottom line
- [ ] Unannotated ledger: output identical to pre-feature behavior
- [ ] Invalid value appears as a flagged note in the status panel

## Out of scope

- Sankey (t004); row badges and export disclosure (t005)
