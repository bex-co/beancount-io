---
id: w4/m3/t008
title: Simplify
worker: worker1
status: done
estimate: 20m
depends_on: [w4/m3/t007]
---

## Objective

Run `/simplify` over the code this milestone changed — the role resolver, the two loaders' meta plumbing, the categorizer rewiring, and the export disclosure gating — looking for reuse, simplification, and efficiency wins that preserve behavior.

## Context

- Changed surface: `dashboard/src/features/reports/cash-flow/` (config, role-resolver, model, loader, status panel), `overview/lib/account-categorizer.ts`, export model, backend-v2 account resolver.
- Watch for: duplicated meta-fetching logic between loaders, heuristic code now dead in one consumer but not the other.

## Steps

1. Run `/simplify` on the changed files.
2. Apply behavior-preserving simplifications only.

## Files

- files changed by this milestone

## Acceptance criteria

- [ ] No duplicated meta-fetch/resolution logic across consumers
- [ ] All existing tests still pass

## Out of scope

- Refactors beyond the milestone's changed files
