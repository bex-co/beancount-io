---
id: w4/m3/t006
title: ADR003 + docs alignment
worker: worker1
status: done
estimate: 25m
depends_on: [w4/m3/t002]
---

## Objective

Record the decision as ADR003 and bring surrounding docs in line: the single-key `cash-flow-role` spec, why the two-key and `custom`-directive designs were rejected, and how the override layer relates to `config.ts`.

## Context

- Source spec: `dashboard/docs/PRFAQ-cash-flow-ledger-classification.md` (appendix is the normative spec).
- ADR002 (`dashboard/docs/ADR002-cash-flow-report.md`) documents the heuristic classification and its disclosure — it needs a cross-reference to ADR003 rather than a rewrite.
- `dashboard/src/features/reports/CLAUDE.md` documents report conventions.

## Steps

1. Write `dashboard/docs/ADR003-cash-flow-ledger-roles.md`: context (heuristic misclassification is unfixable today), decision (one metadata key, four values, declared-beats-heuristic), alternatives considered (two-key split; `custom` subtree defaults; date-effective roles; settings UI), consequences.
2. Add a forward reference from ADR002 to ADR003.
3. Update `reports/CLAUDE.md` with the metadata key and where the resolver lives.

## Files

- `dashboard/docs/ADR003-cash-flow-ledger-roles.md` (new)
- `dashboard/docs/ADR002-cash-flow-report.md` (cross-ref only)
- `dashboard/src/features/reports/CLAUDE.md`

## Acceptance criteria

- [ ] ADR003 captures the decision and rejected alternatives faithfully to the PRFAQ spec
- [ ] ADR002 and `reports/CLAUDE.md` no longer describe heuristics as the only mechanism

## Out of scope

- User-facing docs site content (handled under Adoption surface)
