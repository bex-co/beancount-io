---
id: w4/m3/t005
title: Declared/inferred indicators + export disclosure gating
worker: worker1
status: done
estimate: 30m
depends_on: [w4/m3/t003]
---

## Objective

Surface the classification source in the UI and exports: rows resolved from ledger declarations carry a "declared" indicator vs "inferred" for heuristic rows, and the CSV/Markdown/print exports print the "classification is inferred" disclosure only when at least one row is actually heuristic-resolved.

## Context

- Statement rendering: `dashboard/src/features/reports/cash-flow/cash-flow-content.tsx` (HierarchyListCard rows); export model: `dashboard/src/features/reports/export/model.ts` (`buildCashFlowDocument` + disclosure notices added in w4/m2).
- Per-row source is available from the resolver output threaded through the model in t003.
- i18n: new strings need real translations in the 15 locale files, per repo convention.

## Steps

1. Add a subtle per-row/per-section "declared" vs "inferred" indicator on the cash-flow page.
2. Gate the export disclosure notice on the presence of heuristic-resolved rows.
3. Add locale strings (all 15 locales, real translations).

## Files

- `dashboard/src/features/reports/cash-flow/cash-flow-content.tsx`
- `dashboard/src/features/reports/export/model.ts` (+ renderers as needed)
- cash-flow + export locale files

## Acceptance criteria

- [ ] Declared and inferred rows are visually distinguishable
- [ ] Fully-declared statement exports carry no inferred-classification disclaimer; mixed ones do

## Out of scope

- A settings UI or any write path for metadata
