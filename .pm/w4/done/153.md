# Finish the remaining Russian catalogs outside the Journal surfaces

Severity: minor-to-moderate localization defect. Estimate: 1h, dashboard.
A2/A3: the same half-translated strings `w4/149` repaired on the Journal
surfaces are still present on nine other pages.

`w4/149` repaired `features/journal/locales/ru.ts` and
`features/reports/account/locales/ru.ts` and shipped a guard over exactly those
two catalogs (`5bf51bb3`). The same first-word find/replace damage remains in
the rest of the Russian set.

**Corrected inventory (2026-09-20):** the all-locale guard shipped for
`w4/m24/t010` lists **30** Russian keys, not the 26 this note first recorded.
The difference is three glued single letters the original survey predicate
missed (`Документs`, `Ошибкаs`, `Событиеs` — it required two or more Latin
letters), plus `common.balanceSheet` = `Баланс Sheet`, `common.incomeStatement`
= `Доход Statement` and `seo.welcome.description`, which live in
`src/i18n/locales/common/ru.ts` and `src/i18n/locales/seo/ru.ts` — two files a
`locales/ru.ts` glob does not match. The authoritative list is the `w4/153`
group in `KNOWN_DEVIATIONS` in
`dashboard/src/test/locale-stray-english.test.ts`.

The list, by catalog:

- `reports/trial-balance` (4) — `Капитал Hierarchy`, `Расходы Hierarchy`,
  `Доход Hierarchy`, `Обязательства Hierarchy`
- `user-settings` (9) — `Счёт deleted successfully`, `Изменить Username`,
  `Удалить Account?`, `Управление your active session`, and a whole English
  sentence left inside `userSettings.deleteAccountDialogDescription`
- `ledger-data/holdings` (3) — `Активы by Account`, `Активы by Cost Currency`,
  `Активы by Currency`
- `ledger-data/statistics` (2) — `Счёт Last Entries`, `Последняя запись Date`
- `ledger-data/documents` (1) — `Документы не найдены in this ledger.`
- `ledger-data/events` (1) — `События не найдены for this ledger.`
- `reports/overview` (1) — `Обязательства Distribution`
- `reports` (1) — `Ledger успешно снял пометку`

Discounted as correct: `beancount` and `option` in the Beancount options
description, `fava-option` in the Fava one, and `cash-flow-role` in the two
cash-flow tooltips — all directive or metadata keywords that belong verbatim.

Repro: the scan predicate in
`dashboard/src/features/journal/components/__tests__/russian-journal-labels.test.tsx`
already produces this list; it is scoped to keys under `journal.` and
`page.accountReport.`. Widening `journalSurfaceKeys` to the whole Russian
catalog reproduces the 26 above.

Acceptance: no Russian message outside the allowlist carries a stray English
word, the guard's key filter is widened to cover the whole Russian catalog, and
reverting the repair fails it. Flag keys, account-name examples, currency codes
and directive keywords stay as notation. English and the other 13 locales are
unchanged. Run the dashboard gates.

Ukrainian carries the same damage at four times the scale and is filed
separately as `w4/152`.

---

**Resolved 2026-09-20** — translated all 30 half-translated messages across 12
Russian catalogs, so every one of the 47 `ru.ts` files now scans clean. Removed
the `w4/153` group from `KNOWN_DEVIATIONS` in
`dashboard/src/test/locale-stray-english.test.ts`; Russian is now asserted to
carry no deviations at all. Shipped in `86aa7494` (`dashboard`).

Wording completed sets the catalogs already had — `Иерархия активов` gave the
other four hierarchies, `Распределение активов` gave
`Распределение обязательств`, and `page.holdings.holdings` was already `Активы`,
so its breakdowns became `по счетам`, `по валюте` and `по валюте затрат`.

One deviation from this note's scope, recorded rather than silent: all four
`page.overview.starButton` messages were fixed, not just the one carrying stray
English. The other three were not half-translated but used two further words
for ledger (`Леджер`, `реестр`) and rendered "starred" as `снялся` — "took
off". Fixing one of four would have left the control incoherent.

The guard's staleness case did its job during this work: with the catalogs
repaired and the deviation list untouched, it failed naming each key as
"already clean", which is exactly the property it was added for.

Verified live at `?lang=ru` — the trial balance liabilities tab reads
`Иерархия обязательств` with 87 rows, Holdings shows all three breakdowns with
115 rows, and neither page has Cyrillic adjacent to Latin anywhere in the DOM.
User settings could not be checked on screen; it needs a signed-in session,
which the public example ledger does not grant.
