# Complete the translated Clear all labels

Severity: minor copy defect. Estimate: 20m, dashboard. A2: localized filtering controls should use a complete local-language action label. Source: repeated dashboard QA, 2026-09-17.

Environment: production https://beancount.io, anonymous public synthetic open_ledger/example, QA-owned isolated headless Chromium 149.0.7827.55, French/light at 1440×1000. Local main `26d54a9db7d33c31a549bd12a0454a1c01f3f038`; deployed main-BIr0jJni.js, exact deployed commit unverified.

Repro: open `/ledger/open_ledger/example/journal?lang=fr`, set Période to `2016-02-01 - 2016-02-29`, and wait for the 36 matching entries. The shared clear button is visibly/accessibly named **Effacer all**. The same label repeats on a fresh `/ledger/open_ledger/example/income-statement?lang=fr&time=2016-02-01%20-%202016-02-29` load. The rest of the principal controls use French. Clearing the Journal filter works; no filtering or API defect is claimed.

Expected: a complete translated phrase such as “Tout effacer”, preserving the action's existing meaning. Actual: the message retains the English word “all”. Source inspection finds the same partial-translation pattern in Spanish (`Limpiar all`), Dutch (`Wissen all`), Chinese (`清除 all`), Russian (`Очистить all`), Portuguese (`Limpar all`) and Ukrainian (`Очистити all`); those six are source-traced, not separately exercised live. English `Clear all` and German `Alle löschen` are correct controls.

Root/fix: correct only `component.searchControls.clearAll` in the seven affected `dashboard/src/i18n/locales/common/{fr,es,nl,zh,ru,pt,uk}.ts` catalogs (French line 607). Its sole production consumer is `common/components/ledger-search-controls/index.tsx:171`, shared by layout-header's desktop inline controls and narrow filter sheet. It already translates the whole key; no component concatenation or fallback bug exists. Use natural phrases in each affected locale and retain existing clear behavior/accessible names. Check the resulting runtime labels and normal locale/package gates; no new test framework, API, dependency or unrelated catalog rewrite.

Catalan has a different, broader wrong-message association and belongs to [m14](./m14/README.md), including its value for this same key. Do not duplicate that locale's work here.

Dedupe/history: all `.pm` including done/blocked searched for the exact mixed phrases, clearAll translations and localized filtering. No existing item. The wrong French value dates to `af5339de` and remains on current main; no later repair/deployment lag found. Existing filter-navigation work owns behavior, which passes here.

Evidence: `dashboard/tmp/qa-20260917/localized-filter-labels.json` records the repeated French label/routes alongside separate Catalan investigation. French February API control returns HTTP 200 with time `2016-02-01 - 2016-02-29` and 36 transactions, including February 29; `french-february-income.md` is the real downloaded statement with inclusive February 1–29 dates and translated export headings. Actual screen-reader speech, narrow French runtime and the six other locale runtimes remain unverified. No product fix, production mutation, commit or push.
