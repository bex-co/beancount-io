# Preserve exact unit balances in Statistics Account Last Entries

**Severity:** minor. **Estimate:** 45 minutes. **Owner:** dashboard. **A2/A3:** readers should be able to reconcile an account quantity across Statistics and Holdings without unexplained rounding.

Production `https://beancount.io`, signed out, English/light, isolated headless Chromium149.0.7827.55. Local HEAD26d54a9d, fetched main bd5d1d31; deployed main-BIr0jJni.js, exact SHA unknown.

Open `/ledger/open_ledger/crypto-example/statistics?lang=en` at390×844. In Account Last Entries, Assets:Crypto:Wallet:MetaMask:ETH displays **4.01 ETH**. A fresh reload at1440×1000 repeats it. The real GetLedgerAccountLastEntries HTTP200 response contains `balance: {"ETH":"4.00995"}` for that account. This table reports unit quantities, not a derived ratio or a fiat valuation; it silently adds0.00005 ETH in presentation.

Working control: open `/ledger/open_ledger/crypto-example/holdings?lang=en&view=by-account` through Holdings → Holdings by Account. The same account displays **4.00995 ETH**. Its two underlying lots are2.00 and2.00995 ETH. Desktop Accounts independently displays4.00995 ETH via its string-preserving BalanceCell; this control is in the same evidence JSON. Statistics correctly preserves the simpler0.072 BTC Mining balance and2.065 ETH staking balance, so this is precision-dependent rather than a wrong account or failed request.

Root: `dashboard/src/features/ledger-data/statistics/account-last-entries.tsx:28–43` converts each decimal string with Number(amount), then calls useFormatNumber. `common/lib/format/format-number.ts:21–29` uses toLocaleString(locale) without fraction options for render_commas=true, whose default maximum is three fractional digits; the false branch explicitly toFixed(2). The live true branch renders4.00995 as4.01. The false branch is source-derived, not a separately changed production ledger option. This balance renderer traces to af5339de;49562f6e later added keyboard account links without changing it. Its existing component test mocks useFormatNumber as String and cannot catch the production rounding.

Producer/control: ledger `src/features/ledger/service/ledger-data-service.ts:494` computes exact own unit balances from the filtered report directives; backend-v2 `src/features/ledger/api/resolvers/ledger-data-resolver.query.ts:498–504` forwards the balance map unchanged. The full-ledger last-entry dates beside filtered balances are deliberate Fava semantics, explicitly documented in that service and completed w3/m14 and w3/138; do not change those dates or the account set.

Fix the Account Last Entries quantity renderer to preserve decimal strings through display. Reuse the existing common inventory/decimal formatting contract (`common/lib/format/inventory-cell.ts`, used by Holdings and BQL) or extend it carefully if lossless locale/grouping is needed. Do not import a private Holdings helper, run values through Number/toFixed, or globally alter count/chart formatting to repair this one consumer. Preserve empty-map zero, null N/A, multi-currency lines, filter scope, keyboard account destinations and SSR/client consistency. No API or dependency change is needed.

Acceptance: the real MetaMask quantity remains4.00995 ETH at390/1440; small nonzero quantities and long decimal strings remain numerically exact with render_commas true/false; multiple currencies retain labels, computed zero and null remain distinct. Include meaningful component coverage with the real formatting path, plus existing filter/link behavior. Run dashboard gates when implementing.

Dedupe: searched all board states for Statistics balances, last entries, precision, formatNumber/useFormatNumber and the exact quantity. Completed w3/189 (and retired w4/057) fixes a separate mobile money formatter; w3/110 fixes locale/hydration and count formatting; w3/m37 provides the shared lossless inventory helper; w3/187 deliberately rounds only Holdings derived ratios. Open w4/m20 names these tables and should coordinate this same file, but does not own their quantities. Open117 concerns abbreviated chart-axis ticks, not this unit-balance cell. The local formatBalance has one production call; the broader shared number hook also serves report charts/hierarchies, Overview, Budget and Statistics counts, so leave those semantics unchanged unless independently reproduced and scoped.

Evidence: `dashboard/tmp/qa-20260917/statistics-precision.json` contains both viewport repeats, real responses and the Holdings control; `dashboard/tmp/qa-20260917/statistics-precision-1440.png` was visually inspected. Other engines/locales, failure-state transitions, render_commas=false in production and a patched implementation remain unverified. No product changes, production writes, commits or shipping.
