# Preserve visible Journal filter selection in forced colors

Severity: minor. Estimate: 45m, dashboard. A2: a reader can see which Journal filters caused an empty or restricted result. Repeated dashboard QA, 2026-09-18.

Production beancount.io, anonymous public open_ledger/example, English/light, isolated headless Chromium149.0.7827.55 with browser forced-colors:active emulation;390×844 and fresh1440×1000 reload. HEAD26d54a9d, fetched origin/mainbd5d1d31, deployed main-BIr0jJni.js; exact deploy SHA unknown. This is browser emulation, not a physical Windows session.

Open `/ledger/open_ledger/example/journal?time=2016-02`. Transaction and Metadata are pressed by default but look the same as unpressed neighboring buttons. Select Pending transactions (`!`), then move focus away and reload at desktop width. Results become0–0/0 with the truthful current-filters empty message, but `!` is visually indistinguishable from unselected `*` and `x`. `aria-pressed` correctly reports Transaction, Pending and Metadata as true. This is lost visual selection, not lost semantics or incorrect filtering. The matching real GetLedgerJournal read with directiveTypes[Transaction], transactionSubtypes[pending], time2016-02 returnsHTTP200,total0. Ordinary colors visibly distinguish the pressed buttons with fill, border and shadow.

Computed styles in forced colors: both states have black text, font-weight500,1px black border and no shadow; selected white background and unselected transparent background over white are visually identical. The issue occurs in the main directive buttons, transaction subtypes and Metadata/Postings toggles. Other document/custom subtype buttons share the same styling but were not exercised with populated records.

Root: `dashboard/src/features/journal/components/journal-filters.tsx:335–336,382–386,412–416,484–488` switches secondary/ghost variants, border color and shadow based on isButtonActive. Forced colors removes the shadow and maps both text/border colors to the same system colors. The other subtype branches repeat this styling. Two owning surfaces use JournalFilters: main Journal and the Account report's journal branches at `features/reports/account/index.tsx:202,251`; Account is source-derived regression scope here.

Add a persistent, forced-colors-compatible pressed indicator to these filter buttons, using system colors or distinct geometry while preserving a separate keyboard-focus cue. Reuse a small local style expression across the actual branches if helpful; do not change global Button appearance or interpret every secondary button as selected. Preserve aria-pressed, parent/subtype retention, horizontal narrow scrolling, metadata/posting behavior and ordinary light/dark styling. No API or dependency change is required.

Dedupe: all board states searched for Journal pressed-state/high-contrast defects, plus targeted source history. w4/131 owns the separate Tabs primitive; fixing it does not change these ordinary Button instances. w4/086 is missing semantic state on Net Worth's Chart/Table controls, unlike the correct aria-pressed here. Existing filter behavior and pagination records do not own this visual state. JournalFilters styling remains fromaf5339de with no newer repair.

Acceptance: after selection and focus departure, Transaction, Pending and Metadata are visibly pressed while neighboring unselected buttons remain distinguishable at both widths under forced colors. Deselect Pending and retain the real36-transaction February control; verify keyboard operation, subtype retention and Account's shared consumer. Use browser-rendered styling checks, not only a class-name assertion, and run dashboard gates when implementing. Other engines, actual Windows palettes, screen-reader speech, authenticated forms and patched behavior remain unverified.

Evidence: `dashboard/tmp/qa-20260917/journal-forced-colors.json` records narrow/default and fresh desktop/pressed styles, normal-color controls, semantic snapshot and the API control. `journal-forced-colors-1440.png` was visually inspected at its verified1440×1000 dimensions. An earlier wait used a shortened empty-state string and timed out; the final repeat waits for the real full message and is independent of that harness error. No clipboard access, ledger mutation, product edit, commit or push.
