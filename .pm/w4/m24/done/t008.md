---
id: w4/m24/t008
title: "Adoption surface: verify the repaired screens in Ukrainian"
worker: worker1
status: done
estimate: 20m
depends_on: [w4/m24/t007]
---

## Objective

Walk the screens this milestone changed with `?lang=uk` and confirm the
repaired strings render as Ukrainian at both widths, rather than trusting the
catalogs alone.

## Context

- This milestone changes user-facing strings on most screens in the product, so
  the adoption surface is the screens themselves, not a package README.
- `w4/149` found two defects this way that a catalog diff alone had missed: a
  heading owned by a different catalog than the one under repair, and entries
  that were partly translated rather than untranslated.
- The public `open_ledger/example` ledger is anonymous and read-only, so
  write-path surfaces (the new-entry form, Danger Zone, SSH keys) cannot be
  reached there; say so rather than implying they were checked.

## Steps

1. Open the journal, account report, statistics, documents, events, gallery and settings screens with `?lang=uk` at 1440x900 and 390x900.
2. Collect the rendered headings, placeholders and button labels, and confirm none carries a stray English word outside the allowlist.
3. Confirm `lang="uk"` on the document and that rows still load.
4. Note any screen that could not be reached anonymously, and any defect found that belongs to a different catalog — file it rather than widening this milestone.

## Files

- No source changes expected; this task reports.

## Acceptance criteria

- [ ] Every reachable repaired screen shows no stray English at either width.
- [ ] Screens that could not be reached are named explicitly in the task's close record.
- [ ] Any out-of-scope defect found is filed as its own note.

## Out of scope

- Fixing defects in catalogs this milestone does not own.

## Result (2026-09-20)

Source scan first: across every `locales/uk.ts`, Cyrillic adjacent to Latin now
returns **0** matches, and the stray-English predicate returns 4, all verified
legitimate — `aiAgent.welcome` (the match is the `n` of its `\n\n` escape),
the two cash-flow tooltips (`cash-flow-role` is a metadata key), and
`userProfile.nameAZ` (`A–Z` is sort notation).

Walked at 1440x900 and 390x900 with `?lang=uk`, scanning the whole DOM for
Cyrillic adjacent to Latin. Every screen returned zero:

| screen | evidence |
| --- | --- |
| Journal | 36 rows; first screen to come back clean, in t007 |
| Account report | cards `Баланс рахунку` / `Журнал рахунку`, 10 rows |
| Statistics | `Останні записи рахунку`, `Дата останнього запису`, `Відсоток`, 75 rows |
| Documents | heading `Документи`, was `Вocuments` |
| Errors | heading `Помилки`, was `Помилкаs` |
| Trial balance | `Ієрархія активів`; the liabilities tab reads `Ієрархія зобов'язань`, 87 rows |
| Overview | `Розподіл активів` and `Розподіл зобов'язань` — the pair completed |
| Ledger editor | title `Файли` (was `Файлs`), `Історія версій`, `Копіювати код` |
| Login | `Адреса електронної пошти`, `Введіть пароль (мінімум 6 символів)` |

`lang="uk"` on the document throughout, and rows loaded on every screen that
has them.

**Not reachable anonymously**, so repaired at the catalog level only and not
seen on screen: the ledger dashboard and gallery (both redirect to login), user
settings including the SSH-key and theme strings from t003, the collaboration
invite dialog from t001, the settings Danger Zone from t006, and the
new-directive dialog. These need a signed-in session, which the public example
ledger does not grant.

English text remaining on the ledger-editor screen — `Beancount Example
Ledger`, `Quick Start`, `What's Inside` — is the ledger's own README content,
not UI, and is correctly left alone.

No new out-of-scope defect was found during the walk. The one raised earlier in
the milestone, seven untranslated `auth.oauth*` keys, is filed as `w4/155`.
