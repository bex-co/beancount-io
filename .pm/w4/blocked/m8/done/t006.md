---
id: w4/m8/t006
title: Simplify
worker: worker1
status: done
estimate: 25m
depends_on: [w4/m8/t005]
---

## Objective

Run `/simplify` over the code this milestone changed and apply the reuse, simplification, and efficiency findings without changing behavior.

## Context

- Behavior-preserving only; a bug found here becomes a separate finding, never folded in.

## Steps

1. Run `/simplify` scoped to the files this milestone touched.
2. Apply findings; re-run the package checks.

## Files

- mobile/scripts/
- mobile/metadata/

## Acceptance criteria

- [ ] The package checks pass after the cleanup.
- [ ] No behavior change is introduced; the diff reads as cleanup only.

## Out of scope

- New features or bug fixes.

## Validation evidence

- 2026-09-10 simplification pass over the milestone's Play files (behavior-preserving only, no bug fixes folded in):
  1. `play-api.ts` exports `PLAY_MANAGED_IMAGE_TYPES`; `play-release.ts` uses it in both the digest and the plan builder instead of two inline literals.
  2. `play-metadata.ts` `derivePlayListings` drops the `supplemental` intermediate (`copy` reuses `info` for `bg`/`fa`).
  3. `play-release.ts` `buildPlayPlan` hoists the locale-invariant phone/feature filename lists out of the per-locale map.
- Proof of no behavior change: `yarn typecheck` passes, 29 script tests pass, Prettier check passes, and `plan-play` reproduces the identical plan SHA-256 `b4921f1697ffee42c3218ce4ac7fd7ba454e3e62b3aab73d7fedfcca5cf5636f`.
