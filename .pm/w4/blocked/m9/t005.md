---
id: w4/m9/t005
title: "Docs: multi-currency and crypto section in USAGE and TUTORIAL, skills guide note, drafted reply for issue 176"
worker: worker1
status: todo
estimate: 30m
depends_on: [w4/m9/t003, w4/m9/t004]
---

## Objective

Document the price verbs where users and agents look, keep the generated reference in sync, and prepare the reply that closes the loop with the community member who asked for this.

## Context

- `cli/docs/USAGE.md` has an "Ask (optional extra)" section; add "Prices (optional extra)" in the same style, covering the metadata grammar, `fetch`, `sources`, `--dry-run`, JSON shape, and exit codes.
- `cli/docs/TUTORIAL.md` is the first-month tutorial; add a short "Holding more than one currency" chapter ending with a converted balance via `bea report`.
- `cli/README.md` has a command map; `cli/docs/REFERENCE.md` is generated by `make docs` and gated by `make docs-check`.
- `cli/tests/test_docs_examples.py` executes documented examples; follow its convention for examples that need the network.
- Posting on GitHub is the user's action. Draft the reply to issue 176 and place it in this task's closing notes in the milestone README, not in a repo file.

## Steps

1. Write the USAGE section and the TUTORIAL chapter; update the README command map.
2. Run `make docs` and `make docs-check`; run `make check-all`.
3. Draft the issue reply: what shipped for the CLI and agent path, how to declare sources, and where the hosted and mobile follow-ups are tracked on the board.

## Files

- `cli/docs/USAGE.md`, `cli/docs/TUTORIAL.md`, `cli/README.md`, `cli/docs/REFERENCE.md`

## Acceptance criteria

- [ ] Every command in the new sections runs as written on the tutorial ledger.
- [ ] `make check-all` passes, including `docs-check`.
- [ ] The drafted reply is in the milestone README for the user to post.

## Out of scope

- Posting the reply.
