---
id: w4/m11/t005
title: Verify the rule on the public example ledgers against BQL
worker: worker1
status: todo
estimate: 40m
depends_on: [w4/m11/t003, w4/m11/t004]
---

## Objective

Observe every definition-of-done bullet in the running app, against ledgers whose source can be queried, so the milestone closes on a verified end state rather than on passing unit tests.

## Context

- Use a native development build on the iPhone 17e simulator (the QA environment of [w4/016](../../016.md)).
- `open_ledger/example` is Beancount's generated example ledger; confirm it holds `RGAGX`, `VACHR`, and `IRAUSD` before relying on it. `open_ledger/crypto-example` covers `STETH` and `BTC`. If either lacks a needed shape, use a synthetic ledger on a local stack with the same shapes.
- Read-only: no writes to any hosted ledger.

## Steps

1. Walk the Accounts tree, the RGAGX, Vacation, PreTax401k, Lido STETH, and BofA Checking detail screens, and Home.
2. Compare the operating-currency figures with BQL `sum(cost(position))` and the units with `sum(position)`.
3. Keep screenshots under the gitignored `mobile/tmp/`; record the observed results, without images, in the milestone README.

## Files

- `.pm/w4/m11/README.md` (verification record)

## Acceptance criteria

- [ ] Each definition-of-done bullet is recorded as observed, with the ledger and screen it was observed on.
- [ ] Every operating-currency total checked matches BQL to the cent.

## Out of scope

- Android and physical-device checks, unless a finding makes them necessary; record them as not exercised.
