# VoiceOver + Accessibility Inspector residual for w4/m7

m7 shipped labels, chart alternatives, validation announcements, and the icon-only guardrail. Full Xcode Accessibility Inspector zero-error certificates and an end-to-end VoiceOver journey on a physical device were not captured in the agent session (Inspector is GUI-only). Re-run the Inspector on the ten screens in light/dark and walk the read/write journeys with VoiceOver when convenient.

## Blocked

**Blocked 2026-09-14 by `/loop-worker w4` triage.** The residual is a manual accessibility certification. It needs Xcode Accessibility Inspector audits, which are GUI-only, on the ten w4/m7 screens in light and dark, plus an end-to-end VoiceOver walk of the read and write journeys on a physical device. Neither can be run by this worker. The automated guardrails w4/m7 shipped (labels, chart alternatives, validation announcements, the icon-only label test) stay in the unit suite.

**Unblock:** a person runs the Inspector passes and the device walk. File any finding as its own inbox note, then close this note as done.
