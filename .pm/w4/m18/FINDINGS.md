# BQL cells hide multiline inventories and printed transaction postings

Severity: major presentation defect (amounts are present in the API/CSV but hidden on screen). Expanded estimate: 120m across implementation and verification, dashboard (original inventory-only note was60m). A2: a newcomer querying asset totals must be able to read every returned unit. Source: repeated dashboard QA, 2026-09-17.

Environment: production https://beancount.io; anonymous public synthetic `open_ledger/example`; QA-owned isolated headless Chromium 149.0.7827.55, English/light, 1440×1000 and fresh 390×844 reload. Local main `26d54a9db7d33c31a549bd12a0454a1c01f3f038`; deployed asset main-BIr0jJni.js, exact deployed commit unverified.

Repro: open `/ledger/open_ledger/example/query`, enter `SELECT sum(position) WHERE account ~ '^Assets'`, and execute. The single result cell contains eight unit lines, but only `3609.87 USD` is fully visible; the next line is cut and the remaining six are hidden. Reload the resulting bookmarked query URL at 390px: same clipping. The desktop screenshot was visually inspected. There is no cell disclosure or usable scrolling mechanism to reveal the clipped content.

Expected: all eight amount/unit pairs are visibly readable, without overlapping adjacent rows, and the result remains navigable in larger tables. Actual: fixed row size truncates financial output even though the accessibility tree contains the hidden text.

API control: browser QueryShell returns HTTP 200, one Inventory cell `{USD:"3609.87",VACHR:"-13",VBMPX:"193.442",RGAGX:"597.748",VEA:"36",ITOT:"104",VHT:"64",GLD:"17"}`. Download CSV contains all eight newline-separated pairs in one correctly quoted cell. Desktop cell clientHeight is 35px, scrollHeight 157px, overflow hidden; narrow is 35px/153px. First line spans y665.75–683.25 within cell ending y691.75; later lines extend to y805.75. This is clipping, not missing data or an incorrect sum.

Root: `features/bql/components/query-result-card.tsx:50–59` renders a multi-unit Inventory as a vertical stack. The same component fixes ROW_HEIGHT at 36, passes it to react-window at 148, limits container height to rows.length×36 at 151, and applies truncate/overflow-hidden on each cell at 170. The multiline renderer added by `8f149935` does not inform the virtual row/container sizing. Installed react-window List supports a row-height function or dynamic heights (`dist/react-window.d.ts:406`), so no dependency is needed.

Fix: size each row and the viewport consistently for the maximum displayed cell content, or provide an explicit accessible full-cell presentation that exposes all units without silent clipping. Keep ordinary one-line rows compact and preserve virtualization, aligned columns, row indexes, keyboard scrolling and exact value formatting. Do not simply remove overflow hiding while retaining overlapping 36px rows. Do not fix only the one-row container while leaving multi-row inventories clipped.

Acceptance: the eight-unit query is fully readable on desktop/narrow; a multi-row query with multi-unit inventories does not overlap or skip content when scrolling. One-unit/Amount/plain scalar rows and CSV output stay correct. Working control: `SELECT date, account LIMIT 1001` reports 1001 rows and its CSV has exactly 1001 data rows; retain large-result behavior. Use actual browser/layout coverage or real virtualizer geometry, not a test that merely finds hidden text (the current DOM already contains every unit). Run dashboard native gates when implementing.

Dedupe: all queues/done/blocked searched for query/BQL row height, inventory clipping/truncation, multiline rows. Regression against **w3/done/m37**, whose DoD says multi-currency cells render every unit and preserve m28 virtualization/layout at both widths. Its shipping commit `8f149935` is the latest change to this renderer; preceding `917558c0` owns column alignment. No newer repair or open duplicate found. `w4/077` is the independent “1 rows” copy issue seen alongside this result; coordinate the same component without conflating them. The helper's other consumer, Holdings, has a separate renderer and is not claimed affected.

Verified evidence: `dashboard/tmp/qa-20260917/query-inventory-clipped.json`, `query-inventory-clipped.png`, `query-inventory-clipped-narrow.png`, `query-inventory.csv`, and large-result control `query-1001.csv`. Authenticated roles, other browsers, screen-reader speech, denied/network states and patched behavior remain unverified. No backend/API changes, product fixes, production mutations or shipping performed.

Live shared-helper control: public crypto-example Holdings by Cost Currency at390px has two rows; its second Units cell renders all13 commodities in308px with scrollHeight=clientHeight=308. CSV contains both data rows and the full inventory, including7.07495ETH and230962.50CUSDC. Verified `dashboard/tmp/qa-20260917/holdings-cost-currency.csv`. This confirms Holdings consumes the shared inventory formatter successfully; keep the repair scoped to BQL virtual-row geometry.


Additional reproduced scope — PRINT strings: at desktop1440×1000 open `/ledger/open_ledger/freelancer-invoicing/query`, execute `PRINT`, then fresh-reload its bookmarked `?query=PRINT` URL at390×844. The API returns44 rows in a single directive column, dtype=str. Its Opening/Starting balances row is a four-line directive with Assets:Bank:Business12000.00USD, Assets:Bank:TaxReserve4500.00USD and Equity:Opening-16500.00USD. Narrow UI renders one ellipsized line beginning the transaction header; posting amounts cannot be read. Its cell is338px wide with scrollWidth1005,35px high, white-space:nowrap, overflow:hidden, and no title or full-cell disclosure. Desktop also collapses the original newlines into one line, although that particular row's text fits the wider cell. Do not claim desktop clipping for this PRINT row.

The raw DOM text and CSV retain the newlines and all three posting amounts. `query-print.csv` parses as one header plus44 rows; the Opening cell has all four source lines. This is the same virtual-table cell containment problem through the scalar-string branch: renderQueryCell returns the string, while the common truncate cell class flattens its whitespace and clips overflow. `backend-cluster/ledger/src/features/ledger/service/ledger-shell-mappers.ts:164–175` preserves strings and queryResultToShellResult at189–201 intentionally maps the engine result to a typed table; do not change that API contract or rewrite source directives to one-line text. Current query-result-card history is8f149935,917558c0,af5339de. All .pm queues searched for PRINT/multiline-string/clipped-directive output; CLI w3/242 is a separate export-format contract and does not own this dashboard renderer.

Expanded repair: expose complete cells with correct line structure through row/viewport sizing or a clear keyboard-accessible full-value presentation. A tooltip-only pointer solution is insufficient. Keep ordinary rows compact, aligned columns, row indexes, responsive sizing, virtualization and CSV bytes. Handle both multi-unit Inventory and newline-bearing str values; the generic mechanism should not silently lose long scalar values either. Never simply remove overflow while retaining overlapping36px rows. Retain m28/m37's semantic and exact-value guarantees and the already passing1001-row scroll/export control.

Expanded acceptance: inspect every amount in the eight-unit Inventory query and all four lines of PRINT's Opening transaction on desktop/narrow, including keyboard access to any full-value action and focus return. Exercise multiple tall values in one virtual table, scrolling to later rows, empty/one-line/plain Decimal/Amount cases, and close/reopen/reload behavior. Require meaningful geometry or real browser coverage, not assertions that merely find text already hidden in the DOM. Budget the combined implementation and regression scope as m18 rather than the retired sub-hour078 note.

Additional evidence: `dashboard/tmp/qa-20260917/query-print-clipped.json`, `query-print.csv`, and visually inspected `query-print-narrow.png`. The screenshot is scrolled to the Opening row; its clipped posting content and full correct API/CSV are independently recorded. No product fix, data mutation or shipping.
