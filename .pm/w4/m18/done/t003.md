---
id: w4/m18/t003
title: Verify the query inspection adoption journey
worker: worker1
status: done
estimate: 10m
depends_on: [w4/m18/t002]
---

## Objective

Exercise the real aggregate and PRINT examples with the chosen affordance. Ensure any required action is visible and understandable, and update existing guidance only if the interaction changes.

## Context

See [FINDINGS.md](./FINDINGS.md) and the milestone definition of done; preserve w3/m28/m37 behavior.

## Files

BQL result UI and relevant query guidance if needed

## Acceptance criteria

- [x] A reader can discover and inspect full values using pointer and keyboard without relying on CSV as the only readable copy.

## Out of scope

Backend/API changes, Holdings layout changes, financial rounding policy, new dependencies and unrelated BQL copy fixes.

## Outcome

Ran the real journey rather than bookmarked URLs: open the query page, click
into the Monaco editor, replace the sample query, execute, and read the result.
24/24 checks passed at 1440x1000 and 390x844, each width driven both ways — by
pointer (the visible **Execute Query** button) and by keyboard (Cmd+Enter, the
shortcut the page itself advertises under the editor).

- `SELECT sum(position) WHERE account ~ '^Assets'` on the public `example`
  ledger: all eight units — 3609.87 USD, -13 VACHR, 193.442 VBMPX,
  597.748 RGAGX, 36 VEA, 104 ITOT, 64 VHT, 17 GLD — readable in the cell, no
  clipping, no page errors.
- `PRINT` on `freelancer-invoicing`: scrolling the virtual list reaches the
  Opening row, and its cell shows all four lines with all three posting
  amounts, unclipped, at both widths.

No disclosure control, tooltip or expand action is involved, so there is
nothing new for a reader to discover and no focus behaviour to manage — the
values are simply on screen. CSV is no longer the only readable copy.

Adoption surfaces checked: nothing in the root `README.md`, `dashboard/README.md`,
`dashboard/docs/`, the `skills/` catalog or the BQL locale catalogs describes
result-cell truncation, an expand affordance, or exporting in order to read a
value. The interaction did not change, so per this task's objective no guidance
was updated. The change adds no command, skill or setting, so there are no
package tables or skill catalogs to refresh.
