---
id: w4/m25/t005
title: Simplify the scan module after the second predicate lands
worker: worker1
status: todo
estimate: 20m
depends_on: [w4/m25/t004]
---

## Objective

Run `/simplify` over `locale-scan.ts` and the guard once they carry two
predicates, for reuse, simplification and efficiency, preserving behaviour.

## Context

- `w4/m24/t009` already collapsed two locale records into one and folded an
  adjacency rule into a single predicate. Adding an en-comparison predicate is
  exactly the kind of change that re-introduces that shape, so it is worth a
  second pass.
- Watch for: two allowlists where one would do, a locale roster declared twice,
  and a per-locale `if` ladder that wants to be data on `SCANNED_LOCALES`.

## Steps

1. Run `/simplify` scoped to this milestone's diff.
2. Apply the behaviour-preserving findings; note any skipped and why.

## Files

- `dashboard/src/test/locale-scan.ts`
- `dashboard/src/test/locale-stray-english.test.ts`

## Acceptance criteria

- [ ] No duplicated allowlist, roster or predicate remains.
- [ ] The dashboard gates pass and the guard's behaviour is unchanged.

## Out of scope

- Behaviour changes.
