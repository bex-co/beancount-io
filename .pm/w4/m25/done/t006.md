---
id: w4/m25/t006
title: Extend the guard to all fourteen locales and prove it fails
worker: worker1
status: done
estimate: 30m
depends_on: [w4/m25/t004]
---

## Objective

Ship t001's predicate inside the guard, covering all fourteen non-English
locales, and prove it is load-bearing.

## Context

- The guard runs over seven locales today, from `SCANNED_LOCALES` in
  `dashboard/src/test/locale-scan.ts`. The Latin-script seven are currently
  listed in `LATIN_SCRIPT_LOCALES` as knowingly out of reach; that list is what
  this task retires or repurposes.
- `UNCLASSIFIED_LOCALES` asserts every supported language is accounted for. If
  the two lists merge, that assertion must keep working.
- `KNOWN_DEVIATIONS` is empty as of `w4/156`. Anything this task cannot repair
  goes there key by key against a filed note, never into the allowlist.

## Steps

1. Move the predicate from t001 into `locale-scan.ts` beside `strayEnglishWords`.
2. Run it over all fourteen in `locale-stray-english.test.ts`.
3. Revert one repair from t002 and confirm the guard fails naming that key; restore it.
4. Run the gates from `dashboard/`.

## Files

- `dashboard/src/test/locale-scan.ts`
- `dashboard/src/test/locale-stray-english.test.ts`

## Acceptance criteria

- [ ] The guard covers fourteen locales, and no supported language is unclassified.
- [ ] Reverting a t002 repair fails it by name.
- [ ] Every deviation names a filed note; the allowlist holds only Latin that belongs in a translated string.
- [ ] The dashboard gates pass.

## Out of scope

- English, which is the comparison basis and has nothing to scan.
