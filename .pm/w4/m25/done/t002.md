---
id: w4/m25/t002
title: Repair the keys already repaired in other locales
worker: worker1
status: done
estimate: 30m
depends_on: [w4/m25/t001]
---

## Objective

Repair the Latin-script instances of keys this board has already fixed
elsewhere. These need no new detection work — the key list is known and the
correct meaning is settled by the English source and by the repairs already
shipped.

## Context

- `seo.welcome.description` — English after its first word or two in **ca, de,
  fr, nl, pt, sk**. Already repaired in bg (`w4/156`), fa (`w4/156`) and ru
  (`w4/153`), so nine of fourteen locales carried one bulk edit's damage.
- `page.accountReport.accountBalance`, `accountJournal`, `title` — damaged in
  **es** (`Cuenta Balance`), **fr** (`Compte Balance`) and **pt**
  (`Conta Balance`). Repaired in ru (`w4/149`), uk (`w4/m24/t005`) and zh
  (`w4/156`).
- `page.statistics.accountLastEntries` — **es, fr, pt**. Repaired in ru and uk.
- `page.dashboard.deleteLedger`, `editLedger`, `editLedgerSettings` — **de, es,
  pt**. Repaired in uk (`w4/m24/t004`).
- `page.dashboard.nameMaxLength` — **es, nl**. Repaired in uk.
- `ledgerEditor.deleteFileTitle` — **es, pt**. Repaired in uk (`w4/m24/t007`).
- `journal.clearedTransactions`, `otherTransactions` — **pt**. Repaired in ru and uk.
- `auth.usernameRequired` — **pt** (`Usuárioname is required`). Repaired in uk.
- `userSettings.userProfile` — **de** (`Benutzer Profile`). Repaired in ru and uk.

## Steps

1. For each key, read the English source and the repairs already shipped in the other locales.
2. Use the word for "ledger" each catalog's own `common.ledgerSettings` and `common.failedToLoadLedger` use — **not** `page.dashboard.deleteLedger`, which is itself damaged in de, es and pt.
3. Keep `Beancount` as the brand name; keep `{placeholder}` interpolations.
4. Run the gates from `dashboard/`.

## Files

- `dashboard/src/i18n/locales/seo/{ca,de,fr,nl,pt,sk}.ts`
- `dashboard/src/features/reports/account/locales/{es,fr,pt}.ts`
- `dashboard/src/features/ledger-data/statistics/locales/{es,fr,pt}.ts`
- `dashboard/src/features/ledger-list/pages/dashboard-page/locales/{de,es,nl,pt}.ts`
- `dashboard/src/features/ledger-editor/file-editor/locales/{es,pt}.ts`
- `dashboard/src/features/journal/locales/pt.ts`
- `dashboard/src/features/auth/locales/pt.ts`
- `dashboard/src/features/user-settings/locales/de.ts`

## Acceptance criteria

- [ ] None of the keys above is half-translated in any Latin-script locale.
- [ ] Key counts and `{placeholder}` interpolations are unchanged; English and the seven non-Latin locales are untouched.
- [ ] The dashboard gates pass.

## Out of scope

- Whatever else t001's predicate turns up; that is t003.
