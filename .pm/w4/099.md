# Mark account chart conversion and interval reads pending before relabeling data

Severity: minor pending-state/basis mismatch; no incorrect export demonstrated. Estimate: 55m, dashboard. A2: readers should know whether the selected account chart conversion has actually loaded.

Environment: production https://beancount.io, signed out, synthetic public crypto-example, isolated Chromium149.0.7827.55, English/light390×844 and a separate fresh1440×1000 context. Local/fetched main26d54a9db7d33c31a549bd12a0454a1c01f3f038; deployed main-BIr0jJni.js, exact deployed SHA unverified. Reads were held only inside the QA browser, without fabricating responses or causing a server outage.

Reproduction: open `/ledger/open_ledger/crypto-example/account/Assets%3ACrypto%3ABinance%3ASOL`, select Changes Over Time, keep Monthly/At Cost, and wait for the chart. Hold only GetLedgerAccountReport with conversion=units, then select Units. The control changes immediately, but the old USD chart remains interactive without pending feedback or aria-busy/progressbar. Hover June2026: it still reports USD−7,494. The legend also still says USD, so this is an old-basis chart under a new selected basis, not a claim that the tooltip relabels those dollars SOL. Both widths reproduce after settlement while the request remains held.

Release the unchanged real request: HTTP200, June30 intervalTotalsData.balance becomes {SOL:"-60"}; the chart now reports SOL−60 with the Units selection unchanged. The recorded At Cost control is {USD:"-7494"}. Narrow before/after screenshots were visually inspected. The API honors both conversions; the client presents the old result as though the selection has finished. No account export exists in this tested path, so do not inherit the major CSV severity from the older report-export finding.

Root: `dashboard/src/features/reports/account/index.tsx:338–361` retains data or previousData and only shows initial loading when neither exists. The settled chart receives the current conversion/interval controls while the previous report remains rendered. The fallback dates to af5339de and survives current be1251ab. Unlike the four reports updated by05c78c69, AccountPage does not pair retained data with an explicit pending scope. Both account chart views consume that report; Changes Over Time is the directly reproduced view and Account Balance remains source-traced. Interval relabeling is additionally reproduced below.

Repair the account chart boundary using the existing request-readiness conventions: show loading while the selected read is pending, or clearly label retained data with its completed basis and pending status. Keep selection and journal state alive across this read; avoid introducing w4/m16-style resets by unmounting the journal's controls/pagination. Preserve cached transitions, coherent successful data, real error recovery, account/ledger changes and the journal's independent read state. No backend, API, calculation, dependency or global-store change is indicated.

Acceptance: desktop/narrow At Cost→Units shows honest pending state until the matching response arrives; afterward June2026 is SOL−60 and both chart views agree with the accepted conversion. Exercise interval/conversion races, a rejected read followed by recovery, and instant cache hits. Keep Changes Over Time selected and preserve unrelated journal filters/position. A real held-read regression should check actual rendered old/new scope, not a mocked selector alone. Run dashboard native gates. Natural slow-server frequency, physical devices, other engines and patched behavior remain unverified.

Dedupe: all .pm including done/blocked searched for previousData, stale account/report charts and pending conversion. Completed w3/m35 fixes the common route-pending phase and four other reports' local reads/exports; its explicit implementation scope excludes AccountPage. w4/m16 owns selection resets caused by that new four-report guard. AccountPage's Changes Over Time selection survived a fresh Quarterly transition in this run, so do not count this as another m16 reset. w4/093 owns source-navigation pagination; completed w3/m40 owns journal column meanings. No active item repairs the account chart's local pending read.

Evidence: `dashboard/tmp/qa-20260917/account-pending-conversion.json` has held/released text, request variables, actual At Cost/Units responses and desktop/narrow controls. Screenshots: account-units-held-390.png, account-units-settled-390.png, account-units-held-1440.png. All held requests released, interception removed and the extra context closed. No ledger writes, product edits, commits or shipping.

Interval reproduction, two fresh1440×1000 browser loads: example Checking account with time=2016, Changes Over Time, Monthly/At Cost. Hold only GetLedgerAccountReport(interval=yearly), then select Yearly. All12 monthly USD bars remain byte-for-byte the same, but their rendered axis labels immediately change from2016-01…2016-12 to twelve copies of2016. There is no aria-busy/progressbar or visible pending message. Releasing the unchanged real request returns HTTP200 and replaces those bars with the single correct yearly total−1652.64USD. This directly mislabels the old periods while pending, beyond the conversion selector mismatch already described. Preserve the chart's selected view and independent journal state while withholding or clearly labeling retained scope. `dashboard/tmp/qa-20260917/account-pending-interval.json` contains the before/held/after chart model and actual rendered label strings, request and real response; `account-yearly-held-repeat-1440.png` was visually inspected. The interval-specific narrow case and natural server-delay frequency remain unverified. The request was released and the interceptor removed after its response completed. No new finding group or API change is needed.
