---
id: w2/m23/t003
title: "Defer optional overview panels"
worker: worker2
status: todo
estimate: 60m
depends_on: [w2/m23/t002]
---

## Objective

Let primary overview data render before README and account metadata finish. Isolate metadata-dependent charts with explicit pending/error states, retaining declared cash-flow classification and existing failure semantics.

## Context

- Parent milestone: w2/m23; follow its definition of done and measurement requirements.
- The ledger route awaits ledger metadata and sidebar counts together; the overview loader awaits overview data, README, and account metadata together. Requests already run concurrently, but optional requests still gate completion.

## Steps

1. Let primary overview data render before README and account metadata finish. Isolate metadata-dependent charts with explicit pending/error states, retaining declared cash-flow classification and existing failure semantics.
2. Record applicable evidence against the parent milestone's acceptance requirements.

## Files

- `dashboard/src/features/reports/overview/`
- `dashboard/src/common/components/readme-card.tsx`

## Acceptance criteria

- [ ] A two-second README or metadata delay does not gate primary content; declared roles are authoritative and pending metadata is not silently treated as absent.
- [ ] Preserve the parent milestone's accounting, access, and scope constraints.

## Out of scope

Unrelated feature work, new cross-package imports, backend/API-contract changes, and unapproved dependencies. Do not change accounting semantics to improve timing.

