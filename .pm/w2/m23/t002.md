---
id: w2/m23/t002
title: "Resolve sidebar counts independently"
worker: worker2
status: todo
estimate: 45m
depends_on: [w2/m23/t001]
---

## Objective

Move count loading off the required ledger completion path using the established TanStack/Apollo integration. Preserve honest pending/unavailable states and required ledger access errors.

## Context

- Parent milestone: w2/m23; follow its definition of done and measurement requirements.
- The ledger route awaits ledger metadata and sidebar counts together; the overview loader awaits overview data, README, and account metadata together. Requests already run concurrently, but optional requests still gate completion.

## Steps

1. Move count loading off the required ledger completion path using the established TanStack/Apollo integration. Preserve honest pending/unavailable states and required ledger access errors.
2. Record applicable evidence against the parent milestone's acceptance requirements.

## Files

- `dashboard/src/routes/ledger.$ledgerOwner.$ledgerName.tsx`
- `dashboard/src/common/components/ledger-layout/`

## Acceptance criteria

- [ ] A two-second count delay no longer delays primary content, no false zero appears, and private/missing ledger behavior remains unchanged.
- [ ] Preserve the parent milestone's accounting, access, and scope constraints.

## Out of scope

Unrelated feature work, new cross-package imports, backend/API-contract changes, and unapproved dependencies. Do not change accounting semantics to improve timing.

