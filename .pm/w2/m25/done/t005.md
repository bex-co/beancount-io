---
id: w2/m25/t005
title: Install and update docs for both channels
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m25/t002, w2/m25/t004]
---

## Objective

Rewrite the install and update sections so a newcomer sees exactly two commands, and the maintainer procedure for a release is written down.

## Context

- `cli/README.md` currently documents `uv tool install git+…#subdirectory=cli` and `--reinstall` for upgrades.
- Root `README.md` package table and `CLAUDE.md` describe the CLI's distribution.

## Steps

1. `cli/README.md`: Install (`brew install bex-co/tap/bea`, `uv tool install beancount-io`, optional `'beancount-io[ask]'`), Update (`bea upgrade`, plus the per-channel commands), Maintainers (tagging `cli-vX.Y.Z`, PyPI pending publisher, `BEA_TAP_PUSH_KEY`).
2. Root `README.md` and `CLAUDE.md`: mention PyPI and the tap; `cli/CLAUDE.md`: release workflow pointer.
3. PRFAQ FAQ 6/16 distribution rows: add a dated note pointing here.

## Files

- `cli/README.md`
- `README.md`
- `CLAUDE.md`
- `cli/CLAUDE.md`
- `cli/docs/PRFAQ.md`

## Acceptance criteria

- [ ] Both install commands run as written on a clean machine and end with a working `bea --version`
- [ ] `python3 scripts/check-agent-guidance.py` passes

## Out of scope

- Windows packaging (WSL only, like bex)
