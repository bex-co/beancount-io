---
id: w2/m25/t006
title: Adoption surface — install channels discoverable and working as written
worker: worker2
status: todo
estimate: 30m
depends_on: [w2/m25/t005]
---

## Objective

Verify the two install channels and the update path are discoverable and work exactly as documented on every surface a person or agent meets.

## Context

- Surfaces: `cli/README.md`, root `README.md`/`CLAUDE.md` tables, PyPI project page (README renders), the tap's `Formula/bea.rb`, GitHub Release notes.

## Steps

1. Fresh macOS: `brew install bex-co/tap/bea`; fresh Linux container: `uv tool install beancount-io`; run `bea check` on a fixture ledger and `bea upgrade --check` on each.
2. Check the PyPI page renders the README and links back to the repo.
3. Confirm `bea --version` under `CI=true` prints no update hint.
4. File drift as inbox notes.

## Files

- `cli/README.md`
- `README.md`
- `CLAUDE.md`

## Acceptance criteria

- [ ] Both channels verified on clean machines; PyPI page correct; no surface still documents the git-URL install as primary

## Out of scope

- —
