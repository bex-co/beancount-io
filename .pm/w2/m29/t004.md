---
id: w2/m29/t004
title: Guide and READMEs open with a five-line quickstart
worker: worker2
status: todo
estimate: 45m
depends_on: [w2/m29/t001]
---

## Objective

The MCP guide is thorough and opens with the credential model; a newcomer wants the five lines that connect Claude Code to their ledger, then the model. Restructure the guide and the two README sections around that, and make every code block runnable as written.

## Context

- `backend-cluster/backend-v2/docs/mcp.md` (~880 lines) is accurate but front-loads permissions and ledger boundaries; the root README's "Coding agent (MCP)" section is a paragraph of counts.
- After m26–m28 several sections (dry run, errors, results, rate limits, tool table) need rewriting anyway.

## Steps

1. Write a "Connect in five minutes" opener: mint a key (or run `bea cloud mcp config`), one `claude mcp add` line, one first prompt to try, one line on what a pinned key means, one link to the full model.
2. Move permissions/boundaries after the quickstart; update the tool table and results/failures sections for m26–m28.
3. Replace the counts in the root README with the quickstart's first two lines and a link.

## Files

- `backend-cluster/backend-v2/docs/mcp.md`
- `backend-cluster/backend-v2/README.md`
- `README.md`

## Acceptance criteria

- [ ] A newcomer following only the first screen of the guide connects Claude Code to a local or hosted ledger.
- [ ] Every code block in the guide has been run against a local stack as written.
- [ ] Tool and resource counts in the READMEs match `tools/list` and `resources/templates/list`.

## Out of scope

- Translating the guide.
