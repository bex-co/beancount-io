# Agent eval harness for the MCP surface

The MCP field audit (2026-09-08) ran three Claude Code `-p` sessions against a local stack (cash balance + food spend + record a purchase; spending report + net worth + stale accounts + "do the books balance"; find my ledgers + last five transactions + payees). Baseline: 11 / 21 / 15 turns, $0.21 / $0.35 / $0.24. Check the prompts and a scorer in as `yarn mcp:agent-eval` (docker-mac, Sonnet, strict MCP config) reporting turns, cost, and correctness, and make it the gate for tool-list changes. The probe scripts from the audit (`backend-v2/tmp/mcp-probe.mjs`, `mcp-batch.mjs`) are the starting point. Also worth one end-to-end bank-import run with a Plaid sandbox item (`sync → unsynced → suggested-categories → submit`). More than an hour — promote after w2/m27.
