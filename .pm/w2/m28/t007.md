---
id: w2/m28/t007
title: Simplify
worker: worker2
status: todo
estimate: 30m
depends_on: [w2/m28/t006]
---

## Objective

Run `/simplify` over the code this milestone changed: reuse, simplification, efficiency, altitude — behavior-preserving only.

## Context

- The MCP layer lives in `backend-cluster/backend-v2/src/features/ai-agent/api/` and `src/features/ai-agent/tools/`; the registry is assembled in `src/server/api/composition-root.ts`.
- `backend-cluster/backend-v2/CLAUDE.md` sets the layer rules: transport adapters delegate, workflows orchestrate, services stay transport-agnostic.

## Steps

1. Diff the milestone's commits and run `/simplify` over the touched files.
2. Apply cleanups that keep behavior identical; leave anything that changes semantics for a follow-up note.
3. Run `yarn lint`, `yarn typecheck`, and `yarn test` from `backend-cluster/backend-v2/`.

## Files

- `backend-cluster/backend-v2/src/features/ai-agent/**`
- `backend-cluster/backend-v2/src/server/api/**`

## Acceptance criteria

- [ ] `/simplify` has been run and its accepted findings applied.
- [ ] `yarn lint`, `yarn typecheck`, and `yarn test` pass.

## Out of scope

- Behavior changes of any kind.
