# Deferred Plaid sandbox journey for MCP agent evals

**Disposition:** the core real-client benchmark is promoted to [w5/m4](../../w5/done/m4/README.md) on 2026-09-12 by the user's `$pm for w5 for them all` request. That milestone covers the three original ledger journeys with Claude Code and Codex. This note remains open only for the larger bank-import follow-up below; the benchmark itself is not duplicate pending work in w2.

## Remaining scope

After w5/m4 ships, consider extending its harness with one explicit Plaid sandbox journey: sync → unsynced transactions → suggested categories → preview/confirm submission. Use synthetic sandbox data, check persisted ledger effects and repeat-run behavior, and define provider setup and cleanup before promotion. This extension is deferred, not a prerequisite for w5/m4 or w5/m5. It is a multi-task idea to size before materialization, not a claim that the bank journey shipped.

## Original audit and proposal

The MCP field audit (2026-09-08) ran three Claude Code `-p` sessions against a local stack (cash balance + food spend + record a purchase; spending report + net worth + stale accounts + "do the books balance"; find my ledgers + last five transactions + payees). Baseline: 11 / 21 / 15 turns, $0.21 / $0.35 / $0.24. Check the prompts and a scorer in as `yarn mcp:agent-eval` (docker-mac, Sonnet, strict MCP config) reporting turns, cost, and correctness, and make it the gate for tool-list changes. The probe scripts from the audit (`backend-v2/tmp/mcp-probe.mjs`, `mcp-batch.mjs`) are the starting point. Also worth one end-to-end bank-import run with a Plaid sandbox item (`sync → unsynced → suggested-categories → submit`). More than an hour — promote after w2/m27.

**Blocked 2026-09-23** — The Plaid journey is still unbuilt: `yarn mcp:agent-eval` (`backend-cluster/backend-v2/scripts/mcp-agent-eval.ts`) covers only the three ledger journeys. It cannot proceed autonomously for two reasons. First, the note is a multi-task idea that needs sizing before promotion, and that scope decision belongs to the user. Second, `.pm/DO_NOT_DO.md` limits real-client MCP evaluation to the hosted endpoint with a dedicated QA account. A Plaid sandbox item can only be linked there if the hosted deployment and the QA account support sandbox institutions, and that is operator state this repository cannot observe or provision. **Unblock:** the user approves sizing and promotion (for example via `/pm-brainstorm`) and the operator confirms how the hosted QA account can link a Plaid sandbox item, or declares the journey out of scope, in which case drop this note. **Who can clear it:** the repository owner/operator.
