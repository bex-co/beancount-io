---
id: w2/m30/t009
title: Test coverage: meaningful tests for shipped behavior
worker: worker2
status: done
estimate: 45m
depends_on: [w2/m30/t007]
---

## Objective

Close any test gaps the implementation tasks left: the crash-survival regression (t001), strict-compat schema gate and failover order (t002), config-driven model selection (t003), mobile error classification (t004), and probe wiring (t005). Tests must assert real behavior and failure modes — no tautologies.

## Context

- Most tasks carry their own test acceptance criteria; this task is the sweep that verifies the set is coherent (e.g. the strict-compat gate actually walks every `Output.object` schema, not just the extraction one) and adds anything missed.

## Steps

1. Re-read each task's acceptance criteria against the landed tests.
2. Add missing cases; delete any tautological ones introduced along the way.

## Files

- Backend and mobile test trees as needed.

## Acceptance criteria

- [ ] Every m30 behavior has at least one test that fails when the behavior regresses.
- [ ] Full backend suite and mobile unit suite green.

## Out of scope

- Coverage-number games.
