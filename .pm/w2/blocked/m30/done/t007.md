---
id: w2/m30/t007
title: Adoption surface: env-var docs + mobile strings + agent-facing checks
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m30/t001, w2/m30/t004, w2/m30/t005, w2/m30/t006]
---

## Objective

Verify the milestone's user- and operator-facing surfaces are discoverable and correct everywhere they appear: the new `LLM_MODEL` / `LLM_FALLBACK_MODEL` vars documented consistently across all deploy targets, the mobile error strings present in the base locale (and sensible in the extended locales), and no instruction file (`CLAUDE.md`s, deploy READMEs) still describing the old behavior.

## Context

- Surfaces: `deploy/docker/.env.example`, `deploy/docker-mac/.env.example`, `deploy/dev-sandbox/.env.example`, `bex.yaml`, `deploy/*/README.md`, `backend-cluster/backend-v2/CLAUDE.md` (if it names LLM env vars), mobile translation files.
- Both Claude Code and Codex read the same instruction files — check the canonical `CLAUDE.md`s, not the symlinks.

## Steps

1. Grep the tree for stale references to removed vars/behavior (`OPENAI_MODEL`, placeholder wording, "required to boot").
2. Confirm every deploy target documents the same AI env-var story.
3. Spot-check mobile strings render in en + one extended locale.

## Files

- deploy configs, instruction files, mobile translations (verification pass; fixes as needed)

## Acceptance criteria

- [ ] One consistent AI configuration story across all deploy targets and instruction files.
- [ ] No stale references to retired vars or behavior.

## Out of scope

- New documentation beyond correcting drift.
