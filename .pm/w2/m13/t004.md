---
id: w2/m13/t004
title: Migrate user identity and API-credential operations
worker: worker2
status: todo
estimate: 55m
depends_on: [w2/m13/t003]
---

## Objective

Route protected user profile/lifecycle and API-key list/create/revoke operations through the centralized PDP exactly once.

## Context

- Existing checks are split across op classes, GraphQL decorators, and `ApiKeyService`.
- API-key minting rules such as no self-replication and scope narrowing remain credential/domain constraints inside the PDP/application workflow.

## Steps

1. Map every affected GraphQL/REST/MCP alias to canonical user actions.
2. Authorize before reads or side effects and pass trusted targets downstream.
3. Remove only superseded policy checks while preserving validation, quota, and secret-handling invariants.

## Files

- `backend-cluster/backend-v2/src/features/auth/**`
- `backend-cluster/backend-v2/src/features/apikeys/**`
- `backend-cluster/backend-v2/src/server/api/op-class.ts`
- `backend-cluster/backend-v2/src/server/api/authorization/**`

## Acceptance criteria

- [ ] Each protected operation reaches one final PDP decision on every exposed surface.
- [ ] API keys cannot mint successors or widen scopes/pins.
- [ ] `user.delete` behavior from m12 remains unchanged.

## Out of scope

- Sign-in/signup/OTP/OIDC ceremonies and non-user business domains.
