---
id: w2/m24/t009
title: Adoption surface — `bea` discoverable and usable on every surface
worker: worker2
status: todo
estimate: 30m
depends_on: [w2/m24/t008]
---

## Objective

Verify the renamed CLI is discoverable and usable everywhere a person or coding agent meets it: package README quickstart, root README and CLAUDE.md package tables, the `beancount-ask` skill, and the CLAUDE.md/AGENTS.md symlink rules.

## Context

- Root `CLAUDE.md` package table describes `cli/` as `beancount-cli`; `skills/CLAUDE.md` lists skills that mention the CLI.
- Agent-facing behavior must hold for both Claude Code and Codex: same instructions, same triggers.

## Steps

1. Follow `cli/README.md` install and quickstart on a clean `uv tool install` from the checkout; fix anything that does not run exactly as written.
2. Check root `README.md`/`CLAUDE.md` tables, `skills/CLAUDE.md`, and run `python3 scripts/check-agent-guidance.py`.
3. Invoke the `beancount-ask` skill flow that shells out to `bea query` under Claude Code and Codex.
4. File any drift as inbox notes rather than silently diverging.

## Files

- `cli/README.md`
- `README.md`
- `CLAUDE.md`
- `skills/CLAUDE.md`

## Acceptance criteria

- [ ] Quickstart runs as written from a clean install
- [ ] Guidance check passes; no surface still says `beancount-cli`

## Out of scope

- New surfaces (PyPI, Homebrew) — m25
