---
id: w2/m24/t008
title: Docs converge: USAGE.md, README, PRFAQ, skills, backend client name
worker: worker2
status: todo
estimate: 45m
depends_on: [w2/m24/t003, w2/m24/t006, w2/m24/t007]
---

## Objective

Make every document and every remaining mention across the monorepo describe the `bea` command that now exists, so the usage guide and the executable agree for the first time.

## Context

- PRFAQ FAQ 3 and 16 record that `docs/USAGE.md` promised `--file`, universal `--json`, and archive behavior the executable lacked; after t004–t006 the guide must describe implemented behavior only.
- Tracked files still naming `beancount-cli`: root `README.md`, `CLAUDE.md`, `ACKNOWLEDGEMENTS.md`; `skills/.claude/skills/beancount-ask/SKILL.md` (three `beancount-cli query`/`report` mentions); `cli/docs/agent-skills.md`; `cli/graphql/schema.graphql` and `cli/src/cli/api/gql_client/input_types.py` (a doc comment; regenerate via `make codegen`, do not hand-edit the client); `dashboard/src/graphql/definitions.ts` (doc comment); `backend-cluster/backend-v2/src/features/auth/**` (example client name in a resolver description, a model comment, and test fixtures); `mobile/.claude/commands/pm-brainstorm.md` (stale file).
- `cli/docs/PRFAQ.md` FAQ 4 says "use the existing executable name"; FAQ 16 inventories `beancount-cli` 0.1.0. Both need a short note that the naming decision superseded them, with the new command tree.
- Backend fixtures are data, not behavior: the backend accepts any client name; only descriptions and examples change.

## Steps

1. Rewrite `cli/docs/USAGE.md` around the new tree: global options, target resolution, exit codes, `list`/`add`, `ask`, `ledger create --clone`, `BEA_*` variables; every example must run as written.
2. Update `cli/README.md` install/quickstart to `uv tool install 'git+https://github.com/bex-co/beancount-io#subdirectory=cli'` yielding `bea` (PyPI and brew arrive in m25).
3. Update the skill, root docs, backend descriptions/comments/fixtures, and dashboard comment; delete the stale `mobile/.claude/commands/pm-brainstorm.md` if nothing references it.
4. Amend PRFAQ FAQ 4 and FAQ 16 with a dated note pointing at this milestone.

## Files

- `cli/docs/USAGE.md`
- `cli/docs/PRFAQ.md`
- `cli/docs/agent-skills.md`
- `cli/README.md`
- `README.md`
- `CLAUDE.md`
- `ACKNOWLEDGEMENTS.md`
- `skills/.claude/skills/beancount-ask/SKILL.md`
- `cli/graphql/schema.graphql` (+ `make codegen`)
- `dashboard/src/graphql/definitions.ts`
- `backend-cluster/backend-v2/src/features/auth/**`
- `mobile/.claude/commands/pm-brainstorm.md`

## Acceptance criteria

- [ ] `git grep -l beancount-cli` lists only files under `.pm/` `done/` trees and PRFAQ's historical inventory
- [ ] Every command in USAGE.md exists in `bea --help` output and every documented flag is accepted
- [ ] `python3 scripts/check-agent-guidance.py` passes; backend-v2 `yarn test` for the auth feature passes

## Out of scope

- Install instructions for PyPI and Homebrew (m25)
