---
id: w2/m17/t001
title: Complete the ledger-content action catalog and alias matrix
worker: worker2
status: todo
estimate: 60m
depends_on: []
---

## Objective

Map the complete ledger data-plane inventory to one executable TypeScript action catalog and a separate exhaustive transport-alias/operational-class matrix before changing enforcement.

## Context

- The current read/write classes hide differences among reports, files, entries, receipts, repositories, and reviews.

## Steps

1. Inventory all affected `VERB_TABLE` aliases, operational rate classes/overrides, client error contracts, and trusted resource shapes.
2. Assign canonical actions with credential ceiling, single/composite relationships, audit class, and any denial/concealment behavior in the PDP catalog.
3. Update `.fga` and its truth table only for semantic relationship changes; add exhaustive coverage so protected aliases fail until mapped.

## Files

- `backend-cluster/backend-v2/src/server/api/op-class.ts`
- `backend-cluster/backend-v2/src/server/api/authorization/**`
- `backend-cluster/backend-v2/authz/**`

## Acceptance criteria

- [ ] Every data-plane verb is mapped exactly once.
- [ ] Transport aliases are not permissions.
- [ ] Unknown or unmapped protected operations fail coverage/deny.
- [ ] Existing read/write/admin rate budgets and overrides are recorded and cannot loosen during migration.

## Out of scope

- Ledger control-plane, Plaid operations, new credential grants, or a second policy file/DSL.
