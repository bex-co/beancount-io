---
id: w2/m3/t007
title: Test coverage — failure-mode evals
worker: worker2
status: todo
estimate: 45m
depends_on: [w2/m3/t005]
---

## Objective

Extend the evals to assert real failure modes: a count/balance delta must be surfaced in the report (never silently absorbed), unmapped categories must land in the review account (never guessed into a plausible one), and conversion must not start before the user confirms the proposed account mapping.

## Acceptance criteria

- [ ] ≥3 failure-mode evals (delta surfacing, no-guess mapping, confirm-gated conversion) passing
- [ ] No trivial/tautological evals
