---
id: w2/m32/t002
title: "backend-v2: managed price status on REST, GraphQL and MCP (parity)"
worker: worker2
status: done
estimate: 60m
depends_on: [w2/m32/t001]
---

## Objective

Expose the ledger's managed price status on all three backend-v2 surfaces through one service method and one canonical read action, following the parity workflow in `backend-cluster/backend-v2/AGENTS.md`.

## Context

- Template: the vocabulary reads (`errors`, `commodities`): `features/ledger/service/ledger-data-service.ts`, `api/rest/v1/vocabulary-handler.ts`, `api/resolvers/ledger-data-resolver.query.ts`, `features/ai-agent/api/mcp-resources.ts`, `server/api/op-class.ts`.
- Regenerate the ledger client with `yarn generate-fava-client` after t001's IDL change.

## Steps

1. Add a service method authorized with `LEDGER_REPORTS_READ`.
2. Register the REST route, GraphQL query and MCP resource, plus the `VERB_TABLE` row.
3. Add contract tests and regenerate `docs/openapi/v1.json`.

## Files

- `backend-cluster/backend-v2/src/features/ledger/**`, `src/features/ai-agent/api/**`, `src/server/api/op-class.ts`, `src/foundation/fava/Api.ts`, `docs/openapi/v1.json`

## Acceptance criteria

- [x] REST, GraphQL and MCP return identical records for the same ledger.
- [x] `surface-parity` passes with no new exemption.
- [x] Denied credentials get the same denial they get for other ledger reads.

## Out of scope

- The refresh action (t003).
