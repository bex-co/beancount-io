---
id: w2/m7/t003
title: Update getFileLanguage/getMimeType and blob download path
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m7/t002]
---

## Objective

Extensionless text files get correct Monaco language and download MIME so editing and downloading behave like other text files.

## Context

- `getFileLanguage:111` maps `ext → language`, falls back to `plaintext`; `LICENSE` currently → `plaintext` via `""` which is acceptable but `Makefile`/`Dockerfile` should map to `makefile`/`dockerfile`.
- `getMimeTypeFromExtension:138` and `downloadFile:194` branch on `isTextFile`.

## Steps

1. Extend `getFileLanguage` to check basename allowlist before extension map: `makefile/gnumakefile → makefile`, `dockerfile/containerfile → dockerfile`, `readme/license/etc → markdown/plaintext` as appropriate.
2. Ensure `getMimeTypeFromExtension` returns `text/plain` for basenames in `TEXT_BASENAMES`.
3. Verify `downloadFile` text branch now triggers for extensionless files (it checks `isTextFile`).

## Files

- `dashboard/src/features/ledger-editor/shared/lib/utils.ts`

## Acceptance criteria

- [ ] `getFileLanguage("Makefile")` → `makefile`, `getFileLanguage("Dockerfile")` → `dockerfile`, `getFileLanguage("LICENSE")` → `plaintext`
- [ ] `downloadFile("LICENSE", base64)` takes text path (Blob `text/plain;charset=utf-8`)

## Out of scope

- View routing (t004)
- Tests (t005)
