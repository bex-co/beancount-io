---
id: w2/m7/t006
title: Adoption surface — ledger file preview discoverable and consistent
worker: worker2
status: done
estimate: 20m
depends_on: [w2/m7/t005]
---

## Objective

Shipped preview fix is consistent on every surface that exposes the ledger file browser.

## Context

- Per `.claude/commands/pm.md` standing closing tasks: adoption surface check when milestone ships user-facing change.

## Steps

1. Verify blob preview, tree view, and direct file route all use same `getFileType` path.
2. Check `dashboard/README.md` and root `README.md` package tables still accurate (no env/config change).
3. Flag any drift in `AGENTS.md` symlinks or skill tables (none expected — dashboard-only change).

## Files

- `dashboard/README.md` (verify only)
- `README.md` (verify only)

## Acceptance criteria

- [ ] Manual navigation `tree → blob` for `LICENSE` works from both entry points
- [ ] No docs drift

## Out of scope

- Code simplification (t007)
