---
id: w2/m7/t005
title: Tests for extensionless/dotfile/unknown cases + manual LICENSE verification
worker: worker2
status: done
estimate: 45m
depends_on: [w2/m7/t002]
---

## Objective

Guard the new classification with unit and component tests and manually verify the reported TinySnow URL.

## Context

- Existing coverage in `dashboard/src/features/ledger-editor/shared/lib/__tests__/utils.test.ts` (`isTextFile`, `getFileType`, `getFileExtension`).
- Need fixtures for extensionless text vs binary unknowns.

## Steps

1. Add tests in `utils.test.ts`: `isTextFile` true for `LICENSE`/`LICENCE`/`Makefile`/`Dockerfile`/`.gitignore`/`CONTRIBUTING` (case-insensitive), false for `archive.zip`/`image.png`; `getFileType("LICENSE")` → `text`, `getFileType("file")` → `unknown` (or `text` if sniff enabled — document).
2. Add `getFileLanguage` tests for `Makefile`/`Dockerfile`.
3. Optionally add `FileContentView` render test for `LICENSE` → `TextFileView`.
4. Run `yarn test` in `dashboard/`; manual check: open blob preview for `LICENSE` fixture and confirm Monaco content, save, download.

## Files

- `dashboard/src/features/ledger-editor/shared/lib/__tests__/utils.test.ts`
- `dashboard/src/features/ledger-editor/file-editor/components/ledger-file-view/__tests__/*` (if added)

## Acceptance criteria

- [ ] New tests pass; existing `utils.test.ts` still green
- [ ] `LICENSE` preview verified not to show unsupported message

## Out of scope

- Adoption surface docs (t006)
