---
id: w2/m7/t004
title: Route extensionless text through TextFileView + fix UnknownFileView empty-ext message
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m7/t002]
---

## Objective

Blob preview actually shows the Monaco editor for extensionless text files and the unsupported message no longer renders as `This file type () is not supported`.

## Context

- `FileContentView:43` computes `getFileType(getFilename(filePath))` and delegates; after t002, extensionless text → `text` → `TextFileView` (`dashboard/src/features/ledger-editor/file-editor/components/ledger-file-view/file-content-view.tsx:77`).
- `UnknownFileView:353` interpolates `getFileExtension(filename)` which is `""` for `LICENSE`, producing `()`.

## Steps

1. Confirm `FileContentView` needs no structural change beyond the `getFileType` fix; keep `image`/`pdf`/`unknown` branches.
2. Harden `UnknownFileView` fallback message: when `getFileExtension` is `""`, display filename or `"unknown"` instead of empty parens (e.g., `This file type is not supported for preview` or `This file type (LICENSE) ...`).
3. Smoke-check `TextFileView` receives `fileContent.content` base64 for `LICENSE` and renders editable plaintext.

## Files

- `dashboard/src/features/ledger-editor/file-editor/components/ledger-file-view/file-content-view.tsx`
- `dashboard/src/features/ledger-editor/file-editor/components/non-text-file-views.tsx`

## Acceptance criteria

- [ ] `/files/blob/main/LICENSE` renders `TextFileView` (breadcrumb + `FileMetadataBar` + Monaco), not `UnknownFileView`
- [ ] True unknown binaries still render `UnknownFileView` without `()` artifact

## Out of scope

- Tests (t005)
