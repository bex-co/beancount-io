---
id: w2/m3/t003
title: Verification — counts/balances vs source, unmapped-category report
worker: worker2
status: done
estimate: 45m
depends_on: [w2/m3/t002]
---

## Objective

Write the Verify + Report phases in SKILL.md: after conversion, reconcile row counts and per-account balances against source totals, run `bean-check`, and emit a migration report listing what mapped, what didn't (unmapped categories parked in a review account), and any count/balance deltas.

## Context

- A migration that silently drops rows is worse than none; the report is the trust artifact.
- Unmapped categories go to an explicit `Expenses:Uncategorized`-style review account, never guessed.

## Steps

1. Define count reconciliation (source rows vs generated postings, transfer-dedup adjustments explained).
2. Define balance reconciliation per account vs source's final balances; deltas surfaced, not hidden.
3. Define the migration-report format and the bean-check gate before declaring success.

## Files

- `skills/.claude/skills/beancount-migrate/SKILL.md`

## Acceptance criteria

- [ ] Report format covers counts, balances, unmapped categories, deltas
- [ ] bean-check failure blocks success reporting

## Out of scope

- Historical price/commodity backfill for investment accounts (flag as follow-up in the report)
