---
id: w2/m3/t001
title: SKILL.md skeleton — triggers/skip, composes beancount-init
worker: worker2
status: done
estimate: 45m
depends_on: []
---

## Objective

Create `skills/.claude/skills/beancount-migrate/SKILL.md` with trigger/skip description and workflow scaffold (Identify source → Scaffold via beancount-init → Map accounts → Convert → Verify → Report), explicitly composing the existing `beancount-init` skill for the scaffold step.

## Context

- Triggers: "migrate from Mint/Monarch/QuickBooks/Copilot", "Mint shut down, get me out", an export CSV in hand; skip: ongoing bank imports (importer workflows), single-statement reconciliation, starting fresh with no history (`beancount-init` alone).
- House conventions: confirm gate before writes, bean-check verify, explicit trigger + skip.

## Steps

1. Draft frontmatter description with trigger + skip cases.
2. Scaffold the workflow; the scaffold step invokes/reuses `beancount-init` conventions rather than duplicating them.
3. State mapping principles: propose the account hierarchy from source categories, confirm with the user before conversion.

## Files

- `skills/.claude/skills/beancount-migrate/SKILL.md`

## Acceptance criteria

- [ ] SKILL.md exists with explicit trigger + skip description
- [ ] Scaffold step composes beancount-init (no duplicated scaffold logic)

## Out of scope

- Per-source format detail (t002)
