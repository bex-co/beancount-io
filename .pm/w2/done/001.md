# Fix stale root CLAUDE.md package table

Root `CLAUDE.md` still lists `skills/` and `cli/` as empty placeholders and omits `dashboard/`, `fava-slim/`, and `docs/` entirely — while root `README.md` is already accurate (dashboard active, cli 0.1.0, fava-slim 0.1.0, skills active). Sync the CLAUDE.md package table to reality and drop/refresh the stale "When `skills/` or `cli/` get real code, add a CLAUDE.md" line (`skills/CLAUDE.md` exists). The instruction file is itself an agent surface — staleness here misleads every agent session. ~25m.
