---
id: w2/m1/t004
title: Fix flow — confirm-gated corrective entries + balance assertion
worker: worker2
status: done
estimate: 45m
depends_on: [w2/m1/t003]
---

## Objective

Write the Propose + Verify phases in SKILL.md: present the reconciliation diff, propose entries for missing transactions (categorized from ledger payee history) and a period-end `balance` assertion, append only on explicit yes, then run `bean-check` and surface any failure.

## Context

- Mirror the `beancount-options` confirmation block: target file, new `open` directives if any, exact entries, yes/no prompt.
- The balance assertion uses the statement's ending balance + period end date from t002.

## Steps

1. Define the diff-report format (per-class sections, counts, proposed action per line).
2. Define the confirmation block and the append rules (date-sorted insert, preserve formatting).
3. Verify phase: `bean-check` on the modified file; on error surface output, propose fix, never silently revert.

## Files

- `skills/.claude/skills/beancount-reconcile/SKILL.md`

## Acceptance criteria

- [ ] No write path exists without an explicit yes
- [ ] Balance assertion emitted for the period end; bean-check failure path documented

## Out of scope

- Editing or deleting existing ledger entries (append-only; suspects are reported, not removed)
