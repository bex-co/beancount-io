---
id: w2/m1/t001
title: SKILL.md skeleton — triggers/skip, discovery, config block
worker: worker2
status: done
estimate: 45m
depends_on: []
---

## Objective

Create `skills/.claude/skills/beancount-reconcile/SKILL.md` with a trigger/skip frontmatter description and the phase scaffold (Discover → Normalize → Match → Propose → Verify), reusing the ledger-discovery and `;; config` comment-block pattern established by `beancount-options`.

## Context

- `skills/.claude/skills/beancount-options/SKILL.md` is the house pattern: discovery phase, persisted config block, explicit confirm gate, bean-check verify.
- `skills/CLAUDE.md` conventions: description must state both when to trigger AND when to skip.

## Steps

1. Draft frontmatter: triggers ("reconcile my checking account", "does my ledger match my statement", "find missing transactions for May"); skip (bulk statement import, categorization-only, P&L questions).
2. Scaffold the five phase sections; Discover reuses the config-block pattern (main file, account mapping, append target).
3. State the hard rule up front: no ledger write without explicit confirmation.

## Files

- `skills/.claude/skills/beancount-reconcile/SKILL.md`

## Acceptance criteria

- [ ] SKILL.md exists with explicit trigger + skip description
- [ ] All five phases scaffolded; confirm gate and bean-check verify stated

## Out of scope

- Matching-rule detail (t003), statement parsing detail (t002)
