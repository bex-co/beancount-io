---
id: w2/m1/t009
title: Closeout
worker: worker2
status: done
estimate: 15m
depends_on: [w2/m1/t008]
---

## Objective

When all other tasks are complete **and the definition of done actually holds** (verified reconciliation on fixtures, confirm-gated writes, bean-check-clean output), close the milestone: mark remaining tasks done, move task files to `w2/m1/done/`, set `**Status:** done` and `— **DONE**` rows in the milestone README, move `w2/m1/` to `w2/done/m1/`, and check the `- [x]` box in `w2/README.md`. Run via `/pm done w2/m1/t009` last — do not run before the DoD is real.

## Acceptance criteria

- [ ] DoD verified before any move; board state consistent in all three status locations
