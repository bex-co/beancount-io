---
id: w2/m1/t005
title: Evals — fixture statement+ledger pairs per mismatch class
worker: worker2
status: done
estimate: 1h
depends_on: [w2/m1/t004]
---

## Objective

Build `evals/evals.json` + fixture files: ledger + statement pairs covering a clean match, missing entries, a duplicate, an amount mismatch, and date drift; iterate via the skill-creator loop until the skill handles all of them.

## Context

- Follow the `beancount-options/evals/` structure (evals.json + `files/` fixtures).
- `bean-check` lives at `/tmp/beancount_venv/bin/bean-check` or via `pip install beancount` (skills/CLAUDE.md).

## Steps

1. Author 5+ fixture pairs (one per mismatch class + one clean).
2. Write eval prompts with expected diff outcomes and expected proposed entries/assertions.
3. Run with and without the skill via skill-creator; iterate SKILL.md/references until passing.

## Files

- `skills/.claude/skills/beancount-reconcile/evals/evals.json`
- `skills/.claude/skills/beancount-reconcile/evals/files/*`

## Acceptance criteria

- [ ] ≥5 eval cases, at least one per mismatch class, all passing
- [ ] Every passing eval's appended output is bean-check-clean

## Out of scope

- Failure-mode evals (t008 Test coverage)
