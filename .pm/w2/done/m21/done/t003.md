---
id: w2/m21/t003
title: "Isolate SSR language state and hydrate matching resources"
worker: worker2
status: done
estimate: 60m
depends_on: [w2/m21/t002]
---

## Objective

Use request-scoped translation initialization on the server and seed the browser with the same selected resources. Audit singleton consumers and preserve server/client language detection semantics.

## Context

- Parent milestone: w2/m21; follow its definition of done and measurement requirements.
- dashboard/src/i18n/init.ts eagerly imports all 15 languages and dashboard/src/routes/__root.tsx imports that initialization.

## Steps

1. Use request-scoped translation initialization on the server and seed the browser with the same selected resources. Audit singleton consumers and preserve server/client language detection semantics.
2. Record applicable evidence against the parent milestone's acceptance requirements.

## Files

- `dashboard/src/i18n/`
- `dashboard/src/routes/__root.tsx`
- `dashboard/src/common/providers/`
- `dashboard/src/router.tsx`

## Acceptance criteria

- [x] Concurrent requests in distinct languages remain isolated; rendered text and hydration agree without language flashes.
- [x] Preserve the parent milestone's accounting, access, and scope constraints.

## Out of scope

Unrelated feature work, new cross-package imports, backend/API-contract changes, and unapproved dependencies. Do not change accounting semantics to improve timing.


## Verification

All 15 languages passed concurrent SSR and individual production-browser hydration checks, with no hydration errors or extra locale downloads. Route metadata, the document shell, components, and native bridge now receive the router-owned instance.
