---
id: w2/m21/t004
title: "Handle language transitions and add a bundle budget command"
worker: worker2
status: done
estimate: 45m
depends_on: [w2/m21/t003]
---

## Objective

Keep language switching usable while chunks load or fail, including rapid changes. Add a repeatable package-local bundle budget command using the baseline and measured resulting asset structure.

## Context

- Parent milestone: w2/m21; follow its definition of done and measurement requirements.
- dashboard/src/i18n/init.ts eagerly imports all 15 languages and dashboard/src/routes/__root.tsx imports that initialization.

## Steps

1. Keep language switching usable while chunks load or fail, including rapid changes. Add a repeatable package-local bundle budget command using the baseline and measured resulting asset structure.
2. Record applicable evidence against the parent milestone's acceptance requirements.

## Files

- `dashboard/src/i18n/`
- `dashboard/src/common/providers/language-sync/`
- `dashboard/scripts/`
- `dashboard/package.json`
- `dashboard/README.md`

## Acceptance criteria

- [x] All 15 language transitions work; a failed chunk can recover; the documented budget command detects unused-language inclusion without relying on unstable hashed filenames.
- [x] Preserve the parent milestone's accounting, access, and scope constraints.

## Out of scope

Unrelated feature work, new cross-package imports, backend/API-contract changes, and unapproved dependencies. Do not change accounting semantics to improve timing.


## Verification

Real locale tests cover all 15 transitions and superseding pending selections. Aborting the French chunk in a production browser retained English and its preference; Try Again reloaded French SSR resources and persisted French. The manifest-based budget is independent of hashed filenames.
