---
id: w2/m21/t002
title: "Split locale imports and load selected resources"
worker: worker2
status: done
estimate: 60m
depends_on: [w2/m21/t001]
---

## Objective

Replace eager locale aggregation in the runtime path with explicit dynamic imports for the selected language and English fallback, using existing i18next resource APIs. Preserve the canonical language list and translation shape checks.

## Context

- Parent milestone: w2/m21; follow its definition of done and measurement requirements.
- dashboard/src/i18n/init.ts eagerly imports all 15 languages and dashboard/src/routes/__root.tsx imports that initialization.

## Steps

1. Replace eager locale aggregation in the runtime path with explicit dynamic imports for the selected language and English fallback, using existing i18next resource APIs. Preserve the canonical language list and translation shape checks.
2. Record applicable evidence against the parent milestone's acceptance requirements.

## Files

- `dashboard/src/i18n/`

## Acceptance criteria

- [x] Production assets and network requests demonstrate that unselected locale resources are excluded from initial loading.
- [x] Preserve the parent milestone's accounting, access, and scope constraints.

## Out of scope

Unrelated feature work, new cross-package imports, backend/API-contract changes, and unapproved dependencies. Do not change accounting semantics to improve timing.


## Verification

Initial JavaScript fell from 951,559 to 333,903 gzip bytes. The manifest has 14 separate non-English dynamic locale entries; yarn perf:locales passes and rejects the baseline build.
