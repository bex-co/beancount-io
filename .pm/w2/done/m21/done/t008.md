---
id: w2/m21/t008
title: Simplify
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m21/t007]
---

## Objective

Run `/simplify` across the Dashboard OAuth migration and remove duplicated credential/lifetime logic without changing the reviewed protocol.

## Context

- A staged cookie migration can leave parallel helpers, redirect validators, client classifiers, and misleading session terminology.

## Steps

1. Review client catalog, lifetime selection, first-party predicate, PKCE transaction, callback, cookie, Apollo, logout, and compatibility code as one flow.
2. Reuse shared safe-redirect, issuer/resource, cookie, and OAuth validation primitives where their trust boundaries match.
3. Remove dead session issuance/refresh paths only when the dated compatibility condition and rollback contract allow it, then rerun focused checks.

## Files

- Files changed by `w2/m21`

## Acceptance criteria

- [ ] One obvious Dashboard OAuth completion and cookie path remains.
- [ ] Client classification and TTL policy each have one executable source of truth.
- [ ] No token reaches browser storage, logs, analytics, URLs, or duplicated state.
- [ ] Behavior and focused checks remain green after simplification.

## Out of scope

- Removing compatibility early or redesigning unrelated auth code.
