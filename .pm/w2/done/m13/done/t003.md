---
id: w2/m13/t003
title: Add user-self and API-key relationship evaluators
worker: worker2
status: done
estimate: 40m
depends_on: [w2/m13/t002]
---

## Objective

Resolve user ownership and API-key ownership from trusted identity and backend data behind the PDP evaluator interface.

## Context

- `user#owner` is exact-self in `authz/model.fga`.
- API-key ownership already lives in the API-key model and must not become a tuple.

## Steps

1. Implement exact-self user checks using stable internal IDs.
2. Resolve API-key ownership without revealing foreign key existence.
3. Add neutral fixtures shared by model and runtime conformance tests where practical.

## Files

- `backend-cluster/backend-v2/src/server/api/authorization/**`
- `backend-cluster/backend-v2/src/features/apikeys/**`
- `backend-cluster/backend-v2/authz/**`

## Acceptance criteria

- [ ] Cross-user and foreign-key access deny without an enumeration oracle.
- [ ] No relationship fact is persisted or cached across requests.
- [ ] Model and evaluator agree on covered user relationships.

## Out of scope

- Credential verification, token storage, or ledger relationships.
