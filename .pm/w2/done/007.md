# Stale `mobile/.claude/commands/` shadows the canonical skills

Found while closing w2/m24 (the `bea` rename): `mobile/.claude/commands/` still holds
four pre-monorepo command files — `commit.md`, `mermaid.md`, `pm.md`, `pm-brainstorm.md`.
m24 deleted only `pm-brainstorm.md`, because it was the one naming the old CLI.

The rest are drift against the root `CLAUDE.md` rule that every `/name` workflow lives at
`skills/.claude/skills/<name>/SKILL.md` and that a `.claude/commands/` directory makes a
command invisible to Codex. `mobile/.claude/commands/pm.md` also describes a board layout
(`.pm/GOAL.md`, a private sibling checkout) that no longer matches this repo.

Check each against its canonical skill, then delete the directory. Sub-hour.

---

**Resolved 2026-09-12** — deleted `mobile/.claude/commands/` (`commit.md`, `mermaid.md`, `pm.md`)
after checking each against its canonical skill. `mermaid.md` was a stale copy of
`.agents/skills/mermaid/SKILL.md`; the two fixes worth keeping were ported to the canonical
skill (the repo-map pointer named a `docs/ADR002-architecture.md` that does not exist in this
monorepo, and the scratch-file step now names a real gitignored directory). `commit.md` has no
canonical counterpart — `/ship` owns committing. `pm.md` was superseded by
`.agents/skills/pm/SKILL.md`. Dangling pointers to the deleted file were repointed in
`mobile/CLAUDE.md` and `mobile/.pm/README.md`; archived task files under `done/` still mention
the old path and were left as history. `python3 scripts/check-agent-guidance.py` and
`python3 skills/scripts/ci-check.py` are green.

Left open, out of this note's scope: `mobile/.pm/` is a second, historical board with its own
`GOAL.md` and an open note (`w1/033.md`). Whether it is merged into the root `.pm/` or retired
is a separate decision.
