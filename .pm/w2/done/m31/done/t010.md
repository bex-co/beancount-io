---
id: w2/m31/t010
title: Test coverage: meaningful tests for shipped behavior
worker: worker2
status: done
estimate: 45m
depends_on: [w2/m31/t008]
---

## Objective

Cover the acceptance table from PRFAQ002 FAQ 18 for the ledger layer with controlled clocks and responses.

## Context

- Standing closing task. Rows in scope: one-line setup, within-day refresh and caching, manual precedence, missing or stale data, bounded input and privacy, portability (read-only error).

## Steps

1. Add loader-level tests through `loadCachedFileMapForRepo` with a stubbed Gitea client, an in-memory cache, a fake clock, and a fake fetch.
2. Assert the SHA cache value never contains a virtual path.
3. Assert freshness flips from `recent` to `stale` as the fake clock advances with an unchanged ETag.

## Files

- `backend-cluster/ledger/src/foundation/clients/__tests__/load-cached-ledger-file-map-managed-prices.test.ts`

## Acceptance criteria

- [x] Each in-scope FAQ 18 row maps to at least one named test.
- [x] `yarn test` passes.

## Out of scope

- Live-network tests.

## Evidence

- PRFAQ002 FAQ 18 rows in scope map to named tests: one-line setup → `overlays the feed after the SHA cache and never stores it in that cache` and `adds the feed under the engine key and reports a recent source`; within-day refresh and caching → `refreshes prices on an unchanged commit without any repository read`, `serves from cache inside the window`, `sends a conditional GET after the window and treats 304 as a refresh`, `swaps to a new revision on 200 and deletes the superseded blob`, `turns stale as the clock advances even though the bytes are unchanged`; manual precedence → `lets a same-date ledger price or its reciprocal win, line-preserving`, `materializes one feed and a placeholder when the same URL is included from two directories`; missing or stale data → the seven-case `keeps the last revision through %s and backs off one minute` table, `reports unavailable with the cause when no revision ever validated`, `keeps serving the last revision when the refresh fails`, `loads the books with no virtual file when the feed is unavailable`; bounded input and privacy → the policy rejection table, `stops reading past the byte cap`, redirect/timeout/network cases, and every non-price directive rejection in the validator table; portability (read-only error) → `source-slice writes against a managed price feed` (update, delete, batch delete) and `refuses to project onto a managed price feed's virtual file`; tier counting → `directive limit with managed price feeds` and `counts the customer's directives and none of the feed's`.
- The SHA cache value is asserted to contain no virtual path, and coalescing is asserted with ten concurrent loads producing one fetch.
