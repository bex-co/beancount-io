---
id: w2/m31/t008
title: Adoption surface: env docs, README, deploy examples, package guidance
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m31/t007]
---

## Objective

Make the feature discoverable and configurable everywhere the ledger service is described.

## Context

- Standing closing task. The milestone adds `MANAGED_PRICE_*` environment variables and a user-visible include behavior.

## Steps

1. Document the variables and defaults in `backend-cluster/ledger/.env.example` and `backend-cluster/ledger/README.md`.
2. Add the `managed-prices/` directory to the layout section of `backend-cluster/ledger/CLAUDE.md` (the `AGENTS.md` symlink follows automatically) and run `python3 scripts/check-agent-guidance.py`.
3. Mention the optional override in the deploy targets where ledger env is listed (`deploy/docker-mac`, `deploy/dev-sandbox`, `deploy/docker`, `bex.yaml`) only as a commented example; defaults need no change.
4. Point `docs/prfaqs/PRFAQ002-include-live-price.md` readers to ADR 015 for the hosted loader status.

## Files

- `backend-cluster/ledger/.env.example`, `backend-cluster/ledger/README.md`, `backend-cluster/ledger/CLAUDE.md`
- deploy target files listed above

## Acceptance criteria

- [x] `python3 scripts/check-agent-guidance.py` passes.
- [x] Every ledger env listing mentions `MANAGED_PRICE_ORIGINS` and its default.

## Out of scope

- Dashboard or CLI documentation for features not yet built.

## Evidence

- `backend-cluster/ledger/.env.example` and `README.md` document `MANAGED_PRICE_ORIGINS` (default `https://beancount.io`) and the six tuning variables; `backend-cluster/ledger/CLAUDE.md` lists `src/foundation/managed-prices/`.
- Commented `MANAGED_PRICE_ORIGINS` examples in `deploy/docker-mac`, `deploy/dev-sandbox`, `deploy/docker`, `deploy/docker-single-psql` compose files and `bex.yaml`; `docker compose config --quiet` passes for all four and `bex blueprints validate` reports valid.
- `docs/prfaqs/PRFAQ002-include-live-price.md` hosted-loader rows point at ADR 015.
- `python3 scripts/check-agent-guidance.py` passes (17 scopes).
