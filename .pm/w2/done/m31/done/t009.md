---
id: w2/m31/t009
title: Simplify: run /simplify over the changed code
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m31/t008]
---

## Objective

Review the milestone's diff for reuse, simplification, and efficiency without changing behavior.

## Context

- Standing closing task.

## Steps

1. Run `/simplify` over `backend-cluster/ledger/src/foundation/managed-prices/` and the touched loader, cache, and service files.
2. Apply the behavior-preserving cleanups; rerun `yarn lint && yarn typecheck && yarn test`.

## Files

- the milestone's changed files

## Acceptance criteria

- [x] No behavior change; the package checks pass.

## Out of scope

- New features.

## Evidence

- `/simplify` ran four review angles (reuse, simplification, efficiency, altitude) over the diff on 2026-09-15. Applied: one read-only guard (`assertNotManagedPricePath`) shared by `locateSlice` and `checkProjectedErrors`; a `committedOnly` loader option for the directive limit and source-file listing so counting never resolves feeds; the committed `LoadedLedger` type stays free of managed fields (`LoadedLedgerWithManagedPrices` on the cached loader); slimmer head (`revision`, `nextRefreshAt`, `lastError`) and status records; single error mapping in the fetcher; reuse of `isIsoDate` and `sliceSha256`; overlay reuses the loader's include closure, counts include lines incrementally, and scans ledger prices only when a feed resolved; the manual-refresh stub, rejection enum, `fetched` flag, and debug log were removed.
- Skipped on purpose: widening the write guard to every non-source file (behavior beyond ADR 015), subtracting a managed count instead of matching directives (inexact under `time` filters), and memoizing the include scan per file-map identity (complexity outweighs a regex pass that is already cheaper than the per-request snapshot hash).
- `yarn typecheck`, `yarn lint`, `yarn test` (75 suites, 1016 tests) pass after the changes.
