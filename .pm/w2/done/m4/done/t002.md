---
id: w2/m4/t002
title: BQL recipe reference — reuse bean-query and cli/
worker: worker2
status: done
estimate: 1.5h
depends_on: [w2/m4/t001]
---

## Objective

Write `references/bql-recipes.md`: tested BQL for the standard question set — top expenses (period/category), monthly trends, net worth over time, burn rate, recurring-charge detection + price creep, and anomaly spotting — each with the question it answers, the query, and how to read the result.

## Context

- The `cli/` package (0.1.0) already runs BQL and reports and ships a local-ledger chat agent — audit its query surface first and reuse/point to it rather than duplicating; `bean-query` is the fallback everywhere.
- Recurring-charge detection: same payee + similar amount at regular intervals; price creep: amount trend per recurring payee.

## Steps

1. Audit `cli/`'s existing BQL/report commands; note in the reference where the skill should call `cli` vs raw `bean-query`.
2. Author and test each recipe against a fixture ledger.
3. Document result-reading pitfalls (inverted signs on Income, cost vs market value).

## Files

- `skills/.claude/skills/beancount-ask/references/bql-recipes.md`

## Acceptance criteria

- [ ] All six recipe families present and verified against a fixture ledger
- [ ] cli/ reuse documented; no duplicated query machinery

## Out of scope

- New cli/ features (flag gaps as follow-up notes instead)
