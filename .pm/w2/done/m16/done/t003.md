---
id: w2/m16/t003
title: Migrate parsing, suggestion, and temporary assets
worker: worker2
status: done
estimate: 55m
depends_on: [w2/m16/t002]
---

## Objective

Route parsing/category/account suggestion and temporary asset operations through protected application-service/workflow boundaries and one final PDP call before data access.

## Context

- These paths currently combine op classes, ledger pins, and object-key ownership checks at different seams.

## Steps

1. Make protected public service methods accept `Identity`; authorize in a workflow only when the operation genuinely coordinates multiple services.
2. Keep operation IDs in AsyncLocalStorage and preserve content validation, object-key safety, and owner-bound mutation predicates as domain/data-layer invariants.
3. Remove only superseded authorization branches after parity, keeping not-found concealment and actionable denial behavior in the action catalog.

## Files

- `backend-cluster/backend-v2/src/features/llm/**`
- `backend-cluster/backend-v2/src/features/s3/**`
- receipt/file parsing call sites

## Acceptance criteria

- [x] Denied calls touch no S3 object, parser, or ledger client.
- [x] A ledger-pinned credential cannot cross ledgers through an asset or parser argument.
- [x] Existing successful responses remain contract-compatible.
- [x] Missing/foreign assets do not create an existence oracle, and source outage is not disguised as not found or forbidden.

## Out of scope

- General ledger entry mutation migration or authorization-only wrappers.
