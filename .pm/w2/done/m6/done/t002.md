---
id: w2/m6/t002
title: Normalize references — CSV/OFX/QIF, column mapping, data sources
worker: worker2
status: done
estimate: 1h
depends_on: [w2/m6/t001]
---

## Objective

Write `references/formats.md`: detecting CSV vs OFX vs QIF, the column-mapping flow (confirm once per new source, persist to the config block, zero questions on repeat imports), sign conventions (signed amounts vs debit/credit split columns), date-format handling (DMY vs MDY), and pending/posted markers. Plus `references/getting-data.md`: how users obtain exports — manual download per major bank/broker, and SimpleFIN as the read-only feed option.

## Context

- Sign and date ambiguity are the classic silent-corruption points: the rule is ask-once-then-persist, never guess.
- OFX carries FITID (feeds t003's dedup); QIF and CSV usually don't.
- No scraping of any kind (anti-goal); manual export and SimpleFIN only.

## Steps

1. Document format detection and per-format parsing guidance with examples.
2. Define the column-mapping confirmation dialog and its persisted config representation.
3. Write the getting-data reference (manual export paths, SimpleFIN setup pointer).

## Files

- `skills/.claude/skills/beancount-import/references/formats.md`
- `skills/.claude/skills/beancount-import/references/getting-data.md`

## Acceptance criteria

- [ ] CSV (both sign conventions), OFX, QIF covered with examples
- [ ] Mapping is confirmed once and persisted; ambiguity always routes to a question

## Out of scope

- PDF statement text (reconcile skill's domain); writing reusable beangulp importers (m2)
