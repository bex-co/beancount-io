---
id: w2/m6/t005
title: Confirm + Write + Verify — review table, flags, bean-check gate
worker: worker2
status: done
estimate: 45m
depends_on: [w2/m6/t003, w2/m6/t004]
---

## Objective

Write the Confirm, Write, and Verify stages in SKILL.md: the review table (every row with target account + confidence, duplicates-skipped count, suspected duplicates awaiting decision, new `open` directives, explicit target file path), per-row editability before a single explicit yes; then date-sorted append with `!`/`*` flags and `import-id` metadata; then `bean-check` with failures surfaced verbatim.

## Context

- The target file must already exist — never auto-create: a file nothing `include`s silently swallows entries.
- Rows the source marks as pending get flag `!`; posted rows get `*`.
- Mirror the `beancount-options` confirmation block structure (target file, opens, exact entries, yes/no).

## Steps

1. Define the review-table format and the partial-edit loop (user adjusts rows → re-present → confirm).
2. Define write rules: existing-file check, date-sorted insertion, formatting preservation, metadata on every entry.
3. Define Verify: `bean-check` on the modified file; on error surface exact output, propose a fix, never claim success or silently revert.

## Files

- `skills/.claude/skills/beancount-import/SKILL.md`

## Acceptance criteria

- [ ] No write path without explicit confirmation; target file must pre-exist
- [ ] Flags and import-id metadata on every written entry; bean-check failure path documented

## Out of scope

- Balance assertions (reconcile skill's domain — suggest running `beancount-reconcile` afterward instead)
