---
id: w2/m6/t006
title: Evals — fixture exports incl. overlapping re-import
worker: worker2
status: done
estimate: 1h
depends_on: [w2/m6/t005]
---

## Objective

Build `evals/evals.json` + synthetic fixtures: a standard signed-amount CSV, a debit/credit split-column CSV, an OFX with FITIDs, a file overlapping a prior import (dedup must skip the overlap), and a file containing pending rows — each with an expected resulting ledger.

## Context

- Follow the `beancount-options/evals/` structure; all data synthetic (public repo, gitleaks-gated).
- The overlapping-file fixture is the acceptance test for the `import-id` convention.

## Steps

1. Author the five fixture exports + companion starting ledgers with opened accounts and payee history.
2. Write eval prompts with expected staged output, review-table facts, and final ledger.
3. Run via the skill-creator loop with and without the skill; iterate until passing.

## Files

- `skills/.claude/skills/beancount-import/evals/evals.json`
- `skills/.claude/skills/beancount-import/evals/files/*`

## Acceptance criteria

- [ ] All five fixtures passing; outputs bean-check-clean with import-id metadata present
- [ ] Overlap fixture yields exactly the non-overlapping entries

## Out of scope

- Failure-mode and idempotency evals (t009 Test coverage)
