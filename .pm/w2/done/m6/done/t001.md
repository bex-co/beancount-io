---
id: w2/m6/t001
title: SKILL.md skeleton — seven-stage pipeline, triggers/skip, config block
worker: worker2
status: done
estimate: 45m
depends_on: []
---

## Objective

Create `skills/.claude/skills/beancount-import/SKILL.md` with a trigger/skip frontmatter description, the seven-stage pipeline scaffold (Discover → Normalize → Stage → Dedup → Suggest → Confirm → Write+Verify), the persisted `;; beancount-import config` block, and routing to sibling skills.

## Context

- House pattern: `beancount-options` and `beancount-reconcile` (discovery, config comment block, confirm gate, bean-check verify).
- Config block persists per-source state: column mapping, sign convention, default counter-account, target file, import count (drives the importer-author handoff suggestion).
- Routing: full-history SaaS export → `beancount-migrate`; checking ledger against a statement → `beancount-reconcile`; "make this source a reusable importer" → `beancount-importer-author`; single trades → `beancount-options`.

## Steps

1. Draft frontmatter: triggers ("import this CSV", "record my bank export", bank/card export in hand); skip cases (the four sibling routings above).
2. Scaffold the seven stages; state the two hard rules up front: staged entries never touch the ledger before explicit confirmation, and accounts are never invented — suggestions come only from accounts already opened in the ledger.
3. Define the config block schema and the "3+ imports of the same source → suggest beancount-importer-author" nudge.

## Files

- `skills/.claude/skills/beancount-import/SKILL.md`

## Acceptance criteria

- [ ] SKILL.md exists with explicit trigger + skip description and all seven stages scaffolded
- [ ] Config block schema documented; sibling-skill routing stated

## Out of scope

- Format details (t002), dedup mechanics (t003), categorization rules (t004)
