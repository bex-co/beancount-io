---
id: w2/m6/t007
title: Adoption surface — beancount-import discoverable on every surface
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m6/t006]
---

## Objective

Verify the new skill is discoverable and usable on every surface it exposes, for both Claude Code and Codex.

## Steps

1. Add the `beancount-import` row to `skills/CLAUDE.md`'s Skills table with an accurate purpose line.
2. Check root `README.md` / `CLAUDE.md` skills mentions still hold; the README's skills capability line should now cover the import→reconcile loop.
3. Verify `AGENTS.md` symlinks resolve to sibling `CLAUDE.md` and `.agents/skills` → `skills/.claude/skills/`.
4. Confirm the skill triggers on the same phrases under Claude Code and Codex (same SKILL.md, no divergent copies).

## Files

- `skills/CLAUDE.md`, root `README.md`

## Acceptance criteria

- [ ] Skills table lists beancount-import; symlinks resolve; triggers fire in both agents
