---
id: w2/m11/t003
title: Spot-check rendered heads, OG images, canonical and hreflang on the 3 pages
worker: worker2
status: done
estimate: 20m
depends_on: [w2/m11/t002]
---

## Objective

Verify SSR + client heads actually emit the new snippets, plus the m10 canonical/hreflang contract still holds on the 3 acquisition pages.

## Context

- `PageSEO` at `dashboard/src/common/components/seo/page-seo.tsx:42` self-canonicalizes via `getSelfCanonicalUrl` and adds 13 `hreflang` + `x-default` alts when `noIndex` is false (which it is for `/auth/login`, `/auth/sign-up`, `/auth/forgot-password`). Titles map to OG image `https://opengraph-image.blockeden.xyz/api/og-beancount-io?title=` at line 83 — new titles automatically update OG unfurls.
- Redirect sources `/login`, `/sign-up`, `/forgot-password` 302 to the `/auth/*` canonicals (routes `src/routes/login.tsx`, `sign-up.tsx`, `forgot-password.tsx`) and carry no head — only the targets are inspected.
- Hreflang must stay consistent with canonical: each `?lang=xx` variant self-canonicalizes so alternates remain valid (m10 design decision).

## Steps

1. `yarn build` or `yarn dev` locally and curl/visually check SSR output for `/auth/login`, `/auth/sign-up`, `/auth/forgot-password`: one `<title>`, one `<meta name="description">`, one `rel=canonical` (`https://beancount.io/auth/login` + `?lang=xx` iff supported), 14 `rel=alternate` links, OG tags reflect new title. Check `?lang=de` preserves and `?lang=zz` + `?next=/ledger/...` are stripped from canonical.
2. Confirm `/login` 302 lands on `/auth/login` canonical (DevTools network) and that no duplicate head remains from route `head()` vs `PageSEO`.

## Files

- `dashboard/src/routes/auth.login.index.tsx`
- `dashboard/src/routes/auth.sign-up.tsx`
- `dashboard/src/routes/auth.forgot-password.tsx`
- `dashboard/src/common/components/seo/page-seo.tsx` (read-only spot-check)

## Acceptance criteria

- [ ] SSR head for each of the 3 pages contains new title/description, one self-canonical, and 14 hreflang alts; `?lang=zz` canonicalizes to clean path.
- [ ] `/login` → `/auth/login` redirect observed; OG image URL encodes new title.

## Out of scope

- Sitemap/robots (dashboard has no `public/sitemap.xml` yet) or CMS `/docs` linking — file separately if needed.
