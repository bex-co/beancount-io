---
id: w2/m11/t005
title: Simplify
worker: worker2
status: done
estimate: 20m
depends_on: [w2/m11/t004]
---

## Objective

Run `/simplify` over the code this milestone changed (`dashboard/src/i18n/locales/seo/*.ts`, `scripts/prepare-translations.ts` output) — reuse, simplification, efficiency; behavior-preserving only.

## Context

- 14 locale files now share the same 3-key pattern; check for duplication that can be reduced via shared helpers or `prepare-translations` conventions without changing shipped strings.

## Steps

1. `/simplify` over the milestone diff.
2. Apply behavior-preserving cleanups; `yarn lint && yarn test` in `dashboard/`.

## Files

- Files changed by t001–t002

## Acceptance criteria

- [ ] No behavior change; lint and tests pass.

## Out of scope

- New features or policy changes.
