---
id: w2/m11/t001
title: Rewrite EN acquisition SEO strings for login/sign-up/forgot-password
worker: worker2
status: done
estimate: 30m
depends_on: []
---

## Objective

Replace the 3 generic EN acquisition snippets with brand + keyword + differentiator copy that matches `beancount login` / `beancount sign up` intent and uses the full 50–60 char title budget.

## Context

- Current EN at `dashboard/src/i18n/locales/seo/en.ts:238` ships `Sign In` (7 chars), `Create Account` (14), `Forgot Password` (15) + 69–94 char boilerplate descriptions with zero `plain-text` tokens. Diagnosis C shows this predicts `<3%` CTR at position 4–20 via `dashboard/scripts/search-console-report-core.ts:32`.
- Target EN per diagnosis: `Sign In to Beancount — Free Plain-Text Accounting` (52), `Create Free Beancount Account — Git-Backed Accounting` (56), `Reset Beancount Password — Secure Access` (44); descriptions 128/134/105 chars with `open-source`, `Git-backed`, `Fava` differentiators. Keep `seo.signUpOtp/welcome` unchanged (noIndex per `dashboard/src/common/lib/seo/indexability.ts:18`).
- `seo.home.title` stays `Beancount Dashboard - Plain-Text Accounting` (43 chars) — not touched.

## Steps

1. Edit `dashboard/src/i18n/locales/seo/en.ts` — update `seo.login.title/description`, `seo.signUp.title/description`, `seo.forgotPassword.title/description` to the target copy; verify lengths 44–56 chars (title) and 100–155 chars (description) and that each title contains `Beancount`.
2. Run `yarn prepare-translations` check (translation key extraction still sees all `seo.*` keys); confirm no i18n key rename.

## Files

- `dashboard/src/i18n/locales/seo/en.ts`

## Acceptance criteria

- [ ] EN titles are 52/56/44 chars and each contains `Beancount`; descriptions are 128/134/105 ±5 chars and each contains `plain-text` or `Beancount` + one differentiator.
- [ ] No other `seo.*` keys changed; `yarn prepare-translations` (if run) reports no missing keys.

## Out of scope

- Other locales (t002), head/canonical wiring (already done in m10), CMS linking.
