---
id: w2/m11/t004
title: Adoption surface
worker: worker2
status: done
estimate: 20m
depends_on: [w2/m11/t003]
---

## Objective

Verify the shipped snippet change is discoverable and consistent on every surface it touches: `dashboard/CLAUDE.md` Search Console docs, `dashboard/README.md` quickstart, root `README.md`/`CLAUDE.md` package tables, and the `AGENTS.md`/`.agents/skills` symlinks.

## Context

- `dashboard/CLAUDE.md:134` documents `yarn search-console-report --markdown --days 28` and dashboard-owned path scoping (`/ledger`, `/login`, `/sign-up`, `/auth`, `/settings`, `/lgasset`, `/oauth`; `/forum`, `/api`, `/.well-known`, CMS excluded). The snippet rewrite does not change scoping, but the next GSC read should rank real CTR, not variant dilution (m10).
- Root `CLAUDE.md` compatibility rules: every scoped `AGENTS.md` must resolve to its sibling `CLAUDE.md` and `.agents/skills → skills/.claude/skills`.

## Steps

1. Re-read `dashboard/CLAUDE.md` Growth planning section against the new EN copy and `m10` report aggregation; update only if the expected output description is stale.
2. Confirm `dashboard/README.md` install/quickstart steps still work as written (`yarn install --immutable`, `yarn dev`).
3. Verify `AGENTS.md` symlinks and `.agents/skills` target still resolve per root `CLAUDE.md` rules; flag drift as follow-up, don't silently diverge.

## Files

- `dashboard/CLAUDE.md` (via canonical path if edited)
- `dashboard/README.md` (check only)

## Acceptance criteria

- [ ] Docs describe the aggregated report output accurately if touched.
- [ ] Symlink check passes; any drift filed as follow-up.

## Out of scope

- New docs surfaces.
