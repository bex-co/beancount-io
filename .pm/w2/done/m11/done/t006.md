---
id: w2/m11/t006
title: Test coverage
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m11/t004]
---

## Objective

Add meaningful tests for the shipped strings and head emission on the 3 acquisition pages.

## Context

- Existing suites: `dashboard/src/common/components/seo/__tests__/page-seo.test.tsx`, `dashboard/src/common/lib/seo/__tests__/seo-helpers.test.ts`, locale flatten tests at `dashboard/scripts/__tests__/`.

## Steps

1. Add/update `page-seo` or `seo-helpers` tests asserting the new `seo.login/signUp/forgotPassword` titles contain `Beancount` and meet length bounds; keep locale parity check via `prepare-translations` rather than snapshotting every translation.
2. If needed, add a head-render test for `/auth/login` that exactly one `<title>` and one canonical are emitted when indexable.

## Files

- Test files adjacent to `dashboard/src/common/components/seo/` and/or `dashboard/scripts/__tests__/`

## Acceptance criteria

- [ ] `yarn test` passes in `dashboard/`; new tests fail if acquisition titles regress to generic `Sign In`.

## Out of scope

- E2E crawler simulation.
