---
id: w2/m11/t007
title: Closeout
worker: worker2
status: done
estimate: 10m
depends_on: [w2/m11/t006]
---

## Objective

Close the milestone once all other tasks are done and the definition of done actually holds (new snippets shipped across 14 locales, heads spot-checked, lint/tests green).

## Context

- Closing procedure is the `/pm done` subcommand step 4: move tasks to `w2/m11/done/`, mark rows `— **DONE**`, set `**Status:** done`, move `w2/m11/` to `w2/done/m11/`, check the box in `w2/README.md`.

## Steps

1. Verify the definition of done in `w2/m11/README.md` against shipped state.
2. Run `/pm done w2/m11/t007` last to trigger the milestone move.

## Files

- `.pm/w2/m11/**`, `.pm/w2/README.md`

## Acceptance criteria

- [ ] Milestone exists only at `w2/done/m11/` with all tasks under `done/` and the workstream checkbox checked.

## Out of scope

- Any new implementation work (file as inbox notes instead).
