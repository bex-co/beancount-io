# Re-check amazon commit CTR after m9 bakes

Around 2026-09-04 (~2 weeks after m9 shipped the commit-detail head + canonical), re-run `yarn search-console-report --markdown --days 14` from `dashboard/` and check whether the `"amzn-20260630.htm"` commit page (`/ledger/open_ledger/amazon/commit/c121e11e...`, was position ~4.3 at 0% CTR) has started converting impressions to clicks. If still 0% with meaningful impressions, propose a follow-up (snippet wording, structured data) via `/pm-brainstorm`.

## Completed 2026-09-06

Ran the requested command successfully from `dashboard/`. It requested
2026-08-23 through 2026-09-06 with `dataState: final`; the targeted daily
response ended on 2026-09-04. The report's opportunity table omitted this
commit, so a direct, read-only Search Analytics check removed the report's
per-query impression threshold before drawing a conclusion.

The clean commit URL has **0 clicks / 25 impressions (0% CTR), average
position 4.76**. Across five URL variants, it has **0 clicks / 43 impressions**.
The original quoted search term has only **0 clicks / 2 impressions** across
two variants: insufficient evidence about that query alone. The clean page
nevertheless exceeds the report's 20-impression screening threshold. This is
enough to investigate, not proof that m9's snippet wording failed; the original
26-impression baseline covered 28 days, so these are not matched windows.

Reproduction: use the report's existing service-account authentication and
fixed `https://beancount.io/` property. POST to its `searchAnalytics/query`
endpoint with the dates above, `type: web`, `dataState: final`,
`dimensions: ["page"]`, and a `page contains` filter for
`/ledger/open_ledger/amazon/commit/c121e11e8e941633c2b8e252d0cd642ae120885a`.
For the original term, use `dimensions: ["query", "page"]` and additionally
filter `query contains amzn-20260630.htm`. For data coverage, use
`dimensions: ["date"]` with the page filter. Keep credentials and raw responses
local; only this public example's aggregate findings are recorded here.

URL Inspection reported **NEUTRAL / Page with redirect**, last crawled
2026-08-26, with both Google's and the declared canonical pointing to
`https://beancount.io/auth/login`. An anonymous live fetch on 2026-09-06 instead
returned HTTP 200, the title `Commit c121e11 - amazon`, the description
`Changes in commit c121e11 for amazon. Review modified files and diffs.`, and
the expected commit canonical. Indexed crawl state and current HTML differ;
the inspection does not prove a redirect still exists today.
Direct requests with redirect following disabled also returned 200 without a
Location header for both the default client and a mobile Googlebot user agent.
The latter is only a user-agent simulation, not a verified Google crawl.

## Follow-up proposed via pm-brainstorm

Propose a **50-minute inbox note in w2: Verify Amazon commit crawl state before
another snippet experiment**. This is an A3 discovery task for prospective
users finding public ledger examples. w2 has finished m23 and has capacity;
this extends m9's measurement rather than repeating its head/canonical work.

1. Compare current anonymous desktop/mobile responses and Search Console live
   inspection with the August 26 indexed redirect; identify whether Google
   needs a recrawl or whether a reproducible routing issue remains (20m).
2. Assess the rendered commit subject/content against the hash-only snippet.
   Draft a factual, content-specific title/description only after confirming
   the intended page is served. Evaluate structured-data eligibility against
   visible content; do not assume schema produces a rich result (20m, after 1).
3. Record the disposition and the next comparable page-level 14-day check,
   including clicks, impressions, CTR, position, canonical, and last crawl
   date. Keep the sparse original-query cohort separate (10m, after 2).

Done means a documented crawl-state diagnosis, a justified snippet/schema
decision, and a reproducible follow-up measurement. Expected adoption signal:
searchers reach the public example and page-level impressions begin producing
clicks; no CTR uplift is promised from this small sample. No product change or
new milestone is warranted by this recheck alone. The proposal remains text;
materialize with `/pm add w2 Verify Amazon commit crawl state before another snippet experiment`.

Google's [snippet guidance](https://developers.google.com/search/docs/appearance/snippet)
favors descriptions grounded in page content. Its
[structured-data policies](https://developers.google.com/search/docs/appearance/structured-data/sd-policies)
require relevant visible content and do not guarantee rich results.
