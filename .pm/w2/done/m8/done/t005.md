---
id: w2/m8/t005
title: Smoke-check representative allowlisted and denylisted routes
worker: worker2
status: done
estimate: 20m
depends_on: [w2/m8/t003, w2/m8/t004]
---

## Objective

Verify the shipped HTML matches the policy on representative public URLs before closing implementation work.

## Context

- Check useful public reads and acquisition pages separately from transactional/internal routes.
- Local `yarn build` / SSR preview is acceptable if production deploy is unavailable; prefer checking the same meta shape the SSR head emits.

## Steps

1. Render public overview/journal/account/commit and login/sign-up — assert no robots noindex.
2. Render reset/welcome/query/errors/settings/editor/consent/receipt — assert robots noindex.
3. Verify base Ask is indexable, prefilled Ask is noindex, and both Ask route forms canonicalize to `/agent`.

## Files

- None required beyond verification of prior task outputs

## Acceptance criteria

- [x] Representative social/acquisition routes are indexable.
- [x] Representative transactional/private/write/parameterized routes are noindex.
- [x] Relevant SEO tests and dashboard lint pass.

## Out of scope

- Waiting for a new 28-day GSC window to prove impression drops (follow-up signal, not DoD).
