---
id: w2/m8/t008
title: Test coverage
worker: worker2
status: done
estimate: 45m
depends_on: [w2/m8/t006]
---

## Objective

Add meaningful tests for robots noindex emission and hreflang gating — assert real head output / props, including failure modes (flag off vs on).

## Context

- Existing tests: `dashboard/src/common/lib/seo/__tests__/`, `dashboard/src/common/components/seo/__tests__/`.
- Do not game coverage with tautological snapshots; assert robots meta presence/absence and hreflang count.

## Steps

1. Extend seo-helpers / LedgerSEO / Hreflang tests for noindex on and off.
2. Cover at least one deep-route wiring path if route tests are the established pattern; otherwise component-level is enough if t003 only passes a prop.
3. Run `yarn test` from `dashboard/`.

## Files

- `dashboard/src/common/lib/seo/__tests__/`
- `dashboard/src/common/components/seo/__tests__/`

## Acceptance criteria

- [ ] Tests fail if noindex is dropped from the helper API or hreflang renders on noindex pages.
- [ ] `yarn test` green in `dashboard/`.

## Out of scope

- Snapshotting full production HTML for every ledger route.
