---
id: w2/m14/t007
title: Closeout
worker: worker2
status: done
estimate: 15m
depends_on: [w2/m14/t004, w2/m14/t005]
---

## Objective

Archive m14 only after billing authorization and regression evidence satisfy the definition of done.

## Context

- `/pm done` must move completed work out of the open tree.

## Steps

1. Recheck scope, tests, docs, migration contract, and no-runtime constraint.
2. Apply required development migrations, exercise the current billing client against a deployed backend, and verify persisted allowed/denied/error audit evidence plus no-side-effect denial.
3. Confirm no secret, lockfile, or unrelated package change, then complete and archive the milestone.

## Files

- `.pm/w2/m14/`
- `.pm/w2/README.md`

## Acceptance criteria

- [x] Every DoD item has evidence.
- [x] Runtime evidence includes applied migrations and persisted audit rows, not only a successful response.
- [x] m14 exists only under `done/` after completion.
- [x] The w2 checkbox is checked.

## Evidence

- Development migrations were applied through `1787527070724`; the pending migration count was zero.
- The current source was loaded into the healthy local development backend. An anonymous `GQL Query.allTierQuotas` returned all five static tiers and created no authorization-audit row, confirming the documented public exception.
- A development backend smoke exercised `GQL Mutation.createSubscriptionSession`: a browser session was allowed, an API key was denied before billing work, and the `paid_customers` row count stayed unchanged.
- Persisted audit rows were observed for the allowed session decision and denied API-key decision. An injected relationship-source failure produced a persisted `error` row for `user.billing.checkout.create`, returned service unavailable, and performed zero Stripe/model calls.
- The temporary development user, API key, and JWT were removed after the smoke; the non-secret audit evidence remains. Production was not touched.
- Focused billing/authz/API-key suites, drift guards, the full backend test suite, typecheck, changed-file formatting, lint, build, OpenFGA model validation/tests, guidance validation, and a redacted changed-file secret scan passed.

## Out of scope

- Production deployment or m15 implementation.
