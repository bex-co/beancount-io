---
id: w2/m9/t005
title: Simplify
worker: worker2
status: done
estimate: 20m
depends_on: [w2/m9/t004]
---

## Objective

Run `/simplify` over every file touched by this milestone and apply safe, behavior-preserving reuse / simplification / efficiency cleanups; do not change observable head/canonical behavior.

## Context

- Scope is limited to `dashboard/src/routes/ledger.$ledgerOwner.$ledgerName.commit.$commitSha.tsx`, `dashboard/src/features/git/commits/pages/commit-detail-page.tsx`, `dashboard/src/i18n/locales/seo/*.ts`, `dashboard/scripts/search-console-report-core.ts`.

## Steps

1. Run `/simplify` checks over touched code (deduplicate `encodeURIComponent` via existing `getLedgerFileCanonicalUrl` helper pattern if it cleanly applies; otherwise keep explicit inline encoding for commit URL).
2. Apply only safe simplifications; keep i18n interpolation keys `{shortSha}` and `{ledgerName}` intact.

## Files

- (touched files above)

## Acceptance criteria

- [ ] No functional head/canonical change introduced by simplification; tests still pass.

## Out of scope

- Cross-feature refactors.
