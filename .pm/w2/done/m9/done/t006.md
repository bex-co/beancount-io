---
id: w2/m9/t006
title: Test coverage
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m9/t004]
---

## Objective

Add meaningful tests for the commit head/canonical behavior and the locale-prefixed CMS classification, asserting real SEO output and failure modes without tautological snapshots.

## Context

- Existing coverage: `dashboard/src/common/lib/seo/__tests__/seo-helpers.test.ts`, `dashboard/src/common/components/seo/__tests__/ledger-seo.test.tsx`, `dashboard/scripts/__tests__/search-console-report.test.ts`.
- Must test: commit title includes short hash + ledger name, description is distinct from list, canonical is stable and stripped of query params, and `zh/blog` → `cms`.

## Steps

1. Extend `search-console-report.test.ts` for locale-prefixed CMS (already in t003) — ensure it lands.
2. Add/extend coverage for commit `head` (unit for `createHeadMeta(getSEOMetadata(...))` with `shortSha` param + integration smoke that `commit/$sha` route head does not return `noindex`).
3. Run `yarn test` in `dashboard/` and confirm green.

## Files

- `dashboard/scripts/__tests__/search-console-report.test.ts`
- `dashboard/src/common/lib/seo/__tests__/*` or `dashboard/src/routes/__tests__/*` as appropriate

## Acceptance criteria

- [ ] New tests fail before the fix and pass after; no trivial/snapshot-only tests.

## Out of scope

- E2E crawler tests.
