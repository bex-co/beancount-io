---
id: w2/m9/t007
title: Closeout
worker: worker2
status: done
estimate: 10m
depends_on: [w2/m9/t006]
---

## Objective

Close the milestone when its definition of done is actually met — every task done, head verified in SSR output, classification fixed, tests and lint green — and move the milestone to done per `/pm` conventions.

## Context

- Must set every remaining task `status: done`, move each `tNNN.md` to `w2/m9/done/`, mark every row `— **DONE**` and set `**Status:** done` in `.pm/w2/m9/README.md`, move whole `w2/m9/` to `w2/done/m9/`, and check `- [x]` in `.pm/w2/README.md`.

## Steps

1. Confirm t001–t006 are done and DoD holds (hash in title, distinct description, canonical, hreflang still present, `zh/blog` → `cms`, `yarn lint && yarn test` pass).
2. Execute the closeout moves and verify the exit condition: no open tasks remain at `w2/m9/tNNN.md`, milestone exists only at `w2/done/m9/`, workstream checkbox checked.

## Files

- `.pm/w2/m9/README.md`
- `.pm/w2/README.md`
- `.pm/w2/m9/t*.md`

## Acceptance criteria

- [ ] Exit condition verified before reporting success.

## Out of scope

- Landing unrelated follow-ups.
