---
id: w2/m9/t002
title: Create ledgerCommit SEO translation entries across 14 locales
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m9/t001]
---

## Objective

Provide `seo.ledgerCommit.title` / `seo.ledgerCommit.description` with `{ledgerName}` and `{shortSha}` interpolation in all supported SEO locales, so the new commit head resolves in every language without falling back to missing-key rendering.

## Context

- Locales at `dashboard/src/i18n/locales/seo/*.ts` (14 files: bg, ca, de, en, es, fa, fr, ja, ko, nl, pt, ru, sk, uk, zh — zh maps to `zh.ts` plus `ja/ko` presence). English source at `dashboard/src/i18n/locales/seo/en.ts:335-343` defines `seo.ledgerCommits`.
- Pattern: `title: "{shortSha} - {ledgerName}"` or "Commit {shortSha} - {ledgerName}" and description: "Changes in commit {shortSha} for {ledgerName}. Review modified ledger files and diffs."
- Keep messages short (<60 chars title, <155 chars description) for SERP truncation; do not include secrets or user data.

## Steps

1. Add `seo.ledgerCommit.title` and `seo.ledgerCommit.description` to `dashboard/src/i18n/locales/seo/en.ts` (singular commit).
2. Replicate to the other 13 locale files with localized phrasing (keep `{ledgerName}` and `{shortSha}` placeholders).
3. Verify `dashboard/src/test/translations.test.ts` passes (it already checks `seo.${key}.title/description` usage).

## Files

- `dashboard/src/i18n/locales/seo/en.ts`
- `dashboard/src/i18n/locales/seo/{bg,ca,de,es,fa,fr,ja,ko,nl,pt,ru,sk,uk,zh}.ts`

## Acceptance criteria

- [ ] No missing-key warnings for `seo.ledgerCommit.*` in any locale; `yarn test:translations` (or `yarn test`) passes.
- [ ] Titles stay within SERP length and include short hash.

## Out of scope

- CMS or forum translations.
