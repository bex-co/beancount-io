---
id: w2/m9/t004
title: Adoption surface
worker: worker2
status: done
estimate: 20m
depends_on: [w2/m9/t002, w2/m9/t003]
---

## Objective

Verify the shipped commit-detail SEO change is discoverable on every surface that users and crawlers touch, and that docs/board reflect the new indexability edge.

## Context

- Surfaces: dashboard SSR head, dashboard README package table, root README/CLAUDE package tables, `skills/CLAUDE.md` if skills touched (not this milestone), and `AGENTS.md` / `.agents/skills` symlink integrity.
- Follow root `CLAUDE.md` Codex/Claude compatibility rules: `AGENTS.md` must stay symlinked to `CLAUDE.md`.
- Public ledger commit is crawler-facing; confirm `indexable` still true per `dashboard/docs/LEDGER_SEO.md` and `dashboard/src/common/lib/seo/indexability.ts`.

## Steps

1. Confirm milestone scope had a user-facing surface (it does — public ledger commit) so this task applies.
2. Check shipped head appears in SSR HTML for the commit route and canonical is stable.
3. Verify no drift in root `README.md`, `CLAUDE.md` package tables, or `dashboard/CLAUDE.md` Growth/Search Console notes; update only if stale.
4. Verify `AGENTS.md` symlinks still resolve to sibling `CLAUDE.md` and `.agents/skills` → `skills/.claude/skills`.

## Files

- `README.md`
- `CLAUDE.md`
- `dashboard/CLAUDE.md`
- `dashboard/docs/LEDGER_SEO.md` (if the commit edge needs clarifying)

## Acceptance criteria

- [ ] Commit detail head visible to an unauthenticated crawler; canonical correct.
- [ ] No drift flagged without follow-up; symlinks intact.

## Out of scope

- Marketing copy beyond technical accuracy.
