---
id: w2/m9/t003
title: Fix locale-prefixed CMS classification in search-console-report-core
worker: worker2
status: done
estimate: 20m
depends_on: []
---

## Objective

Teach `classifyDashboardPath` that `/{lang}/blog`, `/{lang}/docs`, `/{lang}/bean-labs`, `/{lang}/compare`, `/{lang}/open-ledger`, `/{lang}/tools`, `/{lang}/research-logs` are `cms` (same as unprefixed), so inbox note `w2/004.md` no longer inflates `unknown`.

## Context

- File `dashboard/scripts/search-console-report-core.ts` classifies via `cmsPrefixes`, `dashboardPrefixes`, etc. Today only unprefixed CMS paths are recognized; `/zh/blog` falls through to `unknown`.
- Test file `dashboard/scripts/__tests__/search-console-report.test.ts` already asserts CMS vs dashboard vs api vs forum.
- This is the sub-hour inbox note `w2/004.md` — bundling it into m9 to unblock clean GSC reads before next iteration.

## Steps

1. Extend `classifyDashboardPath` to strip or match an optional `/{lang}` prefix (two-letter `bg|ca|de|en|es|fa|fr|ja|ko|nl|pt|ru|sk|uk|zh|it` — reuse `SUPPORTED_LANGUAGES` list from `dashboard/src/i18n`) before checking `cmsPrefixes`.
2. Add coverage in `dashboard/scripts/__tests__/search-console-report.test.ts` for `https://beancount.io/zh/blog` and `https://beancount.io/ja/docs` → `cms`.
3. Confirm `yarn search-console-report` no longer buckets those rows as `unknown`/`dashboard`.

## Files

- `dashboard/scripts/search-console-report-core.ts`
- `dashboard/scripts/__tests__/search-console-report.test.ts`

## Acceptance criteria

- [ ] `classifyDashboardPath("https://beancount.io/zh/blog") === "cms"` and `"/ja/docs"` === `cms`; `"/ledger/…"` still `dashboard`.
- [ ] Existing tests pass.

## Out of scope

- Touching production sitemap host (CMS owns `beancount-cms-v2.onbex.co` → `beancount.io` mismatch — out of scope for dashboard).
