---
id: w2/m17/t005
title: Remove competing gates and verify budgets/audit/failures
worker: worker2
status: done
estimate: 50m
depends_on: [w2/m17/t004]
---

## Objective

Remove or demote duplicate ledger-content gates so the PDP is the sole final authority, then prove rate budgets, errors, audit, and source-failure behavior did not regress.

## Context

- Existing checks span op classes, GraphQL auth/pin middleware, services, workflows, and route guards.

## Steps

1. Enumerate and remove superseded content-domain assertions without removing authentication, routing, validation, or atomic defense-in-depth predicates.
2. Check each action's catalog requirement against its operational class/override, denial message/concealment, and expected audit class.
3. Inject source failure and duplicate/concurrent calls; verify service-unavailable error logging/audit, exact operation-ID isolation, per-call audit, and no side effect.

## Files

- `src/server/graphql/**`
- `src/server/api/**`
- affected ledger/AI/MCP helpers

## Acceptance criteria

- [ ] No data-plane operation can bypass or double-intersect the PDP.
- [ ] Public/authentication behavior outside the domain is unchanged.
- [ ] Every authorization call produces the applicable audit result; no decision memo suppresses duplicate write/audit events.
- [ ] Existing rate budgets do not loosen and source outage is never recorded as a security denial.

## Out of scope

- Removing gates still needed by unmigrated domains.
