---
id: w2/m15/t005
title: Test coverage
worker: worker2
status: done
estimate: 50m
depends_on: [w2/m15/t003]
---

## Objective

Cover public/authenticated social reads, self-only mutations, visibility boundaries, and no-side-effect denial.

## Context

- Gitea errors must not be mistaken for authorization decisions.

## Steps

1. Add catalog/evaluator/service truth tables for protected actions plus coverage tests for explicit public exclusions.
2. Assert denial reaches no Gitea write; assert source outage logs/audits an error and surfaces service unavailable without becoming a public/nullable result.
3. Cover rate budgets, per-call and concurrent operation-ID audit, direct-call fallback, and cross-surface client errors; run focused/full backend and model checks.

## Files

- social, workflow, and authorization test files

## Acceptance criteria

- [x] Cross-user/self and public/private cases are covered.
- [x] Relationship denial and evaluator failure produce their distinct declared outcomes.
- [x] Duplicate protected operations produce duplicate decisions/audits without shared memoization.
- [x] Required checks pass.

## Out of scope

- Live Gitea end-to-end tests unless already supported.

## Completion

- Focused social/authz, surface-parity, resolver, workflow, rate, and scope tests pass. ESLint, Knip, changed-file formatting, diff checks, OpenFGA validation (10/10 tests and 92/92 checks), guidance validation, and changed-source gitleaks scanning pass.
- After rebasing onto the current `main`, the full backend suite passes 2,977 tests across 222 suites; `yarn typecheck`, `yarn build`, and `yarn lint` pass in the package itself.
- The earlier failures came from six obsolete nested `@ai-sdk/provider-utils` directories in the local generated `node_modules` tree. Removing those stale copies restored Yarn's already-declared root resolution, exactly matching a clean `yarn install --no-lockfile`; no source, dependency declaration, or lockfile change was needed.
