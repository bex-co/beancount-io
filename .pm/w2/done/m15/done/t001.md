---
id: w2/m15/t001
title: Catalog social actions, exclusions, and trusted resources
worker: worker2
status: done
estimate: 45m
depends_on: []
---

## Objective

Partition the social inventory into canonical protected actions and explicit public exclusions, then define each protected action's trusted resource, credential, relationship, audit, denial, and operational-risk behavior.

## Context

- Social data lives primarily in Gitea and must not be copied to an authz store.

## Steps

1. Inventory feed/profile/follow/star aliases and classify each exactly once as protected or deliberately public.
2. Put protected requirements in the one TypeScript catalog; keep transport aliases and operational classes in `op-class.ts` without using those classes as credential policy.
3. Specify stable-ID resource derivation and composite checks such as self preference plus readable ledger; update `.fga` only for real durable relationship changes.

## Files

- `backend-cluster/backend-v2/src/server/api/authorization/**`
- `backend-cluster/backend-v2/authz/**`
- `backend-cluster/backend-v2/src/server/api/op-class.ts`

## Acceptance criteria

- [x] Every social verb has one protected action/resource shape or a written public-exclusion reason.
- [x] Usernames and caller arguments never substitute for stable authenticated IDs.
- [x] Public behavior is explicit rather than inferred from missing identity.
- [x] Credential reachability, audit class, denial behavior, and rate budget are reviewable without a second policy file.

## Out of scope

- Changing social product semantics or modeling follow/star/request facts as credential or contextual tuples.

## Completion

- Cataloged feed, follow/unfollow, and star/status/unstar as canonical actions in the shared PDP catalog; authenticated-user resources derive from `Identity.userId`, while ledger resources use the canonical validated `owner/name` application ID.
- Recorded profile, followers, following, and starred-repository discovery as named public exclusions in `op-class.ts` with their unchanged GraphQL-only surface.
- Kept credential policy and denial/audit behavior in the action catalog and preserved the legacy operational budgets through explicit rate overrides.
