---
id: w2/m10/t007
title: Closeout
worker: worker2
status: done
estimate: 10m
depends_on: [w2/m10/t006]
---

## Objective

Close the milestone once all other tasks are done **and the definition of done actually holds** (one canonical per indexable page, lang-only preservation, no double canonicals, aggregated report rows, lint/tests green).

## Context

- Closing procedure is the `/pm done` subcommand's step 4: move tasks to `w2/m10/done/`, mark rows `— **DONE**`, set `**Status:** done`, move `w2/m10/` to `w2/done/m10/`, check the box in `w2/README.md`.

## Steps

1. Verify the definition of done in `w2/m10/README.md` against the shipped state.
2. Run `/pm done w2/m10/t007` last to trigger the milestone move.

## Files

- `.pm/w2/m10/**`, `.pm/w2/README.md`

## Acceptance criteria

- [ ] Milestone exists only at `w2/done/m10/` with all tasks under `done/` and the workstream checkbox checked.

## Out of scope

- Any new implementation work (file as inbox notes instead).
