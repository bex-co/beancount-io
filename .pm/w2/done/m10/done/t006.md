---
id: w2/m10/t006
title: Test coverage
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m10/t004]
---

## Objective

Add meaningful tests for the shipped behavior: canonical emission on indexable vs noindex pages, lang-param preservation vs stripping, single-canonical reconciliation on commit/blob/ask routes, and report aggregation math. Tests must assert real behavior and failure modes — no snapshot-everything or tautological coverage.

## Context

- Existing suites: `dashboard/src/common/lib/seo/__tests__/indexability.test.ts`, `dashboard/src/common/components/seo/__tests__/` (page-seo, ledger-seo), `dashboard/scripts/__tests__/search-console-report.test.ts`.

## Steps

1. Component tests: `LedgerSEO`/`PageSEO` render exactly one canonical when indexable, none when `noIndex`, and honor the `canonicalUrl` override.
2. Helper tests beyond t001's basics: encoded account paths (e.g. `Expenses%3AFinancial%3AFees`), hash stripping.
3. Report tests: variant aggregation with impression-weighted position and CTR recomputation.

## Files

- Test files adjacent to the changed modules

## Acceptance criteria

- [ ] `yarn test` passes in `dashboard/`; new tests fail if canonical emission or aggregation regresses.

## Out of scope

- E2E/crawler simulation.
