---
id: w2/m10/t004
title: Adoption surface
worker: worker2
status: done
estimate: 20m
depends_on: [w2/m10/t002, w2/m10/t003]
---

## Objective

Verify the shipped canonical behavior is discoverable and consistent on every surface it touches: `dashboard/CLAUDE.md`'s Search Console section still describes the report accurately (now aggregated by canonical page), `dashboard/README.md` quickstart steps still work as written, and the `AGENTS.md` symlinks still resolve per the root compatibility rules. Flag drift as follow-up work rather than silently diverging.

## Context

- `dashboard/CLAUDE.md` documents `yarn search-console-report` usage and dashboard-owned path scoping.
- Root `CLAUDE.md` compatibility rules: every scoped `AGENTS.md` must resolve to its sibling `CLAUDE.md`.

## Steps

1. Re-read `dashboard/CLAUDE.md`'s "Growth planning and Search Console" section against the new report output; update if the aggregation changes what a reader should expect.
2. Confirm no user-facing README steps changed behavior.
3. Verify `AGENTS.md` symlinks and `.agents/skills` still resolve.

## Files

- `dashboard/CLAUDE.md` (via its canonical path if edited)
- `dashboard/README.md` (check only)

## Acceptance criteria

- [ ] Docs describe the aggregated report output accurately.
- [ ] Symlink check passes; any drift is filed as follow-up, not silently fixed into divergence.

## Out of scope

- New documentation surfaces.
