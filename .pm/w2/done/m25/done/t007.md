---
id: w2/m25/t007
title: Simplify
worker: worker2
status: done
estimate: 20m
depends_on: [w2/m25/t006]
---

## Objective

Run `/simplify` over the code this milestone changed (`update.py`, `commands/upgrade.py`, the release workflow, `render-formula.sh`) for reuse, simplification, and efficiency without behavior change.

## Context

- Behavior-preserving only; `make check-all` and `docker compose config`-style validations stay green.

## Steps

1. Run `/simplify` scoped to the milestone's diff.
2. Apply accepted findings; rerun `make check-all`.

## Files

- `cli/src/cli/update.py`
- `cli/src/cli/commands/upgrade.py`
- `cli/scripts/render-formula.sh`
- `.github/workflows/release-cli.yml`

## Acceptance criteria

- [ ] Findings addressed or explicitly declined with a reason
- [ ] `make check-all` green

## Out of scope

- New behavior
