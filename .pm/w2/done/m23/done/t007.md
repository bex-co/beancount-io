---
id: w2/m23/t007
title: "Test coverage"
worker: worker2
status: done
estimate: 40m
depends_on: [w2/m23/t005, w2/m23/t006]
---

## Objective

Verify real behavior and failure modes from this milestone's definition of done.

## Context

- Parent milestone: w2/m23; follow its definition of done and measurement requirements.
- The ledger route awaits ledger metadata and sidebar counts together; the overview loader awaits overview data, README, and account metadata together. Requests already run concurrently, but optional requests still gate completion.

## Steps

1. Add meaningful adjacent tests for the changed behavior, including failures and lifecycle boundaries. Use browser/production-build measurements for performance claims rather than mocked timing assertions. From dashboard run yarn format:check, yarn lint, yarn test, and yarn build.
2. Record applicable evidence against the parent milestone's acceptance requirements.

## Files

- `dashboard/src/`
- `dashboard/scripts/`

## Acceptance criteria

- [x] Relevant behavior and failure modes are covered, production evidence supports the definition of done, and the native dashboard checks pass.
- [x] Preserve the parent milestone's accounting, access, and scope constraints.

## Out of scope

Unrelated feature work, new cross-package imports, backend/API-contract changes, and unapproved dependencies. Do not change accounting semantics to improve timing.


## Verification

Added tests for the prefetch helper (browser start, swallowed failure, SSR skip), the overview loader (resolves while README/metadata are pending, starts them before the overview, swallows overview failure, starts nothing optional in SSR), the layout route loader (awaits only `GetLedger`, rethrows access errors), `useAccountMeta` (pending, declared roles, failure fallback, ledger switch), the Sankey pending state and declared-role precedence, and the usage indicator's no-false-zero behaviour. Performance claims come from production-build browser traces, not mocked timing. From `dashboard/`: `yarn format:check`, `yarn lint`, `yarn test` (278 files, all passing), and `yarn build` pass.
