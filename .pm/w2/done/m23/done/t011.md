---
id: w2/m23/t011
title: Closeout
worker: worker2
status: done
estimate: 25m
depends_on: [w2/m23/t009, w2/m23/t010]
---

## Objective

Close the continuation only when deployed behavior proves that new Dashboard authentication is OAuth-only and the sole legacy exception is read-only acceptance of already-issued valid tokens.

## Context

- m21 closeout proved OAuth use after authentication but did not prove absence of intermediate legacy issuance.
- This closeout must inspect issuance side effects and cookie/token identity across the complete login transaction.
- Production deployment remains separately authorized.

## Steps

1. Verify every definition-of-done item and record package, protocol, policy, guidance, and secret-scan evidence without public credentials or user data.
2. On a deployed development stack, exercise password, OTP signup, and magic link from a clean browser; prove zero legacy-session writes/returns and subsequent OAuth-authenticated SSR/CSR/GraphQL/REST/agent behavior.
3. Exercise an already-issued valid token without logout or renewal, plus expiry/revocation, OAuth logout, Mobile refresh, MCP rejection, and failure cases; then archive via `/pm done` only if all evidence passes.

## Files

- `.pm/w2/m23/`
- `.pm/w2/README.md`

## Acceptance criteria

- [x] Deployed evidence proves clean-browser login issues only the exact Dashboard OAuth credential and never creates an intermediate legacy session.
- [x] A valid existing token remains usable without reauthentication and receives no extended legacy lifetime.
- [x] No Dashboard legacy issuer, bootstrap, or runtime fallback remains; every retained session-related component has a non-issuer justification.
- [x] All tasks are archived under `done/`, the milestone is moved to `.pm/w2/done/m23/`, and the w2 checkbox is checked.

## Out of scope

- Production deployment or early invalidation of already-issued valid tokens.
