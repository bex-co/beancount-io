---
id: w2/m23/t004
title: "Verify hydration deduplication and freshness"
worker: worker2
status: done
estimate: 45m
depends_on: [w2/m23/t003]
---

## Objective

Inspect Apollo hydration and request ownership after deferred loading. Correct any duplicate equivalent requests or stale states introduced across repeat navigation, ledger changes, and successful writes.

## Context

- Parent milestone: w2/m23; follow its definition of done and measurement requirements.
- The ledger route awaits ledger metadata and sidebar counts together; the overview loader awaits overview data, README, and account metadata together. Requests already run concurrently, but optional requests still gate completion.

## Steps

1. Inspect Apollo hydration and request ownership after deferred loading. Correct any duplicate equivalent requests or stale states introduced across repeat navigation, ledger changes, and successful writes.
2. Record applicable evidence against the parent milestone's acceptance requirements.

## Files

- `dashboard/src/common/apollo/`
- `dashboard/src/router.tsx`
- `dashboard/src/features/reports/overview/`
- `dashboard/src/common/components/ledger-layout/`

## Acceptance criteria

- [x] Traces show no hydration-induced duplicate equivalent requests; refreshed content reflects writes and never leaks another ledger's cached result.
- [x] Preserve the parent milestone's accounting, access, and scope constraints.

## Out of scope

Unrelated feature work, new cross-package imports, backend/API-contract changes, and unapproved dependencies. Do not change accounting semantics to improve timing.


## Verification

Fixture request logs for every configuration show each operation requested at most once per navigation: SSR-prefetched `GetLedger`/`GetLedgerOverview` are never re-requested by the browser, the candidate's dehydrated cache excludes the optional operations, and the browser prefetch of README/metadata is deduplicated with the panels' `useQuery`. Writes keep clearing `ROOT_QUERY` so active optional queries refetch; a ledger switch returns `useAccountMeta` to pending, the README card to its skeleton, and the usage gauge to empty (pinned by tests). No console or page errors in any run.
