---
id: w2/m32/t006
title: Adoption surface
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m32/t004, w2/m32/t005]
---

## Objective

Check the new status read, refresh action and dashboard UI are discoverable where users and agents look.

## Steps

1. Update `docs/openapi/v1.json`, the backend-v2 API docs, the MCP resource/tool descriptions, and ADR 015's follow-up list (dated amendment).
2. Update any skill or README that documents managed price includes. Check Claude Code and Codex see the same MCP instructions.

## Acceptance criteria

- [x] Every surface above names the new read and refresh, or drift is recorded as follow-up work.

## Result

- `backend-cluster/backend-v2/docs/mcp.md` lists `refreshManagedPrices` in the tool table and `managed-prices` among the vocabulary resources (10 → 11).
- ADR 015 has a dated amendment recording the status surface, the refresh semantics, the `ledger.entries.write` gate and the dashboard changes. PRFAQ002's hosted-loading evidence row points at it.
- `docs/openapi/v1.json` was regenerated with t002/t003.
- Claude Code and Codex read one MCP server, so tool and resource descriptions are identical for both.
- No skill documents the hosted surface. `beancount-ask` already points local users at `bea price status`.
