---
id: w2/m32/t007
title: Simplify
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m32/t006]
---

## Objective

Run `/simplify` over the code this milestone changed (behavior-preserving).

## Acceptance criteria

- [x] `/simplify` applied; package checks still pass.

## Result

Ran `/simplify` (reuse, simplification, efficiency and altitude reviewers) over the five m32 code commits.

**Fixed:**
- **Ledger refresh does one load.** It now makes one file-map load, one HEAD lookup and at most one upstream fetch per feed, instead of two loads. The overlay zeroes each feed's head just before resolving it (`refreshManagedPrices` load option), and a test asserts the single commit lookup.
- **Mapper placement.** `toManagedPricesPublic` no longer splits `toBeancountErrorsPublic` from its doc comment.
- **MCP output schema.** The refresh tool's schema derives from the shared status schema (`pick`) instead of redeclaring the freshness enum.
- **Write-gate rationale.** Kept once, on the op-class entry.
- **Formatting.** Fixed a formatting nit in `op-class.ts`.
- **Dashboard pair label.** The panel reuses `commodityPairLabel`.
- **Refresh cache write.** The refresh writes its returned records into the status query instead of refetching it; the commodities charts are still refetched.
- **Journal guard.** Dropped a redundant `locationLabel` guard.

**Skipped:**
- **`getClient` action type.** Not widened to `AuthorizationAction`: the narrow union is the existing allowlist pattern.
- **Freshness as a GraphQL enum.** Not done: it changes the published GraphQL type (String → enum), a contract change outside a behavior-preserving pass.
- **Managed-entry detection.** Deriving it from the ledger's `managedPricePaths` rather than a path regex needs changes to the entry-context read in three packages. Recorded as [w2/033](../../../033.md).

**Checks:** ledger (lint, build, 1137 tests), backend-v2 (typecheck, lint, 4583 tests) and dashboard (format, lint, build) pass. The dashboard suite had one intermittent run with 2 failing files. The next two full runs were green (4866 tests), and I could not identify the failing files.
