---
id: w2/m2/t002
title: Distilled beangulp v3 reference (API + test harness)
worker: worker2
status: done
estimate: 1h
depends_on: [w2/m2/t001]
---

## Objective

Write `references/beangulp-api.md`: the minimal, correct beangulp v3 surface an agent needs — `Importer` base class (`identify`, `extract`, `account`, `filename`, `date`), the `beangulp.Ingest` CLI entry point, and how its built-in test/archive harness runs against sample documents.

## Context

- beangulp replaced `beancount.ingest` in v3; custom importers subclass its base class. 50 open issues upstream — note API sharp edges where known.
- Include where importers conventionally live in a user's ledger repo and how `import.py` wires them.

## Steps

1. Distill the base-class contract with a complete minimal importer example.
2. Document the test-harness loop: run extraction on a sample, compare to expected output, archive.
3. Note common pitfalls (identify() over-matching, date parsing, duplicate handling via existing entries).

## Files

- `skills/.claude/skills/beancount-importer-author/references/beangulp-api.md`

## Acceptance criteria

- [ ] A reader can write and test a minimal importer from this reference alone
- [ ] Test-harness invocation documented with exact commands

## Out of scope

- smart_importer/ML categorization integration (mention as pointer only)
