---
id: w2/m2/t003
title: Authoring workflow — sample-driven draft → test → iterate; drift repair
worker: worker2
status: done
estimate: 1h
depends_on: [w2/m2/t002]
---

## Objective

Write the core workflow in SKILL.md: inspect the sample file, draft the importer, run the beangulp test harness, iterate until green, wire into the user's import config — plus the drift-repair path (existing importer + new failing sample → patch → re-test).

## Context

- The loop is the moat: the harness gives a deterministic pass/fail, so the agent iterates against ground truth instead of eyeballing.
- Account mapping comes from ledger discovery (config-block pattern); ask the user for the target account when new.

## Steps

1. Define the inspect step: header/column analysis, encoding, sign conventions, ask on ambiguity.
2. Define draft → `test` → iterate with exact commands and a stop condition (harness green).
3. Define drift repair: diagnose from harness failure output, patch minimally, re-test.
4. Wire-in step: add to `import.py`/config, confirm with the user before writing.

## Files

- `skills/.claude/skills/beancount-importer-author/SKILL.md`

## Acceptance criteria

- [ ] Workflow ends only on harness green; no untested importer is handed over
- [ ] Drift-repair path documented with the same test-gated loop

## Out of scope

- Runtime categorization of extracted entries (separate import workflow)
