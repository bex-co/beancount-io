---
id: w2/m28/t008
title: Test coverage
worker: worker2
status: done
estimate: 60m
depends_on: [w2/m28/t006]
---

## Objective

Add meaningful tests for the behavior this milestone shipped and for its failure modes. Tests assert real behavior; no tautologies, no snapshot-everything.

## Context

- MCP tool tests live in `src/features/ai-agent/tools/__tests__/`; surface guards (`surface-parity`, `op-class-coverage`, `always-public`, `parity-baseline`) live in `src/server/api/__tests__/` and must stay green.
- ADR 0007 D9 requires socket-level tests for transport properties; reuse that harness for anything observable only on the wire.

## Steps

1. For each acceptance criterion in the implementation tasks, write the test that would have failed before the change.
2. Cover the refusal path for every new capability (wrong scope, pin violation, missing ledger selector).
3. Run `yarn test` and `yarn test:integration` where the change touches Gitea or the ledger service.

## Files

- `backend-cluster/backend-v2/src/features/ai-agent/**/__tests__/`
- `backend-cluster/backend-v2/src/server/api/__tests__/`

## Acceptance criteria

- [x] Every new tool, annotation, envelope, and fix in this milestone has a test that fails without it.
- [x] The four surface guard suites still pass.

## Out of scope

- Coverage-only tests with no behavioral assertion.
