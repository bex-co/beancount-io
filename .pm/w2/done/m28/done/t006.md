---
id: w2/m28/t006
title: Adoption surface
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m28/t002, w2/m28/t004, w2/m28/t005]
---

## Objective

Check that what this milestone ships is discoverable and usable on every surface it touches. This milestone changes every result and error an MCP client sees and adds a write tool with a REST twin, so the guide's "Results and failures" section, the tool table, and the v1 OpenAPI snapshot change.

## Context

- `backend-cluster/backend-v2/docs/mcp.md` is the client-facing MCP guide; `backend-cluster/backend-v2/README.md#connecting-an-mcp-client` and the root `README.md` "Coding agent (MCP)" section summarize it.
- `yarn mcp:conformance <base-url>` is what an operator runs to prove a deployment is connectable; extend it when the contract grows.
- Agent-facing behavior must hold for both Claude Code and Codex: same tool names, same instructions, same refusal dialect.

## Steps

1. Re-read the guide and READMEs against the shipped behavior; fix every sentence the milestone made false and add the new capability where a newcomer would look for it.
2. Run the documented quickstart exactly as written against a local stack (`deploy/docker-mac`).
3. Run `yarn mcp:conformance http://localhost:42601` and, if the milestone added a checkable contract, add the check.
4. Flag any drift you cannot fix in-scope as a new `w2/NNN.md` inbox note rather than diverging silently.

## Files

- `backend-cluster/backend-v2/docs/mcp.md`
- `backend-cluster/backend-v2/README.md`
- `README.md`
- `backend-cluster/backend-v2/scripts/mcp-conformance.ts`

## Acceptance criteria

- [x] Guide, backend README, and root README describe the shipped behavior and nothing the milestone removed.
- [x] The quickstart works as written against a local stack.
- [x] `yarn mcp:conformance` passes locally, including any check this milestone added.

## Out of scope

- Rewriting unrelated sections of the guide.
