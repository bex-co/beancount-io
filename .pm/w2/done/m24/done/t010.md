---
id: w2/m24/t010
title: Simplify
worker: worker2
status: done
estimate: 20m
depends_on: [w2/m24/t009]
---

## Objective

Run `/simplify` over the code this milestone changed (`cli/src/cli/main.py`, `context.py`, `errors.py`, `output.py`, the command modules, `config.py`) for reuse, simplification, and efficiency without behavior change.

## Context

- Behavior-preserving only; `make check-all` must stay green.

## Steps

1. Run `/simplify` scoped to the milestone's diff.
2. Apply accepted findings; rerun `make check-all`.

## Files

- `cli/src/cli/**`

## Acceptance criteria

- [ ] `/simplify` findings addressed or explicitly declined with a reason
- [ ] `make check-all` green

## Out of scope

- New behavior
