---
id: w2/m24/t012
title: Closeout
worker: worker2
status: done
estimate: 15m
depends_on: [w2/m24/t011]
---

## Objective

When all other tasks are complete **and the definition of done actually holds** (single `bea` script, `list`/`add`/`ask` tree, global options, exit codes, JSON read-side, `~/.config/bea`, docs converged, `git grep beancount-cli` clean outside historical board files), close the milestone: mark remaining tasks done, move task files to `w2/m24/done/`, update the milestone README status/rows, move `w2/m24/` to `w2/done/m24/`, and check the box in `w2/README.md`. Run via `/pm done w2/m24/t012` last — not before the DoD is real.

## Context

- m25 depends on this milestone being closed.

## Steps

1. Verify the DoD checklist in `w2/m24/README.md` item by item.
2. Run `/pm done w2/m24/t012`.

## Files

- `.pm/w2/m24/**`
- `.pm/w2/README.md`

## Acceptance criteria

- [ ] DoD verified before any move; board state consistent in all three status locations

## Out of scope

- —
