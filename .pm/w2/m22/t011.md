---
id: w2/m22/t011
title: Closeout
worker: worker2
status: todo
estimate: 25m
depends_on: [w2/m22/t009, w2/m22/t010]
---

## Objective

Close the personal API-key migration only after both OAuth profiles, the retirement gate, checks, and deployed development behavior are demonstrated.

## Context

- Green unit tests do not prove a real headless authorization, workload assertion exchange, stable rate identity, revocation, or absence of live API-key consumers.
- Production deployment remains outside the board closeout action.

## Steps

1. Verify every definition-of-done item and record package/protocol/guidance/secret-scan evidence plus aggregate API-key exit evidence without credentials or user data.
2. On a deployed development stack, authorize an interactive client and an unattended workload, call allowed and denied GraphQL/REST/MCP operations, rotate/revoke the grant, verify one-hour expiry semantics, stable rate/audit attribution, and post-sunset API-key rejection.
3. Confirm Dashboard/Mobile/Discourse regressions, compatibility/rollback completion, docs and OpenAPI publication, then archive the milestone via `/pm done`.

## Files

- `.pm/w2/m22/`
- `.pm/w2/README.md`

## Acceptance criteria

- [ ] Deployed evidence shows interactive and workload OAuth tokens with the intended client/grant, resource, scopes, ledger pin, and expiry.
- [ ] Aggregate evidence and the completed exit gate justify removal of personal-key issuance and authentication.
- [ ] Rate, audit, authorization, revocation, and cross-client regression evidence matches the published contract.
- [ ] All tasks are archived under `done/`, the milestone is moved to `.pm/w2/done/m22/`, and the w2 checkbox is checked.

## Out of scope

- Production deployment, deleting audit history, or claiming immediate revocation of an already-issued self-contained access token.
