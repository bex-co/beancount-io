---
id: w2/m14/t001
title: Model billing actions and credential rules
worker: worker2
status: todo
estimate: 35m
depends_on: []
---

## Objective

Add canonical billing actions and explicit credential requirements to the shared PDP and declarative model vocabulary.

## Context

- Billing is currently `session-only` plus resolver-local `assertSessionIdentity` checks.

## Steps

1. Map billing reads/writes to `user.billing` permissions.
2. Encode interactive-session restrictions in credential policy, not tuples.
3. Keep public/static behavior unchanged unless already classified public.

## Files

- `backend-cluster/backend-v2/src/server/api/authorization/**`
- `backend-cluster/backend-v2/authz/**`
- `backend-cluster/backend-v2/src/server/api/op-class.ts`

## Acceptance criteria

- [ ] Every billing verb has one action and accepted credential set.
- [ ] No Stripe identifiers or token claims enter the FGA model.
- [ ] Unknown or noninteractive credentials fail closed.

## Out of scope

- Changing plans, pricing, or checkout contracts.
