# Dedupe /ledger SEO title; fix stale language count in docs

`src/routes/ledger.index.tsx` sets `seo.ledgerDashboard.*` meta via route `head()`, while `DashboardPage` also renders `<PageSEO titleKey="seo.dashboard.title" .../>` — two different keys for the same page. Pick one and remove the other. Also fix `dashboard/CLAUDE.md`'s stale "13 languages" claim → 15 (`src/i18n/config.ts` already lists 15).
