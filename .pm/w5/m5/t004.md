---
id: w5/m5/t004
title: "Verify and repair import categorization behavior"
worker: worker1
status: todo
estimate: 60m
depends_on: [w5/m5/t001]
---

## Objective

Verify account selection, confirmation, and repeat-run effects in the shipped categorization prompt, repairing demonstrated workflow gaps.

## Context

- `skills/.claude/skills/beancount-import/` owns the discover/normalize/stage/dedup/suggest/confirm/write/verify workflow.
- The import-id convention must remain consistent across local skills, CLI imports, and hosted writes.
- The shipped prompt operates on staged bank transactions. Use synthetic staged data and controlled provider responses for these cases; live Plaid linking/sync remains deferred in w2/009.

## Steps

1. Add m4 cases with synthetic staged transactions, known existing accounts, uncertain payees, and duplicates. State exactly which provider responses are controlled and which MCP/service behavior is real.
2. Exercise suggestion, rejection/discard, preview, unconfirmed, and confirmed submission paths in both clients; ensure unavailable accounts are not silently invented.
3. Repeat the import and compare persisted ledger state to detect duplicate entries. Repair only demonstrated gaps while preserving existing import-id and bank-import deduplication contracts, confirmation, and validation.

## Files

- `backend-cluster/backend-v2/src/features/ai-agent/api/mcp-prompts.ts`
- `backend-cluster/backend-v2/evals/mcp-agent/ (staged import journeys)`

## Acceptance criteria

- [ ] Suggestions use existing accounts and do not silently invent new account names.
- [ ] Replaying the same synthetic import does not create duplicate externally sourced entries.
- [ ] No write occurs before confirmation; unsupported inputs or unavailable capabilities receive an honest blocked path.

## Out of scope

- New bank integrations or the deferred Plaid sandbox journey in w2/009.
- Changing import-id grammar or replacing existing import logic.
