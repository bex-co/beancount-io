---
id: w5/m2/t005
title: Adoption surface
worker: worker1
status: done
estimate: 30m
depends_on: [w5/m2/t001, w5/m2/t002, w5/m2/t003, w5/m2/t004]
---

## Objective

Verify the accessibility changes are consistent across every surface they touch and that docs still match.

## Context

- Standing closing task (see `.claude/commands/pm.md`); included because this milestone ships user-facing dashboard changes.

## Steps

1. Confirm the logo/home treatment is identical in `dashboard-sidebar.tsx` and `ledger-switcher.tsx`.
2. Smoke-test keyboard flows on `/ledger` and `/ledger-gallery` in `yarn dev`: sidebar navigation, ledger row actions, create/edit/delete dialogs, gallery search.
3. Check `dashboard/CLAUDE.md` for any statements the changes invalidate; flag drift as inbox notes.

## Acceptance criteria

- [ ] Both logo surfaces consistent; keyboard smoke test passes on both pages.
- [ ] Doc drift, if any, filed as w5 inbox notes.

## Out of scope

- A full-app a11y audit.
