---
id: w5/m2/t007
title: Test coverage
worker: worker1
status: done
estimate: 45m
depends_on: [w5/m2/t005]
---

## Objective

Add meaningful tests asserting the accessibility behavior this milestone shipped.

## Context

- Standing closing task. Tests live in adjacent `__tests__` directories; Vitest via `yarn test`. `@testing-library/react` is in use (see existing feature tests); use its queries (`getByRole`, `getByLabelText`) so the tests fail if accessible names disappear.
- Gallery combobox already has a test file to extend (`pages/gallery-page/__tests__/gallery-page.test.tsx`); the sidebar suite was created in w5/m1/t008.

## Steps

1. Extend the sidebar tests: accessible names on home button and icon-only triggers; row actions reachable as separate controls.
2. Extend gallery tests: listbox role, `aria-activedescendant` follows keyboard highlight, clear button focusable.
3. Cover the delete dialog's accessible description and initial focus.
4. Assert real behavior — no trivial or snapshot-only tests.

## Files

- `dashboard/src/features/ledger-list/pages/dashboard-page/components/__tests__/dashboard-sidebar.test.tsx`
- `dashboard/src/features/ledger-list/pages/gallery-page/__tests__/gallery-page.test.tsx`

## Acceptance criteria

- [ ] Tests fail if any of the four fixes regress.
- [ ] `yarn test` passes from `dashboard/`.

## Out of scope

- Automated axe-core integration (worth an inbox note if desired).
