---
id: w5/m2/t006
title: Simplify
worker: worker1
status: done
estimate: 30m
depends_on: [w5/m2/t005]
---

## Objective

Run `/simplify` over the code this milestone changed — reuse, simplification, efficiency; behavior-preserving.

## Context

- Standing closing task. Primary surface: `dashboard-sidebar.tsx`, `ledger-switcher.tsx`, `gallery-page/index.tsx`.

## Steps

1. Run `/simplify` scoped to the files changed in t001–t004.
2. Apply only behavior-preserving simplifications (e.g. shared aria-label helpers if the same pattern repeats).

## Acceptance criteria

- [ ] `/simplify` run; applied changes are behavior-preserving.
- [ ] `yarn lint && yarn test` pass from `dashboard/`.

## Out of scope

- Refactoring unrelated a11y debt discovered along the way (file inbox notes instead).
