---
id: w5/m2/t008
title: Closeout
worker: worker1
status: done
estimate: 10m
depends_on: [w5/m2/t007]
---

## Objective

Close the milestone once all other tasks are complete **and** the definition of done actually holds.

## Steps

1. Verify the DoD: the four violations are gone; sidebar and gallery are keyboard-operable and screen-reader-correct.
2. Set every remaining task's `status: done`, move each `tNNN.md` to `w5/m2/done/`, mark every row `— **DONE**` and set `**Status:** done` in this milestone's `README.md`.
3. Move `w5/m2/` to `w5/done/m2/` and check `- [x]` in the w5 `README.md`.
4. Verify the exit condition: no completed files remain in the open tree.

## Acceptance criteria

- [ ] Milestone exists only at `.pm/w5/done/m2/`, workstream checkbox checked, all statuses in sync.

## Out of scope

- Acting on inbox notes w5/001–004 (separate work).
