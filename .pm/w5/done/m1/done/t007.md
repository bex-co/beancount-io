---
id: w5/m1/t007
title: Simplify
worker: worker1
status: done
estimate: 30m
depends_on: [w5/m1/t006]
---

## Objective

Run `/simplify` over the code this milestone changed — reuse, simplification, efficiency; behavior-preserving.

## Context

- Standing closing task. Primary surface: `dashboard-sidebar.tsx`, `blog-feed.tsx`, `ledger-switcher.tsx`.
- Known pre-existing duplication this pass may legitimately touch: create/edit/delete mutation logic shared between `DashboardSidebar` and the (soon-to-be-deleted per w5/001) `ledger-list.tsx`.

## Steps

1. Run `/simplify` scoped to the files changed in t001–t005.
2. Apply only behavior-preserving simplifications.

## Acceptance criteria

- [ ] `/simplify` run; applied changes are behavior-preserving.
- [ ] `yarn lint && yarn test` pass from `dashboard/`.

## Out of scope

- Deleting dead code (that is inbox note w5/001).
