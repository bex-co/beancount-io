---
id: w5/m1/t008
title: Test coverage
worker: worker1
status: done
estimate: 45m
depends_on: [w5/m1/t006]
---

## Objective

Add meaningful tests for the behavior this milestone shipped — targeting the **live** `DashboardSidebar`, which currently has zero coverage.

## Context

- Standing closing task. The 854-line `components/__tests__/ledger-list.test.tsx` covers the dead predecessor and gives false confidence; do not pattern-match it blindly, but its fixtures/mocks may be reusable.
- New behavior to cover: visibility badge + meta rendering, skeleton count behavior, empty state CTA opening the create dialog, post-create navigation.
- Tests live in `__tests__` directories adjacent to the code (per `dashboard/CLAUDE.md`); run with `yarn test` (Vitest).

## Steps

1. Create `pages/dashboard-page/components/__tests__/dashboard-sidebar.test.tsx`.
2. Cover: loading skeleton, error + retry, empty state + CTA, populated rows with badge/meta, create success → navigation.
3. Assert real behavior and failure modes — no snapshot-everything tests.

## Files

- `dashboard/src/features/ledger-list/pages/dashboard-page/components/__tests__/dashboard-sidebar.test.tsx` (new)

## Acceptance criteria

- [ ] `DashboardSidebar` has tests for all four states and the create-navigation flow.
- [ ] `yarn test` passes from `dashboard/`.

## Out of scope

- Deleting the dead `ledger-list.test.tsx` (inbox note w5/001).
