---
id: w5/m1/t009
title: Closeout
worker: worker1
status: done
estimate: 10m
depends_on: [w5/m1/t008]
---

## Objective

Close the milestone once all other tasks are complete **and** the definition of done actually holds.

## Steps

1. Verify the DoD: a signed-in user sees each ledger's public/private state and last activity at a glance; loading/empty/error states are intentionally designed; creating from the sidebar lands inside the new ledger.
2. Set every remaining task's `status: done`, move each `tNNN.md` to `w5/m1/done/`, mark every row `— **DONE**` and set `**Status:** done` in this milestone's `README.md`.
3. Move `w5/m1/` to `w5/done/m1/` and check `- [x]` in the w5 `README.md`.
4. Verify the exit condition: no completed files remain in the open tree.

## Acceptance criteria

- [ ] Milestone exists only at `.pm/w5/done/m1/`, workstream checkbox checked, all statuses in sync.

## Out of scope

- Starting w5/m2 (separate milestone).
