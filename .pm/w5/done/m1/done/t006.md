---
id: w5/m1/t006
title: Adoption surface
worker: worker1
status: done
estimate: 30m
depends_on: [w5/m1/t002, w5/m1/t003, w5/m1/t004, w5/m1/t005]
---

## Objective

Verify the shipped sidebar changes are discoverable and consistent on every surface they touch.

## Context

- Standing closing task (see `.claude/commands/pm.md`); included because this milestone ships user-facing dashboard changes.

## Steps

1. Check `dashboard/CLAUDE.md` and the root `README.md`/`CLAUDE.md` package tables for anything the sidebar changes invalidate (feature descriptions, i18n language count).
2. Verify the ledger-list data shown in the sidebar and `ledger-switcher.tsx` are consistent (badges, ordering).
3. Sanity-check the create → navigate flow end-to-end in `yarn dev`, in at least two locales.
4. Flag any drift as follow-up inbox notes rather than silently diverging.

## Files

- `dashboard/CLAUDE.md`
- Root `README.md` / `CLAUDE.md` (only if drift found)

## Acceptance criteria

- [ ] Docs match shipped behavior, or drift is filed as w5 inbox notes.
- [ ] Sidebar and switcher render consistent ledger metadata.

## Out of scope

- New documentation for the feed (unchanged by this milestone).
