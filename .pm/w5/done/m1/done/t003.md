---
id: w5/m1/t003
title: Empty state with icon + inline Create Ledger CTA
worker: worker1
status: done
estimate: 25m
depends_on: []
---

## Objective

Replace the sidebar's bare "No ledgers found" muted text with a designed empty state: icon, short copy, and an inline Create Ledger action.

## Context

- `dashboard-sidebar.tsx`: empty state is a centered `p-4 text-center text-muted-foreground text-sm` line — no icon, no CTA, though this is the "deleted my last ledger" recovery path.
- A shared `EmptyState` component exists at `src/common/components/empty-state` (currently only used by the dead `ledger-list.tsx`) — prefer it over a one-off.
- The Create Ledger dialog and `LedgerForm` already exist and are triggered from the sidebar header button.

## Steps

1. Render the shared `EmptyState` (icon + copy) in the sidebar's empty branch.
2. Add an inline "Create Ledger" button that opens the same create dialog as the header button.
3. Localize new copy in all 15 languages.

## Files

- `dashboard/src/features/ledger-list/pages/dashboard-page/components/dashboard-sidebar.tsx`
- `dashboard/src/features/ledger-list/locales/*.ts`

## Acceptance criteria

- [ ] Empty state shows icon, copy, and a working Create Ledger CTA.
- [ ] Uses the shared `EmptyState` component.
- [ ] `yarn lint` passes.

## Out of scope

- BlogFeed's empty state copy (acceptable as-is).
