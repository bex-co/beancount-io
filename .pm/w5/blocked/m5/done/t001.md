---
id: w5/m5/t001
title: "Audit shipped prompt capabilities and argument handling"
worker: worker1
status: done
estimate: 45m
depends_on: [w5/m4/t009]
---

## Objective

Map the four shipped playbooks to actual capabilities and make their argument, target-selection, and workflow acceptance requirements executable through the m4 harness.

## Context

- Commit `adfcf587` added `mcp-prompts.ts` and prompt registration to `src/server/api/composition-root.ts`; the existing list/protocol tests and static metadata classification are already shipped.
- The four customer playbooks are the behavior source; w5/m4 supplies the completed real-client benchmark.

## Steps

1. Map ledger selection, input requirements, reads, previews, writes, validation, and completion evidence for all four workflows to current protected operations.
2. Exercise the existing list/get surface and client argument handling, including explicit empty arguments, malformed values, ambiguous targets, and requests outside a credential pin. Add acceptance cases for uncovered behavior and repair only demonstrated gaps.
3. Preserve the documented metadata-only classification and the full parity gate for any domain behavior touched; record the capability map and remaining gaps before workflow repair tasks.

## Files

- `backend-cluster/backend-v2/src/server/api/composition-root.ts`
- `backend-cluster/backend-v2/src/features/ai-agent/api/mcp-prompts.ts`
- `backend-cluster/backend-v2/evals/mcp-agent/ (capability map and client cases)`
- `backend-cluster/backend-v2/docs/api-parity.md (if classification documentation is needed)`

## Acceptance criteria

- [ ] All four prompt names and their required/optional arguments are discoverable.
- [ ] Retrieval returns instructions without executing reads or writes against ledger contents.
- [ ] Unknown prompts and invalid arguments fail clearly, and every planned workflow step has a real supported capability.

## Out of scope

- New accounting or authorization semantics.
- New parity exemptions used to conceal missing adapters.
- Recreating the shipped prompt registration or treating its existing tests as new work.
