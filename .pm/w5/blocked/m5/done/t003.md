---
id: w5/m5/t003
title: "Verify and repair account reconciliation behavior"
worker: worker1
status: done
estimate: 60m
depends_on: [w5/m5/t001]
---

## Objective

Verify that the shipped reconciliation prompt produces the intended mismatch classifications and ledger effects, and repair reproduced deviations.

## Context

- `skills/.claude/skills/beancount-reconcile/` defines statement normalization, mismatch classes, date tolerance, and assertion-date semantics.
- Existing MCP writes already support validation and preview behavior that the prompt must preserve.
- The reconciliation prompt body already shipped in `adfcf587`; m4 provides the real-client runner and state scorer.

## Steps

1. Add synthetic statement/ledger cases for missing entries, duplicates, amount mismatches, and date drift, with explicit expected classifications and assertion dates.
2. Run unconfirmed, read-only, denied-target, and confirmed-correction paths through the clients; verify concrete proposed changes and persisted effects with m4.
3. Repair only observed deviations in the existing instructions or eligible adapters, preserving the customer's mismatch, confirmation, and final-validation rules.

## Files

- `backend-cluster/backend-v2/src/features/ai-agent/api/mcp-prompts.ts`
- `backend-cluster/backend-v2/evals/mcp-agent/ (reconciliation journeys)`

## Acceptance criteria

- [ ] Synthetic missing/duplicate/mismatched/date-drift cases are classified without inventing a match.
- [ ] Unconfirmed or read-only sessions do not write, and denied targets remain denied.
- [ ] A proposed assertion is not represented as successful when validation fails.

## Out of scope

- Reimplementing matching/accounting code or changing credential ceilings.
- Automatically modifying existing suspected duplicate transactions.
