# Design spike: ledger cards vs feed-only dashboard home

The `/ledger` content column renders only `BlogFeed` — the ledger list exists solely in the left sidebar. For returning users the feed-only home is the page's biggest first-impression weakness. This is a product decision, not a polish task: write a short options note (keep feed-only / ledger cards + feed / hybrid) with a recommendation before any milestone is proposed.
