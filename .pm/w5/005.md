# Give the user-profile header logo an accessible treatment

`src/features/user-profile/components/user-profile-header.tsx` still uses the generic `alt="Logo"` on the `/lgasset/logo.png` image inside its home button — the same violation w5/m2/t001 fixed on the dashboard sidebar and the ledger switcher, on a third surface that was out of that task's scope. Apply the identical treatment: `alt=""` (the adjacent username text already names the control) and, since the button's visible label is the username rather than its destination, an `aria-label` for "Go to dashboard" (`page.dashboard.goToDashboard`, already in all 15 locales). Verify with `yarn lint && yarn test` from `dashboard/`. Surfaced during the w5/m2 adoption-surface check.
