# Delete dead ledger-list code

Delete `dashboard/src/features/ledger-list/components/ledger-list.tsx` (477 lines, imported by nothing live) and the feature-local `user-nav.tsx` (58 lines; the dashboard layout uses `@/common/components/user-nav.tsx`), plus their two test files (~1,400 lines testing dead code — `components/__tests__/ledger-list.test.tsx`, `components/__tests__/user-nav.test.tsx`). Remove the now-unused i18n keys only referenced by the dead component: `page.dashboard.searchLedgers`, `loadingLedgers`, `manageLedgers`, `manageLedgersDescription`, `failedToLoadLedgersDescription` (all 15 locales). Verify with `yarn lint && yarn test` from `dashboard/`. The false-confidence test suite is the real cost of leaving this around.
