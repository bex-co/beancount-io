---
id: w3/m3/t003
title: Implement decided iterations (interval filter / panel tuning)
worker: worker3
status: todo
estimate: 45m
depends_on: [w3/m3/t002]
---

## Objective

Ship whatever t002 decided "yes" on: an interval filter row on /budget (reusing the pill component) and/or the Home panel empty-state change.

## Context

- Interval filter reference: dashboard `budget-filter-pills.tsx` — chronological `All/Daily/Weekly/Monthly/Quarterly/Yearly`, filters the card list client-side (does not affect card queries); mobile would reuse `TimeRangePills`.
- Panel tuning options from the spec: keep CTA / soften copy / hide panel when zero budgets.
- If t002 decided "no" on both, this task closes with a note — that outcome is a valid, data-justified result.

## Steps

1. Implement per t002's record; strings for any new UI go through `en.ts` + the 13 locales (extend t001's mapping).
2. `yarn test` green.

## Files

- `mobile/src/screens/budget-screen/budget-screen.tsx` and/or `mobile/src/screens/home-screen/components/budget-card.tsx`
- `mobile/src/translations/*` (if new strings)

## Acceptance criteria

- [ ] Decisions implemented exactly as recorded (or closure note for double-"no"); all locales stay key-complete.

## Out of scope

- Search on /budget (remains cut unless a future review reopens it); widgets/notifications.
