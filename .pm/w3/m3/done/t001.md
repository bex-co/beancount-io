---
id: w3/m3/t001
title: Translate budget strings into the 13 non-English locales
worker: worker3
status: done
estimate: 45m
depends_on: []
---

## Objective

Bring every budget translation key (added in m1/m2 to `en.ts`) to all 13 non-English locale files so budget ships in the user's language.

## Context

- Locales: bg, ca, de, es, fa, fr, ja, ko, nl, pt, ru, sk, uk, zh in `mobile/src/translations/`.
- Terminology source: the dashboard already localized this exact feature — `dashboard/src/features/ledger-data/budget/locales/<lang>.ts` has vetted translations for the same 15 languages; reuse those rather than re-translating (note: dashboard has 15, mobile 13 — map only mobile's set).
- Keep semantic choices intact per language: "Actual", "Above/Below/On target".

## Steps

1. Enumerate the budget keys in `en.ts`; map each to the dashboard's per-language equivalents; adapt to mobile's flat-key naming.
2. Fill all 13 files; run the translation key-parity tests.

## Files

- `mobile/src/translations/{bg,ca,de,es,fa,fr,ja,ko,nl,pt,ru,sk,uk,zh}.ts`

## Acceptance criteria

- [ ] Key-parity test passes across all locales; no en fallbacks for budget keys.
- [ ] Spot-check zh/de/fr against the dashboard's vetted strings.

## Out of scope

- New locales; dashboard locale changes.
