---
id: w3/m3/t006
title: Test coverage
worker: worker3
status: done
estimate: 30m
depends_on: [w3/m3/t004]
---

## Objective

Tests guarding m3's behavior: locale key parity for the budget key set, plus any t003 filter logic.

## Context

- Existing pattern: `mobile/src/translations/__tests__/` key-parity tests — extend or confirm they cover the budget keys across all 13 locales (a missing key must fail, not fall back silently).
- If t003 shipped the interval filter: pure filter-predicate tests (group interval → visible set).

## Steps

1. Ensure budget-key parity is asserted per locale; add filter tests if applicable.
2. `yarn test:unit` green.

## Files

- `mobile/src/translations/__tests__/` (additions if needed)
- `mobile/src/screens/budget-screen/selectors/__tests__/` (if filter shipped)

## Acceptance criteria

- [ ] Removing any budget key from any locale fails a test; filter logic (if shipped) covered.
- [ ] `yarn test` passes.

## Out of scope

- Translation quality assertions beyond key presence.
