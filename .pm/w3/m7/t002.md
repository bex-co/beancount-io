---
id: w3/m7/t002
title: "`v1Route` declarations for the sixteen reads"
worker: worker3
status: todo
estimate: 60m
depends_on: [w3/m7/t001]
---

## Objective

Put the sixteen report and journal reads on the v1 REST surface, following t001's parameter shape.

## Context

- `src/features/ledger/api/rest/v1/` — one `v1Route({...})` each, listed in `v1/index.ts`.
- Paths address a ledger as `{owner}/{name}`, two segments (ADR 0006 D7).
- Each op is already classified `read`; fill the `rest` identifier on the existing `VERB_TABLE` row and drop its `deferred:` `restExempt`.
- Call the same service the GraphQL resolver calls. A reimplemented read is a second behaviour, not a second surface.
- `yarn generate-v1-openapi` then review the snapshot diff.

## Steps

1. Declare the sixteen; list them in `v1/index.ts`.
2. Apply t001's bounds to the three unbounded reads.
3. Fill `VERB_TABLE`; regenerate the OpenAPI snapshot.

## Files

- `src/features/ledger/api/rest/v1/`, `src/server/api/op-class.ts`, `docs/openapi/v1.json`

## Acceptance criteria

- [ ] All sixteen answer over curl against a local stack.
- [ ] Each calls the same service as its GraphQL twin.
- [ ] The three unbounded reads enforce t001's bound; a request that would exceed it is handled as decided, not truncated silently.
- [ ] `openapi-completeness.test.ts` green.

## Out of scope

- The MCP half (t003).
