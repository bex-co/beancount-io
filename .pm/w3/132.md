# Current plan summary ignores the selected app language

**Minor · 30–45m · dashboard.** A2: readers should understand their current plan limits in their selected language.

Production https://beancount.io/settings/general?lang=es, 2026-09-11, authenticated QA GROWTH account, isolated headless Chrome152, light. No billing or account action invoked. Evidence is restricted to the plan's public quota description; no profile, credentials, usage or payment details retained.

Fresh1440×1000 with browser en-US and app es: the section says Suscripción and html.lang is es, but the current-plan description reads “2,000,000 AI tokens · 20 ledgers · Unlimited directives · 10 collaborators/ledger”. Fresh390×1000 with browser es-ES reproduces the English words, with2.000.000. An earlier independent390×844 Spanish context agrees. English1440 control renders the same English words appropriately. All repeats have no page errors; cropped summary screenshot visually inspected.

Root: dashboard/src/features/user-settings/pages/general/subscription-plan-banner.tsx:30–38 builds featureSummary from literal English nouns/plurals and calls toLocaleString() without the app locale. Surrounding labels use t(), so this paragraph alone bypasses localization; its punctuation follows the browser rather than the chosen application language. The component's one caller is SubscriptionSection. This is not a hydration failure or incorrect quota calculation.

Read control: GetAllTierQuotas HTTP200 supplies GROWTH aiCfoTokensMax2000000, maxLedgers20, maxCollaboratorsPerLedger10, maxDirectives-1. The displayed numeric allowances and Unlimited directives interpretation match. The presentation ignores language.

Fix: use translated allowance messages and explicit app-locale number formatting for this paragraph. Reuse the existing userSettings allowance keys used by subscription-upgrade-cards.tsx:80–105 where their meaning fits (Spanish already includes Directivas ilimitadas and Hasta {count} colaboradores por libro mayor). Keep singular/plural, unlimited and absent-quota fallback behavior correct. Do not translate the Growth product name, change quotas, billing state or shared backend contracts, or mutate runtime locale globally.

Acceptance: Spanish current-plan prose is Spanish with Spanish number formatting even under an English browser; English remains English under a Spanish browser. Cover finite/singular/unlimited and missing-quota rendering through actual translated messages, not mocked t(key) output. Run dashboard format/lint/test/build and the package's locale checks on implementation; add no dependencies.

Dedupe: all open/done board and history searched for subscription/plan/English/quota descriptions. Read the complete historical110 record from4574a7d8: it owns shared report number formatting and hydration and explicitly excludes inline quota formatters. Its closure7ef73e1e does not repair this component. Existing m22 covers relative dates/calendars; no open subscription-summary item overlaps. Source is unchanged since af5339de; local/fetched main f1965988 has the same implementation. Full deployed SHA unverified.

Ignored evidence dashboard/tmp/qa-20260911-w3-loop/: subscription-locale-read.json, subscription-locale-repeat.json, subscription-quota-control.json, subscription-summary-es-{1440,390}.png and subscription-summary-en-1440.png. All three fresh repeat contexts closed. No product fix, subscription/portal session, account mutation or shipping. Other locale prose, screen-reader speech, live tier changes and patched results remain unverified.
