# Trial Balance narrow view selection leaves the wrong chart displayed

Severity: **minor**. Package: **dashboard**. Estimate: **30–45m**.
A2: a reader choosing an account category must see its corresponding chart.

Production https://beancount.io, 2026-09-11; public synthetic open_ledger/freelancer-invoicing, authenticated reader, English/light, isolated headless Chrome152. No active global filters. Local/fetched origin main f1965988; implicated source matches origin. Full deployed SHA unverified.

1. Fresh390×1000: open /ledger/open_ledger/freelancer-invoicing/trial-balance. Show charts if previously collapsed.
2. In View choose Income. Selector says Income, but panel heading remains Assets Hierarchy and rectangles remain Business/TaxReserve.
3. Independent fresh390 repeat with Expenses produces the same Assets panel.
4. Resize that context to1440, click desktop Equity tab, then resize to390. The panel correctly becomes Equity, but narrow selector still says Expenses. Fresh desktop clicking all five tabs works. Narrow Income Statement Expenses selection correctly displays Expenses.

Expected/fix: one controlled selectedTab drives both desktop Tabs and narrow Select. TrialBalanceContent uses useState at54, passes its value/setter to ResponsiveTabTriggerList at184–185, but the enclosing Tabs at181 has only defaultValue="assets". Its internal desktop state and the narrow state never synchronize. Bind value/onValueChange as existing Balance Sheet, Income Statement and Cash Flow callers do. Do not rewrite the shared selector or alter hierarchy data.

API control: GraphQL GetLedgerTrialBalance and REST /api-gateway/v1/ledgers/open_ledger/freelancer-invoicing/trial-balance?conversion=at_cost both200. After normalizing REST snake_case names, all selected account/balance/rollup/children projections match exactly. USD rollups: Assets30950, Income-29492.4, Expenses15042.4, Equity-16500. No missing chart data or page errors. Narrow screenshot and resized mismatch visually inspected.

Root file: dashboard/src/features/reports/trial-balance/trial-balance-content.tsx:181. Shared component: dashboard/src/common/components/responsive-tab-trigger-list/index.tsx. History traces uncontrolled Tabs to af5339de; later localization aeec496b does not repair state. All open/done board searches for Trial Balance, view selector, responsive tabs and narrow chart selection found no duplicate. Existing125 concerns treemap click displacement, not selecting the wrong dataset. No newer fix in fetched origin.

Acceptance: all five narrow choices render their matching panel; desktop/narrow resize preserves selected category; desktop keyboard tab navigation stays synchronized; existing zero-data Liabilities and report table are unchanged. Use a real responsive selector/panel interaction regression, not a mock-only state assertion; run dashboard format/lint/test/build for implementation. No dependencies/backend changes.

Evidence under dashboard/tmp/qa-20260911-w3-loop/: trial-fresh-controls.json, trial-selection-repeat.json, trial-selection-api.json, trial-view-390.png, trial-resize-390.png. Two fresh narrow reproductions, working desktop and sibling controls. All repeat contexts closed. No product fix, production writes or shipping; native touch/screen-reader and patched behavior remain unverified.
