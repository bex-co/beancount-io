# Direct ledger loads lose the access error needed for the correct response

**Severity:** minor. **Owner:** dashboard. **Estimate:** 45–60 minutes.
**A2:** following a ledger link produces the intended sign-in or unavailable
response, rather than an unexplained server failure.

Production `https://beancount.io`, 2026-09-08, Chrome 152, English/light,
1440×1000 and390×844. Fresh guests and the existing QA session. Synthetic
nonexistent path `open_ledger/qa-missing-ledger-20260908`; no real private ledger
is probed. Local HEAD `a315c273`, fetched main `afb36a9b`; implicated sources
unchanged. Deployed SHA unverified.

1. Directly open `/ledger/open_ledger/qa-missing-ledger-20260908?lang=en`
   (the `/overview` alias also reaches it).
2. Both identities receive HTTP500, **Failed to Load Ledger**, an unexpected
   error explanation and a Try Again button. A guest is not sent to sign-in.
   Initial and fresh repeats at both widths agree.
3. Real read-only GraphQL controls show that `GetLedger` already distinguishes
   the needed handling: the missing path returns HTTP200 with
   `UNAUTHENTICATED` / **Authentication required** for guests, and `FORBIDDEN` /
   **Authorization denied** for the QA session. Neither returns ledger data.
   Public `open_ledger/example` returns data without errors for both identities.
4. Inspect only the mounted error object's safe shape: it is plain `Error`,
   with own properties `stack` and `message`, no Apollo symbol and no `.errors`
   array. Its message matches the API denial, but the structured code is gone.
5. As a diagnostic control, start on public Example and navigate to the same
   missing path using the mounted application's router. Client navigation
   correctly sends guests to login with the full relative `next`, and shows
   **Page not found** for QA. This is the actual router/API behavior, not a
   fabricated response or a claim of clicking an existing link to the test path.

Expected: direct and client navigation follow the same access policy. Preserve
the backend's privacy behavior: authenticated denial is an unavailable/not-found
presentation, and anonymous denial requests sign-in. Do not distinguish real
private ledgers from nonexistent ones or grant access to either.

Root: `routes/ledger.$ledgerOwner.$ledgerName.tsx:9–19` awaits `GetLedger` and
lets its Apollo error cross the router's server-to-browser boundary unchanged.
`common/components/ledger-layout/ledger-route-error.tsx:18–28` depends on
`isUnauthenticatedError`; `ledger-layout-error.tsx:28–36` depends on the Apollo
class guard to choose not-found/auth variants. Once hydrated as a plain Error,
neither guard recognizes these two messages, and the generic variant wins.

Pinned dependency proof: local `@tanstack/router-core`1.167.0 installs
`ShallowErrorPlugin` first in its default Seroval plugins. Its source explicitly
keeps only `message` and reconstructs `new Error(message)`. Apollo Client4.1.3
brands `CombinedGraphQLErrors` with a non-enumerable `Symbol.for("apollo.error")`
and `.is()` checks that brand. The deployed object's observed shape matches
this mechanism. Existing registry archives for router-core/react-router1.167.0
and Apollo4.1.3 were SHA512-verified again; no dependency was installed or changed.

Handle these known ledger-access outcomes before that lossy boundary, using
the router's existing redirect/not-found signals at the ledger loader. Preserve
the complete safe relative destination, including child path/search, for login.
Use the existing unavailable presentation/status for denied/missing access;
keep actual unexpected/network/server errors as errors. Do not add message-text
authentication guesses, weaken authorization or build a general serializer that
exports arbitrary GraphQL error internals. No SDK/backend change is required.

Backend `ledger/api/resolvers/ledger-resolver.query.ts:74–84` permits anonymous
entry to the resolver but its workflow at `ledger/workflow/ledger-workflow.ts:1000–1011`
authorizes `LEDGER_METADATA_READ` before fetching ledger content. Keep that
boundary and its real codes. The one parent dashboard loader serves every child
ledger route. Root ErrorPage has a similar class-dependent auth check, but its
other error paths were source-audited rather than live-proven here; do not
expand this small ledger-access correction into unrelated API error handling.

Acceptance: fresh direct/alias and client navigation agree for guest denial,
signed-in denial and a readable public ledger. Denials no longer become generic
HTTP500 pages; login keeps a safe full return destination, and unavailable pages
remain noindex. Add a real loader/SSR regression: current tests construct a
branded error or mock the auth predicate, so they do not test serialization.
Retain the privacy mask, optional identity behavior and healthy public reads.
060 separately owns false expiry copy;088 owns a non-refetching Retry button.

Dedupe: all local open/completed boards, fetched main and targeted history
reviewed. Completed `w2/m23` explicitly requires correct ledger authorization
and not-found behavior, but its route-error tests stop before an SSR round-trip.
`afcd6bae` added the route error integration; no newer repair exists.014 is an
admin-only collaborator query,032 a missing commit response,060 expiry copy,
and082/083 Ask-specific navigation/query behavior.

Ignored evidence under `dashboard/tmp/qa-20260907-w3-loop/`:
`ledger-access-ssr-evidence.json`, four `ledger-access-error-{guest,qa}-{1440,390}.png`
captures and extracted pinned serializer/error sources. Twelve retained direct
loads return500 across initial/fresh/retry controls; four client-navigation
controls handle the same denials correctly. Eight direct GraphQL controls are
real read-only responses. Each cold error also reports React520, which React
[documents as recovered concurrent rendering](https://react.dev/errors/520),
not a permanently crashed browser. Robots remain `noindex, follow`. All contexts
closed; no credential submission, access-policy change or accepted write occurred.
