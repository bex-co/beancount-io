# Net Worth and Recent Activity dates ignore the selected language

**Minor · 45–60m · dashboard.** A2: translated financial overviews should display their dates in the selected application language.

Production https://beancount.io, 2026-09-11, isolated headless Chrome152, light, authenticated QA reader of public synthetic open_ledger/freelancer-invoicing. Local/fetched main f1965988; exact deployed SHA unverified. No product changes or production mutations.

Open /ledger/open_ledger/freelancer-invoicing?time=2026&lang=fr in an en-US browser. Wait for the report and Recent Activity, then choose **Afficher le tableau** in **Patrimoine net**. Both fresh1440×1000 and390×1000 contexts show English **Dec 2026 / Nov 2026 / Oct 2026** amid French text; the chart's actual axis formatter also produces **Jan / Feb / … / Dec**. Recent Activity shows **Jul 20 / Jun 30 / Jun 15**, also English-generated dates. The ledger-authored payees and descriptions are not the defect.

A reverse fresh1440 control (app en, browser fr-FR) shows **déc. 2026** in the Net Worth table and **20 juil.** in Recent Activity, surrounded by English UI. A matching fresh390 fr/fr-FR control renders the expected French labels. Expected: French labels in either browser when app fr is selected, and English labels in either browser when app en is selected. Numeric values already follow the selected app language: the same latest balance is30 950USD in French and30,950USD in English. No financial-value discrepancy is claimed.

Four contexts complete without page errors or additional write attempts. The narrow French-app/English-browser Net Worth crop was visually inspected. GraphQL GetLedgerJournal with time2026, directiveTypes[Transaction], limit8 returns200,total23 and dates2026-07-20,2026-06-30,2026-06-15,…, establishing the date-only input behind the first activity labels. The chart contains the expected twelve month-end date strings. This is display-language selection, not a changed period or timezone-day allegation.

Root: dashboard/src/features/reports/overview/components/net-worth-card.tsx:27–34 defines formatMonth with Intl.DateTimeFormat(undefined); its table calls it at212. Its chart axis callback separately constructs the same browser-default formatter at105. recent-activity-card.tsx:33–40 uses Intl.DateTimeFormat(undefined) for formatActivityDate, called in the desktop and narrow layouts at230/259. Both components already use useTranslations but omit its i18n language. NetWorthCard's chart option is memoized and must depend on the active language when repaired. Each card has one production caller, overview/index.tsx:162/187.

Pass the active application language explicitly through these month and activity-date formatters, and include it in chart memo dependencies so a language switch updates the chart too. Preserve the existing short-month formats, month/year versus month/day distinction, date parsing and invalid-date fallback. Reuse the localization approach of overview-utils.ts:345 formatPeriodMonth, already used by Money Movement, without replacing every short label with its long-month style or changing global browser/Intl settings. No dependency or API/schema change is needed.

Acceptance: the reproduced mismatched and matching locale combinations display dates in the app language at both widths; switching app language updates all three consumers while preserving point dates, amounts, selected period and activity links. Cover actual rendered table/activity text and chart formatter behavior with meaningful checks. Keep the existing short labels, zero/empty handling and invalid-date fallback; run dashboard checks on implementation. Live language switching and patched behavior remain unverified in this report.

Dedupe: all queues including done searched for date language, formatMonth, formatActivityDate and these card names. Full completed108 covers only Money Movement's local helper and its two callers; these are separately reproduced consumers outside that scope. Completed110 is the shared number formatter/hydration repair, and m22 covers its enumerated relative-time/calendar consumers. Existing139 is a single-point Net Worth symbol issue; coordinate edits to that card without duplicating its fix. Targeted histories leave NetWorthCard ataf5339de and RecentActivityCard ataf5339de/e63a78ee (pointer cursor only), with no newer date-language repair on fetched main.

Verified ignored evidence dashboard/tmp/qa-20260911-w3-loop/: overview-date-language-fr-en-US-{1440,390}.json/.png, overview-date-language-en-fr-FR-1440.json/.png, overview-date-language-fr-fr-FR-390.json/.png and overview-date-api.json. Screenshots cover the Net Worth table; JSON contains recent-activity text and chart axis results. All temporary contexts closed. No product fix, commit or shipping.

REST journal control with the same time/directiveTypes/limit/offset also returns200,total23 and exactly the same eight date strings. Verified ignored overview-date-rest.json supplements the GraphQL evidence.
