# Make native wheel selection emphasis follow the option Confirm will save

**Severity:** minor. **Estimate:** 30–45 minutes. Owner: mobile. A2: preference and draft-form selections give newcomers one consistent indication of the value they are about to confirm.

September 11, 2026, HEAD f1965988 / mobile 7538fb0d, io.beancount.ios 1.20260906.47, iPhone 17e / iOS 26.5, Expo MCP 8083, English, default large text, System theme with light simulator appearance. Authenticated production session; only local theme changes and discarded MiniMax budget drafts, no ledger mutation.

Repro: actual Home drawer -> Settings -> Theme with System selected. System starts centered, green and bold. Drag down inside the wheel from approximately (195,633) to (195,760), then wait for snapping to settle. Light is now centered in the selection rectangle, but System remains green/bold below it and Light has ordinary black text. Cancel preserves System. Terminate/relaunch (fresh PID 68731), verify Home, repeat actual drawer/Settings/Theme journey and drag: same mismatch. Confirm saves Light correctly. Reopen, drag upward to center System: Light remains green/bold above it. Confirm saves System; restoration verified in Settings.

Independent form corroboration from this run: actual Home -> Budget See all -> Add Your First Budget -> Monthly interval. Drag upward to Yearly. Monthly retains green/bold emphasis above the centered ordinary-text Yearly; Done correctly returns Yearly to the unsaved draft. Back/reopen restores Monthly. Never submitted the budget.

Expected: selected-option emphasis and the center rectangle identify the same pending value that Confirm submits. Actual: emphasis identifies the previously committed value until the modal closes, contradicting the pending selection. Submission itself is correct; do not describe this as incorrect persisted settings or budget data.

Root: mobile/src/components/picker/index.tsx:227–235 updates scrollY from native scroll and confirms wheelIndexAtOffset(scrollY.value, items.length). However, renderItem at242–268 computes isSelected from item.value === selectedValue, the unchanged committed prop, and applies selectedItemText (green, bold, larger) independently of the pending scroll position. The commented offset-based alternative at245 is not active. Only changing the callback dependency list will not make a Reanimated shared value cause React text styling to update.

Fix the shared Picker so the pending clamped wheel index drives selection emphasis as well as confirmation, using an appropriate animated row style or a bounded index-change update. Seed/reset from the committed selection when opening or changing the item set, preserve the existing native offset initialization, and do not reset an in-progress user scroll on every render. Cancel must leave the caller unchanged. Avoid adding a dependency or rewriting each caller separately.

Acceptance: after a settled scroll, exactly the centered pending row has selected styling and Confirm returns that row; old committed rows lose emphasis. Verify Theme System -> Light and back, and the Add Budget Monthly -> Yearly draft, in light/dark. Preserve untouched non-first confirmation, Cancel/reopen restoration, first/last bounds and reused item lists. Add a meaningful rendered selection/style assertion driven by a scroll update; the existing wheel-position helper tests do not exercise renderItem styles. Run owning-package checks. Android, physical-device and VoiceOver behavior remain unverified.

Blast radius: searched imports and Picker instances: Settings language/theme, Add Budget interval/currency, Open Account root/currency share this component. Transaction filters' PickerTarget type does not use this wheel. Only Theme and budget interval were exercised for this issue. There is no GraphQL/REST operation for local theme or pending wheel state; network/loading/denied-ledger conditions are independent. Bounded JS error capture was empty.

Dedupe/history: searched all open/done .pm markdown for wheel/picker highlight, bold, green, selected styling and stale selection. Read full w3/done/101.md and its independent native closeout: 7538fb0d fixes untouched confirmation and that still passes; this is residual visual feedback, not a regression of its confirmed-value behavior. git log -S on the renderItem comparison traces it to 182a07f1; latest picker commit 7538fb0d leaves styling unchanged. w3/154 is the account SectionList initial reveal, a different component and mechanism. No overlapping open item or newer main fix found.

Verified ignored evidence in mobile/tmp/qa-2026-09-11/: picker-theme-before.jpg, picker-theme-scrolled.jpg, picker-highlight-fresh-home.jpg, picker-theme-fresh-before.jpg, picker-theme-fresh-scrolled.jpg, picker-theme-confirm-light.jpg, picker-theme-restore-wheel.jpg, picker-theme-system-restored.jpg, budget-interval-scrolled.jpg, budget-yearly-confirmed.jpg, picker-highlight-js-errors.json. English/System/light restored. No product fix, commit or shipping.
