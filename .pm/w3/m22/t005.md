---
id: w3/m22/t005
title: Simplify the formatting changes
worker: worker3
status: todo
estimate: 20m
depends_on: [w3/m22/t004]
---

## Objective

Review only this milestone's implementation for redundant locale maps, loading state and formatting wrappers while preserving its behavior.

## Context

- Seven relative-time consumers and the calendar should share locale selection rather than repeat maps.
- The existing localization instance already owns language changes and failure handling.

## Steps

1. Run /simplify on the milestone diff.
2. Remove duplicated locale resolution or avoidable wrappers without introducing global mutable state.
3. Retain duration semantics, per-request isolation and existing lazy-loading behavior.

## Files

- Dashboard files changed by t001–t003 and t008

## Acceptance criteria

- [ ] There is one clear shared locale-selection path for these relative timestamps and the calendar, preserving upstream accessibility translations.
- [ ] No unnecessary abstraction or duplicate per-feature locale map remains.
- [ ] The review preserves observed behavior and the localization loading contract.

## Out of scope

Typed-date formatting/parsing policy, API timestamp changes, auth gates, manual
language/URL precedence repair027, social pagination, avatar delivery, new
dependencies, private data and real bank/ledger mutations.
