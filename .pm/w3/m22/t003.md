---
id: w3/m22/t003
title: Localize feed ledger-row and bank-sync timestamps
worker: worker3
status: todo
estimate: 25m
depends_on: [w3/m22/t001]
---

## Objective

Use the same relative-time behavior on the three remaining source-traced consumers without requesting private feed data or triggering bank operations.

## Context

- FeedTimestamp serves both ledger activity and blog cards.
- Dashboard sidebar ledger rows and BankItem.lastSync call the same default-English formatter.
- These consumers are source-traced blast radius; production bank/feed journeys were not exercised.

## Steps

1. Replace the three remaining direct calls, using each component's active application language.
2. Retain valid/invalid-date and absent-lastSync branches, source timestamp values and existing display conditions.
3. Exercise the components with synthetic fixtures for at least English and Chinese; do not initiate bank linking, sync or private-ledger actions.

## Files

- dashboard/src/features/ledger-list/components/feed-card.tsx
- dashboard/src/features/ledger-list/pages/dashboard-page/components/dashboard-sidebar.tsx
- dashboard/src/features/plaid/pages/plaid-connections/components/bank-item.tsx

## Acceptance criteria

- [ ] All three consumers follow active app language in local rendered fixtures.
- [ ] Feed invalid-date fallback and bank missing-sync state retain their behavior.
- [ ] No network mutation or private data is needed to verify the repair.

## Out of scope

Typed-date formatting/parsing policy, API timestamp changes, auth gates, manual
language/URL precedence repair027, social pagination, avatar delivery, new
dependencies, private data and real bank/ledger mutations.
