---
id: w3/m1/t002
title: Port budget selectors (grouping + daily-equivalent proration, no date-fns)
worker: worker3
status: done
estimate: 45m
depends_on: []
---

## Objective

Port the dashboard's `budget-utils.ts` pure logic into mobile selectors, preserving Fava semantics exactly: grouping by account+currency (newer directive supersedes even across cadence changes) and daily-equivalent proration for budget-per-interval.

## Context

- Source: `dashboard/src/features/ledger-data/budget/budget-utils.ts` (`parseBudgetAmount`, `groupBudgetEntries`, `calculateBudgetForInterval`) and `types.ts` (`BudgetEntry`, `BudgetGroup`, `BudgetHistoryEntry`).
- Mobile has **no date-fns** — replicate Monday-start weeks (`weekStartsOn: 1`), quarter boundaries, and leap-year day counts by hand, in the style of `mobile/src/common/series-util.ts`.
- Interval normalization: lowercase then strip trailing `ly` (`daily`→`day`); unknown intervals fall back to monthly.
- jest-lite requires **relative imports** for value modules (no `@/` alias).

## Steps

1. Create `mobile/src/screens/budget-screen/selectors/budget-selectors.ts` with the three exported functions + types.
2. Hand-roll the date helpers (interval start, next interval start, days-in-period) without date-fns.
3. Add a utilization selector for the Home panel (current-period actual ÷ current-period budget, sign-flipped for income budgets).

## Files

- `mobile/src/screens/budget-screen/selectors/budget-selectors.ts` (new)

## Acceptance criteria

- [ ] `groupBudgetEntries` groups by account+currency, drops malformed entries, picks the newest entry ≤ today.
- [ ] `calculateBudgetForInterval` reproduces dashboard results: monthly 310 → weekly 70; mid-period effective date → 160; mid-period cadence change → 470; negative income targets keep sign.
- [ ] No date-fns (or any new dependency) introduced; `yarn typecheck` passes.

## Out of scope

- Test port (t003); any UI.
