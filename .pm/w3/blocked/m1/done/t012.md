---
id: w3/m1/t012
title: Test coverage
worker: worker3
status: done
estimate: 45m
depends_on: [w3/m1/t010]
---

## Objective

Add meaningful mobile-side tests beyond the verbatim port (t003): the behavior m1 introduced that the dashboard suite doesn't cover.

## Context

- jest-lite: pure selectors only (no RN rendering); relative imports; limited matchers.
- Candidates: Home panel ranking selector (utilization desc, cap 3, income-budget sign flip, over-budget flag), time-span pill → `time` filter mapping, journal-response → `BudgetEntry` narrowing with malformed payloads, chart series alignment helpers (labels/actuals/budgets parallel arrays).

## Steps

1. Write tests asserting real behavior and failure modes (malformed directives, empty ledgers, >cap group counts).
2. `yarn test:unit` green.

## Files

- `mobile/src/screens/home-screen/selectors/__tests__/select-budget-panel.test.ts`
- `mobile/src/screens/budget-screen/selectors/__tests__/` (additions)

## Acceptance criteria

- [ ] Ranking, mapping, and narrowing behavior covered with failure-mode cases; no tautological or snapshot tests.
- [ ] `yarn test` passes.

## Out of scope

- E2E/simulator automation.
