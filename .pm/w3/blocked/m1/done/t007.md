---
id: w3/m1/t007
title: "BudgetGroupCard: stats, variance badge, progress, chart"
worker: worker3
status: done
estimate: 45m
depends_on: [w3/m1/t002, w3/m1/t004, w3/m1/t005, w3/m1/t006]
---

## Objective

Build the per-group card for the /budget page: account header with interval + currency badges, budget/actual/variance stats with an Above/Below/On-target badge, progress bar, and the budget-vs-actual chart. History stays a collapsed "History (n) ›" row (expansion ships in m2).

## Context

- Behavior reference: `dashboard/src/features/ledger-data/budget/budget-chart-card.tsx` — favorability logic (expenses: under is good; income budgets are negative and flip it), `progressPercent` clamping, `conversion: "units"`.
- One `useGetLedgerIntervalTotalsQuery` per card, `cache-first`, `interval` = group cadence lowercase, `time` from the page's pill selection.
- Styling: `getStyles(theme)` + `useThemeStyle`; progress via `mobile/src/components/progress/`; skeleton via `LoadingTile`; amounts via `amount-text`/`currency-util`.

## Steps

1. Create `components/budget-group-card.tsx` in `src/screens/budget-screen/` taking `{ group, time }`.
2. Derive stats via t002 selectors; render variance badge with semantic colors (success/error/muted tokens).
3. Wire `BudgetBarChartD3` with prorated budget series + actual series; chart empty state when no data.
4. Collapsed history row showing entry count (no-op tap for now; m2 wires expansion).
5. `LoadingTile` skeleton mirroring the card layout.

## Files

- `mobile/src/screens/budget-screen/components/budget-group-card.tsx` (new)

## Acceptance criteria

- [ ] Card shows budget / actual / variance with correct favorability for both expense and income (negative) budgets.
- [ ] Progress bar caps at 100% width and switches to error color when unfavorable.
- [ ] Loading shows a skeleton, not a spinner; query errors render a visible error state.

## Out of scope

- History expansion + delete (m2); add/update entry points (m2).
