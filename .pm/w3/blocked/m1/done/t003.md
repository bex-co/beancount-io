---
id: w3/m1/t003
title: Port dashboard budget-utils tests verbatim to jest-lite
worker: worker3
status: done
estimate: 30m
depends_on: [w3/m1/t002]
---

## Objective

Port the dashboard's budget-utils test suite to mobile's jest-lite runner as the semantic contract for t002 — these tests, not the implementation, define correctness of the date-fns-free port.

## Context

- Source suite: `dashboard/src/features/ledger-data/budget/__tests__/budget-utils.test.ts`.
- jest-lite constraints: only `toBe/toEqual/toBeCloseTo/toBeTruthy/toBeFalsy/toThrow` (+ `.not`) — rewrite other matchers (e.g. `toHaveLength(n)` → `expect(x.length).toBe(n)`); relative imports only for value modules.
- Runner picks up `**/__tests__/*.test.ts` under `src/`; run via `yarn test:unit` in `mobile/`.

## Steps

1. Copy each dashboard test case; adapt imports and matchers to jest-lite.
2. Keep case names and fixture values identical to the dashboard suite for future diffability.
3. Run `yarn test:unit`; fix t002's selectors (not the tests) on any mismatch.

## Files

- `mobile/src/screens/budget-screen/selectors/__tests__/budget-selectors.test.ts` (new)

## Acceptance criteria

- [ ] Every dashboard case is represented: grouping across cadence changes, malformed-directive drop, future-directive exclusion, 310→70 cadence conversion, 310→160 mid-period proration, →470 cadence-change composition, negative income targets.
- [ ] `yarn test:unit` green.

## Out of scope

- New mobile-only cases (m1 Test coverage task).
