---
id: w3/m2/t004
title: History expansion on BudgetGroupCard
worker: worker3
status: done
estimate: 30m
depends_on: []
---

## Objective

Make the collapsed "History (n) ›" row from m1 expandable: dated budget entries, newest first, current entry highlighted — the management surface delete (t005) hangs off.

## Context

- Data already on the group: `budgetHistory` (sorted ascending; reverse for display — dashboard `budget-history-table.tsx` parity, current entry = newest ≤ today).
- Row: date (mono), interval badge, amount string (e.g. `300 USD`), right-aligned; delete affordance added in t005.
- m1 card renders the count row as a no-op tap — this task adds the expand/collapse state and rows.

## Steps

1. Expand/collapse state on the card; chevron rotates; rows render from `budgetHistory` reversed.
2. Highlight the current entry; respect `rowMinHeight` spacing tokens.

## Files

- `mobile/src/screens/budget-screen/components/budget-group-card.tsx` (or a `budget-history-rows.tsx` extraction)

## Acceptance criteria

- [ ] Tap toggles history; rows show date/interval/amount newest-first with the active entry visually distinct.
- [ ] Single-entry groups still default collapsed.

## Out of scope

- Delete (t005).
