---
id: w3/m23/t005
title: Test dismissal and asynchronous completion
worker: worker3
status: todo
estimate: 55m
depends_on: [w3/m23/t004]
---

## Objective

Prove the repairs through actual dialog interactions and preserve successful, empty, busy and failure behavior.

## Context

- Pinned Radix prevents default restoration and focuses its registered trigger; tests must not replace the real focus behavior with a stub.
- The original reproduction waits beyond close animation/deferred unmount focus.
- Account-row tests must also wait for the opening menu to unmount before dismissal; immediate closure can hide the missing return target.

## Steps

1. Add focused real-component keyboard tests for Escape, Cancel and Close from Export, each Budget opener and all three Account dialogs.
2. Test successful populated/empty export closure and a local successful first-budget transition that removes its original opener.
3. Cover card Add Entry, nested calendar/select dismissal, account deep links/onboarding, removed row-action fallbacks and validation/server errors that keep the form open.
4. Run dashboard format:check, lint, test and build; reproduce the repaired journeys on the local client with isolated data and repeat at1440/390.

## Files

- dashboard/src/features/journal/components/**tests**/
- dashboard/src/features/ledger-data/budget/**tests**/
- dashboard/src/features/ledger-data/accounts/**tests**/

## Acceptance criteria

- [ ] Tests fail when focus returns to BODY, the wrong opener or a disconnected element.
- [ ] Successful completion, empty results, busy/error behavior and permissions preserve their contracts.
- [ ] All dashboard gates pass; any unavailable live/fixture scenario is reported accurately.

## Out of scope

New test suites that mirror implementation, production budget writes and unrelated coverage expansion.
