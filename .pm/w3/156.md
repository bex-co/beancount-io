# Keep the last-posting automatic-balance toggle available when off

**Severity:** minor. **Estimate:** 30–45 minutes. Owner: mobile. A2: a newcomer can recover automatic balancing without rebuilding a transaction draft.

September 11, 2026, authenticated native development client io.beancount.ios 1.20260906.47, local HEAD f1965988, Expo MCP8083, sole iPhone17e/iOS26.5, English/SystemLight/default large text. Public open_ledger/minimax, MUSD. Unsaved drafts only; never pressed Done or sent an entry mutation.

1. Actual Transactions tab -> plus -> New Transaction. The second posting defaults to Expenses:CostOfRevenue with an auto control.
2. Tap auto. It disappears, leaving no control for returning that same posting to automatic mode.
3. Enter 1.25 in the first Cash posting. The second remains zero, remainder is +1.25MUSD and Done is disabled. Those arithmetic/validation states are correct for manual mode; the failure is the missing way to restore automatic mode.
4. Fresh PID56655, settled Home verified before Transactions/plus, repeats this exact sequence. Initial PID52322 also loses the control after manually flipping the automatic posting's sign: both postings become -1.25, remainder -2.50, Done disabled.
5. Working control before manual override: first +1.25 automatically produces second -1.25; reversing the first sign reverses the second and keeps zero remainder. Native recovery control: add a third posting, then swipe/remove that new third posting; the existing second posting becomes automatic again, shows -1.25 and auto, and Done becomes enabled. No submission occurred. This workaround proves the model can restore automatic mode but requires an unrelated structural change.

Expected: the last posting exposes an accessible, discoverable control in both manual and automatic states. Explicitly enabling automatic mode recomputes its counteramount; turning it off preserves the current value. Editing its amount/sign can still intentionally switch to manual mode. Actual: the only toggle removes itself when switched off.

Root: mobile/src/screens/multi-postings-transaction/multi-postings-transaction-screen.tsx:315–326 renders onToggleAuto only under isLast && posting.isAuto. The handler at551–552 calls toggleLastPostingAuto. postings-utils.ts:127–133 already flips either direction and reapplies balancing; __tests__/postings-utils.test.ts:104–115 explicitly covers enabling from manual mode. updatePostingAmount:110–124 intentionally clears isAuto for last-row edits. addPosting/removePosting:135–152 explain the verified workaround. The model's reverse transition is unreachable from its intended UI control.

Fix the final row's control so both states remain available, with localized action/name and accessibility state. Keep amount input, sign override, row identity/account, add/remove semantics and validation intact; earlier rows must not acquire an automatic toggle. Use existing styling/dependencies. Add meaningful rendered interaction coverage: auto -> manual -> auto, edit/sign override -> restore, populated and zero drafts, and a third row becoming last after removal. Assert recalculated values and actionable control state, not only a call to the helper. Recheck native default and large text, keyboard access and light/dark. Patched behavior, VoiceOver audio, Android, saving and receipt entry are unverified.

Blast radius: searched toggleLastPostingAuto/onToggleAuto; one production consumer, PostingRow within MultiPostingsTransactionScreen, reached by Home/Transactions add and receipt prefill. Only actual Transactions add was reproduced here. Fresh and populated states covered; metadata denial/network failure are not implicated. Bounded JS error capture empty. Source history traces the conditional to9c240d2e (default multi-posting add flow); current source retains it after bc151c5c accessibility additions.

Dedupe: searched all open/done queues for automatic posting, auto-balance, toggleLastPostingAuto and re-enabling auto. w3/m34 owns exact decimal arithmetic/serialization and must coordinate shared-screen edits; this is independently reproducible with exact two-decimal1.25 and needs no numeric-model change. w3/149 owns amount-field accessible names; w3/done/m26 owns dashboard transaction inference. None owns this hidden reverse control. No product fix or shipping.

Verified ignored evidence under mobile/tmp/qa-2026-09-11/: entry-sign-initial.jpg, entry-sign-flipped.jpg, entry-sign-unbalanced.jpg, entry-auto-fresh-home-settled.jpg, entry-auto-off-repeat.jpg, entry-auto-off-populated.jpg, entry-auto-off-type.log, entry-auto-workaround-remove.jpg, entry-auto-restored-control.jpg, entry-auto-js-errors.json. A scratch swipe attempt stopped at its keyboard-state guard and timed out in XCTest teardown; excluded from evidence. After visiting Payee and returning to dismiss focus, the actual swipe/remove recovery passed. All drafts discarded with Back afterward.
