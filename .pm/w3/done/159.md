# Give the shared native Back control a spoken name and button role

**Severity:** minor. **Estimate:** 35–50 minutes. Owner: mobile. A2/A3: screen-reader users can identify the action that exits account details and entry forms.

September11,2026, local HEAD f1965988/mobile7538fb0d, development client io.beancount.ios1.20260906.47, production API, authenticated public open_ledger/minimax, ExpoMCP8083, sole iPhone17e/iOS26.5, English/SystemLight/default large text. Read-only navigation and a blank discarded form; no production mutation.

1. Actual Accounts tab -> Liabilities:Current:ConvertibleRedeemablePreferredShares. Inspect the visible Back control with native XCTest metadata. It is hittable/enabled at x20,y50.67, but has type Other (XCUIElementTypeOther=1), empty identifier/value, and label consisting only of Unicode U+F229 (an icon-font private-use glyph).
2. Terminate/relaunch, PID66112, verify settled Home; actual Accounts -> Assets:NonCurrent:Goodwill. The same header metadata repeats. The arrow is visible and tapping its position successfully returns to Accounts, so this is naming/role failure rather than broken navigation.
3. Actual Transactions -> plus -> New Transaction. Back again has the same Other/U+F229 metadata. Working control in the same native navigation bar: Done has type Button (9), label Done, enabled false for the blank draft. The New Transaction title is readable static text. Back discards the blank draft successfully.

Expected: Back is exposed as a button with a localized meaningful name, rather than a glyph. Actual: native accessibility offers an icon character without the action or button semantics. VoiceOver audio, focus sequence and an Accessibility Inspector audit were not run; do not claim a recorded spoken utterance or an end-to-end assistive-technology pass.

Root: mobile/app/(app)/_layout.tsx:12–29 defines DefaultHeaderLeftBack as a Pressable with router.back, padding and hitSlop but neither accessibilityRole nor accessibilityLabel. Its only child is Ionicons. The App stack installs it at49, so the child glyph becomes the accessible name. The static guardrail mobile/src/__tests__/accessibility-labels.test.ts:13–14 and111–113 scans only src/screens and src/components; this app/ component is omitted. Its allowlist is empty, so this is a scan-coverage gap, not an approved exception.

Fix the shared control with a localized Back label, button role, and decorative-icon handling that avoids a duplicate glyph announcement. Preserve tintColor, directionalIcon/RTL, hit area, visual header and router.back behavior. Respect the explicit hook-free header renderer contract at _layout.tsx:9–11: obtain translations in the owning component and pass the label to a proper rendered component, or another safe existing pattern; do not call hooks conditionally through header render callbacks. Extend meaningful guardrail coverage to this app-level control and verify removing its label causes the check to fail. Do not broadly rewrite navigation or change every screen's header separately.

Acceptance: fresh account-detail and New Transaction native metadata reports Back as a named button; Done's label/disabled state remains correct. Recheck a Settings or file-editor screen using the shared header, light/dark and RTL direction/localized label. Verify actual Back navigation and unsaved-change guards remain intact. Add behavioral prop/guardrail coverage and run owning-package checks; a final VoiceOver focus/activation check remains required for a claimed spoken-accessibility pass. No dependency is needed.

Blast radius: searched DefaultHeaderLeftBack references; one definition and the App stack assignment. Stack defaults cover account detail, forms, Settings, notifications, commit/file screens unless a screen supplies its own headerLeft; explicit Cancel/Reset or headerHidden flows are separate. Only the two account details and New Transaction were inspected natively. Initial/cached data, denied/expired/network errors do not determine this static control. Bounded JS error capture empty.

Dedupe/history: searched all open/done queues for DefaultHeaderLeftBack, Back labels/roles and header accessibility. Reviewed full completed w4/m7 DoD, daily-read t002 and remaining-read t004; its closeout explicitly lacks final Inspector/VoiceOver proof. This is residual shared-header coverage, not a new claimed regression after a verified native pass. bc151c5c labels screen controls but leaves this header without semantics; current file retains the definition from182a07f1 through later route additions. w3/111 concerns recurring Switch naming,149 posting input values,151 large-text header clipping; none fixes this component. No current-main fix found.

Verified ignored evidence in mobile/tmp/qa-2026-09-11/: account-header-elements-ax.log, header-back-fresh-ax.log, header-back-form-ax.log, header-back-home-settled.jpg, header-back-fresh-account.jpg, header-back-form-control.jpg, header-back-js-errors.json. Targeted public-screen metadata only; no private hierarchy included. No product fix, commit or shipping.

---

**Resolved 2026-09-11** — fixed and shipped in `89cc7dd7` (`mobile`), with the package's full check gate green. Triage confirmed the reported root cause against current code before the fix. Helpers are unit-tested; the spoken VoiceOver result is NOT verified (this package has no render harness) and still wants a simulator pass.
