---
id: w3/m4/t006
title: Test coverage
worker: worker3
status: done
estimate: 30m
depends_on: [w3/m4/t004]
---

## Objective

Cover the behavior this milestone shipped: the published output contract, and the conformance check's own pass/fail/skip logic.

## Context

- ADR 0007 D9's three properties are already guarded (`mcp-route-methods.test.ts`, `scope-enforcement.test.ts`, `error-middleware.test.ts`). This adds the D8 half.
- The output-schema test should assert against a real `tools/list` response, not against the descriptor table — the question is what a client receives.
- The conformance script's value is that a failure names the right check, so a test that feeds it a deployment failing exactly one check and asserts which one is the test worth having.

## Steps

1. Assert `tools/list` publishes an `outputSchema` for all seven tools and that a success result validates against it.
2. Assert the conformance check reports a skip (not a failure) when no credential is supplied, and names the failing check when one fails.

## Files

- `src/features/ai-agent/**/__tests__/`, plus a test beside the new script

## Acceptance criteria

- [ ] Each new test fails against the code as it stood before its change.
- [ ] No tautological or snapshot-everything assertions.

## Out of scope

- Integration tests requiring a live deployment.
