---
id: w3/m4/t004
title: Adoption surface
worker: worker3
status: done
estimate: 20m
depends_on: [w3/m4/t001, w3/m4/t002, w3/m4/t003]
---

## Objective

Check that this milestone's changes are discoverable and usable on every surface they expose — the MCP endpoint is agent-facing, so both Claude Code and Codex are the audience.

## Context

- Surfaces: `backend-cluster/backend-v2/README.md` and its `CLAUDE.md`, the root `README.md` and `CLAUDE.md` package tables, and the `AGENTS.md` symlinks (root `CLAUDE.md` compatibility rules).
- `backend-v2/CLAUDE.md` already points at ADR 0007 from its MCP line; confirm that pointer still resolves and that the new script and docs are reachable from it.
- Agent-facing check: the connection instructions must work identically for Claude Code and Codex — same endpoint, same credential requirement, same tools.

## Steps

1. Sweep the surfaces above; update what drifted.
2. Run `python3 scripts/check-agent-guidance.py` if any instruction file or symlink changed.
3. File follow-ups for drift rather than silently diverging.

## Files

- `backend-cluster/backend-v2/README.md`, `backend-cluster/backend-v2/CLAUDE.md`, root `README.md` / `CLAUDE.md`

## Acceptance criteria

- [ ] Every listed surface reflects the MCP endpoint and its tooling, or a follow-up is filed.
- [ ] `python3 scripts/check-agent-guidance.py` passes.

## Out of scope

- Restructuring the package tables beyond what this milestone changed.
