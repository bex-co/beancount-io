---
id: w3/m4/t007
title: Closeout
worker: worker3
status: done
estimate: 20m
depends_on: [w3/m4/t006]
---

## Objective

Close m4 once its definition of done actually holds — not merely when the code is written.

## Context

- DoD: see `w3/m4/README.md`.
- The DoD is satisfiable entirely in-repo: the `outputSchema` and documentation checks need no deployment, and the conformance script is verified against a local `yarn dev`. The three out-of-repo items in the milestone's `## Out of repo scope` are **not** part of this DoD and must not block it.
- Run `/pm done w3/m4/t007` last — completing it triggers the milestone move to `w3/done/m4/`.

## Steps

1. Verify the DoD end-to-end, including running the conformance script against a local `yarn dev`.
2. Set remaining task statuses `done`, move task files to `done/`, mark table rows `— **DONE**`, set `**Status:** done`.
3. Move `w3/m4/` → `w3/done/m4/`; check `- [x] m4` in `w3/README.md`.

## Files

- `.pm/w3/m4/**`, `.pm/w3/README.md`

## Acceptance criteria

- [ ] DoD verified, not just code-complete; board state consistent in all three places.

## Out of scope

- The operator actions in `## Out of repo scope`.
