---
id: w3/m23/t007
title: Return Open Account to its route opener
worker: worker3
status: done
estimate: 40m
depends_on: []
---

## Objective

Return keyboard focus to the Accounts header after dismissing its route-driven Open Account form.

## Context

- Header Open Account/New adds action=open-account and opens a controlled dialog without a trigger association.
- All three dismissal paths lose focus at both widths after transitions settle; action-query cleanup already works.
- The Overview empty-ledger link and direct bookmarks reach the same dialog with no connected original opener on Accounts.
- Inbox076 owns the separately reproduced Cancel draft-reset bypass in this same dialog; coordinate that caller cleanup without counting it as focus work.

## Steps

1. Register or explicitly capture the persistent Accounts header link and pass an appropriate return target into OpenAccountDialog.
2. Preserve route-driven open state and existing action-query cleanup, form resets and write permission.
3. On close, focus the connected originating header link; use that header as fallback for direct/onboarding arrivals.
4. Keep focus inside the form on validation/server errors and preserve the return target across successful cache refresh.

## Files

- dashboard/src/features/ledger-data/accounts/index.tsx
- dashboard/src/features/ledger-data/accounts/open-account-dialog.tsx
- dashboard/src/features/reports/overview/components/empty-ledger-setup.tsx

## Acceptance criteria

- [ ] Escape, Cancel and Close return focus to Open Account/New after animation; Enter reopens it.
- [ ] Direct action bookmarks and the onboarding link have a connected persistent return target.
- [ ] URL cleanup, permission gates, defaults, reset and cache refresh preserve their existing behavior.

## Out of scope

m24 date parsing, production account creation and a shared Dialog override.
