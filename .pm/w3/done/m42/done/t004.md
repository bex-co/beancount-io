---
id: w3/m42/t004
title: Simplify the date pattern wiring
worker: worker3
status: done
estimate: 15m
depends_on: [w3/m42/t003]
---

## Objective

Run `/simplify` over the shared picker, its utils and the touched call sites, with zero behavior change.

## Context

- The hint and the parser must stay derived from one source; a "simplification" that reintroduces a separately-maintained hint string would recreate this defect. Guard against that specifically.
- Watch for locale-resolution logic duplicated between the calendar (m22's work) and the text pattern — they should share one resolved locale.

## Steps

1. Run `/simplify` scoped to the changed files.
2. Confirm the t001 and t002 acceptance outcomes are unchanged.

## Files

- The files touched by t001, t002 and t003.

## Acceptance criteria

- [ ] Parsing, hints and rejection behavior are identical before and after in every tested locale.
- [ ] Hint and parser still derive from one source.
- [ ] `yarn format:check && yarn lint && yarn test && yarn build` pass in `dashboard/`.

## Out of scope

New behavior, and refactoring the calendar beyond shared locale resolution.

## Verification record

Simplify pass 2026-09-15: removed the dead long-format branch of `formatDate` (all four call sites passed `short=true`; the branch also carried a hardcoded `en-US` `toLocaleDateString`, now gone) and dropped the `short` parameter. No other extraction: hint and parser already derive from the single `displayPattern`, and text pattern and calendar already share the one `useDateLocale()` resolution. Parsing, hints, and rejection behavior identical before and after (43 picker tests green on both sides); `yarn format:check`, `yarn lint`, `yarn test` (351 files / 4034 passed, 1 skipped), and `yarn build` all green.
