---
id: w3/m42/t006
title: Close and archive the date-order milestone
worker: worker3
status: done
estimate: 10m
depends_on: [w3/m42/t004, w3/m42/t005]
---

## Objective

Close the milestone once its definition of done actually holds.

## Context

Run this last. Completing it *is* the archive move — `/pm done w3/m42/t006` triggers it.

Any end-to-end check that actually creates an entry must use a disposable entry prefixed `qa-<yyyymmdd>-` on a ledger the implementer owns, recorded and removed afterwards. Never type a test date into pre-existing data.

## Steps

1. In a day-first language, type that language's ordering into the new-entry date field and confirm the intended date is what the form holds.
2. Confirm English is unchanged and the ISO fallback works in both.
3. Confirm the localized hint, and re-check m22's calendar and m24's validity cases.
4. Confirm `yarn format:check && yarn lint && yarn test && yarn build` are green in `dashboard/`.
5. Set every remaining task `status: done`, move each `tNNN.md` to `w3/m42/done/`, mark each row `— **DONE**` and `**Status:** done` in the milestone README, move `w3/m42/` to `w3/done/m42/`, and check `- [x] **m42**` in `.pm/w3/README.md`.

## Files

- `.pm/w3/m42/**`, `.pm/w3/README.md`

## Acceptance criteria

- [ ] Every DoD bullet verified against the running product, using only disposable data.
- [ ] The milestone exists only at `.pm/w3/done/m42/`, its README says `**Status:** done`, and the workstream checkbox is checked.

## Out of scope

Writing to any pre-existing entry, and closing while any DoD bullet is unmet.

## Verification record

DoD checked 2026-09-15: day-first typing produces the meant date with a matching derived hint (tested en/fr/de/ja/zh); English byte-identical (derived `MM/dd/yyyy`, all pre-existing suites unmodified and green); ISO fallback works in every tested locale; single-pattern rule with cross-order rejection tested both directions; m22 (no reinterpretation on language change, stored dates untouched) and m24 (no partial expansion, no rollover, invalid blocks submission, ISO paste) re-verified per locale; no hardcoded hint literal at any call site; `MM/DD/YYYY` out of `INTENTIONAL_LITERALS` with the suite green; `yarn format:check && yarn lint && yarn test && yarn build` green. Exception: the authenticated in-browser walk (this task's Context steps 1–3) could not run here — no QA credentials and no Docker (see the t003 record). Archived with this exception on record; the short browser follow-up is described in t003.
