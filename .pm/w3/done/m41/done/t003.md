---
id: w3/m41/t003
title: Verify the delete-confirmation adoption surface
worker: worker3
status: done
estimate: 15m
depends_on: [w3/m41/t002]
---

## Objective

Confirm the guard is real on all three surfaces that open this dialog, and that nothing in the repo documents a one-click delete.

## Context

- Callers: `features/journal/pages/journal-page.tsx`, `features/reports/account/index.tsx`, `features/reports/overview/components/recent-activity-card.tsx`.
- The delete mutation is also reachable through REST and MCP. This milestone adds a **client-side** confirmation only; an API-level change would fall under the repo's REST/GraphQL/MCP parity rule and is deliberately not in scope. Record that explicitly so the parity gate is not misread.
- Check `dashboard/CLAUDE.md` and any skill or doc that describes deleting entries from the dashboard.

## Steps

1. Open the dialog from each of the three callers and confirm the guard appears, at 1440 and 390.
2. Confirm a read-only context still shows no Delete.
3. Grep the repo, including `skills/`, for text describing entry deletion; flag drift as follow-up rather than editing unrelated packages.

## Files

- `dashboard/CLAUDE.md` (only if it describes this behavior)

## Acceptance criteria

- [ ] All three callers show the confirmation.
- [ ] Read-only contexts are unchanged.
- [ ] A note records that no REST/GraphQL/MCP surface changed and why.
- [ ] No doc or skill still implies a one-click delete.

## Out of scope

Adding confirmations to the REST or MCP surfaces, and editing other packages.

## Verification record

- Callers: `features/journal/pages/journal-page.tsx:362`, `features/reports/account/index.tsx:284`, and `features/reports/overview/components/recent-activity-card.tsx:294` all render the same `EntryContextDialog`, so the central guard covers all three by construction (page-level suites mock the dialog away, so behavior is asserted at the component level per the task brief).
- Read-only: the `canWrite` gate is unchanged; covered by test ("offers no confirmation to read-only users").
- Docs/skills grep (`skills/`, `docs/`, dashboard README/CLAUDE.md) for entry-deletion copy: no doc or skill describes dashboard entry deletion, so nothing implies a one-click delete; no doc edits needed.
- Parity: client-side confirmation only. No REST, GraphQL, or MCP surface changed — no new input, result, side effect, authorization, or failure behavior on any API — so the parity gate is unaffected by design.
- Browser: authenticated in-browser verification was not possible in this environment (no QA credentials on disk, no Docker for a local stack, and every ledger route redirects signed-out sessions to login). The confirmation path is verified through component tests exercising the real Radix dialog behavior (open/cancel/Escape/focus-return), the same evidence standard m23 accepted. Follow-up for anyone with QA access: open the dialog from each caller at 1440 and 390, cancel once (nothing deleted, focus back on Delete), and confirm once on a disposable `qa-<yyyymmdd>-` entry.
