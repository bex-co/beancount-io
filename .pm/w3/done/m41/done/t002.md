---
id: w3/m41/t002
title: Localize the confirmation copy in all 15 locales
worker: worker3
status: done
estimate: 30m
depends_on: [w3/m41/t001]
---

## Objective

Add the confirmation's title, body, confirm-label and cancel-label to every locale file, translated rather than copied from English. The body must say plainly that the entry will be removed from the ledger and that the app offers no undo.

## Context

- The dashboard supports 15 languages; `src/i18n/config.ts` is the canonical list and `src/test/translations.test.ts` checks locale shape.
- Journal strings live in `dashboard/src/features/journal/locales/`; `journal.entryDeletedSuccess` is the existing neighbour.
- Match the tone of the existing destructive copy in `budget/delete-budget-dialog.tsx` so the app reads consistently.
- Do not overstate: the deletion is committed to git history, so the copy should say it cannot be undone **from the app**, not that the data is unrecoverable anywhere.

## Steps

1. Add the four keys to the English journal locale.
2. Add matching translated keys to the other 14 locale files.
3. Run the translation shape test.

## Files

- `dashboard/src/features/journal/locales/*.ts` (all 15)

## Acceptance criteria

- [ ] All four keys exist in all 15 locale files with matching interpolation variables.
- [ ] `src/test/translations.test.ts` passes.
- [ ] No English string is left in a non-English locale file.
- [ ] The copy does not claim the data is permanently unrecoverable.

## Out of scope

Rewording existing journal strings, and the confirmation's behavior (t001).

## Verification record

Added `journal.entryDeleteTitle`, `journal.entryDeleteDescription` (with `{location}`), `journal.entryDeleteConfirm`, and `journal.entryDeleteCancel` to all 15 files in `dashboard/src/features/journal/locales/`, translated in each (no English carried over, no TODO markers). The body says the entry is removed from the ledger and cannot be undone "from the app" — never claiming unrecoverability from git history. `src/test/translations.test.ts`: 14/14 pass, including cross-language key/variable consistency and the no-unused-keys check.
