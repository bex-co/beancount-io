---
id: w3/m41/t004
title: Simplify the confirmation wiring
worker: worker3
status: done
estimate: 10m
depends_on: [w3/m41/t003]
---

## Objective

Run `/simplify` over the changed dialog and locale additions, with zero behavior change.

## Context

- If this milestone's confirmation and `budget/delete-budget-dialog.tsx` end up as near-identical wrappers around the shared primitive, consider one small shared destructive-confirm component — but only if both call sites genuinely collapse into it without extra props to paper over differences.
- Do not weaken the focus handling introduced in t001.

## Steps

1. Run `/simplify` scoped to the changed files.
2. Confirm the t001 acceptance outcomes are unchanged.

## Files

- The files touched by t001, t002 and t003.

## Acceptance criteria

- [ ] Confirmation behavior, copy and focus order are identical before and after.
- [ ] `yarn format:check && yarn lint && yarn test && yarn build` pass in `dashboard/`.

## Out of scope

Refactoring the other confirmation dialogs beyond a proven shared extraction, and any behavior change.

## Verification record

Simplify pass 2026-09-15: no shared destructive-confirm extraction — the entry confirmation (nested, focus-return ref, single `{location}` interpolation, no internal data fetch) and `budget/delete-budget-dialog.tsx` (standalone, target-driven open state, internal sha lookup, four interpolations) would need roughly nine props papering over differences, and all four existing precedents inline the AlertDialog primitives. Only change: corrected a stale fallback comment. Behavior, copy, and focus order identical; `yarn format:check`, `yarn lint`, `yarn test` (351 files / 4001 passed, 1 skipped), and `yarn build` all green.
