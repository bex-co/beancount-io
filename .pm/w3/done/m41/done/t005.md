---
id: w3/m41/t005
title: Test the confirmation across all three dialog callers
worker: worker3
status: done
estimate: 45m
depends_on: [w3/m41/t003]
---

## Objective

Prove the guard holds where it matters: the mutation must not fire without confirmation, from any caller, by pointer or keyboard.

## Context

- The strongest assertion is on the mutation itself — spy on the delete mutation and assert it is **not** called when Delete is clicked and the confirmation is cancelled or dismissed, and called exactly once when confirmed.
- Existing suites: `dashboard/src/features/journal/components/__tests__/`, plus the account and overview callers' own tests. Note that some page-level suites mock the dialog away, so the behavioral assertions belong with the component.
- Cover: cancel, Escape, confirm, double activation, and the read-only case where Delete is absent.

## Steps

1. Add component tests asserting the mutation is not called on cancel/Escape and called once on confirm.
2. Add a double-activation test.
3. Assert focus returns to the Delete button after cancel.
4. Confirm each test fails with the confirmation removed.

## Files

- `dashboard/src/features/journal/components/__tests__/entry-context-dialog*.test.tsx`
- tests adjacent to the account and overview callers

## Acceptance criteria

- [ ] Removing the confirmation makes a test fail.
- [ ] Tests assert on the delete mutation, not merely on rendered text.
- [ ] Cancel, Escape, confirm, double activation and read-only are all covered.
- [ ] `yarn test` passes in `dashboard/`.

## Out of scope

Exercising a real deletion against production, and coverage for unrelated dialog behavior owned by m31.

## Verification record

Extended `dashboard/src/features/journal/components/__tests__/entry-context-dialog.test.tsx` with 7 tests asserting on the delete mutation: confirmation opens without mutating; cancel and Escape mutate nothing, keep the dialog open, and refocus Delete; confirm deletes exactly once with `{ledgerId, entryHash, sha256sum}`; rapid double activation issues one deletion; deletion failure keeps the confirmation open with an error toast; read-only users see no Delete and no confirmation. Mutation check: with the confirmation reverted, 6 of the 7 new tests fail (the read-only guard passes either way, as intended). File: 14/14 green.
