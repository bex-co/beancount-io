---
id: w3/m5/t005
title: Adoption surface
worker: worker3
status: done
estimate: 20m
depends_on: [w3/m5/t003, w3/m5/t004]
---

## Objective

An MCP client's view of this server changes in this milestone — it gains a capability. Check that is discoverable everywhere the server is described.

## Context

- `backend-cluster/backend-v2/README.md` "Connecting an MCP client" documents seven tools and says nothing about resources.
- Root `README.md` has a "Coding agent (MCP)" section with the same claim.
- `backend-cluster/backend-v2/CLAUDE.md` points at ADR 0007 and ADR 0008.
- Agent-facing check: the behaviour must hold for both Claude Code and Codex — a resource-capable server should degrade cleanly for a host that surfaces no resources.

## Steps

1. Update both READMEs to describe resources alongside tools.
2. Confirm `yarn mcp:conformance` still passes; extend it if the capability deserves a check.
3. Run `python3 scripts/check-agent-guidance.py` if any instruction file changed.

## Files

- `backend-cluster/backend-v2/README.md`, root `README.md`, `backend-cluster/backend-v2/CLAUDE.md`

## Acceptance criteria

- [ ] Both READMEs describe the resource surface; no doc still implies tools are the only primitive.
- [ ] `check-agent-guidance.py` passes.

## Out of scope

- Documenting templates that do not exist yet.
