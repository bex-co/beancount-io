---
id: w3/m5/t007
title: Test coverage
worker: worker3
status: done
estimate: 30m
depends_on: [w3/m5/t005]
---

## Objective

Cover what this milestone shipped: the exemption split, the three guards, and the resource layer's authorization.

## Context

- The guards from t003 are themselves the coverage for t001/t002 — assert they fail against the defect, not merely that they pass now.
- The resource test that matters is the revocation one: a read refused on the call *after* access is removed. That is the property ADR 0007 D5 exists for, and a resource layer is exactly where it would get skipped.
- `src/features/ai-agent/api/__tests__/mcp-revocation.test.ts` is the model for tool-side revocation; mirror its shape.

## Steps

1. Assert each guard fails against its target defect and passes after the fix.
2. Assert `resources/read` is refused on the next call after a grant is revoked.
3. Assert the tool list did not change.

## Files

- `src/server/api/__tests__/`, `src/features/ai-agent/api/__tests__/`

## Acceptance criteria

- [ ] Each new test fails against the code as it stood before its change.
- [ ] No tautological or snapshot-everything assertions.

## Out of scope

- Integration tests needing a live deployment.
