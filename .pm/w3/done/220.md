# Expose tappable transaction rows as named native buttons

**Minor · 40–55m · mobile.** A2: a newcomer using assistive technology should be able to identify transaction rows as actions that open details, with a meaningful name instead of decorative icon text.

Environment: September 13, 2026, HEAD84231f9c, native io.beancount.ios1.20260906.47(47), Expo57/RN0.86, iPhone17e/iOS26.5, English/default large/System light. Dedicated QA session against https://beancount.io; public read-only open_ledger/alphabet. No ledger or star mutation.

Repro: actual Home drawer → Browse ledgers → Explore → search alphabet → open its public row → Reports → scroll to Recent Transactions. The June30,2026 Q2 2026 Expenses row is a generic native `Other` element, labelled `U+F1C6, Q2 2026 Expenses, $26,560,000,000.00`. Tap it: the correct Transaction detail opens, showing Income tax provision and the two opposite USD postings. Terminate/relaunch from PID89049 to8080, wait for settled Home and dismiss the development banner, then use the actual Transactions tab. The first row again exposes `Other, {{0.0,173.3},{390.0,64.0}}` with the same label. Other transaction rows are also Other; glyph-backed rows include private-use icon characters, while the Google-logo revenue row has readable text but still lacks the button role. Tap the first row at(180,205): the same correct detail opens.

Working control: on the fresh Transactions screen, Filters is a native `Button`, label `Filters`, at{{345.7,107.3},{16.0,16.7}}. The inspector therefore distinguishes the expected role. Public getLedgerJournal returns entry63e37c79d4a02c7cfaa844f0a27cd4e8, date2026-06-30, payeeQ2 2026 Expenses, narrationIncome tax provision, Expenses:Income-Tax +26560000000.00USD and Assets:Current:Cash-And-Equivalents −26560000000.00USD, matching both opened details. This establishes native metadata and touch navigation, not a recorded VoiceOver utterance or rotor failure.

Root: `mobile/src/screens/transactions-screen/entry-row/index.tsx:164–172` conditionally wraps an actionable row in TouchableOpacity with onPress/activeOpacity, but no accessibilityRole or explicit accessible name. Its content at130 includes AccountTypeIcon; that component's fallback at`src/components/account-type-icon/index.tsx:131–136` renders Ionicons without excluding the decorative glyph from accessibility. The implicit grouped name incorporates that glyph. The no-onPress branch at176 is a plain View and must remain noninteractive.

Fix the actionable EntryRow wrapper with the native button role and an explicit localized accessible name/value built from the full displayed payee/narration, amount/currency and meaningful pending state where applicable. Exclude decorative glyphs from that row's announcement without discarding any meaningful transaction information. Keep the full-row tap target, correct entry identity, amount formatting, brand/fallback visuals and noninteractive branch. Do not globally turn AccountTypeIcon or every generic pressable into a button. No API change or dependency is needed.

Callers: TransactionsScreen238; Home RecentTransactionsCard80; Reports AccountTransactionsCard118; MerchantDetailScreen575. Reports and Transactions were freshly exercised for this finding; Home and merchant detail are traced callers. AccountDetail uses the separate AccountEntryRow and is outside this repair. Preserve skeleton/empty/error states and do not label static directives as actionable merely because they share row content. Pending transactions, missing payee/narration fallback, logo fallback, localized names, RTL and noninteractive rows need focused regression coverage; this public native repro uses cleared transactions only.

Acceptance: both actual Reports and Transactions expose the reproduced row as a Button with its complete meaningful name and amount, no private-use glyph; activation still opens the correct June30 detail and Back returns to its originating list. Verify shared Home/merchant callers and the static row branch. Add meaningful component/accessibility-prop coverage, then verify native metadata. Spoken VoiceOver focus/activation, Android, physical hardware and corrected behavior remain unverified.

Dedupe/history: searched all root .pm queues including done for EntryRow, transaction/journal/recent row accessibility, native roles and icon glyphs; scanned every open milestone title. Open212 concerns the separate semantic Button component, which EntryRow does not use. Completed159 owns Back;173 owns AccountTable disclosure;109 owns this row's amount precision;149 owns amount-input labels; w4/done/m7 does not repair this row wrapper. Dashboardm31/m40 are different implementations. EntryRow history69001418/8cfd4f57/baaee6b1/0a7b6d24 retains this omission; targeted accessibilityRole history has no repair. Current main still has the unlabelled wrapper, so this is not deployment lag.

Verified local evidence under `mobile/tmp/qa-2026-09-13/`: alphabet-report-margin-scroll.log, alphabet-income-breakdown.jpg, alphabet-report-row-open-detail.jpg, entry-row-role-settled-home.jpg, entry-row-role-fresh-journal-ax.log, entry-row-role-fresh-journal.jpg, entry-row-role-fresh-detail.jpg and alphabet-journal-api.json. Both native inspection helpers executed one passing test. entry-row-role-console.json is a bounded diagnostic capture, not proof of an error-free session. The earlier entry-row-role-fresh-home.jpg is a splash screen and is excluded. Screenshots946×2048 map to390×844 logical points. No product fix or shipping performed.

---

**Resolved 2026-09-15** — fixed and shipped in `109d95ae` (`mobile`). EntryRow exposes accessibilityRole and label.
