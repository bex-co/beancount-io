# Empty Statistics periods render NaN percentages

**Minor · 20–30m · dashboard.** A2: a newcomer selecting a period before their ledger begins should see a meaningful empty result.

Production https://beancount.io, 2026-09-11, isolated headless Chrome152, English/light, authenticated QA reader, public synthetic open_ledger/freelancer-invoicing. Fresh1440×1000 and390×1000. Local/fetched main f1965988; full deployed SHA unverified.

Open /ledger/open_ledger/freelancer-invoicing/statistics?time=2025&lang=en (the example begins in2026). Wait for the actual Entries Count by Type rows, not the loading skeleton. Its summary correctly says Total0 entries across12types and each count is0, but every Percentage cell says NaN%. Both fresh widths reproduce; an independent1900 narrow control agrees. Screenshot inspected after all12rows load. No page errors or writes.

GraphQL GetLedgerEntriesCountPerType for1900 returns HTTP200 with all12named types and number0. REST GET /api-gateway/v1/ledgers/open_ledger/freelancer-invoicing/entries-count?time=2025 returns the same valid zero-filled shape in both repeats. This is legitimate no-activity data, not a failed request. Change to2026-01: Open15 displays83.3%, Transaction3 displays16.7%, the remaining zero counts show0.0%; normal division works with a positive denominator. Postings per Account correctly renders no results for2025. Account Last Entries intentionally retains full-ledger last dates while balances honor filters, as documented in completedm14; that neighboring table is not a finding.

Root: dashboard/src/features/ledger-data/statistics/entries-count-by-type.tsx:112 totals numbers, then143–145 computes ((entry.number / totalCount) *100).toFixed(1) without guarding totalCount0. 0/0 becomes NaN and the template appends %. The service intentionally returns all12types with zero defaults in backend-cluster/ledger/src/features/ledger/service/ledger-data-mappers.ts:69–75, so an empty array check alone is insufficient. There is one production EntriesCountByType caller, StatisticsPage at statistics/index.tsx:41.

Fix locally in the Statistics presenter: when the total is zero, retain accurate zero counts and show an explicit not-applicable percentage (for example an em dash with accessible localized meaning), or render the existing truthful no-data state for the whole percentage table. Do not perform division or invent an activity share for this undefined denominator. Keep positive-total ratios, period filters, loading/errors and current count formatting. No API contract, financial engine or global number-formatter change is required.

Acceptance: the exact zero-filled12-type response produces no NaN/Infinity in DOM or accessibility output; positive-total18-entry control still shows83.3%/16.7% and zero-count types0.0%. Also cover an empty array and transition between zero/positive responses in the actual component; no shallow style-only assertion. Repeat desktop/narrow and run dashboard gates on implementation.

Dedupe: all queues including done searched for NaN, zero denominator, empty statistics and percentages. Completedm14 owns filtered Postings per Account, whose empty state works; this is the neighboring entry-type ratio. Completed110 owns shared locale/hydration and count formatting, not this inline division. m26 NaN concerns native transaction auto-balance. No overlapping open item found; targeted component history traces the unguarded division toaf5339de, unchanged on fetched main.

Verified ignored evidence dashboard/tmp/qa-20260911-w3-loop/: statistics-before-ledger.json, statistics-zero-repeat-{1440,390}.json, statistics-zero-{1440,390}.png. Initial repeat waited only for the table and captured skeleton rows; corrected fresh repeats wait for the Transaction cell and are the retained evidence. All contexts closed. No product fix, production mutation, commit or shipping. Other locales, actual screen-reader speech and patched results remain unverified.

---

**Resolved 2026-09-11** — fixed and shipped in `86ca4531` (`dashboard`), with the package's full check gate green. Triage confirmed the reported root cause against current code before the fix; the note was not stale.
