# Public ledger collection loses search, sorting and expansion on Back

Severity: minor. Owner: dashboard. Estimate: 45–60 minutes. A2/A3: visitors can inspect several public example ledgers without rebuilding their collection search each time.

Production https://beancount.io, 2026-09-11; isolated headless Chromium152, signed out, public open_ledger profile, English/light. Fresh1440×1000 and390×1000. Local/fetched main f1965988; latest observed entry main-RkgtsKH_.js, exact deployed commit unverified.

Repro: open /ledger/open_ledger?lang=en, search example and select Name A–Z. The three results are crypto-example, example and real-estate-example. Open crypto-example, then browser Back. The same profile URL returns with empty search, Recently updated sorting and the default12 of44 ledgers (snowflake through carnival); none of the three searched examples is visible. Both fresh widths reproduce without page errors.

A separate fresh390 control selects Name A–Z, Show more ledgers to24, and opens the last visible card meta. Back again resets sorting and visible count to the first12 Recently updated cards. Expected: preserve the collection's query, sort and expanded count for this profile/history entry while inspecting a ledger, so the visitor can continue from the same list.

Root: dashboard/src/features/user-profile/components/ledger-collection.tsx:20–22 stores query="", sort="updated" and visibleCount=12 solely in useState. RepositoryListItem's native router Link at43–46 navigates to the ledger and unmounts the collection. The /ledger/$username route's schema (routes/ledger.$username.tsx:6–8) persists only the profile tab. Returning recreates all three defaults. Filtering/sorting themselves work: the correct three examples render before navigation, Show more gives24, and earlier public-owner-search.json verifies search outside the initial12 plus clear/show-more controls. The public profile response contains44 repositories; no server pagination/data-loss defect is claimed.

Fix: retain a validated profile-specific collection snapshot (query, allowed sort and bounded visible count) in route/history state and restore it on Back. Scope it to the profile/history entry, preserve the existing tab state, and keep query/sort changes resetting the visible slice appropriately. Handle changed result counts safely. Do not persist another user's search globally or change the card destinations, API, follower/starred pagination or Gallery's separate search-reset policy. Avoid one new browser history entry per keystroke.

Acceptance: desktop/narrow search example + Name A–Z→crypto-example→Back restores those three results and controls;24-card expansion→meta→Back restores24 and A–Z. Forward still works; direct links without collection state use current defaults; invalid state and smaller result sets are safely bounded. Add a real route unmount/Back regression using LedgerCollection state and card navigation, plus username isolation and search/sort-reset controls. Preserve loading, unavailable profile and empty/no-match rendering. Run dashboard format/lint/test/build when implementing.

Blast radius/dedupe: LedgerCollection has one production caller in UserProfileTabs at189 for the Ledgers tab, shared by public and other user profiles; only signed-out open_ledger is live-reproduced. All queues including done searched for collection search/sort/history restoration. 120,131 and160 own separate Accounts, Journal and Holdings state; completedm21 owns social-list pagination and explicitly separates collection search/show-more. Gallery163 owns Escape focus and does not change its explicit selection reset. The collection implementation dates to5855c32a with no later history/main repair or matching item.

Evidence under dashboard/tmp/qa-20260911-w3-loop/: profile-collection-back-1440.json, profile-collection-back-390.json, profile-expanded-back.json, profile-collection-reset-390.png, public-owner-search.json and public-catalog.json. Narrow reset screenshot visually inspected. All fresh contexts closed, no writes, follows, stars, clipboard actions or product changes. No shipping. Patched behavior, Forward/reload and cross-profile state remain acceptance work, not claimed coverage.

---

**Resolved 2026-09-11** — fixed and shipped in `4d701a91` (`dashboard`), with the package's full check gate green (format, lint, 3879 tests, build). Triage confirmed the reported root cause against current code before the fix.

Beyond the reported defect, the fix closed a latent crash: TanStack Router surfaces raw URL values through `useSearch` for keys a route's `validateSearch` omits, so `?search=["x"]` reached the Accounts page and threw. Each view now re-coerces at the use site, with a hostile-input regression per suite.
