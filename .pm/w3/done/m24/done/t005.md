---
id: w3/m24/t005
title: Test real typing, invalid submission and recovery
worker: worker3
status: done
estimate: 55m
depends_on: [w3/m24/t004]
---

## Objective

Add meaningful coverage that fails for the observed keystroke rewriting and stale-date submissions, using the actual DatePicker and form integration.

## Context

- The milestone README contains fresh Budget/Balance evidence and the shared cause.
- Live failures were reproduced without accepting any production mutation.

## Steps

1. Type 06/15/2025 one character at a time; assert every editable prefix is preserved and only a complete valid date becomes submittable.
2. Replace a prior valid date with empty text, not-a-date, 02/30/2025 and a partial date. Assert no stale payload for pointer and Enter, and correct recovery to February28, a valid leap day and Today.
3. Use real picker/RHF wiring with data/mutation boundaries isolated. Preserve Balance's native empty-required control; do not rely solely on Budget's existing readOnly DatePicker mock.
4. Exercise parent reset/reopen and the seven consumer boundaries. Run desktop/narrow repro checks plus dashboard format, lint, tests and build. Keep all production writes blocked.
5. In real America/Los_Angeles and UTC browser contexts, paste 2025-06-15 and 06/15/2025 and assert June 15 throughout the input/form/prepared payload. Reject an impossible ISO date too. Use actual timezone behavior rather than a mocked parser that returns a chosen date.

## Files

- dashboard/src/common/components/ui/**tests**/date-picker.test.tsx (new)
- dashboard/src/features/ledger-data/budget/**tests**/add-budget-dialog.test.tsx
- dashboard/src/features/journal/components/new-directive-dialog/**tests**/ (new interaction coverage)

## Acceptance criteria

- [ ] Tests detect the original stale Date, immediate prefix reformatting and impossible-day normalization.
- [ ] Timezone controls catch ISO June 15 becoming June 14 in Los Angeles while preserving valid slash-date input.
- [ ] Valid corrections produce exact calendar dates once; invalid drafts produce no mutation.
- [ ] The dashboard package gates pass without adding dependencies or weakening tests.

## Out of scope

Snapshot-only coverage, actual ledger mutations, native-mobile QA or a timezone migration.
