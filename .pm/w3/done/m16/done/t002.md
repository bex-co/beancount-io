---
id: w3/m16/t002
title: Preserve parent amounts in the activity hierarchy
worker: worker3
status: done
estimate: 35m
depends_on: [w3/m16/t001]
---

## Objective

A direct parent account's amount remains visible when the Cash Flow table also
renders its descendants.

## Steps

1. Separate a node's own direct amount from its descendants' aggregate in the
   activity-tree builder. Build `balanceChildren` from both exactly once.
2. Retain real account paths, classification source and the shared hierarchy
   display contract; structural ancestors must not become duplicate postings.
3. Reproduce the current Monthly table/export discrepancy, then verify the
   restored parent amount reaches the displayed table with its child unit intact.

## Files

- `dashboard/src/features/reports/cash-flow/lib/statement-tree.ts`
- `dashboard/src/features/reports/cash-flow/lib/__tests__/statement-tree.test.ts`
- Cash Flow content coverage as needed

## Acceptance criteria

- [ ] Federal displays its restored -27635.92 USD alongside descendant IRAUSD,
      agreeing with the flat model and export data.
- [ ] Parent-only, child-only and parent-plus-child cases aggregate correctly
      at multiple depths, including opposite signs and separate units.
- [ ] The parent remains a usable real-account link and summary totals stay unlinked.
