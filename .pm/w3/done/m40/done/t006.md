---
id: w3/m40/t006
title: "Close and archive the account journal column milestone"
worker: worker3
status: done
estimate: 10m
depends_on: [w3/m40/t004, w3/m40/t005]
---

## Objective

Close the milestone once its definition of done actually holds: re-open the reproduction account in a browser, read every column, confirm every DoD bullet, then archive.

## Context

Run this last. Completing it *is* the archive move — `/pm done w3/m40/t006` triggers it.

## Steps

1. Re-open `/ledger/<owner>/example/account/Assets%3AUS%3AETrade%3AGLD?time=2017` and confirm the fourth column matches its header, at 1440 and 390.
2. Inspect the accessibility tree for column headers and per-row cells on both journals, and check the postings disclosure by keyboard.
3. Confirm `yarn format:check && yarn lint && yarn test && yarn build` are green in `dashboard/`.
4. Set every remaining task `status: done`, move each `tNNN.md` to `w3/m40/done/`, mark each row `— **DONE**` and `**Status:** done` in the milestone README, move `w3/m40/` to `w3/done/m40/`, and check `- [x] **m40**` in `.pm/w3/README.md`.

## Files

- `.pm/w3/m40/**`, `.pm/w3/README.md`

## Acceptance criteria

- [ ] Every DoD bullet is verified against the running product, not just in tests.
- [ ] The milestone exists only at `.pm/w3/done/m40/`, its README says `**Status:** done`, and the workstream checkbox is checked.

## Out of scope

Shipping unrelated work, and closing while any DoD bullet is unmet.




## Verification

Dashboard gate green. Units derived client-side; real table semantics.
