---
id: w3/m44/t008
title: Simplify
worker: worker3
status: done
estimate: 20m
depends_on: [w3/m44/t007]
---

## Objective

Run `/simplify` over the code this milestone changed — reuse, simplification, efficiency, altitude. Behavior-preserving only.

## Context

- The likely finds: the response projection in t004 duplicating logic that already lives in `identity.ts` helpers (`effectiveCapabilities`, `identityAssurance`, `identityUserId`), and the lifetime projection in t002 repeated three times across the resolvers where one helper would do.
- Resist collapsing the three resolvers into one generic path. They are deliberately separate and ordered, and `resolveIdentity`'s comments explain why.

## Acceptance criteria

- [ ] `/simplify` run over the milestone's diff and its findings applied or explicitly declined with a reason.
- [ ] No behavior change — the t004 acceptance matrix still passes unchanged.
- [ ] `yarn lint`, `yarn typecheck`, `yarn test` pass.

## Out of scope

Bug hunting (that is `/code-review`). Refactoring code this milestone did not touch.
