---
id: w3/m9/t004
title: Test coverage
worker: worker3
status: done
estimate: 45m
depends_on: [w3/m9/t002]
---

## Objective

Prove the depth this milestone restored, which means testing the layer *below* the gate.

## Context

- The test that matters: a `ledger.read`-only identity is refused a write-tier Plaid verb **by the service**, with the transport gate not in the picture at all. Call the service directly. If it passes with the gate bypassed, the depth is real; if it only fails through a route, nothing was gained.
- The mirror test is just as important: a **system** identity still succeeds on the scheduler's path, so the fix did not break unattended sync.
- `src/features/plaid/service/__tests__/` holds the existing suites to extend.

## Steps

1. Service-level refusal test for a scope-narrowed identity on a write verb.
2. Service-level success test for `systemIdentity` on the scheduler's verb.
3. Confirm the refusal test fails against the pre-t001 code.

## Files

- `src/features/plaid/service/__tests__/`

## Acceptance criteria

- [ ] A narrowed identity is refused with no transport layer involved.
- [ ] The system path still succeeds.
- [ ] The refusal test was verified to fail before the fix.

## Out of scope

- Contract-testing Plaid's own API.
