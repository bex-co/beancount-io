# Let account-picker scroll recovery run before declaring success

**Minor · 40–55m · mobile.** A2: a newcomer editing a posting should immediately see its selected account. Remaining failure after completed w3/154; report only.

Environment: September 13, 2026; https://beancount.io; HEAD84231f9c, native io.beancount.ios 1.20260906.47 (47), Expo57/RN0.86, iPhone17e/iOS26.5, English/light/default large text. Authenticated QA session, public open_ledger/minimax. Unsaved drafts only; no Done/save, account creation, or ledger mutation.

1. Select MiniMax. Open New Transaction and tap the second posting, Expenses:CostOfRevenue.
2. Wait for the Account Picker to settle. All is selected, but the viewport starts at Recent/Assets; the selected expense and its checkmark remain offscreen.
3. Tap the Expenses category chip. Expenses:CostOfRevenue immediately appears with its green checkmark. No account selection is required: the value was retained, only its initial visibility failed.
4. Relaunch (repeat PID80933), then use Home → plus → Enter new transaction → second posting. The same initial top-of-list failure repeats. Dismiss the development warning banner and recheck: the settled list still starts at Recent/Assets.

The initial pass reached New Transaction through inspected beancount:///(app)/add-transaction; the fresh repeat used the actual Home menu. Local Recent contains Assets:Current:Cash and Income:Revenue from earlier draft navigation, not the selected expense. Do not select the expense before reproducing, because adding it to Recent can mask the failure. Fresh ledgerMeta(userId:open_ledger, ledgerId:open_ledger/minimax) succeeds and includes Expenses:CostOfRevenue; this is local positioning, not missing API data. Category-chip control shows the correct selection. First posting's near-top Cash is a control from the original 154 reproduction, not independently rechecked in this pass.

Root: mobile/src/screens/account-picker-screen/account-picker-screen.tsx:324–342 calls scrollToLocation and then unconditionally schedules a 64ms settle timer. Installed @react-native/virtualized-lists/Lists/VirtualizedList.js:180–195 invokes onScrollToIndexFailed synchronously for unmeasured targets. The callback at374–396 schedules recovery at120ms, but after it returns, the caller arms the 64ms timer. stopScrollingToSelected at307–322 cancels the pending retry and marks completion before it can run. scroll-to-selected.ts defines these delays. A null list ref also falls through to false success. The timer ordering is source-confirmed; native callback timing was not instrumented, so it is the source-supported cause of the repeated symptom, not a captured native trace.

Fix: only settle a real attempt that did not synchronously fail, and do not mark an absent/unready ref successful. Make recovery wait for usable layout and actually reveal the selected row, with bounded retries and reliable cancellation on drag, search/category change and unmount. If measurement needs an estimated advance, account for sections and enlarged rows. Preserve ranking, current identity, callbacks and user-controlled scrolling; no permanent virtualization disable or unbounded timer loop.

Acceptance: fresh native MiniMax second-posting picker reveals the current expense automatically, including cached/delayed metadata and an initially unmeasured target; top/Recent controls remain visible; missing selections terminate safely. A regression test must exercise the component scheduling sequence with synchronous onScrollToIndexFailed, advance beyond64ms and120ms, and demonstrate that recovery actually runs before completion. Existing scroll-to-selected.test.ts tests retry policy counts, not the component's competing timers. Verify cancellation and enlarged text without snapping back after user input. Other callers, Android, expired session, network failure and enlarged text remain unverified in this pass.

Blast radius: all seven pushAccountPicker call sites: multi-posting, transaction filters, add-budget, quick-add source/destination and add-transaction-next source/destination. This is a mobile-only fix; no REST/GraphQL/MCP contract changes.

Dedupe: full .pm search including done for selected account, picker scrolling, SCROLL_SETTLE/SCROLL_RETRY matches done154 and unrelated wheel-picker items101/162/167; openm30 is account-journal filters. No open matching recovery item. Targeted git log -S SCROLL_SETTLE_MS traces the current retry implementation to a7c13568. The completed154 note explicitly leaves native behavior unverified; this repeat establishes that its observable acceptance still fails on current main, not deployment lag.

Verified ignored evidence under mobile/tmp/qa-2026-09-13/: picker-selected-current.jpg, picker-expense-control.jpg, picker-current-repeat.jpg, picker-repeat-clean.jpg, picker-meta.json. The banner-free repeat is the primary failure image. No error overlay occurs in that capture; bounded logs and exact native failure-callback tracing remain unverified. The draft used for the initial pass also verified Auto off/on at ±1.25MUSD; relaunch discarded it without submitting.


## Resolved

Fixed and shipped in `96837d1d`.
