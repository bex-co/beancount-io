# Keep Referral actions reachable with enlarged text

Minor · 40–55m · mobile · A3: readers using larger text can reach the app's existing invitation controls.

Environment: production-selected native app, authenticated QA session, HEAD84231f9c, io.beancount.ios1.20260906.47(47), iPhone17e/iOS26.5, English/System/light. No invitation sent, clipboard changed or ledger write submitted.

Repro: Settings → actual Invite Friends card opens Referral at default text size with complete summary, Copy and Share visible. Set simulator content_size to accessibility-extra-extra-extra-large and relaunch74903; open inspected `beancount:///(app)/referral` after boot. Title wraps to two very large lines, summary extends below screen, and Copy/Share are unreachable. A real native upward swipe does not reveal them. Fresh79113, same destination and another native swipe repeat; dismiss the development warning and the summary still runs off the bottom with neither action visible. These maximum-size checks prove the destination, not the Settings entry action at maximum size.

Native geometry: viewport390×844, zero ScrollViews. Copy has frame x186,y1116.67,w187,h48 and hittable=false; Share x16,y1180.67,w358,h90.33 and hittable=false. Both are enabled. Initial geometry test found Copy correctly but incorrectly assumed Share had button role; its failed assertion is an inspection assumption, not an app crash. Corrected generic-element inspection finds both and passes. Default large restored, fresh81822: Copy y392,h48 and Share y734,h44, both hittable=true; banner-free screenshot shows the complete page. Bounds well beyond844 establish overflow independently of the development warning.

Root: `mobile/src/screens/referral-screen/referral-screen.tsx:91–158` places every child in a non-scrollable View inside SafeAreaView. The body has flex1; unrestricted title20pt and summary16pt text, gift and margins precede the copy row, then a flex spacer and Share. When text grows beyond available height, children overflow without any scroll recovery. The copy container/button also use fixed height48 at44–66 and need to accommodate their own enlarged label once reachable. Sole ReferralScreen consumer is `mobile/app/(app)/referral.tsx`; Settings About navigates here.

Fix: use a content layout that scrolls when it exceeds the safe viewport, keeping the entire summary and both actions reachable at default and maximum text sizes. Preserve comfortable spacing at ordinary size, the full copy target, and usable copy-row height; keep Share visible or reachable without competing nested scroll regions. Do not cap the whole page's prose or replace the original text with truncated copy. Retain current clipboard/share handlers, accessible names, theme and localization. No API behavior change or new dependency required. Verify max/default and a longer translated summary with meaningful layout coverage and native captures.

Dedupe: searched all PM queues including done for Referral, shareLinkContainer, Invite Friends, Copy/font and scroll. Completed w3/105 fixes the separate Settings InviteSection and explicitly leaves Referral unverified. Completed182 repairs the shared Button's internal enlarged-label height; here Share grows to90.33 correctly but the parent places it beyond the viewport. Done151/open204 concern native header actions, and199 the drawer's separate constrained list. No matching open Referral task. Targeted history latestbc151c5c/a7c088d2/0a7b6d24/d85b625c retains the body View; `git log -S '<View style={styles.body}>'` reaches182a07f1. No pending deployment repair found.

Verified local evidence under `mobile/tmp/qa-2026-09-13/`: referral-default-current.jpg, referral-max-first.jpg, referral-max-repeat-after-swipe.jpg, referral-max-clean.jpg, referral-max-repeat-geometry.log, referral-default-geometry.log, referral-default-clean.jpg. Screenshots946×2048 correspond to390×844 logical points. referral-max-geometry.log is the initial partial inspection described above. Bounded diagnostic capture referral-max-console.json does not establish absence of runtime errors. Personalized referral identifiers remain local and are unnecessary to reproduce this static layout.

Unverified: VoiceOver scrolling, Android, dark mode, localized maximum text, actual clipboard content and share-sheet operation. This screen has no ledger query/loading/denied-data state; no backend error is implicated. Normal text size restored. Report only: no product implementation, product tests, commit or push.

---

**Resolved 2026-09-15** — fixed and shipped in `109d95ae` (`mobile`). Referral screen scrolls within the safe viewport.
