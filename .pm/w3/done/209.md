# Keep the full transaction title readable with enlarged text

Minor · 25–35m · mobile. A2/A3: a reader opening a transaction should be able to read its complete payee, including when using enlarged text.

Environment: September 13, 2026, main 84231f9c; production https://beancount.io; native io.beancount.ios 1.20260906.47 (47), iPhone 17e/iOS 26.5, English/System/light. Authenticated QA reader of public synthetic open_ledger/example. No ledger write.

Repro:
1. At normal `large` text size, open Example's Transactions tab, search the literal `E.B.'s Beer and Wine`, and tap April 18, 2016. The full payee, -$8.76, both postings and #trip-los-angeles-2016 are readable. Search returns all four matching transactions, consistent with the live journal.
2. Set iOS content size to `accessibility-extra-extra-extra-large`, terminate and relaunch (PID41200), then open `beancount:///ledger/open_ledger/example/entry/2350fcc3e023c39f6e27ac766f726b22` after Home settles. The detail title occupies two enlarged lines, `E.B.'s` and `Beer a…`. The remainder of the payee is unavailable visually; the title has no expansion action.
3. Independently relaunch at the same maximum size (PID42796), open the same link, dismiss the development warning banner and capture again. Identical truncation. Native accessibility contains the complete `E.B.'s Beer and Wine` label in a 330×204.7-point title: the data was not lost.
4. Restore normal `large`, relaunch PID45278 and open the same entry. The entire title returns. The amount and transaction identity do not change.

Expected: the transaction detail can expose the full payee/title while retaining useful text enlargement. A detail page is the destination for reading the identity abbreviated in lists. Actual: its fixed two-line limit permanently ellipsizes this ordinary payee at the largest text setting.

Root: `mobile/src/screens/transaction-detail-screen/transaction-detail-screen.tsx:94` gives heroTitle 92% maximum width and a 24-point scalable font; line499 fixes that Text to `numberOfLines={2}`. `selectors/select-transaction-detail.ts:110` supplies the complete payee, falling back to narration. The containing ScrollView at491 can already accommodate additional height; this is a title line limit, not a failure to scroll or fetch. The narration-only fallback shares the limit but was not independently reproduced at maximum text.

Fix locally: allow the full title to wrap in the existing scroll content, or provide a clear accessible expansion that reveals it completely. Preserve ordinary layout, useful Dynamic Type growth, exact title text, amount, date, metadata and posting navigation. Do not globally disable text scaling or truncate the producer's payee. No dependency or API change is required. Check a long narration-only title and a short payee alongside the reproduced full-payee case; verify the lower content remains reachable after the title grows.

Blast radius: the single TransactionDetailScreen route serves native public entry links and openTransactionDetail calls from Home recent transactions, Transactions, account detail, merchant detail and Reports account transactions; edit return also targets it. Native normal-size Transactions navigation and maximum-size public-link entry were exercised here. The tag is a working enlarged-text control: a real native upward swipe reveals its entire wrapped value. Posting-account labels have their own ellipsis/navigation behavior and are outside this finding. VoiceOver speech, Android, physical devices, maximum-size narration-only titles and failure/loading states remain unverified; retain their existing behavior.

Dedupe/history: all .pm queues, including done and current milestone titles, searched for transaction titles, heroTitle, payee truncation, two-line limits and enlarged metadata. No overlapping open item. 200 concerns merchant-directory badge layout; 208 concerns Discovery ledger names; 204/151 concern native navigation-bar actions. Completed109 concerns numerical precision and completed w4/m7 accessibility labels; this title's native label is already complete. Targeted git log -S traces the line limit to 7ad2aabc; latest detail changes 30afc89d,75d45df6,7538fb0d,41de310c leave it in place.

Verified ignored evidence under `mobile/tmp/qa-2026-09-13/`: `example-punctuated-entry.jpg`, `example-tagged-max-top.jpg`, `example-title-max-repeat.jpg`, `example-title-max-ax.log`, `example-title-default-repeat.jpg`, `example-tagged-max-scrolled.jpg`, `example-tagged-journal-api.json`, `example-title-console.json`. Screenshots 946×2048 correspond to 390×844 logical points. The bounded console capture is diagnostic only, not proof that no errors occurred. Standard text size restored; no product fixes, commits or shipping.

---

**Resolved 2026-09-15** — fixed and shipped in `109d95ae` (`mobile`). Transaction detail title wraps fully at large text.
