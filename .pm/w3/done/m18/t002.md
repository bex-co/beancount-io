---
id: w3/m18/t002
title: Identify a compatible corrected engine artifact
worker: worker3
status: abandoned
estimate: 45m
depends_on: [w3/m18/t001]
---

## Objective

Establish a concrete, reproducible dependency path that fixes the producer error and fits this repository's containment rules.

## Context

The parent milestone records the live public repro, exact engine/source trace,
working controls and the corrected-artifact prerequisite. Follow the owning
ledger package's instructions; this task does not authorize new dependencies
or upstream publication.

## Steps

1. Inspect supported engine releases/interfaces for a booking-aware fix to cost-derived implicit quotes. Versions 0.21.0 and 0.24.0 are already proven failing; do not repeat that upgrade as a proposed repair.
2. Run the t001 fixtures against the exact candidate artifact and verify query constructor variants, explicit annotations and date behavior. Record version, provenance, compatibility and results.
3. Use a normal package dependency or supported interface only; never vendor upstream source/WASM bytes into this public repo. If no candidate exists, record the exact unavailable prerequisite and leave dependent work open.

## Files

- backend-cluster/ledger/package.json (candidate selection; pin only after verification)
- backend-cluster/ledger/tmp/ (ignored artifact evaluation)
- backend-cluster/ledger/README.md (final supported version/behavior when available)

## Acceptance criteria

- [ ] A named reproducible artifact or supported configuration passes the reduction contract, or the specific blocker is recorded honestly.
- [ ] The solution does not require new dependencies, copied upstream bytes, a parallel valuation engine or changed user data.
- [ ] Current loader/module and plugin contracts are assessed before integration.

## Out of scope

- Publishing upstream code/issues/releases, inventing an artifact version or marking blocked integration complete.
