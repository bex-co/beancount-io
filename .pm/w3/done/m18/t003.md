---
id: w3/m18/t003
title: Integrate the verified price fix at the ledger engine boundary
worker: worker3
status: abandoned
estimate: 55m
depends_on: [w3/m18/t002]
---

## Objective

Make the exact corrected engine behavior reach normal ledger queries and their structured/text results.

## Context

The parent milestone records the live public repro, exact engine/source trace,
working controls and the corrected-artifact prerequisite. Follow the owning
ledger package's instructions; this task does not authorize new dependencies
or upstream publication.

## Steps

1. Integrate the candidate verified in t002 through the existing dependency/loader interface. Regenerate any changed lockfile with package-local Yarn; preserve GPL containment and CommonJS loading.
2. Run the cost-only sale through queryLedgerFilesResult and the actual LedgerShellService, both JSON and text. Preserve Decimal/Inventory serialization, source provenance, plugins and cached worker execution.
3. Check all Holdings query shapes against the full public fixture and a minimal original fixture. Keep cost basis/units unchanged; the remaining 0.10 BTC is worth 9800 USD at the valid quote.

## Files

- backend-cluster/ledger/package.json
- backend-cluster/ledger/yarn.lock (generated only)
- backend-cluster/ledger/src/foundation/rustledger/loader.ts
- backend-cluster/ledger/src/foundation/rustledger/engine.ts
- backend-cluster/ledger/src/features/ledger/service/ledger-shell-service.ts

## Acceptance criteria

- [ ] The real service returns 98000 as the quote and 9800 USD for the remaining synthetic 0.10 BTC.
- [ ] The public fixture returns 32046 USD for 0.327 BTC without changing its units or cost.
- [ ] JSON/text output, worker caches, existing plugins and source metadata retain their contracts.

## Out of scope

- New API fields/endpoints, per-query text rewriting, fabricated Price directives and dashboard-only compensation.
