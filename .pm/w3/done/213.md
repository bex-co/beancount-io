# Keep New ledger template choices above the keyboard while scrolling

**Minor · 35–50m · mobile.** A2: users enlarging text should still be able to read the Starter/Sample choice while composing their first ledger.

Environment: Sep13,2026, HEAD84231f9c, io.beancount.ios1.20260906.47(47), Expo57/RN0.86, iPhone17e/iOS26.5, English/System light, maximum accessibility content size. Dedicated QA session against https://beancount.io; only an unsaved new-ledger draft. Never activate Create.

Repro: set content size accessibility-extra-extra-extra-large, terminate/relaunch, settle Home, open the existing internal route `beancount:///(app)/create-ledger`. Replace default `my-book` with synthetic `QA Cafe / 2026`; keep the keyboard open. Swipe upward repeatedly within the visible form above the keyboard until the bottom stops moving (five native drags from195,447 to195,152). Starter is visible, but Sample's label is largely behind the prediction bar and its explanation is hidden beneath the keyboard. Native AX places Starter at y182.67/h301.33 and Sample at y484/h301.67; keyboard/prediction UI begins around y510. Sample remains technically hittable because a small part of its row is exposed—do not claim that activation is wholly impossible. Fresh processes62165 and63096 give the same final positions and screenshots.

Working controls: at default large size, the entire Sample option fits above the open keyboard and an actual tap selects it; the name preview correctly reads `qa-cafe-2026`. At maximum size, tapping the noninteractive Template heading dismisses the keyboard, revealing Sample at the same scroll offset. The development warning at the very bottom is separate from the keyboard obstruction. Back discards the local draft; the fresh run reseeds my-book/Private/Starter. The known clipped Create header is204 and is not this finding.

Root: `mobile/src/screens/create-ledger-screen/create-ledger-screen.tsx:269–272` uses an ordinary ScrollView with only contentContainerStyle and keyboardShouldPersistTaps="handled". It has no keyboard-adjusted inset/viewport; the content can reach the physical bottom while the keyboard still covers it. Installed `react-native/Libraries/Components/ScrollView/ScrollView.js:184–190` documents automaticallyAdjustKeyboardInsets default false, and Fabric's `RCTScrollViewComponentView.mm:149,187–191` initializes it to NO and returns from keyboard-frame handling when disabled. Thus repeated scrolling exhausts the unadjusted content range before the final option becomes readable.

Fix the New ledger scroll viewport/insets for the open keyboard, using the existing native keyboard adjustment or an appropriate existing layout primitive. Preserve form state, native field focus, scrolling, template selected state, private default and default-size layout. Avoid a hardcoded keyboard height or disabling Dynamic Type. Ensure the full last option can be scrolled above the keyboard, then returns to normal spacing after dismissal. Add native verification; a test asserting only text existence would miss the obstruction. No API/dependency change or production submission is necessary.

Scope/dedupe: one CreateLedgerScreen route; this run reached it through a deep link, not a new claim about every create entrypoint. Searched all PM queues including done for creation/template/keyboard and scanned open milestone titles.204 explicitly left maximum-size keyboard-open creation unverified; it repairs header Text scaling only.211 owns a different multiline TextInputScreen with no outer scrolling, not this form's ScrollView.184 concerns the separate TextInputModal. Completed w4/m6 introduced this form; targeted history ends atc40bf646 with no newer keyboard-inset repair. Other forms using ScrollView are not asserted to fail without independent native reproduction.

Acceptance: on iPhone17e maximum text size, focus/edit Name and scroll until Sample's complete title and explanation are visible above the open keyboard; choose Sample without submitting. Dismiss/reopen the keyboard and switch templates without losing the draft. Default-size name preview, template choice and Back cancellation still work. Submission-time validation, real creation, Android, physical hardware and VoiceOver speech remain unverified.

Verified ignored evidence in `mobile/tmp/qa-2026-09-13/`: `create-name-preview.log`, `create-name-preview-keyboard.jpg`, `create-sample-keyboard-control.jpg`, `create-name-max.log`, `create-max-keyboard-scroll.log`, `create-max-keyboard-bottom.jpg`, `create-max-dismissed-control.jpg`, `create-name-max-repeat.log`, `create-max-keyboard-scroll-repeat.log`, `create-max-keyboard-repeat.jpg`. No product fix or shipping.


## Resolved

Fixed and shipped in `96837d1d`.
