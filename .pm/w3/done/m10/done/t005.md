---
id: w3/m10/t005
title: Simplify
worker: worker3
status: done
estimate: 30m
depends_on: [w3/m10/t004]
---

## Objective

Run `/simplify` over the API-key Dashboard and documentation changes, applying behavior-preserving reuse, clarity, and efficiency improvements before the final coverage pass.

## Context

- Prefer existing user-settings cards, dialogs, form controls, clipboard behavior, date formatting, and GraphQL error handling.
- Plaintext-secret state must stay narrow and ephemeral; simplification must not lift it into a shared cache or persistent abstraction.

## Steps

1. Run `/simplify` over the milestone diff.
2. Remove avoidable duplication and abstractions while preserving the verified behavior.
3. Re-run the focused checks affected by any cleanup.

## Files

- Files changed by w3/m10/t001–t004

## Acceptance criteria

- [ ] `/simplify` has reviewed the complete milestone diff.
- [ ] Safe recommendations are applied and rejected recommendations are noted with reasons.
- [ ] The one-time-secret boundary and docker-verified behavior remain unchanged.

## Out of scope

- New API-key features or unrelated user-settings refactors.

## Closeout notes

- `/simplify` completed independent reuse, quality, and efficiency reviews across the full milestone diff.
- Applied: moved creation fields onto the existing React Hook Form/Zod/Form components; consolidated safe API-key metadata into a GraphQL fragment; reused canonical locale types; made locale coverage compile-time exhaustive; moved misplaced imports; replaced nested status/scope ternaries with typed maps; clarified config comments; and removed unnecessary optional access.
- The review found and fixed a one-time-secret reliability hazard: mint success no longer waits for the list refetch, so a secondary read failure cannot discard the only plaintext or falsely report creation failure. Closing is blocked while minting so a late response cannot repopulate dismissed state.
- Revocation now relies on Apollo's normalized mutation entity instead of making a redundant list request. The mutation still returns every safe lifecycle field and never plaintext.
- Declined reuse of the generic `CodeBlock` clipboard component: its presentation does not provide the credential-specific label, focused one-time warning, or read-only secret control this flow requires.
- Dashboard formatting, typecheck, and 20 focused tests pass after cleanup; backend formatting, typecheck, and all 212 suites / 2,839 tests pass. The docker-verified API behavior and component-local secret boundary are unchanged.
