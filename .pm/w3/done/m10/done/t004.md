---
id: w3/m10/t004
title: Adoption surface and verified launch-copy handoff
worker: worker3
status: done
estimate: 30m
depends_on: [w3/m10/t003]
---

## Objective

Make the proven token flow discoverable from the public API contract and monorepo quickstarts, then prepare concise launch-article copy that describes only steps verified in t003.

## Context

- The API reference currently accepts `bearerAuth` or `x-api-key`, but a newcomer needs an explicit path to the Dashboard issuer.
- The public board must not reference or depend on private source repositories; the article owner receives public-safe copy rather than a private-tree task.
- This milestone changes no skill or agent-guidance behavior, but the normal adoption-surface audit still checks those tables and symlinks for drift.

## Steps

1. Add the canonical Dashboard Personal Access Tokens URL to the API-key security description or other backend-owned OpenAPI copy so the interactive reference answers “where do I get this key?”.
2. Add or update a minimal REST quickstart in the owning public README using an environment-variable placeholder and `x-api-key`, never a real token.
3. Check the Dashboard entry point and relevant root/package documentation for discoverability; record why unrelated package tables and skill symlinks need no edit.
4. Using only t003's redacted evidence, write a short “Get a personal access token” block with navigation path, scope guidance, one-time-copy warning, and first request for the launch article owner.

## Files

- `backend-cluster/backend-v2/src/server/rest/openapi-registry.ts`
- `backend-cluster/backend-v2/README.md`
- `dashboard/README.md` when it is the clearest owner for the settings path
- `.pm/w3/m10/t004.md` closeout notes for the public-safe launch-copy handoff

## Acceptance criteria

- [ ] The generated OpenAPI contract tells a user where to create an API key.
- [ ] A checked-in quickstart reaches a safe endpoint with `x-api-key: $BEANCOUNT_API_KEY` and matches the docker-mac proof.
- [ ] The settings page is reachable from ordinary Dashboard navigation.
- [ ] Launch-copy handoff is concise, accurate, public-safe, and written only after t003 passes.
- [ ] Root/package README and guidance-table checks are recorded; no unrelated documentation or skill files are changed merely to satisfy the checklist.

## Out of scope

- Editing or publishing the launch article's private source tree from this public board.
- Translating the article; its owner controls content-localization policy.

## Closeout notes

- The v1 OpenAPI authentication description and `apiKey` security scheme now point to `https://beancount.io/settings/api-keys`, identify `x-api-key`, and state that plaintext is shown once. The checked-in v1 snapshot was regenerated and its completeness test passes.
- The backend README now has a safe ledger-listing quickstart using `x-api-key: $BEANCOUNT_API_KEY`; the Dashboard README records the ordinary **Settings → Personal access tokens** entry point and the direct route. Browser verification in t003 proved the nav link.
- Root package tables already identify Dashboard and the REST gateway at the right altitude; adding a settings subfeature there would make the table noisy. Agent-guidance and shared-skill links were unchanged, and `scripts/check-agent-guidance.py` confirmed all 16 scoped links plus both shared-skill links.
- Public-safe launch-copy handoff:

  > Sign in to Beancount.io, then open **Settings → Personal access tokens** or visit `https://beancount.io/settings/api-keys`. Token creation is available on paid plans. Name the token, start with `ledger.read`, and add `ledger.write` or `ledger.admin` only when the integration needs it; optionally restrict it to one `owner/name` ledger and set an expiry. Copy the `bcio_` plaintext immediately—Beancount.io shows it only once—and store it in a secret manager. Export it as `BEANCOUNT_API_KEY`, then make a safe first request with `curl --fail-with-body --header "x-api-key: $BEANCOUNT_API_KEY" https://api.v3.beancount.io/api-gateway/v1/ledgers`. Return to the same settings page to inspect or revoke the token; revocation applies to the next request.
