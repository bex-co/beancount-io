---
id: w3/m10/t007
title: Closeout
worker: worker3
status: done
estimate: 20m
depends_on: [w3/m10/t006]
---

## Objective

Close the milestone only after every task is complete and the observable Dashboard-to-REST journey in the definition of done is real.

## Context

- Code completion alone is insufficient: t003's docker-mac evidence and t004's verified adoption handoff are required.
- Completed tasks and milestones must exit the open `.pm` tree.

## Steps

1. Confirm every acceptance criterion and the milestone definition of done.
2. Mark all remaining tasks done and move task files under `w3/m10/done/`.
3. Mark every milestone table row done and set `**Status:** done`.
4. Move the milestone to `w3/done/m10/` and check m10 in `w3/README.md`.
5. Verify no completed m10 task or milestone remains in the open tree.

## Files

- `.pm/w3/README.md`
- `.pm/w3/m10/`
- `.pm/w3/done/m10/`

## Acceptance criteria

- [x] Every non-closeout task is done and the definition of done is verified, not assumed.
- [x] The milestone exists only under `.pm/w3/done/m10/` with `**Status:** done`.
- [x] Every task exists only under the milestone's `done/` directory and every row says **DONE**.
- [x] The w3 milestone checkbox is checked.

## Out of scope

- Closing with unresolved integration evidence or an unpublished external article edit.

## Closeout notes

- Rebuilt and recreated the Dashboard service from the final source in `deploy/docker-mac`; the route remained reachable with the full seven-service stack healthy.
- Repeated the one-time-secret lifecycle on the rebuilt image without logging or persisting plaintext: the ledger list and scoped account read returned 200, a schema-valid write using the read-only token returned 403, the plaintext was absent after reload, the revoked state rendered immediately, and the next authenticated request returned 401.
- Revoked every temporary verification token. The English-only launch article retains the `changelog` tag and now documents the Dashboard key path, least-privilege choices, one-time copy behavior, and revocation check.
