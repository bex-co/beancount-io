---
id: w3/m37/t006
title: "Test inventory cells across renderer, export and edge shapes"
worker: worker3
status: done
estimate: 45m
depends_on: [w3/m37/t004]
---

## Objective

Add meaningful tests that would fail against today's `JSON.stringify` behavior and that pin the edge shapes this milestone has to survive. Assert real rendered text and real exported CSV text — no snapshot-everything tests and no assertions that a mock returned what it was told.

## Context

- Existing suites to extend: `dashboard/src/features/bql/components/__tests__/query-result-card.test.tsx` (which already has a `dtype: "Position"` fixture at :426), `dashboard/src/features/bql/lib/__tests__/export-utils.test.ts`, and the Holdings tests that must keep passing unchanged.
- Use the real captured payload from the parent README as the fixture, including the `{}` zero-net rows.

## Steps

1. Add renderer cases: single-currency inventory, multi-currency inventory, `{}` zero-net, `Position` column, unrecognized object shape, and the scalar controls.
2. Add export cases for the same shapes, asserting the exact CSV text including quoting.
3. Add or keep a case proving no cross-feature import regression, and confirm the Holdings tests still assert their unchanged output.

## Files

- `dashboard/src/features/bql/components/__tests__/query-result-card.test.tsx`
- `dashboard/src/features/bql/lib/__tests__/export-utils.test.ts`
- tests adjacent to the new shared `common/lib/` module

## Acceptance criteria

- [ ] Each new test fails when the fix is reverted to `JSON.stringify(cell)` and passes with it.
- [ ] Rendered text and exported CSV text are asserted literally, not through snapshots of the whole tree.
- [ ] The `{}` zero-net case asserts an empty cell, and the multi-currency case asserts every unit.
- [ ] `yarn test` passes in `dashboard/`.

## Out of scope

Testing the ledger service's fava-parity serialization (another package, deliberate behavior) and adding coverage for unrelated BQL features.
