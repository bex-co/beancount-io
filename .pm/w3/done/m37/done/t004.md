---
id: w3/m37/t004
title: "Verify the query-result adoption surface"
worker: worker3
status: done
estimate: 15m
depends_on: [w3/m37/t002, w3/m37/t003]
---

## Objective

Check that the repaired query result is discoverable and usable on every surface it is documented on, and that nothing in the repo now describes the old behavior. Confirm the BQL/query capability description in `dashboard/CLAUDE.md`, the root `README.md` / `CLAUDE.md` package tables, and any docs or skill text that shows a `sum(position)` example still match what the product does.

## Context

- Root `CLAUDE.md` lists BQL under the CLI and dashboard capabilities; `dashboard/CLAUDE.md` and `dashboard/src/features/reports/CLAUDE.md` describe export conventions.
- Customer-facing ledger skills live in `skills/.claude/skills/`; check whether any of them show query output that would now differ.
- The parity rule in the root `CLAUDE.md` applies to API capabilities, not to browser rendering — no REST/GraphQL/MCP change is expected from this milestone. Record that explicitly.

## Steps

1. Re-run the reproduced query in the browser and confirm the documented behavior.
2. Grep the repo for `sum(position)` examples and stale descriptions of JSON-shaped query output.
3. Flag any drift as follow-up rather than editing unrelated packages here.

## Files

- `dashboard/CLAUDE.md`, root `README.md` / `CLAUDE.md` (only if they describe this behavior)

## Acceptance criteria

- [ ] No instruction file, README or skill still describes or shows the JSON-object query output.
- [ ] The Query page and its CSV are reachable and behave as documented for a signed-in reader.
- [ ] A note records that no REST/GraphQL/MCP surface changed, because the API payload was already correct.

## Out of scope

Editing other packages' docs, and any behavior change (that belongs in t002/t003).
