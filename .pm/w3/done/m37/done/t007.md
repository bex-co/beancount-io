---
id: w3/m37/t007
title: "Close and archive the BQL inventory rendering milestone"
worker: worker3
status: done
estimate: 10m
depends_on: [w3/m37/t005, w3/m37/t006]
---

## Objective

Close the milestone once its definition of done actually holds: re-run the reproduced query and its CSV download in a browser, confirm every DoD bullet, then archive.

## Context

Run this last. Completing it *is* the archive move — `/pm done w3/m37/t007` triggers it.

## Steps

1. Re-verify every bullet in the parent README's **Definition of done**, including the Holdings control and the m28 layout/semantics checks at 1440 and 390.
2. Confirm `yarn format:check && yarn lint && yarn test && yarn build` are green in `dashboard/`.
3. Set every remaining task `status: done`, move each `tNNN.md` to `w3/m37/done/`, mark each row `— **DONE**` and `**Status:** done` in the milestone README, move `w3/m37/` to `w3/done/m37/`, and check `- [x] **m37**` in `.pm/w3/README.md`.

## Files

- `.pm/w3/m37/**`, `.pm/w3/README.md`

## Acceptance criteria

- [ ] Every DoD bullet is verified against the running product, not just the code.
- [ ] The milestone exists only at `.pm/w3/done/m37/`, its README says `**Status:** done`, and the workstream checkbox is checked.

## Out of scope

Shipping unrelated work, and closing while any DoD bullet is unmet.
