# Make the missing-file recovery button actually open Files

**Severity:** minor. **Estimate:** 25–35 minutes. Package: mobile. A2 — a stale file link's recovery action should lead to the promised file browser.

Environment: main84231f9c, Sep13,2026, native1.20260906.47/47, iPhone17e/iOS26.5, English/light/standard text, signed-in QA viewing public `open_ledger/real-estate-example`. No writes.

Repro:
1. From the valid Sale transaction detail, open `beancount:///ledger/open_ledger/real-estate-example/files/blob/main/qa-nonexistent-20260913.bean`. This is an explicit missing-file negative, not a claimed valid resource.
2. The settled editor correctly says `This file is no longer in the ledger.` and offers Reload / Back to Files. Reload leaves the unavailable state.
3. Tap **Back to Files**. It returns to the Sale transaction detail, not Files (initial PID55857).
4. Terminate/relaunch (PID61713), open the same missing-file link and tap Back to Files: it returns to Home, not Files. The launch screenshot caught the splash and is excluded; the missing screen and settled Home result were captured.
5. Working control: select the actual Files tab, open the same missing-file link, then Back to Files. This time it returns to Files because that was already the prior screen.

Expected: the explicitly named recovery button opens the selected ledger's Files destination regardless of the screen beneath the incoming link. Actual: it only pops navigation history, so the destination varies with the origin. This does not trap the user; the normal tab navigation still reaches Files.

Root: `mobile/src/screens/ledger-file-editor-screen/index.tsx:627` branches on `router.canGoBack()` and calls `router.back()`, using `router.replace("/(app)/(tabs)/ledger")` only if there is no history. History existence does not establish that the prior screen is Files. `30afc89d` added this button as part of completed179's missing-state fix; no subsequent repair is on this HEAD. The explicit file app-link resolver at `common/app-links/resolve-app-link.ts:142` can enter this editor from any screen.

Fix: navigate explicitly to the current ledger's file browser for this labeled action, using the existing router/tab destination and preserving the ledger identity. Keep the ordinary header Back behavior separate. Preserve Reload's same-file query, missing/empty/error/loading distinctions, disabled save behavior and editor identity/unsaved guards. Add focused navigation coverage with Home, transaction detail, Files and no-history origins.

Blast radius: one unavailable-state action in LedgerFileEditorScreen, exposed by `app/(app)/ledger-file-editor.tsx` and incoming file links. Only null-result recovery is affected; existing-file content and ordinary header Back are separate. Read-only `getLedgerFile` for the negative path returns `{getLedgerFile: null}`, while the real Files browser lists accounts.bean, main.bean and the transactions/prices directories. No API surface change required. No-history fallback, nested-directory restoration, other locales, Android, offline/denied responses, and writable unsaved drafts were not exercised here.

Dedupe: searched every open/done queue for Back to Files, missing-file, unavailable and history-navigation terms, and scanned open milestones. Completed `w3/done/179` explicitly requested safe recovery to Files; its explanatory state now passes, but this new button chooses the wrong destination for deep links. Open196/197 concern dashboard file creation/draft handling. No overlapping open mobile navigation fix found.

Verified evidence under `mobile/tmp/qa-2026-09-13/`: `missing-file-current.jpg`, `missing-file-back-settled.jpg`, `missing-file-repeat.jpg`, `missing-file-repeat-back.jpg`, `files-working-control.jpg`, `missing-file-files-origin-control.jpg`, `missing-file-api.json`. The development banner in the second pass is below the recovery controls and does not cover the destination title. No product fixes, shipping or created test resources; current screen restored to Files.

---

**Resolved 2026-09-15** — fixed and shipped in `109d95ae` (`mobile`). Back to Files navigates to Files rather than history back.
