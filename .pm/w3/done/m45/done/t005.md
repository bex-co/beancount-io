---
id: w3/m45/t005
title: Test coverage
worker: worker3
status: done
estimate: 40m
depends_on: [w3/m45/t003, w3/m45/t004]
---

## Objective

Add meaningful regressions through actual import commands for the reproduced short-decimal and currency collision cases, reverse-order replays, old and new IDs, native-ID conflict controls, equivalent number spellings and identical-row multiplicity. Assert written tokens, counts and unchanged ledger bytes on duplicate/conflict paths. Run make check-all inside cli and applicable skills checks.

## Context

See [the milestone](./README.md) for exact reproduction, scope and compatibility requirements.

## Acceptance criteria

- [x] Both original reproductions fail on the original implementation and pass with the fix.
- [x] No duplicate append or false skip is hidden by only checking exit codes.
- [x] Owning package gates pass without weakened existing tests.

## Out of scope

Backend/API changes, new dependencies, edits to real ledgers and unrelated import behavior.

## Resolution

`cli/tests/test_import_identity_exactness.py` (17 tests): the rendering itself, both reported reorder cases plus the distinct-cents case as a control, distinct ids for sub-cent rows, the N-identical-rows guarantee, legacy id recognition, the lossy-collision rule, native-id conflict preservation, and equivalent decimal spellings. Reverting only the behaviour fails 3 of them; the controls pass either way. Four pre-existing tests that pinned the old digests were updated to compute them from the published base.
