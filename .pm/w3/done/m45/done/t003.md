---
id: w3/m45/t003
title: Adoption surface — document the shared identity and compatibility policy
worker: worker3
status: done
estimate: 25m
depends_on: [w3/m45/t002]
---

## Objective

Update cli/docs/IMPORTING.md and the customer dedup convention in skills/.claude/skills/beancount-import/references/dedup.md in their owning package changes. Check migration-prefix consumers, CLI README quickstarts, root README/AGENTS package tables and customer/internal skill catalogs for actual impact. Keep Claude Code and Codex guidance equivalent and existing shared-skill links intact; avoid unrelated edits.

## Context

See [the milestone](./README.md) for exact reproduction, scope and compatibility requirements.

## Acceptance criteria

- [x] CLI and skills describe one exact identity and legacy policy.
- [x] Customer migration consumers are checked and any required updates are included.
- [x] Applicable skills CI and python3 scripts/check-agent-guidance.py pass.

## Out of scope

Backend/API changes, new dependencies, edits to real ledgers and unrelated import behavior.

## Resolution

`skills/.claude/skills/beancount-import/references/dedup.md` (normative) and `cli/docs/IMPORTING.md` now describe the same amount field, the same worked example, and the same legacy/lookup-only + content-check policy. Migration prefixes keep pointing at this one normalization section. `python3 skills/scripts/ci-check.py` and `python3 scripts/check-agent-guidance.py` pass.
