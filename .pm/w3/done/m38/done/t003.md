---
id: w3/m38/t003
title: "Verify the ledger-route metadata adoption surface"
worker: worker3
status: done
estimate: 15m
depends_on: [w3/m38/t002]
---

## Objective

Settle the indexability question these two routes could not previously express, and confirm nothing else in the repo describes the old behavior. Because head metadata is how completed `w2/m8` applies its `robots`/`noindex` policy, a route without a head block had no robots decision at all; adding one makes that decision explicit for the first time.

## Context

- Read `.pm/w2/done/m8/README.md` — its audit lists which surfaces are indexable and which get `noindex`, and it names neither `/accounts` nor `/budget`.
- Its stated policy leaves ordinary public reads on a public ledger indexable (`/journal`, `/statistics`) and marks transactional, private, write, thin and parameterized surfaces `noindex`.
- Verify on both a private authenticated ledger and the public `open_ledger/example`.

## Steps

1. Decide the robots value for each route against m8's policy and write the reasoning into this task and the milestone README.
2. Re-check both routes on a private and a public ledger, at 1440 and 390.
3. Grep `dashboard/CLAUDE.md`, the root `README.md` / `CLAUDE.md` and the skills trees for any statement about ledger page metadata that is now stale; flag drift as follow-up rather than editing unrelated packages.

## Files

- `dashboard/CLAUDE.md` (only if it describes this behavior)

## Acceptance criteria

- [ ] Each route's robots/indexability value is explicit and justified against w2/m8's policy, and recorded.
- [ ] No other route's robots value changed.
- [ ] Both routes verified on a private and a public ledger.
- [ ] No instruction file, README or skill still implies these routes carry no metadata.

## Out of scope

Re-auditing w2/m8's other routes, and any behavior change beyond the robots value for these two.


## Robots decision (2026-09-15)

Indexable (default — no `noIndex`). Matches w2/m8 policy for ordinary public ledger reads (`/journal`, `/statistics`, `/holdings`). `/accounts` was already named in `dashboard/src/common/lib/seo/indexability.ts`; `/budget` was added to that comment in this milestone. No other route's robots value changed.
