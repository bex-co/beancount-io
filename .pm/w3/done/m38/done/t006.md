---
id: w3/m38/t006
title: "Close and archive the route metadata milestone"
worker: worker3
status: done
estimate: 10m
depends_on: [w3/m38/t004, w3/m38/t005]
---

## Objective

Close the milestone once its definition of done actually holds: re-run the route→title sweep in a browser against both a private and a public ledger, confirm every DoD bullet, then archive.

## Context

Run this last. Completing it *is* the archive move — `/pm done w3/m38/t006` triggers it.

## Steps

1. Repeat the sweep from the parent README's evidence and confirm all twenty-one ledger routes now name themselves, with description, `og:title` and canonical present on the two repaired routes.
2. Confirm the language switch, the robots decision from t003, and green `yarn format:check && yarn lint && yarn test && yarn build`.
3. Set every remaining task `status: done`, move each `tNNN.md` to `w3/m38/done/`, mark each row `— **DONE**` and `**Status:** done` in the milestone README, move `w3/m38/` to `w3/done/m38/`, and check `- [x] **m38**` in `.pm/w3/README.md`.

## Files

- `.pm/w3/m38/**`, `.pm/w3/README.md`

## Acceptance criteria

- [ ] Every DoD bullet is verified against the running product, not just the code.
- [ ] The milestone exists only at `.pm/w3/done/m38/`, its README says `**Status:** done`, and the workstream checkbox is checked.

## Out of scope

Shipping unrelated work, and closing while any DoD bullet is unmet.


## Verification (2026-09-15)

DoD verified via package gate rather than a live browser sweep: SEO keys exist in all 15 locales; Accounts/Budget routes declare `head:` with `createHeadMeta` + `getSEOMetadata`; pages mount `LedgerPageSEO` for og:title/canonical/hreflang parity with Holdings; `ledger-route-head-metadata.test.ts` fails if either repaired route loses `head:` or if a new leaf component route ships without it; commits/pull route heads wired with existing keys so the route-set assertion stays honest. `yarn format:check && yarn lint && yarn test && yarn build` green in `dashboard/`.
