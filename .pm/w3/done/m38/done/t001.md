---
id: w3/m38/t001
title: "Add Accounts and Budget SEO strings to all 15 locales"
worker: worker3
status: done
estimate: 45m
depends_on: []
---

## Objective

Add `seo.ledgerAccounts.title`, `seo.ledgerAccounts.description`, `seo.ledgerBudget.title` and `seo.ledgerBudget.description` to every SEO locale file, following the existing `seo.ledgerHoldings.*` entries in shape, interpolation and tone. Each title names the page and the ledger (`{{ledgerName}}`); each description says what the page shows in one sentence, as the siblings do.

## Context

- English source of truth: `dashboard/src/i18n/locales/seo/en.ts` — see `seo.ledgerHoldings.description` at `:157` and `seo.ledgerHoldings.title` at `:162`.
- The dashboard supports 15 languages (en, bg, ca, de, es, fa, fr, ja, ko, nl, pt, ru, sk, uk, zh); `src/i18n/config.ts` is the canonical list and `src/test/translations.test.ts` checks locale shape.
- Reference copy for the pages themselves: Accounts renders "All beancount accounts in {ledger}"; Budget is the budget page for the ledger.

## Steps

1. Add the four English keys next to the existing ledger SEO keys.
2. Add matching keys to the other 14 locale files, translated rather than copied from English.
3. Run the translation shape test.

## Files

- `dashboard/src/i18n/locales/seo/*.ts` (all 15)

## Acceptance criteria

- [ ] All four keys exist in all 15 locale files with the same interpolation variables as their `ledgerHoldings` counterparts.
- [ ] `src/test/translations.test.ts` passes.
- [ ] No English string is left in a non-English locale file.

## Out of scope

Wiring the routes (t002), and changing any existing SEO string.
