---
id: w3/m19/t001
title: Validate complete amount tokens with one conversion contract
worker: worker3
status: done
estimate: 35m
depends_on: []
---

## Objective

Make every accepted amount represent its whole input token and use the same validated conversion in CSV parsing and inline editing.

## Context

Read the parent milestone for exact production evidence, source trace, working
controls and limits. Follow dashboard and importer guidance. This is the sole
implementation queue for promoted038 and its reproduced error-loss extension.

## Steps

1. Implement the documented signed-decimal grammar in csv-validator.ts. Preserve surrounding whitespace and finite signed values, including literal zero; reject unsupported grouping, trailing text, malformed exponents and base prefixes without stripping characters into a number.

2. Use the validated parse result in the inline edit path instead of independent parseFloat calls. Keep rejected input distinguishable from a legitimate zero for t002.

3. Pin the exact038 grouped-token and suffix cases against the real validator, with valid positive/negative/fractional/zero controls. Preserve current calendar-date behavior.

## Files

- dashboard/src/features/importer/utils/csv-validator.ts
- dashboard/src/features/importer/utils/row-edit-schema.ts
- dashboard/src/features/importer/hooks/use-csv-parser.ts
- dashboard/src/features/importer/components/steps/preview/editable-preview-row.tsx

## Acceptance criteria

- [ ] The038 tokens never silently become-1 or12; each accepted value reflects its full token.
- [ ] Upload and inline edit use the same validity and numeric result; literal zero remains valid.

## Out of scope

- Locale-specific grouping support, source/target posting signs and new dependencies.
