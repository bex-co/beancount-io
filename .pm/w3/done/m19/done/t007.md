---
id: w3/m19/t007
title: Test coverage — parsing, unrelated edits, repair and configuration
worker: worker3
status: done
estimate: 45m
depends_on: [w3/m19/t005, w3/m19/t006]
---

## Objective

Verify the actual importer pipeline rejects invalid values until their corresponding fields are repaired.

## Context

Read the parent milestone for exact production evidence, source trace, working
controls and limits. Follow dashboard and importer guidance. This is the sole
implementation queue for promoted038 and its reproduced error-loss extension.

## Steps

1. Use actual parser/FileReader, preview components and React Hook Form to assert038's token cases plus the three-row invalid-date/abc fixture. Editing only a payee must not change another field's validity or the configured row set.

2. Exercise true date/amount repairs, literal zero, invalid first/all-invalid preview states, structural diagnostics, duplicate rows, form remount, Enter/blur/Escape and validation settling before Continue. Coordinate deletion-specific coverage with037.

3. Assert the configuration boundary preserves exact validated calendar dates and amounts with a fixed test clock, and never uses that clock to replace bad input. Keep no-submit browser checks at desktop and390px.

4. Run dashboard format:check, lint, test and build from dashboard/. Record the complete outcomes; do not replace actual components with callback-only mocks that conceal the data transition.

## Files

- dashboard/src/features/importer/hooks/**tests**/
- dashboard/src/features/importer/utils/**tests**/
- dashboard/src/features/importer/components/steps/preview/**tests**/
- dashboard/src/features/importer/components/steps/configure/**tests**/

## Acceptance criteria

- [ ] Tests fail for the old prefix parser, partial-error overwrite and current-date fallback for the intended reasons.
- [ ] Desktop/narrow journeys retain1Valid/2Errors after unrelated edits and admit only correctly repaired values.
- [ ] All dashboard gates pass and no actual import or AI call is required for these CSV controls.

## Out of scope

- Production ledger mutations, weakening normal gates and tautological mock-only assertions.
