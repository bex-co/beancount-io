---
id: w3/m3/t004
title: Adoption surface
worker: worker3
status: done
estimate: 20m
depends_on: [w3/m3/t001, w3/m3/t003]
---

## Objective

Verify m3's user-facing changes (locales, any new filter/panel behavior) are reflected wherever mobile's features and i18n coverage are documented.

## Outcome (2026-08-24) — verified, no edits needed

t003 shipped nothing (double "no"), so m3's only user-facing surface is budget strings now localized. Docs already state the truth: `mobile/README.md` lists "Set and track budgets" and "the app ships with 13 locales"; root `README.md`'s package row says "budgets, … 13 locales"; `mobile/CLAUDE.md`'s i18n line enumerates the same 13. No doc claims budget is English-only or states a per-feature locale count, and no locale-count statement drifted (13 = en + 12 non-English files, unchanged by m3 — t001 filled keys in existing files only). `mobile/AGENTS.md` → `CLAUDE.md` symlink intact; `scripts/check-agent-guidance.py` passes (16 scopes).

## Context

- Same sweep as w3/m1/t010; additionally, anywhere the repo states mobile's supported-locale count or i18n process should still be accurate.

## Steps

1. Sweep `mobile/README.md`, `mobile/CLAUDE.md`, root docs; update or file follow-ups.
2. Verify `AGENTS.md` symlinks at touched scopes.

## Files

- `mobile/README.md`, `mobile/CLAUDE.md`, root docs (as needed)

## Acceptance criteria

- [x] Docs match shipped state; symlink checks pass.

## Out of scope

- Docs-site content.
