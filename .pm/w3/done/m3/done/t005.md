---
id: w3/m3/t005
title: Simplify
worker: worker3
status: done
estimate: 20m
depends_on: [w3/m3/t004]
---

## Objective

Run `/simplify` over m3's diff — behavior-preserving cleanup.

## Outcome (2026-08-24) — clean, nothing to apply

m3's code surface (locale files + parity tests) shipped inside `6119176` and was reworked by the later locale-integrity sweeps (`0c21ca8`, `178a9db`), so the diff under review here is the three decision-record edits (t002/t003/t004). All four /simplify angles ran over it: **reuse** — the dead analytics-sink finding appears nowhere else on the board, so the t002 record is the canonical first mention, not a duplicate; **simplification** — each outcome section is one paragraph with cross-references instead of restated context (task files stay readable standalone), nothing to trim; **efficiency** — prose only, no runtime surface; **altitude** — decisions live in the task files the DoD names, and the pipeline fix is routed to the w3 inbox rather than ballooning m3. Zero findings; the one action item — actually creating that inbox note — is done at closeout. `yarn test` green (see t007).

## Context

- Scope: locale files (consistency/ordering), any t003 UI changes.

## Steps

1. `/simplify` over the m3 diff; apply accepted findings.
2. `yarn test` green.

## Files

- m3-touched files only.

## Acceptance criteria

- [x] Findings applied or explicitly declined; `yarn test` passes.

## Out of scope

- New behavior.
