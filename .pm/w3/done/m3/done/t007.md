---
id: w3/m3/t007
title: Closeout
worker: worker3
status: done
estimate: 20m
depends_on: [w3/m3/t006]
---

## Objective

Close m3 — and the budget program — once locales are key-complete and the iteration decisions are shipped or explicitly declined with data.

## Outcome (2026-08-24) — DoD verified, m3 closed

- **Locale parity:** `yarn test` green (lint + typecheck + 1504 unit tests, exit 0), including the locale-integrity/parity suites that fail on any dropped or invented key. One count note, recorded rather than papered over: the DoD says "13 non-English locale files" but the app ships 13 locale files total — en + 12 non-English (`bg ca de es fa fr nl pt ru sk uk zh`). t001's context listed `ja`/`ko`, which never existed in `mobile/src/translations/`, and adding locales was explicitly out of its scope. Parity holds across every locale file that exists; no budget key falls back to English (the only deliberate English values, e.g. `budgetAccountPlaceholder`, are pinned in `known-gaps.ts`).
- **Decisions recorded with numbers:** t002 holds both — interval filter **no**, Home panel CTA **keep** — driven by zero collected events and the verified root cause (`mixpanelProjectToken: ""` → `track()` no-ops in every production build; the shipped release `1.20260814.42` carries the instrumentation but a dead sink).
- **"Yes" items shipped:** none — the double "no" is the data-justified result t003's context names as valid; t003 closed by decision with no code change.
- **Follow-up routed:** `w3/001.md` (revive or retire mobile analytics) so the next analytics-gated decision doesn't hit the same wall unaware.

## Context

- DoD: see `w3/m3/README.md`.
- Run `/pm done w3/m3/t007` last — completing it triggers the milestone move to `w3/done/m3/`.

## Steps

1. Verify DoD (locale parity test, decision records in t002, t003 outcome).
2. Close out tasks, milestone README, and workstream checkbox per board conventions.

## Files

- `.pm/w3/m3/**`, `.pm/w3/README.md`

## Acceptance criteria

- [x] DoD verified; board state consistent in all three places.

## Out of scope

- Follow-on budget ideas (widgets, notifications) — new inbox notes if they earn a place.
