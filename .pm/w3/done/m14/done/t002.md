---
id: w3/m14/t002
title: Publish the filtered count IDL contract and regenerate its client
worker: worker3
status: done
estimate: 30m
depends_on: [w3/m14/t001]
---

## Objective

Describe the new internal ledger report once in OpenAPI and regenerate the gateway's owning client through the IDL workflow.

## Context

- Use the service path and array result from t001.
- Accepted optional fields are account, filter and time; omission and empty strings follow existing analysis reads.

## Steps

1. Read IDL guidance and add the endpoint, result schema and invalid-filter errors to the source specification.
2. Run `yarn codegen` in `backend-cluster/idl/beancount-ledger-cli/` and `yarn generate-fava-client` in `backend-cluster/backend-v2/`; review generated client/type changes without hand-editing output.
3. Verify the generated client can pass all three filters and decode empty/nonempty counts.

## Files

- backend-cluster/idl/beancount-ledger.openapi.json
- backend-cluster/idl/CLAUDE.md (read for generation commands)
- backend-cluster/idl/beancount-ledger-cli/src/generated/api.ts
- backend-cluster/backend-v2/src/foundation/fava/Api.ts

## Acceptance criteria

- [ ] The source contract and generated client match the implemented endpoint and exact result fields.
- [ ] Omitted filters remain optional; no unrelated client/schema drift is introduced.
- [ ] Required IDL generation/check commands pass.

## Out of scope

Manual lockfile/generated-client edits and changing any existing report default.
