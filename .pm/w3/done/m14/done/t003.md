---
id: w3/m14/t003
title: Expose the count read through GraphQL, REST and MCP
worker: worker3
status: done
estimate: 45m
depends_on: [w3/m14/t002]
---

## Objective

Expose one protected filtered-count service through all eligible public adapters in the same change.

## Context

- Read backend-v2's required API parity workflow and docs/api-parity.md before editing.
- Planned names: getLedgerPostingsPerAccount, /postings-per-account, ledgerPostingsPerAccount; use the existing analysis descriptor and authorization patterns.

## Steps

1. Add the service interface/forwarder using the generated client and canonical LEDGER_REPORTS_READ protection.
2. Add the GraphQL typed result/arguments and ANALYSIS_READS entry for REST/MCP; register the real operation in VERB_TABLE and applicable schema/discovery mappings.
3. Regenerate public OpenAPI; execute a positive filtered read and relevant validation/refusal through the actual adapters.

## Files

- backend-cluster/backend-v2/src/features/ledger/service/ledger-data-service.ts
- backend-cluster/backend-v2/src/features/ledger/api/resolvers/ledger-data-resolver.query.ts
- backend-cluster/backend-v2/src/features/ledger/api/rest/v1/analysis-handler.ts
- backend-cluster/backend-v2/src/features/ai-agent/api/mcp-resources.ts
- backend-cluster/backend-v2/src/server/api/op-class.ts
- backend-cluster/backend-v2/docs/openapi/v1.json

## Acceptance criteria

- [ ] All adapters accept the same optional filters/defaults and return the same sorted counts.
- [ ] Current ledger relationships, credential pins/scopes and protocol-specific OAuth audiences remain enforced.
- [ ] Typecheck, unit/parity suite and OpenAPI generation pass; frozen baseline and zero-gap assertions are not weakened.

## Out of scope

Credential-policy changes, registry-only stubs, approximate BQL substitutes and production writes.
