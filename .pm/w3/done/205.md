# Keep date-dialog actions readable at the largest text size

**Minor · 35–50m · mobile.** A2: users needing enlarged text can read the controls that accept or cancel a date without changing system settings.

Environment: September13,2026, HEAD84231f9c, production https://beancount.io, authenticated QA, public open_ledger/crypto-example, iPhone17e/iOS26.5, native io.beancount.ios1.20260906.47(47), System/light, app language German. No ledger mutation or applied filter.

Repro: set iOS content_size accessibility-extra-extra-extra-large and relaunch. Open Transaction Filters, choose Benutzerdefiniert (Custom), scroll down and tap the end-date row BIS2026-09-13. The date modal opens with its ordinary September13,2026 wheel, but both Confirm and Cancel grow beyond their separate action cards: their lower portions are cut off. Initial fresh PID30391 and second fresh PID32356 reproduce it. The filters destination was reached with inspected internal route `beancount:///(app)/transaction-filters`; Custom, scrolling and date selection used actual controls. The preceding default-size flow used the actual Transactions tab and filter icon. Dismiss the development warning banner in the repeat: clipping remains in both cards. At default-large text size after fresh launch35002, the same Custom/end-date dialog shows both labels fully. Cancel closes it; Cancel on the filter sheet discards the unsaved range. No Confirm or Apply required to reproduce.

Root: `mobile/src/components/date-picker-modal/date-picker-modal.tsx:17–36` supplies ThemedCancelButton with unrestricted20pt Text; styles75–87 constrain its card to height57. The wrapper at55–70 keeps the library's default Confirm. Installed `react-native-modal-datetime-picker/src/DateTimePickerModal.ios.js:246–303` also uses unrestricted Text in a height57 TouchableHighlight. Neither scales the card with the text nor bounds label growth. This is the application's shared date wrapper and its supported customization seam, not the separate account/theme Picker. The wrapper deliberately pins locale=en_US; English wheel order is documented and is outside this finding.

Fix: give both date-dialog actions a tested sizing policy through DatePickerModal, using the library's customConfirmButtonIOS hook and the existing custom cancel hook as needed. Keep the full labels inside visible, hittable cards at maximum text size; preserve useful text growth, theme tokens, labels/roles, callbacks and native date state. A deliberate Text scaling bound for these constrained cards or adaptive heights that still fit the whole modal is acceptable. Do not edit node_modules, add a dependency, globally disable Dynamic Type, or change the documented date-order policy. Keep default-size geometry and caller overrides deliberate.

Blast radius: five production consumers of this wrapper: Transaction Filters, MultiPostingsTransactionScreen, AddTransactionNextScreen, OpenAccountScreen and AddBudgetScreen. Only Filters was reproduced at maximum size; a default-size New Transaction date dialog was also exercised earlier. No API changes. Loading/denied/server errors do not determine this local layout; unsaved draft, actual date changes, VoiceOver activation, dark mode, Android and physical devices are not newly verified.

Acceptance: both action labels are complete at default, accessibility-large and maximum text sizes on iPhone17e; confirm/cancel touch targets remain usable; Cancel preserves the prior date and Confirm returns the selected date exactly once in an isolated draft. Verify the shared wrapper's actual text/layout props and native screenshots, not just a style constant. Check a second caller for integration without saving any production entry.

Dedupe/history: searched every open/done queue for DatePickerModal, date picker,57-point cards, Confirm/Cancel and enlarged-text clipping, and scanned open milestones. Completed167 explicitly concerns the distinct account/theme Picker and calls the filter date target unrelated; completed151/new204 concern native navigation bars; completed182 is the shared Button, unused by these actions. Dashboard m24/m42 own typed-date parsing, not this native wrapper. git-log -S height57 points to1a2eda9f; later5ee21328 changes theme treatment and retains the geometry. No current-main repair or matching open item found.

Verified ignored evidence under `mobile/tmp/qa-2026-09-13/`: german-max-date-picker.jpg, date-modal-max-repeat-clean.jpg, date-modal-default-control.jpg, german-max-custom-range.jpg. Screenshots946×2048 represent390×844 logical points. The retained clean repeat has no development banner over the actions; the earlier banner-covered repeat is excluded. This layout has no API-dependent input; no mocked response or source mutation was used. Default-large text restored and all filter/date drafts canceled. No product implementation or shipping.


## Resolved

Fixed and shipped in PLACEHOLDER.
