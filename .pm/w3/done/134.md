# Cash Flow keeps an old error after the filter is corrected

**Minor · 45–60m · dashboard.** A2: a successful corrected filter should replace the previous error without requiring an unrelated extra retry. State-recovery follow-up to completedm15; keep its historical group count unchanged.

Production https://beancount.io, 2026-09-11; authenticated reader of public synthetic open_ledger/freelancer-invoicing, English/light, isolated headless Chrome152. Fresh1440×1000 and390×1000 real UI repeats; no delayed or substituted responses.

1. Open /ledger/open_ledger/freelancer-invoicing/cash-flow?lang=en and wait for the populated report/Export.
2. Enter2026-02-30 in Time; at390 use Filters then close that sheet. The view becomes Something went wrong / This section couldn't be displayed / Try Again. Retrying the still-invalid selection repeats the error.
3. Correct Time to2026-01. Wait for the exact GetLedgerCashFlow request to complete: HTTP200, data present, no GraphQL errors. After another600ms, the URL showsJanuary but the old error boundary remains and Export is absent.
4. Press Try Again only now. The valid January report appears and Export returns, without another filter change. Both widths reproduce. A separate cold-load correction also needs this extra action; directly loading the valid January URL works.

Important limit: the desktop filters initially looked absent on a cold error page because their attributes read was still loading. They subsequently render and remain available during the ordinary UI journey; no permanent loss of filter controls is claimed. A direct invalid-date Cash Flow document returned500, while the backend correctly classifies its parse failure as BAD_USER_INPUT. React's section boundary logs the caught error; no uncaught pageerror event was needed.

Root: features/reports/cash-flow/loader.ts:12–26 directly awaits the report query, propagating a parameter/read error into the route outlet. The sibling Trial Balance/Balance Sheet/Income Statement loaders settle their report-query failures so their page-level error states can render. common/components/ledger-layout/index.tsx:100–103 wraps Outlet in ErrorBoundary keyed only by pathname. Its common/components/error-boundary.tsx:52–54 clears hasError only on Try Again; changing time on the same pathname leaves the old boundary mounted after the new loader succeeds. This is stale error state, not stale financial data or failed parsing of the corrected date.

Fix the Cash Flow report-read lifecycle: keep expected query failures in the classified page error state with usable filter controls, matching sibling report behavior, and allow a corrected successful scope to render automatically. Forward the caught error into ReportErrorState too. Preserve parent-ledger access gating, real programming-error boundaries, loading/empty states and existing statement calculations; do not globally swallow loader/access errors or clear every application boundary on every URL change. The potential partial-data memo dereference was only a source-review suspicion; no such TypeError was reproduced and it is not the claimed root cause.

Acceptance: real router + controllable Cash Flow query rejects invalid input, then resolves January; content recovers without pressing Try Again after correction at both widths. Classified guidance is safe/localized; retrying an unchanged invalid value remains an input error; valid direct SSR/client loads and denied parent-ledger access preserve their contracts. Run dashboard format/lint/test/build on implementation. No backend/dependency change.

Dedupe: searched all open/done records for Cash Flow invalid/error, boundary reset and filter recovery; m15's resolved-URL/state contract covers this additional residual case, but its implemented provider synchronization does not clear this boundary.129 owns held successful reads with no pending indication; coordinate its rejected-read acceptance with this note, without duplicating implementation. Native113 concerns rejecting reversed dates before submission; m24 concerns form date parsing. The Cash Flow loader was introduced in47672769 and its filter inputs changed ina462057c; no newer fix in current/fetched f1965988. Full deployed SHA unverified.

Evidence dashboard/tmp/qa-20260911-w3-loop/: cashflow-invalid-inspect.json, cashflow-invalid-journey-{1440,390}.json, cashflow-correction.json, cashflow-correction-repeat-{1440,390}.json (exact successful response bodies), cashflow-corrected-stuck-{1440,390}.png, cashflow-invalid-1440.png and invalid-period-api.json. Corrected narrow screenshot visually inspected. The first generic-report survey assumed Cash Flow used the same error card and timed out; the subsequent survey/source trace established its distinct boundary. All contexts closed. No product fix, production mutation or shipping. Offline/denied query variants, other browsers, native screen-reader and patched behavior remain unverified.

---

**Resolved 2026-09-11** — fixed and shipped in `86ca4531` (`dashboard`), with the package's full check gate green. Triage confirmed the reported root cause against current code before the fix; the note was not stale.
