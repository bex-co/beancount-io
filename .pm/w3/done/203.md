# Give cold native detail links a working return destination

**Minor · 45–60m · mobile.** A2/A3: someone opening a shared transaction should be able to continue into the ledger instead of being stranded on a detail screen. Estimate assumes a focused navigation-boundary fix; promote if auth/route restructuring exceeds an hour.

Environment: September13,2026, https://beancount.io, HEAD84231f9c, native io.beancount.ios1.20260906.47(47), Expo57/RN0.86, iPhone17e/iOS26.5, English/System/light/default large. Existing authenticated QA session, public open_ledger/example, read-only. No app data cleared or ledger mutation performed.

1. Terminate the app, then open this valid native link while it is stopped: beancount:///ledger/open_ledger/example/entry/1866463f43c2caf740964eae7ab5870d . For the simulator use simctl terminate followed by simctl openurl, without launching Home between them.
2. Wait for the transaction to load. Starbucks, June3,2017, -$7.25 appears, with Liabilities:US:Chase:Slate -$7.25, Expenses:Food:Coffee +$7.25 and #trip-boston-2017.
3. Tap the visible header Back button at(40,68). The same entry remains. Development output reports that the action GO_BACK was not handled by a navigator. The warning is evidence; the product failure is the inert Back action and absence of a tab/drawer return route on this screen.
4. Terminate and repeat the cold link and Back. The failure repeats. No chart touched, so this is independent of w3/191's worklet exception.
5. Working control: terminate, launch normally, wait for Home, then open the exact same link. It displays the same transaction; one Back returns to Home. This control ran in PID16950, with the entry independently captured and Back repeated. Fresh getLedgerEntryContext for that exact ledger/hash returns the real entry/source, ruling out an invalid fixture or missing data.

Expected: cold linked details provide a working way back into the selected ledger's normal app navigation. Preserve the actual previous screen for warm links and ordinary in-app pushes. Do not solve this by hiding the only exit, logging less, opening an external browser, or unconditionally replacing every Back action with Home.

Root: mobile/app/(app)/_layout.tsx:15–38 always renders DefaultHeaderLeftBack with onPress=router.back, including root entries with no history. The layout supplies initialRouteName="(tabs)" only as a Stack prop at57; it exports no deep-link anchor settings. Installed expo-router/build/getRoutesCore.js:651–675 derives the linking route's initialRouteName from the layout's unstable_settings.anchor/initialRouteName, and getReactNavigationConfig.js:59–64 forwards that route metadata to the linking config. A Stack render prop alone does not establish this cold-link back entry. app/+native-intent.tsx rewrites the incoming route; common/app-links/open-app-link.ts:68 replaces that destination after the readability probe, preserving the history-free cold state. Native failure/warm control confirms absent back handling; the full navigator state was not dumped.

Fix: establish a valid authenticated tabs anchor for cold non-tab destinations using this installed router's supported route configuration, and/or give the shared Back action an appropriate no-history fallback. Ensure the linked ledger is selected/readable before showing its return destination. Preserve warm history, the existing duplicate-link replacement fix, canceled/expired auth handling and the cross-ledger account guard. Do not add a second copy of the linked entry or expose a previous ledger's detail under the new selection.

Acceptance: cold valid transaction link → loaded entry → one header Back reaches the selected ledger's Home/appropriate normal landing screen with usable tabs, without an unhandled GO_BACK; warm same-link and ordinary detail Back preserve their original destination. Add meaningful linking/navigator coverage, not a test merely asserting router.back was invoked. Check account, commit and file detail aliases and their overrides as shared-boundary regression coverage; test signed-out continuation/cancel and unreadable targets before closing. No REST/GraphQL/MCP behavior change is needed.

Blast radius: the single DefaultHeaderLeftBack renderer is installed by the authenticated Stack; all non-tab screens without a header override inherit it. Public link resolver aliases include account-detail, transaction-detail, commit-detail, ledger-file-editor and budget. Only the public transaction cold/warm pair is a retained finding here; an earlier cold internal Merchants route also produced GO_BACK but is not the public reproducer. Android, physical Universal Link dispatch, signed-out flow, denied/private links and the other aliases remain unverified.

Dedupe/history: full .pm open/done searches for cold Back/history, unhandled GO_BACK and no-op Back found no open matching repair. Completed w4/m5 covers link landing; done w3/122 binds retained accounts across ledger switches, done159 labels Back, and new201 fixes a missing-file button's specific destination. Openm43 covers the browser's absent entry route. None provides a cold native return destination. git log -S initialRouteName traces the Stack prop to182a07f1; later label changes retain unconditional router.back. Current source and fresh native repeats agree; not deployment lag.

Verified ignored evidence under mobile/tmp/qa-2026-09-13/: cold-entry-native.jpg, cold-entry-back.jpg, cold-entry-repeat-back.jpg, warm-entry-home-origin.jpg, warm-entry-control.jpg, warm-entry-back-home.jpg and cold-entry-api.json. Screenshots show the native warning text; no credentials or private account payloads are required. Original light/large/System/English preferences remain restored, and the warm control returns to Home. Report only; no implementation or shipping.


## Resolved

Fixed and shipped in PLACEHOLDER.
