# Holdings loses the selected grouping after an account drill-down

Severity: minor. Owner: dashboard. Estimate: 40–55 minutes. A2: let readers inspect an account and return to the same holdings grouping.

Production https://beancount.io, 2026-09-11, isolated headless Chromium152, authenticated QA reader, public synthetic open_ledger/example, English/light, fresh1440×1000 and390×1000. Local/fetched main f1965988; latest observed entry main-RkgtsKH_.js, exact deployed commit unverified.

1. Open /ledger/open_ledger/example/holdings?lang=en and select “Holdings by Account” (desktop tab or narrow View option). The grouped table has11 body rows.
2. Activate the Assets:US:BofA:Checking account button. Its account page opens correctly.
3. Browser Back returns to the same Holdings URL, but the selected view is now “Holdings” and the table has115 body rows. Both fresh widths reproduce without page errors.

Expected: restore Holdings by Account and its11 rows so the reader can continue comparing accounts. The API has not lost grouping support: selecting By Account still produces the grouped table. A separate fresh desktop→390→desktop resize control preserves By Account and11 rows throughout, proving the responsive selector itself is controlled correctly. Earlier real QueryShell200 evidence in holdings-export-views.json covers all four query variants; raw13 grouped rows become11 through the deliberate row filter. This is navigation-state loss, not an API row-count or valuation defect.

Root: dashboard/src/features/ledger-data/holdings/index.tsx:32 initializes selectedTab only in component useState("holdings"). The controlled Tabs at43–47 and ResponsiveTabTriggerList at50–53 share that value, but it is never stored in navigation state. DatasetTable account actions in holdings-table.tsx:151–168 and197–212 navigate away and unmount the page. The route dashboard/src/routes/ledger.$ledgerOwner.$ledgerName.holdings.tsx has no view search validation/restoration, so Back remounts the default.

Fix: keep a validated Holdings-specific view in route/history state and derive the controlled tabs from it. Accept only holdings, by-account, by-currency and by-cost-currency with a safe default. Preserve it through account drill-down/Back without leaking it into global account/time/filter behavior; avoid adding incidental history entries per rerender. Keep direct no-view links at the current default and the four query/row-filter mappings intact. No dependencies, API changes or account mutations.

Acceptance: desktop/narrow By Account→account→Back restores By Account/11 rows; Forward remains usable. Exercise both row and named account-button navigation, valid/invalid view state and all four selectors; responsive resizing must still retain the selected view. Add a meaningful router unmount/Back regression against the real state owner, plus safe invalid-view handling. Preserve loading, query-error and empty-result states for the restored view. Run dashboard format/lint/test/build on implementation.

Dedupe: searched all queues including done for Holdings Back/grouping/state persistence, plus targeted history. 120 owns Accounts search/type state and131 Journal pagination; these are separate state owners and do not restore Holdings. 126 is Trial Balance's mismatched controlled/uncontrolled selectors (the responsive control passes here). Completedm25 owns row semantics/activation,150 narrow CSV visibility, and m15 shared global filters. No existing Holdings restoration task found. The local-only default dates to af5339de; later356f6ee6/071d0db2 leave it unchanged, and refreshed origin/main has no repair.

Evidence under dashboard/tmp/qa-20260911-w3-loop/: holdings-drill-back-1440.json, holdings-drill-back-390.json, holdings-drill-back-1440.png, holdings-drill-back-390.png, holdings-tab-resize-control.json, holdings-by-account-before-drill.png and holdings-export-views.json. Narrow returned screenshot visually inspected. All fresh contexts closed. No product changes, production mutations or shipping. Full reload/Forward/invalid-state and failure recovery of a patched implementation remain acceptance work, not claimed live coverage.

---

**Resolved 2026-09-11** — fixed and shipped in `4d701a91` (`dashboard`), with the package's full check gate green (format, lint, 3879 tests, build). Triage confirmed the reported root cause against current code before the fix.

Beyond the reported defect, the fix closed a latent crash: TanStack Router surfaces raw URL values through `useSearch` for keys a route's `validateSearch` omits, so `?search=["x"]` reached the Accounts page and threw. Each view now re-coerces at the use site, with a hostile-input regression per suite.
