# Editing a multiline import description joins its lines

**Minor · 40–60m · dashboard.** A2: correcting imported narration must retain the original text boundaries.

Production https://beancount.io, 2026-09-11, isolated headless Chrome152, English/light, authenticated QA reader of public synthetic open_ledger/freelancer-invoicing. Fresh1440×1000 and390×1000. Local/fetched main f1965988; deployed SHA unverified.

1. Open /ledger/open_ledger/freelancer-invoicing/import?lang=en and choose this local CSV:

```csv
Date,Payee,Description,Amount
2026-09-10,QA Supplies,"First line
Second line",-4.50
```

2. Preview correctly shows one valid transaction. Description button's accessible value contains `First line\nSecond line` (an actual newline).
3. Open Description. Its text input value is already `First lineSecond line`.
4. Press End, append ` updated`, then Enter. The preview now contains `First lineSecond line updated`, without a separator between the original lines.
5. Continue to Configure: that joined description persists in the selected transaction, with the correct date, payee and -4.5 amount. Do not click Import or AI Fill.

Both fresh widths reproduce. Separate desktop control: Enter without any edit preserves the original newline in stored preview state; appending then Escape also preserves it. Appending then Tab commits the joined text, like Enter. No page errors or attempted writes. Screenshot inspected. The parser's multiline support works; the editing control loses text only after an actual input change.

Root: dashboard/src/features/importer/components/steps/preview/editable-cell.tsx:124 renders the shared native Input with type=text for description. The browser removes line breaks from the single-line input's DOM value. handleChange at114 then replaces localValue with that sanitized value on the next edit; commitValue at71 propagates it. editable-preview-row.tsx:134 binds that value to the description and buildParsedRow; transaction-config-form.tsx:124 copies it unchanged. Initial localValue retains the original string, explaining unchanged-Enter and Escape controls. The parser use-csv-parser.ts:18–60 deliberately preserves embedded lines (CRLF normalized to LF). This is entirely client-side, so no matching ledger API mutation/read is involved in the text transformation.

Fix in importer: provide a multiline-capable editor for free text, using the existing textarea primitive, preserving line boundaries through typing, blur and explicit commit. Make newline insertion and commit shortcuts clear and accessible; retain Escape cancellation and focus restoration from047. Keep Date/Amount single-line. Search finds only EditablePreviewRow using EditableCell, four fields Date/Payee/Description/Amount; payee uses the same text editor and deserves coverage, but its multiline failure was not exercised live. Do not change the global Input primitive or silently flatten descriptions in the parser.

Acceptance: the exact description remains `First line\nSecond line updated` through real typing and Enter/blur commit paths and Configure; define a usable multiline insertion shortcut. Unchanged commit and Escape preserve original content, focus returns correctly, and one-line text/date/amount editing and validation still work. Include a real browser regression because native text-input sanitization is the mechanism. Repeat at desktop/narrow width; run dashboard checks when implementing. Actual persistence and AI input remain unverified and out of this read-only reproduction.

Dedupe: searched all .pm queues including done for multiline/newline/line breaks and preview editors. Full historical015 (closed47394449) owns parsing logical CSV records, which now works; full047 (closed20be4691) owns Space activation/focus, also working. Full done/m19 owns raw amount/date validity and unrelated-field errors, not multiline editing. m33 owns Configure draft lifetime across Back. CLI w1/done/016 concerns unterminated CSV records, another implementation. No overlapping open repair found. EditableCell still uses a text Input on main; no subsequent multiline editor fix found in targeted history.

Verified ignored evidence in dashboard/tmp/qa-20260911-w3-loop/: import-multiline-edit-1440.json, import-multiline-edit-390.json, import-multiline-control.json, import-multiline-edited-1440.png. A first screenshot helper failed locally; its context closed, then the corrected fresh control completed. All probe contexts closed, no production changes, product fixes, commits or shipping.

---

**Resolved 2026-09-11** — fixed and shipped in `008aca68` (`dashboard`), with the package's full check gate green. Triage confirmed the reported root cause against current code before the fix; the note was not stale.
