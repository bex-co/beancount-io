# Keep transaction posting account names readable at enlarged text sizes

Minor · 30–45m · mobile · A2: a newcomer using larger text can identify both accounts in a transaction's money flow.

Environment: September13,2026, https://beancount.io, HEAD84231f9c, native io.beancount.ios1.20260906.47(47), iPhone17e/iOS26.5, authenticated QA session, public read-only open_ledger/example, English/System/light. No ledger mutation.

Repro: open the real public entry link `beancount:///ledger/open_ledger/example/entry/2336fd613c49c037cedca0b2529c1b71` (Legal Seafood, June3,2017). At default text size, Money flow shows the complete `Liabilities:US:Chase:Slate` and `Expenses:Food:Restaurant`, with −$33.82 and +$33.82. Set the simulator to accessibility-extra-extra-extra-large, terminate/relaunch, reopen the link, dismiss the development warning, and swipe upward once. Both fully visible posting rows reduce their account names to `Lia…ate` and `Exp…ant`; the distinguishing hierarchy and names cannot be read. A second fresh launch (PID7290, following6700) reproduces the same settled result. Restoring default large and relaunching (PID7671) restores the complete names. This proves the linked destination and real native scrolling; it does not claim the transaction-list journey was exercised for this entry.

Expected: the money-flow rows let users read each posting's complete account identity at the supported text size, while retaining the signed amount and account navigation. Actual: large rows still constrain account text to one line and replace most of each identity with an ellipsis. The separate tag chip wraps `#trip-boston-2017` completely at maximum size, and both amounts remain readable: useful adjacent controls. An initial live text-size change produced stale measurements; that transient capture is excluded from the finding.

Root: `mobile/src/screens/transaction-detail-screen/components/posting-row.tsx:13–26` puts the flexible account column beside the amount and chevron; at77 its Text explicitly has `numberOfLines={1}` and `ellipsizeMode="middle"`. Row height can grow, but the name cannot wrap. The producer `selectors/select-transaction-detail.ts:94–103` retains `p.account` unchanged; the sole parent at `transaction-detail-screen.tsx:532–544` passes that full identity to the row and navigation. The public GraphQL journal control returns precisely these two accounts/amounts for this hash. This is consumer layout, not lost source data.

Fix this PostingRow with wrapping or an adaptive stacked arrangement that keeps the full account and signed amount readable. Preserve meaningful text enlargement, FROM/TO association, currency/precision, account press targets and the full accessibility label already provided at71. Keep ordinary-size geometry and lower metadata reachable. Do not truncate amounts or globally disable font scaling. No API or dependency change is needed.

Blast radius: this exported PostingRow has one production parent, the shared transaction-detail screen reached from Home, Transactions, Accounts, Reports, Merchants and public entry links. The similarly named local PostingRow in multi-postings-transaction is a separate editor component. Only the two-posting linked detail above was reproduced; multi-posting detail, other currencies, RTL, Android and physical VoiceOver remain unverified. Loading, missing/denied/expired/network-error branches do not render these populated rows and must retain their current handling.

Dedupe/history: searched the root board across every open/done queue for PostingRow/posting-row, money flow, account truncation and middle ellipsis, and scanned all open milestone titles. No matching detail-row item. Open217 owns the separate Accounts table;209 owns the detail title's two-line cap; completed183 owns Reports CategoryBreakdown, and149 owns editable posting accessibility. File history7ad2aabc/0a7b6d24/429483f5 retains the constraint; targeted history traces middle ellipsis to4d3d3e44. No newer fix on tested main.

Acceptance: repeat the linked entry at maximum/default text sizes and read both complete account names, both signed amounts and the full tag; scroll to all content and open the intended account from each row. Include meaningful constrained-layout coverage rather than a source-text assertion, preserve the existing accessible full label and exact amount formatting, then run mobile checks after implementation.

Verified ignored evidence under `mobile/tmp/qa-2026-09-13/`: `example-tagged-legal-seafood.jpg` (default), `tagged-max-fresh-bottom.jpg`, `posting-name-repeat-bottom.jpg`, `posting-name-default-restored.jpg`, `example-all-transactions-api.json` (hash above). Screenshots946×2048 represent390×844 logical points. `posting-name-console.json` is a bounded diagnostic capture, not proof of an error-free session. Normal text size restored; no product fix, commit or shipping.

---

**Resolved 2026-09-15** — fixed and shipped in `5256e2b1` (`mobile`), with the package's checks green. Triage confirmed the reported root cause against current code before closing. Same Money-flow PostingRow stacking fix as w4/014.

