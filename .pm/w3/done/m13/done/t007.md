---
id: w3/m13/t007
title: Test coverage — filter, balance and pagination behavior
worker: worker3
status: done
estimate: 45m
depends_on: [w3/m13/t005, w3/m13/t006]
---

## Objective

Add missing behavioral checks for the full filtered account read, focusing on cross-layer failure modes.

## Context

See [the reproduced failure and fix constraints](./README.md). Keep work within
its owning package; use the documented contracts across package boundaries.

## Steps

1. Exercise mixed directive/subtype fixtures across page boundaries and compare retained running balances with the unfiltered baseline.
2. Cover date clamp, child accounts, conversion, omitted/empty filters, and unchanged shared main-journal behavior.
3. Test real transport parsing/forwarding and rendered UI selection, loading, empty/error and page reset; run each changed package's native gates.

## Files

- `backend-cluster/ledger/src/features/ledger/`
- `backend-cluster/ledger/src/foundation/rustledger/__tests__/`
- `backend-cluster/backend-v2/src/features/ledger/`
- `backend-cluster/backend-v2/src/features/ai-agent/api/`
- `dashboard/src/features/reports/account/`

## Acceptance criteria

- [ ] Tests fail if filters are ignored, applied only after page slicing, or alter retained balances.
- [ ] Counts and every page agree with the selected row set; stale unfiltered results are not presented as settled filtered output.
- [ ] Applicable package checks and contract generation checks pass with recorded evidence.

## Out of scope

- Production ledger writes, unrelated report calculations, and other QA findings.
