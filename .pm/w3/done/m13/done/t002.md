---
id: w3/m13/t002
title: Publish the account filter contract and regenerate the IDL client
worker: worker3
status: done
estimate: 30m
depends_on: [w3/m13/t001]
---

## Objective

Describe the implemented optional account-journal selectors in the ledger OpenAPI contract and regenerate the owning IDL client.

## Context

See [the reproduced failure and fix constraints](./README.md). Keep work within
its owning package; use the documented contracts across package boundaries.

## Steps

1. Read IDL guidance and update the account-journal operation from the authoritative service contract.
2. Regenerate the IDL client from its owning package using the existing codegen command; never patch generated output.
3. Check types and retain existing default/array encoding semantics.

## Files

- `backend-cluster/idl/beancount-ledger.openapi.json`
- `backend-cluster/idl/beancount-ledger-cli/`

## Acceptance criteria

- [ ] The spec describes all four optional lists and their wire encoding/defaults.
- [ ] The generated client can express the filtered account read without casts or new dependencies.
- [ ] Existing account-journal callers remain valid.

## Out of scope

- Production ledger writes, unrelated report calculations, and other QA findings.
