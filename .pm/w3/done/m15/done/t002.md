---
id: w3/m15/t002
title: Preserve same-ledger filter context in links and loaders
worker: worker3
status: done
estimate: 40m
depends_on: [w3/m15/t001]
---

## Objective

Make a report link describe the same filtered result before navigation, after
navigation, after reload and when opened in a new tab.

## Steps

1. Apply a shared same-ledger retention policy to account/filter/time. Verify
   RelatedLinks, expanded and collapsed sidebar links, and account drill-down.
2. Reset inherited values when switching ledgers unless the target explicitly
   supplies them. Do not copy unrelated action/directive/query/file state.
3. Bind Overview, Balance Sheet, Income Statement, Trial Balance and Cash Flow
   loaders to validated target-route search/dependencies rather than a separate
   global current-URL snapshot. Preserve the report guidance's loader boundary.
4. Enumerate all ten production provider consumers and verify they keep their
   existing supported-filter and loading/error contracts.

## Files

- `dashboard/src/common/components/related-links.tsx`
- `dashboard/src/common/components/ledger-layout/ledger-sidebar.tsx`
- `dashboard/src/routes/ledger.$ledgerOwner.$ledgerName.tsx`
- `dashboard/src/features/reports/*/loader.ts`
- Related ledger links and existing search helpers as needed

## Acceptance criteria

- [ ] Journal 2016 → Balance Sheet retains the URL period and 80149.80683 USD
      assets at cost through reload/new-tab on desktop and narrow screens.
- [ ] Server and client use the destination's selection and reach the same
      settled values; no unnecessary request with discarded stale filters remains.
- [ ] Ledger changes and existing journal/file action links still work as specified.

## Out of scope

New filter controls on intentionally unsupported reports or changing report math.
