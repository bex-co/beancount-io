---
id: w3/m15/t005
title: Test shared filter navigation, history and hydration
worker: worker3
status: done
estimate: 45m
depends_on: [w3/m15/t003]
---

## Objective

Pin the observed failures through the real router/provider integration.

## Steps

1. Navigate between real route definitions with an active filter, then traverse
   Back/Forward and reload/direct-load the destination. Assert URL, controls and
   outgoing report variables agree rather than mocking the filter hook itself.
2. Cover time=2016, account and quoted expression values, explicit clear,
   existing double-encoded links, unrelated route state and a ledger change.
3. Check SSR/initial hydration and loading→settled behavior with delayed data;
   keep old results from being represented as a settled different selection.
4. Repeat the public reproducer at desktop/narrow and run dashboard
   `yarn format:check`, `yarn lint`, `yarn test`, and `yarn build`.
5. Exercise both real Money movement View all links and category navigation
   after changing the selected month. Assert the destination's values and journal
   scope; include a partial monthly range and preserve non-time filters. Generic
   Account balances navigation must retain its own scope.

## Acceptance criteria

- [ ] Regression assertions fail with the original provider/raw-history code.
- [ ] Repeated navigation stays correct; tests cover values and requests as
      well as URL text, with no mock-only or implementation-mirroring tests.
- [ ] Required package checks pass and remaining unverified states are recorded.
