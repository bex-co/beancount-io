---
id: w3/m7/t003
title: The same sixteen as MCP resource templates
worker: worker3
status: done
estimate: 60m
depends_on: [w3/m7/t001]
---

## Objective

Expose the sixteen as MCP resource templates over the same services t002 uses, with URI-template parameters named as t001 fixed them.

## Context

- Needs m5/t004's resource layer.
- **Same service call as t002.** Two adapters over one service; anything else drifts.
- RFC 6570 templates carry the date range and interval, e.g. `…/{owner}/{name}/account-report/{account}{?from,to,interval}`.
- The tool list must not grow — that is the entire reason these are resources (ADR 0008 D2).

## Steps

1. Register the sixteen templates against the existing services.
2. Authorize per read through `authorizeLedger`.
3. Drive `resources/templates/list` and `resources/read` with a real client.

## Files

- `src/features/ai-agent/api/` (resources fragment), `src/server/api/op-class.ts`

## Acceptance criteria

- [ ] All sixteen appear in `resources/templates/list` and read real data.
- [ ] Each resolves through the same service function as its REST twin.
- [ ] `tools/list` still returns seven tools.
- [ ] A read after revocation is refused.

## Out of scope

- `subscribe` / `listChanged`.
