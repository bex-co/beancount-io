---
id: w3/m7/t005
title: Adoption surface
worker: worker3
status: done
estimate: 20m
depends_on: [w3/m7/t004]
---

## Objective

Make sixteen new reads discoverable on both surfaces.

## Context

- The REST half rides the regenerated OpenAPI document; the MCP half is discoverable only through `resources/templates/list`, so the template descriptions are the discovery mechanism and have to read well.
- `backend-cluster/backend-v2/README.md` and the root `README.md`.

## Steps

1. Update both READMEs.
2. `yarn mcp:conformance` against a local stack.
3. `python3 scripts/check-agent-guidance.py` if instruction files changed.

## Files

- `backend-cluster/backend-v2/README.md`, root `README.md`

## Acceptance criteria

- [ ] Both surfaces documented; template descriptions readable in a raw `resources/templates/list` response.
- [ ] `check-agent-guidance.py` passes.

## Out of scope

- Documenting unported verbs.
