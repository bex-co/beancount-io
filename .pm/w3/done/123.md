# Mobile Home budget rows omit amounts from native accessibility

Minor; estimate30m, mobile. A2/A3: assistive-technology users need the same budget comparison visible on Home.

Environment: September11,2026 main f1965988/mobile7538fb0d, native io.beancount.ios1.20260906.47 on iPhone17e iOS26.5. Production public synthetic open_ledger/budgeting-envelopes, authenticated read-only QA account, English/System/Light. Seven budget directives; no production changes.

Repro: select public budget ledger, scroll real Home down to Budget. Visible rows Dining $0.00/$200.00, Groceries $0.00/$550.00, Insurance:Car $0.00/$100.00. Inspect native XCTest accessibility metadata. Each row is a hittable enabled Button with only its full account as label, value:"", children:0. Example {label:"Expenses:Dining",value:"",enabled:true,hittable:true,children:0}. Terminate/relaunch PID96755, verify budget fixture Home, dismiss dev warning, scroll twice with native XCTest, repeat metadata: identical for all3 rows. Screenshot confirms visible amounts remain.

Expected: each accessible budget row conveys account, actual amount, budget amount/currency and meaningful progress/favorability, while retaining its button action. Actual native accessibility exposes none of the visible amounts or progress and no separate descendant elements expose them. This is metadata verification, not an actual VoiceOver audio/session claim. The impact inferred from the absent accessible content is that a reader cannot obtain Home's comparison from that row.

Root: mobile/src/screens/home-screen/components/budget-card.tsx:85–102 groups the row in TouchableOpacity and sets accessibilityLabel={row.account} at91, overriding automatic descendant text. AmountText at97–99 and BudgetMeter at102 are visually rendered inside but excluded from the resulting native Button. No accessibilityValue supplied. Sole Home budget card caller; applies to every populated row and currency. Native Home recent-transaction elements retain combined payee/amount labels (e.g. Budget,$2,087.50), while Open menu is correctly named; metadata extraction works. Budget See all action also works normally.

Fix: keep a concise account label and add a localized accessibilityValue or combined localized description that conveys the same displayed actual/budget/currency and progress/status. Preserve unknown actual values, income/expense favorability, and button navigation; avoid duplicate announcements. Regression coverage should assert actual content in the native rendered row, not merely that a label exists. Verify fixture rows expose 0 of200USD,0 of550USD,0 of100USD, and nonzero/over-budget/localized states when implementing. Empty/skeleton states should not expose stale budget values.

Dedupe: all .pm markdown including completed searched budget/accessibility/VoiceOver/label. Completed w4/m7 read-surface accessibility owns general baseline but no current budget-row amount item. Existing111 concerns an unnamed merchant switch, a different component and fix. m1/t013 remains open for budget visual/lifecycle closeout; this specific accessibility gap needs repair, not a premature closeout. Target history latest a7c088d2 analytics removal,69bc9756 Home tap-through,0a7b6d24 RTL,29be4826 motion. No newer fix in checkout.

Evidence mobile/tmp/qa-2026-09-11: home-budget-ax-first.json,home-budget-ax-repeat.json (sanitized explicit native attributes); home-budget-ax-repeat.jpg,home-populated-budget-panel.jpg; budget-example-directives.json and budget-example-access.json. Full local XCTest snapshots home-budget-native-ax-values.log and home-budget-native-ax-repeat.log remain ignored. Native metadata collected by scratch native-scroll/Inspect.m via a copied installed ExpoMCP XCTest runner, uniquely identified io.beancount.qa.scroll.xctrunner; no product code or node_modules edits. Native scroll helper genuinely moved Home; MCP screenshots946x2048 correspond390x844points.

Unverified: VoiceOver speech, Android/TalkBack, non-English numeric announcements, nonzero current-period budget rows, denied/expired/offline states. No changes to device accessibility preference. No product fix, product test run, commit or shipping performed.

---

**Resolved 2026-09-11** — fixed and shipped in `49b9270d` (`mobile`), with the package's full check gate green. Triage confirmed the reported root cause against current code before the fix; the note was not stale.
