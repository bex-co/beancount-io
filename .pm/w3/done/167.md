# Keep native wheel picker actions and rows usable at large text sizes

**Severity:** minor. **Estimate:** 45–60 minutes. Owner: mobile. A2/A3: users needing enlarged text can confirm account-type choices without reducing the system text size.

September 11, 2026, HEAD f1965988/mobile7538fb0d, io.beancount.ios1.20260906.47, Expo MCP8083, sole iPhone17e/iOS26.5, English/System-light. Authenticated production session on public open_ledger/minimax. Only blank discarded Open Account drafts and local text-size changes; no account/ledger mutation.

1. Set iOS content_size to accessibility-extra-extra-extra-large, relaunch and verify Home. Actual Accounts tab -> plus -> Assets prefix (open-account-prefix-picker) opens the shared wheel titled Choose account type.
2. The title and Cancel grow across the modal's width; Done is entirely offscreen. Native XCTest metadata reports picker-confirm enabled=true, x543.33,width133 on a390-point-wide screen, hittable=false. The selected Assets text is also vertically cut off by the50-point row geometry. Cancel remains visible/hittable and actually dismisses the picker.
3. A second fresh maximum-size launch repeats the same screenshot and coordinates: initial PID75222, repeat PID75937.
4. Fresh accessibility-large launch PID76465: row labels fit better, but Done remains entirely offscreen at x477,width80.67,hittable=false. Thus this is not limited to the maximum category. The artifact called picker-axlarge-control.jpg is a smaller-size failure, not a passing control.
5. Restore default large and relaunch PID77109. The same actual journey shows full title, actions and rows; Done is x334.67,width39.33,hittable=true. Earlier actual default-size scroll to Expenses -> picker Done correctly changed the draft root; currency wheel -> No currency also worked. Back/reopen reset Assets/MUSD/empty name. These are local draft choices, not account creation.

Expected: Cancel, the purpose/title, the centered option and Done remain legible and reachable within the modal at supported text sizes. Actual: natural-width children overflow the fixed-width header, and fixed row height cannot contain the largest text. No invisible-button activation was attempted or inferred from a testID-only command; actual native hit testing reports Done unavailable.

Root: mobile/src/components/picker/index.tsx:67–88 uses a horizontal space-between header with unrestricted text and no title flex/shrink/available-width policy. At285–296 it renders Cancel, title and Done as sibling natural-width children, pushing the third beyond the screen. wheelItem at95–101 uses constant ITEM_HEIGHT50 while wheelItemText/selectedItemText scale unrestricted; wheel/indicator heights and snap/offset arithmetic use the same constant from wheel-position.ts. There is no horizontal header scroll or alternative confirmation action to reveal Done.

Fix the shared Picker's constrained layout: reserve visible touch areas for both actions, constrain/reflow the title within remaining width, and use a deliberate row/text scaling policy so the full pending option remains readable. Preserve useful Dynamic Type growth; do not globally disable scaling or truncate action labels. If row height changes, update initial offset, snapping, padding, selected indicator and confirmed-index math together rather than leaving one at50. A tested scaling bound for this constrained wheel is an alternative to variable row geometry. Keep confirmation/cancel values and selection initialization intact; coordinate with w3/162's separate pending-emphasis fix without conflating causes. No new dependency needed.

Acceptance: fresh iPhone17e at default large, accessibility-large and maximum accessibility size has both action bounds inside390points and natively hittable, with full title/selected row. Actual scroll/Done returns Expenses, Cancel preserves Assets, and untouched non-first values retain themselves. Cover a long translated title, optional/no title and selected/unselected row text in light/dark; add meaningful rendered geometry/scaling and unchanged confirmed-value coverage, not a snapshot of current overflow. Run mobile checks. Other picker callers, RTL/long locales under these failing sizes, VoiceOver activation, Android, physical devices and product changes remain unverified.

Blast radius/dedupe/history: shared Picker callers are Settings language/theme, Open Account root/currency and Add Budget interval/currency; transaction filters' PickerTarget is unrelated. Only Open Account root was reproduced at enlarged sizes. Network/metadata/error states do not determine this settled local layout. Searched all .pm open/done records for picker/wheel Dynamic Type, font, clipping, overflow and large text. w3/151 owns vertical clipping in native navigation bars; this modal's horizontal overflow and row geometry are distinct. w3/162 owns stale selected styling at normal size; done/101 fixes untouched confirmation and still passes. Full completed w4/m7 DoD/closeout reviewed: screen-reader labels with residual native audit, not this layout repair. History retains the header and fixed-row model from182a07f1 and6dda6746;7538fb0d only repairs selection initialization. No current-main layout fix found.

Verified ignored evidence in mobile/tmp/qa-2026-09-11/: open-account-root-maxtext.jpg, picker-maxtext-geometry.log, picker-maxtext-repeat-home.jpg, picker-maxtext-repeat.jpg, picker-maxtext-repeat-geometry.log, picker-axlarge-home.jpg, picker-axlarge-control.jpg, picker-axlarge-geometry.log, picker-default-restored-home.jpg, picker-default-control.jpg, picker-default-geometry.log, open-account-expenses-root.jpg, open-account-no-currency.jpg, open-account-reset-control.jpg, picker-geometry-js-errors.json. Bounded JS error capture empty. Earlier live text-size-change screenshots with partially laid-out labels are excluded from the fresh-launch finding. All drafts discarded, English/System/light/default large restored. No product fix, commit or shipping.

---

**Resolved 2026-09-11** — fixed and shipped in `31f1e4ce` (`mobile`), with the package's full check gate green. Triage confirmed the reported root cause against current code before the fix. Geometry at fontScale 1 is unchanged by design. NOT verified: this is layout and the package has no render harness, so the visual result still needs a simulator pass at the largest Dynamic Type setting.
