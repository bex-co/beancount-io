# Mobile budget charts lack an accessible data summary

Minor; estimate30m, mobile. A2/A3: budget history must remain understandable through assistive technology.

September11,2026, main f1965988/mobile7538fb0d, native io.beancount.ios1.20260906.47, iPhone17e/iOS26.5, English/System/Light. Production public synthetic open_ledger/budgeting-envelopes, authenticated QA reader (push:false). Report only.

Repro: open Budget (actual Home See all works), select ALL. Dining chart visibly shows January160.50USD, February0, March0, with monthly200USD budget line. Native XCTest accessibility snapshot exposes the entire chart as one Other element labeled 'Vertical scroll bar, 2 pages, Actual, Budget', without accessible descendants containing months or values. The card's separate headline actual0 is accessible but describes March; it does not supply January's historical spending. Terminate/relaunch PID98255, verify selected ledger Home, deep-link budget, dismiss dev warning, select ALL: screenshot and native metadata repeat the same omission.

Working control: open the same ledger's income-statement shared link to Reports. Its chart native label is 'Income and expenses over 3 months. Income $12.3K, expenses $4.7K, net $7.6K.' This proves meaningful chart text reaches native accessibility through the shared chart shell. Actual VoiceOver speech/navigation was not tested; this finding is the missing native metadata and its inferred reading limitation.

Root: mobile/src/common/d3/budget-bar-chart-d3.tsx passes chart dimensions/series/legend into ScrollableAxisChart but never supplies its accessibilityLabel prop. scrollable-axis-chart.tsx:67 marks the wrapper accessible, so default descendant text collapses to scroll-bar/legend labels and SVG data is absent. IncomeExpenseBarChart at156 supplies incomeExpenseChartSummary and works. BudgetGroupCard is the sole BudgetBarChartD3 caller; both Budget cards and changing spans use this route. No numerical/API bug claimed: budget-dining-all-api.json returns Jan160.5, Feb/Mar empty, matching the visible chart.

Fix: give budget series a localized accessible summary that identifies its period coverage and actual/budget comparison, preserving nonzero historical information, currency, favorability, empty/loading and selected-span changes. Reuse the existing shell's accessibilityLabel support; do not announce SVG layout or scrollbar internals as data. Keep visible charts and the Reports summary working. Regression coverage must assert the supplied summary's data and updates, not only a nonempty label. Check a nonzero past period with a zero latest period, over-budget and empty series.

Dedupe: searched all .pm markdown including done for budget/chart/accessibility/VoiceOver/labels. Completed w4/m7 owns general chart text alternatives; no active budget-series-summary item. w3/123 covers Home budget buttons discarding their amounts, a distinct row-label implementation; this report owns the dedicated chart caller. m1/t013 remains open for full visual closeout. Current BudgetBarChart caller lacks the prop while Reports already uses it; no newer source repair in checkout.

Verified evidence under mobile/tmp/qa-2026-09-11: budget-chart-ax-first.json, budget-chart-ax-repeat.json, reports-chart-ax-control.json (sanitized native hierarchy excerpts); budget-chart-ax-repeat.jpg (ALL visually inspected); budget-dining-all-api.json; budget-chart-ax-relaunch-home.jpg. Full local logs remain ignored. Metadata comes from scratch native-scroll/Inspect.m using a copied uniquely identified XCTest runner; app code, node_modules and device accessibility settings unchanged. Native screenshot946x2048, logical390x844.

Unverified: VoiceOver audio, Android/TalkBack, non-English summaries, denied/expired/offline states and patched behavior. No production mutations, product fixes, product test run, commits or shipping.

Targeted chart history:86062503 extracted the shared chart shell; earlier69bc9756,0a7b6d24 andbaadf767 cover tap-through/RTL/motion. Current callers searched: only BudgetGroupCard uses BudgetBarChartD3; only budget and income-expense charts use ScrollableAxisChart. Reports control screenshot also visually verified: mobile/tmp/qa-2026-09-11/reports-chart-accessibility-control.jpg.

---

**Resolved 2026-09-11** — fixed and shipped in `89cc7dd7` (`mobile`), with the package's full check gate green. Triage confirmed the reported root cause against current code before the fix. Helpers are unit-tested; the spoken VoiceOver result is NOT verified (this package has no render harness) and still wants a simulator pass.
