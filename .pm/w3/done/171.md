# Preserve the exact target amount when chart counting finishes

**Severity:** minor. **Estimate:** 40–55 minutes. Owner: mobile. A2/A3: a public example's chart and account table should show the same balance rather than disagree after an animation settles.

September 11, 2026; HEAD f1965988 / mobile 7538fb0d; native io.beancount.ios 1.20260906.47; iPhone 17e / iOS 26.5, authenticated production/public open_ledger/minimax, English/System-light. Initial settled maximum-text launch PID83681; independent fresh default-large launch PID84346. Only read actions and local text-size restoration.

1. Open Home and select Liabilities in the top balance card. At maximum text size the deliberate horizontal tab strip can be scrolled to reveal the tab; actual tap succeeds. Wait for animation to finish: headline -367.98 MUSD, delta +3,368.61 MUSD (+90.15%).
2. Tap the same card's See all. Accounts renders the Liabilities root as -367.99 MUSD.
3. Restore default large, terminate/relaunch and repeat actual Home Liabilities -> See all. Same settled -367.98 vs -367.99 discrepancy. This is not clipping, a sampled intermediate counting frame or a text-size-dependent value.
4. Read-only GetLedgerBalanceSheet(conversion:at_cost) latest June 30,2026 liabilitiesData.balance.MUSD and GetLedgerTrialBalance(conversion:at_cost) liabilitiesHierarchyData.balanceChildren.MUSD both equal the exact string -367.985. Both asset inputs equal 1714.668; their native displays agree at 1,714.67. No server calculation difference or ledger write.

Expected: the resting animated headline formats the same exact target as a static amount: -367.99 MUSD under the existing formatter. Actual: the animation permanently replaces that target with -367.98 before formatting it.

Root: mobile/src/components/amount-text/animated-amount.tsx:82–92 schedules Math.round(displayed.value*100)/100 into shown, and93–96 passes shown while animate=true even after withTiming has completed. For -367.985 the cent rounding is -36798 (negative half-cent rounds toward positive infinity), so the original target never reaches the resting formatter. ScrubHeader in mobile/src/common/d3/interactive-line-chart.tsx:344–347,367–376 passes formatSignedMoneyWithCurrency and animate={!scrubbing}; account-table/account-table.tsx:253–255 calls that same formatter with the original value. number-utils.ts:49–54 formats the absolute original with toFixed(2), yielding367.99. The two API selectors both use resolveCurrencyBalance, so input conversion is not the discrepancy.

Fix AnimatedAmount so a completed or bypassed animation renders format(value) for the exact current target. Keep intermediate update throttling if useful, but do not let a rounded intermediate representation replace the resting amount. Handle interrupted/rapid target changes, zero and reduce-motion completion without stale callbacks restoring an old target. Preserve direct finger-following scrub behavior and existing formatting/currency/sign policy; no global rounding-policy rewrite, source-data rounding or new dependency needed. Add meaningful lifecycle coverage showing completion equals direct formatting for this negative half-cent, positive/negative ordinary values, rapid replacement, animate=false and reduced-motion completion. Native repeat must show -367.99 in both places and retain the1,714.67 asset control.

Blast radius: AnimatedAmount has one production consumer, shared InteractiveLineChartD3; callers are Home's balance card and BalanceChartCard (including account detail). Only Home Liabilities is natively reproduced here. An offline probe executes the unchanged number-utils formatter and the source cent expression: -367.985 => animation -367.98/static -367.99;1714.668,-362.932 and0 agree. It corroborates arithmetic, not Reanimated scheduling. Chart scrubbing, reduce motion, other negative targets, Android and physical hardware remain unverified. Loading/error states are separate from this settled target defect.

Dedupe: all root .pm open/done searched for AnimatedAmount, animation/counting rounding, groupThousands, negative half-cents and chart precision; no matching open work. w3/109 loses small commodity quantities in transaction headlines, and m34 rounds submitted posting amounts; both use different paths. Commit baadf767 introduced this component and its cent reaction; it is still the last change to this file. Its original motion feature intended the same formatter for counting and resting values; this is a residual boundary case, not deployment lag.

Verified evidence under mobile/tmp/qa-2026-09-11/: home-maxtext-liabilities.jpg, home-maxtext-seeall-accounts.jpg, home-rounding-default-home.jpg, home-liabilities-default-repeat.jpg, home-liabilities-accounts-control.jpg, minimax-chart-table-rounding-api.json, qa-trial-rounding.graphql, animated-amount-rounding-probe.json, animated-amount-js-errors.json. No product changes, commits or shipping. Default-large text size restored.

---

**Resolved 2026-09-11** — fixed and shipped in `30afc89d` (`mobile`), with the package's full check gate green. Triage confirmed the reported root cause against current code before the fix.
