---
id: w3/m8/t005
title: Adoption surface
worker: worker3
status: done
estimate: 20m
depends_on: [w3/m8/t004]
---

## Objective

Document the bank-import path end to end, including the part that stays a browser step.

## Context

- The honest story: link the bank once in a browser, then everything after is API-driven. A reader who does not know that will look for a link endpoint and conclude the API is incomplete.
- `backend-cluster/backend-v2/README.md`, root `README.md`.

## Steps

1. Document the one-time browser link and the API surface after it.
2. `yarn mcp:conformance` against a local stack.
3. `python3 scripts/check-agent-guidance.py` if instruction files changed.

## Steps beyond docs

- If t001 left verbs REST-only, say so where an agent author will read it, not only in the verb table.

## Files

- `backend-cluster/backend-v2/README.md`, root `README.md`

## Acceptance criteria

- [ ] The browser-link step is documented as deliberate, not missing.
- [ ] Any REST-only verbs are called out for agent authors.
- [ ] `check-agent-guidance.py` passes.

## Out of scope

- Documenting the ceremony verbs as API.
