---
id: w3/m8/t002
title: "`v1Route` declarations for the in-scope bank verbs"
worker: worker3
status: done
estimate: 60m
depends_on: [w3/m8/t001]
---

## Objective

Put the bank-import family on the v1 REST surface, in the shape t001 decided.

## Context

- `src/features/ledger/api/rest/v1/` plus `v1/index.ts`; parameters follow m7/t001's family shape where they overlap.
- Several are `admin`-class — classify each in `op-class.ts` per the table, and remember the class comes from the table, not the HTTP method.
- `getUnsyncedPlaidTransactions` can return a large set; apply m7/t001's bounding rule.
- Same services the GraphQL resolvers call. Plaid client construction, token refresh, and error mapping already live there and must not be reimplemented at the transport edge.

## Steps

1. Declare the routes t001 approved for REST; list them in `v1/index.ts`.
2. Fill `VERB_TABLE`; narrow `plaidBinding` to the three ceremony verbs.
3. `yarn generate-v1-openapi`; review the snapshot diff.

## Files

- `src/features/ledger/api/rest/v1/`, `src/server/api/op-class.ts`, `docs/openapi/v1.json`

## Acceptance criteria

- [ ] Each approved verb answers over curl against a local stack.
- [ ] `plaidBinding` covers exactly the three ceremony verbs.
- [ ] A write against a ledger the caller cannot reach is refused.
- [ ] `openapi-completeness.test.ts` green.

## Out of scope

- The MCP half (t003).
