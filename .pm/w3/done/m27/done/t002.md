---
id: w3/m27/t002
title: Integrate the verified upstream interpolation fix
worker: worker3
status: done
estimate: 60m
depends_on: [w3/m27/t001]
---

## Objective

Adopt a supported engine containing the verified amount-preservation fix and preserve the owning package's integration contract.

## Context

The milestone README contains live trigger evidence, original fixtures, upstream provenance and limitations.

## Steps

1. Review the candidate release and API compatibility against the current loader, workers, DTOs and plugin/query boundaries.
2. Update the existing @rustledger/wasm pin to a verified compatible release containing81b28698;0.24.0 is proven for this fixture. Regenerate yarn.lock with the package manager inside ledger.
3. Make only necessary package-local integration changes. Coordinate the pin with m18 without treating this as a fix for its remaining price bug.

## Files

- backend-cluster/ledger/package.json
- backend-cluster/ledger/yarn.lock (generated)
- backend-cluster/ledger/src/foundation/rustledger/loader.ts and affected adapters only if compatibility requires changes

## Acceptance criteria

- [ ] Real loaded Cash amounts remain-4 or0 and the genuine imbalance is reported.
- [ ] CommonJS/ESM initialization, worker cleanup and existing supported operations remain usable.
- [ ] No dependency is added, no lockfile is hand-edited and no upstream code/WASM is vendored.

## Out of scope

No replacement interpolation algorithm, dashboard workaround, unrelated upgrades or m18 closeout.
