# Name the native posting amount fields and expose their signed values

**Minor · 35–45m · mobile.** A2: a newcomer using assistive technology must be able to identify the account and signed amount being edited. Residual gap after completed w4/m7/t003, not a repeat of its icon-button work.

Environment: production https://beancount.io; September11,2026; HEAD f1965988/mobile7538fb0d; io.beancount.ios1.20260906.47 on iPhone17e/iOS26.5; English/System-light/default text size. Authenticated QA account, public open_ledger/minimax, pull/push true/admin false. No Done/save or production mutation; all local drafts discarded.

1. Select MiniMax, open the actual Transactions tab, and tap the plus button.
2. Inspect the two native amount TextFields beside Assets:Current:Cash and Expenses:CostOfRevenue. Both are enabled and hittable, but both expose `label:"", identifier:"", placeholder:"0.00", value:"0.00"`.
3. Relaunch and verify Home before repeating the actual tab/plus journey. Fresh PID46694 reproduces both empty labels (initial PID44571).
4. Enter1.25 in the first field through verified native typing. Both fields still expose empty labels and value1.25, although the UI's second posting is negative. The account buttons independently expose their complete account names and Done accurately changes from disabled to enabled; these are working metadata controls.

Expected: each amount input has a localized meaningful name identifying its posting/account and amount purpose, and exposes its signed value and currency. The two fields must remain distinguishable when both have the same absolute value. Actual: field metadata has neither account identity nor signed/currency value. This is an observed native accessibility-metadata defect; VoiceOver speech/focus order was not recorded, and the report does not claim a full screen-reader journey or an Accessibility Inspector audit.

Root: `mobile/src/screens/multi-postings-transaction/multi-postings-transaction-screen.tsx:305–315` creates the TextInput with value=absValue and only the0.00 placeholder; it sets no accessibilityLabel/value/hint. The account button at269–273 is a separate accessible sibling, and sign state at291–294 is on another sibling button. Those labels do not label the TextInput itself. PostingRow derives absValue/isNegative at232–240; pass the selected currency into this row as needed. The single production row owner maps all postings in MultiPostingsTransactionScreen. /add-transaction is the default Transactions/Home add flow and receipt destination; legacy add flow is separate and unverified.

Fix: add a localized amount-field label including the selected account (or a localized posting-index fallback when unset), and accessible signed amount/currency information while preserving native text-field editing semantics. Keep raw visible input and value synchronized as the user edits, changes account, toggles sign or adds/removes rows. Preserve separate focusable account/sign/auto controls; do not group the whole row so its input becomes inaccessible. Coordinate edits with w3/m34's exact-decimal work, but this metadata fix is independently shippable and must not depend on rounding values.

Acceptance: native metadata identifies both accounts and their amount purpose in blank and populated drafts; the−1.25 automatic counterposting is not exposed as an indistinguishable positive1.25. A changed account updates its field name, and added/missing-account rows have unique meaningful fallback names. Add targeted behavioral prop coverage and recheck fresh iPhone17e metadata; use a real VoiceOver focus/edit check for the final assistive-technology acceptance when available. Preserve keyboard, native input value, sign toggle and auto behavior. Android, alternate locales, denied/loading states and VoiceOver audio remain unverified; they are not claimed passed.

Dedupe: searched all open/done queues for posting/amount labels and accessibility, and read completed w4/m7/t003 plus its full implementation commitbc151c5c. That commit added account/sign/auto/Done labels and validation announcements while leaving the amount TextInput unchanged; it is still the latest change to this screen. w3/123 and124 concern Budget metadata,128 Reports categories,111 the recurring switch; m34 concerns numeric precision. No matching open amount-field task found and no newer main fix exists.

Verified ignored evidence under mobile/tmp/qa-2026-09-11/: posting-fields-ax.log, posting-fields-ax-repeat.log, posting-fields-ax-populated.log, posting-ax-populated-type.log, posting-ax-fresh-home.jpg and posting-fields-ax-repeat.jpg. Logs contain only targeted public-fixture metadata. The initial/repeat helper tests passed; no product test or source change was made for this report.

---

**Resolved 2026-09-11** — fixed and shipped in `89cc7dd7` (`mobile`), with the package's full check gate green. Triage confirmed the reported root cause against current code before the fix. Helpers are unit-tested; the spoken VoiceOver result is NOT verified (this package has no render harness) and still wants a simulator pass.
