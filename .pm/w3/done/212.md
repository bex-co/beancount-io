# Expose the shared native Button as a button to accessibility

**Minor · 25–40m · mobile.** A2: primary actions should convey their interactive purpose to assistive technology.

Environment: Sep13,2026, HEAD84231f9c, native io.beancount.ios1.20260906.47(47), Expo57/RN0.86, iPhone17e/iOS26.5, English/default large/System light; dedicated QA session on https://beancount.io with public open_ledger/freelancer-invoicing. Read-only filter changes only.

Actual journey: Transactions → search-bar Filters → Custom → Start date, choose October13,2026 with End September13,2026 → Confirm. The range error correctly appears and tapping Apply does nothing. Native AX exposes `Other, {{16.0,758.0},{358.0,44.0}}, label:'Apply', Disabled`; neighboring controls expose `Button, label:'Cleared'` and `Button, label:'Custom', Selected`. Cancel returns to the original unfiltered journal. Terminate/relaunch process42336, open the real `beancount:///(app)/transaction-filters` route: its enabled default Apply is again `Other, {{16.0,758.0},{358.0,44.0}}, label:'Apply'`, while chips are Buttons. The repeat verifies the destination through a deep link; the initial run used actual navigation.

Expected: Apply retains its localized name and enabled/disabled state and also exposes the native button role. Actual: the name and disabled state exist, but the interactive role is absent. This establishes native metadata, not a recorded VoiceOver utterance or rotor/navigation failure. Reversed-date validation itself passes and is not part of this finding.

Root: `mobile/src/components/button/index.tsx:119–133` renders PressableScale with label, disabled/loading and handlers but no accessibilityRole. Its props do not provide a role override either. `mobile/src/components/pressable-scale/index.tsx` forwards accessibility props unchanged to AnimatedPressable; it cannot supply the missing Button semantics. `transaction-filters-screen.tsx:362` uses Button for Apply, whereas its FilterChip explicitly supplies accessibilityRole="button", explaining the working native control.

Fix the semantic Button component with accessibilityRole="button" on its native wrapper. Preserve label, disabled/loading state, pointer guards, press feedback, primary/outline styling and enlarged-label sizing. Keep the generic PressableScale wrapper usable by other semantic controls; do not globally assign all its consumers a button role. Add focused coverage that fails when the actual Button role forwarding is removed and verify enabled/disabled states in the native accessibility tree.

Callers searched: Filters Apply; Welcome Sign in/Explore; Server Settings connection-test/save controls; Budget empty-state action; Referral Share. Only Filters was freshly reproduced for this finding. Prior207's Referral inspection also found Share as a generic element but concerned offscreen layout, not semantics. Completed182 owns shared Button height, completed159 owns the separate Back renderer. All open/done PM queues and open milestone titles were searched for Button role/semantics/accessibility and Apply. Completed w4/m7/t002 aimed to role filter controls; this is remaining coverage from that audit. Shared Button history ends at31f1e4ce (layout), with prior608e19d7/0a7b6d24/20ee05fd; no subsequent role repair exists on current main.

Acceptance: actual Filters Apply is a named native Button in enabled and reversed-range disabled states; disabled activation remains blocked; valid Apply and Cancel preserve existing filter behavior; chips retain button/selected semantics. Verify primary/outline and loading-state forwarding in component coverage. No API change, new dependency, ledger write, physical device or Android claim.

Verified ignored evidence under `mobile/tmp/qa-2026-09-13/`: `filter-reversed-validation.jpg`, `filter-reversed-apply-ax.log`, `filter-cancel-journal.jpg`, `filter-apply-fresh-ax.log`, `filter-apply-fresh.jpg`. No product fix or shipping performed.

---

**Resolved 2026-09-15** — fixed and shipped in  (). Shared Button sets accessibilityRole=button.
