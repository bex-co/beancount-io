# Account suggestions extend above short browser viewports

Severity: minor. Owner: dashboard. Estimate: 35–45 minutes. A2: readers can see and select every account option at short window heights.

Production https://beancount.io,2026-09-11, isolated headless Chromium152, authenticated public reader, open_ledger/example, English/light. Fresh844×390 and390×360, with390×420 and844×700 controls. These are browser viewport sizes; no native keyboard or device rotation was exercised. Local/refreshed origin main f1965988; latest observed entry main-RkgtsKH_.js, exact deployed commit unverified.

Repro: open /ledger/open_ledger/example/journal?lang=en, wait for the journal, open Filters, then Account suggestions. At844×390 the popover is302px tall, top−24/bottom278; Assets spans−19..13 and is clipped. A separate fresh844×390 repeats. At390×360 the popover spans−54..248 and Assets is entirely outside the viewport at−49..−17; Equity is partly clipped. The list is already at scrollTop0, and a browser-local upward wheel leaves it at0 with the same inaccessible top. No page errors.

Working controls: at390×420 all of Assets is visible; at844×700 the same302px list fits from286..588. A fresh390×360 browser-only CSS control caps the list using its available-height variable (subtracting the popup border): the popup becomes248px tall at0..248 and Assets becomes fully visible at5..37. No product CSS changed and no account/filter selection was made.

Root: common/components/ui/combobox.tsx:253–266 renders PopoverContent with only p-0 and width, and its CommandList inherits common/components/ui/command.tsx:61's fixed max-height300px. Radix reports --radix-popover-content-available-height as278px/248px in the failing cases, but this consumer does not constrain its scrollable list to that available space. The popup border adds2px. It is not the Filter Sheet's horizontal layout or the report chart toolbar overflow.

Fix the shared Combobox popup/list sizing to respect available viewport height while retaining the current300px cap and an internal scroll region. Account for wrapper/border space; preserve all options and keyboard scrolling. Do not globally clip every Popover/CommandList or merely hide the overflowing first options. Direct Combobox consumers are LedgerSearchControls plus account, currency, source-file, payee, narration and operating-currency wrappers; these are source-traced siblings, while the filter Account popup is live-reproduced.

Acceptance: all first/last options are visible or reachable through the list's own scroll at844×390 and390×360, including opening above/below and resizing while open. Arrow navigation, selection/blur, custom values, empty and loading/error states remain coherent; normal-height popovers retain their layout. Add a real portal/viewport geometry regression instead of asserting a CSS classname. Run dashboard format/lint/test/build when implementing.

Dedupe: all open/done queues searched for short viewports, popup height and clipped combobox options. Completedm20 explicitly covers narrow widths/horizontal filter fit;147 chart toolbar clipping,127 hidden chart focus and161 sidebar dismissal are separate mechanisms. The fixed CommandList cap and unconstrained Combobox popup date toaf5339de; laterfa3a0b9c/213789aa do not size the popup. Refreshed main has no repair.

Evidence under dashboard/tmp/qa-20260911-w3-loop/: short-filter-popover-{844,390}.json/.png; short-filter-repeat-{844x390,390x360,844x700}.json/.png; short-filter-height-control.json/.png. The390×360 failure screenshot was visually inspected. All fresh contexts closed; no production mutations, product fixes or shipping. Other callers, native touch/keyboard and patched keyboard-selection behavior remain unverified.

---

**Resolved 2026-09-11** — fixed and shipped in `2a59a6ba` (`dashboard`), with the package's full check gate green (format, lint, 3860 tests, build). Triage confirmed the reported root cause against current code before the fix; the note was not stale.
