---
id: w3/m39/t003
title: "Verify the navigation adoption surface"
worker: worker3
status: done
estimate: 15m
depends_on: [w3/m39/t002]
---

## Objective

Confirm the shipped landmarks and skip link are real on every surface that renders a sidebar, and that no instruction file describes the old structure.

## Context

- `dashboard/CLAUDE.md` tells contributors to reuse common responsive and accessibility primitives; if the skip link or landmark becomes the reusable pattern for future shells, say so there.
- Both sidebars must be covered: the `/ledger` dashboard shell and the ledger route shell.
- Public ledgers render the same shell for anonymous readers — check one.

## Steps

1. Re-run the landmark and skip-link sweep on `/ledger`, a private ledger overview, a journal and a public balance sheet, at 1440 and 390.
2. Check the same on a public ledger while signed out.
3. Note in `dashboard/CLAUDE.md` where the shell's accessibility structure lives, if that helps the next contributor; flag any other drift as follow-up rather than editing other packages.

## Files

- `dashboard/CLAUDE.md` (only if it should describe the shell structure)

## Acceptance criteria

- [ ] Both shells expose their navigation landmark and the skip link, signed in and signed out.
- [ ] Narrow viewport behaves the same.
- [ ] No instruction file, README or skill still implies the sidebar carries no navigation semantics.

## Out of scope

Editing other packages, and any behavior change beyond what t001/t002 shipped.
