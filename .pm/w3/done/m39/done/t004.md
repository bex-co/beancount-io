---
id: w3/m39/t004
title: "Simplify the landmark and skip-link wiring"
worker: worker3
status: done
estimate: 10m
depends_on: [w3/m39/t003]
---

## Objective

Run `/simplify` over the two sidebar shells, the skip link and the locale additions, for reuse and consistency, with zero behavior change.

## Context

- Watch for the two shells duplicating the same landmark wrapper markup, and for a hand-rolled visually-hidden style where `src/common/components/ui/` already has one.
- Do not push the landmark down into the shared `sidebar.tsx` primitive as a "simplification" — t001 deliberately kept it at the composing level.

## Steps

1. Run `/simplify` scoped to the changed files.
2. Confirm the t001 and t002 acceptance outputs are unchanged.

## Files

- The files touched by t001, t002 and t003.

## Acceptance criteria

- [ ] Landmark names, skip-link behavior and focus order are identical before and after.
- [ ] No duplicated visually-hidden or landmark-wrapper implementation remains.
- [ ] `yarn format:check && yarn lint && yarn test && yarn build` pass in `dashboard/`.

## Out of scope

New behavior, and refactors outside the milestone's changed files.
