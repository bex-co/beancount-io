---
id: w3/m21/t006
title: Test profile pagination and request isolation
worker: worker3
status: todo
estimate: 45m
depends_on: [w3/m21/t004, w3/m21/t005]
---

## Objective

Add meaningful regression coverage for the complete user journey and its asynchronous failure modes, then run dashboard checks.

## Context

- Dashboard-only pagination repair from the reproduced public Followers journey in this milestone's README.
- Existing API total values count the returned page; do not redefine the backend contract.

## Steps

1. Exercise real hook/tab integration with controlled GraphQL pages: 38 items, a short page, exact page multiples, empty first page, duplicate page items and client/network failure with retry.
2. Cover Followers and the distinct Following/Starred result identities; switch username while a prior page is pending and ensure it cannot replace the current list.
3. Assert named keyboard continuation and correct focus when the last page removes the control; retain tab URL synchronization and lazy fetching.
4. Repeat the public Followers desktop/narrow journey and the observed Following-one/Starred-empty and Ledgers 12/24/36/43 controls. Record unavailable live cases rather than creating production relationships.
5. From dashboard run format:check, lint, test and build. Existing backend page-size-total and empty-page compatibility remain unchanged.

## Files

- dashboard/src/features/user-profile/hooks/**tests**/
- dashboard/src/features/user-profile/components/**tests**/
- dashboard/src/features/user-profile/locales/**tests**/

## Acceptance criteria

- [ ] A regression fails against the current page-one-only behavior.
- [ ] Distinct pages, retries, duplicate activation and stale usernames are covered through actual hook/tab state.
- [ ] Dashboard gates and repeated public read-only journeys pass.
- [ ] Source-only large Following/Starred cases are identified as local fixture coverage, not live verification.

## Out of scope

Backend/API redesign, avatar delivery, social relationship writes, private user data,
and changing the intentionally capped activity feed.
