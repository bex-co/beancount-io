---
id: w3/m18/t006
title: Test coverage
worker: worker3
status: todo
estimate: 50m
depends_on: [w3/m18/t004, w3/m18/t005]
---

## Objective

Verify the final price correction through the real engine, service and exposed consumer paths.

## Context

The parent milestone records the live public repro, exact engine/source trace,
working controls and the corrected-artifact prerequisite. Follow the owning
ledger package's instructions; this task does not authorize new dependencies
or upstream publication.

## Steps

1. Complete live-WASM failure-mode coverage for long/short reductions, new positions, @/@@ annotations, explicit price precedence, missing quotes, same-day/as-of selection and genuine zero prices.
2. Verify the actual ledger structured/text handlers and existing GraphQL/REST/MCP adapters using the same nontrivial fixture at the engine boundary. Do not mock the corrected service or weaken the zero-debt API parity gate.
3. Run ledger lint, typecheck, build, tests and yarn verify:rustledger. Reproduce fresh desktop/narrow public Holdings, its CSV and BQL values against the tested deployment; record exact version and any remaining limits.

## Files

- backend-cluster/ledger/scripts/verify-rustledger.ts
- backend-cluster/ledger/src/features/ledger/service/**tests**/
- Existing adapter integration harnesses (verification only unless a scoped defect is found)

## Acceptance criteria

- [ ] Tests would fail if the historical-cost quote were reintroduced.
- [ ] Actual JSON/text and eligible API consumer values agree, with unchanged authorization/envelopes.
- [ ] Required package checks and browser/export controls pass on the final change.

## Out of scope

- Snapshot-only assertions, fabricated passing adapter mocks and unsupported production claims.
