# Gallery Escape abandons keyboard focus in the search field

Severity: minor. Owner: dashboard. Estimate: 25–35 minutes. A2: dismiss suggestions and continue editing a ledger search without refocusing the field.

Production https://beancount.io, 2026-09-11, isolated headless Chromium152, QA GROWTH reader, English/light, fresh1440×1000 and390×1000. Public search target open_ledger/crypto-example. Local/fetched main f1965988; latest entry observed main-RkgtsKH_.js, exact deployed commit unverified.

Repro: visit /ledger-gallery?lang=en, type crypto and wait for the real crypto-example option. Press Escape, then Backspace. The list closes correctly but focus moves to BODY; the input still contains crypto after Backspace. Refocus the same field and Backspace works, producing crypt. Both fresh widths reproduce without page errors, and a separate fresh390 confirms BODY focus. Expected: Escape closes the suggestions and clears the active option while retaining the focused input and query, allowing continued editing.

Root: dashboard/src/features/ledger-list/pages/gallery-page/index.tsx:125–130 explicitly calls inputRef.current?.blur() in its Escape handler. This is independent of Sidebar161's missing Sheet opener and does not need the dialog focus helper. Remove the forced blur while preserving list dismissal, highlight reset and the guard against selecting hidden/stale results. Cover both populated and empty suggestions, plus a pending query; Enter immediately after Escape must not navigate using a stale selection. The page is reached by /ledger-gallery and /gallery's redirect; no API change, global focus policy or dependency needed.

Working controls: after refocus Backspace edits normally; ArrowDown/Enter selects the real crypto-example ledger, and Back returns to gallery. Search is deliberately cleared by handleLedgerSelect before navigation; this note does not change that selection/reset policy or claim it as another bug. Earlier gallery-pending-crypto-repeat.json and gallery-failure-390.json cover honest pending and failed reads; preserve those states.

Dedupe: searched all queues including done for gallery Escape, blur and focus. Completedw5/m2/t003 added listbox/active-descendant semantics and clear-button keyboard access, not this explicit blur; those semantics are separate. Historical61edb5ac fixes stale results and empty-list Escape but carries forward the blur from af5339de. Recent620c7b0a/e4ba513d/071d0db2 do not remove it. Current gallery tests check Escape dismissal without a focus/continued-edit assertion. No existing item or newer main repair found.

Acceptance: fresh desktop/narrow populated and empty searches retain input focus after Escape; Backspace edits the existing query immediately. Escape preserves query text, closes the list and removes its active highlight; Enter cannot select the closed list, and subsequent typing/search/selection works. Add meaningful keyboard interaction coverage for focus plus continued editing and retain stale-result controls. Run dashboard format/lint/test/build when implementing.

Evidence under dashboard/tmp/qa-20260911-w3-loop/: gallery-back-keyboard-1440.json, gallery-back-keyboard-390.json, gallery-escape-focus-control.json, gallery-escape-focus-390.png, gallery-pending-crypto-repeat.json and gallery-failure-390.json. Screenshot visually inspected; exact DOM focus and Backspace outcomes are in JSON. All fresh contexts closed, no production mutations or product changes. No shipping. Other engines, native screen-reader behavior and patched pending/empty-state focus remain unverified.
