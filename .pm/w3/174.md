# Size Home chart pages for wrapped large-text amounts

**Severity:** minor. **Estimate:** 45–60 minutes. Owner: mobile. A2/A3: users who enlarge text can still read the full historical plot in public company examples.

September 11, 2026, HEAD f1965988 / mobile 7538fb0d, native io.beancount.ios 1.20260906.47; iPhone 17e / iOS 26.5; English/System-light; authenticated production/public open_ledger/adyen. Actual Home drawer -> Discover -> Explore -> Adyen; no ledger mutation. Default 6M Net Worth.

1. Default-large control shows the full €6,040,830,000.00 headline and the six-month curve: flat January–May then rising to June, with the baseline visible.
2. Set system content_size accessibility-extra-extra-extra-large and relaunch PID89108. After loading/animation settles, the headline wraps as €6,040,830,000. followed by00 on the next line. The lower plot is cut off at the pager boundary: January–May's flat line and the low portion of June's rise disappear; only the upper-right slope/end dot remain above the period pills.
3. Fresh maximum-size relaunch PID89290 reproduces exactly. A smaller accessibility-large fresh launch PID89516 still wraps the amount and clips the same plot, so this is not limited to the largest system category.
4. Restore default large and relaunch PID89744: the complete flat-then-rising chart returns. No data, range or currency changed during these controls.

Read-only GetLedgerBalanceSheet confirms the six 6M points are January 31 through May 31, 2026 at 5417489000 EUR and June 30 at 6040830000 EUR. Delta 623341000/+11.51% is correct. This is lost plot visibility, not missing API points or a wrong balance. Ordinary small-value MiniMax at maximum size retained a full plot when its headline fitted one line; Home tab-strip horizontal scrolling and See all navigation were separately verified and are not the finding.

Root: mobile/src/screens/home-screen/components/account-charts-card.tsx:21–25 fixes CHART_HEIGHT 170 and PAGE_HEIGHT 240, with a comment assuming the headline/change block has a bounded height. At154–159 the child chart gets170, while171 fixes the SegmentedPages page to240. components/segmented-pages/index.tsx passes that height to PagerView and fills each page with a flex1 View. The child line chart at common/d3/interactive-line-chart.tsx:630–649 lays out its variable-height header then the fixed170-point SVG. AnimatedAmount/AmountText permits text growth up to1.4 and wraps long values; the extra header line pushes the SVG below its page. No page-height update follows text measurement. The native screenshots demonstrate clipping at that boundary; exact platform-internal clipping implementation is not separately asserted.

Fix the Home chart-page sizing contract so the measured/constrained readable header and complete170-point plot fit together at supported text sizes and widths. Account for all three pages, wrapped amount/delta text, font/locale/currency/range changes and loading-to-loaded transitions. Prefer bounded height derived from real content requirements instead of another fixed padding guess. Preserve meaningful text enlargement, the full amount, shared range/tab state, chart touch geometry and pinned See all; do not globally disable font scaling or hide/truncate digits. Keep skeleton sizing consistent without permanently reserving a needlessly oversized default card. No backend or dependency change is required.

Acceptance: fresh iPhone17e default large, accessibility-large and maximum size shows the full Adyen six-month plot (flat January–May, June rise, baseline and endpoint) with full headline/delta. Verify Assets/Liabilities and a small-value control, light/dark and a longer translated label, after range changes and font-size changes. Add a meaningful layout/measurement regression for a wrapped header and retained plot height; do not merely snapshot the current PAGE_HEIGHT constant. Account-detail BalanceChartCard uses the shared chart outside this Home pager; it was not reproduced as affected and must retain its existing layout. VoiceOver, Android and physical-device rendering remain unverified.

Dedupe/history: all root .pm open/done searched for page/pager height, chart clipping, wrapped amounts and Dynamic Type. No matching open item. w3/167 is the shared wheel modal;151 is native action-bar text;171 is numerical target rounding;143 is chart gesture ownership. Those causes are distinct. Commit ec8e9b25 introduced the fixed Home chart page;23555c9a moved See all into the tabs, and a7c088d2 removed analytics without sizing the content. No current-main repair found.

Verified ignored evidence in mobile/tmp/qa-2026-09-11/: adyen-amount-layout-default.jpg, adyen-amount-layout-max.jpg, adyen-amount-layout-max-repeat.jpg, adyen-amount-layout-axlarge.jpg, adyen-amount-layout-restored.jpg, adyen-balance-api.json, adyen-amount-layout-js-errors.json. Development warning overlays at the bottom are excluded; they do not cover the affected top chart. All text-size changes restored. No product fixes, commits or shipping.
