---
id: w3/m27/t005
title: Verify loaded entries, errors and query amounts
worker: worker3
status: todo
estimate: 45m
depends_on: [w3/m27/t004]
---

## Objective

Prove the integrated engine's real loading and query paths preserve the authored amounts and validation failures.

## Context

The milestone README contains live trigger evidence, original fixtures, upstream provenance and limitations.

## Steps

1. Run all six original cases through direct and worker-backed package paths, asserting exact account/number/currency mappings and errors.
2. Verify journal/errors and BQL consumers receive the same-4/0 amounts; retain correct omitted-currency balancing and fully omitted-amount inference.
3. Run ledger test, lint, typecheck, build and verify:rustledger. Keep ordinary unit tests WASM-free and actual engine checks in the package's supported verification path.
4. Record remaining known failures such as m18 separately; do not alter expectations to hide them.

## Files

- backend-cluster/ledger/scripts/verify-rustledger.ts
- Owning ledger integration/worker verification fixtures and tests

## Acceptance criteria

- [ ] Invalid original inputs cannot be declared valid by replacing their supplied amounts.
- [ ] Both direct and worker-backed reads expose the same corrected values/errors.
- [ ] Required gates pass; any unrelated known limitation is documented honestly.

## Out of scope

No mocked corrected-engine evidence, redundant coverage-only tests or production writes.
