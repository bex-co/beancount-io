# Errors table source navigation is pointer-only

Severity: minor. Owner: dashboard. Estimate: 30–45 minutes. A2: a keyboard user can open the source location of a validation error.

Environment limitation: this is a **controlled-fixture UI reproduction**, not a validation error observed in a production ledger. Thirteen public catalog ledgers returned zero errors. An isolated local ledger generates the validation error below; only GetLedgerErrors is replaced inside fresh signed-out headless Chromium152 contexts loading production https://beancount.io. Other reads and file navigation remain real. English/light,1440×1000 and390×1000; local/fetched main f1965988, latest observed entry main-RkgtsKH_.js, exact deployed commit unverified.

Fixture provenance: backend-cluster/ledger/tmp/qa-20260911-errors/probe.mjs uses the existing installed @rustledger/wasm0.24.0 against local main.bean with opened Assets:Cash/Equity:Opening and an unbalanced2USD/-1USD transaction. Ledger.getErrors emits E3001, “Transaction does not balance: residual 1 USD”, main.bean:3. The probe calls the actual ledger-data-mappers.ts toBeancountErrorsPublic function, then flattens its source fields as backend-v2 ledger-data-resolver.query.ts:393–399 does. No dependencies, production files, API data or vendored engine bytes are changed.

Repro: in an isolated browser context, fulfill GetLedgerErrors with the derived filename/lineno/message array and visit /ledger/open_ledger/crypto-example/errors?lang=en. The table shows the error, main.bean and Line3. Focus the last tabbable control before the table (Log In / Sign Up), then Tab: focus jumps directly to Related Pages → Files, skipping the error source action. The error row is a native TR with tabIndex=-1 and zero links/buttons/tabbable descendants. Both fresh widths reproduce. Clicking that same row opens files/blob/main/main.bean with lineNumber=3. A third390 repeat waits for the real file editor to settle; the read-only guard removes editMode=true. This demonstrates the pointer navigation path, not that the production file contains the local fixture's error.

Expected: the error location has a named native keyboard control with visible focus; Enter/Space as appropriate opens the same file/line, while the error remains a table row with three data cells.

Root: dashboard/src/features/ledger-data/errors/index.tsx:135–145 attaches only onClick to TableRow. Filename is a plain span at158–165; no cell contains a source action. handleGoToFile at42–46 already supplies the location through useFileNavigate. The generic table component correctly preserves native TR behavior; this consumer never provides keyboard activation.

Fix the Errors consumer: put a named source-location button/link inside the filename cell using the existing navigation handler, prevent duplicate activation if row pointer handling remains, and preserve native table semantics. Do not change TR into role=button/link or make every TableRow globally interactive. Treat missing source locations as non-navigable diagnostics rather than inventing a file. Keep the existing loading/empty/query-error states and read-only file guard intact; no backend/API change required.

Acceptance: the derived fixture's source control is reachable in normal Tab order at both widths, has a meaningful file/line name, shows focus and activates exactly once through keyboard and pointer. Table/header/row/cell roles remain intact. Cover missing source fields, successful empty data and query failure separately. Add a meaningful rendered Errors-page regression using the real table/navigation component; reuse existing cell-action patterns from Accounts/Holdings/Documents, not an implementation-mirroring onClick test. Run dashboard format/lint/test/build when implementing.

Dedupe: all open/done queues searched for Errors/source-row/keyboard navigation. Completedm25 repairs Accounts, Holdings and Documents, with an explicit local document fixture; Errors is not one of its consumers. m31 repairs Entry Context and m32 hierarchy table structure. This page's pointer-only row dates to af5339de; later42ee1dec/53b0babb change public access/Related Files without adding row controls. No duplicate item or newer main repair found. Only LedgerErrorsPage calls this handler.

Evidence: backend-cluster/ledger/tmp/qa-20260911-errors/{probe.mjs,main.bean,fixture.json}; dashboard/tmp/qa-20260911-w3-loop/errors-local-fixture-{1440,390}.{json,png}, errors-local-fixture-settled-390.json, ledger-errors-fixtures.json and ledger-errors-extra-fixtures.json. Narrow table screenshot inspected. All test contexts and local engine instances closed; no page errors or production mutations. No product fix or shipping. An actual production ledger with errors, other engines and native screen-reader speech remain unverified.
