---
id: w3/m19/t002
title: Preserve raw import fields and their validation diagnostics
worker: worker3
status: todo
estimate: 35m
depends_on: [w3/m19/t001]
---

## Objective

Keep enough original field data and diagnostics to repair a rejected CSV row without losing what made it invalid.

## Context

Read the parent milestone for exact production evidence, source trace, working
controls and limits. Follow dashboard and importer guidance. This is the sole
implementation queue for promoted038 and its reproduced error-loss extension.

## Steps

1. Extend the internal parsed-row representation or equivalent existing state so invalid raw amount abc remains available instead of becoming only a numeric0. Associate field and record-level errors with the input they describe.

2. Adapt the CSV producer and shared preview initialization to consume that representation. Preserve diagnostics for malformed records until a complete valid record exists; an unrelated payee change must not clear them.

3. Audit all ParsedRow producers and consumers, including AI results. Preserve current CSV-to-AI fallback behavior and existing valid typed rows; do not invent raw source text for AI output. Keep internal data out of mutation payloads.

## Files

- dashboard/src/features/importer/types/index.ts
- dashboard/src/features/importer/hooks/use-csv-parser.ts
- dashboard/src/features/importer/hooks/use-llm-parser.ts
- dashboard/src/features/importer/components/steps/preview/editable-preview-row.tsx

## Acceptance criteria

- [ ] The user can identify and edit the original rejected token and its field error.
- [ ] A valid zero and an invalid amount with no numeric result remain distinct throughout the workflow.
- [ ] Existing producers remain compatible and no new API fields or dependencies are required.

## Out of scope

- CSV record assembly015, header recognition045, row identity037 and AI parsing policy changes.
