# Holdings hides CSV downloads on narrow screens

**Severity:** minor. **Estimate:** 20–30 minutes. Owner: dashboard.

Production https://beancount.io, September 11, 2026; isolated headless Chrome152, English/light, authenticated QA reader, public synthetic open_ledger/example. Local/fetched main f1965988; exact deployed SHA unverified. No production mutations.

1. Fresh390×1000 open /ledger/open_ledger/example/holdings?lang=en and wait for its actual table:115 body rows.
2. There is no accessible Export CSV button. The existing button is in the DOM with display:none and a zero-sized rectangle; no replacement action is offered.
3. Widen the same page to1440: Export CSV appears and downloads holdings-export-2026-09-11.csv. Python csv.reader verifies115 data rows plus the eight-column header, matching the displayed row count.
4. Return to390: the button disappears again while the same data remains available.
5. Independently repeat from a fresh600×1000 load: identical disappearance, working1440 download and disappearance after resizing back. Both contexts close without page errors or write attempts.
6. A third fresh390 load exercises all four views. Holdings / by Account / by Currency / by Cost Currency show115 /11 /8 /2 body rows respectively, and none exposes Export CSV. Their real QueryShell reads all return200. Source row filters intentionally remove ineligible rows from raw responses; no row-count or valuation defect is claimed here.

Expected: readers can download the existing CSV at narrow widths with a visible, named action, just as at desktop width. Actual: responsive CSS removes that capability even when data and the download implementation are available. The390 screenshot was visually inspected; document width remains390, so the button is hidden rather than merely offscreen.

Root cause: dashboard/src/features/ledger-data/holdings/holdings-table.tsx:114 applies `hidden sm:flex` to the sole export Button; its text at117 independently uses `hidden sm:block`. The surrounding results/action container already stacks vertically below sm. DatasetTable is instantiated four times by holdings/index.tsx:56,64,72,80 for the four views. No other production caller or alternative narrow export control exists.

Fix: expose the existing action at all supported widths and retain an accessible, preferably visible Export CSV label. Let the existing stacked layout fit the button at narrow widths; preserve the desktop layout, current rowsFilter behavior, empty/loading/error branches and shared download helper. No new API, query, dependency, menu or permission path is needed. A layout-only repair does not require a test that merely asserts Tailwind class strings; verify actual narrow keyboard access and downloads for the default/grouped views and retain existing export tests.

Working control: the real desktop downloads in both fresh contexts contain all115 displayed default-view rows. All four narrow views load successfully; lack of data or access is not the cause. Loading/error/empty states already withhold the table action, and those states must remain honest after the change. Native download dialogs, other browsers, grouped CSV contents and the patched implementation remain unverified.

Dedupe: searched all open/done board files for Holdings, hidden/narrow/mobile exports and current milestone titles. Completedm25 concerns table semantics and row activation; done/m18 and113 concern engine prices; historical035/116 concern amount formatting. w4/done/m1/t004 explicitly excludes Holdings and owns statement exports only.147 concerns clipped chart toolbars and148 Trial conversion controls, both different call sites. No matching open repair found. Targeted git log -S dates the responsive classes to af5339de; later6ee28f03 changes table-row semantics and leaves them intact. Fetched main has no repair for this path.

Verified ignored evidence under dashboard/tmp/qa-20260911-w3-loop/: holdings-export-responsive-{390,600}.json; holdings-export-narrow-{390,600}.png; holdings-export-control-{390,600}.csv; holdings-export-views.json. The last capture includes the real successful query results for the public fixture. All contexts closed; no product fix, commit or shipping.
