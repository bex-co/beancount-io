---
id: w3/m33/t002
title: Reconcile preview edits using stable parsed-row identities
worker: worker3
status: todo
estimate: 35m
depends_on: [w3/m33/t001]
---

## Objective

Key configuration choices by the existing ParsedRow.id rather than the filtered-array position. Reconcile edited, removed and newly eligible rows without attaching one row’s mapping/selection to another. Recompute financial/date fields through the existing validators; never preserve stale parsed values over the latest preview.

## Context

Read the milestone README for reproduction, source trace, controls and dedupe. Implement only this task’s scope.

## Files

- dashboard/src/features/importer/types/index.ts
- dashboard/src/features/importer/hooks/use-import-workflow.ts
- dashboard/src/features/importer/components/steps/configure/transaction-config-form.tsx
- dashboard/src/features/importer/components/steps/configure/account-mapping-table.tsx

## Acceptance criteria

- [ ] Unrelated preview edits retain the same row’s target and selected flag.
- [ ] Deleting a row cannot shift its configuration onto the next row; invalid rows cannot enter the submission set.
- [ ] Newly eligible rows have explicit safe defaults and removed rows do not retain active mappings;037/m19 validity and identity fixes stay intact.

## Out of scope

Production imports/AI requests, backend changes, new dependencies and unrelated forms.
