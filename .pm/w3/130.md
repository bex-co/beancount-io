# Mobile Reports recent entries use a different month from the chart

**Minor · 45–60m · mobile.** A2: a newcomer must be able to reconcile the selected report window with its supporting transactions.

Native iPhone17e, iOS26.5, io.beancount.ios1.20260906.47, checkout f1965988, https://beancount.io, 2026-09-11. Authenticated reader of public synthetic open_ledger/freelancer-invoicing, pull:true/push:false/admin:false; English/System light/default text size.

Repro: select the fixture, open Reports, choose1M, scroll below the chart. The chart explicitly shows JUL at zero; income/expenses are each $0.00 with No accounts, and Cash flow says no activity in this range. Recent Transactions nevertheless lists Fabrikam Inc $3,200 and IRS $4,150. Tap Fabrikam: its native detail is dated June30,2026, write-off invoice2026-004. The API dates IRS June15. Terminate/relaunch (PID30098), verify Home, tap Reports, choose1M, capture JUL chart, scroll once: the same two June expenses appear. ALL is a working control: income becomes $29,492.40 and the recent list also includes May J.Okafor $2,400 and April Contoso +$792.40, all in range.

Expected: the range-filtered Recent Transactions card uses the same resolved report month/window as its chart and category summaries; July has no matching income/expense entries, so show the existing empty state. Actual: each section separately chooses an anchor, and the list silently moves backward to June. The summary amounts themselves are correct.

API control: GetLedgerJournal with directiveTypes:[Transaction],limit200,offset0 returns23 (complete, not window truncation). Latest transaction is July20 sales-tax remittance, with only Assets/Liabilities postings. Latest matching Income/Expenses entry is June30; July matching count0. IncomeStatement monthly at_cost includes July31 zero buckets, and June expenses7350=3200+4150. Sanitized period-control JSON contains these exact dates/hashes.

Root: mobile/src/screens/reports-screen/selectors/select-account-transactions.ts:52–62 filters to matching account postings before deriving rangeStartMonth from matching[0].date. Its comment claims the report chart's anchor but that is not generally the same date. components/account-transactions-card.tsx:88–96 only passes entries/accountPrefix/timeRange; ReportsScreen206–214 supplies no resolved report anchor. Chart selector select-income-expense-chart.ts filters the statement series; select-ranged-account-tree.ts anchors its category window to the statement's latest dated point. Source history9c773335 introduced the matching-entry anchor;0bf3e112 reused it for the combined Reports card. No later fix found. The sole runtime selector caller is AccountTransactionsCard; its sole screen caller is Reports.

Fix: derive a stable report window/anchor from the statement data at the Reports owner and pass it to the recent-entry selector, including correct start/end month bounds. Preserve the app's historical-ledger anchoring convention rather than replacing it with today's date. Do not fall back to the matching entry's month while the report data is loading or unresolved; keep a loading/error state until the common window is known. Preserve ALL, account-prefix filtering, newest-first cap and row navigation. The200-entry fetch window is a separate limitation, not this failure.

Acceptance: the live July1M case shows no recent income/expense rows; a report window that includes June shows the two June expenses; ALL still shows older entries. Regression coverage must combine a latest statement month containing only a transfer with older matching income/expenses, plus year-boundary/YTD and loading-to-settled behavior. Existing selector tests explicitly use the latest matching month and never compare it with a separate statement anchor; adjust the contract and add cross-section coverage rather than merely snapshotting current output.

Dedupe: searches of all open/done board for selectAccountTransactions, report anchors/date windows and recent transaction ranges found no matching item. w3/109 is amount precision; done106 entry navigation import;113 custom date validation;115 cross-ledger filter carryover; none fixes the report-window owner. Targeted selector/card history has no newer fix.

Evidence mobile/tmp/qa-2026-09-11/: freelancer-reports-1m.jpg, freelancer-1m-wrong-month-detail.jpg, freelancer-period-relaunch-home.jpg, freelancer-1m-fresh-chart.jpg, freelancer-1m-fresh-list.jpg, freelancer-all-control-scroll.jpg; freelancer-report-transactions-api.json, freelancer-report-period-control.json, freelancer-income-api.json, freelancer-access.json. Prior bounded Reports JS capture freelancer-reports-errors.json is empty; no runtime exception claimed. Native taps/scrolls on real app, screenshots946x2048 at390x844 logical points. No product edits or production writes.

Unverified: Android, differing-currency windows, network failures, missing statements, latest-year boundary and patched behavior. Estimate includes selector/owner change and meaningful regressions; no backend change needed for this complete23-entry reproducer. Report only, not shipped.
