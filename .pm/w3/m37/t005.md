---
id: w3/m37/t005
title: "Simplify the shared cell-formatting changes"
worker: worker3
status: todo
estimate: 15m
depends_on: [w3/m37/t004]
---

## Objective

Run `/simplify` over the code this milestone changed — the shared formatter module, the BQL result renderer and the CSV export path — for reuse, simplification and efficiency, with zero behavior change.

## Context

- Watch for a second near-duplicate of the display and text formatters surviving the move, and for dtype-detection logic duplicated between the renderer and the export path.
- `formatNumber` and `formatDecimalCell` may now overlap; consolidate only if the rendered output is provably identical.

## Steps

1. Run `/simplify` scoped to the changed files.
2. Keep every acceptance outcome from t001–t003 true.

## Files

- The files touched by t001, t002 and t003.

## Acceptance criteria

- [ ] No behavior change: the t002 and t003 acceptance outputs are identical before and after.
- [ ] No duplicated inventory formatter remains in the tree.
- [ ] `yarn format:check && yarn lint && yarn test && yarn build` pass in `dashboard/`.

## Out of scope

New behavior, new abstractions with a single caller, and refactors outside the milestone's changed files.
