# Generated padding rows open an unusable Entry Context dialog

**Minor · 45–60m · dashboard.** A2: clicking a valid generated transaction should explain its read-only nature rather than report a missing record.

Production https://beancount.io, 2026-09-11, isolated headless Chrome152, English/light, authenticated QA account reading public open_ledger/minimax. Fresh1440×1000 and390×1000. The ordinary source dialog exposes editing controls in this session; no edit, save, delete or other production mutation was performed. Local/fetched main f1965988; deployed SHA unverified.

Open /ledger/open_ledger/minimax/journal?lang=en&time=2026-06-29. Expand the postings of the padding row whose narration contains “Balance of -1346.663 MUSD for difference -3994.853 MUSD”. The two equity postings are available. Click the row itself: both fresh viewport runs open **Entry Context → The requested resource could not be found.** There is no explanation that this transaction was generated and has no editable source. The exact hash is508f22c1ceccbfb12344da6959fec867.

API control: GetLedgerJournal returns200 and includes this Transaction with flagP, empty metadata and two postings, Equity:RetainedEarnings -3994.853MUSD and Equity:Adjustments +3994.853MUSD. GetLedgerEntryContext for that hash returns HTTP200 with GraphQL codeNOT_FOUND/“Ledger resource not found”; the REST entry-context route returns404 with the same classification. The journal record is real, but source context does not exist for it. Do not manufacture a source filename, editable slice or balances-before/after from these facts.

Working control: the same ledger's ordinary source-backed hash864c29173af59ac1164edd8214ad41c7 has a successful context response. Fresh390 Journal time2026-06-30 → “FY2026 H1 Income Statement” opens Location: FY2026/FY2026H1.bean:76 and actual source, with no not-found message. Source controls are only inspected. The generated row's ordinary postings expansion also remains usable. Display rounding in the expanded postings is already covered by116, not counted again here.

Root: dashboard/src/features/journal/pages/journal-page.tsx:260–262 opens the shared dialog for every directive. components/entry-context-dialog.tsx:351–356 queries source context for any nonempty hash when open; its rendering at438–444 maps the expected absent generated source to a generic API error. No generated-row branch uses the complete entry already passed into the dialog. Targeted dialog history ends atcc3f4c2f (location/read-only source controls); the current source has no padding handling.

Fix the shared EntryContextDialog for the observed known generated-padding case: identify Transaction flagP, skip its impossible source-context query and show a localized generated-entry explanation with a read-only summary of the supplied date/narration/postings, reusing existing display components. Hide source/edit/delete actions for this branch independently of canWrite. Missing metadata alone must not classify an ordinary thin journal record as generated. Preserve genuine not-found/auth/service errors and normal context fetching for source-backed entries; do not blanket-convert every NOT_FOUND into a generated entry. No backend resolver enhancement or cross-package import is needed.

Three production dialog callers are Journal (journal-page.tsx:313), Account (reports/account/index.tsx:283) and Overview Recent Activity (recent-activity-card.tsx:289). Implement the guard centrally so callers supplying a known P entry get the same fallback; only the Journal generated path was exercised live here. Meaningful rendered coverage should distinguish a P entry on a writable ledger, an ordinary entry with thin/absent row metadata and a genuine ordinary context error. Repeat the generated and ordinary UI controls, preserve postings navigation and source-backed permission rules, and run dashboard checks on implementation. Other generated flags, actual editing and patched behavior remain unverified.

Dedupe: all queues including done searched for generated/padding/context. Open140 is mobile-only prevention of broken Share/Copy links for the same public fixture; its scoped fix does not repair dashboard row navigation. Openm31 owns Entry Context keyboard controls/focus, not this source-availability branch; coordinate changes to the shared dialog. Completed source-location work and116 precision repair remain separate. This finding schedules no mobile or backend changes.

Verified ignored evidence dashboard/tmp/qa-20260911-w3-loop/: generated-entry-context-{1440,390}.json/.png, generated-entry-api.json and generated-entry-working.json. Narrow error-dialog crop inspected. The ordinary control first used the inexact label Location instead of actual Location:; corrected fresh inspection confirms source rendering. All contexts closed, no page errors or additional write attempts. No product fix, commit or shipping.
