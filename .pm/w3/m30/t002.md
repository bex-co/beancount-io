---
id: w3/m30/t002
title: "Publish the separate account-journal filter in the ledger IDL"
worker: worker3
status: todo
estimate: 25m
depends_on: [w3/m30/t001]
---

## Objective

Publish the separate account-journal filter in the ledger IDL.

## Context

Read the parent milestone for the exact reproduced journey, filter ordering, API scope, controls and limits. Follow the owning package and nested guidance. Scope implementation to the package assigned here.

## Steps

1. Update the source account-journal operation with optional filter_account and clearly distinguish the target account from the report-account predicate.

2. Regenerate affected IDL client types from the source contract using the owning CLI package commands. Never patch generated output by hand.

3. Verify nullable/empty/omitted behavior agrees with the service and existing response envelope; run the affected IDL client type checks.

## Files

- backend-cluster/idl/beancount-ledger.openapi.json
- backend-cluster/idl/beancount-ledger-cli/src/generated/api.ts

## Acceptance criteria

- [ ] The generated ledger client can express both account identities without changing existing required arguments or responses.
