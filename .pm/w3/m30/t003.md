---
id: w3/m30/t003
title: "Expose the restriction through gateway GraphQL, REST and MCP"
worker: worker3
status: todo
estimate: 40m
depends_on: [w3/m30/t002]
---

## Objective

Expose the restriction through gateway GraphQL, REST and MCP.

## Context

Read the parent milestone for the exact reproduced journey, filter ordering, API scope, controls and limits. Follow the owning package and nested guidance. Scope implementation to the package assigned here.

## Steps

1. Regenerate the Fava client from the updated ledger IDL using yarn generate-fava-client inside backend-v2. Extend shared AccountJournalQueryParams and forwarding with optional filter_account.

2. Add the same optional restriction to GraphQL AccountJournalQueryInput and the REST/MCP JOURNAL_READS descriptor. Preserve LEDGER_JOURNAL_READ authorization, defaults and validation.

3. Update generated public schema/OpenAPI artifacts and resource descriptions through their native generators. Verify real adapter parity and run backend-v2 checks.

## Files

- backend-cluster/backend-v2/src/features/ledger/service/ledger-journal-service.ts
- backend-cluster/backend-v2/src/features/ledger/api/resolvers/ledger-journal-resolver.query.ts
- backend-cluster/backend-v2/src/features/ledger/api/rest/v1/journal-reads.ts
- backend-cluster/backend-v2/src/foundation/fava/

## Acceptance criteria

- [ ] GraphQL, REST and MCP pass both account identities through the same protected service and return equal rows, counts and balances for equivalent inputs.
