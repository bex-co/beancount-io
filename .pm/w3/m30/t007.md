---
id: w3/m30/t007
title: "Test coverage — filtered account streams, adapters and browser transitions"
worker: worker3
status: todo
estimate: 45m
depends_on: [w3/m30/t005, w3/m30/t006]
---

## Objective

Test coverage — filtered account streams, adapters and browser transitions.

## Context

Read the parent milestone for the exact reproduced journey, filter ordering, API scope, controls and limits. Follow the owning package and nested guidance. Scope implementation to the package assigned here.

## Steps

1. Add meaningful regression coverage using the purchase/sale/repayment fixture: BTC plus Margin yields only the purchase; omitted restriction yields both BTC transactions. Assert exact totals and balances.

2. Exercise real GraphQL/REST/MCP adapters, optional defaults, account component/regex behavior, nontransaction references, denied/invalid/empty results and combined time/display selectors.

3. Verify actual desktop/narrow Statistics→Account, direct load, Clear, Back/reload and a paginated filter change. Run relevant package checks and required schema/parity gates.

## Acceptance criteria

- [ ] Tests fail if the shared restriction is dropped, applied after pagination, or conflated with the target; ordinary account-journal callers remain compatible.
