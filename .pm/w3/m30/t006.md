---
id: w3/m30/t006
title: "Simplify the account-filter changes"
worker: worker3
status: todo
estimate: 20m
depends_on: [w3/m30/t005]
---

## Objective

Simplify the account-filter changes.

## Context

Read the parent milestone for the exact reproduced journey, filter ordering, API scope, controls and limits. Follow the owning package and nested guidance. Scope implementation to the package assigned here.

## Steps

1. Run simplify over this milestone’s changes. Reuse the existing canonical account predicate, shared API descriptor and query patterns.

2. Remove duplicated filtering or handwritten expression translation without changing the tested ordering and defaults.

3. Re-run owning-package checks justified by resulting edits.

## Acceptance criteria

- [ ] One domain implementation owns report-account filtering; adapters and UI forward it without redundant selection logic.
