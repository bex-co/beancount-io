---
id: w3/m11/t006
title: "Test coverage across context callers and transports"
worker: worker3
status: todo
estimate: 40m
depends_on: [w3/m11/t004, w3/m11/t005]
---

## Objective

Extend focused regression coverage across shared context callers and transport output, beyond the checks shipped with each fix.

## Context

- GraphQL and REST both reproduced missing metadata; MCP shares ANALYSIS_READS. Journal and overview are known callers, with account-report coverage also required.

## Steps

1. Cover resolved/included/duplicate/unavailable source states and guest, reader, writer, loading and permission-change dialog behavior.
2. Check GraphQL/REST metadata parity and the MCP analysis binding without bypassing the service or authorization.
3. Run the owning packages' normal gates and repeat the documented desktop/narrow public journeys. Record platforms/transports that remain unverified.

## Files

- `dashboard/src/features/journal/`
- `dashboard/src/features/reports/`
- `backend-cluster/ledger/src/features/ledger/service/__tests__/`
- `backend-cluster/backend-v2/src/features/ledger/api/resolvers/__tests__/`

## Acceptance criteria

- [ ] Tests assert observable behavior and submitted/returned data, rather than only mocked callback invocations.
- [ ] Relevant package gates pass and the milestone's demonstrated failures no longer reproduce.

## Out of scope

- Writing to real production data without separate authorization or adding tests that only mirror implementation.
