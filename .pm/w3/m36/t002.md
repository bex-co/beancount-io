---
id: w3/m36/t002
title: "Keep rejected precision inputs out of automatic parser fallback"
worker: worker3
status: todo
estimate: 30m
depends_on: [w3/m36/t001]
---

## Objective

When a recognized CSV contains the new unsupported-precision classification, keep its parsed rows available for repair even if validCount is zero. Do not fall through to an LLM parser that converts already-rounded numeric JSON back to strings. Use a structured result/reason, not an English error-message substring. Keep existing non-CSV and unrecognized-format fallback behavior separate. Verify mixed-validity and all-unsupported inputs, correction and retry/reset; do not invoke real AI or upload services in verification.

## Context

Read the parent README for exact reproduction, source trace, controls and limits. This is a residual of completed m19's accepted-value contract, not a new historical finding group.

## Steps

1. Implement the bounded objective above in the owning dashboard package.
2. Verify the specified acceptance outcomes and preserve the parent exclusions.

## Files

dashboard/src/features/importer/hooks/use-multi-stage-parser.ts; hooks/use-csv-parser.ts; types/index.ts; existing parser and workflow tests

## Acceptance criteria

- [ ] All-unsupported canonical CSV reaches a truthful invalid preview with no LLM/upload call; mixed CSV configures only the valid control. Editing a token to a supported value admits exactly that value. Other format fallback paths retain their intended behavior.

## Out of scope

Production imports/AI calls, arbitrary-precision model migration, new dependencies, backend/schema changes, native m34, and unrelated142 initial field-error wiring.
