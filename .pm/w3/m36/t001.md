---
id: w3/m36/t001
title: "Reject amount conversions that change the exact decimal value"
worker: worker3
status: todo
estimate: 45m
depends_on: []
---

## Objective

Add a representability check to the existing complete-token parser. Compare the original decimal value with the numeric value's eventual serialized decimal text using exact normalization of sign, coefficient and exponent. Preserve equivalent spellings (1.2500, +2.5e1, signed zero) and ordinary fractional controls; do not compare two already-rounded Numbers or reject every inexact binary float. Return an explicit unsupported-precision classification and preserve the original editable token. Wire the existing CSV, inline-edit and Configure callers to that result. No arbitrary-precision model migration, dependency or API change.

## Context

Read the parent README for exact reproduction, source trace, controls and limits. This is a residual of completed m19's accepted-value contract, not a new historical finding group.

## Steps

1. Implement the bounded objective above in the owning dashboard package.
2. Verify the specified acceptance outcomes and preserve the parent exclusions.

## Files

dashboard/src/features/importer/utils/csv-validator.ts; utils/row-edit-schema.ts; hooks/use-csv-parser.ts; components/steps/preview/editable-preview-row.tsx; components/steps/configure/transaction-config-form.tsx

## Acceptance criteria

- [ ] The three reproduced tokens are rejected before becoming ImportTransaction amounts;1.25,0,1.2500,+2.5e1,0.004 and1e-8 remain valid with equivalent serialized values. Existing malformed/grouped/overflow checks still pass. Original text remains editable and the precision reason remains available through the row-error channel; coordinate142 for initial field-message presentation without duplicating that repair.

## Out of scope

Production imports/AI calls, arbitrary-precision model migration, new dependencies, backend/schema changes, native m34, and unrelated142 initial field-error wiring.
