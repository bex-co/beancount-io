---
id: w3/m36/t003
title: "Verify the import validation adoption surface"
worker: worker3
status: todo
estimate: 15m
depends_on: [w3/m36/t002]
---

## Objective

Check that the import UI explains rejected precision through the existing validation flow and that a user can repair or remove rejected rows. Review dashboard README and applicable root/package guidance for numerical import claims; change documentation only where it actually promises unsupported behavior. Preserve shared instruction/skill symlinks; no new skill is needed. Flag unrelated drift as follow-up.

## Context

Read the parent README for exact reproduction, source trace, controls and limits. This is a residual of completed m19's accepted-value contract, not a new historical finding group.

## Steps

1. Implement the bounded objective above in the owning dashboard package.
2. Verify the specified acceptance outcomes and preserve the parent exclusions.

## Files

dashboard importer user-facing validation/help; dashboard/README.md and applicable root/package guidance

## Acceptance criteria

- [ ] The existing supported CSV example remains usable, and rejection does not silently appear as a valid zero or a network failure. Any changed guidance passes scripts/check-agent-guidance.py; no new unsupported precision promise is introduced.

## Out of scope

Production imports/AI calls, arbitrary-precision model migration, new dependencies, backend/schema changes, native m34, and unrelated142 initial field-error wiring.
