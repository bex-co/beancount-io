---
id: w3/m36/t005
title: "Test exact values and parser-stage boundaries"
worker: worker3
status: todo
estimate: 45m
depends_on: [w3/m36/t003]
---

## Objective

Add meaningful real-parser and rendered workflow regressions using the four-row fixture in the milestone README. Assert original tokens, validity counts, Configure exclusion and locally captured submission values for supported controls. Exercise all-unsupported CSV with a local LLM/upload spy, inline correction, negative/signed-zero/exponent equivalents, malformed grouping and overflow. Repeat desktop/narrow browser previews without importing. Run dashboard format:check, lint, test and build from dashboard/.

## Context

Read the parent README for exact reproduction, source trace, controls and limits. This is a residual of completed m19's accepted-value contract, not a new historical finding group.

## Steps

1. Implement the bounded objective above in the owning dashboard package.
2. Verify the specified acceptance outcomes and preserve the parent exclusions.

## Files

dashboard importer parser/configuration/workflow tests and browser evidence

## Acceptance criteria

- [ ] Three changed-value inputs cannot be configured or submitted; the valid control remains1.25. The all-unsupported stage cannot bypass rejection via fallback. Existing date/calendar, original m19 validity and m26 display controls pass. Native API writes are not used as tests.

## Out of scope

Production imports/AI calls, arbitrary-precision model migration, new dependencies, backend/schema changes, native m34, and unrelated142 initial field-error wiring.
