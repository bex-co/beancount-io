---
id: w3/m16/t005
title: Test the interval-to-statement and export pipeline
worker: worker3
status: todo
estimate: 45m
depends_on: [w3/m16/t003]
---

## Objective

Prove that real response semantics survive merging, calculation, hierarchy
rendering and export, rather than testing a mocked rollup assumption.

## Acceptance criteria

- [ ] A captured-response-shaped fixture exercises merge → model → tree/export;
      monthly and yearly period totals are identical and reconcile to independent
      cash snapshots. Assertions fail against the original leaf-only code.
- [ ] Table and CSV/Markdown retain a direct parent and descendant in different
      units, including zero/empty children and multiple hierarchy levels.
- [ ] Empty/zero data, declared roles and cash transfers remain correct; the
      non-colliding real-estate November control is unchanged.
- [ ] Print's shared document is covered without invoking a native print dialog;
      chart series use the corrected interval points.
- [ ] Run dashboard `yarn format:check`, `yarn lint`, `yarn test`, and `yarn build`.
