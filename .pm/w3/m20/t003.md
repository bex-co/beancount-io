---
id: w3/m20/t003
title: "Verify filter commits and clearing across responsive views"
worker: worker3
status: todo
estimate: 25m
depends_on: [w3/m20/t002]
---

## Objective

Make the responsive presentation use the same committed selections and result requests as the desktop controls.

## Context

Read the parent milestone and promoted 004 for exact evidence, root causes,
working controls, scope and limits. Follow the owning dashboard guidance.
This milestone owns shared filter visibility; m15 owns URL/provider synchronization.
Existing keyboard suggestion commits are separately reproduced in [052](../052.md):
the common Combobox's immediate blur overwrites a selected value with its draft.
Coordinate that primitive repair before accepting panel selection behavior;
do not add a panel-specific workaround for it.

## Steps

1. Exercise the existing blur/Enter combobox commits inside the panel, including its portaled suggestions, Escape dismissal, explicit option selection, and Clear all. Resolve focus or duplicate-commit problems introduced by the panel.

2. Use the public crypto-example Journal control: time=2025 returns one transaction, clearing returns 68. Resize with active filters, reload, and reopen the panel without losing committed values.

3. Check Cash Flow plus the other currently enabled header routes with the same component. Preserve pending/error behavior of the attributes query and do not advertise an empty selection while values are still loading.

## Files

- dashboard/src/common/components/ledger-search-controls/index.tsx
- dashboard/src/common/components/ledger-layout/layout-header.tsx
- dashboard/src/common/components/ledger-search-controls/**tests**/filter-combobox.test.tsx

## Acceptance criteria

- [ ] Time, Account and payee/tag changes issue the same filter inputs at desktop and narrow widths.
- [ ] Clear all removes account/filter/time, restores the 68-transaction control and preserves unrelated valid route state.
- [ ] Panel open/close and resizing do not accidentally clear or recommit values; attribute-loading and failure states remain honest.

## Out of scope

- m15's URL/provider synchronization implementation, Journal pagination policy, and production writes.
