---
id: w3/m20/t002
title: "Provide a usable filter panel below the desktop breakpoint"
worker: worker3
status: todo
estimate: 40m
depends_on: [w3/m20/t001]
---

## Objective

Keep Time, Account, payee/tag and Clear all reachable at narrow browser widths.

## Context

Read the parent milestone and promoted 004 for exact evidence, root causes,
working controls, scope and limits. Follow the owning dashboard guidance.
This milestone owns shared filter visibility; m15 owns URL/provider synchronization.

## Steps

1. Replace the hidden-only narrow behavior with an accessible Filters trigger and existing Sheet/Dialog primitives. Keep the inline controls on desktop and expose active filter state on the narrow trigger.

2. Adapt LedgerSearchControls layout so its three comboboxes, suggestions and Clear all fit within 390px and 768px widths without horizontal clipping. Reuse the existing provider values and handlers.

3. Give the panel a localized accessible title and named close action; preserve focus return and keyboard access. Use existing translations where applicable and add locale entries only for new product copy.

## Files

- dashboard/src/common/components/ledger-layout/layout-header.tsx
- dashboard/src/common/components/ledger-search-controls/index.tsx
- dashboard/src/common/components/ledger-layout/**tests**/layout-header.test.tsx

## Acceptance criteria

- [ ] A fresh narrow Journal URL with time=2025 provides a visible way to inspect and change that selected period.
- [ ] The controls and suggestion lists fit at 390px and 768px; desktop still has inline filters.
- [ ] Opening/closing the panel returns focus appropriately and creates no second independent filter state.

## Out of scope

- A new filter language, dependencies, account-report interval/valuation controls in 044, or unrelated sidebar redesign.
