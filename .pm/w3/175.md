# Keep zero-balance accounts inside their actual parent branches

**Severity:** minor. **Estimate:** 45–60 minutes. Owner: mobile. A2: users can browse newly opened and zero-balance accounts without the tree misrepresenting their parentage.

September 11, 2026; HEAD f1965988 / mobile7538fb0d; native io.beancount.ios1.20260906.47; iPhone17e/iOS26.5; English/System-light/default large. Authenticated production/public open_ledger/minimax. Actual Discover selection -> Home balance-card See all -> Accounts. No account created or ledger changed.

1. With Assets expanded and NonCurrent collapsed, the rows NonCurrent:Goodwill and NonCurrent:IntangibleAssets are still visible as direct children of Assets, both0.00 MUSD.
2. Tap NonCurrent's chevron at(41,241). Its nonzero Other211.14 and PropertyPlantEquipment74.91 rows appear indented inside the branch. The two zero rows remain outside it at the shallower indentation. Collapse again: the nonzero children disappear correctly but both zero rows remain.
3. Terminate/relaunch PID91612, settle Home and take the actual See all route. The same zero rows appear outside the collapsed parent. Initial repeat sequence ran in PID89744 after switching from Adyen to MiniMax; both use default text size.

Expected: Goodwill and IntangibleAssets are children of Assets:NonCurrent, reveal with that branch and hide when it collapses, while retaining zero values and valid detail navigation. Actual: the local tree promotes them to Assets-level siblings and the parent's disclosure does not control them.

Read-only GetLedgerTrialBalance at_cost returns Assets:NonCurrent with four correctly nested children: Goodwill and IntangibleAssets have empty balance maps/hasTxns:false; Other211.138 MUSD and PropertyPlantEquipment74.913 MUSD are nonzero. Parent total286.051 MUSD and Assets1714.668 remain correct. ledgerMeta.accounts includes both exact zero account names. This API already contains the zero children; the server has not flattened or omitted them in this fixture.

Root: mobile/src/components/account-list/select-account-list.ts:toNode/buildTopLevel filters zero-valued nodes before compressing chains/pass-through levels. select-trial-balance.ts:96–120 uses that result, then appendZeroBalanceAccounts at48–81 treats the removed names as missing and appends each directly to category.children with name=account.slice(root.length+1), value0, children[]. The actual unchanged selector probe yields Assets children [Current,NonCurrent,NonCurrent:Goodwill,NonCurrent:IntangibleAssets]; NonCurrent has only its two nonzero children. AccountTable/flatten-rows correctly respects the supplied tree, so changing row indentation alone would leave disclosure membership wrong.

Fix the Accounts tree preparation so zero accounts retain their real ancestor chain, including metadata-only accounts absent from a cached/API tree. Merge/preserve them at the correct structural level before deciding whether ancestors can be compressed, rather than reattaching them to the category after compression. Keep balances unchanged, avoid duplicate ancestors/accounts, and retain zero-account discoverability from the open-account feature. Scope zero inclusion to Accounts: the shared hierarchy helpers also drive report category views whose existing zero-filtering behavior must not change unintentionally. Do not repair this by hiding zero accounts again or renaming full ledger accounts.

Acceptance: fresh MiniMax Accounts hides both zero children when NonCurrent is collapsed, shows all four properly nested children when expanded, and each zero row still opens its correct detail. Nonzero sibling disclosure and parent totals remain correct. Add selector/flattening regressions for a zero API leaf under an existing parent, a metadata-only new zero leaf, a missing intermediate parent, mixed zero/nonzero siblings, compressed chains and duplicate metadata. Preserve Reports' existing hierarchy policy. No new dependency/backend change needed. Actual production creation, Android, VoiceOver and network-race behavior remain unverified.

Dedupe/history: all root .pm open/done searched for zero-balance account hierarchy, metadata merge, parent disclosure, Goodwill and appendZeroBalanceAccounts; no matching open item. w3/173 concerns accessibility exposure of the separate disclosure control, not wrong tree membership;159 concerns Back metadata on a Goodwill detail working control. Dashboard closed-account filtering in w4/m2 and compact MCP summary work in w2/m28 are unrelated. Commit6dda6746 added flat zero-account restoration for Open Account visibility after the shared report-oriented filtering; the only earlier selector change6b237cb0 handles valuation. Current main still reproduces the structure.

Verified ignored evidence under mobile/tmp/qa-2026-09-11/: account-zero-parent-collapsed.jpg, account-zero-parent-expanded.jpg, account-zero-parent-recollapsed.jpg, account-zero-fresh-home.jpg, account-zero-fresh-repeat.jpg, minimax-zero-tree-api.json, minimax-picker-meta-api.json, qa-zero-tree.graphql, account-zero-selector-probe.json, account-zero-js-errors.json. Original component code was transpiled unchanged for the offline probe; native expansion/collapse was exercised separately. No product fixes, commits or shipping; fresh launch leaves the default collapsed branches.
