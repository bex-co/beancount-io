# Expandable mobile report categories omit accessible amounts

**Minor · 30–40m · mobile.** A2: readers using assistive technology must be able to compare expense and income categories on the phone.

Native iPhone 17e, iOS26.5, io.beancount.ios1.20260906.47, checkout f1965988, https://beancount.io, 2026-09-11. Authenticated read-only public synthetic open_ledger/freelancer-invoicing; pull:true, push:false, admin:false. English/System light, default text size, Reports6M.

Repro: open the public ledger's income-statement shared link, scroll Reports to Expenses. Tax visibly shows $8,650.00 and58% of total $15,042.40. Native XCTest attributes expose its enabled, hittable Button as {label:"Tax",value:"",children:0}; no amount or percentage is exposed below it. BadDebt on the same card has accessible StaticText for its name and $3,200.00. Expand Tax: its visible children correctly show FederalEstimated $5,000 and SelfEmployment $3,650. Terminate/relaunch (PID28260), verify Home, tap the actual Reports tab, scroll once: collapsed Tax again exposes only its name with empty value and zero children; fresh screenshot still shows amount/percentage. This verifies native accessibility metadata, not VoiceOver speech or navigation.

Expected: an expandable category conveys its name, formatted amount/currency and top-level share, while preserving button/expanded semantics. Actual: the custom name-only label excludes the visible financial content. API IncomeStatement monthly at_cost independently confirms tax5000+3650=8650, total expenses15042.40 and 6M income13992.40; this is not a financial calculation error.

Root: mobile/src/screens/reports-screen/components/category-breakdown.tsx:259–267 top-row TouchableOpacity sets accessibilityLabel={node.name}; its AmountText and percent label are descendants, so native grouping replaces their automatic text. The recursive child-row branch at191–201 uses the same name-only label (nested expandable case source-traced, not natively exercised here). These labels were introduced by bc151c5c in completed w4/m7. ReportsScreen179/195 are the two callers for Expenses and Income. Leaf rows use plain View and retain accessible text, providing the working control.

Fix both owned expandable branches with a localized accessible value/summary containing the displayed amount and currency, and meaningful top-level percentage. Preserve expanded/collapsed state, hierarchy, tap behavior and leaf text. Verify income and expenses with nested expandable categories, zero/negative amounts and range changes; avoid duplicate announcements. Loading skeleton, empty, denied and failed-query behavior should remain separate from settled amount labels. No API change required.

Dedupe: full open/done board searches for category-breakdown, report accessibility and category amount labels found no existing category-row item. w4/m7 is the completed baseline whose change caused this omission. w3/123 concerns the separate Home Budget row, and124 the budget chart summary; neither fixes this Reports component. w3/127 is dashboard hidden-chart keyboard focus, a different package/cause. Targeted file history shows no later fix.

Evidence under mobile/tmp/qa-2026-09-11/: freelancer-tax-ax.json and freelancer-tax-ax-repeat.json (sanitized explicit native attributes); freelancer-reports-scroll-1.jpg, freelancer-tax-expanded.jpg, freelancer-tax-fresh-repeat.jpg, freelancer-tax-relaunch-home.jpg; freelancer-income-api.json and freelancer-access.json. Full local native snapshots freelancer-tax-ax.log and freelancer-tax-ax-repeat.log remain ignored. Bounded JS error capture freelancer-reports-errors.json provides no diagnostic for this semantic defect. Screenshots946x2048 correspond390x844 logical points; scratch XCTest inspection and native drag helpers use the copied installed runner, with no product/dependency edits.

Acceptance: on the same native journey the Tax element exposes $8,650.00 and58% alongside Tax, and expansion remains operable; nested income/expense branches expose their own values; leaf controls continue exposing text. Add a meaningful accessible-props regression during implementation and recheck native attributes. Android, VoiceOver audio, other locales, nested expandable native rows, error states and patched results remain unverified. Report only: no product fix, ledger mutation or shipping.
