---
id: w3/m5/t008
title: Closeout
worker: worker3
status: todo
estimate: 20m
depends_on: [w3/m5/t007]
---

## Objective

Close m5 once its definition of done actually holds — not merely when the code is written.

## Context

- DoD: see `w3/m5/README.md`, including the "what this milestone is NOT" section. Porting the 52 reads is explicitly not required here.
- Satisfiable entirely in-repo: no deployment needed. Do not let ADR 0007's outstanding operator items (migrations, the `/mcp` edge alias) block this.
- Update ADR 0008's status line to record which decisions landed.
- Run `/pm done w3/m5/t008` last — completing it triggers the milestone move to `w3/done/m5/`.

## Steps

1. Verify the DoD end to end, including driving `resources/read` against a real client.
2. Update ADR 0008's status and record the URI-scheme decision if t004 left it open.
3. Close out tasks, milestone README, and workstream checkbox per board conventions.

## Files

- `.pm/w3/m5/**`, `.pm/w3/README.md`, `backend-cluster/backend-v2/docs/ADR0008-surface-parity.md`

## Acceptance criteria

- [ ] DoD verified, not just code-complete; board state consistent in all three places.

## Out of scope

- Porting reads, grouping writes, the REST tail — follow-on milestones.
