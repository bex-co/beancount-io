# Report filter changes expose old results without pending feedback

**Promoted to [m35](./m35/README.md)** as the sole implementation queue. The original minor route-feedback case below is now joined by a major pending-conversion export mismatch; expanded scope and estimates live in the milestone.
Residual coverage of completed **w3/m15**'s explicit “no stale data is presented as a settled response for a different URL” DoD. Do not count another historical group or reopen its working URL-persistence tasks.

Production https://beancount.io, 2026-09-11, public synthetic open_ledger/freelancer-invoicing, authenticated reader, English/light, isolated headless Chrome152. These are explicitly **browser-only delayed-read simulations**, not production outages or changed API payloads. No server mutations. Local/fetched origin f1965988; full deployed SHA unverified.

1. Fresh1440×1000 open /ledger/open_ledger/freelancer-invoicing/trial-balance?time=2026-01 and wait for real January data: Assets32000USD, Income-15500USD.
2. Hold only the next GetLedgerTrialBalance request with variables.time2026-02 in the owned browser's route interception. Enter2026-02 in Time and commit with Enter.
3. After1.5s while request remains held, URL and input show February, but January numbers/chart remain without a loading label, aria-busy, progressbar or status. Screenshot shows the visible February control over January values.
4. Release the unchanged real request: HTTP200 replaces values with February Assets41200USD and Income-9200USD. No page errors.
5. Fresh390×844 repeats through Filters → Time → Enter → Close. The same old values remain under the new URL with no pending feedback, then correct values appear after release.
6. Independent fresh desktop Balance Sheet, Income Statement and Cash Flow repeats hold their corresponding February report operation. All retain January results without pending feedback. Balance Sheet Assets32000→41200; Income-15500→-9200; Cash closing16500→27500. All succeed200 after release without page errors.

Export control/limit: Export stays enabled on all three exportable pages. A second fresh Balance Sheet delay permits an actual CSV download before releasing the February read. That file correctly says time_selection2026-01, as_of_date2026-01-31, Assets32000; its filename/print-period also correctly say January. After release, CSV says February/2026-02-28 and41200. This is an unannounced old-scope export under a new visible selection, **not mislabeled CSV data, stale settled print props, or an API calculation bug**.

Source trace: shared LedgerSearchParamsProvider at common/providers/ledger-search-params-provider/ledger-search-params-provider.tsx uses router useSearch and replace navigation at23–43. Each report route's loaderDeps includes time and its loader awaits the report query. The new browser location/combobox draft precede the resolved route match, leaving the old report tree and its old export filter props mounted while the read waits. dashboard/src/router.tsx and these report route wrappers provide no pending component, and LedgerLayout has no route-pending feedback. Installed TanStack Match.tsx:54–57 resolves absent pending UI to null. This route-loader phase is the directly reproduced cause; merely changing an Apollo isLoading branch does not cover it.

Related later-phase exposure: the four features/reports/{trial-balance,balance-sheet,income-statement,cash-flow}/index.tsx retain previousData and show ReportLoadingState only without existing data. Preserve coherent behavior for Apollo-only interval/conversion refetches too, but those transitions were not independently delayed in this finding.

Fix: expose the outstanding report scope as pending at the shared report/ledger boundary while keeping filter correction/navigation usable. Do not present old numbers as settled for the visible new selection; either replace the old report with an accessible loading state or clearly identify its prior scope and pending status. Disable current-scope export actions until matching data arrives. Preserve cached instant transitions, query reuse, error recovery, filter URL retention, prior chart preferences and settled export accuracy. No independent filter store, backend change, dependency or global unrelated-page spinner is needed.

Regression acceptance: with a controllable real router-loader promise, January→February shows pending feedback before fulfillment, then matching February data; rapid replacement cannot show an older late response as current. Cover narrow Filters close, correction after a rejected read, instant cache hits and settled CSV consistency. Existing provider filter-navigation tests use placeholder pages and no delayed loader; extend that meaningful integration boundary plus relevant report behavior. Run dashboard format/lint/test/build on implementation.

Dedupe: full m15 DoD owns this residual transition case; a462057c repaired URL/search ownership and settled filters, which now work. Historical020/12a777f7 repaired a separate printDocument useState snapshot after settlement; the CSV/print-period controls here prove internally correct old props during pending navigation. m16 concerns period computation;126/127 concern tab state and hidden chart controls. No open note duplicates this delayed-filter journey. Keep historical group count unchanged.

Evidence dashboard/tmp/qa-20260911-w3-loop/: trial-pending-before.json, trial-pending-held.json, trial-pending-released.json, trial-pending-held-390.json, trial-pending-released-390.json, reports-pending-controls.json, pending-export.json, pending-balance-february.csv, settled-balance-february.csv, trial-pending-held-1440.png, trial-pending-released-1440.png and trial-pending-held-390.png. Desktop held screenshot visually inspected. All interception holds released and their contexts closed. An earlier700ms natural-read capture was still pending, not a settled filter failure; invalid calendar date eventually shows Error loading data and correcting to cached January restores the correct report. No product fix or shipping. Actual slow server incidence, native screen reader, other browsers and patched results remain unverified.

Related recovery follow-up: [134](./134.md) owns the reproduced stale Cash Flow boundary after a corrected read succeeds. This note remains scoped to pending feedback during held successful reads; coordinate rejected-read acceptance without duplicating that repair.

Overview shares the same pending phase: two fresh1440×1000 repeats and a fresh390×1000 repeat start at /ledger/open_ledger/freelancer-invoicing?time=2026-01&lang=en. Hold only GetLedgerOverview with time2026-02, then commit February through Time (narrow: Filters→Time→Enter→Close). During the1.2s hold, URL is February but NetWorth32000 and January MoneyIn15500 remain with no busy/status/pending feedback. The January section labels correctly describe that old content; no mislabeled card data is claimed. Release the unchanged real read:200 updates NetWorth41200, February MoneyIn9200 and the account balances. No page errors/writes. Source features/reports/overview/loader.ts:33–47 awaits the primary query; its route uses ledgerFilterLoaderDeps and the same provider/pending boundary. Optional README/account metadata are already nonblocking and are not the cause. Include Overview in this shared fix and its delayed-scope acceptance; do not create a separate implementation or historical group. Evidence dashboard/tmp/qa-20260911-w3-loop/overview-pending-{0,1}.json, overview-pending-390.json and overview-pending-held-390.png (inspected). All holds released and contexts closed.

Promotion evidence: at both1440/390, selecting Units while its real read is held allows a CSV declaring conversion=units with At Cost Assets USD80149.80683/VACHR25. Release yields the actual Units map beginning USD6763.51. m35 records the full reproduction, API/CSV controls and implementation tasks. This extends the existing pending-state finding; the earlier date-navigation CSV remained correctly labeled January.
