---
id: w3/m13/t003
title: Carry account filters through gateway GraphQL, REST and MCP
worker: worker3
status: todo
estimate: 45m
depends_on: [w3/m13/t002]
---

## Objective

Forward the optional account selector lists through the gateway's existing authenticated read path.

## Context

See [the reproduced failure and fix constraints](./README.md). Keep work within
its owning package; use the documented contracts across package boundaries.

## Steps

1. Regenerate the Fava client with yarn generate-fava-client from backend-v2 after the source IDL changes.
2. Add optional input fields and forward them explicitly; share the existing REST/MCP journal descriptor and array parsing conventions.
3. Regenerate the v1 OpenAPI snapshot and relevant agent types when affected; verify real adapter forwarding and existing authorization.

## Files

- `backend-cluster/backend-v2/src/features/ledger/api/resolvers/ledger-journal-resolver.query.ts`
- `backend-cluster/backend-v2/src/features/ledger/service/ledger-journal-service.ts`
- `backend-cluster/backend-v2/src/features/ledger/api/rest/v1/journal-reads.ts`
- `backend-cluster/backend-v2/src/features/ai-agent/api/mcp-resources.ts`
- `backend-cluster/backend-v2/src/foundation/fava/Api.ts`

## Acceptance criteria

- [ ] GraphQL, REST and MCP carry every supplied selector and paging field to the account service.
- [ ] Omitted selectors remain compatible; no adapter silently strips the new fields.
- [ ] Backend typecheck, parity/contract tests and snapshot checks pass.

## Out of scope

- Production ledger writes, unrelated report calculations, and other QA findings.
