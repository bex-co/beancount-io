---
id: w3/m13/t001
title: Filter account journal rows before counting and paging
worker: worker3
status: todo
estimate: 45m
depends_on: []
---

## Objective

Implement optional account-journal display selectors in the ledger service without changing the running balances of retained rows.

## Context

See [the reproduced failure and fix constraints](./README.md). Keep work within
its owning package; use the documented contracts across package boundaries.

## Steps

1. Reuse existing directive/transaction/document/custom predicates and the main journal's omitted/empty-list semantics.
2. Compute account rows with the established clamp/valuation/children rules; filter those rows before total and page slicing.
3. Carry optional selector parameters through the account-journal HTTP handler and add a targeted service regression.

## Files

- `backend-cluster/ledger/src/features/ledger/service/ledger-journal-service.ts`
- `backend-cluster/ledger/src/api/journal.ts`
- `backend-cluster/ledger/src/foundation/rustledger/journal-running-balance.ts`

## Acceptance criteria

- [ ] A mixed-flag, multi-page fixture returns only matching rows and the correct total.
- [ ] Retained change/balance values exactly match the unfiltered account journal.
- [ ] Omitted filters preserve the prior response; input validation and access controls still apply.

## Out of scope

- Production ledger writes, unrelated report calculations, and other QA findings.
