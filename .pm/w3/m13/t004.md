---
id: w3/m13/t004
title: Connect the account page selectors to the filtered read
worker: worker3
status: todo
estimate: 40m
depends_on: [w3/m13/t003]
---

## Objective

Make each visible account-journal type/subtype selection participate in the server read and its pagination identity.

## Context

See [the reproduced failure and fix constraints](./README.md). Keep work within
its owning package; use the documented contracts across package boundaries.

## Steps

1. Regenerate dashboard GraphQL types against the updated schema using the package's codegen flow.
2. Pass the four selector lists with the main journal's semantics; reset the effective page when the selected result set changes.
3. Render a truthful pending/empty/error state and keep Metadata/Postings independent; rerun the public Salary reproduction at both widths.

## Files

- `dashboard/src/features/reports/account/index.tsx`
- `dashboard/src/features/reports/account/graphql/account.graphql`
- `dashboard/src/graphql/definitions.ts`
- `dashboard/src/features/journal/components/journal-filters.tsx`

## Acceptance criteria

- [ ] Pending contains no cleared payroll entries; Open-only contains no transaction rows.
- [ ] Changing and clearing filters produces matching totals/pages, with no client-only page filtering.
- [ ] Main Journal filtering and account presentation toggles still work.

## Out of scope

- Production ledger writes, unrelated report calculations, and other QA findings.
