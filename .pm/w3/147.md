# Report chart toolbars clip conversion controls on narrow screens

**Minor · 35–45m · dashboard.** A2: readers should be able to see and operate report conversion controls without widening the browser.

Production https://beancount.io, 2026-09-11, isolated headless Chrome152, English/light, authenticated QA reader of public open_ledger/example, time2016. Local/fetched main f1965988; deployed SHA unverified.

At320×900 open /ledger/open_ledger/example/income-statement?lang=en&time=2016, with charts shown. Choose View → Expenses Hierarchy. The conversion selector is almost completely clipped: its button spans x303.81–401.45, while the chart wrapper ends at312; only8.19px of its97.64px width remains. Its At Cost text and arrow are not visible. Fresh320 and390 repeats measure the same mechanism: at390 the last19.45px remain clipped. A1440 desktop control shows the full97.64px button.

The wrapper has overflow-x:hidden. A browser-local horizontal wheel gesture leaves scrollLeft0 and the same clipped bounds; document width equals viewport width, so page scrolling cannot reveal the rest. This is not the intentional horizontal scroll behavior of Journal/Accounts rows. Focusing the underlying control and pressing Enter opens all four options (At Cost, At Market Value, Units, Converted to USD), establishing that the selector itself works; no conversion/API calculation failure is claimed. Screenshots precede that focus action and show the actual clipped layout.

Fresh Balance Sheet320 repeats clip the default At Cost selector, with only a smaller remnant after Assets Hierarchy is selected. Cash Flow320 also clips its default selector. Balance Sheet390 is a near-fitting control rather than an allegation of a material one-pixel defect. Income Statement320 and390 screenshots were inspected.

Root: the chart-toolbar row is non-wrapping flex, paired with a shrink-0 interval/conversion group, inside the collapse wrapper's overflow-hidden container. Owning rows are features/reports/balance-sheet/balance-sheet-content.tsx:190, income-statement/income-statement-content.tsx:229 and cash-flow/cash-flow-content.tsx:227. The shared ResponsiveTabTriggerList uses a content-width narrow View selector, so longer selected text increases the overflow. Wrapping only the inner group cannot wrap the two outer siblings.

Allow these toolbar rows to wrap or stack their View and interval/conversion groups when space is insufficient. Keep every full control within its visible container at320/390, including the exercised long View labels, while retaining the desktop arrangement. Preserve chart collapse and its focus repair in127; removing the collapse container's clipping is not the fix. Reuse the existing control components; no new abstraction, dependency or API change is needed.

Acceptance: real browser geometry and screenshots at320/390 show complete View, interval and conversion triggers for the exercised reports/views; pointer and keyboard selection work, and1440 remains aligned. Check a longer translated label as a regression control during implementation. Keep the current values, chart state and report semantics. Use meaningful browser checks rather than tests that only assert a CSS class; run dashboard checks on implementation.

Dedupe: all queues including done searched for chart toolbar/conversion clipping and narrow controls. Completedm20 repairs global Time/Account/Filter access, and w4/done/m1 wraps the separate export header. Neither owns this inner chart toolbar. 127 concerns controls retained inside collapsed charts; this case occurs with charts fully shown. 126 concerns Trial Balance tab state. Targeted content-file history/current fetched source retain these row classes. Trial Balance's explicitly hidden selector is a separate responsive branch, not part of this overflow mechanism.

Verified ignored evidence dashboard/tmp/qa-20260911-w3-loop/: report-controls-clip-{balance-sheet-320,balance-sheet-390,income-statement-320,cash-flow-320}.json/.png and report-control-scroll-{320,390,1440}.json/.png. Initial aggregate probes stopped on Trial Balance because its selector is hidden; the four earlier per-route artifacts completed, and independent Income Statement repeats completed. All contexts closed, no page errors or additional write attempts. No product fix, commit or shipping.
