---
id: w3/m12/t002
title: "Restore editor text from bookmarked query parameters"
worker: worker3
status: todo
estimate: 35m
depends_on: [w3/m12/t001]
---

## Objective

Keep the query editor aligned with a query supplied by the page URL on initial load and external URL changes.

## Context

- The query route accepts a query string and auto-executes it, but editor state always starts as select * from accounts.
- The exact two-row bookmark reproduces the mismatch on desktop and at 390×844.

## Steps

1. Initialize the editor from the normalized URL query and synchronize deliberate external query changes.
2. Keep normal typing independent of unrelated renders and preserve the existing guard against duplicate execution after a button/shortcut updates the URL.
3. Verify reload, a direct bookmark, and subsequent editing/execution using the public example.

## Files

- `dashboard/src/features/bql/pages/index.tsx`
- `dashboard/src/routes/ledger.$ledgerOwner.$ledgerName.query.tsx`
- `dashboard/src/features/bql/pages/__tests__/`

## Acceptance criteria

- [ ] The editor and result match select account limit 2 after reload/direct opening.
- [ ] An external query change updates the draft appropriately, while unrelated renders preserve an in-progress edit.
- [ ] URL-driven and locally submitted execution each issue the intended query exactly once.

## Out of scope

- Redesigning query history, URL formats, or result exports.
