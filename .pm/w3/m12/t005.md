---
id: w3/m12/t005
title: "Test coverage for query execution and restoration"
worker: worker3
status: todo
estimate: 40m
depends_on: [w3/m12/t003, w3/m12/t004]
---

## Objective

Verify the interaction between current-text execution, URL restoration, history, error recovery and exports.

## Context

- Live button execution and CSV are working controls; tests must preserve them while fixing shortcut and bookmark behavior.

## Steps

1. Exercise a mounted editor's shortcut after successive edits, fresh URL initialization, external query changes, and duplicate-request prevention.
2. Cover invalid-query recovery and verify that history/export data stays associated with the executed query.
3. Run the owning packages' normal gates and repeat the documented desktop/narrow public journeys. Record platforms/transports that remain unverified.

## Files

- `dashboard/src/features/bql/`
- `dashboard/src/routes/ledger.$ledgerOwner.$ledgerName.query.tsx`

## Acceptance criteria

- [ ] Tests assert observable behavior and submitted/returned data, rather than only mocked callback invocations.
- [ ] Relevant package gates pass and the milestone's demonstrated failures no longer reproduce.

## Out of scope

- Writing to real production data without separate authorization or adding tests that only mirror implementation.
