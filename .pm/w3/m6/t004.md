---
id: w3/m6/t004
title: Adoption surface
worker: worker3
status: todo
estimate: 20m
depends_on: [w3/m6/t003]
---

## Objective

Eleven new endpoints on two surfaces are only useful if someone can find them.

## Context

- `backend-cluster/backend-v2/README.md` — the v1 section and "Connecting an MCP client".
- The published OpenAPI document at `/api-gateway/v1/openapi.json` carries the REST half automatically once t001's snapshot regenerates; the MCP half is discoverable through `resources/templates/list` and needs prose.
- Agent-facing check: an agent should learn these exist without being told — verify the template descriptions read well in a `resources/templates/list` response, since that listing is the whole discovery mechanism.

## Steps

1. Update the READMEs for both halves.
2. Run `yarn mcp:conformance` against a local stack.
3. Run `python3 scripts/check-agent-guidance.py` if instruction files changed.

## Files

- `backend-cluster/backend-v2/README.md`, root `README.md`

## Acceptance criteria

- [ ] Both surfaces' new reads are documented; the eleven are named somewhere a reader will find them.
- [ ] `check-agent-guidance.py` passes.

## Out of scope

- Documenting reads not yet ported.
