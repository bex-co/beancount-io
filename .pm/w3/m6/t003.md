---
id: w3/m6/t003
title: Lower both ratchet ceilings, and prove they hold
worker: worker3
status: todo
estimate: 20m
depends_on: [w3/m6/t001, w3/m6/t002]
---

## Objective

Spend the ratchet in the direction it was built for. m5 set ceilings on `deferred:` count per surface; eleven verbs left the deferred set on each, so both come down by eleven.

## Context

- `src/server/api/__tests__/surface-parity.test.ts` — the ceilings from m5/t003.
- Lowering is the whole point: the ratchet exists so this number can only fall unless someone raises it in a visible diff.
- Verify the new ceilings are *tight* — set to the actual count, not a round number above it. A ceiling with slack is a ceiling that lets the next unpaired verb through unnoticed.

## Steps

1. Recount `deferred:` per surface; set both ceilings to the actual numbers.
2. Confirm the test fails if either ceiling is set one lower — that is what proves it is tight.

## Files

- `src/server/api/__tests__/surface-parity.test.ts`

## Acceptance criteria

- [ ] Both ceilings fall by eleven and equal the real counts.
- [ ] Setting either one lower fails the test.

## Out of scope

- Changing what the ratchet measures.
