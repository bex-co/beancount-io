# New Entry squeezes its heading into a column of letters on narrow screens

**Severity:** minor. **Owner:** dashboard. **Estimate:** 30–45 minutes.
**A2:** a person opening a new directive can read its heading and reach the
form without a needlessly tall header or horizontal dialog overflow.

Production `https://beancount.io`, 2026-09-08, Chrome 152, light theme.
Existing QA session, public `open_ledger/minimax`, English/French/Chinese.
Local HEAD `a315c273`, fetched main `afb36a9b`; implicated source unchanged.
Deployed SHA unverified. All checks use empty local forms; no submission occurs.

1. At390×844 open Journal → New Entry. The supported direct route is
   `/ledger/open_ledger/minimax/journal?lang=fr&action=new-entry&directive=transaction`.
2. **Nouvelle écriture** is squeezed to13.2px wide and fourteen lines,252px
   tall. Its four-tab strip extends to x394.2, past the390px viewport. The
   dialog has scrollWidth393 versus clientWidth388 and shows horizontal overflow.
3. English behaves similarly: **New Entry** is14.3px wide, seven lines and126px
   tall; its tab strip ends at x395.3. Both reproduce at320px as well.
4. At1440×1000 both headings occupy one18px line. Chinese is also a useful
   compact-label control: one line at390px, two at320px, with tabs fitting.
5. In four fresh English/French narrow controls, change only the mounted
   header's flex direction to column, stretch its children, and prevent the
   title from shrinking. Both headings become one18px line; the header is70px
   tall and dialog scrollWidth equals clientWidth. This is an isolated DOM
   diagnostic, not a source patch or fabricated server response.

Expected: the title wraps at usable word widths and the tabs fit inside the
dialog. Actual: reserving a full-width tab strip on the same row leaves only
roughly one letter of space for the title. French consumes252px of vertical
space before the form begins. Tabs still work after navigation settles and the
form can be scrolled; this is a layout defect, not a claim that entry creation
or a particular tab is inaccessible.

Root: `features/journal/components/new-directive-dialog/index.tsx:68–79`
keeps title and four-column `TabsList` on one flex row at every width, while
the tab list explicitly has `shrink-0`. The root shell's existing
`[overflow-wrap:anywhere]` at `common/root-route/shell-component.tsx:20`
is inherited by the title, so shrinking produces letter-by-letter lines.
The modal deliberately uses full narrow width with24px padding at
`new-directive-dialog/index.tsx:54` and `common/components/ui/dialog.tsx:66`;
there is no hidden extra space available to that row. Computed styles and
actual CSS rules confirm normal word-break plus overflow-wrap:anywhere.

Make this header responsive locally: give the heading sufficient width and
place the tabs on their own fitting row when both cannot fit together. Keep
the desktop presentation, full tab names, selected state, keyboard navigation,
Close action and existing form scrolling. Do not remove the root wrapping
protection, globally change Dialog/Tabs, abbreviate translations or alter
accounting fields to compensate for this layout.

Blast radius: this one header serves Transaction, Balance, Note and Account.
`journal/pages/journal-page.tsx:277–300` opens it for writers and controls its
tab through the action/directive search parameters. All four pointer tab
transitions pass in each fresh narrow control after waiting for the actual
selected state. Some initial180ms samples preceded navigation completion;
those are explicitly excluded as tab-selection failures.

Acceptance: at320/390px in English and French the title is readable without
single-letter stacking, the header stays within the dialog and all four tabs
remain operable by pointer/keyboard. Confirm Chinese and1440px controls, all
four form panels and vertical scrolling. Screenshot/viewport verification is
appropriate for this CSS-only repair; preserve existing behavioral tests and
run the owning dashboard gates. Native browsers, zoom and keyboard tab changes
under the diagnostic style were not separately tested in this QA pass.

Dedupe: local open/completed boards and fetched main searched for New Entry
heading, dialog overflow, tabs and wrapping.025 concerns financial table cells;
076/m23/m24 concern draft cleanup, focus and dates;084 concerns validation copy.
None owns this header layout. The component's row/shrink policy dates to
`af5339de`; later root locale loading (`7b7b35ed`) and current main do not fix it.

Ignored evidence under `dashboard/tmp/qa-20260907-w3-loop/`:
`new-entry-heading-evidence.json`, three
`new-entry-heading-fr-{1440,390,320}.png` captures, and four
`new-entry-heading-stacked-{en,fr}-{390,320}.png` diagnostic controls.
Nine initial documents and four fresh controls all return HTTP200, with no
page exceptions or mutation attempts. Sixteen settled pointer tab transitions
pass in those fresh controls. CSS inspection used another read-only context.
Every isolated context was closed; no product edit or ledger operation occurred.
