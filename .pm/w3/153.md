# Dark-theme statement PDFs retain black page margins

**Severity:** minor. **Estimate:** 20–30 minutes. Owner: dashboard. Reproduces when background printing is enabled; the default background-disabled headless PDF is a working control.

Production https://beancount.io, September11,2026; isolated headless Chrome152, English, signed out, public synthetic open_ledger/freelancer-invoicing, January2026. Local/fetched main f1965988; exact deployed SHA unverified. Real loaded report data; no API replacement or writes.

1. Fresh390×1000, system dark preference: open /ledger/open_ledger/freelancer-invoicing/cash-flow?time=2026-01&lang=en. Wait for the chart; html has class dark.
2. Generate an A4 headless browser PDF with printBackground:true, exercising the mounted statement print portal. The report body is white, but the surrounding page margins are black. First page rendered with Poppler and visually inspected.
3. Fresh1440×1000 dark context on /ledger/open_ledger/freelancer-invoicing/balance-sheet?time=2026-01&lang=en reproduces the same black page canvas with backgrounds enabled.
4. Render that same Balance Sheet with printBackground:false: margins are white. Independent fresh light-theme controls are white with backgrounds both enabled and disabled.
5. At778×1100 raster size, dark/background-enabled first-page pixels at(5,5) and(5,500) are RGB18,18,18; an interior blank point is255,255,255. All three other mode/background combinations are white at those same points.
6. A further fresh dark390 Cash Flow page receives only a browser-local print CSS diagnostic override: light color-scheme and white backgrounds on html/body, scoped to the mounted statement portal. Background-enabled PDF now has white margins at all sampled points while its screen class remains dark. No repository CSS was edited.

Expected: the explicitly white financial statement prints on a white page regardless of screen theme, including when readers enable background graphics. Actual: dark-theme page canvas surrounds the white statement with a large black border. This is a presentation/ink-use issue, not missing financial data: the Cash Flow Markdown and extracted PDF retain January dates, operating15500, investing-15500, financing16500 and closing16500USD. Native print dialogs and physical printers were not exercised.

Root cause: dashboard/src/features/reports/export/statement-print.css:19–28 sets white background and dark text only on .statement-print-root. It does not reset the document/page canvas. src/style.css:134 retains color-scheme:dark and its dark --background at141; the body background applies at189 even in print media. Actual print computed styles show transparent html/dark color-scheme, dark body background, and white statement root still inheriting dark color-scheme. The isolated diagnostic above proves that a print-only canvas reset resolves the observed border.

Fix: when a statement portal is present, use a white print canvas and light color scheme on the relevant document/body surface, preserving the statement's existing black-on-white colors. Scope this to print media and the existing portal condition. Keep normal dark-screen behavior, light-theme output, margins/pagination, tables and totals, and printing of non-statement pages intact. Do not globally hide non-statement content or change financial formatting. No API/dependency change needed.

Blast radius: StatementExportMenu mounts PrintableStatement for Balance Sheet (balance-sheet-content.tsx:163), Income Statement (income-statement-content.tsx:202) and Cash Flow (cash-flow-content.tsx:199); all share the globally imported statement print CSS. Dark Balance Sheet and Cash Flow were independently reproduced; Income Statement is source-traced, not separately rendered in this check. Blank/error/pending report behavior belongs to its existing owners. Verify rendered PDFs in both themes with backgrounds on/off, and retain non-statement print content; do not substitute a class-string test for actual page rendering.

Dedupe: searched all open/done queues for dark/black PDF margins, print backgrounds and existing export work. Historical022/b41ba310 repaired global app hiding on pages without a print portal; its successful conditional isolation must remain.020 concerns settled print data; m35 pending request identity. w2/done/m22 broadly checks chart dark theme and exports, but does not own a page-canvas reset. No matching open repair exists. Current stylesheet history d162671f then b41ba310 leaves document background untouched; no main-branch fix found.

Verified ignored evidence under dashboard/tmp/qa-20260911-w3-loop/: export-dark-print.json; cash-flow-dark.md/.pdf/.txt; cash-flow-dark-page1.png; print-page-theme-controls.json; print-page-theme-pixels.json; print-page-{dark,light}-{true,false}.pdf/.png; print-white-canvas-control.json/.pdf; print-white-canvas-pixels.json. All probe contexts closed. Initial optional Pillow inspection was unavailable; no dependency was installed, and pixel checks used Poppler PPM output plus Python standard library. No product fix, commit or shipping.
