# Import Select All hides partially selected state

**Minor · 20–30m · dashboard.** A2: accessible import review must communicate partial selection accurately.

Production https://beancount.io, 2026-09-11, isolated headless Chrome152, English/light, authenticated reader, public synthetic open_ledger/freelancer-invoicing. Fresh1440×1000 and390×1000, independently repeated at both widths. Local/fetched main f1965988, deployed SHA unverified.

Open /ledger/open_ledger/freelancer-invoicing/import?lang=en, choose this local CSV and Continue to Configure:

```csv
Date,Payee,Description,Amount
2026-09-10,QA Shop,Expense,-4.50
2026-09-11,QA Refund,Refund,12.75
2026-09-11,QA Zero,Zero,0
```

All three rows start selected; Select all visible rows has aria-checked=true. Uncheck Select transaction2: rows1/3 remain selected, Import2Transactions is correct, but the aggregate checkbox has aria-checked=false and an empty visual box. The accessibility snapshot presents it as unchecked, indistinguishable from no selected visible rows. Expected: mixed/indeterminate for some selected rows, true for all, false for none.

Controls: repeated Space toggles row2 and retains its focus, so049 is repaired. Search Refund shows only deselected row2 and correctly unchecked aggregate; selecting all visible affects that row alone and restores total3. Search Absent disables the aggregate while total3 remains accurate; clearing search restores all three selected rows. Hidden selections and counts are correct. No page errors or write attempts, no Import/AI action. All probe contexts closed.

Root: dashboard/src/features/importer/components/steps/configure/account-mapping-table.tsx:174–180 calculates someVisibleSelected, but the checkbox at226 passes only allVisibleSelected as checked; partial selection only changes opacity. The shared Checkbox forwards checked to Radix. Installed @radix-ui/react-checkbox/dist/index.js:145 already maps checked="indeterminate" to aria-checked="mixed". Pass the existing calculated mixed state through the supported checked contract; ensure the visual indicator distinguishes partial selection rather than resembling disabled/empty. Keep visible-only toggle semantics, stable form identities, keyboard focus and disabled empty results. No selection algorithm or backend change is required.

One production caller: TransactionConfigForm. Cover zero/some/all visible rows plus search-hidden selections using the actual form/checkbox primitive and accessible checked state. Repeat keyboard selection and desktop/narrow browser journey; verify the mixed visual indicator. Actual assistive-technology speech and production import persistence were not exercised.

Dedupe: searched all queues including done for mixed/indeterminate/bulk/SelectAll. Full historical049, closed8ce71e3f, owns row remount/focus; its positive controls now pass and did not specify mixed state. m33 owns draft preservation across Back. Targeted history af5339de→8ce71e3f→135db0a6 retains boolean-only aggregate checked; no newer fix on fetched main.

Verified ignored evidence dashboard/tmp/qa-20260911-w3-loop/: import-selection-{1440,390}.json, import-selection-repeat-{1440,390}.json and import-selection-mixed-{1440,390}.png. Narrow screenshot inspected after account controls finished loading. An initial settled-control locator used an obsolete accessible name; the corrected Source Account locator and fresh repeats passed. No product fix, mutation, commit or shipping.
