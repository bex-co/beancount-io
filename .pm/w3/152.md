# Missing public profiles become internal server errors

**Severity:** minor. **Estimate:** 45–60 minutes, using the existing profile parity harness. Owner: backend-v2; dashboard already handles NOT_FOUND.

Production https://beancount.io, September11,2026; isolated headless Chrome152, English/light, authenticated QA reader, fresh1440×1000 and390×1000. Local/fetched main f1965988; exact deployed SHA unverified. Synthetic missing username qa-no-such-user-20260911; no account created or changed.

1. Open /ledger/qa-no-such-user-20260911?lang=en. Wait for the profile request to finish.
2. Both fresh widths show Error loading profile / Something went wrong on our end. Please try again later.
3. Click Try again. The same result repeats. Each of the four actual GetUserProfile reads returns HTTP200 with extensions.code=INTERNAL_SERVER_ERROR, message Internal server error and no profile.
4. Independently request the anonymous REST /api-gateway/v1/social/profile?username=qa-no-such-user-20260911: HTTP500, error.code INTERNAL_SERVER_ERROR.
5. Working control: anonymous /ledger/open_ledger renders the known public profile; its REST request returns200, profile.username open_ledger and44 repositories. The public profile service is not generally unavailable.
6. Browser-only control: replace the missing-profile GraphQL response with a sanitized NOT_FOUND error. The existing390 dashboard correctly renders User not found and the classified resource-not-found description. This is explicitly a simulated UI control, not the production response.

Expected: a missing public username is represented as NOT_FOUND / REST404 and the existing missing-user screen. Actual: it is represented as an internal service failure with a retry suggestion. Screenshot390 inspected; no page exceptions. All contexts closed and no production writes performed.

Source trace: backend-cluster/backend-v2/src/features/gitea/user-profile/service/user-profile-service.ts:81–98 wraps every users.userGet rejection in a plain Error and also throws a plain Error for an empty user result. The generated client at client/gitea-api.ts:3095–3120 throws the Response object with its HTTP status for non-OK results; wrapping discards that classification. The GraphQL formatter server/graphql/format-error.ts translates DomainError categories but intentionally masks unknown/internal errors. REST server/rest/error-middleware.ts likewise maps unknown errors to500. The frontend user-profile-page.tsx:76–94 already recognizes NOT_FOUND; do not fix this by relabeling every internal error in the dashboard.

Repair contract: translate an actual Gitea user lookup404, and a successful lookup with no user, into the existing NotFoundError at the service boundary. Preserve other failures as distinct failures; do not convert outages, authentication problems or arbitrary thrown errors into user-not-found. Keep internal error masking, anonymous discovery and optional activity/repository fallback behavior intact. Avoid generated-client edits or transport-specific domain exceptions. The exact upstream status for the production missing lookup was not directly observed: anonymous calls to git.beancount.io/api/v1/users/{username} returned401 for both missing and known users. The server-side cause is source-traced; the external probe does not independently prove its upstream404.

Blast radius: GraphQL user-profile-resolver.ts:31–35, anonymous REST public-profile-route.ts:46–59, and the publicUserProfile MCP resource in ai-agent/api/mcp-resources.ts:416–429 all delegate to this service. Keep their failure semantics aligned through the domain error; live MCP was not exercised. Known-profile rendering and anonymous REST200 are positive controls. Loading, Retry and classified missing UI were exercised; actual upstream outage/denial and corrected backend behavior remain unverified.

Validation for implementation: update service tests that currently expect a generic error at service/__tests__/user-profile-service.test.ts:136–155. Use the actual generated-client rejection shape (Response404), empty successful response, successful known user and non404 error controls. Extend the existing public-profile-parity.test.ts harness through real service + GraphQL formatting, HTTP REST and MCP serialization, rather than mocking adapters to return canned errors. Preserve the zero-gap parity contract; run backend-v2 typecheck/test and generate-v1-openapi, reviewing any generated change. No new endpoint, credential or dependency is required.

Dedupe: searched all open/done queues for missing/nonexistent profiles, getUserProfile, userGet and profile404/errors; scanned current milestones. Completedw1/m10 profile parity work adds surfaces, and w2/m15 social authorization preserves anonymous access; neither owns lost missing-user classification. w1/done/006 concerns CLI authenticated-account status, a different service. Current-source history retains the wrapper from the package move ca9a50d2; later f07b3f79 authorization and f7f1dabb follow-on-login changes do not repair it. No duplicate open fix found.

Verified ignored evidence under dashboard/tmp/qa-20260911-w3-loop/: missing-profile-classification-{1440,390}.json/.png; missing-profile-api-control.json; profile-not-found-ui-control.json. Filings contain only the synthetic missing username and public open_ledger control. No product fix, commit or shipping.
