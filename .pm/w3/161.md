# Closing the narrow sidebar loses keyboard focus

Severity: minor. Owner: dashboard. Estimate: 45–60 minutes. A2: keyboard users can dismiss and reopen navigation without losing their position.

Production https://beancount.io, 2026-09-11; isolated headless Chromium152, authenticated QA reader, public synthetic open_ledger/crypto-example, English/light. Fresh390×1000 and600×1000; desktop1440 control. Local/fetched main f1965988, latest observed entry main-RkgtsKH_.js, exact deployed commit unverified.

Repro: open /ledger/open_ledger/crypto-example/errors?lang=en, wait for No Errors Found and hydration. Focus “Toggle Sidebar”, press Enter, wait for the sidebar dialog, then Escape. After800ms, the drawer is gone but document.activeElement is BODY; the original trigger remains connected and focusable. Enter does not reopen it. Both fresh390/600 reproduce; explicitly refocusing the trigger and pressing Enter works. A separate fresh390 repeats the focus loss. Desktop1440 toggle keeps focus on the same BUTTON and uses no dialog.

Expected: Escape dismisses the sidebar and returns focus to the connected opener, so immediate Enter reopens it and Tab continues predictably. A separate keyboard journey successfully opens Journal from the drawer, closes the drawer on navigation and returns via Back; do not break that route-change behavior or force focus onto a removed old-page element.

Root: dashboard/src/common/components/ui/sidebar.tsx:129–131 toggles openMobile from SidebarTrigger's ordinary Button at329–352. The narrow Sidebar branch at254–274 renders a controlled Sheet/SheetContent without SheetTrigger registration or onCloseAutoFocus handling. shared sheet.tsx delegates to @radix-ui/react-dialog; installed1.1.15 dist/index.mjs:146–149 prevents default close autofocus and focuses context.triggerRef.current, which this composition never registers. Thus closing removes the focused dialog content without returning to the still-connected external opener. Desktop does not enter this Sheet branch.

Fix in the shared Sidebar composition: retain the actual opener when opening mobile navigation and restore it on dismissal through the existing common/lib/focus/restore-focus-on-dialog-close.ts helper or valid trigger registration. Respect keyboard-shortcut opening, still-connected targets and a sensible current-page fallback after navigation. Do not change every Sheet's default behavior, add a dependency, or impose focus on a stale trigger. Keep mobile drawer state and desktop width/collapse behavior intact.

Blast radius: production Sidebar consumers are ledger-layout/ledger-sidebar.tsx, ledger-list/pages/dashboard-page/components/dashboard-sidebar.tsx and ledger-layout-loading.tsx; triggers live in ledger layout header, dashboard layout and loading layout. The common keyboard shortcut also calls toggleSidebar. The settled public ledger drawer is live-reproduced; private dashboard, loading transition, shortcut opener and route-removal fallback are source-traced acceptance cases, not claimed tested.

Acceptance: Enter→Escape→Enter on the trigger closes then reopens the narrow drawer with correct focus at390/600; Tab after dismissal continues from the opener. Verify overlay dismissal and keyboard-shortcut opening, current-page navigation and detached-opener fallback; desktop toggle continues to retain focus. Use the real Sidebar/Sheet composition and DOM activeElement assertions in a rendered regression, not a mocked close handler. Run dashboard format/lint/test/build on implementation. Reads, loading/errors and ledger permissions must remain unchanged.

Dedupe: all open/done queues searched for sidebar/drawer focus return. Completedm23 explicitly repairs Journal Export, Budget and Accounts dialog consumers; its listed flows remain separate and the shared helper is available for reuse. m31 owns Entry Context. w1/m3 owns sidebar width/persistence, and w5/m2 dashboard delete-ledger focus is a separate AlertDialog. No item repairs this Sidebar Sheet's missing opener. The composition dates to af5339de and persists after4652596f/4895dc16; refreshed origin main has no later fix.

Evidence under dashboard/tmp/qa-20260911-w3-loop/: sidebar-keyboard-control.json (navigation succeeds, Escape focus fails), sidebar-focus-repeat-390.json, sidebar-focus-repeat-600.json, sidebar-focus-controls.json, sidebar-focus-control-390.png and sidebar-focus-control-1440.png. Narrow screenshot inspected; JSON records exact focus state and connected trigger. All fresh contexts closed, no page errors or production mutations. No product fix or shipping. Native screen-reader speech, other engines and patched behavior remain unverified.

Additional same-primitive control: fresh390 Journal time2016, focus Filters→Enter→Escape→Enter returns focus to Filters and immediately reopens Report filters. filter-sheet-focus-control.json records both true. layout-header.tsx:98–118 registers SheetTrigger, unlike the Sidebar composition. This narrows the repair to Sidebar opener wiring; the shared Sheet primitive works with its registered trigger. Context closed, no filter changed.
