# Import preview hides existing field errors until an unrelated edit

**Minor · 45–60m · dashboard · residual of [completed m19](./done/m19/README.md).** A2: users should immediately see which imported values require repair.

Production https://beancount.io, 2026-09-11, isolated headless Chrome152, English/light, authenticated QA reader, public synthetic open_ledger/freelancer-invoicing. Fresh1440×1000 and390×1000 contexts. Local/fetched main f1965988; deployed SHA unverified.

Open /ledger/open_ledger/freelancer-invoicing/import?lang=en and choose this local CSV:

```csv
Date,Payee,Description,Amount
2026-09-10,QA Coffee,Purchase,-1.25
2025-02-29,QA Grocery,Purchase,-2.50
2026-09-11,QA Supplies,Purchase,abc
```

1. Wait for Preview. It correctly reports **1 Valid / 2 Errors** and warns that invalid rows will be skipped, but neither invalid date nor amount has a field diagnostic or error styling. The table has four rows including its header and no field-error text.
2. Edit only the two invalid rows' payees, appending ` renamed`, and press Enter. The same date and amount now show “Invalid date format. Expected YYYY-MM-DD” and “Amount must be a valid number”, respectively. The table adds two diagnostic rows. Counts remain1Valid/2Errors: row eligibility is correct.
3. Continue to Configure, observe Import1Transaction, then Back. The error messages disappear again while the original invalid values and1Valid/2Errors remain. Both fresh viewport repeats produce the same result without page errors or write attempts.

Expected: explain each rejected field immediately and after remount, without requiring an unrelated edit. Preserve the working invalid-row exclusion and original editable tokens.

Root: dashboard/src/features/importer/components/steps/preview/editable-preview-row.tsx:36 initializes useForm with default values and mode onBlur; errors and row styling come solely from initially empty formState.errors. Whole-row trigger is called only by handleFieldChange at64. Parsed validity already knows these rows are invalid, but initial/remounted rendering does not expose its diagnostics. Targeted history includes a90ee4a6 (whole-row validity repair) and7cc66fed (stable row identities); this remaining initialization path is present locally, not merely an old source snapshot.

Fix the preview's initial/remounted diagnostics using its existing whole-row validation or a coherent structured mapping from parsed diagnostics. Avoid a second inconsistent validator, focus stealing, invented replacement values or a dummy required edit. Scope is the shared editable preview row and its owner; no API change. Preserve stable row IDs, Enter/blur/Escape behavior and accepted-row gating. Verify a valid control stays unmarked, untouched invalid fields are explained before any edit, unrelated edits retain their messages, actual repairs clear only repaired errors, and Configure→Back retains the messages. Include meaningful lifecycle coverage and check large previews remain usable; run dashboard checks on implementation.

Dedupe: all queues including done searched for initial diagnostics and form-remount errors. Completedm19 explicitly requires correct field diagnostics and covers preview initialization/remount in t002/t007; this is a residual follow-up, not another historical finding group or a reopening of its entire numeric-validation milestone. m33 owns Configure draft lifetime,135 multiline text editing, and141 timezone-dependent date classification; none fixes initial field-error visibility.

Verified ignored evidence dashboard/tmp/qa-20260911-w3-loop/: import-initial-diagnostics-{1440,390}.json and import-initial-diagnostics-{1440,390}.png. Initial desktop screenshot inspected. In JSON, counts.valid/errors and remount.valid/errors are locator occurrence counts for the literal labels “1 Valid” and “2 Errors”, not numeric transaction totals. All contexts closed; no Import or AI action, production mutation, product fix, commit or shipping. Actual import persistence and screen-reader announcements remain unverified.
