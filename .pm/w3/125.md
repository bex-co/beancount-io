# Hierarchy chart clicks move accounts off-screen with no reset control

Severity: **minor**. Package: **dashboard**. Estimate: **30–45m**.
A2: a reader inspecting the public Balance Sheet composition should not lose
part of the chart after clicking an account rectangle.

Production https://beancount.io, 2026-09-11, authenticated public reader of
synthetic open_ledger/crypto-example, English, isolated headless Chrome152.
Original390×844 dark context plus fresh1440×1000 and390×844 light repeats.
Local and fetched origin/main f1965988; no newer implicated source fix.
Complete deployed SHA is unverified.

1. Open `/ledger/open_ledger/crypto-example/balance-sheet?time=2024&lang=en`.
   Keep Monthly / At Cost. Choose **Assets Hierarchy** via the desktop tab or
   narrow View selector and let the animation finish.
2. The full-width composition includes Coinbase USD/BTC on the left and
   Checking on the right. Click the **Checking** rectangle. Its tooltip reads
   **42,341.5 USD (38.8%) — Assets:Bank:Checking**.
3. Move the pointer away and press Escape. The chart stays shifted left,
   clipping left-hand accounts and leaving a blank strip on the right.
   At1440 the chart ends at approximately881 of1136px; at390 it ends at275
   of374px. There is no chart reset, back or breadcrumb control.
4. Fresh desktop/narrow contexts repeat after1.2-second animation waits.
   Switching to Equity Hierarchy and back restores the full composition at
   both widths. The report URL/filter and account list remain unchanged.

Expected/fix: keep these composition treemaps stationary when an account is
clicked. The existing non-roaming view has no navigation affordance; set its
ECharts nodeClick behavior to false while preserving hover tooltips, chart
labels, hierarchy data, theme, sizing and report tables. Adding a separate
zoom-navigation feature is outside this small repair. A click must not hide
other accounts or require changing views to recover.

Root: `dashboard/src/features/reports/balance-sheet/hierarchy-tree-map.tsx:203–210`
combines roam:false, nodeClick:zoomToNode and breadcrumb.show:false. Installed
ECharts6.0.0 `lib/chart/treemap/TreemapView.js:455–474,522–529` dispatches
zoom actions on clicks even though roam controls are disabled at353.
`treemapLayout.js:433–469` recenters the target by shifting the root rectangle;
Breadcrumb.js:64 skips the hidden breadcrumb. This is caller configuration,
not incorrect account data.

API controls: GraphQL getLedgerBalanceSheet and REST
`/api-gateway/v1/ledgers/open_ledger/crypto-example/statements/balance-sheet`
with time2024, monthly and at_cost both return200. Full Assets account/balance/
rollup/children projections agree: Assets109046.579925USD and Checking42341.5USD.
The initial report read is SSR-populated; an empty browser response-listener
capture does not imply a missing read. Explicit controls establish the data.
No page errors in fresh repeats. A desktop console404 came from the probe's
initially incorrect REST path, corrected before retaining the successful
control; narrow had no console errors.

Blast radius: one HierarchyTreeMap caller in HierarchyVisualizationCard, used
for Assets/Liabilities/Equity by BalanceSheetContent. Assets independently
reproduces in three contexts; Equity's single Opening rectangle renders
correctly. No separate Liabilities click failure is claimed. Preserve signed
financial values and all three tabs. Use real chart interaction coverage that
checks the rendered viewport after a click, plus tooltip and view-switch
controls. Existing unit tests replace the chart with an option dump and cannot
detect displacement. Run dashboard format/lint/test/build when implementing.

Dedupe: all open/done board and historical treemap/zoom/breadcrumb searches
found no interaction-reset item. Former018 owns shared dark chart colors,
110 locale formatting, and file breadcrumb notes041/046/061 are unrelated.
Targeted history shows the combination introduced in af5339de; aeec496b changes
translated formatting, not interaction. No dependency/backend change is needed.

Verified ignored evidence in `dashboard/tmp/qa-20260911-w3-loop/`:
`hierarchy-click.json`, `hierarchy-recovery.json`, `hierarchy-fresh-before.json`,
`hierarchy-fresh-after-{1440,390}.json`, `hierarchy-fresh-recovery.json`,
`hierarchy-api-controls.json`, and
`hierarchy-fresh-{before,after,recovered}-{1440,390}.png`.
Before/after screenshots were visually checked after animation and pointer
exit. All hierarchy/theme repeat contexts closed. No product fix, production
mutation or shipping. Native touch hardware, other browsers, empty/failed reads
and patched rendering remain unverified. Concurrent native notes and existing
113/index conflicts were preserved.
