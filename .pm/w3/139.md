# A one-month Net Worth chart has no visible data point

**Minor · 30–45m · dashboard.** A2: a new ledger or a short selected period should produce a readable chart even with one observation.

Production https://beancount.io, 2026-09-11, isolated headless Chrome152, English/light, authenticated reader, public synthetic open_ledger/freelancer-invoicing. Fresh1440×1000 and390×1000, plus an earlier independent narrow capture. Local/fetched main f1965988; full deployed SHA unverified.

1. Open /ledger/open_ledger/freelancer-invoicing?time=2026-01&lang=en and wait for Net Worth chart initialization and animation to finish.
2. Its headline says32000USD. Chart view has January and numeric axes but no visible point or trend at32000. The horizontal bottom line is an axis, not a wrongly valued net-worth series.
3. Choose Show table: Jan2026 correctly lists32000USD.
4. Open the same Overview with time2026: its12monthly observations produce a drawable multi-point line. The problem is the one-observation chart, not missing data or incorrect financial arithmetic.

Both fresh widths agree, with no page errors/writes. Live ECharts options contain xAxis[2026-01-31], one line series data[32000], symbol=none. Its rendered ec-polyline and ec-polygon each have exactly one coordinate pair, so neither draws a trend/area. GraphQL GetLedgerOverview and REST /api-gateway/v1/ledgers/open_ledger/freelancer-invoicing/overview?time=2026-01&interval=monthly&conversion=at_cost both return200 and exactly [{date:2026-01-31,balance:{USD:"32000"}}]. Headline, table and chart input agree.

A temporary browser-only diagnostic changes only that live chart's symbol to circle/size8. The same32000 point becomes visible at the correct height, with unchanged dates/data. Before/after cropped screenshots were inspected. This is not a source patch or a claim of persisted repair; switching views/navigation and closing contexts discarded it.

Root: dashboard/src/features/reports/overview/components/net-worth-card.tsx:121 unconditionally disables every point symbol. The shared ReactECharts empty check correctly regards [32000] as data; it should not show a no-data state. Installed ECharts helper/SymbolDraw.js:53 deliberately excludes symbol=none, while a one-vertex line has no connecting segment. One production NetWorthCard caller exists in reports/overview/index.tsx:162. This is the card's option choice, not an upstream rendering defect.

Fix within NetWorthCard: retain a visible marker when a displayed series has exactly one finite observation, including a single non-null value among missing months; keep the normal multi-point line styling. Preserve zero/negative values, currency selection, headline/table amounts, labels and tooltip behavior. Do not add invented points, duplicate dates, fake a trend, change accounting values, or globally enable markers on every chart.

Acceptance: the exact January view visibly plots32000 at January and the table remains correct; zero/negative single values are also visible. Empty data retains the existing honest empty state. Multi-point and null-gap controls preserve their data and line behavior. Verify an actual renderer/browser result as well as meaningful card data cases, and run dashboard checks when implementing. Other chart consumers and actual assistive-technology speech remain unverified; no shared global repair is proposed.

Dedupe: all .pm queues including done searched for single-point/single-month charts, symbols and net worth. Existing129 owns the separate pending-filter phase; this case occurs after a settled successful response. 138 owns zero-total percentages, m32 table semantics, and110 locale hydration. w2/m28 proposes compact report API shapes, not chart rendering. No overlapping open fix found. Targeted NetWorthCard history still traces toaf5339de with no later repair on fetched main.

Verified ignored evidence dashboard/tmp/qa-20260911-w3-loop/: networth-single-repeat-{1440,390}.json, networth-single-before-{1440,390}.png and networth-single-diagnostic-{1440,390}.png. An initial locator used Table view instead of the actual Show table label; corrected fresh repeats completed. All contexts closed and temporary chart overrides discarded. No product fix, production mutation, commit or shipping.
