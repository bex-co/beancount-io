---
id: w3/m2/t009
title: Closeout
worker: worker3
status: todo
estimate: 20m
depends_on: [w3/m2/t008]
---

## Objective

Close m2 once the full lifecycle — add, update, delete — verifiably works from the app against a real ledger.

## Status (2026-08-09)

**Blocked on in-app verification.** t001–t008 are done and the gate is green, but no write has been exercised against a real ledger. To finish: add a budget and confirm the directive lands in the ledger file (check via the web journal or the in-app file editor); update from a card and confirm a second dated entry appears in the history and the card's current amount changes; delete a history entry and confirm it leaves the file. Also exercise the failure paths — a save rejected by the server must show its message rather than closing silently, and the delete must surface a visible error when the optimistic-lock `sha256sum` is absent (the bug this milestone deliberately fixed rather than ported).

## Unblocked (2026-08-27)

This milestone's central flow was broken, not merely unverified: adding a budget
through `bulkEntries` failed for every client because the ledger service never
implemented the `custom` entry type the contract defines for budgets. Fixed and
verified end to end — see `w3/done/002.md`.

Still to do here: the in-app round trip. Add a budget from mobile and confirm the
directive lands in the ledger file; update from a card and confirm a second dated
entry supersedes it; delete one and confirm it leaves the file. Then the error
paths — a rejected save must show its message rather than closing silently, and
delete must surface a visible error when the optimistic-lock `sha256sum` is
absent.

## Context

- DoD: see `w3/m2/README.md`; verify the ledger file actually reflects each operation (web journal or file view), not just the mobile UI.
- Run `/pm done w3/m2/t009` last — completing it triggers the milestone move to `w3/done/m2/`.

## Steps

1. Verify DoD end-to-end (add → appears; update → new history entry supersedes; delete → directive gone; error paths visible).
2. Close out tasks, milestone README, and workstream checkbox per board conventions.

## Files

- `.pm/w3/m2/**`, `.pm/w3/README.md`

## Acceptance criteria

- [ ] DoD verified against the ledger file; board state consistent in all three places.

## Out of scope

- m3 work.
