---
id: w3/m2/t001
title: build-budget-entry.ts + unit tests
worker: worker3
status: done
estimate: 30m
depends_on: []
---

## Objective

Pure builder mapping form values to the `bulkEntries` payload for a budget directive, mirroring the open-account entry-builder pattern, with jest-lite tests.

## Context

- Pattern: `mobile/src/screens/open-account-screen/open-account-entry.ts` (`buildOpenAccountEntry`).
- Payload shape (dashboard parity, `add-budget-dialog.tsx`): `{ type: "BUDGET", budget: { date: "yyyy-MM-dd", account, interval, amount: { number, currency } } }` — `number` stays a free string; negative = income target.
- `BudgetInterval` enum values: `DAILY WEEKLY MONTHLY QUARTERLY YEARLY`.

## Steps

1. Create `build-budget-entry.ts` in `src/screens/budget-screen/` (or an `add-budget-screen/` sibling — match t002's placement).
2. Tests: date formatting, interval pass-through, negative amounts preserved, currency required.

## Files

- `mobile/src/screens/budget-screen/build-budget-entry.ts` + `__tests__/build-budget-entry.test.ts` (new)

## Acceptance criteria

- [ ] Builder output matches the dashboard's `AddEntryInput` shape exactly; tests cover negative and decimal-string amounts.
- [ ] `yarn test:unit` green.

## Out of scope

- The form screen (t002).
