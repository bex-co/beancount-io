---
id: w3/m2/t008
title: Test coverage
worker: worker3
status: done
estimate: 45m
depends_on: [w3/m2/t006]
---

## Objective

Meaningful tests for m2's behavior beyond t001's builder tests.

## Context

- jest-lite: pure logic only; candidates — update-mode param → form-prefill mapping, history display ordering/current-entry selection (reversal + ≤ today rule), delete precondition logic (missing `sha256sum` → error state, not mutation call).
- Extract logic into pure functions where needed to make it testable — that's a design improvement, not test gaming.

## Steps

1. Tests asserting the flows' decision logic and failure modes.
2. `yarn test:unit` green.

## Files

- `mobile/src/screens/add-budget-screen/__tests__/`
- `mobile/src/screens/budget-screen/**/__tests__/` (additions)

## Acceptance criteria

- [ ] Prefill, ordering, and delete-precondition behavior covered with failure cases; no tautological tests.
- [ ] `yarn test` passes.

## Out of scope

- Simulator E2E.
