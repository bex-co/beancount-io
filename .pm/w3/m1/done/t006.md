---
id: w3/m1/t006
title: Budget strings (en) + analytics events
worker: worker3
status: done
estimate: 20m
depends_on: []
---

## Objective

Land the English strings and analytics event constants the m1 surfaces need, so UI tasks (t007–t009) consume final copy instead of placeholders.

## Context

- Copy source: `dashboard/src/features/ledger-data/budget/locales/en.ts` (`page.budget.*` namespace, ~40 keys) — mobile uses **flat keys** in `mobile/src/translations/en.ts`; `budget` and `budgetEntries` already exist there.
- Semantics to keep: "Actual" (not "spending"), "Above/Below/On target" (not "over budget").
- Analytics: `mobile/src/common/analytics` + `usePageView` convention (`page_view_<name>`).

## Steps

1. Add flat en keys for: page title, panel title, stats labels, variance badges, interval names, time-span labels, empty states, CTA, chart legend, history.
2. Define event names: `budget_panel_see_all`, `page_view_budget` (via `usePageView("budget")` in t008), plus placeholders reserved for m2 (`budget_add_submitted`, `budget_deleted`).
3. Only `en.ts` in this milestone — the 13 other locales are w3/m3/t001.

## Files

- `mobile/src/translations/en.ts`

## Acceptance criteria

- [ ] All m1 UI copy exists as flat en keys; no hard-coded UI strings in t007–t009.
- [ ] Translation tests (`src/translations/__tests__/`) still pass with en-only additions.

## Out of scope

- Non-English locales (w3/m3/t001).
