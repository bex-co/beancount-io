---
id: w3/m1/t010
title: Adoption surface
worker: worker3
status: done
estimate: 30m
depends_on: [w3/m1/t009]
---

## Objective

Verify the shipped budget read surfaces are discoverable and documented everywhere they're exposed, for humans and coding agents alike.

## Context

- Surfaces: `mobile/README.md` (if it enumerates features), root `README.md` + `CLAUDE.md` package tables (mobile row description), `mobile/CLAUDE.md` (screen/feature conventions if it lists screens).
- Compatibility rules in root `CLAUDE.md`: every `AGENTS.md` must resolve to its sibling `CLAUDE.md`; `.agents/skills` → `skills/.claude/skills/`.

## Steps

1. Sweep the surfaces above for places the budget feature should be mentioned; update or file follow-ups — don't silently diverge.
2. Verify symlinks still resolve (`AGENTS.md` → `CLAUDE.md` at touched scopes).
3. Confirm any documented steps (e.g. codegen) work exactly as written after t004's changes.

## Files

- `mobile/README.md`, `mobile/CLAUDE.md`, root `README.md`/`CLAUDE.md` (as needed)

## Acceptance criteria

- [ ] Budget feature discoverable where the repo documents mobile features; no stale instructions introduced by m1.
- [ ] Symlink checks pass.

## Out of scope

- Marketing/docs-site content.
