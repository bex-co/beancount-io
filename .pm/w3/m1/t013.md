---
id: w3/m1/t013
title: Closeout
worker: worker3
status: todo
estimate: 20m
depends_on: [w3/m1/t012]
---

## Objective

Close m1 once its definition of done actually holds: read-only budget surfaces live on Home and /budget, gates green.

## Status (2026-08-09)

**Blocked on in-app verification.** t001–t012 are done and `yarn test` (lint + typecheck + 1106 unit tests) is green, but the DoD is deliberately "verified in-app, not merely code-complete" and nothing here has been run against a live ledger yet. To finish: launch the app signed in to a ledger that has `custom "budget"` directives, then confirm — Home panel ranks by utilization and turns over-budget rows red; "See all" reaches /budget; time-span pills re-scope every card's chart; a ledger with no budgets shows the CTA row and the page's empty state; skeletons (not spinners) on first load; and all of it in **both light and dark** themes (`mobile/CLAUDE.md` calls that out — missing background colors on loading states caused dark-mode flicker before).

## Verification progress (2026-08-27)

Ran against the `deploy/docker-mac` stack, not production. **Verified:** the
`/budget` empty state ("No budgets found" + "Add your first budget" CTA), and
that Home's cards still render their "see all" affordance after the analytics
retirement swapped `HomeDashboardCard` for the shared `DashboardCard`
(`w3/done/001.md`). **Still unverified:** every populated state — utilization
ranking, over-budget colouring, pills re-scoping, skeletons, dark theme.

Budget directives could not be created through the API at all when this was
first attempted; that bug is now fixed and verified (`w3/done/002.md`), so a
fixture ledger can be built the normal way. What remains is signing the app in
to a local account — the sign-in tap opens an OAuth browser flow that did not
complete against the local origin.

Useful tooling notes for whoever picks this up: `expo-mcp` has no scroll tool,
so Home's Budget panel is only reachable by deep link, not by scrolling; and
text entry, which `mobile/CLAUDE.md` says is unavailable, does in fact work via
`osascript -e 'tell application "System Events" to keystroke "..."'` after
activating the Simulator.

## Context

- DoD: see `w3/m1/README.md` — verify against a real ledger with budget directives (empty-state check against one without).
- Completing this task is the move: run `/pm done w3/m1/t013` last to trigger the milestone move to `w3/done/m1/`.

## Steps

1. Verify the DoD end-to-end in the app (panel, page, pills, empty states, skeletons).
2. Set remaining task statuses `done`, move task files to `done/`, mark table rows `— **DONE**`, set `**Status:** done`.
3. Move `w3/m1/` → `w3/done/m1/`; check `- [x] m1` in `w3/README.md`.

## Files

- `.pm/w3/m1/**`, `.pm/w3/README.md`

## Acceptance criteria

- [ ] DoD verified in-app, not just code-complete; board state consistent in all three places.

## Out of scope

- Any m2 work.

## Additional native verification (2026-09-09)

Read-only QA on iPhone 17e/iOS 26.5, rebuilt development client 1.20260906.47 at main `7f4b6190`, production public `open_ledger/example` (push=false). Scrolled the actual Home view and tapped Budget See all: the empty budget page loads and one Back returns to the lower Home content. Home's empty spending-target row also opens Budget. Verified settled Home card and Budget empty page in light and dark appearance. API control returned zero budget directives. Evidence under `mobile/tmp/qa-2026-09-09/`: `home-lower-content.jpg`, `budget-from-home.jpg`, `home-budget-dark.jpg`, `budget-from-home-dark.jpg`, `budget-home-empty-row-dark.jpg`, `budget-directives.json`. System theme, English, and simulator light appearance restored.

Tooling update: native scrolling can use Simulator accessibility AXScrollDownByPage. A bounded recursive search for the first AXScrollArea (depth limit 40) reached Home's scroll view; enumerating the entire Home accessibility tree was too slow. This is real native navigation, not a deep-link substitute.

This extends empty/read-only coverage only. Populated utilization ranking, over-budget rows, chart periods and loading behavior still require a suitable authorized fixture. Do not close this milestone from this evidence. No production ledger mutation or product fix was made.


## Additional populated native verification (2026-09-11)

Production public synthetic open_ledger/budgeting-envelopes (push:false), iPhone17e/iOS26.5 native1.20260906.47, mainf1965988/mobile7538fb0d. Native scrolling now works through a scratch XCTest helper using the installed Xcode/ExpoMCP runner; screenshots remain ExpoMCP. Actual Home Budget panel shows Dining0/200USD, Groceries0/550USD, Insurance:Car0/100USD. Its See all opens Budget and one Back preserves lower Home. All seven cards load through scrolling. ALL shows Insurance:Car March600 against budget100 with red bar/meter, Above target and +500 variance, matching GetLedgerIntervalTotals. At bottom, LY updates Utilities to0/0 before its2026 budget start. Populated Home and insurance over-budget card are readable in dark; Light restored. Evidence mobile/tmp/qa-2026-09-11/home-populated-budget-panel.jpg, budget-populated-from-home.jpg, budget-all-scroll-{1,2,3,4}.jpg, budget-insurance-all-api.json, budget-bottom-ly.jpg, budget-dark-scrolled-cards.jpg and home-populated-budget-dark-return.jpg.

Keep closeout open: current September fixture utilization is zero for every Home row, so unequal-utilization ranking and over-budget Home coloring remain unverified, as do first-load skeletons in both appearances. New independently reproduced accessibility and permission gaps are w3/123 (Home amounts absent from native accessibility) and w3/121 (readonly history offers Delete); no production deletion was submitted. No product tests rerun by this report pass.
