---
id: w3/m1/t013
title: Closeout
worker: worker3
status: todo
estimate: 20m
depends_on: [w3/m1/t012]
---

## Objective

Close m1 once its definition of done actually holds: read-only budget surfaces live on Home and /budget, gates green.

## Status (2026-08-09)

**Blocked on in-app verification.** t001–t012 are done and `yarn test` (lint + typecheck + 1106 unit tests) is green, but the DoD is deliberately "verified in-app, not merely code-complete" and nothing here has been run against a live ledger yet. To finish: launch the app signed in to a ledger that has `custom "budget"` directives, then confirm — Home panel ranks by utilization and turns over-budget rows red; "See all" reaches /budget; time-span pills re-scope every card's chart; a ledger with no budgets shows the CTA row and the page's empty state; skeletons (not spinners) on first load; and all of it in **both light and dark** themes (`mobile/CLAUDE.md` calls that out — missing background colors on loading states caused dark-mode flicker before).

## Context

- DoD: see `w3/m1/README.md` — verify against a real ledger with budget directives (empty-state check against one without).
- Completing this task is the move: run `/pm done w3/m1/t013` last to trigger the milestone move to `w3/done/m1/`.

## Steps

1. Verify the DoD end-to-end in the app (panel, page, pills, empty states, skeletons).
2. Set remaining task statuses `done`, move task files to `done/`, mark table rows `— **DONE**`, set `**Status:** done`.
3. Move `w3/m1/` → `w3/done/m1/`; check `- [x] m1` in `w3/README.md`.

## Files

- `.pm/w3/m1/**`, `.pm/w3/README.md`

## Acceptance criteria

- [ ] DoD verified in-app, not just code-complete; board state consistent in all three places.

## Out of scope

- Any m2 work.
