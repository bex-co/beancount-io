# Trial Balance hides its conversion selector below desktop width

**Minor · 20–30m · dashboard.** A2: narrow-screen readers need the same report-unit selection offered on desktop.

Production https://beancount.io, 2026-09-11, isolated headless Chrome152, English/light, authenticated QA reader of public open_ledger/example. Fresh390×900 and768×900, each with1440×900 controls. Local/fetched main f1965988; deployed SHA unverified.

Open /ledger/open_ledger/example/trial-balance?lang=en&time=2016 with charts shown. On both narrow widths, View → Assets is available but there is no visible conversion selector or alternative conversion action. The existing At Cost combobox is in the DOM under a parent with display:none; it is excluded from accessible role queries.

Widen the same context to1440: At Cost appears, offering At Cost, At Market Value, Units and Converted to USD. Select Units; the real GetLedgerTrialBalance read with conversion=units returns200 without errors. Resize back to the original390/768 width: the selected Units combobox disappears again, so the narrow UI cannot show or change this selection. Both independent width journeys reproduce; the390 screenshot was inspected. This is control availability, not a claim that any conversion value is miscomputed.

Root: dashboard/src/features/reports/trial-balance/trial-balance-content.tsx:189 wraps its only ConversionSelect in `items-center gap-2 hidden lg:flex`. There is no narrow alternative; the responsive View component beside it selects account categories, not conversion. constants.ts defaults conversion to at_cost, and the page's existing state/query callback works when the desktop selector is exposed. Targeted git log -S traces this class toaf5339de, with no newer repair on fetched main.

Expose the existing selector at narrow widths and arrange it with View so both fit without clipping. Reuse the existing value/callback/options and preserve selected conversion across resizing; do not add a separate unsynchronized mobile state or change the default/API contract. Keep chart-collapse behavior and coordinate126 (tab state) and127 (collapsed-control focus) without replacing their fixes.

Acceptance:390 and768 show the current conversion and permit selecting Units and At Cost with successful existing queries;1440 remains unchanged, resizing preserves the chosen value, and all narrow triggers fit. Verify pointer and keyboard access with meaningful browser checks and run dashboard checks on implementation. Actual conversion arithmetic, other locales and patched behavior remain unverified in this control-availability finding.

Dedupe: all queues including done searched for Trial Balance conversion and narrow/hidden selectors. Completedm20 concerns global date/account/payee filters. 126 fixes the narrow View selector's stale content, and127 excludes collapsed chart controls from focus; neither changes this unconditional below-lg display:none branch. The clipping found in other report toolbars is a separate layout cause.

Verified ignored evidence dashboard/tmp/qa-20260911-w3-loop/: trial-conversion-narrow-{390,768}.json/.png. Each records absent narrow controls, available desktop options, the real200 Units read, and disappearance after narrowing. All contexts closed with no page errors or additional write attempts. No product fix, production mutation, commit or shipping.
