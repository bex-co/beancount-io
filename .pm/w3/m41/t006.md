---
id: w3/m41/t006
title: Close and archive the delete-confirmation milestone
worker: worker3
status: todo
estimate: 10m
depends_on: [w3/m41/t004, w3/m41/t005]
---

## Objective

Close the milestone once its definition of done actually holds.

## Context

Run this last. Completing it *is* the archive move — `/pm done w3/m41/t006` triggers it.

Verify in a browser against a **disposable** entry the implementer created for the purpose, prefixed `qa-<yyyymmdd>-`, on a ledger they own — never against pre-existing data. Record the entry and confirm it is gone afterwards.

## Steps

1. Verify every DoD bullet, including a real confirmed deletion of a disposable entry and a real cancel that deletes nothing.
2. Check keyboard operation and focus return at 1440 and 390.
3. Confirm `yarn format:check && yarn lint && yarn test && yarn build` are green in `dashboard/`.
4. Set every remaining task `status: done`, move each `tNNN.md` to `w3/m41/done/`, mark each row `— **DONE**` and `**Status:** done` in the milestone README, move `w3/m41/` to `w3/done/m41/`, and check `- [x] **m41**` in `.pm/w3/README.md`.

## Files

- `.pm/w3/m41/**`, `.pm/w3/README.md`

## Acceptance criteria

- [ ] Every DoD bullet verified against the running product, using only a disposable entry.
- [ ] The milestone exists only at `.pm/w3/done/m41/`, its README says `**Status:** done`, and the workstream checkbox is checked.

## Out of scope

Deleting any pre-existing entry, and closing while any DoD bullet is unmet.
