---
id: w3/m41/t003
title: Verify the delete-confirmation adoption surface
worker: worker3
status: todo
estimate: 15m
depends_on: [w3/m41/t002]
---

## Objective

Confirm the guard is real on all three surfaces that open this dialog, and that nothing in the repo documents a one-click delete.

## Context

- Callers: `features/journal/pages/journal-page.tsx`, `features/reports/account/index.tsx`, `features/reports/overview/components/recent-activity-card.tsx`.
- The delete mutation is also reachable through REST and MCP. This milestone adds a **client-side** confirmation only; an API-level change would fall under the repo's REST/GraphQL/MCP parity rule and is deliberately not in scope. Record that explicitly so the parity gate is not misread.
- Check `dashboard/CLAUDE.md` and any skill or doc that describes deleting entries from the dashboard.

## Steps

1. Open the dialog from each of the three callers and confirm the guard appears, at 1440 and 390.
2. Confirm a read-only context still shows no Delete.
3. Grep the repo, including `skills/`, for text describing entry deletion; flag drift as follow-up rather than editing unrelated packages.

## Files

- `dashboard/CLAUDE.md` (only if it describes this behavior)

## Acceptance criteria

- [ ] All three callers show the confirmation.
- [ ] Read-only contexts are unchanged.
- [ ] A note records that no REST/GraphQL/MCP surface changed and why.
- [ ] No doc or skill still implies a one-click delete.

## Out of scope

Adding confirmations to the REST or MCP surfaces, and editing other packages.
