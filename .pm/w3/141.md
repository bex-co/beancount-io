# Import rejects a valid calendar date in a different browser timezone

**Minor · 45–60m · dashboard.** A2: imported ledger dates should retain their Gregorian day independently of the reader's current timezone. This is a reproduced historical-date edge case, not a claim about ordinary contemporary dates failing.

Production https://beancount.io, 2026-09-11, isolated headless Chrome152 with explicit browser timezone emulation, English/light, authenticated QA reader, public synthetic open_ledger/freelancer-invoicing. Narrow390×1000 America/Los_Angeles and Pacific/Apia; fresh1440×1000 UTC and Pacific/Apia. Local/fetched main f1965988; deployed SHA unverified. No machine timezone changed.

Open /ledger/open_ledger/freelancer-invoicing/import?lang=en and select exactly the same local CSV in each context:

```csv
Date,Payee,Description,Amount
2011-12-29,QA Day29,Calendar control,-4.50
2011-12-30,QA Day30,Calendar control,-4.50
2011-12-31,QA Day31,Calendar control,-4.50
2026-09-10,QA Current,Calendar control,-4.50
```

Los Angeles and UTC show4Valid; Configure includes QA Day30 on12/30/2011. Both fresh Apia repeats show3Valid/1Error and Configure omits QA Day30 while retaining the other three rows. The same input bytes and date values are used. The preview warns that errored rows will be skipped; no Import or AI Fill action was invoked, so no actual booking loss is claimed. All contexts close without page errors or write attempts; the desktop rejection screenshot was inspected.

Independent controls: Python datetime.date(2011,12,30) accepts that Gregorian date. GraphQL and REST Journal reads with time2011-12-30 both return200 with zero matching records and no date error; this is a date-filter control, not an import-persistence test. In the actual browsers, new Date(2011,11,30) yields local day30 in LA/UTC but day31 in Apia. Contemporary DST-boundary/NewYear/leap-day preview→Configure controls pass in LA and Pacific/Kiritimati, and an impossible2025-02-29 remains excluded after a payee edit.

Root: dashboard/src/features/importer/utils/csv-validator.ts:33 constructs local-midnight Date and validates by round-tripping local Y/M/D. A valid date-only ledger string becomes invalid when that local civil date cannot be represented in the browser zone. This helper drives buildParsedRow at109, row-edit-schema.ts:20 and transaction-config-form.tsx:53/118. Configure displays Date using local toLocaleDateString at account-mapping-table.tsx:313; use-import-submit.ts:55 serializes through formatImportDate, whose local getters are at52–56.

Fix the importer's date-only representation coherently: validate Gregorian Y/M/D without constructing a local civil instant; use a UTC carrier consistently if retaining Date objects, with explicit UTC display and UTC calendar serialization, or keep canonical date strings throughout this internal form. Preserve exact source YYYY-MM-DD through Preview, Configure and prepared submission. Updating only validation is insufficient because local display/formatting can still change the day. Do not roll the date to31, discard the row, globally change the browser timezone or revert the earlier west/east-of-UTC protections. No new dependency or backend date-policy change is required.

Acceptance: all four identical rows remain valid and retain their dates in LA, UTC, Apia and Kiritimati contexts; ordinary DST boundaries and invalid leap dates retain the passing behavior. Exercise the parser→form→prepared date string through real timezone behavior, including existing submission-hook tests, without booking production transactions. Run dashboard checks on implementation.

Dedupe: full completedm19 owns invalid-date/amount error propagation and accepted-row validation; its observed non-leap-year repair still works. 6304627a fixed ordinary importer calendar-day shifts using local dates but leaves this historical timezone edge. Completedm24 owns the shared date-picker's ISO/slash input path, not this CSV helper; no global date-input migration is proposed here. All queues including done searched for Apia/timezone/Gregorian/calendar-day cases and targeted helper history checked. No overlapping open repair found. Coordinate m19 and m33 without duplicating their numeric validation or draft-lifetime work.

Verified ignored evidence dashboard/tmp/qa-20260911-w3-loop/: import-zone-America-Los_Angeles.json, import-zone-Pacific-Apia.json, import-zone-repeat-UTC.json, import-zone-repeat-Pacific-Apia.json, import-zone-repeat-Pacific-Apia-1440.png, calendar-api-control.json, import-calendar-controls.json and import-calendar-kiritimati.json. Other historic timezone transitions and actual import persistence remain unverified. No product fix, production mutation, commit or shipping.
