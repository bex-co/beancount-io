---
id: w3/m38/t005
title: "Test that every ledger route emits head metadata"
worker: worker3
status: todo
estimate: 40m
depends_on: [w3/m38/t003]
---

## Objective

Add the regression that makes this class of gap impossible to reintroduce: a test that enumerates the ledger routes from the generated route tree and asserts each one declares head metadata, plus focused tests for the two repaired routes' actual title and description output.

## Objective boundary

The point is coverage of the *route set*, not a snapshot of strings. A new twenty-second ledger route with no `head:` must fail this test.

## Context

- Route definitions live in `dashboard/src/routes/`; the generated route tree is produced by `yarn typecheck` / route generation.
- Existing SEO helper tests and `src/test/translations.test.ts` are the neighbours to extend rather than duplicate.
- The two repaired routes are Accounts and Budget; `/holdings` is the working control to assert against.

## Steps

1. Write the route-set assertion over the ledger route files or the generated tree.
2. Add per-route assertions for the Accounts and Budget title and description, including the `{{ledgerName}}` interpolation.
3. Confirm the test fails when `head:` is removed from either route.

## Files

- `dashboard/src/routes/__tests__/` (route metadata coverage)
- tests adjacent to `src/common/lib/seo/`

## Acceptance criteria

- [ ] Deleting `head:` from either repaired route makes the test fail.
- [ ] Adding a new ledger route without head metadata makes the test fail.
- [ ] The Accounts and Budget titles and descriptions are asserted literally, with the ledger name interpolated.
- [ ] `yarn test` passes in `dashboard/`.

## Out of scope

Snapshotting every route's metadata strings, testing crawler behavior, and adding coverage for unrelated routes' content.
