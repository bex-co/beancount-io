---
id: w3/m38/t004
title: "Simplify the route head wiring"
worker: worker3
status: todo
estimate: 10m
depends_on: [w3/m38/t003]
---

## Objective

Run `/simplify` over the two route files and the locale additions, for reuse and consistency, with zero behavior change.

## Context

- Twenty-one route files now repeat the same `head:` closure. Only extract a shared helper if it removes real duplication without hiding the per-route keys — a helper with one caller, or one that makes the SEO keys harder to find, is worse than the repetition.
- Keep route files thin per `dashboard/CLAUDE.md`.

## Steps

1. Run `/simplify` scoped to the changed files.
2. Confirm the t002 and t003 acceptance outputs are unchanged.

## Files

- The files touched by t001, t002 and t003.

## Acceptance criteria

- [ ] Titles, descriptions, `og:title`, canonicals and robots values are identical before and after.
- [ ] `yarn format:check && yarn lint && yarn test && yarn build` pass in `dashboard/`.

## Out of scope

Refactoring the other nineteen routes' head configuration, and any new abstraction with a single caller.
