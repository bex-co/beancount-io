# Accept supported digit-led and Unicode names in native Open Account

**Severity:** minor. **Estimate:** 45–60 minutes. Owner: mobile. A2/A3: users can create accounts with valid Beancount names, including numbered retirement accounts and accented names, without renaming them to satisfy a stricter mobile-only grammar.

September 11, 2026, HEAD f1965988 / mobile 7538fb0d, io.beancount.ios 1.20260906.47, sole iPhone 17e / iOS 26.5, Expo MCP 8083, English/System with light appearance/default large text. Authenticated public open_ledger/minimax on production. All actions were local unsaved drafts; no production account was created.

1. Actual Accounts tab -> plus (open-account-button). Open Account starts with Assets prefix, empty name, MUSD and today's date. Done is correctly disabled for an empty name.
2. Enter 401k in open-account-name-input. The resulting name is Assets:401k. The screen shows “Each account component must start with an uppercase letter.” and keeps Done disabled.
3. Back discards the draft. Terminate/relaunch (initial PID71776, fresh PID72979), verify settled Home, use the same actual Accounts -> plus path, enter401k: identical rejection.
4. Working native control in a separate blank draft: Retirement401k has no validation error and enables Done. Back discards it.
5. Another blank native draft with Épargne produces Assets:Épargne, but the same uppercase-letter error and disabled Done. Back discards it. Never press header Done or the keyboard return/checkmark: the latter is wired to submission too.

Expected: names accepted by the supported Beancount account grammar pass local validation. Actual: native validation rejects both a digit-led subaccount and an accented uppercase initial.

Independent controls: installed Beancount3.2.3 loader.load_string and the repository's installed @rustledger/wasm0.24.0 through withLedger both accept isolated `2026-09-11 open <name> MUSD` for Assets:401k, Assets:Épargne and Assets:Retirement401k with zero errors. Both reject Assets:bank and Assets:Bank_Checking. These are offline synthetic parses, not submitted API requests or persisted server accounts. The official [getting-started naming rules](https://beancount.github.io/docs/getting_started_with_beancount/) allow a capital letter or number at a component's start; upstream [account validation source](https://github.com/beancount/beancount/blob/master/beancount/core/account.py) distinguishes root initials from Unicode-uppercase/digit subcomponent initials.

Root: mobile/src/screens/open-account-screen/account-name.ts:77–82 requires /^[A-Z]/ for every component and /^[A-Za-z0-9-]+$/ for the remainder. It disallows leading digits and all accented letters. OpenAccountScreenComponent at229–231 validates the composed name and derives canSubmit; header at289–296 disables Done, and its submit handler returns before useOpenAccount runs. English translation en.ts:274–277 repeats the overly strict rule; the same key is overridden by the other12 locales. Existing open-account-name.test.ts:40–46 covers digits only after an ASCII uppercase initial.

Repair the mobile validator to follow the supported engine's root/subcomponent grammar, including valid Unicode letters and digit-led subcomponents, while retaining the selected root whitelist and rejection of lowercase-led ASCII names, underscores, spaces and invalid shapes. Do not simply allow arbitrary Unicode or remove validation. Keep compose/split behavior and account text intact; do not force capitalization or rename the user input. Update the affected validation message in every locale so it accurately describes permitted initials. Preserve permission guards, date/currency defaults, callback cleanup and both submission paths. No new dependency or backend API change is required for these evidenced cases.

Acceptance: fresh iPhone17e Open Account accepts401k and Épargne without error and enables Done for a valid otherwise-complete draft; Retirement401k remains valid, invalid controls remain rejected. Add validator and rendered enabled/error-state regressions tied to the accepted/rejected engine corpus, including accented letters after an ASCII initial. Verify cancel/reopen resets the draft and the Account Picker prefill caller preserves these names. A mocked transport test may verify unchanged name serialization; no production-write authorization is implied. Run mobile checks. Custom root names, every Unicode normalization/category edge case, Android, physical keyboards/locales and successful server persistence remain unverified.

Blast radius/dedupe/history: validateAccountName has one production consumer, OpenAccountScreenComponent. It is reached by Accounts plus and the account picker's no-match Create action via pushOpenAccount; only Accounts plus was used for this finding. Account picker read-only gating is separately w3/114. Searched all .pm open/done records for validator, account naming, uppercase, Unicode and401k and scanned open milestone summaries; no matching native validation work found. Completed w3/m23 is dashboard dialog focus, not this parser; its validation-preserving scope does not own native grammar. git log -S identifies6dda6746 introducing this validator; d8994cc9 later adds prefill splitting without repairing it. No current-main fix found.

This local synchronous validation is independent of metadata loading/network/expired auth; all captures were settled with currency loaded. Bounded JS error capture empty. Verified ignored evidence in mobile/tmp/qa-2026-09-11/: open-account-entrypoint.jpg, open-account-initial.jpg, open-account-digits-initial.jpg, open-account-fresh-home.jpg, open-account-digits-repeat.jpg, open-account-valid-control.jpg, open-account-unicode.jpg, open-account-digits-type.log, open-account-digits-repeat-type.log, open-account-valid-type.log, open-account-unicode-type.log, open-account-upstream-control.json, open-account-wasm-control.json, open-account-name-js-errors.json. No product fix, commit or shipping. All drafts discarded; original preferences retained.
