---
id: w3/m42/t003
title: Verify the date-entry adoption surface
worker: worker3
status: todo
estimate: 15m
depends_on: [w3/m42/t002]
---

## Objective

Confirm the new behavior holds at every date input in the app, and that no documentation still teaches US ordering as the app's format.

## Context

- Enumerate the `DatePicker` consumers: the new-entry transaction and balance forms, budget, receipt review, and any report or filter control that takes a typed date.
- Completed `m24`'s notes and DoD are written in `MM/DD/YYYY` examples; they are historical records in `done/` and must not be rewritten, but check no *live* doc or skill teaches that ordering as the app's format.
- Record explicitly that no API or stored format changed: the ledger keeps ISO dates, and this is an input-parsing change only.

## Steps

1. Walk each date input in at least one day-first language and in English.
2. Grep `dashboard/CLAUDE.md`, the root docs and `skills/` for date-format guidance.
3. Note the no-API-change outcome.

## Files

- `dashboard/CLAUDE.md` (only if it describes the date format)

## Acceptance criteria

- [ ] Every date input verified in a day-first language and in English, at 1440 and 390.
- [ ] No live doc or skill teaches `MM/DD/YYYY` as the app's input format.
- [ ] A note records that no API, schema or stored-format change was made.

## Out of scope

Editing archived `done/` records, and other packages.
