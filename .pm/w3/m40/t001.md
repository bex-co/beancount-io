---
id: w3/m40/t001
title: "Decide and implement what the fourth account-journal column shows"
worker: worker3
status: todo
estimate: 45m
depends_on: []
---

## Objective

Stop the account journal's **Units** header from labelling the postings-count control. Choose one of two outcomes, record the reasoning, and implement it:

1. **Show units.** Derive the entry's unit amount for the displayed account from the `entry` already in the payload, render it under Units, and move the postings disclosure to its own slot as the `/journal` branch does.
2. **Relabel.** Rename the column to what it contains and drop the unused `journal.unitsHeader` usage.

Prefer (1) — it is what the header promises and what the account page exists to show — but only if the units can be derived correctly and honestly from data already fetched.

## Context

- Header: `dashboard/src/features/journal/components/journal-table.tsx:103-117`. Body: `:252-270`. Slot 4 is the mismatch; slots 5 and 6 (`change`, `balance`) are correct.
- `JournalTableItem` (`:20-23`) carries only `{ directive, change?, balance? }`. `features/reports/account/index.tsx:237-238` maps only those two, and `features/reports/account/graphql/account.graphql` requests `items { entry change balance }`.
- **Do not widen the API** to add a units field: that is a cross-package contract change subject to the repo's REST/GraphQL/MCP parity rule and needs its own decision. The postings in `entry` are the intended source.
- Non-transaction directives (Balance, Note, Pad, Price, Document, Custom) appear in this list and must not be given an invented value.
- Both narrow (`sm:hidden`) and desktop (`sm:block`) branches render a postings toggle; keep exactly one reachable per width.

## Steps

1. Decide between (1) and (2) and write the reasoning into this task.
2. Implement it in the shared table's account-journal branch.
3. Check a multi-unit entry, a zero/absent case and at least one non-transaction row.
4. Confirm the `/journal` branch is untouched.

## Files

- `dashboard/src/features/journal/components/journal-table.tsx`
- `dashboard/src/features/reports/account/index.tsx` (only if the item mapping needs the derived value)
- `dashboard/src/features/journal/locales/*.ts` (only if the header label changes)

## Acceptance criteria

- [ ] The fourth column's header describes its own contents on the GLD reproduction and on at least one non-commodity account.
- [ ] The postings disclosure remains reachable and operable by keyboard at 1440 and 390, with its accessible name intact.
- [ ] Non-transaction rows show no invented value.
- [ ] `Change` and `Balance` values and alignment are unchanged.
- [ ] `/journal`'s four columns and behavior are unchanged.

## Out of scope

Table semantics (t002), the API contract, filters (owned by m30), and the mobile package.
