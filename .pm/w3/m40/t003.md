---
id: w3/m40/t003
title: "Verify the account journal adoption surface"
worker: worker3
status: todo
estimate: 15m
depends_on: [w3/m40/t002]
---

## Objective

Confirm the repaired columns hold on every surface that renders this shared table, and that no documentation or skill still describes the old column set.

## Context

- Two production consumers: `features/journal/pages/journal-page.tsx` (`/journal`) and `features/reports/account/index.tsx` (account detail).
- Public ledgers render the same page for anonymous readers — check one signed out.
- If t001 changed what the column means, any doc, screenshot or skill text describing the account journal's columns is now stale.
- Record explicitly whether the API contract changed. It should not have — t001 forbids widening `getLedgerAccountJournal` — so the REST/GraphQL/MCP parity rule should have nothing to do here.

## Steps

1. Re-check `/journal` and an account page at 1440 and 390, signed in and signed out on a public ledger.
2. Grep the repo (including `skills/`) for descriptions of the account journal's columns; flag drift as follow-up rather than editing unrelated packages.
3. Note the parity outcome.

## Files

- `dashboard/CLAUDE.md` or `dashboard/src/features/reports/CLAUDE.md` (only if they describe these columns)

## Acceptance criteria

- [ ] Both consumers verified at both widths, signed in and signed out.
- [ ] No instruction file, README or skill still describes a "Units" column that shows a posting count.
- [ ] A note records that no REST/GraphQL/MCP surface changed, or names the change if one was unavoidable.

## Out of scope

Editing other packages, and any behavior change beyond t001/t002.
