---
id: w3/m40/t004
title: "Simplify the journal table column wiring"
worker: worker3
status: todo
estimate: 15m
depends_on: [w3/m40/t003]
---

## Objective

Run `/simplify` over the shared journal table and its two consumers, with zero behavior change.

## Context

- The header and both body branches currently repeat the same width classes (`w-20 shrink-0 … sm:w-32`) in six places, and the account and plain branches duplicate the postings-toggle block. A single column definition shared by header and body is the natural shape — and it is what stops slot 4 drifting out of alignment again.
- Do not merge the narrow and desktop postings toggles into one unless both widths still behave exactly as before.

## Steps

1. Run `/simplify` scoped to the changed files.
2. Confirm the t001 and t002 acceptance outputs are unchanged.

## Files

- The files touched by t001, t002 and t003.

## Acceptance criteria

- [ ] Rendered columns, values, widths and keyboard behavior are identical before and after, at 1440 and 390.
- [ ] Header and body derive their columns from one definition, or the duplication is deliberately justified.
- [ ] `yarn format:check && yarn lint && yarn test && yarn build` pass in `dashboard/`.

## Out of scope

New behavior, and refactors outside the milestone's changed files.
