# Verify the m39 transaction editor on Android and against an authorized save

m39 (Edit Transaction: Beancount highlighting + quick actions) was verified on the iOS simulator only — the verification machine had no Android SDK/emulator, and the dev build points at the production ledger, so no save was exercised end to end.

- Run the Edit Transaction and ledger file editor flows on an Android emulator or device: open, type, selection replacement, quick actions, undo, keyboard hide/show, both themes.
- With explicit authorization, exercise one real `updateLedgerEntrySourceSlice` save (edit, save, edit again, save again — the second save must use the refreshed checksum), then clean up the test edit.
- Chinese IME composition inside the CodeMirror editor deserves one manual pass on a real device.
