---
id: w1/m24/t008
title: "Tests: contrast guard + one locale-parity helper"
worker: worker1
status: todo
estimate: 35m
depends_on: [w1/m24/t006]
---

## Objective

Lock in the thing that regressed silently before — token contrast — and collapse the duplicated locale-parity tests while touching this area.

## Context

- A contrast guard is the only kind of test that can stop this defect coming back: the ramp drifted to 1.10–1.34 with no test noticing.
- `src/translations/__tests__/account-picker-parity.test.ts` and `budget-parity.test.ts` are near-identical, and the locale list is now maintained in **three** places (both tests plus `SUPPORTED_LOCALES` in `translations/index.ts`).
- jest-lite constraints: relative imports for value modules, limited matcher set (`toBe`/`toEqual`/`toBeCloseTo`/`toBeTruthy`/`toBeFalsy`/`toThrow` and `.not`).

## Steps

1. Write a contrast helper and assert the control-boundary pair clears 3:1 in **both** themes, plus placeholder-on-fill.
2. Extract `expectLocaleParity(prefix, { except })` deriving locales from the directory; rewrite both parity tests on it.
3. Run `yarn test:unit`.

## Files

- `src/__tests__/` (new contrast test), `src/translations/__tests__/`

## Acceptance criteria

- [ ] A failing contrast ratio fails the test suite, in either theme
- [ ] One parity helper; the locale list is derived, not maintained in three places
- [ ] `yarn test:unit` green

## Out of scope

- Translating anything (inbox note `022`).
