---
id: w1/m2/t007
title: Simplify pass over accounts-tab code
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m2/t006]
---

## Objective

Run `/simplify` over the code this milestone changed — reuse, simplification, efficiency; strictly behavior-preserving.

## Context

- Milestone diff: `src/screens/accounts-screen/`, `src/screens/account-detail-screen/`, `src/components/account-list/`, `src/common/graphql/queries/*.graphql`, touched home-screen files.

## Steps

1. Run `/simplify` scoped to the milestone's changed files.
2. Apply accepted findings; re-run `yarn test` (lint + typecheck + unit).

## Files

- Whatever the simplify pass flags within the milestone's diff.

## Acceptance criteria

- [ ] Simplify findings applied or explicitly declined
- [ ] `yarn test` green; no behavior change

## Out of scope

- Bug hunting (`/code-review` territory) and new features.
