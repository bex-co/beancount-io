---
id: w1/m2/t008
title: Unit tests for accounts selectors + behavior
worker: worker1
status: todo
estimate: 40m
depends_on: [w1/m2/t006]
---

## Objective

Add meaningful unit tests for the behavior this milestone shipped — net-worth header selection, grouped subtotals, and account-journal pagination handling — passing with `yarn test:unit`.

## Context

- Test conventions: `src/__tests__/` plus colocated `__tests__/` dirs (see `src/screens/home-screen/selectors/__tests__/` from w1/m1).
- Prime targets: `select-net-worth-header` (t003), the shared `account-list` grouping/subtotal logic (t004), pagination merge/dedupe for the account journal (t005), multi-currency balance parsing edge cases via `balance-util`.

## Steps

1. Test `select-net-worth-header`: series conversion, empty data, multi-currency balances.
2. Test group subtotals: Assets/Liabilities filtering and totals, including zero-balance and deep-nesting cases (see `select-account-totals-zero-bug.test.ts` for prior art).
3. Test journal pagination logic: append without duplicates, `total`-based end-of-list.
4. Run `yarn test:unit`.

## Files

- `src/screens/accounts-screen/selectors/__tests__/` (new)
- `src/components/account-list/__tests__/` (new)
- `src/screens/account-detail-screen/__tests__/` (new)

## Acceptance criteria

- [ ] Tests assert real behavior and failure modes (no tautological or snapshot-everything tests)
- [ ] `yarn test:unit` green

## Out of scope

- E2E/UI snapshot testing.
