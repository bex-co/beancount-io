# Surface ledger errors via getLedgerErrors

`getLedgerErrors(ledgerId): [BeancountError!]!` is unused by the app. A broken ledger currently fails silently on mobile. Add an error badge (ledger drawer or settings) + a simple list screen of errors with file/line context. Quality-bar item: "errors surfaced honestly". Sub-hour.
