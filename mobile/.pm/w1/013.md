# Dead chart code: `line-chart-d3.tsx` and the unused scrub callbacks

Two pieces of `src/common/d3/` have no consumer.

**`src/common/d3/line-chart-d3.tsx` (136 lines).** Nothing imports it except its own test (`src/common/d3/__tests__/line-chart-d3.test.ts:4`). The four charts actually in use are `interactive-line-chart.tsx` (Home net-worth carousel + account detail), `bar-chart-d3.tsx` (Home spending card), `income-expense-bar-chart.tsx` (Reports), and `budget-bar-chart-d3.tsx` (Budget). Check whether `generateTicks` in `src/common/d3/utils.ts:1` dies with it — `line-chart-d3.tsx` is its only caller.

**`onScrubStart` / `onScrubEnd` on `InteractiveLineChartD3`.** Declared at `src/common/d3/interactive-line-chart.tsx:52-54` and wired through the pan responder, but neither of the two call sites passes them: `src/components/balance-chart-card/index.tsx:68-74` and `src/screens/home-screen/components/account-charts-card.tsx:98-104` both omit both props. Either a caller wanted them and never landed, or they are leftovers.

Sub-hour. Companion to `w1/012` (orphaned `homeCharts.graphql`) — same class of cleanup, and like that one it wants its own diff rather than riding along with unrelated work.

← from `/pm-brainstorm` 2026-08-14 (Monarch animation / delight pass)

**Re-checked 2026-08-16.** `line-chart-d3.tsx` now has **zero** importers, not one: `__tests__/line-chart-d3.test.ts` imports `d3-scale` and `d3-shape` directly and re-implements the scale math inline — it never touches the component. So the test does not protect anything and goes with the file. `generateTicks` is called only from `line-chart-d3.tsx:49`, but has its own coverage in `__tests__/utils.test.ts:57-84`, so deleting the chart leaves a tested-but-unused helper — delete both, or keep the helper deliberately. `onScrubStart`/`onScrubEnd` are unchanged: declared and invoked inside `interactive-line-chart.tsx`, passed by neither call site.
