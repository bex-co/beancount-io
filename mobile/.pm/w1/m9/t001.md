---
id: w1/m9/t001
title: "GraphQL ops + codegen: update/delete LedgerEntrySourceSlice"
worker: worker1
status: todo
estimate: 20m
depends_on: []
---

## Objective

Add the two source-slice mutations to the app's GraphQL operations and regenerate the typed hooks.

## Context

- Schema (already in `src/generated-graphql/schema.graphql`): `updateLedgerEntrySourceSlice(input: UpdateSourceSliceInput!, ledgerId: String!)` and `deleteLedgerEntrySourceSlice(input: DeleteSourceSliceInput!, ledgerId: String!)` — both take the entry's `sha256sum` for conflict-safe writes.
- Operations live in `src/common/graphql/queries/` (existing `getLedgerEntryContext.graphql` already fetches `slice`/`sha256sum`).
- Never hand-edit `src/generated-graphql/` — run `yarn codegen`.

## Steps

1. Add `updateLedgerEntrySourceSlice.graphql` and `deleteLedgerEntrySourceSlice.graphql` with the fields the UI needs (success flag / error message / updated slice as the schema provides).
2. Run `yarn codegen`; verify the generated hooks typecheck.

## Files

- `src/common/graphql/queries/updateLedgerEntrySourceSlice.graphql` (new)
- `src/common/graphql/queries/deleteLedgerEntrySourceSlice.graphql` (new)
- `src/generated-graphql/` (via `yarn codegen` only)

## Acceptance criteria

- [ ] `yarn codegen` succeeds; `useUpdateLedgerEntrySourceSliceMutation` / `useDeleteLedgerEntrySourceSliceMutation` exist and typecheck.
- [ ] `yarn typecheck` green.

## Out of scope

- Any UI.
