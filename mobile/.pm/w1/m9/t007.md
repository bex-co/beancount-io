---
id: w1/m9/t007
title: Unit tests for edit/delete behavior
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m9/t005]
---

## Objective

Meaningful unit tests under `src/__tests__/` for the behavior this milestone shipped — no trivial, tautological, or snapshot-everything tests.

## Context

- Runner: `yarn test:unit` (jest-lite) — resolves `@/` only for **type** imports; value modules must be imported **relatively**. Extract any pure logic (e.g. param/slice handling, dirty-state check gating Save) into plainly importable helpers.

## Steps

1. Test the Save-gating logic: unchanged slice → disabled; whitespace-only change semantics; dirty tracking.
2. Test error mapping: stale-sha and validation errors produce user-facing messages, not silent failures.
3. Run `yarn test:unit` and keep it green.

## Files

- `src/__tests__/edit-slice.test.ts` (new)
- Helper extraction in `src/screens/transaction-detail-screen/` as needed

## Acceptance criteria

- [ ] Tests assert real behavior and failure modes of edit/delete.
- [ ] Value imports are relative (jest-lite `@/` limitation respected).
- [ ] `yarn test:unit` green.

## Out of scope

- Component/UI snapshot tests.
