---
id: w1/m39/t006
title: Simplify
worker: worker1
status: todo
estimate: 20m
depends_on: [w1/m39/t004, w1/m39/t005]
---

## Objective

Review the milestone's code for behavior-preserving reuse, simplification, and efficiency before final validation.

## Context

- Both editor screens should reuse CodeEditor and KeyboardAccessoryBar without duplicating syntax rules or keyboard behavior.
- Expo DOM callbacks, document replacements, and effects can create unnecessary state or cursor races.

## Steps

1. Run `/simplify` over the milestone diff. If that command is unavailable, perform the same reuse/simplification/efficiency review directly and record the equivalent review.
2. Remove duplicated token rules, unnecessary text mirroring, redundant effects, and passthrough abstractions while preserving selection, save, and keyboard behavior.
3. Re-run focused checks for any code changed during the review.

## Files

- The milestone diff under `src/components/code-editor/`, `src/components/keyboard-accessory-bar/`, `src/screens/edit-transaction-screen/`, and `src/screens/ledger-file-editor-screen/`
- Changed theme modules and focused tests

## Acceptance criteria

- [ ] Both editor surfaces share the same runtime syntax and quick-action behavior.
- [ ] Review findings are resolved or explained; simplifications preserve the verified behavior.

## Out of scope

- Unrelated cleanup, new abstraction frameworks, cross-package deduplication, and behavior changes.
