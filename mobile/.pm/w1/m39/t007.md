---
id: w1/m39/t007
title: Test coverage
worker: worker1
status: todo
estimate: 40m
depends_on: [w1/m39/t004, w1/m39/t005, w1/m39/t006]
---

## Objective

Close meaningful regression gaps in the implementation and complete the mobile package checks.

## Context

- Tests must exercise production token rules and editor/save behavior rather than a separate classifier or a mock returning its configured value.
- `scripts/jest-lite-runner.js` provides the package's unit runner; mobile CI runs format, lint, typecheck, and unit checks.

## Steps

1. Review tests added during implementation and cover missing token cases, selected-text insertion, repeated identical actions, quote cursor placement, and configured-currency handling.
2. Cover delayed context, failed saves, duplicate save requests, revision changes during save, repeated saves, read-only actions, and dirty-draft cancellation using production logic and controlled external boundaries.
3. Verify shared file-editor regressions and theme changes using the appropriate unit or runtime checks. Keep actual keyboard/composition evidence from t004; do not replace it with mocked assertions.
4. From `mobile/`, run `yarn format:check`, `yarn lint`, `yarn typecheck`, and `yarn test:unit`; inspect lint's autofixes and resolve relevant failures.

## Files

- `src/components/code-editor/` and adjacent tests
- `src/components/keyboard-accessory-bar/__tests__/`
- `src/screens/edit-transaction-screen/` and adjacent tests
- `src/__tests__/ledger-m15.test.ts`
- `scripts/jest-lite-runner.js` (reference; change only if a justified harness gap exists)

## Acceptance criteria

- [ ] Regression tests fail when the corresponding runtime behavior is broken.
- [ ] Save races and unsuccessful writes preserve the correct draft in tests.
- [ ] All four package checks pass; platform verification evidence remains accurate.

## Out of scope

- Coverage quotas, snapshot-only assertions, duplicate test implementations, new test dependencies, and unrelated suite rewrites.
