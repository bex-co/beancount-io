---
id: w1/m15/t011
title: "Unit tests: tokenizer, save-flow helpers, path navigation"
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m15/t009]
---

## Objective

Meaningful unit tests under `src/__tests__/` for m15's logic — real assertions on behavior and failure modes, not tautologies.

## Context

- Jest-lite runner (`scripts/jest-lite-runner.js`): value modules must use **relative imports** (`@/` works for type imports only); matchers limited to `toBe`/`toEqual`/`toBeCloseTo`/`toBeTruthy`/`toBeFalsy`/`toThrow` + `.not`.
- Testable seams (kept pure by t004/t005): the beancount `StreamLanguage` tokenizer if t004 took that route (dates, accounts, amounts, comments, directives, tags classified; malformed lines never throw; empty input → no tokens) — if t004 vendored fava's tree-sitter modules instead, skip tokenizer tests (wasm won't run under jest-lite) and cover the remaining seams: base64 encode/decode round-trip, sha-conflict detection helper (server sha ≠ local sha ⇒ conflict), error filename→path suffix normalization (t007), dir-path parent/child building for the browser breadcrumb.

## Steps

1. Extract any remaining logic out of components so it's test-reachable without React.
2. Cover happy paths + the failure modes above.
3. `yarn test:unit` green.

## Files

- `src/__tests__/` (new test files)
- Small extractions in `src/components/code-editor/`, `src/screens/ledger-file-editor-screen/`, `src/screens/ledger-screen/`

## Acceptance criteria

- [ ] Tests assert real behavior and failure modes; `yarn test:unit` green.

## Out of scope

- Rendering/worklet-runtime tests beyond what jest-lite supports.
