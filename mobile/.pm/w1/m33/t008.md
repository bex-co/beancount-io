---
id: w1/m33/t008
title: "Simplify pass over the Sankey diff"
worker: worker1
status: todo
estimate: 20m
depends_on: [w1/m33/t007]
---

## Objective

Run `/simplify` over the code this milestone changed — reuse, simplification, efficiency; behavior-preserving. Watch for: duplication against `src/common/d3/` the chrome doesn't cover (if the Sankey grew a copy of the scrollable-axis shell, that is `w1/028`'s territory — note it there rather than half-fixing it here), and transformer logic that could fold into existing selectors.

## Context

- Changed surface: `src/screens/reports-screen/selectors/sankey-data.ts`, `components/cash-flow-sankey.tsx`, `reports-screen.tsx`, translations.
- Precedent: `w1/done/m20/t009` ran this pass as a multi-angle review and deliberately deferred two findings to inbox notes — do the same for anything that isn't behavior-preserving.

## Steps

1. `/simplify` scoped to the milestone's diff.
2. Apply behavior-preserving findings; file the rest as inbox notes with their reason for deferral.
3. Re-verify both themes after any applied change.

## Files

- The milestone's changed files only

## Acceptance criteria

- [ ] `yarn lint`, `yarn typecheck`, `yarn test:unit` green after applied findings
- [ ] Deferred findings (if any) exist as inbox notes with a written why

## Out of scope

- Bug-hunting (that is `/code-review`'s job); refactors that change behavior
