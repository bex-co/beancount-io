---
id: w1/m33/t007
title: "UX pass — light/dark, translations gate, RTL, loading states"
worker: worker1
status: todo
estimate: 40m
depends_on: [w1/m33/t006]
---

## Objective

The standing UX closing pass over everything this milestone shipped: both themes via `useTheme()` tokens (no hard-coded colors), every new string declared in the English base and translated in all twelve other locales or named in `KNOWN_GAPS`, background colors on loading states, `SafeAreaView` spacing intact, and the t001 RTL decision verified in Persian on device.

## Context

- The translation integrity gate is unconditional since `w1/done/m30` — a missing locale key fails `yarn test:unit`.
- RTL verification: `w1/done/m31/README.md` for the known traps; drive the simulator per `mobile/CLAUDE.md`.
- New strings expected from t005/t006: empty state, section header, any label/legend copy.

## Steps

1. Walk Reports in light and dark; screenshot both.
2. Switch to Persian; verify the t001 RTL decision renders as decided (flow direction, labels, header alignment).
3. Confirm the translations gate is green with no new `KNOWN_GAPS` entries — or each entry names its deferral honestly.
4. Reduce-motion on: draw-in degrades gracefully.

## Files

- `src/screens/reports-screen/components/cash-flow-sankey.tsx` (fixes only)
- `src/translations/*.ts` (gap-closing only)

## Acceptance criteria

- [ ] Light, dark, and Persian screenshots recorded; defects found are fixed or filed as inbox notes
- [ ] `yarn test:unit` green with the translation gate passing

## Out of scope

- Native-speaker review of the twelve translations (the standing caveat since m29/m30)
