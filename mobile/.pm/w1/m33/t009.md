---
id: w1/m33/t009
title: "Test coverage — transformer, rollup, reconciliation"
worker: worker1
status: todo
estimate: 40m
depends_on: [w1/m33/t008]
---

## Objective

Meaningful unit tests for what this milestone shipped, passing under `yarn test:unit`. The transformer is the heart: categorization at depth, exclusions, the top-N + "Other" rollup preserving totals, non-positive link filtering, and the reconciliation invariant (transformer totals == income-statement totals for the same tree). Tests must assert real behavior and failure modes — no snapshot-everything.

## Context

- t003 seeded transformer tests; this task closes the gaps found during t004–t007 (label truncation helpers, color mapping, empty-input handling).
- jest-lite constraints: relative imports for value modules; matchers limited to `toBe`/`toEqual`/`toBeCloseTo`/`toBeTruthy`/`toBeFalsy`/`toThrow` + `.not`.
- Break-the-test check: each new assertion should fail when its rule is deliberately broken once (the m32 discipline).

## Steps

1. Inventory the milestone's testable units vs. existing coverage.
2. Add cases for rollup sum-preservation, empty tree, single-currency fallback, and truncation edge cases.
3. Deliberately break each rule once to confirm the test bites; restore.

## Files

- `src/screens/reports-screen/selectors/__tests__/sankey-data.test.ts`
- `src/__tests__/` for any shared helpers the milestone added

## Acceptance criteria

- [ ] `yarn test:unit` green; each new test was seen to fail when its rule was broken
- [ ] The reconciliation invariant has an explicit test, not just a manual check in t006

## Out of scope

- Rendering/snapshot tests of SVG output; end-to-end simulator automation
