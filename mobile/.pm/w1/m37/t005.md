---
id: w1/m37/t005
title: Mark/unmark toggle + cadence chip in the merchant view
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m37/t003, w1/m37/t004]
---

## Objective

Monarch's "Mark merchant as recurring" / "Mark merchant as not recurring", on our merchant view (m36): a toggle that writes the override store, plus a cadence chip in the stats header when the merchant is recurring (detected or marked).

## Context

- Monarch gates its toggle behind five required fields; ours needs none — cadence and amount are detected, income/expense falls out of postings. Manual mark on an undetectable payee shows "irregular" as its cadence.
- `analytics.track("merchant_toggle_recurring", { on: boolean })`.

## Steps

1. Toggle row in `merchant-detail-screen.tsx` with helper copy (translated).
2. Chip in the stats header; state flows from `resolveRecurringVerdict`.
3. Directory reflects the change immediately on back-navigation (reactive var — no refetch needed).

## Files

- `src/screens/merchant-detail-screen/merchant-detail-screen.tsx`
- `src/translations/en.ts` + locales

## Acceptance criteria

- [ ] Toggling on pins the merchant in the directory's Recurring section; toggling off removes a detected one; both survive restart.
