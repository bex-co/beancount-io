# Delete the unrouted `journal-screen`

`6900141` ("replace journal tab with searchable transactions") swapped the Journal tab for `transactions-screen`, but left the old screen in the tree. `JournalScreen` is exported at `src/screens/journal-screen/journal-screen.tsx:364` and imported by **nothing** — no route under `app/`, no other screen, no test.

The whole directory is dead: `journal-screen.tsx`, `journal-header.tsx`, `journal-empty-state.tsx`, `journal-no-entries-for-filters-state.tsx`, `journal-bottom-sheet/`. It is also a live source of misleading board and code references — `w1/010` counted its header as one of three search-bar copies, `w1/015` filed its `ActivityIndicator` as a skeleton violation, and the deleted `w1/m7/t003` described the recent-transactions card as linking to "Journal".

Before deleting, check whether anything in `transactions-screen` still wants a piece of it (the bottom sheet is the least obviously duplicated part), and grep the translations for `journal*` keys that would become orphans.

Sub-hour.

← found during the 2026-08-16 board triage
