---
id: w1/m30/t003
title: Bring `fa` to full parity with the English base
worker: worker1
status: todo
estimate: 45m
depends_on: []
---

## Objective

Translate the 175 `en.ts` keys that `src/translations/fa.ts` currently inherits instead of declaring, so Persian users read the whole app in Persian, and delete `fa` from the `KNOWN_GAPS` baseline.

## Context

- `src/translations/fa.ts` — declares 143 of the base's 318 top-level keys today, the widest gap on the board.
- `src/translations/en.ts` — the source of truth; translate against the English **value** and use the surrounding `//` group comments to place each key on its screen.
- Missing groups: multi-posting entry, open-account, the journal's directive types, reports/dashboard cards and range chips, transaction edit/delete, AI receipt capture, notifications and commit detail, the ledger file list and editor, and budgets.
- **Right-to-left.** Keep `{{token}}` placeholders positioned so the rendered sentence reads correctly under RTL, and use Persian digits only where the existing file already does — mixing digit systems inside one screen is worse than either choice alone. This is the locale t008 checks for layout, not just length.
- The m29 gate is live: an unknown key or a dropped `{{token}}` fails `yarn test:unit`.
- The six m29 locales are already complete — read `zh.ts` or `de.ts` for how a group was rendered there before inventing a new phrasing.

## Steps

1. Run the integrity suite to get the exact missing list for `fa`.
2. Translate each key in `en.ts` order, under the same `//` group comments, so the files stay diffable side by side.
3. Preserve every `{{token}}`; keep button and chip labels short enough for their control.
4. Reuse the terminology already in this file for terms it already translates (account types, "ledger", "transaction", "budget").
5. Remove the `fa` entry from `KNOWN_GAPS`.
6. `yarn lint` / `yarn typecheck` / `yarn test:unit`.

## Files

- `src/translations/fa.ts`
- `src/translations/__tests__/known-gaps.ts`

## Acceptance criteria

- [ ] `fa` declares all 318 top-level keys; the integrity suite reports `missing: []` for it.
- [ ] `fa` no longer appears in `KNOWN_GAPS`.
- [ ] No value is a byte-identical copy of the English string unless the English text is the correct Persian output, in which case it is listed in `INTENTIONALLY_UNTRANSLATED` with a reason.
- [ ] Every `{{token}}` from the English value survives in the translation.
- [ ] `yarn test:unit` green.

## Out of scope

- Other locales (their own tasks).
- Rendering the strings on screen — t008 walks them.
- Editing `en.ts`.
