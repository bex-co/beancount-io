---
id: w1/m36/t005
title: UX pass (light/dark, RTL, translations, analytics)
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m36/t004]
---

## Objective

Standing UX pass over the merchant view.

## Steps

1. Light + dark walk: header, list, skeleton, error/empty; tokens only; loading backgrounds set.
2. Persian spot-check: amounts and dates on the correct edge; Latin payee names in RTL don't drift (`natural` alignment hazard from m31).
3. New strings in `en.ts` + twelve locales or explicit `KNOWN_GAPS`; rendered via `useTranslations()`.
4. `SafeAreaView`; `page_view_merchant_detail` and `merchants_tap_row` single-fire proven structurally.

## Acceptance criteria

- [ ] Screenshots light + dark + fa in the outcome note; gate + `yarn lint` green.
