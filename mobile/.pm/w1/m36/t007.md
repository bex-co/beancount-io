---
id: w1/m36/t007
title: Test coverage for escaping, filtering, stats selectors
worker: worker1
status: todo
estimate: 45m
depends_on: [w1/m36/t005]
---

## Objective

Unit tests under `src/__tests__/` asserting real behavior: BQL string escaping against hostile names, exact-match filtering (substring payees, missing payee, narration-only entries), per-currency stats bucketing (multi-currency merchant, zero-sum postings). jest-lite constraints apply (relative imports, limited matchers).

## Acceptance criteria

- [ ] `yarn test:unit` green; no tautological tests.
