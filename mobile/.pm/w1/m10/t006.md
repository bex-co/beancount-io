---
id: w1/m10/t006
title: Simplify pass over receipt-capture code
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m10/t005]
---

## Objective

Run `/simplify` over the code this milestone changed — reuse, simplification, efficiency; behavior-preserving.

## Context

- Scope: receipt-capture screens/hooks, split-button menu additions, new GraphQL operations.
- Watch for duplication with `add-transaction-next-screen.tsx` (toast lifecycle, refresh parity) and m8's editor rows worth extracting.

## Steps

1. Run `/simplify` scoped to the milestone's diff.
2. Apply behavior-preserving cleanups; re-verify the capture flow end to end.

## Files

- Files changed by this milestone.

## Acceptance criteria

- [ ] `/simplify` findings applied or explicitly rejected with rationale.
- [ ] `yarn lint` and `yarn typecheck` green; behavior unchanged.

## Out of scope

- Bug hunting (`/code-review`) and new features.
