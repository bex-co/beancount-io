---
id: w1/m10/t005
title: UX pass — light/dark, i18n, loading bg, safe area, analytics
worker: worker1
status: todo
estimate: 40m
depends_on: [w1/m10/t004]
---

## Objective

Standing closing pass over the receipt capture flow: menu item, acquisition, progress states, review screen.

## Context

- Conventions: theme tokens via `useTheme()`, English-base strings via `useTranslations()`, themed backgrounds on every progress/loading state, `SafeAreaView`, analytics on new screen mounts.

## Steps

1. Verify all states in light **and** dark: menu item, permission prompts, uploading/parsing progress, quota-exhausted message, review screen, error states.
2. Audit new strings in `src/translations/en.ts`; add worthwhile locale overrides.
3. Safe-area + keyboard behavior on the review screen; loading backgrounds (no dark-mode flicker).
4. Confirm analytics: `tap_scan_receipt`, parse started/succeeded/failed, `tap_receipt_confirm`, page views.

## Files

- `src/screens/receipt-capture-screen/*`, `src/components/split-button/`
- `src/translations/*.ts`

## Acceptance criteria

- [ ] Both themes verified across every flow state; no hard-coded colors.
- [ ] All new strings localized via the English base + `useTranslations()`.
- [ ] All analytics events fire.

## Out of scope

- New functionality.
