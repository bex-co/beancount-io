---
id: w1/m10/t002
title: '"Scan receipt" menu item + camera/library image acquisition (⚠ new dependency)'
worker: worker1
status: todo
estimate: 35m
depends_on: [w1/m8/t001]
---

## Objective

Add "Scan receipt" to the m8 split-button menu on Home and implement image acquisition: take a photo or pick from the library, producing a local image URI ready for upload.

## Context

- Menu: `src/components/split-button/` + wiring in `src/screens/home-screen/home-screen.tsx` (from w1/m8/t001).
- ⚠ **New dependency: `expo-image-picker`** — nothing camera/picker-like exists in `package.json`. Repo rule: ask the user before adding; install with `yarn` from inside `mobile/` (never hand-edit `yarn.lock`).
- iOS/Android permission strings go in `app.json` (Expo config plugins for image picker).

## Steps

1. Get the user's OK, then add `expo-image-picker` via `yarn` and configure permission strings in `app.json`.
2. Add the localized "Scan receipt" menu item (analytics `tap_scan_receipt`).
3. On tap: camera/library choice, permission handling with honest denial messaging, return the selected image URI (downscale/compress if the picker supports it) to the capture flow.

## Files

- `src/components/split-button/` (menu item slot) / `src/screens/home-screen/home-screen.tsx`
- `app.json`
- `src/translations/en.ts`
- `package.json` (via `yarn add expo-image-picker` only)

## Acceptance criteria

- [ ] User explicitly approved the new dependency before it was added.
- [ ] Menu shows "Scan receipt"; camera and library paths both yield an image URI.
- [ ] Permission denial shows a localized, honest message (no crash, no silent no-op).

## Out of scope

- Upload/parse (t003); multi-image capture.
