---
id: w1/m10/t001
title: "GraphQL ops + codegen: upload URL, parse, insert, aiCfoUsage"
worker: worker1
status: todo
estimate: 25m
depends_on: []
---

## Objective

Add the four receipt-pipeline operations and regenerate typed hooks: `generateTempAssetUploadUrl` (presigned S3 PUT), `parseReceiptWithLLM`, `insertReceiptTransaction(input: InsertReceiptTransactionInput!, ledgerId, receiptObjectKey)`, and `aiCfoUsage` (monthly AI quota).

## Context

- All four already exist in `src/generated-graphql/schema.graphql`; `InsertReceiptTransactionInput` = `{ date, description, documentAccount, payee, postings: [ReceiptPostingInput!]! }` with `ReceiptPostingInput` = `{ account, amountCurrency, amountNumber }`.
- Operations live in `src/common/graphql/queries/`; regenerate with `yarn codegen` — never hand-edit `src/generated-graphql/`.

## Steps

1. Add the four `.graphql` operation files selecting the fields the flow needs (upload URL + objectKey; parsed payee/date/description/postings; insert result; usage/limits).
2. `yarn codegen`; verify hooks typecheck.

## Files

- `src/common/graphql/queries/generateTempAssetUploadUrl.graphql` (new)
- `src/common/graphql/queries/parseReceiptWithLLM.graphql` (new)
- `src/common/graphql/queries/insertReceiptTransaction.graphql` (new)
- `src/common/graphql/queries/aiCfoUsage.graphql` (new)
- `src/generated-graphql/` (via `yarn codegen` only)

## Acceptance criteria

- [ ] `yarn codegen` succeeds; all four generated hooks typecheck.
- [ ] `yarn typecheck` green.

## Out of scope

- Any UI; `parseFileWithLLM` (PDFs).
