---
id: w1/m3/t001
title: Income-statement GraphQL op + use-income-statement hook
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m2/t001]
---

## Objective

Add the operation file and typed hook for `getLedgerIncomeStatement` — the data source for the Spending, Income, and Cash Flow reports.

## Context

- Schema (`src/generated-graphql/schema.graphql`): `IncomeStatementData` — `expensesData`/`incomeData` (per-interval balances + accountBalances + budgets), `expensesHierarchyData`/`incomeHierarchyData` (category trees), `netProfitData`. Params: `ledgerId`, `time`, `interval` (default monthly), `conversion`.
- Follows the exact pattern established in w1/m2/t001 (`balanceSheet.graphql` + hooks).
- No new npm dependency needed.

## Steps

1. Write `incomeStatement.graphql` selecting interval series, both hierarchy trees, and `netProfitData`.
2. Run `yarn codegen` (never hand-edit `src/generated-graphql/`).
3. Add `use-income-statement.ts` under `src/screens/reports-screen/hooks/` (ledgerId from session, `time`/`interval` as arguments).

## Files

- `src/common/graphql/queries/incomeStatement.graphql` (new)
- `src/screens/reports-screen/hooks/use-income-statement.ts` (new)
- `src/generated-graphql/` (regenerated via `yarn codegen` only)

## Acceptance criteria

- [ ] `yarn codegen` and `yarn typecheck` green
- [ ] Hook returns typed income/expense/net-profit data for a valid ledger

## Out of scope

- Report UI (t003–t006).
