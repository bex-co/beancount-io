---
id: w1/m3/t004
title: Income report over incomeData/incomeHierarchyData
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m3/t003]
---

## Objective

The Income segment mirrors the Spending report over `incomeData`/`incomeHierarchyData`: monthly income bars + source breakdown. Small task because it reuses t003's selector and component patterns.

## Context

- t003's `select-spending-report.ts` and `spending-report.tsx` — generalize rather than duplicate (parameterize by series + hierarchy + sign; beancount income balances are negative, display positive).

## Steps

1. Generalize t003's selector/component into a shared "flow report" (or parameterize with a `kind: "spending" | "income"` prop).
2. Render the Income segment with `Income:` prefix stripping and sign inversion.
3. Verify totals against the web app.

## Files

- `src/screens/reports-screen/components/spending-report.tsx` (generalized, possibly renamed `flow-report.tsx`)
- `src/screens/reports-screen/selectors/select-spending-report.ts` (generalized)
- `src/screens/reports-screen/reports-screen.tsx`

## Acceptance criteria

- [ ] Income segment shows positive monthly bars and source breakdown matching the ledger
- [ ] No duplicated report component — one parameterized implementation serves both segments

## Out of scope

- Cash-flow combination view (t005).
