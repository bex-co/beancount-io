---
id: w1/m3/t002
title: Reports tab route + shell: segment switcher + shared time range
worker: worker1
status: todo
estimate: 40m
depends_on: []
---

## Objective

Add the Reports tab with its shell: a segmented switcher across Spending / Income / Cash Flow / Net Worth and a shared time-range state that all four reports read, so switching report type keeps the selected range.

## Context

- Tab registration: `app/(app)/(tabs)/_layout.tsx` (follow the `tabTitles` i18n pattern from w1/m2/t002); Ionicons icon e.g. `stats-chart`.
- Segment switcher: reuse `src/components/tabs/`.
- Time range: `src/components/time-range-pills/`.
- Screen conventions: `SafeAreaView`, `usePageView("reports")`, `LedgerGuard`.
- New strings (`reports`, `spending`, `income`, `cashFlow`, `netWorth`) in the English base `src/translations/en.ts`.

## Steps

1. Create `src/screens/reports-screen/reports-screen.tsx`: segment switcher + `TimeRangePills` + a content slot per report type (placeholders until t003–t006).
2. Create route `app/(app)/(tabs)/reports.tsx` and register the tab after Journal.
3. Hold `reportType` and `timeRange` in screen state; track segment switches via analytics.
4. Add the i18n keys to `src/translations/en.ts`.

## Files

- `app/(app)/(tabs)/reports.tsx` (new)
- `app/(app)/(tabs)/_layout.tsx`
- `src/screens/reports-screen/reports-screen.tsx` (new), `src/screens/reports-screen/index.ts` (new)
- `src/translations/en.ts`

## Acceptance criteria

- [ ] Reports tab appears with icon + localized title; four segments switchable
- [ ] Selected time range persists across segment switches
- [ ] `yarn typecheck` green

## Out of scope

- Actual report content (t003–t006).
