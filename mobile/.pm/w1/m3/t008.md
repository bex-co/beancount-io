---
id: w1/m3/t008
title: UX pass — light/dark, i18n, loading bg, safe area, analytics
worker: worker1
status: todo
estimate: 40m
depends_on: [w1/m3/t004, w1/m3/t005, w1/m3/t006, w1/m3/t007]
---

## Objective

Standing UX pass over everything this milestone shipped: the Reports tab shell, all four report segments, and the drill-down flow.

## Context

- Screens/components from t002–t007: `src/screens/reports-screen/**`, account-detail changes, tab registration in `app/(app)/(tabs)/_layout.tsx`.

## Steps

1. Verify light **and** dark themes — all colors via `useTheme()` tokens (chart palettes included), no hard-coded colors.
2. Verify every new string exists in the English base `src/translations/en.ts` and renders via `useTranslations()` (not `i18n.t()` directly).
3. Set background colors on all loading/skeleton states across the four segments.
4. Verify `SafeAreaView` spacing; verify analytics: page view on Reports mount, segment switches, range changes, drill-down taps.

## Files

- `src/screens/reports-screen/**`, `src/screens/account-detail-screen/**`, `src/translations/en.ts`

## Acceptance criteria

- [ ] All four segments verified in light and dark with no hard-coded colors
- [ ] No missing-translation fallbacks; strings re-render on locale change
- [ ] Loading states have themed backgrounds; safe areas correct; analytics fire

## Out of scope

- Behavior changes — polish only.
