---
id: w1/m3/t006
title: Net-worth report: net-worth line + assets vs liabilities
worker: worker1
status: todo
estimate: 40m
depends_on: [w1/m3/t002, w1/m2/t001]
---

## Objective

The Net Worth segment shows the net-worth line over the selected range with an assets-vs-liabilities breakdown (current totals per side, top accounts from the balance-sheet hierarchies) — complementing the Accounts-tab header with a report-style view.

## Context

- Data: `use-balance-sheet` from w1/m2/t001 — `netWorthData`, `assetsData`, `liabilitiesData`, hierarchy trees.
- Chart: `src/common/d3/line-chart-d3.tsx`; selectors prior art in `src/screens/home-screen/selectors/select-net-worth-series.ts` and the m1 net-worth header selector.

## Steps

1. Selector `select-net-worth-report.ts`: net-worth series for the range + assets/liabilities totals and top contributors from the hierarchies.
2. Component `net-worth-report.tsx`: line chart + two-column assets/liabilities summary.
3. Wire the shared time range; loading/empty states.

## Files

- `src/screens/reports-screen/components/net-worth-report.tsx` (new)
- `src/screens/reports-screen/selectors/select-net-worth-report.ts` (new)
- `src/screens/reports-screen/reports-screen.tsx`

## Acceptance criteria

- [ ] Net-worth series matches the Accounts-tab header for the same range
- [ ] Assets and liabilities totals reconcile with the grouped account list

## Out of scope

- Per-account drill-down (covered by the Accounts tab).
