---
id: w1/m3/t005
title: Cash-flow report: income vs expense bars + net-profit line
worker: worker1
status: todo
estimate: 50m
depends_on: [w1/m3/t001, w1/m3/t002]
---

## Objective

The Cash Flow segment shows income vs expenses side by side per month with the net (savings) highlighted — `netProfitData` as the through-line — plus a summary of total in / total out / net and savings rate for the selected range.

## Context

- Data: `use-income-statement` — `incomeData`, `expensesData`, `netProfitData`.
- Charts: `src/common/d3/bar-chart-d3.tsx` (may need grouped/dual-series support — extend it; if that balloons, fall back to two aligned bar rows) and `line-chart-d3.tsx` for the net line.
- Prior art for month comparison: `src/screens/home-screen/components/spending-card.tsx`, `select-net-profit-array.ts`.

## Steps

1. Selector `select-cash-flow.ts`: align income/expense/net series by month for the selected range; compute totals + savings rate.
2. Component `cash-flow-report.tsx`: paired bars (income up, expenses down or side-by-side) + net summary header.
3. Handle months with only income or only expenses; empty-range state.

## Files

- `src/screens/reports-screen/components/cash-flow-report.tsx` (new)
- `src/screens/reports-screen/selectors/select-cash-flow.ts` (new)
- `src/common/d3/bar-chart-d3.tsx` (only if dual-series support is added)

## Acceptance criteria

- [ ] Per-month income, expenses, and net match the web app's income statement
- [ ] Summary shows totals and savings rate for the selected range

## Out of scope

- Budget vs actual (future work).
