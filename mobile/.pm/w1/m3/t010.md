---
id: w1/m3/t010
title: Unit tests for report selectors + behavior
worker: worker1
status: todo
estimate: 40m
depends_on: [w1/m3/t008]
---

## Objective

Add meaningful unit tests for the behavior this milestone shipped — flow-report aggregation, cash-flow alignment, and net-worth report selection — passing with `yarn test:unit`.

## Context

- Test conventions: `src/__tests__/` plus colocated `__tests__/` dirs (see `src/screens/home-screen/selectors/__tests__/`).
- Prime targets: `select-spending-report` (top-N + "other", % shares, prefix stripping, sign handling for income), `select-cash-flow` (month alignment with gaps, savings rate, months with only one side), `select-net-worth-report` (range slicing, multi-currency balances).

## Steps

1. Test the flow-report selector for both spending and income kinds, including empty ranges and single-category ledgers.
2. Test cash-flow alignment: missing months, negative nets, savings-rate division-by-zero guard.
3. Test net-worth report selection against fixture balance-sheet data.
4. Run `yarn test:unit`.

## Files

- `src/screens/reports-screen/selectors/__tests__/` (new)

## Acceptance criteria

- [ ] Tests assert real behavior and failure modes (no tautological or snapshot-everything tests)
- [ ] `yarn test:unit` green

## Out of scope

- E2E/UI snapshot testing.
