---
id: w1/m3/t009
title: Simplify pass over reports-tab code
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m3/t008]
---

## Objective

Run `/simplify` over the code this milestone changed — reuse, simplification, efficiency; strictly behavior-preserving. Watch especially for duplication across the four report segments.

## Context

- Milestone diff: `src/screens/reports-screen/**`, `src/common/graphql/queries/incomeStatement.graphql`, `src/common/d3/` changes, account-detail extensions.

## Steps

1. Run `/simplify` scoped to the milestone's changed files.
2. Apply accepted findings; re-run `yarn test` (lint + typecheck + unit).

## Files

- Whatever the simplify pass flags within the milestone's diff.

## Acceptance criteria

- [ ] Simplify findings applied or explicitly declined
- [ ] `yarn test` green; no behavior change

## Out of scope

- Bug hunting (`/code-review` territory) and new features.
