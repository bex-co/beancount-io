# Chart skeletons don't match their loaded heights

`mobile/CLAUDE.md` requires skeletons to mirror the loaded layout so "the view doesn't shift a pixel when data lands", and several files carry comments asserting they do (`multi-postings-transaction/list-item.tsx:52`, `account-picker-screen.tsx:212`, `text-input-screen/index.tsx:93`, `budget-screen.tsx:192`). Three chart surfaces have drifted:

| surface                                      | skeleton                   | loaded                                       | shift  |
| -------------------------------------------- | -------------------------- | -------------------------------------------- | ------ |
| Home spending card (`spending-card.tsx:83`)  | `LoadingTile height={200}` | chart is 220 (`bar-chart-d3.tsx:26`)         | ~20px  |
| `balance-chart-card/index.tsx:52`            | `LoadingTile height={240}` | header ~90 + chart 180 + pills ~38 ≈ 305     | ~65px  |
| Reports chart (`reports-screen.tsx:139-140`) | `LoadingTile height={220}` | 220 chart **plus** the legend row (~24-28px) | legend |

The budget group card (`budget-group-card.tsx:249-250`, `LoadingTile height={190}`) has the same legend-height question — its chart is 190 plus a legend row.

Measure rather than eyeball. Sub-hour. Note `w1/m20/t007` fixes these three as part of adding the skeleton→content crossfade — if that milestone lands, this note is already covered and should be closed rather than done twice.

← from `/pm-brainstorm` 2026-08-14 (Monarch animation / delight pass)
