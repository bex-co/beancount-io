---
id: w1/m6/t001
title: Ledger as a pushed stack route + drawer menu row
worker: worker1
status: todo
estimate: 35m
depends_on: [w1/m5/t002]
---

## Objective

Make the Ledger web editor reachable from the reveal drawer's menu section (Monarch pattern, mirroring m5's Settings row): a "Ledger" row with icon + label that closes the drawer and pushes a stack-presented Ledger screen with back navigation.

## Context

- m5/t001–t002 establish the pattern: a pushed stack route registered in `app/(app)/_layout.tsx` plus a menu row in `src/components/ledger-drawer/ledger-drawer.tsx`.
- The screen component already exists and is reused as-is: `src/screens/ledger-screen/ledger-screen.tsx` (WebView of the web editor).
- The tab route `app/(app)/(tabs)/ledger.tsx` still exists until t002; if expo-router complains about the duplicate `/ledger` segment, land this task together with t002's tab removal.

## Steps

1. Add `app/(app)/ledger.tsx` mounting `LedgerScreen`, registered as a stack screen with a header and back button (same options as m5's `/settings` route).
2. Add a "Ledger" row (icon + label, theme tokens) to the drawer menu section beside Settings; tapping closes the drawer and pushes `/ledger`.
3. Reuse the existing Ledger tab-label i18n key for the row; keep the screen's analytics mount tracking intact.

## Files

- `app/(app)/ledger.tsx` (new)
- `app/(app)/_layout.tsx`
- `src/components/ledger-drawer/ledger-drawer.tsx`

## Acceptance criteria

- [ ] The drawer shows a Ledger row in the menu section; tapping it closes the drawer and pushes the Ledger screen with a working back button.
- [ ] The pushed screen renders the web editor exactly as the old tab did, in light and dark.

## Out of scope

- Removing the Ledger tab (t002).
- Any change to the ledger web editor itself.
