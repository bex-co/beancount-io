---
id: w1/m6/t006
title: Simplify pass over navigation/capture changes
worker: worker1
status: todo
estimate: 20m
depends_on: [w1/m6/t005]
---

## Objective

Standing closing task: run `/simplify` over the code this milestone changed — reuse, simplification, efficiency; behavior-preserving only.

## Context

- Scope: the diff of t001–t005 (tab layout, drawer, pushed Ledger route, entry-point consolidation).

## Steps

1. Run `/simplify` scoped to the milestone's changed files.
2. Apply behavior-preserving cleanups (e.g. share row components between the Settings and Ledger drawer rows if duplicated).

## Files

- Files changed by this milestone

## Acceptance criteria

- [ ] `/simplify` findings addressed or explicitly declined; `yarn lint` and `yarn typecheck` green.

## Out of scope

- Behavior changes.
