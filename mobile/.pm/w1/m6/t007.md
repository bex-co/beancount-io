---
id: w1/m6/t007
title: Unit tests for behavior this milestone shipped
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m6/t005]
---

## Objective

Standing closing task: add meaningful unit tests under `src/__tests__/` for the behavior this milestone shipped; they must pass with `yarn test:unit` and assert real behavior and failure modes — no trivial, tautological, or snapshot-everything tests.

## Context

- Behaviors worth asserting: the "+" press handler pushes `/add-transaction` without changing the selected tab; drawer menu items route correctly (Settings vs Ledger); capture-source property mapping per entry point.
- Note: the jest-lite runner has no `@/` path alias for value modules — use relative imports in test-reachable modules.

## Steps

1. Extract press/routing/source-mapping logic into testable pure functions where needed.
2. Write tests covering the behaviors above, including the "does not switch tabs" invariant.

## Files

- `src/__tests__/` (new test files)

## Acceptance criteria

- [ ] `yarn test:unit` green with the new tests asserting real behavior.

## Out of scope

- E2E/simulator tests.
