---
id: w1/m6/t004
title: Consolidate add-transaction entry points + capture-source analytics
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m6/t003]
---

## Objective

With a global "+" always visible, audit the per-screen add-transaction buttons and remove the redundant ones, keeping any that add context (e.g. prefilling the current account); tag the add-transaction analytics event with the capture source so we can see where captures start.

## Context

- Current entry points (grep `add-transaction`): `src/screens/home-screen/home-screen.tsx`, `src/screens/accounts-screen/accounts-screen.tsx`, `src/screens/journal-screen/journal-screen.tsx`, `src/screens/account-detail-screen/account-detail-screen.tsx`.
- Analytics wrapper: the app's Mixpanel wrapper (`expo-mixpanel-analytics` usage patterns in existing screens).

## Steps

1. Audit each entry point: remove buttons that duplicate the global "+" with no added context; keep (and document why) any that prefill data the "+" cannot.
2. Add a `source` property (e.g. `tab-plus`, `account-detail`) to the add-transaction open/track event.
3. Re-run the sweep to confirm no orphaned styles/handlers remain.

## Files

- `src/screens/home-screen/home-screen.tsx`
- `src/screens/accounts-screen/accounts-screen.tsx`
- `src/screens/journal-screen/journal-screen.tsx`
- `src/screens/account-detail-screen/account-detail-screen.tsx`

## Acceptance criteria

- [ ] Every remaining entry point either is the global "+" or demonstrably prefills context; no dead code left behind.
- [ ] The capture analytics event carries a source property from every entry point.

## Out of scope

- Redesigning the add-transaction form.
