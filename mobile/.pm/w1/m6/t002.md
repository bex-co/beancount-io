---
id: w1/m6/t002
title: Remove the Ledger tab + reference sweep
worker: worker1
status: todo
estimate: 25m
depends_on: [w1/m6/t001]
---

## Objective

Drop the Ledger entry from the tab bar now that the drawer route exists, and sweep the codebase for references to the old tab route so nothing navigates to a dead tab.

## Context

- Tab registration lives in `app/(app)/(tabs)/_layout.tsx` (the `ledger` screen with the `wallet` icon).
- The tab route file is `app/(app)/(tabs)/ledger.tsx`.
- After this task the bar is Home, Accounts, Journal (+ Reports when w1/m3 ships).

## Steps

1. Remove the `ledger` `Tabs.Screen` from `app/(app)/(tabs)/_layout.tsx` and delete `app/(app)/(tabs)/ledger.tsx`.
2. Grep for navigations targeting the tab (`/ledger`, `router.push`/`Link`/`navigate` usages) and retarget them at the pushed route from t001.
3. Confirm the tab-label i18n key is still referenced by the drawer row (do not orphan or delete it).

## Files

- `app/(app)/(tabs)/_layout.tsx`
- `app/(app)/(tabs)/ledger.tsx` (delete)
- Any files found by the reference sweep

## Acceptance criteria

- [ ] The tab bar no longer shows Ledger; every former entry point reaches the ledger editor via the pushed route.
- [ ] `yarn typecheck` passes with no dangling imports/routes.

## Out of scope

- The center "+" button (t003).
