---
id: w1/m6/t005
title: UX pass — light/dark, i18n, safe area, analytics
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m6/t004]
---

## Objective

Standing closing task: verify the milestone's user-facing surfaces meet the quality bar before it ships.

## Context

- Surfaces touched: tab bar (`app/(app)/(tabs)/_layout.tsx`), drawer menu (`src/components/ledger-drawer/`), pushed Ledger route, add-transaction entry points.

## Steps

1. Light **and** dark: tab bar with the "+" button, drawer Ledger row, pushed Ledger screen — theme tokens via `useTheme()`, no hard-coded colors.
2. All new strings in the English base under `src/translations/` and rendered via `useTranslations()`; reused keys still resolve in all locales.
3. Background colors set on any loading states; `SafeAreaView` spacing on the pushed screen and the "+" button above the home indicator.
4. Analytics: Ledger screen mount tracking preserved; capture-source events firing from each entry point.

## Files

- Files touched by t001–t004 (verification pass; fixes in place)

## Acceptance criteria

- [ ] Every check above verified in the running app in both themes.

## Out of scope

- New features or refactors (t006 handles simplification).
