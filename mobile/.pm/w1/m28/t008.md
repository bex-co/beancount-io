---
id: w1/m28/t008
title: Unit tests for tap guards and the event shape
worker: worker1
status: todo
estimate: 35m
depends_on: [w1/m28/t006]
---

## Objective

Standing closing task: pin the milestone's pure seams with unit tests green under `yarn test:unit`.

## Context

- jest-lite runner: relative imports for value modules; limited matchers.
- Testable seams: the `DashboardCard` press/analytics contract (given an `onPress` + card id, exactly one `home_card_tap` with the right identifier per invocation — extract the emission into a testable helper if needed), and any explicit swipe-guard predicate t003 added.
- Navigation itself and Expo Router resolution are not unit-testable here — the sim walk in t006 covers them; don't fake it with tautologies.

## Steps

1. Test the event helper: identifier passthrough, single emission, no emission without a handler.
2. Test the guard predicate's decision table if one exists in code.

## Files

- `src/components/dashboard-card/__tests__/` or `src/__tests__/`

## Acceptance criteria

- [ ] Tests fail if the event shape or guard logic regresses; `yarn test:unit` passes.

## Out of scope

- Route-resolution tests; snapshot tests.
