---
id: w1/m18/t006
title: UX pass — light/dark, i18n, empty states
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m18/t004, w1/m18/t005]
---

## Objective

Standing UX pass over the recents work: verify light **and** dark themes (theme tokens only), the Recent header key in the English base rendered via `useTranslations()`, correct behavior with zero usage (no empty Recent section), while searching (section hidden), and across ledger switches; analytics events verified.

## Context

- `CLAUDE.md`: theme tokens, `useTranslations()`, loading-state background conventions.

## Steps

1. Sweep the picker in both themes with empty, small, and rich usage histories, across two ledgers.
2. Grep the diff for hard-coded colors and direct `i18n.t()` calls.

## Files

- `src/screens/account-picker-screen/account-picker-screen.tsx` (verification; fixes as needed)

## Acceptance criteria

- [ ] No hard-coded colors or direct `i18n.t()` calls in the milestone's diff
- [ ] No Recent section renders with zero usage or an active query
- [ ] Recents follow the active ledger after a switch

## Out of scope

- New feature work
