---
id: w1/m18/t008
title: Test coverage
worker: worker1
status: todo
estimate: 35m
depends_on: [w1/m18/t006]
---

## Objective

Add meaningful unit tests for the behavior this milestone shipped beyond t001/t004's module tests: usage-recording semantics (count increment, `lastUsedAt` update, per-ledger keying) and serialization round-trip through the persistent-var layer. Tests must assert real behavior and failure modes.

## Context

- Precedents: `src/common/apollo/__tests__/persistent-var.test.ts` and `src/common/vars/__tests__/index.test.ts` (hand-poked `require.cache` mocking).
- jest-lite cannot render components — keep record/rank logic in pure modules and test those with relative imports.

## Steps

1. Test `recordAccountUsage` semantics and per-ledger isolation.
2. Test the usage map's JSON round-trip with the persistent-var serializer.

## Acceptance criteria

- [ ] Tests fail if per-ledger isolation, count/recency updates, or persistence round-trip regress
- [ ] `yarn test:unit` green

## Out of scope

- Component render tests (unsupported by the runner)
