---
id: w1/m16/t004
title: Accounts tab "+" header action → open-account
worker: worker1
status: todo
estimate: 25m
depends_on: [w1/m16/t003]
---

## Objective

A top-right "+" in the Accounts tab header that navigates to the open-account screen.

## Context

- `src/screens/accounts-screen/accounts-screen.tsx` renders `LedgerDrawerHeader` (`src/components/ledger-drawer/ledger-drawer-header.tsx`), which accepts a `right` prop for top-right actions.
- Pattern to copy (simplest): the Transactions screen header add button — a `TouchableOpacity` (hitSlop 8) wrapping `Ionicons name="add"` — in `src/screens/transactions-screen/transactions-screen.tsx`.

## Steps

1. Pass `right` to the Accounts `LedgerDrawerHeader`: a `TouchableOpacity` (hitSlop 8, activeOpacity 0.7) wrapping `Ionicons name="add"` in a theme-token color.
2. `onPress`: `analytics.track("tap_open_account", {})` then `router.push("/open-account")`.
3. `accessibilityLabel={t("openAccount")}`.

## Files

- `src/screens/accounts-screen/accounts-screen.tsx`

## Acceptance criteria

- [ ] The Accounts tab header shows a "+" on the right; tapping it opens the open-account screen and fires `tap_open_account`.
- [ ] Icon color uses a theme token (no hard-coded color); correct in light and dark.

## Out of scope

- The form itself (t003).
