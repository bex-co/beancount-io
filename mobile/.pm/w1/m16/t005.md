---
id: w1/m16/t005
title: UX pass — light/dark, i18n, submit/loading states, safe area, analytics
worker: worker1
status: todo
estimate: 40m
depends_on: [w1/m16/t004]
---

## Objective

Standing UX pass over the open-account flow (UI-facing milestone).

## Context

- Screens/components touched: `src/screens/open-account-screen/`, `src/screens/accounts-screen/accounts-screen.tsx`, `src/translations/en.ts`.

## Steps

1. Verify light **and** dark: all colors via `useTheme().colorTheme` tokens, no hard-coded colors — check the type picker, currency picker, date modal, the full-name preview, and the error state in both themes.
2. Confirm every new string is in the English base `src/translations/en.ts` and rendered via `useTranslations()` (no `i18n.t()` direct calls, no literals in JSX).
3. Set background colors on any submit/loading state so there is no dark-mode flicker; `SafeAreaView` spacing on the screen.
4. Analytics: `usePageView("open_account")` on mount and `tap_open_account` on the header entry tap.

## Files

- `src/screens/open-account-screen/*`, `src/screens/accounts-screen/accounts-screen.tsx`, `src/translations/en.ts`

## Acceptance criteria

- [ ] Verified correct in light and dark; no hard-coded colors.
- [ ] All new strings localized from the English base via `useTranslations()`.
- [ ] Analytics fire on screen mount and on the entry tap.

## Out of scope

- New behavior — this is a polish/verification pass only.
