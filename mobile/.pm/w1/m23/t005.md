---
id: w1/m23/t005
title: UX pass — light/dark, reduce-motion, i18n, safe area
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m23/t001, w1/m23/t002, w1/m23/t003, w1/m23/t004]
---

## Objective

Standing UX pass over everything this milestone touched: twelve refresh sites, ~18 crossfades, the two retired `Animated` files, and the tab bar.

## Context

- Theme tokens from `src/common/theme/`; `LoadingTile` resolves the effective theme via `useTheme()` — never compare `themeVar` to `"dark"`.
- Motion tokens and `useReduceMotion()` from `w1/m20/t001`; the milestone should introduce no literal durations.
- This milestone should add no new strings; if any appear, they go in the English base and render via `useTranslations()`.

## Steps

1. Walk every screen with a skeleton in light **and** dark and watch the handoff.
2. Toggle reduce-motion and re-walk: crossfades and the icon spring land instantly; `LoadingTile` stops looping.
3. Confirm loading states keep background colors and `SafeAreaView` spacing is unchanged.
4. Grep the diff for literal durations, easings and hard-coded colors.

## Files

- Every file touched by t001–t004

## Acceptance criteria

- [ ] All touched surfaces verified in light and dark
- [ ] Reduce-motion collapses every new animation to its end state
- [ ] No literal durations or hard-coded colors in the diff

## Out of scope

- Behavior changes — polish only.
