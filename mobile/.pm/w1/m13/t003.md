---
id: w1/m13/t003
title: Adopt TransactionAvatar in JournalEntryItem (home/reports/journal)
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m13/t002]
---

## Objective

Replace the inline initials avatar in `JournalEntryItem` with `TransactionAvatar`, upgrading all three surfaces that mount it: home recent-transactions card, reports account-transactions card, and the full journal list.

## Context

- `src/screens/journal-screen/journal-entry-item/index.tsx` — avatar `View` + `avatarText` at lines ~174–176; display name resolved at line ~111 (`entry.payee || entry.narration || entry.directive_type`).
- Consumers (no changes expected, verify only): `src/screens/home-screen/components/recent-transactions-card.tsx`, `src/screens/reports-screen/components/account-transactions-card.tsx`, `src/screens/journal-screen/journal-screen.tsx`.
- Rows sit on different backgrounds (DashboardCard `theme.black10` vs screen `theme.white`) — see the comment in the row style; the logo chip must read correctly on both.

## Steps

1. Swap the avatar `View`/`Text` block for `<TransactionAvatar name={name} />`; pass the payee-preferring name. Consider matching on `entry.payee` only (narration like "Coffee with Amazon colleague" should not trigger a logo) — decide and note the choice in code.
2. Remove now-unused `avatar`/`avatarText` styles and `getAvatarInitials`/`getAvatarColor` imports.
3. Verify non-transaction directives (open/close/other) still render sensibly (initials fallback).

## Files

- `src/screens/journal-screen/journal-entry-item/index.tsx`

## Acceptance criteria

- [ ] Logos appear for recognized payees on home, reports, and journal lists; unrecognized rows unchanged.
- [ ] Open/close/balance directive rows render exactly as before.
- [ ] No row height/alignment change (compare against a pre-change screenshot).

## Out of scope

- Account-detail rows (t004).
