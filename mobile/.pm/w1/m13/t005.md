---
id: w1/m13/t005
title: UX pass: light/dark, loading, no layout shift
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m13/t003, w1/m13/t004]
---

## Objective

Standing UX gate for this UI-facing milestone: verify the avatar upgrade meets the app's quality bar on every surface it touched.

## Context

- Surfaces: home recent-transactions card, reports account-transactions card, full journal list, account-detail rows.
- Conventions: theme tokens via `useTheme()` (no hard-coded colors beyond the pre-existing `#fff` avatar text), no dark-mode flicker on image load, skeleton/loading states keep their background colors.

## Steps

1. Light **and** dark: logo chip legible for transparent, white-heavy, and dark-heavy logos; initials fallback unchanged in both themes.
2. Slow-network / airplane mode: rows never show an empty circle; fallback initials appear immediately and stay if the image never loads.
3. Confirm no new user-facing strings were added (avatars are non-textual); if any were, they must be in the English base under `src/translations/` and rendered via `useTranslations()`.
4. Confirm no layout shift: row heights and text alignment identical before/after on all four surfaces.
5. No new screens were added, so no new analytics mounts are expected — confirm.

## Files

- Verification only; fixes land in the files from t002–t004.

## Acceptance criteria

- [ ] All four surfaces verified in light and dark, online and offline.
- [ ] No hard-coded colors introduced outside the documented pre-existing pattern.
- [ ] No layout shift on any touched row.

## Out of scope

- New features or brand-map additions discovered during the pass (file as inbox notes).
