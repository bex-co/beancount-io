# Scope Plaid bank sync track

Full Plaid subsystem exists in the mobile schema (link tokens, `syncPlaidTransactions`, `suggestPlaidAccountMapping`, `suggestPlaidTransactionCategories`, `submitPlaidTransactionsToLedger`) and web ships it (`beancount-dashboard` `src/features/plaid` + `importer`). Massive effortless-capture win but a multi-milestone track, and mobile Plaid Link needs `react-native-plaid-link-sdk` (new dependency — user approval required). Record for scoping; not committed.

## Re-confirmed 2026-08-18

The Plaid surface is still fully present in the mobile schema — 11 operations,
counted from the roots: `getPlaidAccounts`, `getPlaidAccountsForLedger`,
`getPlaidItem`, `getPlaidItems`, `getUnsyncedPlaidTransactions`,
`suggestPlaidAccountMapping`, `suggestPlaidTransactionCategories`,
`createPlaidLinkToken`, `createPlaidUpdateModeLinkToken`,
`exchangePlaidPublicToken`, `syncPlaidTransactions`,
`submitPlaidTransactionsToLedger`, `reconcilePlaidAccounts`,
`refreshPlaidItemStatus`, `unlinkPlaidItem`, `updatePlaidAccountMapping`,
`updatePlaidAccountCurrency`, `deletePlaidTransactions`.

Unlike `w1/006`, nothing is missing server-side — this is blocked purely on the
`react-native-plaid-link-sdk` dependency decision (repo rule: ask the owner) and
on being a multi-milestone track that wants its own scoping pass.
