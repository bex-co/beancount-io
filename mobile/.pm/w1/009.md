# Inline `open`-directive creation from the picker

When account-picker fuzzy search returns zero matches, offer "Create `<typed account>`" — appending an `open` directive to the ledger (the YNAB "New Category" pattern). Strong fit for Pillar 1 (Effortless capture) and Pillar 4 (plain-text fidelity: writes a real `open` directive), and m16 is building the underlying open-account flow — this note is the picker-side entry point to it. Write-path work with validation questions (root type, currency constraints), so it needs its own sizing pass before promotion; sequence after m16 and m17.

← from /pm-brainstorm 2026-08-14 ("polish the account picker"); moved from the monorepo root board
