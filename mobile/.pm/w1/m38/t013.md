---
id: w1/m38/t013
title: Simplify
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m38/t012]
---

## Objective

Run `/simplify` over the milestone's metadata, locale, screenshot, and release tooling so one manifest and upstream `asc` workflows own each responsibility.

## Context

- The milestone touches version scaffolding, locale coverage, screenshot generation, metadata validation, and release guidance.
- Duplicate locale lists, version discovery, field-limit logic, or upload behavior would recreate the source-of-truth drift this work is meant to remove.

## Steps

1. Run `/simplify` over every code and configuration path changed by `w1/m38/t001` through `t012`.
2. Consolidate locale mapping, fallback decisions, version lookup, screenshot ordering, and metadata paths behind one authoritative representation each.
3. Remove obsolete Fastlane iOS and direct App Store API paths after their `asc` replacements are verified.
4. Re-run focused metadata and screenshot validation after simplification.

## Files

- Mobile metadata and scripts changed by this milestone.
- Mobile documentation changed by this milestone.

## Acceptance criteria

- [ ] Locale and screenshot coverage derive from one manifest rather than repeated hand-maintained arrays.
- [ ] Upstream `asc` owns remote planning/application; repository code owns only project-specific inputs and orchestration.
- [ ] Simplification preserves all validation, review gates, and release ordering behavior.

## Out of scope

- Refactoring unrelated mobile app code or release platforms.
