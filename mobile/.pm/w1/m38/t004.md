---
id: w1/m38/t004
title: Author Slavic metadata and refresh English and Simplified Chinese
worker: worker1
status: todo
estimate: 1h
depends_on: [w1/m38/t002]
---

## Objective

Create Russian, Slovak, and Ukrainian listings and bring the existing English and Simplified Chinese copy up to the same positioning and release baseline.

## Context

- `en-US` and `zh-Hans` are the only localizations currently configured in App Store Connect.
- The live Chinese keyword field overlaps the localized subtitle and leaves useful capacity unexamined.
- The committed metadata currently stops before the version already submitted for review, so source-of-truth parity must be restored before deriving the next version.

## Steps

1. Add `ru`, `sk`, and `uk` app-info and next-version metadata using the approved terminology guide.
2. Refresh `en-US` and `zh-Hans` descriptions, promotional text, and release notes against the current shipped feature set.
3. Resolve the Chinese subtitle/keyword overlap without adding irrelevant filler.
4. Verify Cyrillic copy, punctuation, URLs, and code-point limits in the canonical files.

## Files

- `mobile/metadata/app-info/{ru,sk,uk,en-US,zh-Hans}.json`
- `mobile/metadata/version/<next-version>/{ru,sk,uk,en-US,zh-Hans}.json`

## Acceptance criteria

- [ ] `ru`, `sk`, and `uk` have complete app-info and version metadata.
- [ ] `en-US` and `zh-Hans` reflect the same approved positioning and current mobile capabilities.
- [ ] The existing Simplified Chinese subtitle-overlap finding is removed.
- [ ] No stale prior-version release note survives in the next-version files.

## Out of scope

- Translating the mobile binary into additional languages.
