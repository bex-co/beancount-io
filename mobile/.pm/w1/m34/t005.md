---
id: w1/m34/t005
title: "UX pass — light/dark, translations gate, loading states"
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m34/t004]
---

## Objective

The standing UX closing pass over this milestone's visible surface: the staleness indicator in both themes via `useTheme()` tokens, its strings through the unconditional translation gate, skeletons still correct under the new fetch policies (no flash over cached data, backgrounds set), and RTL placement of the indicator checked in Persian.

## Context

- The indicator is the only new visible element; the riskier UX surface is regressions — t002's policy changes alter when skeletons show on every screen they touched.
- Verify per `mobile/CLAUDE.md`: simulator walk in light and dark, then Persian for the indicator's mirrored layout.

## Steps

1. Light/dark walk of Home, Accounts, Reports on cold start (cache warm and cache cold).
2. Persian: indicator position and text alignment mirror correctly.
3. Translations gate green; no accidental `KNOWN_GAPS` entries.
4. Reduce-motion: no new animation misbehaves.

## Files

- Fixes only, in the milestone's changed files

## Acceptance criteria

- [ ] Screenshots: light, dark, Persian — warm-cache and cold-cache starts
- [ ] `yarn test:unit` green including the translation gate

## Out of scope

- Native-speaker translation review (standing caveat)
