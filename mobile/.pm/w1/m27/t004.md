---
id: w1/m27/t004
title: Simplify pass over the translation diff
worker: worker1
status: todo
estimate: 20m
depends_on: [w1/m27/t003]
---

## Objective

Run `/simplify` over the code this milestone changed — reuse, simplification, efficiency, behavior-preserving only.

## Context

- The diff is twelve locale files; the pass mostly checks ordering consistency with the English base, comment-block placement, and that no locale accidentally re-declares keys outside the receipt group.

## Steps

1. Run `/simplify` over the milestone's diff; apply findings or record them as inbox notes.

## Files

- The twelve locale files.

## Acceptance criteria

- [ ] Findings applied or recorded; no behavior change; `yarn lint` / `yarn typecheck` / `yarn test:unit` pass.

## Out of scope

- Restructuring the translation system.
