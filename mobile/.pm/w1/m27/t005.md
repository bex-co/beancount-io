---
id: w1/m27/t005
title: "`receipt-parity.test.ts` so the group cannot regress"
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m27/t003]
---

## Objective

Standing closing task (test coverage): add the parity test that m21 couldn't ship, following the established pattern.

## Context

- Pattern: `src/translations/__tests__/account-picker-parity.test.ts` and `budget-parity.test.ts` — each asserts every key in a group is **explicitly declared** in each locale file (spread-fallback doesn't count).
- jest-lite runner: relative imports for value modules; limited matcher set (`toBe`/`toEqual`/`toBeTruthy`/… + `.not` — no `toContain`/`toHaveLength`).
- Key set: the `receipt*` group plus `scanReceipt` and `total` (as t001/t002 shipped it — derive the list from `en.ts` by prefix where possible so new receipt keys are auto-covered).

## Steps

1. Write `receipt-parity.test.ts` asserting explicit declaration of every group key in all twelve locales, and that no locale's value equals the English string verbatim where a translation is expected (allow justified exceptions like "OK").
2. Confirm it fails when a key is removed from one locale (mutate locally, watch it fail, revert).

## Files

- `src/translations/__tests__/receipt-parity.test.ts`

## Acceptance criteria

- [ ] Test derives the receipt key list from the English base and covers all twelve locales.
- [ ] Demonstrated red-on-regression; `yarn test:unit` green on the final tree.

## Out of scope

- Parity for other untested key groups (worth an inbox note if the sweep finds more).
