---
id: w1/m27/t003
title: UX pass — longest translations rendered in the flow, light/dark
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m27/t001, w1/m27/t002]
---

## Objective

Standing closing task: prove the translated strings actually fit the receipt capture UI.

## Context

- Flow: `/(app)/receipt-capture` (screen under `src/screens/`); the scan entry lives in the Quick Add menu; the parse-result reveal shipped in m21.
- Switch locale via the app's language setting (drives `localeVar`; `useTranslations()` re-renders live). Simulator driving per `mobile/CLAUDE.md` — **stop before submitting a parsed receipt; the dev server writes to a real ledger.** The capture/parse stages up to the confirm button are safe to walk.
- Pick the two longest locales from t001/t002's length flags (typically de and ru).

## Steps

1. Walk capture → parsing → reveal in both flagged locales, light and dark; screenshot each stage.
2. Check buttons ("Retake", "Upload"), the progress line, and the reveal header for truncation, wrapping, or overflow.
3. Fix string-level issues by shortening translations; file a note for any real layout defect found.

## Files

- Translation files from t001/t002 (fixes only).

## Acceptance criteria

- [ ] Screenshots for both locales × both themes with no truncated or overflowing receipt strings.
- [ ] No layout code changed in this pass (defects filed, not fixed inline).

## Out of scope

- Ledger writes; layout refactors.
