---
id: w1/m27/t002
title: "Translate the `receipt*` group: bg, ca, fa, nl, sk, uk"
worker: worker1
status: todo
estimate: 45m
depends_on: []
---

## Objective

Explicitly declare and translate the full receipt group in the remaining six locales.

## Context

- Same key set and conventions as t001 (`src/translations/en.ts:272-305` plus `scanReceipt`, `total`).
- Target files: `src/translations/{bg,ca,fa,nl,sk,uk}.ts`.
- Farsi (`fa`) is RTL at the string level — check how existing `fa` strings handle Latin brand/product words and follow suit.

## Steps

1. For each locale, translate all keys in the group; keep placeholders byte-identical.
2. Flag over-length translations for t003.

## Files

- `src/translations/bg.ts`, `ca.ts`, `fa.ts`, `nl.ts`, `sk.ts`, `uk.ts`

## Acceptance criteria

- [ ] All six files explicitly declare every key in the group with a non-English translation.
- [ ] Interpolation placeholders unchanged; `yarn typecheck` passes.

## Out of scope

- RTL layout work in the capture screens (none is planned; only string content).
