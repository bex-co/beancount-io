---
id: w1/m27/t001
title: "Translate the `receipt*` group: zh, de, es, fr, pt, ru"
worker: worker1
status: todo
estimate: 45m
depends_on: []
---

## Objective

Explicitly declare and translate the full receipt group in the six highest-traffic non-English locales.

## Context

- Source keys: `src/translations/en.ts:272-305` (~19 `receipt*` keys) plus `scanReceipt` and `total`.
- Target files: `src/translations/{zh,de,es,fr,pt,ru}.ts` — each is `{ ...en, <overrides> }`; add the keys to the overrides object in the same order as the English base, under a matching `// receipt capture` comment.
- Match each locale's existing register (formal/informal address — e.g. German "Sie" vs "du" — must follow what the file already uses) and its existing convention for rendering "AI".

## Steps

1. For each locale, translate all keys in the group; keep placeholders/interpolations byte-identical to the English base.
2. Sanity-check lengths against the English strings; flag any translation >1.5× the English length for t003's rendering pass.

## Files

- `src/translations/zh.ts`, `de.ts`, `es.ts`, `fr.ts`, `pt.ts`, `ru.ts`

## Acceptance criteria

- [ ] All six files explicitly declare every key in the group with a non-English translation.
- [ ] Interpolation placeholders unchanged; `yarn typecheck` passes.

## Out of scope

- The remaining six locales (t002); any new English keys.
