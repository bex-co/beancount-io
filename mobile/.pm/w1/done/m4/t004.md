---
id: w1/m4/t004
title: Simplify pass over account-detail changes
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m4/t003]
---

## Objective

Run `/simplify` over the code this milestone changed — reuse, simplification, efficiency; behavior-preserving.

## Context

- Focus: dead styles/components left after t002 (e.g. a fully superseded `AccountEntryRow`), duplicated row/section styling between Journal and account detail, header/back-button code in `app/(app)/_layout.tsx`.

## Steps

1. `/simplify` scoped to the milestone's diff.
2. Apply behavior-preserving cleanups; delete superseded code.

## Files

- `src/screens/account-detail-screen/`
- `src/screens/journal-screen/journal-entry-item/` (if touched)
- `app/(app)/_layout.tsx`

## Acceptance criteria

- [ ] No superseded components/styles remain from the restyle
- [ ] `yarn lint` and `yarn test:unit` green after cleanups

## Out of scope

- Behavior changes.
