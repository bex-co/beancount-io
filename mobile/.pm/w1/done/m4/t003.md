---
id: w1/m4/t003
title: UX pass — light/dark, i18n, loading bg, safe area, analytics
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m4/t001, w1/m4/t002]
---

## Objective

Standing closing pass over the screens this milestone touched: verify both themes, translations, loading-state backgrounds, safe-area spacing, and analytics.

## Context

- Touched surfaces: `app/(app)/_layout.tsx` header, `src/screens/account-detail-screen/`, possibly `src/screens/journal-screen/journal-entry-item/`.
- Dark-mode flicker on loading states has bitten before (account picker) — check the account-detail loading/empty states explicitly.

## Steps

1. Walk account detail (and Journal, if `JournalEntryItem` changed) in light **and** dark — colors from `useTheme()` tokens only, no hard-coded values.
2. Confirm any new strings live in the English base under `src/translations/` and render via `useTranslations()` (no direct `i18n.t()`).
3. Confirm loading/empty/error states set background colors; `SafeAreaView` spacing intact.
4. Confirm existing analytics still fire (`usePageView("account_detail")`, `account_detail_range`).

## Files

- `src/screens/account-detail-screen/`
- `app/(app)/_layout.tsx`
- `src/translations/` (only if t002 added strings)

## Acceptance criteria

- [ ] Both themes verified on account detail with no hard-coded colors
- [ ] All new strings in the English base and rendered via `useTranslations()`
- [ ] Loading states have background colors; safe area and analytics intact

## Out of scope

- New features or refactors (t004).
