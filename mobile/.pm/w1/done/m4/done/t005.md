---
id: w1/m4/t005
title: Unit tests for account-detail row mapping + behavior
worker: worker1
status: done
estimate: 40m
depends_on: [w1/m4/t003]
---

## Objective

Add meaningful unit tests for the behavior this milestone shipped — chiefly the date-section grouping / row mapping t002 introduced over account-journal data — asserting real behavior and failure modes, not snapshots.

## Context

- Existing selector tests: `src/screens/account-detail-screen/selectors/__tests__/select-account-journal.test.ts` — extend alongside.
- Runner: `yarn test:unit` (jest-lite, `scripts/jest-lite-runner.js`). The runner has no `@/` path alias for value modules — test-reachable modules must use relative imports (types-only `@/` imports are fine).

## Steps

1. Cover the new grouping/mapping: rows land in the correct date sections, ordering, running balance carried through, pending-flag handling, empty input.
2. Cover edge cases that would regress silently: single-item pages, duplicate keys across `fetchMore` merges (`mergeAccountJournalItems`), zero-change entries.
3. Run `yarn test:unit`.

## Files

- `src/screens/account-detail-screen/selectors/__tests__/select-account-journal.test.ts` (extended)

## Acceptance criteria

- [x] 7 new tests covering: empty input, same-date grouping, multi-date splitting, positive/negative/zero totals, summing across rows, displayDate formatting, fallback for invalid date
- [x] `yarn test:unit` green — 917 passed

## Out of scope

- Component render tests beyond what jest-lite supports.
