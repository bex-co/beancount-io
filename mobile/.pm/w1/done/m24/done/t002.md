---
id: w1/m24/t002
title: Adopt the control token across ~24 surfaces; drop the workaround
worker: worker1
status: done
estimate: 45m
depends_on: [w1/m24/t001]
---

## Objective

Roll the t001 token through every surface that fills with `theme.black10`, and delete the per-screen border `w1/m17` added to the account picker as a workaround.

## Context

- ~24 `backgroundColor: theme.black10` surfaces, including `transactions-header.tsx`, `dashboard-card` (which already reaches for a `black20` hairline for exactly this reason), `picker`, `menu-button`, `ledger-drawer`, `text-input-screen`, `progress-bar`.
- Nine screens pass `placeholderTextColor={theme.black60}`.
- If `w1/m23/t003` landed first, `progress-bar.tsx` is already gone — check before editing it.
- Doing this screen by screen is what the note warns against: the app ends up with search fields that look different from each other and the token still broken.

## Steps

1. Replace ad-hoc fills/borders with the token pair at every site.
2. Delete the account picker's per-screen border.
3. Update the placeholder color at the nine sites.
4. Screenshot a representative set in light and dark before/after.

## Files

- The ~24 surfaces above, plus `src/screens/account-picker-screen/`

## Acceptance criteria

- [ ] No control defines its own boundary color inline
- [ ] The account-picker workaround is gone and the picker still reads correctly in both themes
- [ ] Before/after screenshots show a visible boundary in light mode

## Out of scope

- Redesigning any control's shape or size.
