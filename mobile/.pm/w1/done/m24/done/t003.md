---
id: w1/m24/t003
title: Extract `src/components/search-bar/` and adopt it
worker: worker1
status: done
estimate: 45m
depends_on: []
---

## Objective

One search bar component, used by both live copies, taking `{ value, onChangeText, placeholder, right? }`.

## Context

- Two live copies remain after `w1/m22/t001` deleted the dead journal header: `account-picker-screen.tsx` and `transactions-screen/transactions-header.tsx`.
- They have already drifted: height 36 vs 40, `fontSizes.lg` vs `md`, `black90` vs `black` text — the drift is the argument for the extraction.
- The shared props are the same in both: icon + `TextInput` with `returnKeyType="search"`, `clearButtonMode="while-editing"`, no autocorrect.
- `right?` exists so the transactions header can pass its filter button instead of forking the component.

## Steps

1. Create `src/components/search-bar/` with the shared shape; resolve each drifted value deliberately rather than averaging.
2. Adopt at both sites; delete the local copies.
3. Verify search still behaves on both screens, including clear-while-editing and the transactions filter button.

## Files

- `src/components/search-bar/` (new), `src/screens/account-picker-screen/`, `src/screens/transactions-screen/transactions-header.tsx`

## Acceptance criteria

- [ ] One search input implementation in the app
- [ ] Both screens keep their current behavior, including the filter button
- [ ] Verified in light and dark

## Out of scope

- Search _ranking_ behavior (`w1/m18`).
