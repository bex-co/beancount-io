---
id: w1/m24/t007
title: Simplify pass over the shared-primitive diff
worker: worker1
status: done
estimate: 25m
depends_on: [w1/m24/t006]
---

## Objective

Run `/simplify` over this milestone's diff — behavior-preserving. This milestone exists because of deferred simplify findings, so it should not create the next batch.

## Context

- Watch for: the search-bar props growing a flag per caller, the pills component accumulating booleans, and any call site still reaching around the new helper.
- Milestone diff: `src/common/theme/`, `src/components/search-bar/`, `src/components/time-range-pills/`, the picker, and the seven push sites.

## Steps

1. Run `/simplify` scoped to the milestone diff.
2. Apply accepted findings; re-run `yarn test`.

## Files

- Whatever the pass flags within the milestone diff.

## Acceptance criteria

- [ ] Findings applied or explicitly declined
- [ ] `yarn test` green; no behavior change

## Out of scope

- Bug hunting; new features.
