---
id: w1/m24/t006
title: UX pass — light/dark contrast, i18n, safe area
worker: worker1
status: done
estimate: 35m
depends_on: [w1/m24/t002, w1/m24/t003, w1/m24/t004, w1/m24/t005]
---

## Objective

Standing UX pass, with contrast as its headline: this milestone's whole point is that a control is visible, so verify that claim rather than assuming it.

## Context

- Theme tokens from `src/common/theme/`; never hard-code colors. `LoadingTile` resolves the effective theme via `useTheme()`.
- Strings live in the English base and render via `useTranslations()`.
- The picker and the transactions header are the highest-traffic surfaces touched here.

## Steps

1. Re-measure boundary-vs-surface contrast on a representative control per family (search field, chip, card, drawer) in **both** themes; record the numbers next to t001's.
2. Walk the account picker, transactions, quick-add and settings in light and dark.
3. Confirm any string the extraction moved still resolves, and check one long-label locale for the pills.
4. Confirm `SafeAreaView` spacing and loading backgrounds are unchanged.

## Files

- Everything touched by t002–t005

## Acceptance criteria

- [ ] Measured ≥3:1 boundary contrast in light mode on every control family
- [ ] No missing-translation fallback; pills verified in a long-label locale
- [ ] No hard-coded colors in the diff

## Out of scope

- Behavior changes — polish only.
