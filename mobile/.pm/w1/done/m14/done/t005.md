---
id: w1/m14/t005
title: UX pass — light/dark, i18n, loading bg, safe area, analytics
worker: worker1
status: done
estimate: 40m
depends_on: [w1/m14/t004]
---

## Objective

Standing closing pass over everything m14 shipped: bell + badge, notifications screen, commit detail.

## Context

- Conventions in `mobile/CLAUDE.md`: theme tokens via `useTheme()` (no hard-coded colors, no `themeVar === "dark"` comparisons), strings in the English base under `src/translations/` rendered via `useTranslations()`, background colors set on loading states, `SafeAreaView` spacing, analytics on new screen mounts.

## Steps

1. Sweep t002–t004 surfaces in light **and** dark (badge contrast, diff tinting, skeletons).
2. Confirm every new string exists in the English base and renders through `useTranslations()`.
3. Confirm loading states set background colors (no dark-mode flicker) and skeletons mirror loaded layouts.
4. Confirm `view_notifications`, `view_commit_detail`, and `tap_notifications_bell` fire.

## Files

- `src/components/ledger-drawer/ledger-drawer-header.tsx`
- `src/screens/notifications-screen/`, `src/screens/commit-detail-screen/`
- `src/translations/`

## Acceptance criteria

- [ ] All checks above pass in light and dark on device/simulator.
- [ ] `yarn lint` green.

## Out of scope

- New functionality.
