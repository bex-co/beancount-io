---
id: w1/m14/t007
title: Unit tests for notification list + badge behavior
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m14/t005]
---

## Objective

Meaningful unit tests under `src/__tests__/` for the behavior m14 shipped — real assertions on selectors/formatting logic and failure modes, not tautologies.

## Context

- Jest-lite runner (`scripts/jest-lite-runner.js`): value modules must use **relative imports** (`@/` works for type imports only); matchers limited to `toBe`/`toEqual`/`toBeCloseTo`/`toBeTruthy`/`toBeFalsy`/`toThrow` + `.not`.
- Testable seams: badge-count derivation (errors → count, failure → 0), error-row formatting (`filename:lineno` with null filename), diff-line classification (+/−/context), commit-row formatting (shortSha fallback to sha prefix).

## Steps

1. Extract pure helpers where needed so logic is test-reachable without React rendering.
2. Cover happy path + failure modes (null filename, empty diff, query error ⇒ count 0).
3. `yarn test:unit` green.

## Files

- `src/__tests__/` (new test files)
- Small extractions in `src/screens/notifications-screen/`, `src/screens/commit-detail-screen/`, `src/common/hooks/use-ledger-errors.ts`

## Acceptance criteria

- [ ] Tests assert real behavior and failure modes; `yarn test:unit` green.

## Out of scope

- Snapshot-everything or render tests beyond what jest-lite supports.
