---
id: w1/m12/t004
title: Tabular figures on every amount via the shared amount style
worker: worker1
status: done
estimate: 45m
depends_on: [w1/m12/t001]
---

## Objective

Every money amount in the app renders with `fontVariant: ["tabular-nums"]` via the shared `amountStyle` token, so stacked amounts align on the decimal point and stop wobbling as digits change.

## Context

- Already tabular (but 6 duplicated local styles to replace with the token): `src/screens/add-transaction-screen/add-transaction-screen.tsx:46,79`, `add-transaction-next-screen.tsx:43,52`, `multi-leg-screen.tsx:123,164`.
- Missing tabular figures today (amounts render with proportional digits): `src/screens/home-screen/components/spending-card.tsx`, `recent-transactions-card.tsx`, `src/components/dashboard-card/index.tsx`, `src/components/account-list/account-list.tsx`, `src/screens/account-detail-screen/components/account-entry-row.tsx`, `src/screens/account-detail-screen/account-detail-screen.tsx`, journal amount fragments (coordinate with t003).

## Steps

1. Sweep the files above: amount `Text` styles compose `amountStyle` from `@/common/theme`; delete the 6 local `fontVariant` declarations.
2. Grep for stragglers: any `Text` rendering a currency/number in `src/screens` + `src/components` not yet on the token.
3. Verify alignment visually where amounts stack (accounts list, home cards, multi-leg editor).

## Files

- `src/screens/add-transaction-screen/{add-transaction-screen,add-transaction-next-screen,multi-leg-screen}.tsx`
- `src/screens/home-screen/components/{spending-card,recent-transactions-card}.tsx`
- `src/components/dashboard-card/index.tsx`
- `src/components/account-list/account-list.tsx`
- `src/screens/account-detail-screen/account-detail-screen.tsx`
- `src/screens/account-detail-screen/components/account-entry-row.tsx`

## Acceptance criteria

- [ ] `grep -rn 'tabular-nums' src/screens src/components` shows only the theme token (plus composed usages, no locally re-declared style objects).
- [ ] Stacked amounts in the accounts list and multi-leg editor align on the decimal point in the simulator.

## Out of scope

- Font-size/weight normalization (t005); currency formatting logic.
