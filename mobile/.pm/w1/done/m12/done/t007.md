---
id: w1/m12/t007
title: Simplify pass over typography changes
worker: worker1
status: done
estimate: 20m
depends_on: [w1/m12/t006]
---

## Objective

Run `/simplify` over the code this milestone changed — reuse, simplification, efficiency; behavior-preserving.

## Context

- Prime candidates: leftover one-off style objects that now duplicate `amountStyle` or the scale; per-screen style fragments that collapsed to identical token compositions and could share a `StyleSheet` entry.

## Steps

1. `/simplify` scoped to the m12 diff.
2. Apply accepted findings; `yarn lint` + `yarn test:unit` green.

## Files

- Files changed by t001–t006

## Acceptance criteria

- [ ] `/simplify` run over the milestone diff; accepted findings applied; behavior unchanged; CI-equivalent (`yarn test`) green.

## Out of scope

- Bug hunting (use `/code-review`); refactors beyond the milestone's diff.
