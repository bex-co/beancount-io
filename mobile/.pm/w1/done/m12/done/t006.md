---
id: w1/m12/t006
title: UX pass — light/dark, i18n, loading bg, safe area, font scaling
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m12/t005]
---

## Objective

Standing closing pass over every screen the typography sweep touched, plus the milestone-specific check: system font scaling (accessibility text sizes) must not clip or misalign the new type.

## Context

- Conventions: theme tokens via `useTheme()`, English-base strings via `useTranslations()`, themed backgrounds on loading states, `SafeAreaView` spacing, analytics on new screen mounts (none expected — no new screens).
- Milestone-specific: JetBrains Mono is monoline and holds up in dark mode, but verify amounts use `text01`-level contrast, not dimmed grays; bump the OS text size (iOS Accessibility > Larger Text) and confirm fixed-`lineHeight` rows and skeletons don't clip.

## Steps

1. Walk home, journal (list + bottom sheet), accounts, account detail, and the add-transaction flows in light **and** dark on both platforms.
2. Repeat the journal + accounts walk at a larger accessibility text size; fix any clipped `lineHeight` containers.
3. Confirm no new hard-coded colors or strings entered with the sweep (this milestone should add zero new user-facing strings — verify).

## Files

- Screens touched by t003–t005 (verification pass; fixes land wherever found)

## Acceptance criteria

- [ ] Both themes verified on both platforms; no hard-coded colors.
- [ ] Larger accessibility text sizes render without clipping in journal rows and skeletons.
- [ ] No new strings bypassing `useTranslations()` / the English base.

## Out of scope

- New functionality.
