---
id: w1/m8/t001
title: Split-button component + Home wiring (Quick Add ▾ with anchored menu)
worker: worker1
status: done
estimate: 40m
depends_on: []
---

## Objective

Replace the single "Quick Add" `Button` on Home with an in-house split button: the left segment keeps today's exact one-tap behavior (keypad flow), and a visually attached ▾ segment opens a small anchored menu with "Quick add" and "Multi-leg entry". Built with a RN `Modal` positioned under the button — **no new dependency**.

## Context

- Current entry point: `src/screens/home-screen/home-screen.tsx` (~line 103) — a primary `Button` tracking `tap_quick_add`, setting `AddTransactionCallback.setFn(onRefresh)`, navigating to `/add-transaction`.
- Button component: `src/components/button/index.tsx`; export barrel `src/components/index.ts`.
- Theme tokens via `useTheme()` / `useThemeStyle` — no hard-coded colors (menu surface, pressed states, divider).
- Strings: add `multiLegEntry` (and any menu labels) to `src/translations/en.ts`, rendered via `useTranslations()`.

## Steps

1. Create `src/components/split-button/index.tsx`: primary action area + separated chevron segment; chevron opens a `Modal`-anchored menu (measure the anchor with `onLayout`/`measureInWindow`), items with icon + label, dismiss on backdrop tap. Theme tokens throughout.
2. Export it from `src/components/index.ts`.
3. In `home-screen.tsx`, replace the Quick Add `Button` with the split button. Left segment: unchanged behavior (`tap_quick_add` analytics, `AddTransactionCallback.setFn(onRefresh)`, navigate `/add-transaction`). Chevron: track `tap_quick_add_menu` on open; menu items "Quick add" (same as left segment) and "Multi-leg entry" (`tap_multi_leg_add`, set the same refresh callback, navigate `/add-transaction-multi` — screen lands in t002).
4. Add the new English strings.

## Files

- `src/components/split-button/index.tsx` (new)
- `src/components/index.ts`
- `src/screens/home-screen/home-screen.tsx`
- `src/translations/en.ts`

## Acceptance criteria

- [ ] Tapping the left segment behaves exactly as the old button (analytics + refresh callback + keypad flow).
- [ ] ▾ opens the anchored menu; backdrop tap dismisses; correct in light **and** dark with theme tokens only.
- [ ] "Multi-leg entry" fires `tap_multi_leg_add` and routes to `/add-transaction-multi`.
- [ ] New strings live in the English base and render via `useTranslations()`.

## Out of scope

- The multi-leg screen itself (t002+).
- Additional menu entries (AI/receipt capture — future milestones).
