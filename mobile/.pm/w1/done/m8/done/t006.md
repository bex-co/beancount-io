---
id: w1/m8/t006
title: UX pass — light/dark, i18n, loading bg, safe area, analytics
worker: worker1
status: done
estimate: 40m
depends_on: [w1/m8/t005]
---

## Objective

Standing closing pass over everything this milestone touched: the split button + menu on Home and the multi-leg screen.

## Context

- Conventions: theme tokens via `useTheme()` (no hard-coded colors), strings in the English base under `src/translations/` rendered via `useTranslations()`, backgrounds set on loading states (dark-mode flicker precedent on the account picker), `SafeAreaView` spacing, analytics on new screen mounts.

## Steps

1. Verify the split button, anchored menu, and multi-leg screen in light **and** dark — including the menu backdrop, pressed states, remainder footer states, and the legs skeleton/loading background.
2. Audit every new string: present in `src/translations/en.ts`, rendered through `useTranslations()` (not `i18n.t()` directly in components); add worthwhile overrides to other locales.
3. Check `SafeAreaView` edges on the multi-leg screen and keyboard behavior around the amount inputs.
4. Confirm analytics: `tap_quick_add_menu`, `tap_multi_leg_add`, `usePageView("add_transaction_multi")`, `tap_multi_leg_done` with `legCount`.

## Files

- `src/components/split-button/index.tsx`
- `src/screens/home-screen/home-screen.tsx`
- `src/screens/add-transaction-screen/multi-leg-screen.tsx`
- `src/translations/*.ts`

## Acceptance criteria

- [ ] No hard-coded colors; both themes verified by eye on device/simulator.
- [ ] All new strings localized via the English base + `useTranslations()`.
- [ ] Loading states have themed backgrounds; safe-area and keyboard behavior correct.
- [ ] All four analytics events fire.

## Out of scope

- New functionality.
