---
id: w1/m8/t008
title: Unit tests for multi-leg balancing, postings builder, validation
worker: worker1
status: done
estimate: 40m
depends_on: [w1/m8/t006]
---

## Objective

Add meaningful unit tests under `src/__tests__/` for the behavior this milestone shipped: remainder math, last-leg auto-fill, validation gating, and the `EntryInput` postings builder. Tests must assert real behavior and failure modes — no trivial, tautological, or snapshot-everything tests.

## Context

- Runner: `yarn test:unit` (jest-lite in `scripts/jest-lite-runner.js`). It resolves `@/` only for **type** imports — the pure helpers in `multi-leg-utils.ts` must be imported **relatively** from the test file.
- Test surface: `src/screens/add-transaction-screen/multi-leg-utils.ts` (kept pure in t003–t005 precisely so it is testable here).

## Steps

1. Remainder/auto-fill: sums in integer cents (0.10 + 0.20 exact), auto leg tracks edits to other legs, manual override stops tracking, remainder sign correctness.
2. Validation: unbalanced legs, missing account, zero-amount leg each block Done.
3. Postings builder: N legs → N postings with `"<signed> <CURRENCY>"` amount strings; postings sum to zero; date/payee/narration/flag mapped correctly.
4. Run `yarn test:unit` and keep it green.

## Files

- `src/__tests__/multi-leg-utils.test.ts` (new)

## Acceptance criteria

- [ ] Tests cover balancing math, auto-fill semantics, validation failure modes, and the postings builder.
- [ ] Value imports are relative (jest-lite `@/` limitation respected).
- [ ] `yarn test:unit` green.

## Out of scope

- Component/UI snapshot tests.
