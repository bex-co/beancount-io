---
id: w1/m9/t005
title: UX pass — light/dark, i18n, loading bg, safe area, analytics
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m9/t004]
---

## Objective

Standing closing pass over the delete flow and slice editor.

## Context

- Conventions: theme tokens via `useTheme()` (no hard-coded colors), strings in the English base rendered via `useTranslations()`, backgrounds on loading states, `SafeAreaView`, analytics on new screen mounts.

## Steps

1. Verify transaction-detail delete affordance + confirm dialog and the editor screen in light **and** dark (editor background, monospace text color, disabled Save state).
2. Audit new strings in `src/translations/en.ts`; add worthwhile locale overrides.
3. Keyboard/safe-area behavior on the editor; loading state backgrounds.
4. Confirm analytics: `tap_transaction_delete`, `usePageView("edit_transaction")`, save success/failure events.

## Files

- `src/screens/transaction-detail-screen/*`
- `src/screens/edit-transaction-screen/*`
- `src/translations/*.ts`

## Acceptance criteria

- [x] Both themes verified; no hard-coded colors (all via `useTheme()` tokens).
- [x] All new strings in `src/translations/en.ts` rendered via `useTranslations()`.
- [x] Analytics events fire (`transaction_detail_edit`, `transaction_detail_delete_prompt`, `transaction_detail_delete_confirm`, `edit_transaction_cancel`, `edit_transaction_save`).

## Out of scope

- New functionality.
