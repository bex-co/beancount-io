---
id: w1/m9/t007
title: Unit tests for edit/delete behavior
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m9/t005]
---

## Objective

Meaningful unit tests under `src/screens/edit-transaction-screen/__tests__/` for the behavior this milestone shipped — no trivial, tautological, or snapshot-everything tests.

## Context

- Runner: `yarn test:unit` (jest-lite) — resolves `@/` only for **type** imports; value modules must be imported **relatively**. Extract any pure logic (e.g. param/slice handling, dirty-state check gating Save) into plainly importable helpers.

## Steps

1. Test the reactive var: `editTransactionPayloadVar` stores/retrieves/clears the payload.
2. Test `openEditTransaction`: sets the var and calls `router.push("/edit-transaction")`.
3. Run `yarn test:unit` and keep it green.

## Files

- `src/screens/edit-transaction-screen/__tests__/open-edit-transaction.test.ts` (new)

## Acceptance criteria

- [x] Tests assert real behavior: payload storage, mutation, clearance.
- [x] Value imports are relative (jest-lite `@/` limitation respected).
- [x] `yarn test:unit` green (967 tests pass).

## Out of scope

- Component/UI snapshot tests.
