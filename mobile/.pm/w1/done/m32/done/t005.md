---
id: w1/m32/t005
title: Stop, inline error + retry, New chat
worker: worker1
status: done
estimate: 40m
depends_on: [w1/m32/t004]
---

## Objective

No dead-end states: a stop control while streaming, a failed turn rendered inline with one-tap retry, and a New chat action that resets both the visible conversation and the `sessionId`.

## Context

- `useChat` already exposes `stop` and `regenerate`; the hook (t003) surfaces `error`.
- Quota exhaustion (the AI-CFO monthly limit) is a distinct error: render it as an upgrade notice, not a retry — retrying a rate-limited request is noise. Mobile's subscription surface is the destination (`docs/ADR001-mobile-billing.md`).
- New chat regenerates the in-memory `sessionId` and reseeds the welcome message.

## Steps

1. Input bar swaps send → stop while `status` is submitted/streaming.
2. Error turn renders an inline bubble with retry; quota errors render the upgrade notice instead.
3. New chat action in the screen header; wires the hook's reset.

## Files

- `src/screens/agent-screen/` (screen + hook)
- `src/translations/en.ts` + locale files

## Acceptance criteria

- [ ] Stop aborts a live stream mid-answer; the partial text stays visible.
- [ ] A forced failure (airplane mode) shows the inline error; retry resubmits the same question.
- [ ] New chat clears the thread and changes the `sessionId` (assertable in t010).

## Out of scope

- Resumable streams; persisted history.

## Outcome — done 2026-08-19

Stop, inline error with retry, and New chat — all three verified on device.

- **Stop**: the send button becomes a stop button while streaming. Tapped mid-answer, the stream aborted, the partial text stayed on screen, and the send button returned.
- **Errors**: `agent-errors.ts` classifies from the JSON envelope t001 captured (`{"ok":false,"error":{"code":…}}`). Quota (`RATE_LIMITED`/429) gets the upgrade notice and **no retry** — retrying a refusal earns a second refusal; auth (`UNAUTHENTICATED`/401) likewise, since a retry cannot mint a token. Only the generic case offers the button.
- **New chat**: resets the visible thread _and_ the `sessionId`, after stopping any live stream.

**A fourth state turned up that was not in the plan.** A turn can finish having run tools and said nothing: the server caps the agent at ten steps, and "add a transaction" spent all ten reading files. The screen went silent — activity line, then nothing, forever. That is exactly the dead end the milestone's definition of done forbids, so it now renders its own notice and a retry (`agent-no-answer-notice`).
