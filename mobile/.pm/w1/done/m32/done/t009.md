---
id: w1/m32/t009
title: Simplify pass over the agent-chat diff
worker: worker1
status: done
estimate: 20m
depends_on: [w1/m32/t008]
---

## Objective

Run `/simplify` over the code this milestone changed — reuse, simplification, efficiency; behavior-preserving.

## Context

- Likely candidates: bubble components vs existing list primitives, the part-type rendering switch, transport option construction, duplicated state derivations between hook and screen.

## Steps

1. `/simplify` scoped to the m32 diff; apply what survives review.

## Files

- Whatever the pass touches within the m32 diff.

## Acceptance criteria

- [ ] Behavior unchanged (t008's walk still holds); `yarn test` green.

## Out of scope

- New features; bug hunting (that is `/code-review`'s job).

## Outcome — done 2026-08-19

A pass over the milestone's own diff, applied rather than filed:

- **Four notice blocks became one `Notice` component.** Approval, quota, error and no-answer all say a sentence and sometimes offer one way forward; as copies they had already drifted on padding within a single sitting.
- `scrollToEnd(animated: boolean)` was only ever called with `true` — the parameter went.
- `theme.nav02 ?? theme.black` — `nav02` is non-optional on `ColorTheme`, so the fallback was unreachable.
- `messageText` moved out of the component file into `message-text.ts`. That was structure, not cosmetics: the unit runner cannot load a module importing React Native, so the pure half was untestable where it sat.
- An orphaned comment that the notice refactor had separated from `awaitingApproval` was put back with the code it describes.

Behaviour unchanged: the device walk was repeated afterwards and the screen renders identically.
