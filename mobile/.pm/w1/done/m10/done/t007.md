---
id: w1/m10/t007
title: Unit tests for receipt-capture behavior
worker: worker1
status: done
estimate: 40m
depends_on: [w1/m10/t005]
---

## Objective

Meaningful unit tests under `src/__tests__/` for the receipt flow's logic — state machine transitions, input building, failure modes. No trivial or snapshot-everything tests.

## Context

- Runner: `yarn test:unit` (jest-lite) — `@/` resolves only for **type** imports; keep the capture state machine and the `InsertReceiptTransactionInput` builder as pure, relatively-importable modules.

## Steps

1. Test the capture state machine: acquire → upload → parse transitions; each failure (upload, parse, quota) lands in its distinct error state; cancellation resets cleanly.
2. Test the input builder: parsed proposal + user edits → `InsertReceiptTransactionInput` (amount number/currency splitting, posting mapping).
3. Test the no-write guarantee: no insert call is constructed before confirm.
4. `yarn test:unit` green.

## Files

- `src/__tests__/receipt-capture.test.ts` (new)
- Helper extraction under `src/screens/receipt-capture-screen/` as needed

## Acceptance criteria

- [ ] State machine and builder behavior covered including failure modes.
- [ ] Value imports are relative (jest-lite `@/` limitation respected).
- [ ] `yarn test:unit` green.

## Out of scope

- Component/UI snapshot tests; network mocking of S3.
