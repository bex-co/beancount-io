---
id: w1/m30/t009
title: Simplify pass over the milestone's diff
worker: worker1
status: done
estimate: 20m
depends_on: [w1/m30/t008]
---

## Objective

Run `/simplify` over the m30 diff — six locale files, the baseline module, and the integrity suite — for reuse, simplification, and efficiency, behavior preserving.

## Context

- After t007 the suite has three related concepts in one place: key parity, token parity, and value-identity. They share the same per-locale iteration and the same "compare against a documented exception list" shape, and were written at three different times.
- `known-gaps.ts` now holds one empty record and one growing list; check whether both still earn their own module or whether the empty baseline reads better beside the suite it guards.
- The six locale files were written by six separate passes: look for a group comment copied out of `en.ts` order, or the same English term rendered two ways within one file.

## Steps

1. Collapse duplicated per-locale iteration in `locale-integrity.test.ts` into one loop that reports each dimension separately, without merging the failure messages — a red build must still say which rule broke.
2. Re-read each of the six locale diffs for internal inconsistency in terminology.
3. Check the extractor is not re-reading and re-parsing the same locale file once per rule.
4. `yarn lint` / `yarn typecheck` / `yarn test:unit`.

## Files

- `src/translations/__tests__/locale-integrity.test.ts`
- `src/translations/__tests__/locale-parity.ts`
- `src/translations/__tests__/known-gaps.ts`
- `src/translations/{bg,ca,fa,nl,sk,uk}.ts`

## Acceptance criteria

- [ ] No behavior change: the same synthetic breakages from m29/t011 still fail, with messages that still name the rule and the locale.
- [ ] Each locale file is internally consistent in terminology.
- [ ] `yarn test:unit` green.

## Out of scope

- New rules; t007 was the last one.
