---
id: w1/m17/t003
title: "Search field: filter-as-you-type across all accounts"
worker: worker1
status: done
estimate: 40m
depends_on: [w1/m17/t002]
---

## Objective

Add a search input at the top of the picker that filters the entire account list — ignoring the active root-type chip — using `account-search.ts`, with a clear button and a localized placeholder. Localize the "All" chip label (hardcoded English today) and add the new keys to the English translation base, rendered via `useTranslations()`.

## Context

- Search-box pattern: `src/screens/transactions-screen/transactions-header.tsx:71-80`; substring-filter precedent: `src/components/text-input-screen/index.tsx:135-158`.
- "All" is hardcoded at `src/common/ledger-meta-utils.ts:65`.
- i18n convention: add keys to the English base under `src/translations/` (other locales extend/override); render via `useTranslations()`, never `i18n.t()` directly.

## Steps

1. Add the search field above the chips, reusing the app's search-box pattern; results replace the sectioned list, ordered by fuzzy score.
2. Clear button (and empty query) restores the browse view with chips/sections.
3. Add `accountPickerSearchPlaceholder` and an "All" label key to `src/translations/en.ts`; translate the other locales where straightforward.

## Files

- `src/screens/account-picker-screen/account-picker-screen.tsx`
- `src/common/ledger-meta-utils.ts`
- `src/translations/en.ts` (+ other locale files as translated)

## Acceptance criteria

- [ ] Typing filters across every root type regardless of the selected chip, ordered by `account-search` score
- [ ] Clearing the query restores the browse view
- [ ] Placeholder and "All" label come from `useTranslations()` with keys in the English base
- [ ] `yarn lint` / `yarn typecheck` pass

## Out of scope

- Frecency boost of search results (m18 t004)
