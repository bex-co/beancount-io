---
id: w1/m17/t009
title: Test coverage
worker: worker1
status: done
estimate: 40m
depends_on: [w1/m17/t007]
---

## Objective

Add meaningful unit tests for the behavior this milestone shipped, beyond t001's module tests: ranking order across realistic account sets, chip/root filtering logic, and any reshaped `ledger-meta-utils` helpers. Tests must assert real behavior and failure modes — no tautological or snapshot-everything tests.

## Context

- jest-lite cannot render components — extract logic under test into alias-free pure modules (the `suggestion-utils.ts` pattern) and test those with relative imports.
- Existing suites to extend: `src/common/__tests__/ledger-meta-utils.test.ts`, `src/common/__tests__/account-search.test.ts`.

## Steps

1. Identify shipped behavior lacking assertions (search-over-chip precedence, section grouping, selected-item lookup).
2. Extract any logic still trapped in the component; add tests.

## Acceptance criteria

- [ ] New tests fail if ranking order, root filtering, or selected-item lookup regress
- [ ] `yarn test:unit` green

## Out of scope

- Component render tests (unsupported by the runner)
