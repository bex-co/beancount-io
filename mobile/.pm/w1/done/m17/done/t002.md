---
id: w1/m17/t002
title: "Rebuild picker list: single virtualized list + root-type chips"
worker: worker1
status: done
estimate: 45m
depends_on: [w1/m17/t001]
---

## Objective

Replace the tab-bar + PagerView double render in the account-picker screen with a single virtualized list (FlatList/SectionList): horizontal root-type filter chips ("All" + one per root prefix), section headers by root type when unfiltered, and leaf-emphasis row styling (dim the parent path, bold the final segment — theme tokens only). Preserve the loading skeleton and explicit background colors in both themes.

## Context

- `src/screens/account-picker-screen/account-picker-screen.tsx` currently mounts every account roughly twice — once in an eagerly-mounted "All" pager page and again in its root-type page — on plain `ScrollView`s with no virtualization. Hundreds of accounts is the realistic beancount case.
- Root grouping helpers: `src/common/ledger-meta-utils.ts` (`getAccountsAndCurrency` bucket sort, `handleOptions` tab grouping by `split(":")[0]`).
- `CLAUDE.md` scar: set background colors on loading states — missing them caused dark-mode flicker on this exact screen; its skeleton is the reference implementation. Keep both properties.

## Steps

1. Swap the `Tabs`/`PagerView` structure for one virtualized list fed by the selected chip's filter (chip "All" = every account, sectioned by root type).
2. Style rows with the parent path dimmed and the leaf segment emphasized; keep the hairline divider and one-line truncation.
3. Verify light/dark rendering and the skeleton path (chip placeholders + loading tiles with explicit backgrounds).

## Files

- `src/screens/account-picker-screen/account-picker-screen.tsx`
- `src/common/ledger-meta-utils.ts` (only if grouping helpers need reshaping)

## Acceptance criteria

- [ ] Each account renders exactly once per screen state; the list is virtualized (no full-list `.map` in a ScrollView)
- [ ] Chips filter by root type; sections appear when "All" is active
- [ ] Loading state keeps explicit background colors in light and dark (no flicker)
- [ ] All four picker callers (add-transaction review, multi-posting, filters, add-budget) still receive selections

## Out of scope

- The search input itself (t003)
- Localizing the "All" label (t003)
