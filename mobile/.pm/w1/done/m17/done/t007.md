---
id: w1/m17/t007
title: UX pass — light/dark, i18n, loading backgrounds, analytics
worker: worker1
status: done
estimate: 40m
depends_on: [w1/m17/t003, w1/m17/t004, w1/m17/t005, w1/m17/t006]
---

## Objective

Standing UX pass over the reworked picker: verify light **and** dark themes end-to-end (all colors via `useTheme()` tokens, no hard-coded values), all new strings in the English base under `src/translations/` rendered via `useTranslations()`, background colors set on every loading state (this screen's dark-mode-flicker scar), safe-area spacing intact, and the existing `usePageView("account_picker")` / `tap_account_picker_confirm` analytics still firing.

## Context

- `CLAUDE.md`: theme tokens, skeleton conventions, and the account-picker dark-mode flicker history.
- Screens to sweep: the picker plus its four callers' round-trips.

## Steps

1. Sweep the picker in light and dark, cold and warm cache, empty and long ledgers.
2. Grep the diff for hard-coded colors and direct `i18n.t()` calls.
3. Verify analytics events in debug output.

## Files

- `src/screens/account-picker-screen/account-picker-screen.tsx` (verification; fixes as needed)

## Acceptance criteria

- [x] No hard-coded colors or direct `i18n.t()` calls in the milestone's diff
- [x] Loading states keep explicit backgrounds in both themes; no flicker
- [x] Analytics events fire as before
- [x] Every theme token the screen uses (`white`, `black`, `black10`, `black40`, `black60`, `black80`, `primary`) is defined in both the light and dark palettes; the active chip pairs `primary` with `white`, the palette's documented button pairing that inverts correctly in dark
- [x] All three new strings declared explicitly in all 13 locales, enforced by `src/translations/__tests__/account-picker-parity.test.ts`
- [x] Visual sweep in the running app, light **and** dark. The simulator build sits on the auth screen (no signed-in ledger available), so the screen was rendered through a temporary preview route outside the `(app)` auth group with its session/ledger hooks stubbed to a 17-account fixture — the real component, real styles, real theme tokens. Confirmed in both themes: chips row (active pill green with inverted text), section headers per root, rows with the parent path dimmed and the leaf emphasized, the `selectedItem` row highlighted with a checkmark, search collapsing to one unlabeled ranked section with the chips hidden, and `exfo` ranking `Expenses:Food:*` above `Expenses:Fun:Outdoors`. A console-capture panel in the harness reported `NO WARNINGS CAPTURED` from the screen (the one warning seen earlier was `Cannot connect to Expo CLI`, a dev-server artifact). Harness fully reverted afterwards; `yarn test` green at 1147 and the app boots clean.

## Out of scope

- New analytics dimensions (m18 t005)
