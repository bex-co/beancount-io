---
id: w1/m15/t009
title: UX pass — light/dark, i18n, loading bg, safe area, analytics
worker: worker1
status: done
estimate: 40m
depends_on: [w1/m15/t008]
---

## Objective

Standing UX gate over everything m15 shipped: file browser, editor screen, highlighting, keyboard bar, error strip.

## Context

- Verify light **and** dark on every new surface — colors only via `useTheme().colorTheme` (never compare `themeVar` to `"dark"`); pay attention to the editor background, selection color, keyboard bar, and skeleton loading states (background colors set — dark-mode flicker bit the account picker before).
- All new strings in the English base (`src/translations/en.ts`) and rendered via `useTranslations()` — file browser labels, editor save/conflict/discard dialogs, error strip, key bar accessibility labels.
- `SafeAreaView` spacing on the new editor screen; keyboard bar clears the home indicator when the keyboard is closed.
- Analytics tracking on new screen mounts (file browser view within the ledger tab, editor screen), matching existing screen conventions.

## Steps

1. Sweep both themes across browser/editor/bar/error strip; fix token misuse.
2. Audit strings + `useTranslations()` usage; fill the English base.
3. Confirm safe-area and analytics mounts.

## Files

- `src/screens/ledger-screen/`, `src/screens/ledger-file-editor-screen/`, `src/components/code-editor/`, `src/translations/en.ts`

## Acceptance criteria

- [ ] No hard-coded colors in m15 code; both themes verified on device/simulator; all new strings translated via the hook; analytics fire on mount.

## Out of scope

- Translating the 12 non-English locales (they extend the base as needed).
