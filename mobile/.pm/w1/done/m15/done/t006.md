---
id: w1/m15/t006
title: Beancount keyboard accessory bar (sticky above the keyboard, both platforms)
worker: worker1
status: done
estimate: 40m
depends_on: [w1/m15/t005]
---

## Objective

A sticky bar above the keyboard with the keys beancount typing actually needs — today's date, `:`, `"`, `#`, `^`, and a two-space indent — inserted at the cursor, on iOS **and** Android.

## Context

- The bar is **native RN** (not in-page HTML): `react-native-keyboard-controller`'s `KeyboardStickyView` (approved/installed in t003) pins it above the keyboard on both platforms; t003 already hides the iOS webview's default accessory bar (`hideKeyboardAccessoryView`) so ours is the only bar.
- Insert-at-cursor crosses the native↔DOM boundary: call the `insertAtCursor(text)` command t003 exposed on `CodeEditor`, which dispatches a CM6 transaction at the current selection — CodeMirror owns selection state, so no native-side tracking is needed.
- Keys (left→right): `2026-07-14`-style today's date (`YYYY-MM-DD` + trailing space), `:` (account hierarchy), `"` (narration/payee), `#`, `^`, `⇥` two-space indent. Bar uses theme tokens, works in light and dark, and hides when the keyboard does.

## Steps

1. Build `keyboard-bar.tsx`: horizontal row of monospace key buttons in a `KeyboardStickyView`, haptic on tap.
2. Wire key taps to `CodeEditor`'s `insertAtCursor(text)`; verify taps on the native bar don't blur the webview editor (keyboard must stay open — if it blurs, re-focus via a DOM-side command).
3. Mount it on the editor screen (t005) only; verify on iOS + Android that the bar tracks the keyboard (including keyboard hide/show and rotation).

## Files

- `src/components/code-editor/keyboard-bar.tsx` (new)
- `src/components/code-editor/index.tsx` (`insertAtCursor` + selection ref)
- `src/screens/ledger-file-editor-screen/` (mount)

## Acceptance criteria

- [ ] Each key inserts at the cursor (not the end) with the keyboard staying open, on both platforms; date key inserts today's date in `YYYY-MM-DD`; bar themed correctly in light and dark.

## Out of scope

- Account-name autocomplete chips; undo/redo buttons.
