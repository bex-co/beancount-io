---
id: w1/m15/t008
title: "Retire the webview: migrate notifications deep link, remove dashboard-webview"
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m15/t002, w1/m15/t006, w1/m15/t007]
---

## Objective

Make the native path the only path: point the notifications error deep link at the native editor, drop the `fileUrl` webview branch from the ledger screen, and remove `DashboardWebview` if nothing else uses it.

## Context

- `src/screens/notifications-screen/notifications-screen.tsx:30` (`buildFileEditUrl`) + `:155` currently build a webview URL from `filename` + `lineno` and push the ledger tab with `{ fileUrl }`. Replace with a push to the native editor route with `{ path, line }` (normalize the error's absolute filename to a ledger-relative path — same normalization as t007).
- `src/screens/ledger-screen/ledger-screen.tsx` keeps a `fileUrl` param branch rendering `DashboardWebview`; remove the branch and the param.
- `DashboardWebview` (`src/components/dashboard-webview/`) is imported only by the ledger screen and exported from `src/components/index.ts` — verify no other usages (including of `react-native-webview` elsewhere) before deleting; if truly unused, delete the component and its export. Leave the `react-native-webview` dependency removal decision to the user (dependency change — ask).

## Steps

1. Migrate the notifications deep link and delete `buildFileEditUrl`.
2. Strip the `fileUrl` branch from `ledger-screen.tsx`.
3. `grep` for remaining `dashboard-webview` / `WebView` usages; delete the component if orphaned; ask the user about dropping `react-native-webview` from `package.json`.

## Files

- `src/screens/notifications-screen/notifications-screen.tsx`
- `src/screens/ledger-screen/ledger-screen.tsx`
- `src/components/dashboard-webview/` (delete if orphaned) + `src/components/index.ts`

## Acceptance criteria

- [ ] Tapping an error in Notifications opens the native editor at the right file and line; no code path in the app renders the ledger webview; `yarn lint` + `yarn typecheck` green.

## Out of scope

- Removing `react-native-webview` from `package.json` without user sign-off.
