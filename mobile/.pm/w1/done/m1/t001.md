---
id: w1/m1/t001
title: DashboardCard container + card-based home layout skeleton
worker: worker1
status: done
estimate: 45m
depends_on: []
---

## Objective

Introduce a reusable `DashboardCard` wrapper (Monarch-style rounded card with a header row: title, optional "see all →" affordance, optional right-side actions slot) and re-scaffold `home-screen.tsx` so its sections become stacked cards. This task delivers the visual chrome and layout that t003–t005 fill in; it must not lose any data the current screen shows.

## Context

- `src/screens/home-screen/home-screen.tsx` — current flat `ScrollView` with `SmallHeaderText`/`HeaderText` sections (net assets, accounts, quick add, monthly net income bar, monthly net worth line).
- `src/screens/home-screen/text-styled.tsx` — `HeaderText` / `SmallHeaderText`.
- `src/components/` — existing reusable components (`Button`, `List`, `LoadingTile`); follow their folder/index convention.
- Theme + i18n: `useTheme().colorTheme`, `useThemeStyle(getStyles)`, `useTranslations()`; screen util `contentPadding` in `src/common/screen-util.ts`.

## Steps

1. Create `src/components/dashboard-card/index.tsx`: a card with `theme` tokens for background (e.g. a subtle surface over `theme.white`), rounded corners, padding; props `title?`, `onSeeAll?` (renders a right-aligned chevron affordance when set), `right?` (actions slot), `children`. No hard-coded colors.
2. Refactor `home-screen.tsx` to render the existing content inside stacked `DashboardCard`s (keep net assets, accounts, quick-add, and the two charts working) so the screen reads as cards rather than flat sections. Keep the `RefreshControl` + `onRefresh` wiring intact.
3. Add any new strings (e.g. `seeAll`) to `src/translations/en.ts` and render via `useTranslations()`.

## Files

- `src/components/dashboard-card/index.tsx` (new)
- `src/screens/home-screen/home-screen.tsx`
- `src/translations/en.ts`

## Acceptance criteria

- [ ] Home tab renders each section inside a `DashboardCard`; no regression to net assets, accounts, quick-add, or the two existing charts.
- [ ] `DashboardCard` uses only theme tokens (verified in light and dark) and localized strings.
- [ ] Pull-to-refresh still refetches and shows loading skeletons.

## Out of scope

- The swipeable carousel (t002), and the specific new cards (t003–t005). This task only establishes the card container + layout.
