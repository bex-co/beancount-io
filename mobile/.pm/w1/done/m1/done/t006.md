---
id: w1/m1/t006
title: UX pass — light/dark, i18n, loading bg, safe area, analytics
worker: worker1
status: done
estimate: 40m
depends_on: [w1/m1/t003, w1/m1/t004, w1/m1/t005]
---

## Objective

Standing UX pass over every user-facing screen/component this milestone added or changed, verifying the cross-cutting quality bar before the milestone is called done.

## Context

- New/changed UI: `src/components/dashboard-card/`, `src/components/paged-carousel/`, `src/screens/home-screen/components/account-charts-card.tsx`, `recent-transactions-card.tsx`, `spending-card.tsx`, and `src/screens/home-screen/home-screen.tsx`.
- Standards: theme tokens via `useTheme()` (no hard-coded colors); all strings in `src/translations/en.ts` base and rendered via `useTranslations()`; loading states set a background color; `SafeAreaView` spacing; analytics on screen mount.

## Steps

1. Render the dashboard in **light and dark** — confirm every color is a theme token; check the swipe-dots, card surfaces, chart axes, and transaction rows in both.
2. Confirm every new string is in the English base file and rendered through `useTranslations()` (no raw `i18n.t()` and no literal user-facing text).
3. Confirm every loading skeleton (`LoadingTile`) sits on a card/screen background color — no dark-mode flicker (the account-picker regression pattern).
4. Confirm `SafeAreaView` edges/spacing are intact after the layout refactor.
5. Confirm the home screen still tracks a page-view on mount (`usePageView("home")`) and add tracking for meaningful new interactions if warranted (e.g. carousel page change, "see all" tap) via `analytics.track(...)`.

## Files

- All files listed in Context (review/adjust as needed).

## Acceptance criteria

- [ ] Dashboard verified correct in light and dark with only theme tokens.
- [ ] All new strings localized via the English base + `useTranslations()`.
- [ ] Loading states have background colors; `SafeAreaView` spacing intact; analytics on mount present.

## Out of scope

- New features; this is a quality/verification pass only.
