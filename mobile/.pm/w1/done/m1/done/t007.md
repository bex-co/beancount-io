---
id: w1/m1/t007
title: Simplify pass over dashboard code
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m1/t006]
---

## Objective

Standing simplify pass: run `/simplify` over the code this milestone changed — reuse, simplification, efficiency, altitude — behavior-preserving. No new features, no bug hunting.

## Context

- Scope: `src/components/dashboard-card/`, `src/components/paged-carousel/`, `src/screens/home-screen/` (the new card components, new selectors, and the refactored `home-screen.tsx`).
- Watch for: duplicated chart/selector logic that should share `select-chart-array-helper.ts`; a `PagedCarousel` that overlaps with the existing `src/components/tabs/` pager and could share code; repeated currency-formatting/loading-skeleton patterns across the new cards.

## Steps

1. Run `/simplify` over the changed files.
2. Consolidate duplicated selector/formatting/loading patterns; prefer existing helpers (`select-chart-array-helper.ts`, `getCurrencySymbol`, `LoadingTile`).
3. Re-run `yarn lint` and `yarn typecheck`; keep behavior identical.

## Files

- The milestone's changed files (see Context).

## Acceptance criteria

- [ ] `/simplify` applied; duplication reduced with no behavior change.
- [ ] `yarn lint` and `yarn typecheck` pass.

## Out of scope

- Behavior changes, new features, bug fixes unrelated to simplification (use `/code-review` for bugs).
