---
id: w1/m1/t005
title: Spending card — this month vs last month
worker: worker1
status: done
estimate: 50m
depends_on: [w1/m1/t001]
---

## Objective

Add a "Spending" card (Monarch's "This month vs. last month") that compares total spending in the current month against the previous month, so users immediately see whether they're spending more or less. Renders inside a `DashboardCard`.

## Context

- Card chrome from t001 (`src/components/dashboard-card/`).
- Chart: reuse `src/common/d3/bar-chart-d3.tsx` (`BarChartD3`), extending it (or a small sibling) to show two labelled bars ("This month" / "Last month"); keep `d3` + `react-native-svg` — no new dependency.
- **Data gap (flag):** existing queries do not expose a pure monthly _spending_ (expenses-only) series. `homeCharts` returns **net profit** (income − expenses), not spending; `accountHierarchy` gives a current expenses _balance_ snapshot, not per-month windows. Resolve in this order, and note the choice in the PR:
  1. Preferred: if `homeCharts` (or another existing resolver) can return an expenses-only monthly series, use it via a new `.graphql` query file + `yarn codegen` (do **not** hand-edit `src/generated-graphql/`).
  2. If backend support is missing, this needs a **backend/schema change** — surface it to the user (repo rule: flag work that needs backend/new capabilities) rather than faking data. As an interim, derive current-vs-previous-month expense totals client-side from `journalEntries` (sum expense-account postings per month), clearly labelled, and leave a TODO for the proper series.
- Month math: use the app's existing date utilities if present under `src/common/`; otherwise compute month boundaries locally (no new dependency).

## Steps

1. Decide the data path per the flag above; if adding a query, create the `.graphql` file under `src/common/graphql/queries/` and run `yarn codegen`.
2. Add `src/screens/home-screen/selectors/select-spending-compare.ts`: return `{ thisMonth, lastMonth }` totals (+ labels) in the active currency.
3. Build `src/screens/home-screen/components/spending-card.tsx`: a `DashboardCard` titled `t("spending")` with the two-bar comparison and a "this month vs last month" subtitle.
4. Mount in `home-screen.tsx` below the recent-transactions card; wire into `onRefresh` if it refetches.
5. Add `spending` + subtitle strings to `src/translations/en.ts`.

## Files

- `src/screens/home-screen/selectors/select-spending-compare.ts` (new)
- `src/screens/home-screen/components/spending-card.tsx` (new)
- `src/common/d3/bar-chart-d3.tsx` (extend, if needed)
- `src/common/graphql/queries/*.graphql` + regenerated `src/generated-graphql/` (only if a new query is added, via `yarn codegen`)
- `src/screens/home-screen/home-screen.tsx`
- `src/translations/en.ts`

## Acceptance criteria

- [ ] Card shows this-month vs last-month spending with correct currency formatting and a clear which-is-which legend/labels.
- [ ] Data source is honest: either a real expenses series (via codegen, not hand-edited generated files) or a clearly-labelled client-side computation with the backend gap surfaced to the user — no fabricated numbers.
- [ ] Correct in light and dark; loading skeleton with background color.

## Out of scope

- Full budgets/recurring cards from the Monarch reference (separate future milestone); per-category month-over-month drilldown.
