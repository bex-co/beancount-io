---
id: w1/m1/t008
title: Unit tests for dashboard selectors + behavior
worker: worker1
status: done
estimate: 40m
depends_on: [w1/m1/t006]
---

## Objective

Standing test-coverage task: add meaningful unit tests under `src/__tests__/` for the behavior this milestone shipped. Tests assert real behavior and failure modes — no trivial, tautological, or snapshot-everything tests.

## Context

- Runner: `yarn test:unit` (custom Jest-lite runner in `scripts/`); existing tests live in `src/__tests__/`.
- Highest-value, pure logic to cover: `select-spending-by-category.ts` (top-N + "Other" bucket, currency fallback, string/number balances, missing data), `select-spending-compare.ts` (this/last-month split, empty input), and the existing `select-account-totals.ts` edge cases if not already covered.

## Steps

1. Add tests for `select-spending-by-category`: correct top-N ordering, remainder folded into "Other", missing-currency fallback, and empty/undefined `AccountHierarchy` returning zeros.
2. Add tests for `select-spending-compare`: correct current-vs-previous-month totals, month-boundary handling, and empty input.
3. If the `PagedCarousel` page-index logic is unit-testable in isolation, cover page-change bookkeeping; otherwise keep tests on the pure selectors.
4. Run `yarn test:unit` and ensure green.

## Files

- `src/__tests__/select-spending-by-category.test.ts` (new)
- `src/__tests__/select-spending-compare.test.ts` (new)
- Additional test files as needed.

## Acceptance criteria

- [ ] New tests assert real behavior and failure modes (ordering, bucketing, currency fallback, empty data), not tautologies.
- [ ] `yarn test:unit` passes.

## Out of scope

- E2E/UI-snapshot testing; testing third-party libraries (`react-native-pager-view`, `d3`).
