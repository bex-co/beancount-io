---
id: w1/m33/t006
title: "Wire into the Reports screen; reconcile totals with the income statement"
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m33/t005]
---

## Objective

Mount the Sankey on the Reports screen in its decided position, with a translated section header, and prove the numbers: for the same time range, the flow's income and expense totals must equal what the income statement section of Reports shows. A pretty chart with different numbers is worse than no chart.

## Context

- `src/screens/reports-screen/reports-screen.tsx` — the combined dashboard m3 shipped; the Sankey joins its card stack.
- Both the Sankey (t003) and the existing report read from `useIncomeStatementQuery`, so a mismatch means a transformer bug, not a data race.
- Verification is headless: `xcrun simctl` screenshots + the expo-mcp tap driver (see `mobile/CLAUDE.md`) against the production ledger, read-only.

## Steps

1. Add the section (header + `CashFlowSankey`) to the Reports card stack.
2. Pick three ranges on a real ledger; compare the Sankey totals against the income-statement figures on the same screen.
3. Screenshot light + dark for the milestone record.

## Files

- `src/screens/reports-screen/reports-screen.tsx`
- `src/translations/en.ts` (section header key, + locales)

## Acceptance criteria

- [ ] Sankey totals equal income-statement totals for three different ranges on a real ledger
- [ ] Section renders in both themes with no layout overflow at 402pt

## Out of scope

- A Home-screen card for cash flow (would be a new inbox note); iPad layout
