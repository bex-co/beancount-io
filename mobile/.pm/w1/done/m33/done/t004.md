---
id: w1/m33/t004
title: "Render the Sankey: nodes + curved ribbons on react-native-svg"
worker: worker1
status: done
estimate: 90m
depends_on: [w1/m33/t003]
---

## Objective

The chart itself: run `d3-sankey`'s layout over t003's `{ nodes, links }` and draw node rects, curved link ribbons, and labels with `react-native-svg`, in the orientation and label strategy t001 decided. All colors from theme tokens (port the intent of the web's `sankey-colors.ts` onto the token palette — never hard-code).

## Context

- `react-native-svg` is already installed (the d3 charts use it).
- Layout: `sankey().nodeWidth().nodePadding().extent()`; ribbon paths via `sankeyLinkHorizontal()` (or a hand-built cubic if t001 chose vertical flow — d3-sankey's geometry transposes).
- Existing chart conventions to match: `src/common/d3/` components, `chart-chrome.tsx` (error boundary, placeholder, legend — reuse, don't copy; that duplication is what `w1/028` exists to shrink).
- Theme: `useTheme().colorTheme`; verify both themes as you go, not at the end.

## Steps

1. `src/screens/reports-screen/components/cash-flow-sankey.tsx`: measure available width, run the layout, render rects + ribbon `Path`s + labels per t001.
2. Map category colors from theme tokens; keep one mapping function, no inline colors.
3. Ribbon opacity/press feedback consistent with the other charts' touch behavior.
4. Wrap in the shared chart chrome (error boundary + placeholder) from `chart-chrome.tsx`.

## Files

- `src/screens/reports-screen/components/cash-flow-sankey.tsx` (new)
- `src/common/d3/chart-chrome.tsx` (reuse; touch only if a prop is genuinely missing)

## Acceptance criteria

- [ ] A real ledger's flows render legibly at 402pt width in the t001 orientation, light and dark
- [ ] No hard-coded colors (`grep` for hex literals in the new files comes back empty)
- [ ] Labels truncate per t001's strategy instead of overlapping

## Out of scope

- Loading/empty states and draw-in motion (t005); screen integration (t006)
