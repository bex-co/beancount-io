---
id: w1/m28/t001
title: Shared tap affordance in the `DashboardCard` header
worker: worker1
status: done
estimate: 30m
depends_on: []
---

## Objective

Give `DashboardCard` an optional tap-through contract — an `onPress` (or `href`) prop that renders a chevron in the header and makes the affordance consistent — and adopt it in the two cards that already navigate.

## Context

- Shared component: `src/components/dashboard-card/index.tsx`.
- Existing navigators to unify: `src/screens/home-screen/components/recent-transactions-card.tsx:69` (`router.navigate("/transactions")`) and `budget-card.tsx:78` (`router.push("/budget")`) — each currently hand-rolls its tap.
- Chevron uses theme tokens (`useTheme()`); hit target ≥ 44pt; the affordance must not fight card-internal touchables.

## Steps

1. Add the optional prop; render the chevron only when it is set.
2. Migrate recent-transactions and budget cards onto it, deleting their bespoke header taps.
3. Verify unchanged navigation behavior for both.

## Files

- `src/components/dashboard-card/index.tsx`
- `src/screens/home-screen/components/recent-transactions-card.tsx`
- `src/screens/home-screen/components/budget-card.tsx`

## Acceptance criteria

- [ ] Both existing doors render the shared chevron and still navigate as before.
- [ ] A card without the prop shows no affordance and gains no touchable.

## Out of scope

- New destinations (t002/t003); analytics shape (t004).
