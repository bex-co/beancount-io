---
id: w1/m37/t004
title: Recurring section + badges in the directory
worker: worker1
status: done
estimate: 45m
depends_on: [w1/m37/t001, w1/m37/t002, w1/m37/t003]
---

## Objective

The directory grows a pinned "Recurring" section above the full list: cadence label ("Monthly"), typical amount with "~" when approximate, next-expected date, and an overdue indicator — Monarch's four-state calendar language reduced to what a list can carry. Rows elsewhere get a small RECURRING badge (Monarch web parity).

## Context

- Section grouping precedent: `SectionList` usage in the directory (m35/t004); localized relative dates via the locale-aware date formatting shipped 2026-08-18 (`w1/027` outcome).
- Overdue is semantic state — encode it in form (badge/tint via theme tokens), not color alone.

## Steps

1. Compose rollups + series detection + overrides into section view-model (pure selector + tests).
2. Recurring row variant of `merchant-row.tsx`; badge on regular rows.
3. Analytics: section impressions are not needed; keep `merchants_tap_row` with a `recurring: boolean` prop.
4. New strings (cadence labels, "next", "overdue", section title) through the translation gate.

## Files

- `src/screens/merchants-screen/merchants-screen.tsx`, `merchant-row.tsx`
- `src/screens/merchants-screen/selectors/` (section view-model)
- `src/translations/en.ts` + locales

## Acceptance criteria

- [ ] Dev ledger's real subscriptions appear pinned with sensible cadence/amount/next-date; search still reaches them; sort applies within the general section.
- [ ] Overdue state renders distinctly in light and dark.
