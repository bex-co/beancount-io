---
id: w1/m5/t003
title: Remove the Settings tab + reference sweep
worker: worker1
status: done
estimate: 25m
depends_on: [w1/m5/t002]
---

## Objective

Drop the Settings tab so the tab bar has four entries (Home, Accounts, Journal, Ledger), now that the drawer provides the only entry point to `/settings`.

## Context

- `app/(app)/(tabs)/_layout.tsx`: remove the `<Tabs.Screen name="setting" …>` block and the `setting` key from the `tabTitles` state/effect.
- Delete the tab route file `app/(app)/(tabs)/setting.tsx` (verify exact filename with `ls`).
- Sweep for references: `grep -rn "setting" app/ src/ --include="*.tsx" -l` and check `router.push`/`navigate`/`Redirect`/deep links targeting the old tab path (`/(app)/(tabs)/setting` or `/setting`); update them to `/settings`.

## Steps

1. Delete the tab route file and the `Tabs.Screen` block + `tabTitles.setting` entries.
2. Fix any references found by the sweep (logout/back flows especially).
3. Run the app: confirm 4 tabs, drawer → Settings still works, and the `setting` i18n key is either still used (drawer/nav title) or intentionally retired.

## Files

- `app/(app)/(tabs)/_layout.tsx`
- `app/(app)/(tabs)/setting.tsx` (delete)
- any files surfaced by the reference sweep

## Acceptance criteria

- [ ] Tab bar shows exactly Home, Accounts, Journal, Ledger.
- [ ] No navigation call or deep link still targets the removed tab route; `yarn lint`/`yarn typecheck` green.
- [ ] Settings remains fully reachable via the drawer.

## Out of scope

- Re-evaluating the Ledger tab (inbox `w1/001.md`) and `lazy` tab mounting (inbox `w1/002.md`).
