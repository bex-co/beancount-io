---
id: w1/m5/t005
title: Simplify pass over drawer/settings navigation code
worker: worker1
status: done
estimate: 20m
depends_on: [w1/m5/t004]
---

## Objective

Run `/simplify` over the code this milestone changed — reuse, simplification, efficiency; behavior-preserving only.

## Context

- Standing closing task. Candidate targets: drawer menu-row markup vs the ledger list rows (shared row component?), leftover header/inset props from t001, dead code from the removed tab.

## Steps

1. Run `/simplify` scoped to the milestone's diff.
2. Apply accepted findings; re-run `yarn lint` and `yarn test:unit`.

## Files

- Files changed by t001–t003.

## Acceptance criteria

- [ ] `/simplify` findings applied or explicitly declined; behavior unchanged; checks green.

## Out of scope

- New features or bug hunts (`/code-review` is separate).
