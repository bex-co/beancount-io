---
id: w1/m5/t006
title: Unit tests for behavior this milestone shipped
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m5/t004]
---

## Objective

Add meaningful unit tests for behavior introduced by this milestone; `yarn test:unit` green.

## Context

- Standing closing task. This milestone is mostly navigation wiring, so extract-and-test where a pure seam exists rather than forcing UI tests: e.g. if t002 introduces a menu-items selector/helper, test it; extend the translations key test for any new key. jest-lite constraint: test-reachable value modules must use **relative** imports (see memory note).
- Do not add tautological or snapshot-everything tests; if no meaningful pure seam exists after t001–t003, record that in the task and close with the translations-key coverage only.

## Steps

1. Identify pure seams created by t001–t003 (helpers/selectors).
2. Write tests under the appropriate `__tests__/` directory with relative imports.
3. `yarn test:unit` green.

## Files

- `src/**/__tests__/` (new/extended test files)

## Acceptance criteria

- [ ] Tests assert real behavior (navigation-target building, menu-item selection, key presence) and fail if the behavior regresses.
- [ ] `yarn test:unit` green.

## Out of scope

- E2E/simulator automation (covered ad hoc via expo-mcp driving).
