---
id: w1/m5/t004
title: UX pass — light/dark, i18n, safe area, analytics
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m5/t003]
---

## Objective

Verify the drawer menu section and the pushed Settings screen meet the app's UX bar in both themes and all the standing checks.

## Context

- Standing closing task for UI-facing milestones.
- Watch specifically: the drawer menu section colors (theme tokens via `useTheme().colorTheme`, no hard-coded colors), the pushed screen's top inset after the t001 header reconciliation, and the purple `AccountHeader` banner in dark mode without the hamburger.

## Steps

1. Light **and** dark walk-through: open drawer → Settings row → pushed screen → each settings flow → back.
2. Confirm all new strings live in `src/translations/en.ts` and render via `useTranslations()` (no `i18n.t()` direct calls).
3. Confirm loading states have background colors; `SafeAreaView` spacing correct on the pushed screen; no dark-mode flicker.
4. Confirm analytics: `usePageView("mine")` still fires on the pushed screen; `drawer_open_settings` fires from the drawer row.

## Files

- `src/components/ledger-drawer/ledger-drawer.tsx`
- `src/screens/setting/*`
- `app/(app)/settings.tsx`

## Acceptance criteria

- [ ] Both themes verified on a notch/Dynamic Island simulator; no hard-coded colors introduced by this milestone.
- [ ] All new strings localized via the English base; analytics events verified in the debug log.

## Out of scope

- Code-quality changes (t005) and tests (t006).
