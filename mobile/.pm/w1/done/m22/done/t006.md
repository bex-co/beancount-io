---
id: w1/m22/t006
title: UX pass — first visit of every tab, light **and** dark
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m22/t005]
---

## Objective

Standing UX pass, scoped to what actually changed: the first mount of each tab under lazy mounting, and any surface that lost code in t001–t004.

## Context

- Theme tokens from `src/common/theme/`; `LoadingTile` resolves the effective theme via `useTheme()` — never compare `themeVar` to `"dark"`, it can hold `"system"`.
- Strings live in the English base under `src/translations/` and render via `useTranslations()`.
- This milestone should add no new strings; if t001 deleted `journal*` keys, confirm no locale still overrides them.

## Steps

1. Cold-start in light and dark, visit all five tabs in turn, and watch the first paint of each.
2. Confirm loading states keep background colors and no view shifts when data lands.
3. Confirm no locale file references a translation key deleted in t001.
4. Confirm the Home carousel and account detail still scrub after t004.

## Files

- `app/(app)/(tabs)/_layout.tsx`, `src/translations/**`, the surfaces touched by t001–t004

## Acceptance criteria

- [ ] All five tabs verified on first visit in light and dark
- [ ] No missing-translation fallback anywhere
- [ ] No layout shift on any skeleton→content handoff

## Out of scope

- Behavior changes — polish and verification only.

## Outcome

Walked all five tabs in the iOS simulator (iPhone 17 Pro, dev build) in **light and dark**, cold-starting in each theme so every tab was mounting for the first time:

| tab          | light                                         | dark                                 |
| ------------ | --------------------------------------------- | ------------------------------------ |
| Home         | loaded: net-worth chart + recent transactions | themed skeletons, correct background |
| Accounts     | themed row skeletons                          | loaded: full account tree            |
| Transactions | themed row skeletons + search bar             | loaded: sectioned entries            |
| Reports      | loaded: chart + breakdowns                    | themed chart/card skeletons          |
| Files        | loaded: `main.bean`, `zeal.bean`              | themed row skeletons                 |

No missing-translation fallback appeared anywhere, no untinted background flashed on any first mount, and the tab bar kept its five labels after the `useMemo` refactor in t007.

**One finding, proven pre-existing.** Deep-linking `beancount:///(app)/(tabs)/index` renders a fully black screen instead of `+not-found`. Confirmed not ours by stashing only `_layout.tsx` (back to `lazy: false`), re-firing the same URL, and getting the same black screen — then popping the stash. It needs a real route (`/(app)/(tabs)/` works fine), so no user can reach it; filed as inbox note `w1/026`.
