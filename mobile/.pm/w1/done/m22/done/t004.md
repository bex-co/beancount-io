---
id: w1/m22/t004
title: Drop the unused `onScrubStart` / `onScrubEnd` props
worker: worker1
status: done
estimate: 20m
depends_on: []
---

## Objective

Remove the two scrub callbacks from `InteractiveLineChartD3`. They are declared and invoked internally but no call site ever passes them.

## Context

- Declared at `src/common/d3/interactive-line-chart.tsx:67`, invoked at `:453`.
- Both call sites omit both props: `src/components/balance-chart-card/index.tsx` and `src/screens/home-screen/components/account-charts-card.tsx`.
- Re-adding them is trivial if the scrub migration in inbox note `019` wants them — that note plans to rework this exact gesture path, so deleting now costs nothing and removes a false signal that something listens.

## Steps

1. Remove the props from the type and the component, plus the internal invocations.
2. Verify scrubbing still behaves on the Home net-worth carousel and account detail (haptic on touch-in, per-point selection, cursor + headline swap).

## Files

- `src/common/d3/interactive-line-chart.tsx`

## Acceptance criteria

- [ ] The props are gone and no call site changes
- [ ] Scrubbing is unchanged in the simulator on both surfaces
- [ ] `yarn test` green

## Out of scope

- The UI-thread scrub migration (inbox note `019`).

## Outcome

Removed both props, their destructuring, and the three internal invocations (`onPanResponderGrant`, `Release`, `Terminate`), plus the now-inaccurate "or handlers change" in the dependency-array comment. Scrubbing verified unchanged on the Home net-worth carousel in the simulator.
