---
id: w1/m22/t008
title: Unit tests for the lazy-mount behavior and the surviving API
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m22/t006]
---

## Objective

Add tests that would fail if this milestone's changes were reverted or half-applied — not tests that restate the deletions.

## Context

- Test conventions: `src/__tests__/` plus colocated `__tests__/` dirs; the runner is the jest-lite in `scripts/`, so value imports must be relative (the `@/` alias resolves for types only).
- The honest targets here are narrow: a deletion is proved by `yarn typecheck`, not by a unit test.

## Steps

1. Assert the tab layout's screen options: lazy mounting is on, and the option is set once for all five tabs rather than drifting per tab.
2. If t002 kept `generateTicks`, keep its tests; if it deleted it, delete them in the same commit rather than leaving orphans.
3. Run `yarn test:unit`.

## Files

- `src/__tests__/` (new or extended)

## Acceptance criteria

- [ ] Tests assert real behavior, not the absence of deleted files
- [ ] No orphaned test survives its subject
- [ ] `yarn test:unit` green

## Out of scope

- E2E/UI snapshot testing.

## Outcome

`src/__tests__/tab-lazy-mount.test.ts` — six tests over what can actually regress:

- lazy mounting is on, is never set back to `false`, and appears exactly **once** (a per-tab override would drift)
- every tab title key resolves in the English base, and the layout registers exactly the five tabs those titles cover
- **no source file renders a `journal*` key the locales no longer declare** — the one t001 failure mode with direct user impact (a missing-translation fallback on screen). It scans `src/` and `app/`, skipping locales, generated code and tests.

Suite: 1152 passing. Deletions are proved by `yarn typecheck`, not by tests asserting the absence of files.
