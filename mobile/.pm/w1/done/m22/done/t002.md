---
id: w1/m22/t002
title: Delete `line-chart-d3` and settle `generateTicks`
worker: worker1
status: done
estimate: 30m
depends_on: []
---

## Objective

Delete `src/common/d3/line-chart-d3.tsx` (136 lines), which no file imports, and decide explicitly what happens to `generateTicks`, its only caller's only dependency.

## Context

- The four charts actually in use are `interactive-line-chart.tsx` (Home carousel + account detail), `bar-chart-d3.tsx` (Home spending card), `income-expense-bar-chart.tsx` (Reports) and `budget-bar-chart-d3.tsx` (Budget).
- `__tests__/line-chart-d3.test.ts` does **not** import the component — it imports `d3-scale`/`d3-shape` and re-implements the scale math inline, so it protects nothing and goes with the file.
- `generateTicks` (`src/common/d3/utils.ts:38`) is called only from `line-chart-d3.tsx:49`, but has its own coverage at `__tests__/utils.test.ts:57-84`. Deleting the chart leaves a tested-but-unused export.

## Steps

1. Delete `line-chart-d3.tsx` and `__tests__/line-chart-d3.test.ts`.
2. Decide `generateTicks`: delete it with its tests, or keep it and name the intended future consumer in a comment. Do not leave it undocumented and unused.
3. Run `yarn lint`, `yarn typecheck`, `yarn test:unit`.

## Files

- `src/common/d3/line-chart-d3.tsx` (delete)
- `src/common/d3/__tests__/line-chart-d3.test.ts` (delete)
- `src/common/d3/utils.ts` and `__tests__/utils.test.ts` (depending on the decision)

## Acceptance criteria

- [ ] No unreferenced chart component remains under `src/common/d3/`
- [ ] `generateTicks` is either gone or carries a comment naming why it stays
- [ ] `yarn test` green

## Out of scope

- Touching the four live charts, which `w1/m20` is still mid-flight on.

## Outcome

Deleted `line-chart-d3.tsx` and `__tests__/line-chart-d3.test.ts`. The test never imported the component — it imports `d3-scale`/`d3-shape` and re-implements the scale math inline, so it was protecting nothing.

`generateTicks` **deleted** along with its seven tests in `__tests__/utils.test.ts`: the deleted chart was its only caller, and keeping a tested-but-unreachable helper is the drift this milestone exists to remove. `CURVE_LENGTH_SLACK` and `polylineLength` stay — `interactive-line-chart.tsx:37` uses them.
