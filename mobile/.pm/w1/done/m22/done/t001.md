---
id: w1/m22/t001
title: Delete the unrouted `journal-screen`
worker: worker1
status: done
estimate: 40m
depends_on: []
---

## Objective

Delete `src/screens/journal-screen/` in full. `6900141` replaced the Journal tab with `transactions-screen` and left the old screen in the tree; nothing has imported it since.

## Context

- `JournalScreen` is exported at `src/screens/journal-screen/journal-screen.tsx:364` and imported by **nothing** — no route under `app/`, no other screen, no test.
- The directory also holds `journal-header.tsx`, `journal-empty-state.tsx`, `journal-no-entries-for-filters-state.tsx` and `journal-bottom-sheet/`.
- Live counterpart: `src/screens/transactions-screen/` — the same `SectionList`-over-ledger-entries shape, with `transactions-header.tsx` as the live search bar.

## Steps

1. Re-confirm zero importers (`grep -rn "journal-screen\|JournalScreen" src app`).
2. Read `journal-bottom-sheet/` before deleting — it is the least obviously duplicated part; if `transactions-screen` wants it, move it to `src/components/` in this task rather than resurrecting it later.
3. Grep `src/translations/` for `journal*` keys and decide each: delete the orphans, keep anything the transactions screen still renders.
4. Delete the directory; run `yarn lint`, `yarn typecheck`, `yarn test:unit`.

## Files

- `src/screens/journal-screen/**` (delete)
- `src/translations/en.ts` and any locale that overrides a deleted key

## Acceptance criteria

- [ ] The directory is gone and a repo-wide grep for `journal-screen` returns nothing
- [ ] No orphaned `journal*` translation key is left undecided
- [ ] `yarn test` green

## Out of scope

- Changing `transactions-screen` behavior; the shared search bar extraction (`w1/m24/t003`).

## Outcome

Deleted all five files. Nothing was salvaged from `journal-bottom-sheet/`: it imports `EntryContext` and `JournalDirectiveType` **from** `transactions-screen`, so the dependency ran dead → live and the live side loses nothing.

Translations: of the 18 `journal*` keys, **eight were orphans** and are gone from all 13 locales (146 lines) — `journalWelcomeTitle/Message/Instructions/Instruction1/2/3/InstructionFinal` and `journalNoEntriesForFilters`. They turned out to be the dead twins of the live `transactionsWelcome*` set in `en.ts:147-159`. Nine keys stay because `transactions-screen/entry-context/`, `transaction-detail-screen` and `account-detail-screen` still render them (`journalLoadError`, `journalLoadingEntryContext`, `journalError`, `journalNoData`, `journalLocation`, `journalEntryContext`, `journalBalancesBefore`, `journalBalancesAfter`, `journalSource`). `journal: "Journal"` also stays: no screen renders it, but `translations/__tests__/index.test.ts:70` uses it as its i18n smoke-test sample, and swapping that test's sample key is unrelated scope. Decided, not overlooked.

Three section comments in `en.ts` were lying about which screen owns their keys and were corrected, along with a test titled "has journal screen keys" that asserts transactions-screen keys.
