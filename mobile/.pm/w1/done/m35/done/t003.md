---
id: w1/m35/t003
title: Drawer row, route, and screen scaffold
worker: worker1
status: done
estimate: 30m
depends_on: []
---

## Objective

Merchants becomes reachable: a drawer row in the bottom menu section beside Settings (Monarch's exact placement — last items of the left sidebar), a route, and a guarded screen scaffold.

## Context

- The drawer's bottom `menuSection` is `src/components/ledger-drawer/ledger-drawer.tsx:483–493`, currently holding only the Settings row (`drawer-settings-row`); `menuItem` / `menuItemText` styles already exist.
- Route convention: thin file in `app/(app)/` + screen in `src/screens/<name>/` (canonical shapes: `app/(app)/payee-input.tsx`, `app/(app)/account-picker.tsx`).
- Ledger-scoped screens wrap in `LedgerGuard` / `useLedgerGuard()`.

## Steps

1. Add the drawer row: icon (e.g. `storefront-outline`), `t("merchants")`, `testID="drawer-merchants-row"`, handler = `analytics.track("drawer_tap_merchants")` → `onClose()` → `router.push("/(app)/merchants")`.
2. Add `app/(app)/merchants.tsx` with `i18n.t("merchants")` title (the established route-file exception to `useTranslations()`).
3. Scaffold `src/screens/merchants-screen/merchants-screen.tsx` + `index.ts`: `LedgerGuard`, `usePageView("merchants")`, themed background.
4. Add `merchants` (and any scaffold strings) to `src/translations/en.ts` and all twelve locales, or add an explicit `KNOWN_GAPS` deferral — the m29/m30 gate fails otherwise.

## Files

- `src/components/ledger-drawer/ledger-drawer.tsx`
- `app/(app)/merchants.tsx` (new)
- `src/screens/merchants-screen/` (new)
- `src/translations/en.ts` + locale files

## Acceptance criteria

- [ ] Drawer shows Merchants beside Settings; tapping navigates to the screen in the simulator, drawer closed behind it.
- [ ] Translation-integrity gate green; `yarn lint` green.

## Out of scope

- List content (t004) — the scaffold may render the skeleton only.
