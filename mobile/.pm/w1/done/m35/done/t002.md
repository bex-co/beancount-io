---
id: w1/m35/t002
title: "`aggregate-payees` selector: BQL rows → typed rollups"
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m35/t001]
---

## Objective

A pure selector that turns `QueryResultTable`'s positional `rows: [[JSON]]` into `MerchantAggregate[]` (`payee`, `transactionCount`, `firstDate`, `lastDate`), mapping columns by name through `types[]` rather than by position, tolerant of nulls and unexpected column order.

## Context

- Repo convention: pure view-model modules with **relative imports only** so the jest-lite runner can require them (`@/` works for type imports only). Precedents: `picker-sections.ts`, `suggestion-utils.ts`.
- jest-lite has limited matchers: `toBe/toEqual/toBeCloseTo/toBeTruthy/toBeFalsy/toThrow` + `.not` only.

## Steps

1. Create `src/screens/merchants-screen/selectors/aggregate-payees.ts` — column-index resolution from `types[]`, row mapping, and both sort orders (count desc with alpha tiebreak; alpha).
2. Unit tests under `src/__tests__/`: happy path, reordered columns, empty table, null cells, sort orders.

## Files

- `src/screens/merchants-screen/selectors/aggregate-payees.ts` (new)
- `src/__tests__/aggregate-payees.test.ts` (new)

## Acceptance criteria

- [ ] Selector has zero `@/` value imports; `yarn test:unit` green with the new tests.
- [ ] Reordered/renamed columns fail soft (skip or empty result), never crash.

## Out of scope

- Fetching (t001) and rendering (t004).
