---
id: w1/m35/t005
title: Search and count/alphabetical sort
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m35/t004]
---

## Objective

Monarch-parity controls: a search field filtering the directory client-side (the full aggregate list is already in memory), and a sort toggle — transaction count (default) / alphabetical, Monarch's exact two options.

## Context

- Reuse the account picker's `SearchBar` styling/behavior (`src/screens/account-picker-screen/`); its search-placeholder pattern with a live total ("Search N merchants…") matches Monarch's web copy.
- Sort logic lives in the t002 selector (already tested); this task is the control UI + state.

## Steps

1. Search field with debounce-free client filtering (in-memory list) and a cleared-state.
2. Sort control (header icon or segmented) with `analytics.track("merchants_sort_change", { sort })`.
3. No-results branch distinct from blank-ledger empty state.

## Files

- `src/screens/merchants-screen/merchants-screen.tsx`
- `src/translations/en.ts` + locales (search placeholder, sort labels)

## Acceptance criteria

- [ ] Typing filters instantly; sort toggles reorder correctly (verified in simulator).
- [ ] Translation gate green; strings rendered via `useTranslations()`.

## Out of scope

- Server-side search; frecency ranking (a later idea — `account-frecency.ts` is transplantable if wanted).
