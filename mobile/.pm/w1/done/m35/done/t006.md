---
id: w1/m35/t006
title: UX pass (light/dark, RTL, translations, analytics)
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m35/t005]
---

## Objective

Standing UX pass over the new drawer row and Merchants screen.

## Steps

1. Light **and** dark walk of drawer row, list, skeleton, search, sort, all empty states — theme tokens via `useTheme()`, no hard-coded colors; backgrounds set on loading states.
2. Persian spot-check (m31): row layout mirrors, last-date column and counts sit on the correct edge, no physical `left`/`right` pairings with unmirrored geometry.
3. All new strings in `src/translations/en.ts` base and rendered via `useTranslations()`; translation-integrity gate green with no new unexplained `KNOWN_GAPS` entries.
4. `SafeAreaView` spacing; `usePageView("merchants")` fires once; `drawer_tap_merchants` / `merchants_sort_change` verified structurally (analytics is a no-op under `__DEV__` — prove single-fire with a ref guard where applicable, not by watching events).

## Acceptance criteria

- [ ] Screenshots light + dark + fa attached to the task or described in the outcome note.
- [ ] Gate + `yarn lint` green.
