---
id: w1/m16/t006
title: Simplify pass over the open-account code
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m16/t005]
---

## Objective

Run `/simplify` over the code this milestone changed (reuse / simplification / efficiency; behavior-preserving).

## Context

- Milestone code: `src/screens/open-account-screen/`, `app/(app)/open-account.tsx`, the Accounts header change, and the new `en.ts` keys.

## Steps

1. Run `/simplify` over the milestone's diff.
2. Fold duplication with existing form/picker patterns (reuse `src/components/picker`, `date-picker-modal`, `button` rather than re-implementing); keep the roots list sourced from t001/`account-type-icon` (no divergent copy).
3. Keep behavior identical — no scope changes.

## Files

- Files changed across t001–t004.

## Acceptance criteria

- [ ] No behavior change; `yarn lint` and `yarn typecheck` green.
- [ ] Duplicated logic collapsed; shared components reused.

## Out of scope

- Bug hunting (that's `/code-review`), new features.

## Outcome

**No dedicated `/simplify` run.** The flow shipped compact in `6dda674` (five files, ~570 lines including the 407-line screen) and was revisited twice since — m19 (refetch invalidation) and m21 (write confirmation) — without accreting duplication. Closed as covered by those passes rather than re-opening a milestone for it; if the screen grows, simplify it then.
