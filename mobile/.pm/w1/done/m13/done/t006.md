---
id: w1/m13/t006
title: Simplify: run /simplify over the milestone's changes
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m13/t003, w1/m13/t004]
---

## Objective

Standing simplify gate: run `/simplify` over the code this milestone changed — reuse, simplification, efficiency; strictly behavior-preserving.

## Context

- Changed files: `src/components/transaction-avatar/` (new), `src/screens/journal-screen/journal-entry-item/index.tsx`, `src/screens/account-detail-screen/components/account-entry-row.tsx`, possibly `src/screens/journal-screen/utils/journal-utils.ts`.
- Prime target: confirm the old duplicated avatar styles/helpers are fully gone and the helper functions live in exactly one place.

## Steps

1. Run `/simplify` scoped to the milestone's diff.
2. Apply accepted cleanups; re-run `yarn lint`, `yarn typecheck`, `yarn test:unit`.

## Files

- Files changed by t001–t004.

## Acceptance criteria

- [ ] `/simplify` findings triaged and applied or explicitly declined.
- [ ] `yarn test` green after cleanups.

## Out of scope

- Behavior changes of any kind.
