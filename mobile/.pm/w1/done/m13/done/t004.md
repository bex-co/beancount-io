---
id: w1/m13/t004
title: Adopt TransactionAvatar in account-detail AccountEntryRow
worker: worker1
status: done
estimate: 20m
depends_on: [w1/m13/t002]
---

## Objective

Replace the duplicated initials-avatar block in the account-detail transaction rows with the shared `TransactionAvatar`, deleting the copy-pasted styles and helper imports.

## Context

- `src/screens/account-detail-screen/components/account-entry-row.tsx` — imports `getAvatarColor`/`getAvatarInitials` (lines ~10–11), renders the avatar at lines ~89–101 from `row.title`.
- This is the second of the two duplicated avatar implementations; after this task the initials/logo logic lives only in `src/components/transaction-avatar/`.

## Steps

1. Swap the avatar block for `<TransactionAvatar name={row.title} />`.
2. Remove the local `avatar`/`avatarText` styles and the journal-utils avatar imports.
3. Spot-check an account with recognizable payees (e.g. a credit-card account) in light and dark.

## Files

- `src/screens/account-detail-screen/components/account-entry-row.tsx`

## Acceptance criteria

- [ ] Account-detail rows show logos for recognized payees, initials otherwise; geometry unchanged.
- [ ] No remaining imports of avatar helpers outside `src/components/transaction-avatar/` (and journal-utils' own tests, if helpers stay re-exported).
- [ ] `yarn lint` green.

## Out of scope

- Any change to the row's amount/flow layout.
