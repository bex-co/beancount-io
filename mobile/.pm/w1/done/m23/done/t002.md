---
id: w1/m23/t002
title: Crossfade every skeleton into its content (~18 sites)
worker: worker1
status: done
estimate: 60m
depends_on: []
---

## Objective

Every skeleton outside the chart surfaces is a bare `{isLoading ? <Skeleton/> : <Real/>}`, so the pulsing tile hard-cuts to content at whatever opacity the pulse happened to reach. Apply the crossfade `w1/m20/t007` established.

## Context

- `src/components/loading-tile/index.tsx:44-61` pulses opacity 1 → 0.5 → 1 on a 600ms cycle with no easing, which is why the cut lands at a random brightness each time.
- Roughly 18 sites: `transactions-list-skeleton.tsx`, `category-breakdown.tsx`, `notifications-screen.tsx`, `commit-detail-screen.tsx`, `ledger-file-browser-screen/index.tsx`, `multi-postings-transaction/list-item.tsx`, `accounts-screen.tsx`, `account-picker-screen.tsx`, `suggestion-chips.tsx`, `quick-add-accounts-selector.tsx`, `text-input-screen/index.tsx`, `multi-postings-transaction-screen.tsx`, `ledger-file-editor-screen/index.tsx`, `home-screen/components/*`, `budget-screen.tsx`, `reports-screen/components/account-transactions-card.tsx`, `setting/account-header.tsx`.
- Low risk: layout is already pixel-matched at most sites, so a crossfade has nothing to shift.
- Use the duration token from `w1/m20/t001`; `entering={FadeIn.duration(durations.base)}` on the loaded branch is roughly a one-line change per site, exactly as `reports-screen.tsx` does today.

## Steps

1. Apply the pattern site by site, reading the token rather than a literal.
2. Watch for sites where the skeleton and content differ in height — fix the height rather than crossfading a jump.
3. Confirm reduce-motion still lands both branches at their end state.

## Files

- The ~18 files listed above

## Acceptance criteria

- [ ] No `{isLoading ? <Skeleton/> : <Real/>}` remains without a crossfade on the loaded branch
- [ ] No literal durations introduced — all read the motion tokens
- [ ] With reduce-motion on, content appears instantly with no fade

## Out of scope

- The chart surfaces (`w1/m20/t007` already did those).
