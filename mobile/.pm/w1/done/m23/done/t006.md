---
id: w1/m23/t006
title: Simplify pass over the loading/motion diff
worker: worker1
status: done
estimate: 25m
depends_on: [w1/m23/t005]
---

## Objective

Run `/simplify` over this milestone's diff — behavior-preserving. An 18-site sweep is exactly where a repeated three-line idiom should have become one component.

## Context

- Watch specifically for: the crossfade wrapper repeated verbatim (a `<FadeInView>` or a `useSkeletonSwap` may earn its place), and refresh props threaded by hand where the shared wrapper could own them.
- Milestone diff: `src/components/**`, `src/screens/**`, `app/(app)/(tabs)/_layout.tsx`.

## Steps

1. Run `/simplify` scoped to the milestone diff.
2. Apply accepted findings; re-run `yarn test`.

## Files

- Whatever the pass flags within the milestone diff.

## Acceptance criteria

- [ ] Findings applied or explicitly declined
- [ ] `yarn test` green; no behavior change

## Out of scope

- Bug hunting; new features.
