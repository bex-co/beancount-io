---
id: w1/m19/t009
title: Closeout
worker: worker1
status: done
estimate: 10m
depends_on: [w1/m19/t008]
---

## Objective

Close the milestone once its definition of done actually holds — not merely once the code is written.

## Context

- The DoD is four observable checks in `w1/m19/README.md`, each requiring a real ledger: the editor error banner clearing on save, save→back→delete with no sha conflict, a new transaction landing on all five tabs, and an edit updating the Reports income statement plus budget actuals. Walk all four before closing.
- Also confirm the structural claims: exactly one module owns the document list, and `HomeChartsDocument` appears nowhere.

## Steps

1. Walk the four DoD scenarios against a real ledger, in light **and** dark.
2. Confirm `yarn lint` / `yarn typecheck` / `yarn test:unit` pass.
3. Run `/pm done w1/m19/t009` last — that triggers the milestone move: every remaining task to `status: done` and into `w1/m19/done/`, every row marked `— **DONE**`, `**Status:** done`, the directory moved to `w1/done/m19/`, and `- [x]` checked in `w1/README.md`.

## Acceptance criteria

- [ ] All four DoD scenarios verified against a real ledger
- [ ] Checks green
- [ ] Milestone moved to `w1/done/m19/` and checked off in the workstream README

## Out of scope

- Any remaining follow-ups — file them as `w1/NNN` inbox notes instead of holding the milestone open
