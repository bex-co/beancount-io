---
id: w1/m19/t007
title: Simplify
worker: worker1
status: done
estimate: 25m
depends_on: [w1/m19/t006]
---

## Objective

Run `/simplify` over the code this milestone changed — `invalidate-ledger.ts`, the editor and file-browser save/create/delete paths, the add-entries hook, and the five sites whose refetch arrays were collapsed. Reuse, simplification and efficiency cleanups only; behavior-preserving.

## Context

- Watch specifically for what centralization made redundant: leftover local `refetch()` calls that the invalidator now covers, the manual `GetLedgerIntervalTotalsDocument` include in `budget-screen.tsx:116`, and any now-unused `*Document` imports.
- The `refreshSignal` prop-drilling from `home-screen.tsx:76` through three cards is a standing candidate — but it serves pull-to-refresh, not write invalidation. Only touch it if the cleanup is genuinely behavior-preserving; otherwise record it as an inbox note.

## Steps

1. Run `/simplify` scoped to the milestone's diff.
2. Apply accepted cleanups; verify `yarn lint` / `yarn typecheck` / `yarn test:unit` stay green.
3. File anything out of blast radius as a `w1/NNN` inbox note rather than widening the milestone.

## Acceptance criteria

- [ ] Simplify pass completed with behavior unchanged and checks green
- [ ] No unused `*Document` imports remain at the collapsed sites

## Out of scope

- New features or bug hunts
