---
id: w1/m25/t004
title: Analytics on create-row impressions and taps
worker: worker1
status: done
estimate: 20m
depends_on: [w1/m25/t003]
---

## Objective

Instrument the funnel: how often the zero-match create row is shown, tapped, and completed.

## Context

- `src/common/analytics.ts` (`analytics.track`; no-ops under `__DEV__`, so prove single-fire structurally with a ref guard, not in the simulator).
- Existing picker events live in `account-picker-screen.tsx`; follow their naming style.

## Steps

1. Track create-row shown (once per query that renders it — ref-guard against re-render double fire), tapped, and open-completed-from-picker, each carrying the picker `type`.

## Files

- `src/screens/account-picker-screen/account-picker-screen.tsx`
- `src/screens/open-account-screen/use-open-account.ts` (completion event source)

## Acceptance criteria

- [ ] Three events with the picker `type` property; impression fires once per shown query, not per render.

## Out of scope

- Dashboards or funnels downstream of the events.
