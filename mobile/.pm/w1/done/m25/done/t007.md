---
id: w1/m25/t007
title: Unit tests for validity gating and the handoff
worker: worker1
status: done
estimate: 40m
depends_on: [w1/m25/t005]
---

## Objective

Standing closing task: pin the behavior this milestone shipped with meaningful unit tests under `src/__tests__/` (or the screens' existing `__tests__/` dirs), green under `yarn test:unit`.

## Context

- jest-lite runner: value modules must use **relative imports** to be requirable; only type imports may use `@/`. Matchers are limited to `toBe`/`toEqual`/`toBeCloseTo`/`toBeTruthy`/`toBeFalsy`/`toThrow` + `.not`.
- Pure seams to test: the root/sub-path split (t002) round-tripping with `composeAccountName`, and the create-row gating decision (which queries show the row, which proceed directly).

## Steps

1. Test the split: valid roots, rootless strings, illegal characters, round-trip identity with `composeAccountName`.
2. Test the gating decision table, including the empty query.
3. Assert failure modes (invalid input never yields a directly-writable name), not tautologies.

## Files

- `src/screens/open-account-screen/__tests__/` or `src/screens/account-picker-screen/__tests__/`

## Acceptance criteria

- [ ] Tests fail if the gating or split regresses; `yarn test:unit` passes.

## Out of scope

- Snapshot tests; navigation/E2E simulation.
