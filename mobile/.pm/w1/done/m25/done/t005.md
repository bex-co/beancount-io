---
id: w1/m25/t005
title: UX pass — light/dark, i18n, safe area, both entry directions
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m25/t003, w1/m25/t004]
---

## Objective

Standing closing task: walk the whole create path in both themes and confirm the quality bar.

## Context

- Simulator driving per `mobile/CLAUDE.md` (deep links, `xcrun simctl` screenshots, expo-mcp taps). **This flow writes to a real ledger — ask the user before exercising the save, and delete the created account (remove the `open` directive) afterwards.**

## Steps

1. Light and dark: create row, prefilled open-account screen, return-to-form moment — theme tokens only, backgrounds set on any loading state.
2. All new strings render via `useTranslations()` from the English base.
3. `SafeAreaView` spacing intact on both screens; keyboard does not cover the create row.
4. Both entries behave: picker-origin (handoff + pop-two) and the pre-existing Accounts-tab entry (unchanged).

## Files

- Screens touched by t001–t003.

## Acceptance criteria

- [ ] Screenshots or notes for light + dark on each touched surface; no hard-coded colors found.
- [ ] Accounts-tab open-account flow verified unregressed.

## Out of scope

- Code changes beyond fixes to what this pass finds.
