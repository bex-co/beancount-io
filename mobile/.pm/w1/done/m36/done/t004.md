---
id: w1/m36/t004
title: Paging, skeleton, and empty/error states
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m36/t002, w1/m36/t003]
---

## Objective

The screen holds up on big and tiny merchants alike: `fetchMore` paging past the first window (the free-text filter inflates pages, so page until exhausted or a sane cap, showing a footer loader), a house-rules skeleton mirroring header + list, pull-to-refresh, and error/empty branches.

## Context

- Paging pattern: `transactions-screen.tsx` (`fetchMore` + offset concat + `hasMore`).
- Skeleton rules per `mobile/CLAUDE.md`; `refreshing` tracks only the pull.

## Files

- `src/screens/merchant-detail-screen/merchant-detail-screen.tsx`
- `src/screens/merchant-detail-screen/merchant-detail-skeleton.tsx` (new)

## Acceptance criteria

- [ ] A merchant with >20 transactions loads them all on scroll; a one-transaction merchant renders without layout shift.
- [ ] Error state offers retry; both themes verified.
