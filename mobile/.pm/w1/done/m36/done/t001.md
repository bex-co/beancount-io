---
id: w1/m36/t001
title: Route, navigation, and detail-source wiring
worker: worker1
status: done
estimate: 30m
depends_on: []
---

## Objective

A `merchant-detail` route reachable from the m35 directory, with analytics and the transaction-detail source union extended so onward navigation is legal.

## Context

- Route param convention: `useLocalSearchParams` (deep-link `beancount:///(app)/merchant-detail?payee=...` should work, per the simulator-driving docs).
- `TransactionDetailSource` in `src/screens/transaction-detail-screen/open-transaction-detail.ts` is a **closed union** (`"home" | "transactions" | "journal" | "account_detail" | "reports"`) — add `"merchants"`.

## Steps

1. `app/(app)/merchant-detail.tsx` route (title = payee name) + `src/screens/merchant-detail-screen/` scaffold with `LedgerGuard` + `usePageView("merchant_detail")`.
2. m35 rows navigate with `analytics.track("merchants_tap_row")`.
3. Extend `TransactionDetailSource` with `"merchants"`.

## Files

- `app/(app)/merchant-detail.tsx` (new)
- `src/screens/merchant-detail-screen/` (new)
- `src/screens/merchants-screen/merchant-row.tsx`
- `src/screens/transaction-detail-screen/open-transaction-detail.ts`

## Acceptance criteria

- [ ] Row tap and deep link both land on the screen with the payee in the header (simulator).
- [ ] `yarn typecheck` green with the widened union.
