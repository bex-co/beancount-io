---
id: w1/m2/t006
title: UX pass — light/dark, i18n, loading bg, safe area, analytics
worker: worker1
status: done
estimate: 40m
depends_on: [w1/m2/t003, w1/m2/t005]
---

## Objective

Standing UX pass over everything this milestone shipped: the Accounts tab, the shared account list, and the account-detail screen.

## Context

- Screens/components from t002–t005: `src/screens/accounts-screen/`, `src/screens/account-detail-screen/`, `src/components/account-list/`, tab registration in `app/(app)/(tabs)/_layout.tsx`.

## Steps

1. Verify light **and** dark themes — all colors via `useTheme()` tokens, no hard-coded colors.
2. Verify every new string exists in the English base `src/translations/en.ts` and renders via `useTranslations()` (not `i18n.t()` directly).
3. Set background colors on all loading/skeleton states (dark-mode flicker check).
4. Verify `SafeAreaView` spacing on both new screens.
5. Verify analytics: page-view tracking on Accounts tab and account-detail mounts; tap events where meaningful (range pills, account rows).

## Files

- `src/screens/accounts-screen/**`, `src/screens/account-detail-screen/**`, `src/components/account-list/**`, `src/translations/en.ts`

## Acceptance criteria

- [ ] Both new screens verified in light and dark with no hard-coded colors
- [ ] No missing-translation fallbacks; strings re-render on locale change
- [ ] Loading states have themed backgrounds; safe areas correct; analytics fire on mount

## Out of scope

- Behavior changes — this pass polishes, it does not add features.
