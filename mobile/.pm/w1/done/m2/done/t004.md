---
id: w1/m2/t004
title: Grouped account list with subtotals (shared from home account list)
worker: worker1
status: done
estimate: 45m
depends_on: [w1/m2/t002]
---

## Objective

Below the net-worth header, list accounts Monarch-style: grouped as Assets and Liabilities with group subtotals, collapsible per-account rows showing balances. Lift the existing home-screen account list into a shared component instead of duplicating it.

## Context

- Existing implementation: `src/screens/home-screen/components/account-list-page.tsx` (collapsible hierarchy, indent levels, thousands grouping) + `src/screens/home-screen/selectors/select-account-list.ts` and `select-account-totals.ts` (tested).
- Data: `useAccountHierarchy` (`src/screens/home-screen/hooks/use-account-hierarchy.ts`) — already fetched for home, so Apollo cache makes this cheap.
- Shared-component home: `src/components/` (e.g. `src/components/account-list/`).

## Steps

1. Move/generalize `account-list-page.tsx` into `src/components/account-list/` (props: hierarchy nodes, group filter, row-press handler); keep the home screen consuming it unchanged.
2. Move `select-account-list` (and `AccountNode` type) to a shared location the component owns; update home imports.
3. In `accounts-screen.tsx`, render Assets and Liabilities groups with subtotal headers (reuse `select-account-totals`).
4. Expose `onPressAccount(account: string)` for t005 navigation (no-op until t005).

## Files

- `src/components/account-list/index.tsx` (new, lifted from `src/screens/home-screen/components/account-list-page.tsx`)
- `src/screens/home-screen/components/account-list-page.tsx` (delegates or is removed)
- `src/screens/home-screen/selectors/select-account-list.ts` (moved/re-exported)
- `src/screens/accounts-screen/accounts-screen.tsx`

## Acceptance criteria

- [ ] Accounts tab shows Assets and Liabilities groups with correct subtotals and collapsible rows
- [ ] Home screen account list still renders identically (no regression)
- [ ] Existing `select-account-list` / `select-account-totals` tests still pass via `yarn test:unit`

## Out of scope

- Account detail navigation target (t005).
- Income/Expenses/Equity groups (Reports tab covers flows).
