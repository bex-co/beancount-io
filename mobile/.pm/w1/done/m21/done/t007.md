---
id: w1/m21/t007
title: Simplify pass over the confirmation + haptics code
worker: worker1
status: done
estimate: 25m
depends_on: [w1/m21/t006]
---

## Objective

Run `/simplify` over the code this milestone changed — reuse, simplification, efficiency, behavior-preserving only.

## Context

- The prize here is collapse: four write flows that each hand-rolled their own toast/navigate/refetch sequence should now share one helper (t001), and five raw haptic call sites should now share one wrapper (t002). Verify that actually happened rather than a helper being added alongside the old copies.
- `w1/m19`'s Simplify pass found exactly this shape — a helper kept beside the thing it replaced, double-fetching — and deleted `refetchBudgetJournal` as a result. Look for the same pattern here.
- If `w1/m20/t001` has landed, fold t005's local press-feedback constant onto the motion tokens.
- Known adjacent debt deliberately **not** in this pass: the 41-site `TouchableOpacity` sweep, the two legacy RN `Animated` files (`w1/016`), tab icon animation (`w1/018`).

## Steps

1. Run `/simplify` over the milestone's diff.
2. Confirm no screen still imports `expo-haptics` directly and no save path still hand-rolls the toast/navigate/refetch sequence.
3. Confirm no helper was added while its predecessor survived — delete the dead half.
4. Fold t005's local constants onto the m20 motion tokens if available.
5. Record anything real but outside the blast radius as a new inbox note rather than widening this milestone.

## Files

- Any file touched by t001–t005.

## Acceptance criteria

- [ ] `/simplify` run over the milestone's diff and its findings either applied or recorded as inbox notes.
- [ ] Exactly one confirmation helper and one haptics module; no surviving duplicates of what they replaced.
- [ ] No behavior change — the t006 walkthrough still passes afterward.
- [ ] `yarn lint` / `yarn typecheck` / `yarn test:unit` pass.

## Out of scope

- Bug hunting (that is `/code-review`).
- Refactors outside this milestone's diff.
