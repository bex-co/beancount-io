---
id: w1/m34/t006
title: "Simplify pass over the persistence diff"
worker: worker1
status: done
estimate: 20m
depends_on: [w1/m34/t005]
---

## Objective

Run `/simplify` over the code this milestone changed — reuse, simplification, efficiency; behavior-preserving. Likely targets: the staleness hook duplicating logic that `error-handling.ts` already has, and the startup gate in `app/_layout.tsx` growing a second await chain where one would do.

## Context

- Changed surface: `src/common/apollo/{cache,client,error-handling,invalidate-ledger}.ts`, `app/_layout.tsx`, the sign-out handler, three screens' indicator mounts, translations.
- Precedent: defer non-behavior-preserving findings to inbox notes with a written why (`w1/done/m20/t009` discipline).

## Steps

1. `/simplify` scoped to the milestone's diff.
2. Apply behavior-preserving findings; file the rest as inbox notes.
3. Re-run the airplane-mode cold-start walk once if anything in the startup path changed.

## Files

- The milestone's changed files only

## Acceptance criteria

- [x] `yarn lint`, `yarn typecheck`, `yarn test:unit` green after applied findings
- [x] Startup path still restores-before-render (re-verified if touched)

## Out of scope

- Bug-hunting; behavior changes
