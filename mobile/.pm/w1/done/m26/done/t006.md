---
id: w1/m26/t006
title: Simplify pass over the scrub diff
worker: worker1
status: done
estimate: 25m
depends_on: [w1/m26/t005]
---

## Objective

Run `/simplify` over the code this milestone changed — reuse, simplification, efficiency, behavior-preserving only.

## Context

- Likely candidates: the worklet index-resolution helper vs. any similar math m20 left behind; shared `createAnimatedComponent` wrappers; dead code from the deleted PanResponder path.
- Efficiency bar from m20/t009 applies: no per-frame d3 or geometry recomputation, minimum animated-node count.

## Steps

1. Run `/simplify` over the milestone's diff; apply findings or record them as inbox notes.

## Files

- Any file touched by t001–t004.

## Acceptance criteria

- [ ] Findings applied or recorded; no behavior change; `yarn lint` / `yarn typecheck` / `yarn test:unit` pass.

## Out of scope

- Bug hunting (`/code-review`); refactors outside the diff.
