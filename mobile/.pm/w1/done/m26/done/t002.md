---
id: w1/m26/t002
title: Guide line and cursor dot via `useAnimatedProps`
worker: worker1
status: done
estimate: 40m
depends_on: [w1/m26/t001]
---

## Objective

Drive the vertical guide line and cursor dot (currently rendered from React state at ~:307-326) from the shared values with `useAnimatedProps`, so scrub visuals update on the UI thread with no React render.

## Context

- m20's design decisions govern: any prop change repaints the whole `<Svg>`, so the cursor stays the **only** animated SVG node this task adds (the entrance path from m20 is the other); budget accordingly.
- Show/hide on touch-in/out moves from conditional JSX to animated opacity, so mount/unmount doesn't re-enter React mid-gesture.

## Steps

1. `Animated.createAnimatedComponent` wrappers for the guide `Line`/cursor `Circle` (reuse m20's wrappers if the simplify pass shared them).
2. Position via `useAnimatedProps` reading the shared values; interpolate with reanimated's `interpolate` over plain `number[]`.
3. Remove the t001 rendering bridge for these nodes.

## Files

- `src/common/d3/interactive-line-chart.tsx`

## Acceptance criteria

- [ ] Guide + cursor track the finger with zero React re-renders per frame (verify with a render counter during development).
- [ ] Animated SVG node count stays at the m20 budget (entrance path + cursor/guide).
- [ ] Visuals pixel-match the old rendering in light and dark.

## Out of scope

- The headline (t004).
