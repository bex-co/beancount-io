---
id: w1/m18/t005
title: Selection-source + time-to-select analytics
worker: worker1
status: done
estimate: 25m
depends_on: [w1/m18/t003]
---

## Objective

Extend the existing `tap_account_picker_confirm` event with `source` (`recents` | `search` | `browse`) and `timeToSelectMs` (elapsed since the screen opened), so the impact of search and recents is measurable in later analytics reviews.

## Context

- The picker already fires `usePageView("account_picker")` and `analytics.track("tap_account_picker_confirm", { selectedAccount })` — extend, don't duplicate.
- No new PII: `selectedAccount` is already tracked today.

## Steps

1. Timestamp screen open; tag each confirm with source and elapsed ms.
2. Verify event payloads in the analytics debug output.

## Files

- `src/screens/account-picker-screen/account-picker-screen.tsx`

## Acceptance criteria

- [ ] Confirms from the Recent section, search results, and plain browsing carry the correct `source`
- [ ] `timeToSelectMs` is present and plausible (> 0)

## Out of scope

- Dashboards/reporting on these events
