---
id: w1/m18/t003
title: Recent section pinned above the list
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m18/t002]
---

## Objective

When the search query is empty, pin a "Recent" section above the account list: top 5 accounts by frecency for the active ledger, with the clock icon used by the existing suggestion chips. Hide the section while a search query is active or when there is no usage yet. Header string in the English translation base via `useTranslations()`.

## Context

- Iconography precedent: `src/screens/add-transaction-screen/suggestion-chips.tsx` (clock = history).
- Reference pattern: Qonto's "Suggested" item pinned above its category tree; Monarch's grouped picker.
- i18n: key in `src/translations/en.ts` (base); other locales extend/override.

## Steps

1. Compute top 5 via `account-frecency.topAccounts` for the active ledger.
2. Render the pinned section above the browse list; rows select exactly like normal rows.
3. Add the localized "Recent" header key to the English base; translate other locales where straightforward.

## Files

- `src/screens/account-picker-screen/account-picker-screen.tsx`
- `src/translations/en.ts` (+ other locale files as translated)

## Acceptance criteria

- [ ] Section appears only with non-empty usage and an empty query; capped at 5
- [ ] Tapping a recent row confirms the selection like a normal row
- [ ] Header renders via `useTranslations()` with the key in the English base

## Out of scope

- Journal-derived cold-start ranking (recents come from local selections only)
