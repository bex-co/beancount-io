---
id: w1/m31/t009
title: Simplify pass over the RTL diff
worker: worker1
status: done
estimate: 20m
depends_on: [w1/m31/t008]
---

## Objective

Run `/simplify` over everything this milestone changed, before the tests in `t010` pin the shape in place.

## Context

- The likely finding is duplication of the direction decision: `t001`, `t002`, `t005`, `t006`, and `t007` all need to know whether the layout is RTL, and the cheap version of each is a local `I18nManager.isRTL`. One source in `src/common/rtl.ts` is the intended shape; the pass is where drift from it gets collected.
- The second likely finding is near-identical mirroring expressions across the drawer and the charts that want one small helper.
- Behavior-preserving only. `/simplify` does not hunt for bugs — `t008` was the correctness pass and `t010` is the coverage pass.
- m29 and m30 both used this slot to retire something the milestone had made redundant; check whether any pre-existing direction handling elsewhere is now dead.

## Steps

1. Run `/simplify` over the milestone's diff.
2. Apply the findings that hold, and note any that were declined and why.
3. Confirm no behavior changed: re-check two Persian screens and two English screens from `t008`'s walk.

## Files

- Whatever the milestone touched.

## Acceptance criteria

- [x] One source of truth for the layout direction; no site branches on `I18nManager.isRTL` inline.
- [x] No behavior change — spot-checked in both directions.
- [x] `yarn lint` / `yarn typecheck` / `yarn test:unit` pass.

## Out of scope

- Refactors beyond this milestone's diff.
