---
id: w1/m8/t002
title: Multi-leg route + screen scaffold (date/payee/narration, Done header)
worker: worker1
status: todo
estimate: 40m
depends_on: [w1/m8/t001]
---

## Objective

Stand up the multi-leg entry screen: an Expo Router route mounting a single-form screen with date / payee / narration rows (reusing the existing pickers) and a Done button in the header. The legs list arrives in t003.

## Context

- Pattern to follow: `src/screens/add-transaction-screen/add-transaction-next-screen.tsx` — `Stack.Screen` header with Done, `ListItem` rows, `DateTimePickerModal`, `LedgerGuard` + `useLedgerGuard`, `usePageView`.
- Reuse `ListItem` from `src/screens/add-transaction-screen/list-item.tsx`; payee/narration via `/(app)/payee-input` and `/(app)/narration-input` with the `SelectedPayee` / `SelectedNarration` setters in `src/common/globalFnFactory`.
- Route files live in `app/(app)/` (see `add-transaction-next.tsx` for the pattern).

## Steps

1. Create `src/screens/add-transaction-screen/multi-leg-screen.tsx` (colocated to reuse `ListItem`): `LedgerGuard`-wrapped component, `usePageView("add_transaction_multi")`, `SafeAreaView`, themed background.
2. Header via `Stack.Screen`: localized title, Done button (disabled placeholder — enabled/wired in t004/t005).
3. Rows: DATE (opens `DateTimePickerModal`, defaults to today via `getFormatDate`), PAYEE and NARRATION (existing input screens + global setters).
4. Create the route `app/(app)/add-transaction-multi.tsx` mounting the screen; verify the t001 menu item lands here.
5. Add any new English strings to `src/translations/en.ts`.

## Files

- `src/screens/add-transaction-screen/multi-leg-screen.tsx` (new)
- `app/(app)/add-transaction-multi.tsx` (new)
- `src/translations/en.ts`

## Acceptance criteria

- [ ] Menu item from t001 opens the screen; date/payee/narration rows edit and display their values.
- [ ] Screen renders correctly in light **and** dark; `usePageView("add_transaction_multi")` fires on mount.
- [ ] `yarn typecheck` green.

## Out of scope

- Legs list (t003), balancing/validation (t004), saving (t005).
