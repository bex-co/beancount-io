---
id: w1/m8/t007
title: Simplify pass over multi-leg entry code
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m8/t006]
---

## Objective

Run `/simplify` over the code this milestone changed — reuse, simplification, efficiency; behavior-preserving.

## Context

- Scope: `src/components/split-button/`, `src/screens/home-screen/home-screen.tsx`, `src/screens/add-transaction-screen/multi-leg-screen.tsx`, `src/screens/add-transaction-screen/multi-leg-utils.ts`.
- Watch for duplication with `add-transaction-next-screen.tsx` (toast lifecycle, save flow) worth extracting.

## Steps

1. Run `/simplify` scoped to the milestone's diff.
2. Apply behavior-preserving cleanups; re-verify the flows.

## Files

- Files changed by this milestone.

## Acceptance criteria

- [ ] `/simplify` findings applied or explicitly rejected with rationale.
- [ ] `yarn lint` and `yarn typecheck` green; behavior unchanged.

## Out of scope

- Bug hunting (`/code-review`) and new features.
