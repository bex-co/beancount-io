---
id: w1/m7/t002
title: Wire the account-charts carousel pages to their deep views
worker: worker1
status: todo
estimate: 40m
depends_on: [w1/m7/t001]
---

## Objective

Make each page of the Home top carousel tappable to its deep view: net-worth page → Reports/Net Worth, spending-by-category page → Reports/Spending, liabilities page → Accounts tab.

## Context

- Carousel card: `src/screens/home-screen/components/account-charts-card.tsx` (pages built in m1 over `PagedCarousel`).
- Targets: the reports deep-link params from t001; the Accounts tab route `app/(app)/(tabs)/accounts.tsx`.
- Taps must not conflict with the horizontal swipe gesture of the pager — tap on the page content, not a swallowing overlay.

## Steps

1. Add a per-page press handler mapping page → destination (net worth → `/reports?segment=networth`, category → `/reports?segment=spending`, liabilities → `/accounts`).
2. Verify swipe-between-pages still works and taps don't fire during swipes.
3. Track an analytics event per page tap (card + page identifier).

## Files

- `src/screens/home-screen/components/account-charts-card.tsx`
- `src/screens/home-screen/home-screen.tsx` (if navigation is lifted)

## Acceptance criteria

- [ ] Each carousel page navigates to its mapped destination with the right segment pre-selected; swiping between pages is unaffected.

## Out of scope

- The spending card (t003); changing chart content.
