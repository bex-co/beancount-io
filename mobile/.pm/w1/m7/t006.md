---
id: w1/m7/t006
title: Unit tests for behavior this milestone shipped
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m7/t004]
---

## Objective

Standing closing task: add meaningful unit tests under `src/__tests__/` for the behavior this milestone shipped; they must pass with `yarn test:unit` and assert real behavior and failure modes — no trivial, tautological, or snapshot-everything tests.

## Context

- Behaviors worth asserting: reports param parsing/validation (valid segments, invalid → default fallback), the carousel page → destination mapping, the spending-card month → range-param mapping.
- Note: the jest-lite runner has no `@/` path alias for value modules — use relative imports in test-reachable modules.

## Steps

1. Keep param parsing and page→destination mapping as pure functions; test them directly.
2. Cover the invalid-param fallback and the month-to-range mapping edge cases (year boundary).

## Files

- `src/__tests__/` (new test files)

## Acceptance criteria

- [ ] `yarn test:unit` green with the new tests asserting real behavior and failure modes.

## Out of scope

- E2E/simulator tests.
