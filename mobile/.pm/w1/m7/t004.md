---
id: w1/m7/t004
title: UX pass — light/dark, i18n, safe area, analytics
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m7/t003]
---

## Objective

Standing closing task: verify the milestone's user-facing surfaces meet the quality bar before it ships.

## Context

- Surfaces touched: Home dashboard cards (`src/screens/home-screen/components/`), shared `DashboardCard`, Reports param handling.

## Steps

1. Light **and** dark: chevron affordances and pressed states use theme tokens via `useTheme()`, no hard-coded colors.
2. Any new strings in the English base under `src/translations/` and rendered via `useTranslations()`.
3. Navigation transitions land with correct loading-state background colors on the target screens; `SafeAreaView` spacing unaffected.
4. Analytics: card-tap events firing with card/page identifiers from every wired card.

## Files

- Files touched by t001–t003 (verification pass; fixes in place)

## Acceptance criteria

- [ ] Every check above verified in the running app in both themes.

## Out of scope

- New features or refactors (t005 handles simplification).
