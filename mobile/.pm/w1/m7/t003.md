---
id: w1/m7/t003
title: Spending card → Reports/Spending + tap affordances + analytics
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m7/t002]
---

## Objective

The this-vs-last-month spending card opens Reports/Spending scoped to the compared month, and every tappable dashboard card gains a visible affordance (chevron) so users can tell the cards are doors.

## Context

- Spending card: `src/screens/home-screen/components/spending-card.tsx` (m1).
- The recent-transactions card already navigates to Journal ("see all") — leave as is, but align its affordance styling.
- Card container: `DashboardCard` (shared, from m1) — the chevron likely belongs in its header for consistency.

## Steps

1. Wire the spending card press to `/reports?segment=spending` with the compared month as the range param (t001).
2. Add a consistent chevron affordance to tappable cards via the shared `DashboardCard` header.
3. Track an analytics event for the spending-card tap; confirm every card tap event carries a card identifier.

## Files

- `src/screens/home-screen/components/spending-card.tsx`
- `src/components/` shared `DashboardCard` (chevron affordance)
- `src/screens/home-screen/components/recent-transactions-card.tsx` (affordance alignment only)

## Acceptance criteria

- [ ] Tapping the spending card opens the Spending report for the month shown on the card; chevrons visible on all tappable cards in light and dark.

## Out of scope

- New report types or card content changes.
