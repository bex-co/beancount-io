---
id: w1/m7/t005
title: Simplify pass over card tap-through code
worker: worker1
status: todo
estimate: 20m
depends_on: [w1/m7/t004]
---

## Objective

Standing closing task: run `/simplify` over the code this milestone changed — reuse, simplification, efficiency; behavior-preserving only.

## Context

- Scope: the diff of t001–t004 (reports param handling, card press handlers, chevron affordance).

## Steps

1. Run `/simplify` scoped to the milestone's changed files.
2. Apply behavior-preserving cleanups (e.g. one shared page→destination map instead of per-card duplicates).

## Files

- Files changed by this milestone

## Acceptance criteria

- [ ] `/simplify` findings addressed or explicitly declined; `yarn lint` and `yarn typecheck` green.

## Out of scope

- Behavior changes.
