---
id: w1/m11/t005
title: Simplify pass over suggestion code
worker: worker1
status: todo
estimate: 20m
depends_on: [w1/m11/t004]
---

## Objective

Run `/simplify` over the code this milestone changed — reuse, simplification, efficiency; behavior-preserving.

## Context

- Scope: `use-payee-account-suggestions.ts`, chips UI, `add-transaction-next-screen.tsx` wiring, new GraphQL operations.

## Steps

1. Run `/simplify` scoped to the milestone's diff.
2. Apply behavior-preserving cleanups; re-verify suggestion behavior.

## Files

- Files changed by this milestone.

## Acceptance criteria

- [ ] `/simplify` findings applied or explicitly rejected with rationale.
- [ ] `yarn lint` and `yarn typecheck` green; behavior unchanged.

## Out of scope

- Bug hunting (`/code-review`) and new features.
