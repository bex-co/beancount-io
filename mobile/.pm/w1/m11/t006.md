---
id: w1/m11/t006
title: Unit tests for suggestion selection + fallback behavior
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m11/t004]
---

## Objective

Meaningful unit tests under `src/__tests__/` for the suggestion logic — auto-apply guards, fallback selection, never-overwrite semantics. No trivial or snapshot tests.

## Context

- Runner: `yarn test:unit` (jest-lite) — `@/` resolves only for **type** imports; the ranking/guard helpers from t002 must be pure and imported **relatively**.

## Steps

1. Test auto-apply guard: manual account choice is never overwritten; stale responses (payee changed again) are ignored.
2. Test fallback selection: history present → history chips, no LLM call; history empty → LLM chips, never auto-applied.
3. Test degradation: query error → no suggestions, flow state unchanged.
4. `yarn test:unit` green.

## Files

- `src/__tests__/payee-account-suggestions.test.ts` (new)

## Acceptance criteria

- [ ] Guard, fallback, and degradation behavior covered including failure modes.
- [ ] Value imports are relative (jest-lite `@/` limitation respected).
- [ ] `yarn test:unit` green.

## Out of scope

- Component/UI snapshot tests.
