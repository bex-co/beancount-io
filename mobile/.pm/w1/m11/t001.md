---
id: w1/m11/t001
title: "GraphQL ops + codegen: getLedgerPayeeAccounts, suggestTransactionCategoriesWithLLM"
worker: worker1
status: todo
estimate: 20m
depends_on: []
---

## Objective

Add the two suggestion operations and regenerate typed hooks: `getLedgerPayeeAccounts(ledgerId, payee): [String!]!` (historical accounts for a payee) and `suggestTransactionCategoriesWithLLM` (AI category suggestion for unseen payees).

## Context

- Both already exist in `src/generated-graphql/schema.graphql`; operations live in `src/common/graphql/queries/`.
- Regenerate with `yarn codegen` — never hand-edit `src/generated-graphql/`.

## Steps

1. Add `getLedgerPayeeAccounts.graphql` and `suggestTransactionCategoriesWithLLM.graphql` (check the LLM op's exact args/result shape in the schema when writing the selection set).
2. `yarn codegen`; verify hooks typecheck.

## Files

- `src/common/graphql/queries/getLedgerPayeeAccounts.graphql` (new)
- `src/common/graphql/queries/suggestTransactionCategoriesWithLLM.graphql` (new)
- `src/generated-graphql/` (via `yarn codegen` only)

## Acceptance criteria

- [ ] `yarn codegen` succeeds; both generated hooks typecheck.
- [ ] `yarn typecheck` green.

## Out of scope

- Any UI.
