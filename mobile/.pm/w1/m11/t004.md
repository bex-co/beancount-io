---
id: w1/m11/t004
title: UX pass — light/dark, i18n, loading bg, safe area, analytics
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m11/t003]
---

## Objective

Standing closing pass over the suggestion UI in the add flow.

## Context

- Conventions: theme tokens via `useTheme()`, English-base strings via `useTranslations()`, themed backgrounds/skeletons on loading states, analytics events present.

## Steps

1. Verify chips (history + AI-marked), their skeleton state, and the auto-applied account row in light **and** dark.
2. Audit new strings in `src/translations/en.ts`; add worthwhile locale overrides.
3. Confirm analytics: `suggestion_auto_applied`, `suggestion_chip_tapped` with source.

## Files

- `src/screens/add-transaction-screen/*`
- `src/translations/*.ts`

## Acceptance criteria

- [ ] Both themes verified; no hard-coded colors; skeleton follows loading conventions.
- [ ] All new strings localized via the English base + `useTranslations()`.
- [ ] Analytics events fire with correct payloads.

## Out of scope

- New functionality.
