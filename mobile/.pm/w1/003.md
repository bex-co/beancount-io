# BQL console — queryShellText screen (web bql parity, scope first)

Mobile schema already has `queryShell` / `queryShellText(ledgerId, query)`; the web dashboard ships a BQL console on them (`beancount-dashboard` `src/features/bql`). A mobile version = query input + monospace results + local query history. Power-user pillar 3+4.

Scope before committing: results rendering on small screens, keyboard ergonomics, whether history/sample queries make it useful enough. Likely a small milestone once scoped.
