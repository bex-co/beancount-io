# Home cards should be doors — what survives of the deleted m7

`w1/m7` ("Dashboard cards tap through to Reports and Accounts") was deleted on 2026-08-16: five of its six tasks were written against the Reports segment switcher that `0bf3e11` removed. `t001` (deep-link params for `?segment=networth` / `?segment=spending`) has no target left, `t002`'s net-worth page has no deep view at all now that net worth lives on Home itself, and `t003`'s premise that the recent-transactions card links to Journal is stale — the Journal tab was replaced by Transactions in `6900141`.

What is still worth doing, and is sub-hour now that the segment plumbing is gone:

- **Spending-by-category carousel page and the spending card → `/reports`.** No segment param needed; Reports is one dashboard. Whether to carry the compared month across as a time range is a judgement call — the Reports range is local state (`useState<TimeRange>` in `reports-screen.tsx`), so passing one means adding a route param and seeding state from it.
- **Liabilities carousel page → `/accounts`.** The only m7 destination that never depended on segments.
- **A consistent tap affordance.** Today `budget-card.tsx:77` is the only Home card that navigates, and nothing marks a card as tappable. If more cards become doors, the chevron belongs in the shared `DashboardCard` header, not per card.
- **An analytics event per card tap**, carrying a card identifier.

Watch the pager: taps must not fire during a horizontal swipe of `PagedCarousel` (`account-charts-card.tsx`).

← salvaged from the deleted `w1/m7` during the 2026-08-16 board triage
