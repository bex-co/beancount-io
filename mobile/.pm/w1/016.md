# Retire the two legacy RN `Animated` files onto Reanimated

Two files still use React Native's core `Animated` with `useNativeDriver: false` — the exact pattern commit `d0c1f13` identified as the cause of the ledger drawer's drag flicker before it was moved to the UI thread.

**`src/common/progress-bar.tsx:28-61`** — `Animated.timing` at 200ms interpolating width `"0%"→"100%"`. This duplicates `src/components/progress/index.tsx`, which already does the same job on Reanimated with a `withTiming` width. Check for callers, then delete in favour of the Reanimated one rather than porting it.

**`src/screens/setting/logout-screen.tsx:70-139`** — two `Animated.Value`s: a 300ms fade-in plus translateY 20→0 (`useNativeDriver: true`) and a progress bar (`useNativeDriver: false`) interpolating to `"0%"→"100%"`. Worth noting this is the most deliberately "designed" motion moment in the app and it is on the logout screen.

Fold both onto the motion tokens if `w1/m20/t001` has landed. Sub-hour.

← from `/pm-brainstorm` 2026-08-14 (Monarch animation / delight pass)
