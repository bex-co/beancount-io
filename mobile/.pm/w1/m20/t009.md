---
id: w1/m20/t009
title: Simplify pass over the motion + chart code
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m20/t008]
---

## Objective

Run `/simplify` over the code this milestone changed — reuse, simplification, efficiency, behavior-preserving only.

## Context

- The five files in `src/common/d3/` share a lot of shape already: the pinned-axis + horizontal `ScrollView` pattern appears in both `income-expense-bar-chart.tsx` and `budget-bar-chart-d3.tsx`, and all five wrap in `ErrorBoundary fallback={null}`. Adding animation to three of them will surface more duplication.
- Likely candidates after t001–t007: a shared "animate on data arrival, not on re-render" hook; a shared entrance-progress shared value; the repeated `Animated.createAnimatedComponent(Path)` / `(Rect)` wrappers.
- Efficiency matters more than usual here: any prop change repaints the whole `<Svg>`, so watch for animated nodes that were added without need, and for `useMemo` dependencies that make geometry recompute per frame.
- Known adjacent debt deliberately **not** in this pass: the two legacy RN `Animated` files (`w1/016`), the app-wide crossfade sweep (`w1/017`), generalizing `TimeRangePills` (`w1/010`).

## Steps

1. Run `/simplify` over the milestone's diff.
2. Extract any genuinely shared animation helper that three charts now duplicate — but only if it is real duplication, not incidental similarity.
3. Confirm d3 work stays inside `useMemo` on the JS thread and does not re-run per frame.
4. Confirm the animated-node count per chart is the minimum needed (one path per series plus the cursor).
5. Record anything real but out of blast radius as a new inbox note rather than widening this milestone.

## Files

- Any file touched by t001–t007.

## Acceptance criteria

- [ ] `/simplify` run over the milestone's diff and its findings either applied or recorded as inbox notes.
- [ ] No behavior change — the t008 walkthrough still passes afterward.
- [ ] No per-frame d3 or geometry recomputation.
- [ ] `yarn lint` / `yarn typecheck` / `yarn test:unit` pass.

## Out of scope

- Bug hunting (that is `/code-review`).
- Refactors outside this milestone's diff.
