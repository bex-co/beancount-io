---
id: w1/m26/t007
title: Unit tests for the scrub math
worker: worker1
status: todo
estimate: 40m
depends_on: [w1/m26/t005]
---

## Objective

Standing closing task: pin the pure seams this milestone created with unit tests green under `yarn test:unit`.

## Context

- jest-lite runner: value modules need **relative imports**; matchers limited to `toBe`/`toEqual`/`toBeCloseTo`/`toBeTruthy`/`toBeFalsy`/`toThrow` + `.not`.
- Testable seams: index-from-x resolution (t001's helper — edges, out-of-bounds x, single-point series, empty series), the haptic gating decision (index transition table from t003), and the per-point headline formatting (t004).

## Steps

1. Test index resolution across boundaries and degenerate series.
2. Test the tick-gating function: same index → no fire; null → index → no tick (touch-in is the impact, not a tick); index → index+1 → fire.
3. Test formatted-headline precomputation against the resting formatter for equality.

## Files

- `src/common/d3/__tests__/` (beside any existing chart-math tests)

## Acceptance criteria

- [ ] Tests fail if index math, gating, or formatting regress; `yarn test:unit` passes.

## Out of scope

- Gesture/E2E simulation; haptic feel (device-only, note `w1/023`).
