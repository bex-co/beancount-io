---
id: w1/m26/t003
title: Haptic ticks from `useAnimatedReaction` via `scheduleOnRN`
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m26/t001]
---

## Objective

Re-home the per-data-point `selectionAsync()` tick (currently fired from the JS-thread move handler at ~:120-122) to a `useAnimatedReaction` on the index shared value, bridged with `scheduleOnRN`.

## Context

- `expo-haptics` is an async native module — it cannot run in a worklet.
- Gate strictly on **index change** (prev !== current, both non-null): fired at frame rate it reads as a buzz, not a tick.
- Haptic feel cannot be verified in the simulator — prove the gating structurally and in unit tests (t007); the device feel check rides the standing hardware walkthrough note `w1/023`.

## Steps

1. `useAnimatedReaction` watching the index shared value; on change, `scheduleOnRN` the selection haptic.
2. Keep the touch-in impact haptic from t001 distinct from the per-point tick.

## Files

- `src/common/d3/interactive-line-chart.tsx`

## Acceptance criteria

- [ ] Tick call sites are reachable only on index transitions; no per-frame invocation path exists.
- [ ] No haptic import appears inside a worklet body.

## Out of scope

- Changing which haptic types fire (m21 settled the vocabulary).
