query suggestTransactionCategoriesWithLLM(
  $ledgerId: String!
  $transactions: [TransactionToCategorizeInput!]!
) {
  suggestTransactionCategoriesWithLLM(
    ledgerId: $ledgerId
    transactions: $transactions
  ) {
    targetAccount
    confidence
    source
  }
}
