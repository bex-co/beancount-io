query GetLedger($ledgerId: String!) {
  getLedger(ledgerId: $ledgerId) {
    id
    name
    fullName
    private
    empty
    size
    createdAt
    description
    permissions {
      admin
      pull
      push
    }
  }
}
