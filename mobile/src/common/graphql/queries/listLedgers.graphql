query ListLedgers($limit: Float, $page: Float) {
  listLedgers(limit: $limit, page: $page) {
    id
    name
    fullName
    private
    empty
    size
    createdAt
    description
    permissions {
      admin
      pull
      push
    }
  }
}