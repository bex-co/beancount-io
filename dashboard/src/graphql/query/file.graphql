query GetLedgerFile($ledgerId: String!, $path: String!) {
  getLedgerFile(ledgerId: $ledgerId, path: $path) {
    name
    path
    sha
    size
    type
    encoding
    content
    lastCommitSha
    lastAuthorDate
    lastCommitterDate
  }
}
